# 优化建议区

存放开发者对项目已有功能或者排版样式的优化建议。

---

## 待完成

（暂无待完成条目）

## 归档区（按轮分组，从新到旧）

> **归档规则**（260902 定）：AI 每实施完一轮优化建议，把该轮完成的全部条目作为一组，在归档区**顶部**新增「### 第N轮（YYMMDD）：本轮主题」小节，条目列于小节下（保留原始描述与完成说明）；轮次编号只增不减，新轮次永远插在最上方。

### 第36轮（260911）：格言库并入文笔坊 + 背景素材库

- [X]  把格言库放到文笔坊中——文笔坊中标签页从左往右依次为格言库，写作台，浮生记（260911 完成：brainstorming 四决策定稿——tab 顺序按指定、**文笔坊默认 tab 格言库**（承袭其曾是启动首页的地位）、**App 启动默认落万象库**（左栏 12→11 项新第一项）、tab 状态会话内保活保留（module-hidden 不卸载，重启回默认）。实施——MottosModule 原地面板化（去 module-page/module-header 外壳保留 mottos-page class、props 增 active、刷新双路径 `useModuleActivated('wenbi')` + active 变 true、AI 解读显式传 `channel: 'motto'`——当前模块已变 wenbi 模块级映射不再命中，万象库辩真同款显式覆盖先例）；WenbiModule 扩三 tab（默认格言库、副标题「格言库 · 写作台 · 浮生记」、新增 onOpenAi/bumpAi props 透传）；App.tsx（MODULES 删项、挂载切换、启动默认 'wiki'、CHANNEL_BY_MODULE 删 mottos 条目）；ModuleId 联合删 'mottos'（shared/types.ts + api.d.ts 两处）+ AiSidebar moduleLabel 死条目清理；零改动：格言数据/IPC/DB、回收站格言页签与恢复、定时生成、格言·解读频道、浮生记/写作台面板、文章页层级。CLAUDE.md 左栏顺序/架构段同步。typecheck 双配置 + build 通过。**待冒烟**：左栏 11 项与启动落万象库、文笔坊默认格言库 tab 切换保活、格言全功能回归（来10条/导入/拖拽/标签/搜索/三区流转/笔记）、AI 解读直达频道、定时生成后切回刷新、回收站照常、双主题目检。设计文档与实施计划已入 `左侧边栏/文笔坊/`)
- [X]  个人中心中原来只能上传一张作为项目背景，我希望升级成背景素材库，用户可以上传多张背景图片，然后在这里切换背景图片。并且浅色模式/深色模式背景图片应该独立分开。（260911 完成：brainstorming 四决策定稿——**两个独立素材组**（浅色库/深色库分开管理，上传时选归入哪组）、存储**文件系统为真相源**（`userData/bg/light|dark/` 两目录、`<时间戳>-<随机4位>.<ext>` 唯一文件名、列表=扫目录新在前；零 DB 迁移无行/文件一致性问题，当前使用仍走现有 settings 键 `bg_bg-light|dark` → **ThemeProvider/applyBg/bzres 协议零改动**）、UI**弹窗管理器**（个人档「背景素材库…」按钮开弹窗）、单张上传自动设为当前/多张只入库、删除在用图回退纯色、每组上限 20、旧图一次性幂等迁移（`bg/bg-light.ext` → `bg/light/` 保持原名，settings 已指向零额外改动）。实施——ipc.ts 新 `image:bgList/bgUpload/bgUse/bgDelete` 四通道（bgUpload 对话框多选、超限 throw 拒整批、bgUse/bgDelete 文件名 `^[\w.-]+$` 安全校验；迁移调用置于工具 const 初始化后防 TDZ）+ `image:pick` 收窄为 avatar|reader-bg（bg-light/bg-dark 分支删除，**保留并行会话同日新增的 reader-bg 单槽位**）；preload.ts + api.d.ts 同步（顺带补齐并行会话漏标的 reader-bg）；新组件 `BgLibraryDialog.tsx + css`（浅/深 pill 切换默认随当前主题、16:9 缩略图网格 bzres 预览、在用图主题色描边+「使用中」角标、点图即设当前、hover 删除→二次确认在用图加提示文案、空态引导）；ProfileModule 原两行按钮替换为「管理素材库」入口。typecheck 双配置 + build 通过（**途中两次 tsbuildinfo 缓存重放旧诊断，均清缓存后暴露真实状态**）。**待冒烟**：重启后旧图迁移幂等、两组独立、单张上传即生效/多张入库、21 张拒传、点图即时切换且双主题互不影响、删在用图回退纯色、重启保持、双主题弹窗目检。设计文档与实施计划已入 `左侧边栏/个人档/`）

### 第35轮（260911）：阅读器字体选择 + 划词气泡修复 + 气泡横排

- [X]  丰富一下书架中书的字体选择；阅读时候划词显示的高光和批注两个气泡图在取消划词的时候不能正常消失；而且我希望把这两个气泡组件排版改成横向排列（260911 完成：brainstorming 三决策定稿——**字体书级记忆**（与阅读模式/字号每书独立口径同构，DB v32 `books.font_family`，NULL=跟随全局；设计初稿 v31 被并行会话万象库占用实落 v32）、**清单 12 项**（跟随全局动态名 + 系统 6：雅黑默认/微软雅黑/宋体/楷体/黑体/仿宋 + 打包 5：沿用现有字体文件零新增）、**字体浮层自渲染预览**（ActionMenu 每项用自身字体渲染自己的名字、当前项打勾、复用锚定/防溢出/外点关闭）。气泡残留根因（epub.js 源码级取证）：正文在 iframe 内、事件不冒泡到父文档，收气泡仅挂 host mousedown——正文内点击取消划词永不触发；修复挂 `rendition.on('mousedown')`（epub.js 本就转发 iframe DOM 事件）+ relocated + 滚动容器 scroll 三挂点，划词气泡与标注点击气泡同治。顺带修隐藏 bug：全局选打包手写体时书内早已静默回落楷体（父文档 @font-face 进不了 iframe）——`rendition.on('rendered')` 向每个 iframe 注入 @font-face（`?url` 资产 new URL 绝对化、幂等、翻章重注入）。高光/批注两键包 `.bk-sel-row-center` 横排居中。字体定义收敛共享模块 `src/theme/fonts.ts`（FONT_FAMILIES 自 ProfileModule 迁出共用）；ActionMenu 通用扩展（icon 可选 + 项级 style + max-height 滚动）。typecheck 双配置 + build 通过（5 字体资产正常产出；途中 api.d.ts 第二处 BooksRecord 漏改被 typecheck 拦下补齐、tsbuildinfo 缓存重放旧诊断清缓存解决）。**待冒烟**（重启 dev 触发 v32）：浮层 12 项预览/打勾、霞鹜文楷真渲染（vs 楷体肉眼可辨）、书级记忆重开保留、翻章字体保持、**划词后正文内点击气泡即收（主 bug）**、滚动/翻页收气泡、标注气泡正常开合、高光/批注并排、双主题、个人档字体页照常；font_download 连字若拆字换 text_fields。设计文档与实施计划已入 `左侧边栏/书架/`，详见 dev log 260911）

### 第34轮（260910）：藏书架书签章节名 label + 改名 + 备注

- [X]  藏书架——新增的标签功能只能粗略显示书名，我希望好好优化一下标签功能，添加的标签默认显示当前所在章节，然后也可以用户手动改标签名，或者在标签中写一些备注。（260910 完成：brainstorming 三决策定稿——**秒加不动**（点书签按钮仍一键入库，改名/备注去侧栏后编辑）、默认 label 口径**「章节名 + 位置后缀」**（epub「章节名 · 42%」/ pdf「章节名 · 第 56 页」，同章节多书签可区分；解析不到兜底「约 42%」/「第 56 页」）、编辑形态**侧栏书签行内展开**（否掉小弹窗）。根因修复：v2.0 书签 label 用 `locate.href` 与目录 href 精确字符串匹配取章节名——目录 href 带锚点/路径差异或当前文件未被目录引用即失败，这就是「只能粗略显示」的病根；修法对齐 pdf 区间锚点思路——`ReaderTocItem`/`ReaderLocate` 增 `spineIndex`（EpubReader 目录解析时 href 去锚点经 `book.spine.get()` 解析序号、navigation+spine 双就绪后上报；relocated 随 locate 上报 `loc.start.index`），模块层新 `epubChapterAt`（DFS 序最后一个 spineIndex ≤ 当前的目录项，与 `tocAnchorAt` 同构）。**改名/备注落地**——DB v30 `book_marks` 加 `note` 列；新 IPC `books:markUpdate(markId, {label?, note?})` 部分更新回读整行（books/ipc/preload/api.d.ts 四处）；侧栏书签行 hover 出编辑铅笔 → 行内展开 label 输入 + 备注 textarea + 保存/取消（label 空禁保存、Enter/Esc 快捷、编辑态点行不跳转），备注非空时常驻 label 下方小字（复用笔记批注样式），保存 toast「已保存」/失败「保存失败」；存量书签不迁移（手动改名精修）。typecheck 双配置 + build 通过。**待冒烟**：重启 dev 触发 v30；epub 跨章加书签看章节名跟随变化与同章节两书签可区分；pdf 无目录兜底；行内改名/备注保存后重开书仍在；清空备注小字消失；双主题。设计文档与实施计划已入 `左侧边栏/书架/archive/`，详见 dev log 260910。**第34轮反馈修订（260910）**：冒烟反馈书签章节名与百分比不准——数学女孩读在第 3 章　ω 的华尔兹，label 却是「网络资料 · 100%」。systematic-debugging 双轨取证（epub.js 源码级推理 + 第33轮同款隔离实例 CDP 探针真实点击复现，真实库零污染）定位**双根因**——①**章节名**：该书一册一个大 html、章靠目录锚点（195 个 navPoint 几乎全为 `text00001.html#nav_point_N`），href 剥锚点后同文件条目共享 spineIndex，文件粒度区间判定必取 DFS 序最后一个同文件条目「网络资料」；修复升**锚点级解析**——EpubReader 持拍平目录，relocated 时以 `rendition.getRange(当前 CFI)` 与同文件锚点元素 `comparePoint` 做文档内定位，locate.href 携带命中的锚点 href，模块加书签精确命中优先、文件粒度区间兜底（锚点不可解析回落册名条目），顺带侧栏目录高亮对锚点书生效。②**百分比**：该书 `locations.generate(800)` 完成后零条目（total=-1），epub.js `percentageFromLocation(-1)` = -1/-1 = **1 的假 100%** 经 `l.start.percentage` 带出（两本 epub DB 进度皆 100% 同源，纸面推理由隔离实例插桩数据 `locLen:0, locTotal:-1` 实证）；修复**弃用 start.percentage**，locations 有产出走 `percentageFromCfi`（真实模块上已验证口径正确）、否则 spine 粒度粗估兜底，阅读条与落库进度同口径修正。开发者实测通过（章节名/百分比恢复正确）。**顺带发现两处遗留**（另轮处理，未扩本轮范围）：生产构建（out/ 产物）下 epub 渲染不可用——rendition manager 建不起来（`Rendition.resize` manager undefined），疑 `src/index.html` CSP（default-src 'self'，frame-src 回落）拦 iframe，dev server 不复现、v1.0 起潜伏；ResizeObserver 回调里 `renditionRef.current?.resize` 无 try/catch，挂载竞态下抛未捕获异常。详见 dev log 260910 反馈修订节）

### 第33轮（260910）：收藏夹分类可发现性

- [X]  收藏夹——当前没有分类，请你新增创建分类的功能（大类和子类都要）；而且我希望在收藏夹模块中分类创建出来能在头部有个标签页导航栏，点击可以快速跳转（260910 完成：**排查定位**——所求功能实已随收藏夹模块当天 13:59 落地：头部「分类」按钮即分类管理弹窗（增大类/子类、改名、上下移、删级联全有），建好的大类本身就是头部 tab（万象库同款胶囊）点击即切视图；只读探针实况佐证——运行实例 18:46:59 启动已载新代码、DB 迁至 v29、`fav_items` 有开发者成功添加的 myNoise 一条（模块链路通）、但 `fav_categories` 只有「未分类」种子 = 从未建过分类。**根因是可发现性**：零分类时 tab 行只有孤零零「未分类」，「分类」按钮语义模糊，开发者实操后仍未发现入口。经开发者拍板**小改可发现性**而非重做——①tab 行尾新增「＋」虚线胶囊按钮，点击弹小输入框，输入大类名回车/点「创建」即建并自动切到新 tab（点击外部/Esc 关闭）；②头部按钮「分类」更名「分类管理」；③零分类引导——空态加「创建分类」次按钮（开管理弹窗），有收藏但只有未分类时列表顶显示引导行「点上方＋新建大类，子类与排序在分类管理里整理」（建首个分类后自动消失）。改动仅 `FavoritesModule.tsx` + `favorites.css` 两渲染层文件（dev 热更即生效），后端/IPC/DB 零改动。typecheck 双配置通过；dev server 编译验证通过。**待冒烟**：切到收藏夹看「＋」建大类即成 tab、引导行出现与消失、双主题目检。**第33轮反馈修订（260910）**：冒烟反馈排版错乱 + 只能建大类不能建子类。systematic-debugging 隔离实例取证（临时 userData 放 DB WAL 快照 + `--user-data-dir` + CDP 探针，真实库零污染）定位两根因——**排版**：全局 `.field{width:100%}`（样式表序在后）同特异度反杀模块 `.fav-search{width:210px}`，搜索框 1203px 挤死 tab 行（0 宽不可见）、按钮换行 97px 高溢出视口，病灶自模块首发即存在；修复 `.fav-header .fav-search` 后代选择器提特异度（`.rs-input .field` 同款惯例），几何实测全恢复。**子类**：功能链路无恙（隔离实例建子类成功），病根是唯一入口藏在管理弹窗纯图标按钮里（悬停才知语义）；修复——主视图大类分组区尾新增「＋ 新增子类」行内入口（就地输入即建分组）、管理弹窗按钮加「＋ 子类」文字标签。E2E 全绿（两入口建子类落库、下拉联动、console 零错误），typecheck 通过，详见 dev log 260910 反馈修订节）

### 第32轮（260910）：格言生成提速

- [X]  格言库生成的质量不错，但是速度太慢了！（260910 完成：brainstorming 实测定位——第31轮 llm_usage 记账唯一一条 `motto:generate`：glm-5.3-flash **249.7s**、completion **10,046 tok**（10 条格言 JSON 实际仅 ~700 字符）；三组对照实测（同 prompt 直连智谱）：现状 141.3s / `thinking disabled` **8.3s**（277 tok）/ 显式 enabled 208.2s——根因即 glm 默认深度思考把「102 条避免清单 + 51 墓碑逐条自查 + 文体标准自检」（第 23/24/27 轮质量约束叠加）全写进思维链，6k~10k token 解码占 95% 耗时。定稿**方案 A 场景级关思维链**（配置级开关被否——场景需求差异表达不了；思考预算留作后手），范围只治格言、机制通用。实施主进程两文件三处——`ChatOptions` 增 `thinking?: 'enabled'|'disabled'`（缺省不发参数，现有场景零变化）；`chatWithConfig` 按模型家族映射（仅显式传值且 `/^glm/i` 匹配才加 `body.thinking`，防 OpenAI 严格端点 400 与溢出路由甩给 gpt 时炸请求）；`callAndParse` 标注 `disabled`（生成/补足/解析重试三处共用自动生效，定时生成同入口受益）。质量兜底：句式/口语化/查重三道代码侧闸照常拦截 + 冒烟人工验收；预期单趟 141~250s → ~8s。typecheck 双配置通过。**待冒烟**：来10条 ~10s 返回、AI 使用表 motto:generate 新行秒级/~300 tok、编撰条对照第 27 轮标准无系统性退化、补足轮触发仍秒级、其他场景无变化；若质量退化走后手（改 enabled 或思考预算，一行回退）。设计文档与实施计划已入 `左侧边栏/格言库/archive/`，详见 dev log 260910。**冒烟通过（260910）**：开发者确认质量与速度均达标——llm_usage 实测新行 21.3s / completion 774 tok（实施前 249.7s / 10,046 tok，约 12 倍提速；单行记账 = 10 条全入库未触发补足轮），本轮闭环）

### 第31轮（260910）：推理角出题效率与 LLM 混用/记账

- [X]  当前推理角出题的速度很慢，而且我看不到后台的一个token消耗情况和AI使用情况。但是就目前这个情况，在保证质量的前提下，我觉得还是有必要想一下如何优化推理角的AI生成题目的效率。（260910 完成：brainstorming 三案对比定 **方案 A「咽喉点记账 + 受控并行」+ 追加模型混用**；实测定位——gpt 中转 TTFT 11.7s 为历史慢感主因、glm-5.3-flash 0.7s 且 4 并发全 200、全仓 27 处调用唯一咽喉点 `chatCompletion` 一直丢弃上游 usage。五件套落地——**①模型池溢出路由**（默认满载自动溢出在途最低的有余量备选配置，`LlmConfig.maxConcurrent` 可选并发上限、个人档表单可填、留空不限行为同旧版）；**②llm_usage 记账 DB v27**（每次逻辑调用一行：场景/模型/成败/耗时/token，usage 缺失按字符估算打标；27 处调用点 scene 标注分模块记账）；**③补充泵并行化**（题池/汤库双路 + 题池内两道配对并行、题型难度互异、单道失败不废泵）；**④汤批审题并行**（首轮 3 碗 Promise.all、打回重出保持串行保互避清单，质量口径严格不变）；**⑤可观测 UI**（个人档「AI 使用」区：时间范围 pill + 数字卡 + 分场景表格、AI 空闲自动刷新；左栏底部 LlmActivity 实时活动指示、空闲不渲染）。typecheck 通过（清 tsbuildinfo 后）+ 路由算法 6 断言脚本 ALL PASS；**待冒烟**：重启 dev 触发 v27 迁移，建池提速实测 / glm 上限设 1 验证溢出 gpt / 活动指示起止归零 / 汤批质量回归观察。设计文档与实施计划已入 `左侧边栏/推理角/archive/`，详见 dev log 260910。**第31轮反馈修订（260910）**：活动指示左栏显示不全——根因 `--sidebar-width: 76px` 横排场景文字必截断；三案对比拍板**竖排图标范式**（`progress_activity` 主题色旋转图标 + 「AI ×N」小字，同 nav-item 同构恒定不跳动，逐场景明细走悬停 title），仅动 LlmActivity.tsx 与 App.css 样式块。**第31轮二次修订（260910）**：开发者指令迁**右下角主题按钮上方**、随主题按钮同规则（右栏任一面板展开即隐藏、三收起才显示；左栏底部留给未来新模块）——App.tsx 挂载点迁移 + 容器钉 `--ai-width-collapsed` 宽防撑细条（同第29轮原理），竖排图标/悬停明细/空闲不渲染不变）

### 第30轮（260909）：推理角题库预生成

- [X]  推理角——海龟汤和每日谜题让AI临时生成不太好……大概每次预存10道海龟汤10道谜题，从题库中抽出后，看情况AI自行在后台补充题目（260909 完成：260907 brainstorming 定稿设计（`左侧边栏/推理角/2026-09-07-reasoning-question-bank-design.md`，会话内逐节批准），因 DB 版本号被他模块连续挤占顺延至今日实施。**方案一：持久预生成池 + 主进程补充泵**——新表 `wall_pool`（DB v26，每日一题/练习场共用；含 title 适配列）；新服务 `electron/services/reasoningStock.ts`（汤侧 fresh<5 补到 10、题侧池<5 逐道补到 10——难度取池内最稀缺档/题型取池内+近 2 日最少见/避免清单近 20 题+池内互避；`pumping` 单例防重入、LLM 未配置静默跳过、单批失败 warn 跳出下次再补，永不弹窗）；三触发点 = 启动延迟 10s / 每次消耗后（每日题转正、练习场取题、开汤新局）/ 进入推理角模块（新 IPC `reasoning:stockCheck`）。**取题改道**：每日一题四级放宽选池题同事务转正（零 LLM 秒开，池空才兜底现场两阶段生成）；练习场按难度/题型筛选取池题、同事务 DELETE 池行 + 入 wall_bank todo 态（并轨 260909 上午生成即入库口径，判答照常回写终态）；汤玩法零变化，「来 3 碗汤」退居手动补充。**UI 刻意做少**：loading 文案改「题库见底，现场出题中…」（仅兜底路径出现）、汤库列表随 `reasoning:stockChanged` 渐进刷新、空态文案改「AI 正在后台备汤」。typecheck 双配置通过（清 tsbuildinfo 后验证），待冒烟）

### 第29轮（260909）：主题按钮右栏展开时隐藏

- [X]  目前我们是把主题切换的按键放到右下角了，但是我觉得这样还是不太美观——就是当右侧边栏展开的时候，这个组件也展开了，不美观。但是我目前也还没想到更好的排版方式。请你和我一起商讨一下如何去优化。（260909 完成：经 brainstorming 可视化陪伴四方案对比（回归左栏底部/通条改小方块/收进边栏头部+细条/FAB 悬浮圆钮），开发者定向拍板**第 5 案「右下角保留 + 右栏展开时隐藏」**——debugzi 或草稿本任一面板展开时主题按钮不渲染，双面板收起（右缘细条态）时显示于右下角，展开态通条病根除。实施仅 `App.tsx` 一处：主题按钮包 `rightPanel === null &&` 条件渲染，注释同步；`.right-col-theme` 样式与收起态外观不变，个人档主题设置不受影响。typecheck 通过。开发者指令本轮免写设计文档。**第29轮反馈修订（260909）**：细条态右侧边栏未贴右缘——根因为主题按钮自然宽约 42px 大于细条 36px，撑宽 `.right-col` 致细条右侧留 6.5px 缝（此病 260908 按钮迁来即埋下，收起态成按钮专属态后显眼）。修复：`.right-col-theme` 钉 `width: var(--ai-width-collapsed)` 与细条同宽，右缘 36px 整列贴齐。经 Chrome devtools 注入 window.api 桩复现实测——收起态细条/按钮缝隙 0、左缘对齐，AI/草稿本两展开态 320px 贴右缝隙 0 且按钮正确隐藏）

### 第28轮（260909）：灵感泉完整点子文档 + 信息源收件箱与时间窗口

- [X]  灵感泉生成的prompt还需要再优化（260909 完成：经 brainstorming 摸底——第18轮两阶段管线 + 第22轮规范 v2 后唯一病灶为 **summary 两句模板太薄**（「是什么+第一步」100 字装不下点子本身，卡片与 md 正文均无完整内容载体，看不出值不值得立项），标题/腔调/风向/配额均无恙不动。定稿方案 A「完整点子文档」——自评阶段升级：选中 5 条各产出 body 完整 md 文档（引子段 + ## 这是什么 / ## 核心机制 / ## 最小版本，300~400 字，白话具体禁文艺化压缩、「第一步」并入最小版本小节），md 正文即完整文档；发散 12 条粗点子与口味注入/避免清单/风向标不动；`InspirationIdea` 类型统一（body 可选）、`parseInspirationArray` 容错升级（title 必填 + summary/body 至少其一非空）；降级路径 pickDiverseFive 无 body 退单行 summary；卡片预览改取首段（首个空行前压平 120 字，不再混 ## 标记符）；存量 AI 条不迁移、零 DB/IPC 改动。设计文档 `docs/project/左侧边栏/灵感泉/2026-09-09-灵感泉完整点子文档-design.md`。typecheck 通过，待冒烟看 body 质量与批间多样性）
- [X]  信息源需要好好优化一下——比如说已读之后的归档机制，选择性加载机制（按时间来规定）（260909 完成：经 brainstorming 三项定稿——**收件箱模式**：主列表只显未读、读完即从列表消失，头部「收件箱 | 已归档」切换（recycle-tab 同款）翻已读，「全部标已读」本就不带时间过滤天然清空全部未读（不改动）；**30 天时间窗口**：`listArticles(feedId, view, sinceDays)` 重构返回 `{articles, remaining}`（同条件去窗口 COUNT），底部「加载更早 30 天 · 还剩 N 篇」连续前移、切视图/切源窗口重置 30——顺带治「进模块全量 SELECT + 逐条剥标签」性能病（预览只算窗口内）；**180 天清正文保轻行**：`cleanupOldArticleBodies()` 置空老文章正文 HTML/AI 总结、保留标题/链接/已读轻行，挂 scheduler 启动 + 零点，点开时 extractArticle 懒抓兜底真想看还能抓回；IPC 三件套透传新参；零 DB 迁移。设计文档 `docs/project/左侧边栏/信息源/2026-09-09-信息源收件箱与时间窗口-design.md`。typecheck 通过，待冒烟）

### 第27轮（260908）：思维墙题型换血 + 格言编撰文风修订

- [X]  思维墙的题目还需要进一步升级。当前题目还是过于偏向数学理论知识，我想要的是偏向推理的思维游戏。（260908 完成：经 brainstorming 定稿**题型完全换血**——三个数学题型全弃，新四题型 detective_case 侦探断案 / lateral_puzzle 情境谜题 / word_logic 文字谜题 / life_logic 生活逻辑；难度标尺整体上移（简单=单一洞察+伪装到位、中等=多线索+主动误导、困难=完整推理链+强误导+唯一假设法；脑筋急转弯/模板套皮/数学理论题任何档位不合格）；出题/审题/判答管线 prompt 重写（judgeWallAnswer 签名增 typeZh 题型语境、宽松等价口径按四类改写），两阶段「出题→审题→打回重出一次」机制不动；练习场 TYPE_PREFS 与类型中文名映射换血（保留旧三条映射兼容存量记录显示）；DB v22 插种子 8 题（4 道经开发者校准定稿——侦探「雨夜失窃的怀表」hard/情境「1208 房」medium/文字「祖父的信」medium〔索引起草误 3-6 经开发者人工数数修正为 3-7〕/生活逻辑「额头上的红漆」hard，另 4 道新起草；easy 2/medium 4/hard 2，两道文字题字位脚本核验通过）；**开发者追加指令删旧题**——DB v23 按标题白名单 DELETE 10 道旧数学题并连带删题解 md、`WALL_BANK_SEED` 种子数组整体移除、v13 迁移改只建表不插种子，新老用户路径均收敛为**题库恰 8 道思维游戏题**；每日一题/练习场仍现场两阶段生成，换血后自动全走新 prompt。实施与追加详情见 dev log 260908 两节，design/plan 已入推理角 archive/）
- [X]  格言库AI生成的句子过于口语化，没有格言的味道。需要进一步优化（260908 完成：诊断第 23 轮禁句式后钟摆荡过——禁令全是「不要端着」、正面没定义格言文体，AI 把「平实」执行成大白话、「有画面」执行成散文（经澄清的两种失败模式）；正式区样本确认干净、单点问题。定稿 **A+B 双层**（否掉两阶段管线留作后手）——**A·prompt 精修**：buildPrompt 编撰条段由「平实、具体、有画面」升级为五条文风标准（凝练≤20字删一字则伤/断言非描述非叮嘱/普遍脱离情境成立/可诵禁空洞对仗/画面是载体不是目的）+ 三则正反例（大白话型与散文型反例各标注不合格原因）+ 告知代码侧检测维度引导模型自检规避，7 类句式禁令/墓碑/避免清单段不动；**B·口语化正则兜底**：COLLOQUIAL_PATTERNS 四维（句尾语气词呢吧啊嘛啦呗了〔句中「了解」不误杀〕/口语虚词其实真的确实反正/句首叮嘱式你要你应该你们别再记得赶紧千万/编撰条限长 22 字含标点），正反行为验证先行（一性子脚本 12 命中/8 放行 ALL PASS 后删脚本，正则终稿与脚本逐字一致）；仅约束编撰条、摘录条豁免；命中剔除计 `colloquialRejected` 独立计数（不占第23轮 patternRejected），GenerateMottosResult 八字段经 preload/api.d.ts 三处手工同步透传，toast 追加条件从句「N 条因口语化未入库」，补足轮共用 insertBatch 自动累计；格言库 specs 头部补第 23/24/27 三轮增量注记。实施详情见 dev log 260908）

### 第26轮（260908）：海龟汤计时净用时 + 复盘报告懒生成

- [X]  海龟汤的计时功能要优化一下（260908 完成：经 brainstorming 定「净思考用时」口径——切走模块/最小化/窗口被完全遮挡/关闭 App 一律暂停计时，回来自动继续；失焦但可见仍计时（可能正盯着汤面思考/用草稿本记线索）。**主进程记账**：DB v18 `turtle_games` 加 `active_ms`（累计净用时）+ `segment_start_at`（当前段起点，NULL=暂停中）；新 IPC `turtle:timerStart/timerPause`（渲染层活跃边界翻转时发，对非 playing 局静默幂等）；`openSoup` 恢复对局清残留段=崩溃自愈（异常退出残留开段丢弃不计离线时间）；`before-quit` 兜底结算所有开段；App.tsx 补 `MODULE_DEACTIVATED_EVENT`（moduleRef 捕获旧模块，防 strict-mode 双触发），TurtlePanel「模块激活 && 窗口可见 && playing」合成边界 + 本地段模型显示（payload 新增 activeMs 种子）；终局 `duration_ms = active_ms`，旧记录墙钟口径不追溯、存量 playing 局从 0 起算。设计文档 `docs/project/左侧边栏/推理角/2026-09-08-海龟汤计时与懒生成复盘-design.md`。typecheck 通过，**待冒烟**；含 DB 迁移需重启 dev 触发 v18。⚠ 版本号互备：书架/信息源 spec 原规划 v18/v19 未实施，实施时顺延 v19/v20）
- [X]  海龟汤放弃汤的时候要直接显示汤底，用户点击查看报告的时候再去生成报告。（260908 完成：**破汤弃汤统一**。`finishTurtleGame` 拆分——删点评调用与 md 写盘只落状态/净用时，弃汤确认后**秒出汤底**零 LLM 等待，`md_path` 留 NULL；新 IPC `turtle:report`（ensure 语义）：已有 md 直接返回（旧记录零变化秒开）、无则现场生成点评→拼 md→写盘→回填路径，失败不落半截数据**可重试**、接 beginJob 全局取消；渲染层 `openReport(gameId,…,knownPath?)` 单点改造承接三入口（对局内「查看复盘」按钮/对局记录列表行/终局汤回看），生成中按钮转圈「复盘生成中…」+停止键、行点击 toast 轻提示，记录行 md 为空尾注「复盘待生成」，放弃确认弹窗文案同步「立即揭示汤底；复盘报告（含 AI 卡点点评）点『查看复盘』时生成」；顺带修复终局后 durationMs 未并入致头部用时闪回 0 的存量瑕疵；全局取消机制原接在终局链的 signal 随拆分移至 report 通道。同轮实施与冒烟清单见 dev log 260908 / 计划文档 Task 9）

### 第25轮（260908）：万象知识卡板块标签 + 写作台页面化

- [X]  万象库中的每个知识卡，标题处显示该知识点，除此之外我希望在标题右侧再补充上该知识点属于的板块，比如在“棘轮效应”右侧标上“经济学”（260908 完成：MdDialog 新增 `titleTag?` 可选 prop——标题行改横排 `.dialog-title-line`（标题文字超长省略、悬停看全文，pill `flex-shrink:0` 不被挤掉；可编辑标题模块的 input 改 flex 填充不冲突），标题右侧渲染主题色小胶囊 `.dialog-title-tag`（primary-soft 底 + 主题色描边 + primary-deep 文字、0.75em、圆角 999px，双主题走既有变量零适配）；WikiModule 传 `sections.find(s => s.id === cardEntry.section_id)?.name`，板块页/总览/笔记本跳转/生成审核四入口自动生效，板块查不到时不渲染不报错；其余 7 个使用方不传不渲染行为不变，零后端改动。设计文档 `docs/project/左侧边栏/万象库/archive/2026-09-08-知识卡板块标签-design.md`。typecheck 通过，待冒烟）
- [X]  文笔坊的写作台中的文档排版需要进一步升级优化；同时由于是写文章区域，这是个长期过程，我希望不要以弹窗形式呈现，而是用页面形式呈现。（260908 完成：**全新 ArticlePage 组件**（`src/modules/wenbi/ArticlePage.tsx` + css）替代 MdDialog 弹窗——点看板卡片/新建文章主栏整体切为文章页，「← 看板」返回（flush→刷新列表→清 openDoc），keep-alive 下切浮生记/其他模块往返文章页原样保留；顶部工具栏：返回｜标题行内编辑（回车/失焦 rename，Esc 还原）｜区标签 pill｜编辑⇄预览切换｜Copilot 四键（仅编辑态）｜去空白字数统计实时；**排版**——正文限宽 46em 居中，编辑态 textarea 正文字体 `font: inherit` 跟随个人中心、行高 2.0、透明底主题色光标、占满高度内部滚动（含文章页时 module-page 定高 + :has 撑满主栏，纯看板布局不变），预览态复用 renderMd 行高 1.9、h1/h2/h3 层级间距、段距 1em（scoped `.article-md` 不污染全局）；**自动保存**——停止输入 800ms 落盘 + touch（草稿本先例），flush 兜底四个时机：切预览/返回看板/模块失活/卸载，无「完成」按钮；**Copilot 整套移植**（选区跟踪/起稿·续写·润色·改写/建议卡对照+采纳·放弃·重新生成/采纳改草稿），请求前先 flush 保证主进程取到最新正文，LLM 未配置去配置分流不变。**MdDialog 纯减法删 copilot 扩展位**（prop/状态/工具行/建议卡/App.css 样式块），浮生记与其余使用方不受影响；浮生记、看板、后端、DB 零改动。设计文档 `docs/project/左侧边栏/文笔坊/archive/2026-09-08-写作台页面化-design.md`。typecheck 通过，待冒烟：新建→默认编辑态聚焦→800ms 落盘重进验证→两态切换一致→返回刷新→keep-alive 往返→Copilot 四动作采纳。**第25轮反馈修订（260908）**：编辑⇄预览切换跳回开头——两态滚动容器不同（编辑=textarea 内部滚动、预览=article-body 滚动）换容器即丢位置；改为切换瞬间按比例捕获滚动进度、新态渲染完成后（rAF 等预览 innerHTML 注入）等比恢复，长文下前后文结构差异大处有小幅偏移属预期。同轮被动适配：并行落地的「AI 生成全局取消」给全部 AI IPC 加 jobId 首参，ArticlePage 的 copilot 调用已按新签名接入（随机 jobId + stop_circle 取消键 + 「已取消」轻提示））

### 第24轮（260908）：格言删除记忆墓碑

- [X]  格言库删除的格言，在“来10条格言”可能又会出现已经删除的格言。就是想能不能建立一个暂存机制，就是暂时记住最近的一些已经删除的格言，然后生成10条格言后去和已经删除格言比对，如果重复的话就删除这条重复格言，重新生成新的格言替换。然后这个暂时记住删除格言采用xx机制（260908 完成：经 brainstorming 摸底定位——生成查重双保险（prompt 避免清单 500 条含回收站软删行 + 代码侧 `isDupMotto`）本就覆盖回收站，真正空洞在三条**物理删除**路径（回收站彻底删除 / 3 天自动清理 / 模块内直接删除）行被 DELETE 后查重依据归零。三项定稿——**墓碑挂物理删除钩子**（恢复不写，软删期间本有双保险）、**统一补一轮**（墓碑/库内/句式三种剔除一律按 5/5 配比补缺口，最多两轮交付，失败非致命保留第一轮结果）、**永久留底+注入限量**（表只增不删零维护，代码侧判重查全量，prompt 注入最近 300 条与避免清单互不挤占）。实施——DB v16 新表 `motto_tombstones`（content / content_norm 预计算 / created_at，写入按 norm 去重防重复行）；`recycle.ts` `hardDelete` mottos 分支与 `ipc.ts` `mottos:deleteForever` 删前写墓碑；`generateMottos` 重构为 `buildPrompt`/`callAndParse`/`insertBatch` 三内部件——prompt 追加「已删除格言清单」段、代码侧墓碑先判（归因 `tombstoneRejected`）库内后判（静默维持旧口径）、补足轮本批已入库进避免清单重跑同一套过滤；`GenerateMottosResult` 增 `tombstoneRejected`/`supplemented` 经 preload/api.d.ts 透传，toast 增「其中 M 条为补足生成」「K 条与已删除格言重复已剔除」条件从句；手动新增不判墓碑。实施偏差一笔：headSort 闭包捕获后 null 收窄失效（TS18047）改三元初始化归一，计划文档已同步。设计文档 `docs/project/左侧边栏/格言库/2026-09-07-格言删除记忆墓碑-design.md`。typecheck 通过，**待冒烟**；改动含 electron/db 需重启 dev 触发 v16 迁移）

### 第23轮（260907）：格言生成句式约束

- [X]  格言区由AI生成的格言其实也需要加一些约束，比如说禁止使用“不是...而是”句型（260907 完成：经 brainstorming 三项定稿——**完整禁用清单**（否决单点禁令，防模型「绕开句式、保留腔调」，一轮到位）/ **只约束编撰条**（摘录条是真实名言、句式客观存在如罗丹「不是缺少美，而是缺少发现」，prompt 与正则均豁免——防 AI 为避句式跳过或篡改真名言、防正则误杀）/ **命中剔除不补足**（与既有查重跳过逻辑同构，`inserted < generated` 本是常态，不为小概率不足额加复杂度）。实施——**7 类禁用句式**（不是/并非…而是、与其…不如、真正的…、所谓…不过是、…才算/唯有…才、所有/一切…都全称断言、愿你祝福腔）写入生成 prompt，附正面要求「平实、具体、有画面，靠内容本身立住」并明确标注仅约束编撰条、摘录条如实引用；代码侧 `COMPOSED_BANNED_PATTERNS` 8 条正则兜底（16 例正反行为验证 ALL PASS），入库循环仅对 composed 条校验、命中计 `patternRejected` 后剔除（被剔除条不占查重集合）；计数经 preload/api.d.ts 透传，toast 有剔除时附加「N 条因句式违规未入库」。设计文档 `docs/project/左侧边栏/格言库/archive/2026-09-07-格言生成句式约束-design.md`。typecheck 通过（执行排障一笔：composite 增量缓存重放旧诊断清 tsbuildinfo 解决；并补计划遗漏的渲染层 api.d.ts 类型声明第三处）。观察点：无逗号变体（如「不是A而是B」）暂不命中正则，冒烟若高频出现再放宽分隔符要求）

### 第22轮（260907）：灵感泉 AI 生成 prompt 规范 v2

- [X]  灵感泉中的AI生成点子需要好好prompt规范一下：当前：【title：灵感标题（10~25 字，具体、可执行，不要空泛口号）；summary：一句话简介（≤50 字，说明这是什么、有什么价值）】（260907 完成：第18轮两阶段管线的二轮反馈——实测 9/6 两批钟摆从「CRUD 工具」荡到「文艺概念装置」，开发者勾选三痛点：标题故弄玄虚/summary 谜语化/新型同质化。三项规范——**标题锚点**：10~25 字「画面+实体锚点」结构，前半可文艺、后半必须落到实体（卡片/电台/程序/8张/7分钟），prompt 内置正反例禁纯隐喻；**summary 白话化**：两句模板 ≤100 字（第一句大白话「是什么+值得在哪」，第二句「第一步：」最小动作），禁「先…」开头与文艺化压缩，卡片预览 100→120 字让第一步更易露出；**反同质化**：发散输出携 form 标签（7 形态枚举），自评加批内硬配额「5 条 ≥3 形态、同 form ≤2」+ 第五维「标题可读性」打分，跨批随机风向标（内置 14 基调词池每批抽 1~2 注入，3~5 条靠拢即可、与方向指引冲突指引优先），自评降级路径同步升级为代码侧配额贪心挑选；toast 顺带「本期风向：X · Y」。设计文档 `docs/project/左侧边栏/灵感泉/archive/2026-09-07-灵感泉生成prompt规范v2-design.md`。typecheck 通过，待冒烟看两批生成质量）

### 第21轮（260907）：草稿本——右缘常驻侧边栏（通用/海龟汤双频道）

- [X]  给推理角——海龟汤补充一个草稿本的侧边记录栏，我在玩海龟汤的时候需要记录一些信息，做一个侧边记录栏就方便很多。甚至我觉得可以不仅限于推理角海龟汤，可以把草稿本做成右侧边栏常驻功能，就像现在的debugzi一样，可以展开收起，在右侧边显示。然后也分成不同频道，默认为通用频道，然后还有像玩海龟汤的时候专门有一个海龟汤频道的草稿本这样。然后草稿也要是可以新建，增删改查这些都有的，格式依旧采用md文档的格式。（260907 完成：经 brainstorming 四项定稿——①**右缘双面板互斥展开**：App 持 rightPanel('ai'|'draft'|null)，都收起时右缘细条双图标入口（debugzi/草稿本），点谁展谁永保三栏，debugzi 常驻挂载（草稿本展开时隐藏不卸载，生成中任务不丢）、草稿本按需挂载，展开态持久化；②**频道固定「通用+海龟汤」起步**：DB v14 新表 drafts（channel 为普通字段，将来加频道零迁移），正文 md/drafts/{id}.md 真实 md 文件；③**海龟汤轻联动**：TurtlePanel 进出对局派发事件（App 接住传面板，面板收起也不丢上下文）→ 展开时自动切海龟汤频道；对局中「+」新建草稿自动预填——标题「《汤名》手记」、正文汤面单行引用块 +「## 我的线索」骨架（可改可清），局终不动永存；④**侧栏内编辑 + 大窗逃生口**：正文 MdView 渲染态双击进 textarea 编辑（保存/取消），底部「大窗编辑」经全局 MdDialog 开同一 md；编辑中切换草稿/频道/收起面板自动落袋保存防丢字。面板交互全镜像 debugzi——左缘 7px 拖宽（280–560px 持久化）、频道 pill、草稿切换浮层（单击切换/hover 重命名·删除）、updated_at 倒序浮顶；**删除走回收站**：RecycleSource 第七个来源 drafts，恢复回原频道、彻底删除删行+md、3 天自动清理适用，回收站新增「草稿本」页签（六→七块）。顺手修 AiSidebar 模块映射漏配 reasoning。设计记录 docs/project/右侧边栏/archive/2026-09-07-草稿本侧边栏-design.md；typecheck 通过，待冒烟。**第21轮反馈修订（260907，冒烟通过后）**：编辑态改**自动保存**——停止输入 800ms 静默落库，无需点保存；底部「取消/保存」改为「完成」键（自动保存下「取消」无法回退已落库内容，留着误导；Esc 同效），切换/收起/卸载落袋逻辑保留兜住防抖窗口内最后输入）

### 第20轮（260906）：Toast 轻提示主题化配色

- [X]  现在在格言库中“来10条格言”，在万象库中“来一个知识”，在灵感泉中“来5条灵感”。AI生成后都会有轻提示显示。当前轻提示的样式是普通白色，我希望可以升级成和项目主题相适配的颜色。（260906 完成：全局 `.toast` 样式由白色系（`--color-surface-strong` 底 + `--color-border` 边）升级为主题色药丸——底色 `--color-primary`（浅色樱花粉 #e8a0bf 配黑字、深色宝蓝 #2e5fa3 配白字，对比度分别约 8:1 / 6.4:1）、描边换 `--color-primary-deep` 增加边缘层次；圆角/内边距/阴影/入场动画不动。Toast 为全局唯一轻提示组件，三处 AI 生成提示及复制/删除/回收站等所有 toast 一并生效；无 JS 改动，typecheck 通过，待冒烟看实机观感）

### 第19轮（260906）：格言菜单文案/图标精简 + 灵感文档正文去标题

- [X]  将格言库中的格言的菜单栏中的复制功能描述从“复制格言+出处”改成“复制”即可（260906 完成：ActionMenu 菜单项 label「复制格言+出处」→「复制」，仅改文案，复制行为仍是「格言 —— 出处」格式不变）
- [X]  格言库-正式区中格言的菜单栏中的“查看笔记”的组件有两个图标，冗余了，请你删除一个，保留一个图标即可；（260906 完成：根因非组件渲染了两个图标——菜单项本就只有一个 `<span>`；是图标名 `notebook` 的连字在本地打包字体 material-symbols 0.47 中不存在，文字整形时拆成 `note`+`book` 两个有效子连字，渲染成两个图标并排（经字体实测页量宽确认：notebook 61px、正常单图标 37px）。换成本字体中单图标的 `description`（文档样式，贴合“查看笔记”），渲染为单图标）
- [X]  目前灵感泉中的笔记区分为标题和正文两栏，但是标题在标题栏和正文栏同时出现，出现两次显得冗余。请你约束一下，正文栏中不要出现标题。（260906 完成：与第10轮格言笔记同款处理——①新建路径两处不再写 `# 标题` 行：手动新建正文从空白开始、AI 生成直接从简介开始；②**DB v11** 存量迁移剥离首行旧模板标题（新 `stripInspirationTitleHeader`，仅与现库标题精确匹配才剥，防误删用户自写 # 开头正文；不过滤 deleted_at，回收站软删条恢复后不重现旧头——实测现存 21 条全部精确匹配）；③三处「跳过首行标题」的补偿正则一并移除（正文预览取前 100 字 / 问 AI 预填取前 300 字 / 主进程 inspirationBody 给 AI 完善供正文），正文即全文）

### 第18轮（260906）：灵感泉 AI 生成质量优化——口味注入 + 两阶段生成管线

- [X]  当前灵感泉中通过AI生成的点子质量不行，不是我想要的。需要好好prompt规范一下（260906 完成：经 brainstorming 定位四宗罪——千篇一律/平庸不惊艳/领域不对味/空泛不落地，根因有二：①「兴趣画像」取自已有灵感，而库里全是 AI 自生成条 → AI 吃自己输出越生成越同质（且用户画像为空、万象库里的经济学/心理学等兴趣信号全没用上，AI 对用户品味一无所知）；②单趟生成无质量标准无筛选。三项改造——**口味注入层**：新 inspirationTasteProfile() 跨模块聚合「用户是谁」（画像摘要 + 格言正式区≤30条领会气质 + 万象词条按板块聚合求知领域 + 手写灵感标题），AI 生成条踢出画像只进避免清单，打破自我循环；**两阶段管线**：generateInspirations() 重构为发散（temp 0.95 出 12 条粗点子，硬性要求覆盖≥4种项目形态、每条带第一步抓手、明令禁用「XX记录/追踪/打卡+工具/助手」拼装句式、至少一半与口味呼应）→ 自评（temp 0.4 四维评审新颖/契合/具体/兴奋，淘汰同质与平庸，恰好挑 5 条并打磨简介≤60字含第一步抓手），自评两趟均失败回退取发散前 5 条不空手而归；**手动指引**：灵感泉头部「来5条灵感」左侧新增 tune 图标按钮 →「灵感方向指引」弹窗两个 textarea（感兴趣的方向/不想要的方向），JSON 落 settings.inspiration_guide（新 SettingsKey），生成时以「最高优先级」置顶注入，留空不注入。角色设定（挑剔的创意策展人）+ 正反质量标准写进 prompt；解析容错/标题查重/入库位置/origin='ai' 流转与失败 toast 路径均不动。代价：两趟调用，「来5条灵感」等待时间约翻倍。typecheck 通过，待冒烟看实际生成质量）

### 第17轮（260906）：AI 输入框提示清理与高度自适应 + ActionMenu 整行可点

- [X]  右侧边栏的AI伴学助手对话输入框中的预览内容包含“/clear 清空会话，/compact压缩上下文”，我觉得没有必要，请你删去预览内容中的这句话。另外就是对话输入框的排版，输入框一次仅能显示两行文字的内容，用户很难直接看到自己输入的完整内容。请你优化AI对话输入框的排版，解决这个问题。（260906 完成：①placeholder 提示删去——致知己追问栏输入框同款提示一并清理（第14轮起两处统一支持斜杠命令，保持一致），`/clear` `/compact` 命令功能保留仅去文案；②输入框改**高度自适应**——基准仍两行，随输入内容长高看全文，超过视口 30% 后内部滚动，发送清空自动回落；重算时机覆盖输入变化/边栏与追问栏拖宽/窗口尺寸变化；追问栏输入框同款处理）
- [X]  格言库中的**ActionMenu** 组件点击范围只有文字组件部分可以点击，我希望是把组件可点击响应范围改成整行，包括空白处，而不是只有文字部分。（260906 完成：根因是菜单项 `<button>` 默认随内容收缩、不满菜单行宽——`.action-menu-item` 加 `width:100%` 拉满行宽，整行含空白处均可点击，hover 高亮同步满行；通用组件改动，当前唯一使用方格言库直接生效）

### 第16轮（260906）：AI 助手起名 debugzi

- [X]  给我的AI起名叫做debugzi（260906 完成：经 brainstorming 敲定**身份全面落地**口径——`src/shared/types.ts` 新增 `AI_NAME = 'debugzi'` 常量（主进程 prompt 与渲染层共用，将来改名只动一处）；①边栏五处：标题「AI 助手」→「debugzi」、收起态 tooltip「展开 debugzi」、消息角色标签 assistant: 'AI'→debugzi（含思考中占位，占位原为硬编码单独改）、输入框 placeholder「问 AI（…）」→「问 debugzi（…）」（仅动前缀，/clear 文案属另一条待办）、画像建议提示「debugzi 想把这条加入『我的画像』」；②主进程 system prompt 身份声明改为「你是 bugzi 的个人工作台『bug子的workspace』的 AI 助手，名叫 debugzi。对话中提到自己时自称 debugzi」——五频道人设不重复写名，身份行+频道行拼合自然形成「debugzi 在此频道扮演该角色」；③欢迎引导第 3 步彩蛋：「建议先去个人中心配置 LLM，让 debugzi 上线陪你开工」。功能描述类「AI」字样（问 AI / AI 解读 / 让 AI 追问 / AI 编撰徽章 / 以启用 AI 功能）按约定保留不动。无 DB 改动，typecheck 通过，待冒烟验证 AI 自称。**第16轮续（260906）**：AI 编撰条出处署名同步 debugzi——生成 prompt 编撰条 source 标「debugzi」；mottoKind 容错正则扩为 `/(?:ai\s*编撰|debugzi)/i`（新署名可识别、旧格式兼容）；**DB v10** 存量迁移 `UPDATE mottos SET source='debugzi' WHERE origin='ai' AND source LIKE '%AI%编撰%'`（含回收站软删行、限定 origin='ai' 防误伤手动条、LIKE 口径同 v6 回填）；编辑表单 placeholder「出处（书名/作者/debugzi）」+ 三处注释同步；行尾「AI」徽章按上轮约定保留（生成方式标识，非署名））

### 第15轮（260906）：格言功能气泡菜单 + 格言·解读频道

- [X]  格言库新增一个解读的功能——就是点击以后会自动把当前句子输入到AI助手——格言·解读的频道（当前还没有，需要你新创建这个频道）。然后就是当前的每条格言后面都有功能图标标签——像“加入沉淀区”“标签”“复制”“删除”等，现在又要新加一个解读，感觉这些功能图标有些占空间了。我认为可以这样优化：把这些功能图标改成以气泡浮窗形式呈现——就是点击两下是进入编辑（仅限正式区格言），那么我们就设计点击一下是召唤功能浮窗（或者以菜单形式呈现也行），然后再点击需要的功能。然后右边的空间空出来了，就放句子的出处——就不用双行了，单行呈现即可，不过出处的样式可以保留——小字体+浅色，和格言本身做一个区分。（260906 完成：新建通用 **ActionMenu** 组件——fixed 锚定行尾弹出、视口防溢出（上下翻转/右溢左移）、createPortal 不被裁剪、外部点击/滚动/Esc/resize 关闭；行布局单行化——正文占左自动换行、出处右侧小字浅色（无出处显「（出处待补）」）、标签行尾小字（首个 + +N 计数悬停看全部）、AI 徽章移序号旁、功能图标排整体撤销；单击行/⋯ 按钮经 260ms 防抖召唤菜单（防抖给双击让路），双击行打开笔记弹窗（正式区且有笔记——经 brainstorming 确认由原设想的「进编辑」改为开笔记，草稿/沉淀区无操作，菜单「查看笔记」兜底），拖拽排序不受影响；菜单项按区显示（AI 解读/编辑/标签/复制/区流转或查看笔记 ∥ 丢弃分隔线，丢弃二次确认不变）；新增 motto 频道零 DB 迁移——AiChannel/CHANNELS/ACTIVE_SESSION_KEYS/CHANNEL_PERSONAS 四处同步扩展，格言解读员人格「字面义→背景出处→引申与适用，150~300 字，不掉书袋、不灌鸡汤」，格言库模块映射新频道，「AI 解读」经 openAiWith(text,{auto:true}) 复用 pending 机制切频道自动发送+乐观上屏，AiSidebar 发送链路零改动，三区均可解读，LLM 未配置走边栏「去配置」现有路径。轮次说明：设计文档原标注第14轮，因同日追问栏优化先归档为第14轮，本轮按「只增不减」顺延为第15轮。**第15轮反馈修订（260906）**：①「AI」徽标撤销独立占位，并入行尾标签组——与用户标签同款样式同位置，显示「AI、首个用户标签 +N」（摘录条不打 AI，悬停看全部）；②标签组与出处对调，标签在出处**左侧**）

### 第14轮（260905）：致知己追问栏——收起/拖宽/多会话 + /clear /compact

- [X]  追问栏可展开/收起；可手动调节宽度（弹窗总宽不变，正文区伸缩）；多会话创建与切换；/clear 与 /compact（260905 完成：①头部「›」收起为右侧 32px 竖条、「‹」展开，收起态与宽度持久化，追问时自动展开；②栏左缘 7px 拖条调宽 240–560px，实时生效松手落库；③头部会话切换器——当前频道会话列表点击切换（与全局边栏同一激活会话）+「+」新会话 + 会话项 hover 重命名/删除（行内改名 Enter 确认，删除二次确认连消息彻底删，删激活会话时同频道自动切换），会话历史即 AI 上下文（带最近 30 条），关联问题共用、不相关问题开新会话由用户掌握；④`/clear` **清空当前会话全部消息**（会话与标题保留，上下文与存储一并清零，防历史无限膨胀；新会话走「+」手动开启——开发者反馈修订，原实现为开新会话）、`/compact` 由主进程把当前会话历史 LLM 压成 ≤500 字「前情摘要」另存新会话（此后上下文=摘要+新消息，原会话原样保留），追问栏与全局边栏输入统一支持。注：全局右侧边栏的展开/收起与拖宽为 v1/第7轮既有能力，本轮三项均指弹窗内嵌追问栏）

### 第13轮（260905）：致知己 AI 初始化 v0 + 弹窗内嵌追问栏 + 画像记忆化

- [X]  致知己区，在填写问题的时候，除了填写问题和标签，需要你再新增一个可勾选项：AI初始化答案——勾选的话即由AI思考该问题并给出初始答案，并且这个版本标记为v0版本。之后我再此基础上写自己的答案这些记成v1,v2...版本（260905 完成：新问题弹窗加「AI 初始化答案（生成 v0 参考答案，我再写自己的 v1）」勾选——勾选后 LLM 先就问题生成 ≤600 字结构化参考答案（含画像摘要），以 v0（seq=0）版本入库，正文顶部注明「AI 初始化的参考答案，请在此基础上写出属于你自己的 v1」；弹窗以渲染态打开供阅读，双击在 v0 上动笔、保存即 v1（序号自然接续）。生成期间按钮显示「AI 思考中…」；LLM 未配置弹「去配置」；生成失败不创建问题、表单保留可重试，不落半截数据。不勾选时行为不变：空白 v1 直接进编辑态）
- [X]  致知己区这里和AI交互是在侧边栏实现的，我不得不从弹窗（答案编辑区）移出来，去查看侧边栏的信息，然后又回到答案编辑区。这样子交互很不方便，需要你进一步优化交互的形式，比方说直接在弹窗下方或者弹窗右侧加一个侧边栏，可以直接在这里交互；或者不要以弹窗的形式呈现答案编辑区，点击某一问题直接进入到一个新页面，这样也可以。（260905 完成：采用「弹窗右侧内嵌追问栏」方案（你给的第一种方向，保持 md 弹窗统一交互不破坏）——MdDialog 新增 sidePanel 扩展位，致知己弹窗加宽为 min(1080px, 95vw) 双栏：左侧照旧看答案/双击编辑/版本切换，右侧 320px 追问栏 = 致知己·追问频道的会话视图（与全局边栏同频道同数据，两处看到同一会话）——消息列表 + 输入框 + 发送 + 思考中占位 + 乐观上屏；「让 AI 追问」按钮直发本栏，追问、看 AI 提问、回应、回正文改答案全程不出弹窗。其他模块不传 sidePanel，弹窗布局不变）
- [X]  个人中心——我的画像……反对直接把用户画像注入到上下文中去，最好是以记忆化的形式"记住"我的画像，需要时才查看获取。（260905 完成：画像改**记忆化**——①边栏对话：system prompt 只带画像索引（每条 = 类别 + 24 字摘要），AI 判断需要时输出 `<<<PROFILE_LOOKUP:关键词>>>` 检索标记，主进程匹配画像（类别/内容含关键词）注入完整条目后再答一轮（最多补一轮），无需了解则直接作答——注意力不被无关画像干扰；②生成类功能（格言/万象/测一测/灵感生成与完善）：注入压缩摘要（每条截断 60 字）——单趟生成无对话轮可检索，摘要兼顾「懂我」与上下文长度，你说的长条目也不会撑爆 prompt；③辩真验证不注入画像（观点核查与画像无关）。个人中心画像区文案同步改为记忆化表述；sqlite-vec 检索式注入仍留作升级路径）

### 第12轮（260905）：灵感泉看板排版——加宽卡片看全灵感内容

- [X]  灵感泉中的灵感显示不全——主要是当前留给一条灵感的显示空间很小，放不下几个字，用户不容易看出来灵感的内容。所以排版这块需要想办法优化一下，最好是能让用户看全灵感的内容。（260905 完成：①看板 4 列改 2×2（≤1100px 窄屏退化单列），卡片内容区宽度翻倍至约 580px——空间小是四列均分所致，这是主解；②卡片标题由单行省略改为自动换行、最多 3 行（超长截断、悬停 title 兜底），AI 徽标移入日期行不再挤标题；③标题下新增正文预览——列表加载时批量读 md，剥离标题行取前 100 字灰色小字显示（最多 3 行），手动条正文为空不显示；④操作按钮改随标题顶对齐。更长的完整内容仍点卡片开 md 弹窗看）

### 第11轮（260905）：AI 编撰格言可编辑 + AI 边栏消息即时上屏

- [X]  格言库中AI编撰的句子，用户可以编辑这个句子。（仅限于AI编写的句子，摘录自现实的句子不需要）（260905 完成：编辑按钮原仅正式区显示，而 AI 编撰条生成后落在草稿区无法编辑 → 改为「正式区全量 + 草稿/沉淀区中的 AI 编撰条」可编辑，判定与 AI 徽章一致（origin=ai 且非摘录）；摘录条/手动录入条在草稿、沉淀区仍不放开，正式区维持原有全量可编辑不变。`mottos:update` IPC 本就与分区无关，无后端改动）
- [X]  和AI交互的时候，用户发送的信息在AI思考中的时候不能显示出来看到，要到AI完成对话才能看到用户的输入和AI的输出。我希望在用户发送信息后，聊天框就能看到用户发送的信息，哪怕是AI思考的时候也要能看到。（260905 完成：`send()` 改乐观渲染——发出即把用户消息以负 id 临时上屏（负 id 不带删除键），下方照旧显示「思考中…」，滚动随消息更新自动到底；AI 完成后 `loadMessages` 用 DB 真实记录（正 id、可删除）替换临时行。请求失败也保留该消息——主进程 `aiChat` 本就先落库用户消息再调 LLM，显示与库一致；LLM 未配置仍走「去配置」弹窗）

### 第10轮（260905）：格言笔记弹窗标题区补出处、正文去重

- [X]  格言库的正式区中句子的md笔记分成了两个区域：标题区和正文区，中间用横线隔开了。我看到正文区中也默认包含了句子+出处的信息。但是在上面的标题区已经有句子信息了，我觉得就没有必要再去重复了。请你在打开笔记显示弹窗中的标题信息中补充出处，并删除正文区中的句子+出处信息。（260905 完成：MdDialog 新增可选 subtitle 副标题行，格言笔记弹窗标题=句子、标题下小字「—— 出处」（与列表行同款格式，无出处显示「—— （出处待补）」）；新建笔记正文从空白开始（create/setStatus 两处模板同步改，不再写入 `# 句子\n\n> 出处` 头）；DB v8 迁移一次性剥离存量笔记旧模板头——含回收站中软删格言的笔记，头部以下自写内容保留、已自行改写头部的不动——转正时对仍带旧头的笔记兜底剥离，覆盖回收站恢复后再转正的路径）

### 第9轮（260903）：万象库测一测 + 生成审核三选 + 直接删除按钮位置调整

- [X]  （源自万象库升级探讨）在万象库中再加一个测一测的功能——基于当前万象库的知识出选择题来考察用户对知识掌握程度——并且要升级一下对万象库质量的把握——第一遍生成的时候以弹窗形式呈现，用户读完选择加入，丢弃，或者直接删除。（260903 完成：**测一测**——每次随机抽 5 张卡片，一次 LLM 调用批量出 5 道四选一（`wiki:quiz`，容错解析、干扰项要求有迷惑性），总览页「测一测」按钮进入独立答题页，逐题作答立即标对错+亮正确答案，结束有得分总结与「再来一轮」；**生成审核**——三条生成路径（随机/板块/手动）完成后卡片以审核模式打开，底部三选：加入=保留（关弹窗/Esc 同效）、丢弃=进回收站、直接删除=彻底删（含高光，新 IPC `wiki:deleteForeverEntry`，后两者均二次确认）；出题频次与知识卡质量优化列入待完成）
- [X]  （第8轮反馈）把「直接删除」按钮从弹窗左下改到「丢弃」的右侧，并且颜色比丢弃深一些。（260903 完成：ConfirmDialog extraAction 改渲染在确认键右侧；新增 `--color-danger-deep`（浅 #8c3a5e / 深 #d34a6d）+ `.btn-danger-deep`，格言与万象两处「直接删除」均用深色）

### 第8轮（260903）：格言直接删除 / 板块菜单锚定 / 指定板块随机生成

- [X]  万象库中的板块右侧的...管理需要优化（具体如何优化先进行商讨）。（260903 完成：①根因菜单写死 top:60/left:300 不跟随按钮 → 改为锚定触发按钮弹出（fixed 定位取按钮 rect，右对齐、防溢出），灵感泉「移动到」菜单同 class 一并修复；②顺带发现并修复存量 bug——板块改名弹窗渲染条件引用已清空的菜单状态导致永不显示，改用独立 renameTarget；③菜单项带图标并新增「本板块随机来一条」）
- [X]  在万象库中也需要能够指定某一板块，然后随机生成一个知识卡片这样的功能。（260903 完成：后端抽 suggestWikiTerm（指定板块用指定、未指定随机挑，规避全部已有词条），generateWikiCard 随机模式尊重传入板块；入口三处——板块详情页头部骰子按钮、「...」菜单「本板块随机来一条」、手动弹窗词条框旁骰子（新 IPC wiki:suggestTerm 只构思词条名，可改后再生成））

### 第7轮（260902）：AI 边栏体验五项优化

- [X]  给右侧边栏添加一个可以左右拖拽调整窗口大小的功能。（260902 完成：边栏左缘 7px 拖拽条，实时调整 --ai-width，范围 280–560px；宽度持久化 settings.ai_width，重启自动恢复；收起态不显示）
- [X]  优化一下右侧边栏中的显示排版（直接显示渲染好的文字信息）。（260902 完成：抽共享 MdView 组件（marked+DOMPurify，与 md 弹窗同一实现、外链走系统浏览器），AI 回复渲染 md（标题/列表/代码块/表格等）；用户消息与系统消息保持纯文本）
- [X]  我还需要可以自定义命名和AI助手的会话名称，而不是只显示“历史会话”。（260902 完成：根因是「双击改名」不可达——单击切换会话即收起浮层，双击事件无法触发；改为会话项 hover 铅笔按钮，点击进入行内编辑，Enter 确认 / Esc 取消，任意会话（含「历史对话」）均可改名）
- [X]  在和AI会话中删除某次会话中的一次对话后，对话窗口会自动返回最下方，我希望还是保留在点击删除时候的位置。（260902 完成：删除前记录 scrollTop，消息重载后恢复原位置；发送新消息等场景仍保持自动滚底）
- [X]  将当前项目的app图标从默认图标换成D:\Code\myapp\bugzi_workspace\resources\app.png这张图片。（260902 完成：原 main.ts 引用的 renderer/assets/icon.png 并不存在，一直落到 Electron 默认图标；BrowserWindow 图标改指 resources/app.png（541×537），打包态预留 process.resourcesPath/app.png 路径，将来接 electron-builder 时配 extraResources 即可）

### 第6轮（260902）：AI 助手多会话对话记录管理

- [X]  侧边栏的AI助手的对话记录管理（开启新对话，删除对话记录等等）。（260902 完成：DB v4 新增 ai_sessions 表、ai_messages 挂 session_id，存量记录归入「历史对话」会话；支持多会话切换、新对话、会话删除+单条消息删除（均二次确认）、首条消息自动命名+双击改名；会话列表为头部按钮+浮层；辩真阁系统消息推送进当前激活会话）

### 第5轮（260902）：六条待办清零（回收站时间 / 万象库占位词条 / 字体三项 / 格言导航栏）

- [X]  回收站时间显示改为「放入回收站时间｜彻底删除时间」，精确到分钟（如 2026/9/2 13:51 ｜ 2026/9/5 13:51）。（260902 完成：删除时间 = 入站时间 + 3 天，与自动清理口径一致；鼠标悬停有字段说明）
- [X]  万象库手动输入生成的预留词条名跟随所选板块。（260902 完成：五个初始板块各配代表词——经济学/通货膨胀、法学/无罪推定、心理学/认知失调、博弈论/囚徒困境、历史神话/特洛伊战争，自定义板块回退通用「词条名」，placeholder 随下拉实时联动）
- [X]  字体设置删除宋体/黑体/等线，内置 5 款手写/楷体字体：漓雨手书、鸿雷板书简体、霞鹜文楷、玄宗体、演示悠然小楷。（260902 完成：字体文件随应用打包（src/renderer/assets/fonts/，约 110MB），无需用户安装即用；保留系统楷体；存量旧字体值自动回退默认）
- [X]  字体粗细调节再细致一些。（260902 完成：改为滑块，100-900 每 100 一档共 9 档，实时预览）
- [X]  字体大小默认 16px，范围扩大到 10-24px。（260902 完成）
- [X]  格言库添加快速导航栏，点击快速在草稿区/沉淀区/正式区跳转。（260902 完成：三区 pill 按钮带计数；反馈迭代——跳转后导航滚出视野 → 改为 sticky 吸顶 + 磨砂底，跳转定位补偿吸顶高度）

### 第4轮（260902）：格言复制 / 序号拖拽排序 / 划词主题色

- [X]  给格言区的格言句条目右侧，编辑按键左侧添加复制按键，点击即可复制”格言+出处“的信息。（260902 完成：三区通用复制键在删除键左侧，格式「格言 —— 出处」，无出处只复制正文，走主进程 clipboard IPC）
- [X]  在格言区的格言条目左侧添加序号；添加一个可拖拽移动格言顺序的功能。（260902 完成：DB v3 加 sort 列，每区独立编号 1..n，区内 HTML5 拖拽排序；新增/生成/恢复/流转均插到目标区开头）
- [X]  我希望样式上可以优化一下，就是划词的时候默认是系统的蓝色提示，我希望划词的颜色提示和项目的主题色保持一致。（260902 完成：::selection 主题化，浅色樱花粉半透明、深色参考图点缀蓝半透明）

### 第3轮（260902）：项目根目录文件整理

- [X]  当前项目代码根目录下有很多单独文件，你能不能帮我整理一下它们？优化一下项目代码文件的结构。（260902 已整理：背景图移入 `resources/`，配色参考图归档至 `docs/project/样式/`，删除调试截图，`.gitignore` 忽略 `*.tsbuildinfo`，详见 docs/log/260902.md）

### 第2轮（260902）：格言单行显示 + 数据存储迁移与数据管理

- [X]  格言区中的出处是放在第二行显示——我希望改成格言+出处就一行显示，出处放在右侧显示即可
- [X]  当前项目的数据存储在C:\Users\Administrator\AppData\Roaming\bugzi_workspace，我希望你把默认存储地址改成D:\Paper\app_output\bugzi_workspace。。并且在个人中心中添加一个数据存储的管理，用户可以在这里查看当前数据存储位置，并且也可以点击修改存储位置，修改逻辑为把当前数据从就路径迁移到新路径下。现在需要你帮我完成数据迁移，并清理原先存放在C盘的数据，然后在个人中心添加数据管理功能。

### 第1轮（260902）：LLM 上游模型列表 + 深色主题配色

- [X]  个人中心——LLM配置中，缺少一个从上游获取模型列表，然后用户可以选择使用的模型。
- [X]  当前项目深色模式下的蓝色主题色看着不舒服，请你参考 `docs/project/样式/深色模式颜色参考.png` 这张图片（仅供作为配色参考，不管界面图形）对深色模式的配色进行优化。（参考图已随本次根目录整理归档至 `docs/project/样式/`）
