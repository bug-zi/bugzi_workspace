# AI 生成全局取消 实施计划

> **状态：已实施（260908 18:42，Task 1-19 全部完成）。** typecheck 双配置 + build 通过；开发日志见 `docs/log/260908.md` 同日小节。唯一偏差：Task 16 的 MdDialog/WritingPanel copilot 扩展位已随写作台页面化（ArticlePage）删除，copilot 取消由 ArticlePage 既有适配满足（本会话验证签名吻合），其余步骤照计划落地。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans（本会话内联执行）实施本计划，逐 Task 推进。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 全项目所有 AI 生成均可手动取消——渲染层生成 jobId 随调用传入 → 主进程注册表挂 AbortController → signal 穿透 services 到 fetch → `ai:cancel(jobId)` 随时掐断；取消即作废（未写库的不入库、已写库的保留），无确认弹窗，取消后立即可重试。

**Architecture:** 方案 A（通用 jobId + 统一取消通道）。新增 `electron/ai/jobs.ts` 注册表；`llm.ts` 的 `chatCompletion` 把已声明的 `ChatOptions.signal` 真正接到 fetch（含 429 退避 race）；23 个可取消 IPC handler 统一「jobId 首参 + beginJob/endJob」模式；渲染层 loading 态从布尔升级为持 jobId，旁挂「取消」按钮，异常 message 含「已取消」走轻提示不走错误弹窗。

**Tech Stack:** Electron 44 主进程（node:fetch 原生支持 AbortSignal）+ React 19 渲染层（crypto.randomUUID）。

**设计依据:** `docs/project/全局/2026-09-08-AI生成全局取消-design.md`（方案 A + 两节设计逐节通过）。

---

## 执行须知（项目硬性规则，覆盖技能默认流程）

1. **禁止任何 git 操作**（CLAUDE.md 开发者指令）：本计划不含 commit 步骤，完成后由开发者自行提交。
2. **本项目无测试设施**（package.json 仅 dev/build/typecheck/smoke）：以 `npm run typecheck` 为验证关卡替代 TDD；`npm run build` 与人工冒烟收尾。
3. **分侧 typecheck 策略**：`npx tsc -p tsconfig.node.json --noEmit`（electron/ + src/shared/）与 `npx tsc -p tsconfig.web.json --noEmit`（src/ 渲染层）独立可用。主进程侧改完先过 node 侧检查；渲染层组件（Task 7-18）全部完成后才过 web 侧检查（preload/api.d.ts 签名变更会使未改造组件临时报错，属预期中间态）。
4. 异常识别口径统一：取消归一为 `new Error('已取消')`，渲染层一律 `msg.includes('已取消')` 判断，排在 `LLM_NOT_CONFIGURED` 等分支之前或独立分支均可，但必须存在。
5. 图标统一 `stop_circle`（https://fonts.google.com/icons 已含，material-symbols 0.47 本地字体有此字形）；按钮用既有 `btn` / `btn btn-ghost` 类，禁彩亮色。

## 逐 handler 核对结论（相对设计文档的偏差记录）

设计文档列 18 个 handler，实施数 **23 个**。偏差来源 = 设计预留的「实施时逐 handler 核对，发现实际含 LLM 则补接」：

| 类别 | handler | 说明 |
|---|---|---|
| 设计已列 | mottos:generate / wiki:generate / wiki:suggestTerm / wiki:quiz / inspirations:generate / inspirations:refine / verify:run / turtle:generate / turtle:ask / turtle:guess / wall:ensureToday / wall:practiceNew / wall:practiceAnswer / wall:bankAnswer / zhijiji:createQuestion / wenbi:copilot / ai:chat / llm:test / mcp:test / mcp:research | 共 20 个（设计计数 18 为算术口径差） |
| **补接** | `wall:answer` | 每日题作答含 judgeWallAnswer LLM；设计取消语义表「已写详情 md 的终态操作（作答）取消仅在判答 LLM 段生效」已覆盖 |
| **补接** | `turtle:abandon` | finishTurtleGame → soupReview LLM（终局点评）；取消只影响点评段（取消 → 点评降级固定文案、终局照常完成，见 Task 4 说明） |
| **补接** | `aiSession:compact` | compactAiSession 含 chatCompletion；AiSidebar 与致知己追问栏 /compact 均有「思考中」等待态 |

**核对后确认不接**：`turtle:openSoup` / `wall:practiceHint` / `wall:bankReveal`（无 LLM，与设计一致）；`llm:models`（快速 GET 非生成，设计未列）；`backfillTrickNotes`（启动期后台回填，无 UI 等待态）；定时格言任务（scheduler，无 UI 等待态）——因此 **services 各函数 signal 形参一律可选**，无 signal 调用点（scheduler/backfill）零改动。

**组件改造实数 13 个文件**（设计写 10：WallPanel 条目实际拆 WallPanel/PracticePanel/BankPanel 三文件，致知己追问栏 ZhijijiChat 内嵌于 ZhijijiModule.tsx）：MottosModule / WikiModule / InspirationsModule / VerifyModule / TurtlePanel / WallPanel / PracticePanel / BankPanel / ZhijijiModule（含 ZhijijiChat）/ MdDialog + WritingPanel / AiSidebar / ProfileModule。

---

## Task 1: 机制层——jobs.ts 注册表 + llm.ts signal 真接线

**Files:**
- Create: `electron/ai/jobs.ts`
- Modify: `electron/ai/llm.ts`

- [x] **Step 1.1 新建 electron/ai/jobs.ts**（全文如下）

```ts
// AI 任务注册表（主进程）：渲染层 jobId → AbortController；统一取消通道 ai:cancel 的后端
// （260908 全局取消，docs/project/全局/2026-09-08-AI生成全局取消-design.md）
const jobs = new Map<string, AbortController>()

export function beginJob(jobId: string): AbortController {
  const ac = new AbortController()
  jobs.set(jobId, ac)
  return ac
}

export function endJob(jobId: string): void {
  jobs.delete(jobId)
}

/** 取消任务；false = 任务已结束/不存在（重复点无害静默） */
export function cancelJob(jobId: string): boolean {
  const ac = jobs.get(jobId)
  if (!ac) return false
  ac.abort()
  jobs.delete(jobId)
  return true
}

/** 阶段间取消检查：已取消即抛「已取消」（渲染层据此走轻提示，不弹错误框） */
export function ensureNotCancelled(signal?: AbortSignal): void {
  if (signal?.aborted) throw new Error('已取消')
}
```

- [x] **Step 1.2 llm.ts：chatCompletion 接线（请求前检查 / fetch 挂 signal / 429 退避与 abort race / 异常归一）**

llm.ts 顶部 import 区（`import { SettingsKeys ...` 之后）加：

```ts
import { ensureNotCancelled } from './jobs'
```

`chatCompletion` 整函数替换为（注释随语义更新）：

```ts
/** 调用默认 LLM 的一次 chat completion。失败抛带 message 的 Error（渲染层 toast 展示）；
 *  signal 取消 → 抛「已取消」（含 429 退避等待期，取消立即中断不等计时） */
export async function chatCompletion(opts: ChatOptions): Promise<ChatResult> {
  const cfg = getDefaultLlm() // 未配置时抛 LlmNotConfiguredError
  const url = normalizeChatUrl(cfg.apiUrl)
  const body: Record<string, unknown> = {
    model: cfg.model,
    messages: opts.messages,
    stream: false
  }
  if (opts.temperature != null) body.temperature = opts.temperature
  if (opts.jsonMode) body.response_format = { type: 'json_object' }

  let res: Response
  for (let attempt = 0; ; attempt++) {
    ensureNotCancelled(opts.signal)
    try {
      res = await fetch(url, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${cfg.apiKey}`
        },
        body: JSON.stringify(body),
        signal: opts.signal
      })
    } catch (e) {
      if (opts.signal?.aborted) throw new Error('已取消')
      throw new Error(`网络请求失败：${(e as Error).message}`)
    }
    // 429 账号并发/限流：尊重 Retry-After 头，否则指数退避 2s/4s/8s 后自动重试
    if (res.status !== 429 || attempt >= RATE_LIMIT_RETRIES) break
    const retryAfter = Number(res.headers.get('retry-after'))
    const backoff = [2000, 4000, 8000]
    const waitMs =
      Number.isFinite(retryAfter) && retryAfter > 0
        ? Math.min(retryAfter * 1000, 15_000)
        : (backoff[attempt] ?? 8000)
    await sleepOrAbort(waitMs, opts.signal)
  }
  if (!res.ok) {
    const text = await res.text().catch(() => '')
    throw new Error(`LLM 服务返回 ${res.status}：${text.slice(0, 300)}`)
  }
  const data = (await res.json()) as {
    choices?: { message?: { content?: string } }[]
  }
  const content = data?.choices?.[0]?.message?.content
  if (typeof content !== 'string') throw new Error('LLM 返回格式异常（无 choices[0].message.content）')
  return { content }
}

/** 退避等待：与 signal abort race（取消立即中断，不等计时走完） */
function sleepOrAbort(ms: number, signal?: AbortSignal): Promise<void> {
  if (!signal) return new Promise((r) => setTimeout(r, ms))
  if (signal.aborted) return Promise.reject(new Error('已取消'))
  return new Promise((resolve, reject) => {
    const t = setTimeout(() => {
      signal.removeEventListener('abort', onAbort)
      resolve()
    }, ms)
    const onAbort = (): void => {
      clearTimeout(t)
      reject(new Error('已取消'))
    }
    signal.addEventListener('abort', onAbort, { once: true })
  })
}
```

- [x] **Step 1.3 llm.ts：testLlmConnection 加可选 signal**

```ts
/** LLM「测试连接」：发一次最小请求（个人中心 specs §3.4）；signal 取消 → 抛「已取消」 */
export async function testLlmConnection(config: LlmConfig, signal?: AbortSignal): Promise<void> {
  const url = normalizeChatUrl(config.apiUrl)
  let res: Response
  try {
    res = await fetch(url, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${config.apiKey}`
      },
      body: JSON.stringify({
        model: config.model,
        messages: [{ role: 'user', content: 'ping' }],
        max_tokens: 1
      }),
      signal
    })
  } catch (e) {
    if (signal?.aborted) throw new Error('已取消')
    throw new Error(`网络请求失败：${(e as Error).message}`)
  }
  if (!res.ok) {
    const text = await res.text().catch(() => '')
    throw new Error(`LLM 服务返回 ${res.status}：${text.slice(0, 300)}`)
  }
}
```

（`listUpstreamModels` 不改——llm:models 不在取消范围。）

- [x] **Step 1.4 验证**：`npx tsc -p tsconfig.node.json --noEmit` → 通过。

## Task 2: mcp.ts McpSession 挂 signal + mcpResearch.ts 三处 chatCompletion 穿透

**Files:**
- Modify: `electron/ai/mcp.ts`
- Modify: `electron/ai/mcpResearch.ts`

- [x] **Step 2.1 mcp.ts：McpSession 构造器第三参 signal，rpc 与 initialized 通知 fetch 均挂 signal，abort 归一**

构造器替换：

```ts
export class McpSession {
  private sessionId: string | null = null
  private initialized = false

  constructor(
    private config: { name: string; url: string; authType?: 'none' | 'bearer'; apiKey?: string },
    private onLog?: (msg: string) => void,
    private signal?: AbortSignal
  ) {}
```

`rpc` 内 fetch 与 catch 替换：

```ts
    let res: Response
    try {
      res = await fetch(this.config.url, {
        method: 'POST',
        headers: { ...this.headers(), ...(this.sessionId ? { 'mcp-session-id': this.sessionId } : {}) },
        body: JSON.stringify(body),
        signal: this.signal
      })
    } catch (e) {
      if (this.signal?.aborted) throw new Error('已取消')
      throw new Error(`MCP ${this.config.name} 请求失败：${(e as Error).message}`)
    }
```

`initialize` 的通知 fetch 加 `signal: this.signal`（该处 catch 本就容忍失败，无需归一）：

```ts
      await fetch(this.config.url, {
        method: 'POST',
        headers: { ...this.headers(), ...(this.sessionId ? { 'mcp-session-id': this.sessionId } : {}) },
        body: JSON.stringify({ jsonrpc: '2.0', method: 'notifications/initialized' }),
        signal: this.signal
      })
```

`findSearchTool` 加 signal 形参并传入 McpSession（辩真验证用）：

```ts
export async function findSearchTool(
  onLog?: (msg: string) => void,
  signal?: AbortSignal
): Promise<{ mcp: McpSession; tool: string } | null> {
  const mcps = getEnabledMcps()
  if (mcps.length === 0) throw new McpNotEnabledError()
  for (const config of mcps) {
    try {
      const session = new McpSession(config, onLog, signal)
```

（`listAllTools` 不改。）

- [x] **Step 2.2 mcpResearch.ts：三处 chatCompletion 全穿 signal + 阶段间检查**

文件头 import 改：

```ts
import { chatCompletion } from './llm'
import { ensureNotCancelled } from './jobs'
```

四个函数签名与 LLM 调用改动（其余逻辑不动）：

```ts
async function extractFromDocs(
  name: string,
  docText: string,
  signal?: AbortSignal
): Promise<McpResearch | null> {
  // ……prompt 不变……
  const r = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    jsonMode: true,
    temperature: 0,
    signal
  })
```

```ts
async function researchViaDocs(name: string, seedUrl?: string, signal?: AbortSignal): Promise<McpResearch | null> {
  // ① LLM 生成候选 URL
  const gen = await chatCompletion({
    messages: [/* ……原文不动…… */],
    jsonMode: true,
    temperature: 0,
    signal
  })
  // ……中段 URL 整备不变……
  // ② 逐个 fetch，取到可提取内容即返回（fetchText 自带 15s timeout，不挂 signal——设计口径）
  for (const u of candidates.slice(0, 8)) {
    ensureNotCancelled(signal)
    const text = await fetchText(u).catch(() => null)
    if (!text || text.length < 200) continue
    const r = await extractFromDocs(name, text, signal).catch(() => null)
    // ……命中返回逻辑不变……
```

```ts
async function researchViaLlm(name: string, signal?: AbortSignal): Promise<McpResearch | null> {
  const r = await chatCompletion({
    messages: [/* ……原文不动…… */],
    jsonMode: true,
    temperature: 0,
    signal
  })
```

主入口 `researchMcpConfig`（阶段间检查 + 调用点传参 + abort 不被降级捕获吞掉）：

```ts
export async function researchMcpConfig(name: string, signal?: AbortSignal): Promise<McpResearch> {
  const notes: string[] = []
  let fallback: McpResearch | undefined

  // ① Registry：official 直接用；officialRepo 转第②步查官方文档
  try {
    ensureNotCancelled(signal)
    const reg = await researchViaRegistry(name)
    if (reg.official) return reg.official
    fallback = reg.fallback
    // ② 文档（需要 LLM，未配置时 chatCompletion 抛 LLM_NOT_CONFIGURED）
    const docR = await researchViaDocs(name, reg.officialRepo, signal).catch((e: Error) => {
      if (e.message === '已取消' || e.message.includes('LLM_NOT_CONFIGURED')) throw e // 取消/未配置直抛
      return null
    })
    // ……docR 命中返回逻辑不变……
    notes.push('官方路径未能定位远程端点')
  } catch (e) {
    const msg = (e as Error).message
    if (msg === '已取消' || msg.includes('LLM_NOT_CONFIGURED')) throw e
    notes.push('Registry 查询失败')
  }

  // ③ LLM 兜底（自有知识）
  try {
    ensureNotCancelled(signal)
    const r = await researchViaLlm(name, signal)
    // ……命中返回逻辑不变……
  } catch (e) {
    const msg = (e as Error).message
    if (msg === '已取消' || msg.includes('LLM_NOT_CONFIGURED')) throw e
  }
  // ……③' 第三方兜底与最终 throw 不变……
```

- [x] **Step 2.3 mcpResearch.ts：testMcpConnection 挂 signal**

```ts
export async function testMcpConnection(config: {
  name: string
  url: string
  authType?: 'none' | 'bearer'
  apiKey?: string
}, signal?: AbortSignal): Promise<{ tools: string[] }> {
  const { McpSession } = await import('./mcp')
  const session = new McpSession(config, undefined, signal)
  const tools = await session.listTools()
  // ……其余不变……
```

- [x] **Step 2.4 验证**：`npx tsc -p tsconfig.node.json --noEmit` → 通过。

## Task 3: services.ts（上）——对话/格言/灵感/万象/辩真/copilot 六族

**Files:**
- Modify: `electron/ai/services.ts`

文件头 import 区加一行：

```ts
import { ensureNotCancelled } from './jobs'
```

- [x] **Step 3.1 chatWithProfileLookup + aiChat + compactAiSession（边栏对话 /compact）**

`chatWithProfileLookup` 的 req 类型与两次调用（首轮 + 检索补轮）穿 signal：

```ts
async function chatWithProfileLookup(req: {
  messages: { role: 'system' | 'user' | 'assistant'; content: string }[]
  temperature: number
  signal?: AbortSignal
}): Promise<{ content: string }> {
  const first = await chatCompletion(req)
  // ……PROFILE_LOOKUP 匹配不变……
  const second = await chatCompletion({
    ...req,
    messages: [
      ...req.messages,
      { role: 'assistant', content: first.content.replace(PROFILE_LOOKUP_RE, '').trim() },
      { role: 'user', content: feedback }
    ]
  })
  return { content: second.content.replace(PROFILE_LOOKUP_RE, '').trim() }
}
```

`aiChat` 加末位可选参并透传（用户消息在 LLM 前已落库——取消时用户消息保留，符合取消语义表）：

```ts
export async function aiChat(
  userMessage: string,
  currentModule: string,
  sessionId: number,
  channel: AiChannel = 'assistant',
  signal?: AbortSignal
): Promise<AiMessage> {
  // ……前段 autoTitle / appendAiMessage / history / system 拼装不变……
  const res = await chatWithProfileLookup({
    messages: [{ role: 'system', content: system }, ...history],
    temperature: 0.8,
    signal
  })
  // ……后段不变……
}
```

`compactAiSession` 加 signal（chatCompletion 先行、成功后才建新会话——取消即无痕）：

```ts
export async function compactAiSession(sessionId: number, signal?: AbortSignal): Promise<AiSession> {
  // ……前段不变……
  const res = await chatCompletion({
    messages: [
      {
        role: 'user',
        content: `请把以下对话历史压缩成一份简明摘要（保留关键事实、结论、待办与用户个人信息，500 字以内），直接输出摘要正文，不要任何前缀：\n\n${transcript}`
      }
    ],
    temperature: 0.3,
    signal
  })
  // ……后段不变……
}
```

- [x] **Step 3.2 generateMottos（含补足轮，阶段间检查）**

签名与关键改动（其余 125 行不动）：

```ts
export async function generateMottos(signal?: AbortSignal): Promise<GenerateMottosResult> {
```

`callAndParse` 内 `call` 挂 signal（chatCompletion 自身已拦截取消，parse 重试只包解析错误，取消异常直接穿透）：

```ts
  const callAndParse = async (prompt: string): Promise<GeneratedMotto[]> => {
    const call = () =>
      chatCompletion({
        messages: [{ role: 'user', content: prompt }],
        temperature: 0.9,
        jsonMode: true,
        signal
      })
```

第一轮入库前拦截（取消发生在首轮 LLM 等待 → 整批不入库，无半批残留）：

```ts
  // 第一轮：5 摘录 + 5 编撰
  const firstItems = await callAndParse(buildPrompt(5, 5, ''))
  let generated = firstItems.length
  ensureNotCancelled(signal)
  insertBatch(firstItems)
```

补足轮：入口检查 + catch 里取消上抛（非取消失败仍静默，维持第24轮口径）。已入库的首轮条目按全局语义「已写库的保留」：

```ts
  if (shortfallExcerpt > 0 || shortfallComposed > 0) {
    try {
      ensureNotCancelled(signal)
      const batchAvoid = batchInsertedContents.map((c) => `- ${c}`).join('\n')
      const more = await callAndParse(buildPrompt(shortfallExcerpt, shortfallComposed, batchAvoid))
      generated += more.length
      const before = inserted
      insertBatch(more)
      supplemented = inserted - before
    } catch (e) {
      // 取消如实上抛（渲染层 toast「已取消」，不走「生成失败」弹窗）；其余失败非致命（第24轮）
      if (signal?.aborted) throw e
    }
  }
```

- [x] **Step 3.3 generateInspirations（两阶段共享 signal；批末统一入库 → 取消全批作废）**

```ts
export async function generateInspirations(signal?: AbortSignal): Promise<GenerateInspirationsResult> {
```

`callDiverge` / `callReview` 的 chatCompletion 均加 `signal`；两处「解析失败重试」catch 均先判取消（取消不重试）：

```ts
  const callDiverge = () =>
    chatCompletion({
      messages: [{ role: 'user', content: divergePrompt }],
      temperature: 0.95,
      jsonMode: true,
      signal
    })
  let candidates: { title: string; summary: string; form: string }[]
  try {
    candidates = parseInspirationArray((await callDiverge()).content)
  } catch (e) {
    // 取消直接上抛，不重试
    if (signal?.aborted) throw e
    candidates = parseInspirationArray((await callDiverge()).content)
  }
```

```ts
  const callReview = (list: { title: string; summary: string; form: string }[]) =>
    chatCompletion({
      messages: [{ role: 'user', content: reviewPrompt(list) }],
      temperature: 0.4,
      jsonMode: true,
      signal
    })
  let items: { title: string; summary: string; form: string }[]
  try {
    items = parseInspirationArray((await callReview(candidates)).content)
  } catch (e) {
    if (signal?.aborted) throw e
    try {
      items = parseInspirationArray((await callReview(candidates)).content)
    } catch (e2) {
      if (signal?.aborted) throw e2
      // 自评两趟均失败 → 代码侧按 form 配额挑前 5（优化建议区任务2）
      items = pickDiverseFive(candidates)
    }
  }
```

入库循环前拦截：

```ts
  // 入库：标题精确查重（含回收站 + 批内互斥）→ 插草稿区末尾（specs §6.2）
  ensureNotCancelled(signal)
  const titles = new Set(existingRows.map((r) => r.title.trim()))
```

- [x] **Step 3.4 refineInspiration / copilotWriting**

```ts
export async function refineInspiration(id: number, signal?: AbortSignal): Promise<string> {
  // ……prompt 拼装不变……
  const res = await chatCompletion({ messages: [{ role: 'user', content: prompt }], temperature: 0.7, signal })
```

```ts
export async function copilotWriting(
  articleId: number,
  action: WenbiCopilotAction,
  selection?: string,
  signal?: AbortSignal
): Promise<string> {
  // ……前段不变……
  const res = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: action === 'draft' || action === 'continue' ? 0.7 : 0.4,
    signal
  })
```

- [x] **Step 3.5 万象族：suggestWikiTerm / generateWikiCard / generateWikiQuiz**

```ts
export async function suggestWikiTerm(
  sectionId: number | null,
  signal?: AbortSignal
): Promise<{ sectionId: number; term: string }> {
  // ……板块挑选/avoid 拼装不变……
  const res = await chatCompletion({
    messages: [
      {
        role: 'user',
        content: `请从「${section.name}」领域中构思一个值得收藏的知识词条（${avoid}），只返回词条名本身，不要任何解释和标点。`
      }
    ],
    temperature: 1.0,
    signal
  })
```

```ts
export async function generateWikiCard(
  term: string | null,
  sectionId: number | null,
  signal?: AbortSignal
): Promise<GenerateWikiResult> {
  const d = getDb()
  // 词条名缺省：LLM 构思（板块 = 指定板块，未指定则随机挑）
  if (!term) {
    const suggested = await suggestWikiTerm(sectionId, signal)
    sectionId = suggested.sectionId
    term = suggested.term
  }
  // ……查重不变……
  const res = await chatCompletion({ messages: [{ role: 'user', content: prompt }], temperature: 0.7, signal })
  // ……后段不变……
}
```

```ts
export async function generateWikiQuiz(signal?: AbortSignal): Promise<WikiQuizQuestion[]> {
  // ……卡片抽取不变……
  const res = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.7,
    jsonMode: true,
    signal
  })
  return parseQuizArray(res.content, idByTerm)
}
```

- [x] **Step 3.6 runVerification（LLM 段全穿 + 阶段间检查；MCP fetch 经 McpSession signal 尽力传播）**

```ts
export async function runVerification(
  claim: string,
  onProgress: (msg: string) => void,
  signal?: AbortSignal
): Promise<VerifyResult> {
  // 动态 import 避免循环依赖
  const { findSearchTool } = await import('./mcp')
  onProgress(`开始验证：${claim}`)
  const found = await findSearchTool(onProgress, signal)
  // ……found 判空不变……

  // 1) LLM 生成检索关键词
  const kwRes = await chatCompletion({
    messages: [
      {
        role: 'user',
        content: `我想验证这个观点的真实性：「${claim}」。请生成 3 组适合搜索引擎检索的中英文关键词（每组关键词一行，直接输出，不要编号和解释）。`
      }
    ],
    temperature: 0.5,
    signal
  })
  // ……keywords 解析与 onProgress 不变……

  // 2) 逐组检索
  const searchResults: string[] = []
  for (const kw of keywords) {
    ensureNotCancelled(signal)
    onProgress(`正在检索：${kw}`)
    // ……检索体不变……
  }
  if (searchResults.length === 0) throw new Error('全部检索失败，无法验证')

  // 3) LLM 综合
  onProgress('正在综合分析…')
  const analysisRes = await chatCompletion({
    messages: [
      {
        role: 'user',
        // 辩真验证不注入画像（优化建议区第13轮）：观点核查与用户画像无关，注入反干扰注意力
        content: `观点：「${claim}」\n\n以下是检索到的资料：\n${searchResults.join('\n\n')}\n\n请基于资料验证该观点。先输出验证分析（引用资料说明依据），然后单独一行输出可信度百分比（0-100 的整数），格式严格为：可信度：N%\n\n分析正文使用 Markdown，若引用了具体来源请在分析中以 Markdown 链接列出。`
      }
    ],
    temperature: 0.3,
    signal
  })
  // ……可信度/analysis 解析不变……

  // 4) 写记录 + md（取消在写库前拦截；已推送边栏的进度消息保留为历史事实）
  ensureNotCancelled(signal)
  const d = getDb()
  // ……写库/写 md/收尾 onProgress 不变……
}
```

## Task 4: services.ts（下）——推理角族（海龟汤出题链 / 判问判答 / 终局点评 / 思维墙两阶段 / 判答）

**Files:**
- Modify: `electron/ai/services.ts`

- [x] **Step 4.1 generateSoups 出题链（共享同一 signal；当前实现为批末统一落库 → 取消全批作废，汤库仅完整碗）**

签名：

```ts
export async function generateSoups(
  preference: 'random' | 'easy' | 'medium' | 'hard',
  signal?: AbortSignal
): Promise<{ generated: number; inserted: number }> {
```

`composeSoups` 加 signal（重试 catch 先判取消）：

```ts
/** 单趟出题调用 + 解析（解析失败自动重试一次；取消即抛「已取消」不重试） */
async function composeSoups(prompt: string, count: number, signal?: AbortSignal): Promise<TurtleSoupDraft[]> {
  const call = () =>
    chatCompletion({ messages: [{ role: 'user', content: prompt }], temperature: 0.9, jsonMode: true, signal })
  try {
    return parseSoupArray((await call()).content, count)
  } catch (e) {
    if (signal?.aborted) throw e
    return parseSoupArray((await call()).content, count)
  }
}
```

`reviewSoup` / `reviewSoupSafe` 加 signal（safe 的双层 catch 均先判取消）：

```ts
async function reviewSoup(
  draft: TurtleSoupDraft,
  ctx: {
    recentTricks: string[]
    peerTricks: string[]
    requiredDifficulty?: 'easy' | 'medium' | 'hard'
  },
  signal?: AbortSignal
): Promise<SoupReview> {
  // ……prompt 不变……
  const res = await chatCompletion({
    messages: [/* ……原文不动…… */],
    temperature: 0.2,
    jsonMode: true,
    signal
  })
```

```ts
async function reviewSoupSafe(
  draft: TurtleSoupDraft,
  ctx: {
    recentTricks: string[]
    peerTricks: string[]
    requiredDifficulty?: 'easy' | 'medium' | 'hard'
  },
  signal?: AbortSignal
): Promise<SoupReview | null> {
  try {
    return await reviewSoup(draft, ctx, signal)
  } catch (e) {
    if (signal?.aborted) throw e
    try {
      return await reviewSoup(draft, ctx, signal)
    } catch (e2) {
      if (signal?.aborted) throw e2
      return null
    }
  }
}
```

`generateSoups` 主体调用点（逐碗串行循环首检查 + 全链传参 + 落库前检查）：

```ts
  const drafts = await composeSoups(
    buildSoupPrompt({ count: 3, difficultyText, avoidList, recentTrickList }),
    3,
    signal
  )
  // ……finals 声明不变……
  for (const draft of drafts) {
    ensureNotCancelled(signal)
    const r1 = await reviewSoupSafe(
      draft,
      {
        recentTricks,
        peerTricks: drafts.filter((o) => o !== draft).map((o) => o.trick_note),
        requiredDifficulty
      },
      signal
    )
    // ……r1 分支不变；重出一次分支改为：
    try {
      const [redone] = await composeSoups(
        buildSoupPrompt({
          count: 1,
          difficultyText: `本碗按「${DIFF_ZH[target]}」难度出题`,
          avoidList,
          recentTrickList,
          redoProblems: r1.problems,
          keepTricks: finals.map((f) => f.soup.trick_note)
        }),
        1,
        signal
      )
      const r2 = await reviewSoupSafe(
        redone,
        {
          recentTricks,
          peerTricks: finals.map((f) => f.soup.trick_note),
          requiredDifficulty
        },
        signal
      )
      // ……r2 分支不变……
    } catch (e) {
      // 重出失败 → 弃碗（取消已在上游各趟上抛，不会走到这个吞错的 catch）
    }
  }
  // ……finals 判空不变……
  ensureNotCancelled(signal)
  const now = nowIso()
  const ins = d.prepare(/* ……原文不变…… */)
```

注意：重出 catch 原为空 catch 吞一切——取消异常在 composeSoups/reviewSoupSafe 内部已先抛「已取消」，但此处空 catch 仍会把它吞掉。**必须改为判取消上抛**：

```ts
    } catch (e) {
      if (signal?.aborted) throw e
      // 重出失败 → 弃碗
    }
```

- [x] **Step 4.2 判问 / 判答 / 终局点评**

```ts
export async function judgeSoupQuestion(
  material: TurtleSoupMaterial,
  historyText: string,
  question: string,
  signal?: AbortSignal
): Promise<SoupAskResult> {
  // ……system/history 不变……
  const res = await chatCompletion({
    messages: [
      { role: 'system', content: system },
      { role: 'user', content: `${history}## 本次提问\n${question}` }
    ],
    temperature: 0.2,
    jsonMode: true,
    signal
  })
```

```ts
export async function judgeSoupGuess(
  material: TurtleSoupMaterial,
  historyText: string,
  reasoning: string,
  signal?: AbortSignal
): Promise<SoupGuessResult> {
  // ……同上模式，chatCompletion 加 signal……
```

```ts
export async function soupReview(
  material: TurtleSoupMaterial,
  transcript: string,
  result: 'solved' | 'abandoned',
  questionCount: number,
  signal?: AbortSignal
): Promise<string> {
  // ……prompt 不变……
  const res = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.5,
    signal
  })
```

**终局点评的取消语义（写死）**：`finishTurtleGame`（ipc.ts）里 soupReview 的 try/catch 本就把点评失败降级为固定文案。signal 传入后，取消点评 → catch 降级 → **终局链照常完成**（对局消息/汤状态/复盘 md 均落库，只是点评为降级文案）。这是有意为之：破汤/弃汤的判定与消息在点评前已落库，按「已写库的保留」语义，终局不可因取消而悬置。该窗口内点「停止」表现为：点评段立即结束、终局正常返回，渲染层走成功路径。

`backfillTrickNotes` 不改（启动期后台任务）。

- [x] **Step 4.3 generateWallPuzzle 两阶段 + judgeWallAnswer**

```ts
export async function generateWallPuzzle(
  difficulty: 'easy' | 'medium' | 'hard',
  opts?: { type?: WallPuzzleType; avoid?: string[] },
  signal?: AbortSignal
): Promise<WallPuzzleDraft> {
  // ……type/avoidBlock/basePrompt 不变……
  let draft = await composeWallPuzzle(type, difficulty, basePrompt, signal)
  // 阶段二：审题人独立验证，不过打回重出一次（问题清单回注 prompt），仍不过抛错
  const v1 = await verifyWallPuzzle(draft, difficulty, signal)
  if (!v1.ok) {
    ensureNotCancelled(signal)
    draft = await composeWallPuzzle(
      type,
      difficulty,
      `${basePrompt}\n\n## 上一次出的题被审题人打回，问题如下（重新出题必须全部规避）\n${v1.problems
        .map((p) => `- ${p}`)
        .join('\n')}`,
      signal
    )
    const v2 = await verifyWallPuzzle(draft, difficulty, signal)
    if (!v2.ok) throw new Error('出题未通过审题验证（两次），不落库')
  }
  return draft
}
```

```ts
async function composeWallPuzzle(
  type: WallPuzzleType,
  difficulty: 'easy' | 'medium' | 'hard',
  prompt: string,
  signal?: AbortSignal
): Promise<WallPuzzleDraft> {
  const res = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.9,
    jsonMode: true,
    signal
  })
  // ……解析不变……
```

```ts
async function verifyWallPuzzle(
  draft: WallPuzzleDraft,
  target: 'easy' | 'medium' | 'hard',
  signal?: AbortSignal
): Promise<{ ok: boolean; problems: string[] }> {
  const res = await chatCompletion({
    messages: [/* ……原文不动…… */],
    temperature: 0.2,
    jsonMode: true,
    signal
  })
  // ……解析不变……
```

```ts
export async function judgeWallAnswer(
  puzzleText: string,
  standardAnswer: string,
  standardReasoning: string,
  myAnswer: string,
  signal?: AbortSignal
): Promise<WallAnswerResult> {
  // ……prompt 不变……
  const res = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.1,
    jsonMode: true,
    signal
  })
```

- [x] **Step 4.4 验证**：`npx tsc -p tsconfig.node.json --noEmit` → 通过（scheduler.ts 调 generateMottos() 无参，可选参不受影响）。

## Task 5: ipc.ts——ai:cancel 通道 + 23 个 handler 统一模式

**Files:**
- Modify: `electron/ipc.ts`

- [x] **Step 5.1 import + 统一取消通道**

import 区加：

```ts
import { beginJob, endJob, cancelJob } from './ai/jobs'
```

`registerIpc()` 内、`// ---------- AI 边栏` 注释块之前（或任意通道区顶部）加：

```ts
  // ---------- 统一取消通道（260908 全局取消）：渲染层 ai.cancel(jobId) → 掐断进行中的 AI 任务 ----------
  ipcMain.handle('ai:cancel', (_e, jobId: string) => cancelJob(jobId))
```

- [x] **Step 5.2 逐 handler 改造（jobId 一律首个参数；beginJob → 调用穿 ac.signal → finally endJob）**

以下给出全部 23 处的替换后代码（保持各 handler 原有错误分流/返回结构）：

```ts
  ipcMain.handle('mottos:generate', async (_e, jobId: string) => {
    const ac = beginJob(jobId)
    try {
      return await generateMottos(ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('wiki:generate', async (_e, jobId: string, term: string | null, sectionId: number | null) => {
    const ac = beginJob(jobId)
    try {
      // 必须在此 await：若把 generateWikiCard 的 Promise 嵌进返回对象，
      // ipcMain.handle 只 await 顶层值，嵌套 Promise 序列化失败 → 渲染层 invoke 永不 settle（卡"生成中"）
      return { ok: true as const, data: await generateWikiCard(term, sectionId, ac.signal) }
    } catch (e) {
      const msg = (e as Error).message
      if (msg.startsWith('CONFLICT:')) return { ok: false as const, conflict: msg.slice(9) }
      throw e
    } finally {
      endJob(jobId)
    }
  })
  // 随机词条名（手动弹窗骰子/指定板块随机生成）：只构思词条名不生成卡片
  ipcMain.handle('wiki:suggestTerm', async (_e, jobId: string, sectionId: number | null) => {
    const ac = beginJob(jobId)
    try {
      const r = await suggestWikiTerm(sectionId, ac.signal)
      return r.term
    } finally {
      endJob(jobId)
    }
  })
  // 测一测：随机 5 张卡片批量出四选一（优化建议区）
  ipcMain.handle('wiki:quiz', async (_e, jobId: string) => {
    const ac = beginJob(jobId)
    try {
      return await generateWikiQuiz(ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('inspirations:generate', async (_e, jobId: string) => {
    const ac = beginJob(jobId)
    try {
      return await generateInspirations(ac.signal)
    } finally {
      endJob(jobId)
    }
  })
  ipcMain.handle('inspirations:refine', async (_e, jobId: string, id: number) => {
    const ac = beginJob(jobId)
    try {
      return await refineInspiration(id, ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('ai:chat', async (_e, jobId: string, message: string, currentModule: string, sessionId: number, channel?: string) => {
    const ac = beginJob(jobId)
    try {
      return await aiChat(message, currentModule, sessionId, (channel ?? 'assistant') as AiChannel, ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  // /compact：会话历史压缩为前情摘要另存新会话（优化建议区第14轮；补接取消——含 chatCompletion）
  ipcMain.handle('aiSession:compact', async (_e, jobId: string, sessionId: number) => {
    const ac = beginJob(jobId)
    try {
      return await compactAiSession(sessionId, ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('verify:run', async (_e, jobId: string, claim: string) => {
    const ac = beginJob(jobId)
    try {
      return await runVerification(claim, (msg) => pushAiSystemMessage(msg), ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('turtle:generate', async (_e, jobId: string, preference: string) => {
    const ac = beginJob(jobId)
    try {
      return await generateSoups(
        preference === 'easy' || preference === 'medium' || preference === 'hard' ? preference : 'random',
        ac.signal
      )
    } finally {
      endJob(jobId)
    }
  })
```

`turtle:ask` / `turtle:guess` / `turtle:abandon`——judge 与终局链穿 signal（abandon 为补接；guess 破汤分支的 finishTurtleGame 也穿，点评段取消按降级处理，见 Task 4 Step 4.2 说明）：

```ts
  ipcMain.handle('turtle:ask', async (_e, jobId: string, gameId: number, question: string) => {
    const ac = beginJob(jobId)
    try {
      const q = question.trim()
      if (!q) throw new Error('EMPTY')
      const d = getDb()
      const game = getPlayingGame(d, gameId)
      const judged = await judgeSoupQuestion(
        turtleMaterial(d, game.soup_id),
        turtleHistoryText(d, gameId),
        q,
        ac.signal
      )
      // ……消息落库/计数/返回全部不变……
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('turtle:guess', async (_e, jobId: string, gameId: number, reasoning: string) => {
    const ac = beginJob(jobId)
    try {
      const g = reasoning.trim()
      if (!g) throw new Error('EMPTY')
      const d = getDb()
      const game = getPlayingGame(d, gameId)
      const verdict = await judgeSoupGuess(turtleMaterial(d, game.soup_id), turtleHistoryText(d, gameId), g, ac.signal)
      // ……vText/消息落库/未破返回不变；破汤分支改为：
      const fin = await finishTurtleGame(gameId, 'solved', ac.signal)
      // ……返回不变……
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('turtle:abandon', async (_e, jobId: string, gameId: number) => {
    const ac = beginJob(jobId)
    try {
      const d = getDb()
      getPlayingGame(d, gameId)
      insertTurtleMsg(d, gameId, 'system', 'notice', '我放弃了本局，揭示汤底。', nowIso())
      return await finishTurtleGame(gameId, 'abandoned', ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

`finishTurtleGame` 加第三参并传给点评（其余不变）：

```ts
async function finishTurtleGame(
  gameId: number,
  result: 'solved' | 'abandoned',
  signal?: AbortSignal
): Promise<{ bottom: string; mdPath: string }> {
  // ……前段不变……
  let review = ''
  try {
    review = await soupReview(
      { surface: soup.surface, bottom: soup.bottom, analysis: soup.analysis },
      transcript,
      result,
      game.question_count,
      signal
    )
  } catch {
    review = '（AI 点评生成失败，可重读上方问答自行复盘。）'
  }
  // ……后段不变……
```

思维墙五处：

```ts
  ipcMain.handle('wall:ensureToday', async (_e, jobId: string) => {
    const ac = beginJob(jobId)
    try {
      // ……row 查询/难度推导/题型轮换/avoid 不变，仅出题行穿 signal：
      const draft = await generateWallPuzzle(difficulty, { type, avoid }, ac.signal)
      // ……落库与返回不变……
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('wall:answer', async (_e, jobId: string, puzzleId: number, myAnswer: string) => {
    const ac = beginJob(jobId)
    try {
      // ……校验与取行不变，判答行穿 signal：
      const verdict = await judgeWallAnswer(
        row.puzzle_text,
        row.answer_standard,
        row.standard_reasoning ?? '',
        a,
        ac.signal
      )
      // ……写 md/更新/返回不变……
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('wall:practiceNew', async (_e, jobId: string, pref: string, typePref?: string) => {
    const ac = beginJob(jobId)
    try {
      // ……难度/题型推导不变，出题行穿 signal：
      const draft = await generateWallPuzzle(difficulty, { type: type ?? undefined }, ac.signal)
      // ……暂存与返回不变……
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('wall:practiceAnswer', async (_e, jobId: string, practiceId: number, myAnswer: string) => {
    const ac = beginJob(jobId)
    try {
      // ……校验/取 entry 不变，判答行穿 signal（取消 → 判答未完成，entry 未删，可重新提交）：
      const verdict = await judgeWallAnswer(entry.puzzle, entry.answer, entry.reasoning, a, ac.signal)
      // ……delete/返回不变……
    } finally {
      endJob(jobId)
    }
  })
```

```ts
  ipcMain.handle('wall:bankAnswer', async (_e, jobId: string, bankId: number, myAnswer: string) => {
    const ac = beginJob(jobId)
    try {
      // ……校验/取行不变，判答行穿 signal：
      const verdict = await judgeWallAnswer(row.puzzle_text, row.answer_standard, row.solution, a, ac.signal)
      // ……writeBankMd/更新/返回不变……
    } finally {
      endJob(jobId)
    }
  })
```

致知己（LLM 段在 handler 内联，整体包 begin/end；取消在 INSERT 前抛出 → 不建半截数据，维持「失败抛错不创建」口径）：

```ts
  ipcMain.handle(
    'zhijiji:createQuestion',
    async (_e, jobId: string, title: string, tags?: string[], aiInit?: boolean) => {
      const ac = beginJob(jobId)
      try {
        const t = title.trim()
        if (!t) throw new Error('TITLE_REQUIRED')
        // ……aiInit 分支内 chatCompletion 加 signal：
        const res = await chatCompletion({
          messages: [{ role: 'user', content: prompt }],
          temperature: 0.7,
          signal: ac.signal
        })
        // ……后续建问题/版本/返回全部不变……
      } finally {
        endJob(jobId)
      }
    }
  )
```

文笔坊 copilot：

```ts
  ipcMain.handle('wenbi:copilot', async (_e, jobId: string, id: number, action: string, selection?: string) => {
    const ac = beginJob(jobId)
    try {
      return await copilotWriting(id, action as 'draft' | 'continue' | 'polish' | 'rewrite', selection, ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

个人中心三处：

```ts
  ipcMain.handle('llm:test', (_e, jobId: string, config: LlmConfig) => {
    const ac = beginJob(jobId)
    return testLlmConnection(config, ac.signal).finally(() => endJob(jobId))
  })
  ipcMain.handle('mcp:research', (_e, jobId: string, name: string) => {
    const ac = beginJob(jobId)
    return researchMcpConfig(name, ac.signal).finally(() => endJob(jobId))
  })
  ipcMain.handle('mcp:test', (_e, jobId: string, config: McpConfig) => {
    const ac = beginJob(jobId)
    return testMcpConnection(config, ac.signal).finally(() => endJob(jobId))
  })
```

- [x] **Step 5.3 验证**：`npx tsc -p tsconfig.node.json --noEmit` → 通过。

## Task 6: preload.ts + api.d.ts 三处同步

**Files:**
- Modify: `electron/preload.ts`
- Modify: `src/renderer/api.d.ts`

- [x] **Step 6.1 preload.ts：ai 命名空间加 cancel；23 个方法首参加 jobId**

`ai` 对象内（`configured` 之后任意位置）加：

```ts
    /** 取消进行中的 AI 任务（false = 任务已结束，静默即可） */
    cancel: (jobId: string): Promise<boolean> => ipcRenderer.invoke('ai:cancel', jobId),
```

逐方法签名与 invoke 替换（仅列变化行，其余方法不动）：

```ts
    chat: (
      jobId: string,
      message: string,
      currentModule: string,
      sessionId: number,
      channel?: string
    ): Promise<{ id: number; role: string; content: string }> =>
      ipcRenderer.invoke('ai:chat', jobId, message, currentModule, sessionId, channel),
```

```ts
    compact: (jobId: string, sessionId: number): Promise<{/* ……原返回类型不变…… */}> =>
      ipcRenderer.invoke('aiSession:compact', jobId, sessionId),
```

```ts
    generate: (jobId: string): Promise<{/* ……原返回类型不变…… */}> =>
      ipcRenderer.invoke('mottos:generate', jobId),
```

```ts
    generate: (
      jobId: string,
      term: string | null,
      sectionId: number | null
    ): Promise</* ……原返回类型不变…… */> => ipcRenderer.invoke('wiki:generate', jobId, term, sectionId),
    suggestTerm: (jobId: string, sectionId: number | null): Promise<string> =>
      ipcRenderer.invoke('wiki:suggestTerm', jobId, sectionId),
    quiz: (jobId: string): Promise</* ……原返回类型不变…… */> =>
      ipcRenderer.invoke('wiki:quiz', jobId),
```

```ts
    generate: (jobId: string): Promise<{ generated: number; inserted: number; winds: string[] }> =>
      ipcRenderer.invoke('inspirations:generate', jobId),
    refine: (jobId: string, id: number): Promise<string> =>
      ipcRenderer.invoke('inspirations:refine', jobId, id),
```

```ts
    run: (jobId: string, claim: string): Promise<{ recordId: number; credibility: number }> =>
      ipcRenderer.invoke('verify:run', jobId, claim),
```

```ts
    createQuestion: (
      jobId: string,
      title: string,
      tags?: string[],
      aiInit?: boolean
    ): Promise<{ questionId: number; versionId: number; mdPath: string }> =>
      ipcRenderer.invoke('zhijiji:createQuestion', jobId, title, tags, aiInit),
```

```ts
    generate: (
      jobId: string,
      preference?: 'random' | 'easy' | 'medium' | 'hard'
    ): Promise<{ generated: number; inserted: number }> =>
      ipcRenderer.invoke('turtle:generate', jobId, preference ?? 'random'),
    ask: (
      jobId: string,
      gameId: number,
      question: string
    ): Promise<{ type: 'yes' | 'no' | 'irrelevant' | 'invalid'; reply: string; questionCount: number }> =>
      ipcRenderer.invoke('turtle:ask', jobId, gameId, question),
    guess: (
      jobId: string,
      gameId: number,
      reasoning: string
    ): Promise<{/* ……原返回类型不变…… */}> => ipcRenderer.invoke('turtle:guess', jobId, gameId, reasoning),
    abandon: (jobId: string, gameId: number): Promise<{ bottom: string; mdPath: string }> =>
      ipcRenderer.invoke('turtle:abandon', jobId, gameId),
```

```ts
    ensureToday: (jobId: string): Promise<unknown> => ipcRenderer.invoke('wall:ensureToday', jobId),
    answer: (
      jobId: string,
      puzzleId: number,
      myAnswer: string
    ): Promise<{/* ……原返回类型不变…… */}> => ipcRenderer.invoke('wall:answer', jobId, puzzleId, myAnswer),
    practiceNew: (jobId: string, pref: string, typePref?: string): Promise<unknown> =>
      ipcRenderer.invoke('wall:practiceNew', jobId, pref, typePref),
    practiceAnswer: (
      jobId: string,
      practiceId: number,
      myAnswer: string
    ): Promise<{ correct: boolean; standardAnswer: string; explanation: string }> =>
      ipcRenderer.invoke('wall:practiceAnswer', jobId, practiceId, myAnswer),
    bankAnswer: (
      jobId: string,
      bankId: number,
      myAnswer: string
    ): Promise<{/* ……原返回类型不变…… */}> => ipcRenderer.invoke('wall:bankAnswer', jobId, bankId, myAnswer),
```

```ts
    copilot: (
      jobId: string,
      id: number,
      action: 'draft' | 'continue' | 'polish' | 'rewrite',
      selection?: string
    ): Promise<string> => ipcRenderer.invoke('wenbi:copilot', jobId, id, action, selection)
```

```ts
    test: (jobId: string, config: unknown): Promise<void> => ipcRenderer.invoke('llm:test', jobId, config),
```

```ts
    research: (jobId: string, name: string): Promise<import('../src/shared/types').McpResearch> =>
      ipcRenderer.invoke('mcp:research', jobId, name),
    test: (jobId: string, config: {/* ……原形参类型不变…… */}): Promise<{ tools: string[] }> =>
      ipcRenderer.invoke('mcp:test', jobId, config)
```

- [x] **Step 6.2 api.d.ts：与 preload 完全镜像**

`ai` 块加：

```ts
    /** 取消进行中的 AI 任务（false = 任务已结束，静默即可） */
    cancel(jobId: string): Promise<boolean>
```

签名改动逐一对应 Step 6.1（返回类型取现值原样保留，只加首参 `jobId: string`）：`ai.chat` / `aiSession.compact` / `mottos.generate` / `wiki.generate` / `wiki.suggestTerm` / `wiki.quiz` / `inspirations.generate` / `inspirations.refine` / `verify.run` / `zhijiji.createQuestion` / `turtle.generate` / `turtle.ask` / `turtle.guess` / `turtle.abandon` / `wall.ensureToday` / `wall.answer` / `wall.practiceNew` / `wall.practiceAnswer` / `wall.bankAnswer` / `wenbi.copilot` / `llm.test` / `mcp.research` / `mcp.test`。例：

```ts
    chat(
      jobId: string,
      message: string,
      currentModule: string,
      sessionId: number,
      channel?: AiChannel
    ): Promise<{ id: number; role: string; content: string }>
```

- [x] **Step 6.3 验证**：`npx tsc -p tsconfig.node.json --noEmit` → 通过（web 侧此时因组件未改会报错，属预期，Task 7-18 消化）。

## 渲染层通则（Task 7-18 共用模式）

各组件 loading 态从布尔升级为持 jobId：`const [xxxJob, setXxxJob] = useState<string | null>(null)`，原布尔以派生量顶替（`const generating = genJob != null`），JSX 的 disabled/文案零改动；调用处 `const jobId = crypto.randomUUID()` → `setXxxJob(jobId)` → `await window.api.<模块>.<方法>(jobId, …原参数)`；catch 新增取消分支（排在原分支之前）：`if (msg.includes('已取消')) toast('已取消')`，其余原路径不变；finally `setXxxJob(null)`。取消按钮统一：

```tsx
<button className="btn" onClick={() => void window.api.ai.cancel(当前jobId)} title="取消本次生成">
  <span className="material-symbols-outlined">stop_circle</span>
  取消
</button>
```

## Task 7: MottosModule（生成）

**Files:** Modify `src/modules/mottos/MottosModule.tsx`

- [x] **Step 7.1** 状态：`const [generating, setGenerating] = useState(false)` → `const [genJob, setGenJob] = useState<string | null>(null)` + `const generating = genJob != null`。
- [x] **Step 7.2** `generate()` 替换：

```ts
  const generate = async (): Promise<void> => {
    if (genJob) return
    const jobId = crypto.randomUUID()
    setGenJob(jobId)
    try {
      const r = await window.api.mottos.generate(jobId)
      toast(
        `本次生成 ${r.generated} 条（摘录 ${r.excerptInserted} + 编撰 ${r.composedInserted}），去重后入库 ${r.inserted} 条` +
          (r.supplemented > 0 ? `，其中 ${r.supplemented} 条为补足生成` : '') +
          (r.tombstoneRejected > 0 ? `，${r.tombstoneRejected} 条与已删除格言重复已剔除` : '') +
          (r.patternRejected > 0 ? `，${r.patternRejected} 条因句式违规未入库` : '')
      )
      await load()
    } catch (e) {
      const msg = String((e as Error).message)
      if (msg.includes('已取消')) toast('已取消')
      else if (msg.includes('LLM_NOT_CONFIGURED')) setGoConfig(true)
      else setFailMsg(msg)
    } finally {
      setGenJob(null)
    }
  }
```

- [x] **Step 7.3** 草稿区 zone-actions 的生成按钮后旁挂取消（`{generating ? '生成中…' : '来10条格言'}` 按钮之后）：

```tsx
                    {genJob && (
                      <button
                        className="btn"
                        onClick={() => void window.api.ai.cancel(genJob)}
                        title="取消本次生成"
                      >
                        <span className="material-symbols-outlined">stop_circle</span>
                        取消
                      </button>
                    )}
```

## Task 8: WikiModule（生成 / 随机词条 / 测一测三态）

**Files:** Modify `src/modules/wiki/WikiModule.tsx`

- [x] **Step 8.1** 三态换 jobId + 派生布尔：`generating → genJob`、`suggesting → suggestJob`、`quizLoading → quizJob`（`const generating = genJob != null` 等）。
- [x] **Step 8.2** `suggestTerm`（骰子）：`setManualTerm(await window.api.wiki.suggestTerm(suggestJob, manualSection))`；catch 加取消分支 `toast('已取消')`（不 setFailMsg）。
- [x] **Step 8.3** `runGenerate`：`window.api.wiki.generate(genJob, term, sectionId)`；catch 加取消分支 `toast('已取消')`。
- [x] **Step 8.4** `startQuiz`：`setQuizQuestions(await window.api.wiki.quiz(quizJob))`；catch 取消分支 `toast('已取消')` + `setView({ kind: 'overview' })`（与原失败路径同样退出测一测视图）。
- [x] **Step 8.5** 取消按钮三处：①总览工具卡「随机来一条」按钮后（`genJob` 时）；②板块页 zone-actions 骰子按钮后（`genJob` 时）；③手动弹窗骰子按钮后（`suggestJob` 时）；④测一测「出题中，约需数秒…」empty-state 内加按钮（`quizJob` 时）。①④代码：

```tsx
            {genJob && (
              <button className="btn" onClick={() => void window.api.ai.cancel(genJob)} title="取消本次生成">
                <span className="material-symbols-outlined">stop_circle</span>
                取消
              </button>
            )}
```

```tsx
            {quizLoading && (
              <div className="empty-state">
                <span className="material-symbols-outlined spin">progress_activity</span>
                <div>出题中，约需数秒…</div>
                <button className="btn" onClick={() => void window.api.ai.cancel(quizJob)} title="取消本次出题">
                  <span className="material-symbols-outlined">stop_circle</span>
                  取消
                </button>
              </div>
            )}
```

②③同款小按钮紧跟各自骰子按钮（icon-btn 旁）。

## Task 9: InspirationsModule（生成 / AI 完善两态）

**Files:** Modify `src/modules/inspirations/InspirationsModule.tsx`

- [x] **Step 9.1** `generating → genJob`、`refining → refineJob` + 派生布尔。
- [x] **Step 9.2** `doGenerate`：`window.api.inspirations.generate(genJob)`；catch 加 `if (msg.includes('已取消')) toast('已取消')`。
- [x] **Step 9.3** `doRefine`：`window.api.inspirations.refine(refineJob, item.id)`；catch 取消分支 `toast('已取消')` + `setRefineFor(null)`（关预览弹窗）。
- [x] **Step 9.4** 取消按钮两处：①module-header「来5条灵感」按钮后（`genJob` 时，通则同款）；②AI 完善预览弹窗 footer——`refining` 期间只显示取消按钮，生成完恢复「放弃 / 追加到文档」：

```tsx
            <div className="dialog-footer">
              {refining ? (
                <button
                  className="btn"
                  onClick={() => void window.api.ai.cancel(refineJob)}
                  title="取消本次生成"
                >
                  <span className="material-symbols-outlined">stop_circle</span>
                  取消
                </button>
              ) : (
                <>
                  <button className="btn" onClick={() => setRefineFor(null)}>放弃</button>
                  <button className="btn btn-primary" onClick={() => void doAppendRefine()}>
                    追加到文档
                  </button>
                </>
              )}
            </div>
```

（原「追加到文档」的 `disabled={refining || !refineResult}` 随分支拆除不再需要。）

## Task 10: VerifyModule（验证）

**Files:** Modify `src/modules/verify/VerifyModule.tsx`

- [x] **Step 10.1** `running → runJob` + 派生布尔。
- [x] **Step 10.2** `doVerify`：`window.api.verify.run(runJob, text)`；catch 加取消分支 `toast('已取消')`（进度消息已在边栏保留，符合语义表）。
- [x] **Step 10.3** 输入区按钮行加取消（`{running && <span className="module-sub">…</span>}` 替换为取消按钮 + 原文案）：

```tsx
          {running && (
            <>
              <button
                className="btn"
                onClick={() => void window.api.ai.cancel(runJob)}
                title="取消本次验证"
              >
                <span className="material-symbols-outlined">stop_circle</span>
                取消
              </button>
              <span className="module-sub">正在通过 MCP 检索并综合分析</span>
            </>
          )}
```

## Task 11: TurtlePanel（出题 / 提问 / 猜汤底 / 放弃）

**Files:** Modify `src/modules/reasoning/TurtlePanel.tsx`；`src/App.css`

- [x] **Step 11.1** `generating → genJob` + 派生布尔；`busy` 布尔保留，旁挂 `const [busyJob, setBusyJob] = useState<string | null>(null)`（ask/guess/abandon 互斥共用）。
- [x] **Step 11.2** `generate`：`window.api.turtle.generate(genJob, preference as …)`；catch 加取消分支 `toast('已取消')`（在 handleErr 之前）。
- [x] **Step 11.3** `ask`：`const jobId = crypto.randomUUID(); setBusy(true); setBusyJob(jobId)`；`window.api.turtle.ask(jobId, gameData.gameId, q)`；catch 在既有乐观回滚之后分支：取消 → `toast('已取消')`，否则 `handleErr(e)`（回滚逻辑不动，主进程未落库该问答，回滚即与库一致）；finally `setBusy(false); setBusyJob(null)`。
- [x] **Step 11.4** `guess`：同模式 `window.api.turtle.guess(jobId, gameData.gameId, g)`；catch 取消 → `toast('已取消')`（guessText/guessOpen 未动，可改后重交）。
- [x] **Step 11.5** `doAbandon`（补接）：`window.api.turtle.abandon(jobId, abandonTarget.gameId)`；该链路的取消只影响点评段（降级固定文案、终局照常完成，见 Task 4 说明），渲染层走成功路径，catch 无需取消分支，保持 handleErr。
- [x] **Step 11.6** 出题取消按钮：库视图工具卡「来 3 碗汤」按钮后（`genJob` 时，通则同款）。
- [x] **Step 11.7** 对局「裁判思考中…」行内停止按钮（覆盖 ask/guess/abandon 三态）：

```tsx
            {busy && (
              <div className="rs-msg ai rs-thinking">
                <span className="material-symbols-outlined spin">progress_activity</span>
                裁判思考中…
                {busyJob && (
                  <button
                    className="btn btn-ghost rs-stop"
                    onClick={() => void window.api.ai.cancel(busyJob)}
                    title="取消本次判答"
                  >
                    停止
                  </button>
                )}
              </div>
            )}
```

- [x] **Step 11.8** App.css 末尾追加：

```css
/* 裁判思考行内的停止按钮（260908 全局取消） */
.rs-stop {
  margin-left: 10px;
  padding: 2px 12px;
  font-size: 0.85em;
}
```

## Task 12: WallPanel（每日题：出题 / 作答）

**Files:** Modify `src/modules/reasoning/WallPanel.tsx`

- [x] **Step 12.1** `loading → ensureJob`、`submitting → submitJob` + 派生布尔（变量名 `loading`/`submitting` 以派生量保留，JSX 零改动）。
- [x] **Step 12.2** `ensure`：`setToday(await window.api.wall.ensureToday(ensureJob))`；catch 加取消分支 `toast('已取消')`（today 保持 null → 显示「今日暂无题」，重新进入思维墙可再出题）。
- [x] **Step 12.3** `submit`：`window.api.wall.answer(submitJob, today.puzzleId, a)`；catch 取消 → `toast('已取消')`（判答未落库，题目仍是 answering 态可重交）。
- [x] **Step 12.4** 取消按钮两处：①「出题中…」empty-state 内（同 Task 8 测一测样式，取消 `ensureJob`）；②作答卡按钮行「判定中…」旁（`submitJob` 时，通则同款）。

## Task 13: PracticePanel（练习场：出题 / 判答）

**Files:** Modify `src/modules/reasoning/PracticePanel.tsx`

- [x] **Step 13.1** `loading → newJob`、`submitting → submitJob` + 派生布尔。
- [x] **Step 13.2** `newPuzzle`：`window.api.wall.practiceNew(newJob, pref, typePref === 'random' ? undefined : typePref)`；catch 取消 → `toast('已取消')`。
- [x] **Step 13.3** `submit`：`window.api.wall.practiceAnswer(submitJob, practice.id, a)`；catch 在 PRACTICE_GONE 分支之外加取消分支 `toast('已取消')`（entry 未删，可重交）。
- [x] **Step 13.4** 取消按钮两处：控制行「来一道」按钮后（`newJob` 时）；作答卡「判定中…」旁（`submitJob` 时）。

## Task 14: BankPanel（题库作答）

**Files:** Modify `src/modules/reasoning/BankPanel.tsx`

- [x] **Step 14.1** `submitting → submitJob` + 派生布尔。
- [x] **Step 14.2** `submit`：`window.api.wall.bankAnswer(submitJob, current.id, a)`；catch 取消 → `toast('已取消')`（判答未落库，todo 态保留可重交）。
- [x] **Step 14.3** 作答卡按钮行「判定中…」旁取消按钮（`submitJob` 时，通则同款）。

## Task 15: ZhijijiModule（AI 起答 + 内嵌追问栏对话与 /compact）

**Files:** Modify `src/modules/zhijiji/ZhijijiModule.tsx`；`src/components/AiSidebar.css`

- [x] **Step 15.1** 主模块 `creating → createJob` + 派生布尔；`createQuestion`：`window.api.zhijiji.createQuestion(createJob, t, parseTagInput(addTags), addAiInit || undefined)`；catch 取消分支 `toast('已取消')`（在 LLM_NOT_CONFIGURED 分支之前或之后均可，须存在）。新问题弹窗 footer 在 `createJob` 时旁挂取消按钮（`ai.cancel(createJob)`，通则同款）。
- [x] **Step 15.2** `ZhijijiChat.sendText` 对话分支：`const jobId = crypto.randomUUID()` → `setSendJob(jobId)`（新增 `const [sendJob, setSendJob] = useState<string | null>(null)`）→ `await window.api.ai.chat(jobId, t, 'zhijiji', sid, 'zhijiji')`；catch 加取消分支：重载消息替换乐观行、不加错误占位：

```ts
    } catch (e) {
      const msg = String((e as Error).message)
      if (msg.includes('LLM_NOT_CONFIGURED')) onNeedConfig()
      else if (msg.includes('已取消')) {
        // 取消：用户消息主进程已落库，重载替换乐观行；不产生 assistant 回复、不加错误占位
        if (sid != null && sidRef.current === sid) await load()
      } else
        setMessages((arr) => [
          ...arr,
          { id: -Date.now(), session_id: sid ?? -1, role: 'system', ai_module: null, content: `请求失败：${msg}`, created_at: '' }
        ])
    } finally {
      sendingRef.current = false
      setSending(false)
      setSendJob(null)
    }
```

- [x] **Step 15.3** `ZhijijiChat.sendText` /compact 分支：`window.api.aiSession.compact(jobId, sid)`（setSendJob 同步设）；catch 改为：

```ts
      } catch (e) {
        const msg = String((e as Error).message)
        toast(msg.includes('已取消') ? '已取消' : `压缩失败：${msg.slice(0, 80)}`)
      } finally {
        sendingRef.current = false
        setSending(false)
        setSendJob(null)
      }
```

- [x] **Step 15.4** 「思考中…」占位加停止按钮（对话与 /compact 共用 sendJob）：

```tsx
        {sending && (
          <div className="ai-msg assistant">
            <div className="ai-msg-role">AI</div>
            <div className="ai-msg-content">
              思考中…
              {sendJob && (
                <button
                  className="btn btn-ghost ai-stop"
                  onClick={() => void window.api.ai.cancel(sendJob)}
                  title="停止生成"
                >
                  <span className="material-symbols-outlined">stop_circle</span>
                  停止生成
                </button>
              )}
            </div>
          </div>
        )}
```

- [x] **Step 15.5** AiSidebar.css 末尾追加：

```css
/* 思考占位内的停止生成按钮（260908 全局取消，AiSidebar 与致知己追问栏共用） */
.ai-stop {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  margin-top: 6px;
  padding: 2px 12px;
  font-size: 0.85em;
}
```

## Task 16: MdDialog + WritingPanel（copilot）

**Files:** Modify `src/components/MdDialog.tsx`；`src/modules/wenbi/WritingPanel.tsx`

- [x] **Step 16.1** MdDialogProps 的 `copilot.request` 签名加第三参（设计定案）：

```ts
  copilot?: {
    request: (
      action: 'draft' | 'continue' | 'polish' | 'rewrite',
      ctx: { title: string; content: string; selection?: string },
      jobId: string
    ) => Promise<string>
    /** 请求失败回调（渲染层区分 LLM_NOT_CONFIGURED 与普通错误）；取消不进此回调 */
    onError: (e: unknown) => void
  }
```

- [x] **Step 16.2** MdDialog 内新增 `const [copilotJob, setCopilotJob] = useState<string | null>(null)`；`runCopilot` 替换：

```ts
  const runCopilot = async (action: 'draft' | 'continue' | 'polish' | 'rewrite'): Promise<void> => {
    if (!copilot || copilotBusy) return
    const sel = draft.slice(selRef.current.start, selRef.current.end)
    if ((action === 'polish' || action === 'rewrite') && !sel.trim()) return
    const jobId = crypto.randomUUID()
    setCopilotBusy(action)
    setCopilotJob(jobId)
    try {
      const text = await copilot.request(action, { title, content: draft, selection: sel || undefined }, jobId)
      setSuggestion({ action, text, selStart: selRef.current.start, selEnd: selRef.current.end })
    } catch (e) {
      // 取消：建议卡不出现、编辑态原样，静默即可（取消按钮即反馈）
      if (!String((e as Error).message).includes('已取消')) copilot.onError(e)
    } finally {
      setCopilotBusy(null)
      setCopilotJob(null)
    }
  }

  const cancelCopilot = (): void => {
    if (copilotJob) void window.api.ai.cancel(copilotJob)
  }
```

- [x] **Step 16.3** 工具行「协笔生成中…」文字替换为取消按钮：

```tsx
                    {copilotBusy && (
                      <button className="btn btn-ghost" onClick={cancelCopilot} title="取消本次生成">
                        <span className="material-symbols-outlined">stop_circle</span>
                        取消
                      </button>
                    )}
```

（原 `{copilotBusy && <span className="module-sub">协笔生成中…</span>}` 删除。）

- [x] **Step 16.4** WritingPanel 的 copilot 扩展位包装：

```tsx
        copilot={
          openDoc
            ? {
                request: (action, ctx, jobId) =>
                  window.api.wenbi.copilot(jobId, openDoc.id, action, ctx.selection),
                onError: onCopilotError
              }
            : undefined
        }
```

## Task 17: AiSidebar（对话 + /compact）

**Files:** Modify `src/components/AiSidebar.tsx`

- [x] **Step 17.1** 新增 `const [sendJob, setSendJob] = useState<string | null>(null)`（`sending` 布尔与 `sendingRef` 守卫保留）。
- [x] **Step 17.2** `sendText` /compact 分支：`const jobId = crypto.randomUUID()` → `setSendJob(jobId)` → `window.api.aiSession.compact(jobId, sid)`；catch 改 `toast(msg.includes('已取消') ? '已取消' : \`压缩失败：${msg.slice(0, 80)}\`)`；finally 清 sendJob。
- [x] **Step 17.3** `sendText` 对话分支：`const jobId = crypto.randomUUID()` → `setSendJob(jobId)` → `await window.api.ai.chat(jobId, t, currentModule, sid, channel)`；catch 加取消分支（用户消息已落库 → 重载替换乐观行，不加「请求失败」占位）：

```ts
    } catch (e) {
      const msg = String((e as Error).message)
      if (msg.includes('LLM_NOT_CONFIGURED')) {
        setNeedConfig(true)
      } else if (msg.includes('已取消')) {
        // 取消：用户消息已落库，重载替换乐观行；不产生 assistant 回复、不加错误占位
        if (sid != null) await loadMessages(sid)
      } else {
        setMessages((arr) => [
          ...arr,
          { id: -Date.now(), session_id: sid ?? -1, role: 'system', ai_module: null, content: `请求失败：${msg}`, created_at: '' }
        ])
      }
    } finally {
      sendingRef.current = false
      setSending(false)
      setSendJob(null)
    }
```

- [x] **Step 17.4** 消息流尾部 assistant 占位加「停止生成」（复用 `.ai-stop`，Task 15.5 已加样式）：

```tsx
          {sending && (
            <div className="ai-msg assistant">
              <div className="ai-msg-role">{AI_NAME}</div>
              <div className="ai-msg-content">
                思考中…
                {sendJob && (
                  <button
                    className="btn btn-ghost ai-stop"
                    onClick={() => void window.api.ai.cancel(sendJob)}
                    title="停止生成"
                  >
                    <span className="material-symbols-outlined">stop_circle</span>
                    停止生成
                  </button>
                )}
              </div>
            </div>
          )}
```

## Task 18: ProfileModule（llm:test / mcp:research / mcp:test）

**Files:** Modify `src/modules/profile/ProfileModule.tsx`

- [x] **Step 18.1** `testing → testJob`、`researching → researchJob`、`mcpTesting → mcpTestJob` + 派生布尔（`llm:models` 获取模型不接，保持 loadingModels 布尔）。
- [x] **Step 18.2** `testLlm`：`window.api.llm.test(testJob, llmForm)`；catch 取消分支 `toast('已取消')`（不弹连接失败）。LLM 弹窗 footer「测试连接」左侧加取消按钮（`testJob` 时）。
- [x] **Step 18.3** `doResearch`：`window.api.mcp.research(researchJob, aiQuery.name.trim())`；catch 取消分支 `toast('已取消')`（researchErr 不设值）。研究弹窗 footer「开始研究/研究中…」左侧加取消按钮（`researchJob` 时）。
- [x] **Step 18.4** `doTestMcp`：`window.api.mcp.test(mcpTestJob, { name, url, authType, apiKey })`；catch 取消分支 `toast('已取消')`。footer「测试连接/测试中…」左侧加取消按钮（`mcpTestJob` 时）。三处取消按钮均通则同款（`btn` + `stop_circle` + 取消）。

## Task 19: 收尾——全量验证 + 开发日志 + 机制归档

- [x] **Step 19.1** `npm run typecheck`（双配置）→ 通过。
- [x] **Step 19.2** `npm run build` → 通过。
- [x] **Step 19.3** 开发日志：在 `docs/log/260908.md` 追加小节（标题下 `> 创建于 YYYY-MM-DD HH:MM`，bash date 取实际时间），记录：机制概要、23 handler 清单与 3 处补接（wall:answer / turtle:abandon / aiSession:compact）、终局点评取消=降级语义、组件 13 文件、typecheck/build 结果、待人工冒烟。
- [x] **Step 19.4** 机制归档提示：本设计/计划落地验收后，由开发者或下轮会话把 design 与 plan 移入 `docs/project/全局/archive/`（按 docs/project/README.md 机制）。
- [x] **Step 19.5** 人工冒烟清单（设计 §验证 原文，交开发者）：
  - 每个模块任选一处——生成中点取消 → 立即停止（网络面板可见 aborted）、toast「已取消」、按钮恢复、可立即重试成功；
  - 格言/灵感取消后库内无残留半批；
  - 海龟汤取消后汤库仅完整碗；
  - 对话取消后无 assistant 占位；
  - copilot 取消后建议卡不出现编辑态原样；
  - 429 退避等待中取消立即生效；
  - 取消已结束的任务（重复点）无害静默。

---

## Self-Review 记录

- **覆盖核对**：设计机制层 5 节 → Task 1/2/5/6；接入清单 23 handler → Task 5；UI 层三节 → Task 7-18（10 组件条目 → 13 文件，偏差已在文首记录）；「明确不做」三项未越界（signal 只加等待段，成功路径与解析容错未动）。
- **类型一致性**：`beginJob/endJob/cancelJob/ensureNotCancelled` 命名全文一致；`已取消` 字符串口径一致；preload 与 api.d.ts 逐方法镜像；services signal 一律末位可选参。
- **已知取舍**：①格言补足轮取消——首轮已入库条目按「已写库的保留」留库（设计验证项「无残留半批」指主路径：取消发生在首轮 LLM 等待段）；②终局点评取消=降级完成终局（Task 4 Step 4.2 写死）；③llm:models 不接（设计范围外）。


