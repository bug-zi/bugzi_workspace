# 学习/生活双模式 + 工作台入口迁移 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 左栏按「学习/生活」双模式过滤模块入口（工作台迁至总导览后第 2 位），右下角新增单按钮模式切换，总导览随模式换块。

**Architecture:** 纯渲染层。模式状态存 settings 三键（`app_mode` / `mode_last_learn` / `mode_last_life`），`App.tsx` 持 `appMode` state 并以 `MODULES[].mode` 字段过滤左栏；主栏 keep-alive 全量挂载不变；总导览经 prop 接模式条件渲染块。零 DB 迁移、零主进程改动。

**Tech Stack:** Electron 44 + React 19 + TypeScript（electron-vite）。

**依据设计:** `docs/project/全局/2026-09-22-学习生活双模式-design.md`（六项已拍板决策，不得偏离）。

**项目纪律（覆盖通用流程）：**
- **禁止 AI 执行任何 git 操作**——本计划不含 commit 步骤，全部改动完成后由开发者自行提交。
- 本项目渲染层无单测基建，验证口径 = `npm run typecheck`（双 tsconfig）+ `npm run build` + dev 冒烟（开发者人工验收）。各代码任务以 typecheck 为闸。

---

### Task 1: shared/types.ts — ModuleMode 类型 + settings 三键

**Files:**
- Modify: `src/shared/types.ts`

- [ ] **Step 1.1: 新增 ModuleMode 类型**（紧接 `ModuleId` 类型定义之后、`ModuleNavDetail` 之前插入）

```ts
// 学习/生活双模式（优化建议区 260922）：左栏两套入口视图 + 总导览随模式换块；
// 模式归属见 App.tsx MODULES[].mode（常驻/学习/生活）
export type ModuleMode = 'learn' | 'life'
```

- [ ] **Step 1.2: SettingsKeys 增三键**（插在 `CloseAction: 'close_action',` 之后）

```ts
  // 学习/生活双模式（优化建议区 260922）：当前模式（'learn'|'life'，缺省 learn）+ 各模式 last 专属模块（MainView 字符串，常驻不记）
  AppMode: 'app_mode',
  ModeLastLearn: 'mode_last_learn',
  ModeLastLife: 'mode_last_life',
```

- [ ] **Step 1.3: typecheck**

Run: `npm run typecheck`
Expected: 双 tsconfig 均无错误

---

### Task 2: App.tsx — 模块归属标记 + 模式状态/切换/记忆 + 左栏过滤 + 工作台迁移

**Files:**
- Modify: `src/App.tsx`（MODULES 数组、import、Shell 内状态与左栏渲染）

- [ ] **Step 2.1: import 增加 ModuleMode**

old:
```ts
import type { AiChannel, ModuleId } from './shared/types'
```
new:
```ts
import type { AiChannel, ModuleId, ModuleMode } from './shared/types'
```

- [ ] **Step 2.2: MODULES 数组加 mode 字段**

old（第 38-52 行整块）:
```ts
// 左栏模块顺序（260908 重排；260911 格言库并入文笔坊 12→11 项；260911 学习库置顶 11→12 项；260912 优化建议区第41轮重排；260912 收藏夹+藏书架合并藏阅阁 12→11 项；260912 总导览置顶 11→12 项；260916 藏阅阁更名图书馆）
const MODULES: { id: ModuleId; label: string; icon: string }[] = [
  { id: 'zonglan', label: '总导览', icon: 'space_dashboard' },
  { id: 'learn', label: '学习库', icon: 'school' },
  { id: 'wiki', label: '万象库', icon: 'public' },
  { id: 'zhijiji', label: '致知己', icon: 'self_improvement' },
  { id: 'inspirations', label: '灵感泉', icon: 'lightbulb' },
  { id: 'wenbi', label: '文笔坊', icon: 'history_edu' },
  { id: 'feed', label: '信息源', icon: 'rss_feed' },
  { id: 'zangyue', label: '图书馆', icon: 'collections_bookmark' },
  { id: 'reasoning', label: '推理角', icon: 'psychology' },
  { id: 'ledger', label: '记账本', icon: 'account_balance_wallet' },
  { id: 'recycle', label: '回收站', icon: 'delete' },
  { id: 'profile', label: '个人档', icon: 'person' }
]
```
new:
```ts
// 左栏模块顺序（260908 重排；260912 总导览置顶 11→12 项；260916 藏阅阁更名图书馆；
// 260922 双模式：mode 标记归属，左栏按当前模式过滤——学习=工作台(特殊项)/学习库/万象库/文笔坊/信息源，
// 生活=致知己/灵感泉/图书馆/推理角/记账本，常驻=总导览/音乐吧(特殊项)/回收站/个人档；
// 各组内保持本数组相对顺序，主栏 keep-alive 挂载仍用全量数组不受过滤影响）
const MODULES: { id: ModuleId; label: string; icon: string; mode: 'learn' | 'life' | 'common' }[] = [
  { id: 'zonglan', label: '总导览', icon: 'space_dashboard', mode: 'common' },
  { id: 'learn', label: '学习库', icon: 'school', mode: 'learn' },
  { id: 'wiki', label: '万象库', icon: 'public', mode: 'learn' },
  { id: 'zhijiji', label: '致知己', icon: 'self_improvement', mode: 'life' },
  { id: 'inspirations', label: '灵感泉', icon: 'lightbulb', mode: 'life' },
  { id: 'wenbi', label: '文笔坊', icon: 'history_edu', mode: 'learn' },
  { id: 'feed', label: '信息源', icon: 'rss_feed', mode: 'learn' },
  { id: 'zangyue', label: '图书馆', icon: 'collections_bookmark', mode: 'life' },
  { id: 'reasoning', label: '推理角', icon: 'psychology', mode: 'life' },
  { id: 'ledger', label: '记账本', icon: 'account_balance_wallet', mode: 'life' },
  { id: 'recycle', label: '回收站', icon: 'delete', mode: 'common' },
  { id: 'profile', label: '个人档', icon: 'person', mode: 'common' }
]
```

- [ ] **Step 2.3: Shell 内新增 appMode 状态**（terminalNewTabSignal 之后）

old:
```ts
  const [terminalOpen, setTerminalOpen] = useState(false)
  const [terminalNewTabSignal, setTerminalNewTabSignal] = useState(0)
```
new:
```ts
  const [terminalOpen, setTerminalOpen] = useState(false)
  const [terminalNewTabSignal, setTerminalNewTabSignal] = useState(0)
  // 学习/生活双模式（优化建议区 260922）：当前模式；启动经下方 effect 恢复（主栏仍固定落总导览）
  const [appMode, setAppMode] = useState<ModuleMode>('learn')
```

- [ ] **Step 2.4: 启动恢复模式**（「启动恢复上次展开的面板」effect 之后追加）

old:
```ts
  // 启动恢复上次展开的面板（默认 debugzi，与既有行为一致）
  useEffect(() => {
    void window.api.settings.get(SettingsKeys.RightPanelExpanded).then((v) => {
      if (v === 'draft') setRightPanel('draft')
      else if (v === 'canvas') setRightPanel('canvas')
      else if (v === 'files') setRightPanel('files')
      else if (v === '') setRightPanel(null)
      else setRightPanel('ai')
    })
  }, [])
```
new:
```ts
  // 启动恢复上次展开的面板（默认 debugzi，与既有行为一致）
  useEffect(() => {
    void window.api.settings.get(SettingsKeys.RightPanelExpanded).then((v) => {
      if (v === 'draft') setRightPanel('draft')
      else if (v === 'canvas') setRightPanel('canvas')
      else if (v === 'files') setRightPanel('files')
      else if (v === '') setRightPanel(null)
      else setRightPanel('ai')
    })
  }, [])

  // 双模式恢复（优化建议区 260922）：左栏按上次模式过滤；主栏仍固定落总导览（现状不变）
  useEffect(() => {
    void window.api.settings.get(SettingsKeys.AppMode).then((v) => {
      if (v === 'life') setAppMode('life')
    })
  }, [])
```

- [ ] **Step 2.5: modeOfView + last 记忆 + switchMode**（activateModule 定义之后追加）

old:
```ts
  // 切换模块 = 激活目标模块（常驻组件监听此事件自行刷新/启停计时）
  const activateModule = useCallback((id: MainView) => {
    const prev = moduleRef.current
    setModule(id)
    moduleRef.current = id
    if (prev !== id) {
      window.dispatchEvent(new CustomEvent(MODULE_DEACTIVATED_EVENT, { detail: prev }))
    }
    window.dispatchEvent(new CustomEvent(MODULE_ACTIVATED_EVENT, { detail: id }))
  }, [])
```
new（尾部追加三段）:
```ts
  // 切换模块 = 激活目标模块（常驻组件监听此事件自行刷新/启停计时）
  const activateModule = useCallback((id: MainView) => {
    const prev = moduleRef.current
    setModule(id)
    moduleRef.current = id
    if (prev !== id) {
      window.dispatchEvent(new CustomEvent(MODULE_DEACTIVATED_EVENT, { detail: prev }))
    }
    window.dispatchEvent(new CustomEvent(MODULE_ACTIVATED_EVENT, { detail: id }))
  }, [])

  // 视图所属模式（双模式）：'agent'（任务中心）视为学习专属，'noise'（音乐吧）常驻，其余查 MODULES
  const modeOfView = useCallback((id: MainView): 'learn' | 'life' | 'common' => {
    if (id === 'agent') return 'learn'
    if (id === 'noise') return 'common'
    return MODULES.find((m) => m.id === id)?.mode ?? 'common'
  }, [])

  // 模式 last 专属模块记录（双模式）：仅专属模块记账，常驻（总导览/音乐吧/回收站/个人档）不记——
  // 切走再切回 = 回到该模式上次工作现场
  useEffect(() => {
    const m = modeOfView(module)
    if (m === 'common') return
    void window.api.settings.set(m === 'learn' ? SettingsKeys.ModeLastLearn : SettingsKeys.ModeLastLife, module)
  }, [module, modeOfView])

  // 模式切换（双模式）：写 app_mode → 主栏落到 landTo（深链等显式指定）或目标模式 last 专属模块（无记录落总导览）。
  // landTo 必须同步 activateModule——不能走异步 get 回来再切，否则会晚于调用方的 activateModule 把主栏抢走
  const switchMode = useCallback(
    (next: ModuleMode, landTo?: MainView): void => {
      setAppMode(next)
      void window.api.settings.set(SettingsKeys.AppMode, next)
      if (landTo) {
        activateModule(landTo)
        return
      }
      void window.api.settings
        .get(next === 'learn' ? SettingsKeys.ModeLastLearn : SettingsKeys.ModeLastLife)
        .then((v) => activateModule((v || 'zonglan') as MainView))
    },
    [activateModule]
  )
```

- [ ] **Step 2.6: 左栏过滤 + 工作台呼吸灯迁移**

old（sidebar 内首个 map，第 263-269 行）:
```tsx
          {MODULES.map((m) => (
            <Fragment key={m.id}>
              {/* 超级工作台呼吸灯（2.0 批次C）：列于记账本与音乐吧之间，点击进任务中心 */}
              {m.id === 'recycle' && (
                <AgentBreathLight active={module === 'agent'} onOpen={() => activateModule('agent')} />
              )}
              {/* 音乐吧控件列于记账本与回收站之间（260908 进列表；260915 更名音乐吧） */}
```
new:
```tsx
          {MODULES.filter((m) => m.mode === 'common' || m.mode === appMode).map((m) => (
            <Fragment key={m.id}>
              {/* 超级工作台呼吸灯（2.0 批次C；260922 双模式迁至总导览后第 2 位——随学习库行插位，学习专属故生活模式随行隐藏）：点击进任务中心 */}
              {m.id === 'learn' && (
                <AgentBreathLight active={module === 'agent'} onOpen={() => activateModule('agent')} />
              )}
              {/* 音乐吧控件（常驻，双模式均显示）：列于回收站之前（260908 进列表；260915 更名音乐吧） */}
```

注意：**主栏 keep-alive 的第二个 `{MODULES.map((m) => (`（第 306 行附近）保持原样不动**——模式只控左栏可见性。

- [ ] **Step 2.7: typecheck**

Run: `npm run typecheck`
Expected: 无错误（本任务后 appMode 尚无使用方，若 lint 报未使用不会——已用于 filter）

---

### Task 3: App.tsx — 深链模式跟随

**Files:**
- Modify: `src/App.tsx`（两个事件监听 effect）

- [ ] **Step 3.1: MODULE_NAVIGATE_EVENT 处理器加模式跟随**

old:
```ts
  // 深链集中切换（260915 优化区：原仅总导览 onNavigate；现在任何常驻模块都可派发事件跳转，
  // 如学习库深挖完成后轻提示点击跨模块跳回）：目标模块内部视图由其 useModuleNavigate 自行处理
  useEffect(() => {
    const onNav = (e: Event): void => {
      const detail = (e as CustomEvent<{ module: ModuleId }>).detail
      if (detail && detail.module) activateModule(detail.module)
    }
    window.addEventListener(MODULE_NAVIGATE_EVENT, onNav)
    return () => window.removeEventListener(MODULE_NAVIGATE_EVENT, onNav)
  }, [activateModule])
```
new:
```ts
  // 深链集中切换（260915 优化区：原仅总导览 onNavigate；现在任何常驻模块都可派发事件跳转，
  // 如学习库深挖完成后轻提示点击跨模块跳回）：目标模块内部视图由其 useModuleNavigate 自行处理；
  // 双模式（260922）：目标模块不在当前模式时自动切到目标模式——深链是明确意图，模式跟着走
  // （回收站恢复跳转同规则）；landTo 显式传深链目标，防异步 last 落点抢走深链跳转
  useEffect(() => {
    const onNav = (e: Event): void => {
      const detail = (e as CustomEvent<{ module: ModuleId }>).detail
      if (!detail || !detail.module) return
      const target = modeOfView(detail.module)
      if (target !== 'common' && target !== appMode) {
        switchMode(target, detail.module)
        return
      }
      activateModule(detail.module)
    }
    window.addEventListener(MODULE_NAVIGATE_EVENT, onNav)
    return () => window.removeEventListener(MODULE_NAVIGATE_EVENT, onNav)
  }, [activateModule, appMode, switchMode, modeOfView])
```

- [ ] **Step 3.2: 任务中心直达加模式跟随**

old:
```ts
  // 任务中心直达（2.0 批次C：呼吸灯/总导览聚合块；'agent' 非 ModuleId，走独立事件不动公共类型）
  useEffect(() => {
    const onOpenAgent = (): void => activateModule('agent')
    window.addEventListener('bugzi:open-agent-center', onOpenAgent)
    return () => window.removeEventListener('bugzi:open-agent-center', onOpenAgent)
  }, [activateModule])
```
new:
```ts
  // 任务中心直达（2.0 批次C：呼吸灯/总导览聚合块；'agent' 非 ModuleId，走独立事件不动公共类型）；
  // 双模式（260922）：'agent' 学习专属——生活模式下直达自动切回学习模式（landTo='agent' 同步落位）
  useEffect(() => {
    const onOpenAgent = (): void => {
      if (appMode === 'life') switchMode('learn', 'agent')
      else activateModule('agent')
    }
    window.addEventListener('bugzi:open-agent-center', onOpenAgent)
    return () => window.removeEventListener('bugzi:open-agent-center', onOpenAgent)
  }, [activateModule, appMode, switchMode])
```

- [ ] **Step 3.3: typecheck**

Run: `npm run typecheck`
Expected: 无错误

---

### Task 4: App.tsx — 右下角模式切换按钮

**Files:**
- Modify: `src/App.tsx`（right-col 按钮列尾部）

- [ ] **Step 4.1: 主题按钮之后追加模式按钮**（flex 纵列中 DOM 靠后 = 更靠下，落在最底位）

old:
```tsx
          {rightPanel === null && (
            <button
              className="right-col-theme"
              onClick={toggleTheme}
              title={theme === 'light' ? '切到深色' : '切到浅色'}
            >
              <span className="material-symbols-outlined">{theme === 'light' ? 'dark_mode' : 'light_mode'}</span>
            </button>
          )}
```
new（尾部追加一块）:
```tsx
          {rightPanel === null && (
            <button
              className="right-col-theme"
              onClick={toggleTheme}
              title={theme === 'light' ? '切到深色' : '切到浅色'}
            >
              <span className="material-symbols-outlined">{theme === 'light' ? 'dark_mode' : 'light_mode'}</span>
            </button>
          )}
          {/* 模式切换（优化建议区 260922 双模式）：右下角最底位（原主题按钮位），其余按钮上移一位；
              单按钮往返，图标=将切去的模式（与主题按钮「显示目标」约定一致）；显隐同主题按钮（四面板收起态） */}
          {rightPanel === null && (
            <button
              className="right-col-mode"
              onClick={() => switchMode(appMode === 'learn' ? 'life' : 'learn')}
              title={appMode === 'learn' ? '切换到生活模式' : '切换到学习模式'}
            >
              <span className="material-symbols-outlined">{appMode === 'learn' ? 'home' : 'school'}</span>
            </button>
          )}
```

---

### Task 5: App.css — .right-col-mode 样式

**Files:**
- Modify: `src/App.css`

- [ ] **Step 5.1: 新增样式块**（`.right-col-noise .material-symbols-outlined.noise-playing` 规则之后）

old:
```css
.right-col-noise .material-symbols-outlined.noise-playing {
  color: var(--color-primary);
}
```
new:
```css
.right-col-noise .material-symbols-outlined.noise-playing {
  color: var(--color-primary);
}

/* ===== 模式切换按钮（优化建议区 260922 双模式）：右下角最底位（原主题按钮位），其余按钮上移一位 =====
   单按钮往返：图标=将切去的模式（与主题按钮「显示目标」约定一致）；显隐同主题按钮（四面板收起态）；
   同款钉宽防撑宽 right-col（第29轮原理） */
.right-col-mode {
  border: none;
  border-left: 1px solid var(--color-border);
  border-top: 1px solid var(--color-border);
  background: var(--color-surface);
  color: var(--color-text);
  padding: 9px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  width: var(--ai-width-collapsed);
}
.right-col-mode:hover {
  background: var(--color-primary-soft);
}
```

---

### Task 6: OverviewModule — 随模式换块 + App.tsx 传 prop

**Files:**
- Modify: `src/modules/zonglan/OverviewModule.tsx`
- Modify: `src/App.tsx`（OverviewModule 用法一行）

- [ ] **Step 6.1: import 类型**

old:
```ts
import type { ModuleId, MottoRecord, ChallengeDailyView, HeatmapDay, WhoamiGetResult } from '../../shared/types'
```
new:
```ts
import type { ModuleId, ModuleMode, MottoRecord, ChallengeDailyView, HeatmapDay, WhoamiGetResult } from '../../shared/types'
```

- [ ] **Step 6.2: 组件签名接 mode prop**

old:
```tsx
export default function OverviewModule() {
```
new:
```tsx
export default function OverviewModule({ mode }: { mode: ModuleMode }) {
```

- [ ] **Step 6.3: 头部注释补双模式说明**

old:
```tsx
// 总导览（左栏置顶 · 启动默认页，260912 新功能开发区）：今日驱动——问候 +
// 每日要求 / 待消化 / 每日一题 / 今日挑战（260916）/ 续读 今日块 + 轻数字条 +
// 热力图卡（260916）+ 底部格言轮播。纯渲染层并行聚合既有 IPC（零 AI、逐块独立
// catch；挑战/热力图走专属 challenge/overview 通道）；深链 = 派发 MODULE_NAVIGATE_EVENT，
// App 集中切模块、目标模块 useModuleNavigate 切内部视图（260915 起单一导航路径）。
```
new:
```tsx
// 总导览（左栏置顶 · 启动默认页，260912 新功能开发区）：今日驱动——问候 +
// 每日要求 / 待消化 / 每日一题 / 今日挑战（260916）/ 续读 今日块 + 轻数字条 +
// 热力图卡（260916）+ 底部格言轮播。纯渲染层并行聚合既有 IPC（零 AI、逐块独立
// catch；挑战/热力图走专属 challenge/overview 通道）；深链 = 派发 MODULE_NAVIGATE_EVENT，
// App 集中切模块、目标模块 useModuleNavigate 切内部视图（260915 起单一导航路径）。
// 双模式（260922）：随 prop 换块——公共=问候/我是谁/挑战/热力图；学习=今日学习/待学习区/
// 超级工作台/信息源未读/格言轮播；生活=每日一题/续读/本月支出+十二问题。块内逻辑与加载照旧。
```

- [ ] **Step 6.4: 「今日学习」块包学习条件**

old:
```tsx
      {/* 今日学习（整卡深链「今日学习」tab；260915 起显示每日要求状态行） */}
      <button className="card zl-row" onClick={() => go('learn', 'today')} title="进入学习库·今日学习">
```
（此块自该注释起至对应 `</button>` 止，整体包一层）
new:
```tsx
      {/* 今日学习（整卡深链「今日学习」tab；260915 起显示每日要求状态行）——学习模式块 */}
      {mode === 'learn' && (
        <button className="card zl-row" onClick={() => go('learn', 'today')} title="进入学习库·今日学习">
          {/* ……块内 JSX 原样保留…… */}
        </button>
      )}
```
即：old_string 取「注释行 + 完整 `<button>…</button>` 块」，new_string 为 `{mode === 'learn' && (` 包裹并整体缩进两格。

- [ ] **Step 6.5: 「待学习区」块包学习条件**（同法，整块包 `{mode === 'learn' && (…)}`，注释补「——学习模式块」）

- [ ] **Step 6.6: 「每日一题」块包生活条件**（整块包 `{mode === 'life' && (…)}`，注释补「——生活模式块」）

- [ ] **Step 6.7: 「超级工作台」块包学习条件**（整块包 `{mode === 'learn' && (…)}`，注释补「——学习模式块」）

- [ ] **Step 6.8: 「续读」块包生活条件**

old:
```tsx
      {/* 续读（无在读书显引导） */}
      {reading ? (
```
new:
```tsx
      {/* 续读（无在读书显引导）——生活模式块 */}
      {mode === 'life' &&
        (reading ? (
```
（块尾对应的三元闭合 `)}` 改为 `))}`，即整体成为 `{mode === 'life' && (reading ? … : …)}`；缩进随包裹调整）

- [ ] **Step 6.9: 轻数字条按模式拆源**

old:
```tsx
      {/* 轻数字条（点击直达对应模块） */}
      <div className="zl-numbar">
        <button className="zl-num" onClick={() => go('feed')} title="进入信息源">
          <span className="zl-num-val">{feedUnread ?? '--'}</span>
          <span className="module-sub">信息源未读</span>
        </button>
        <button className="zl-num" onClick={() => go('ledger')} title="进入记账本">
          <span className="zl-num-val">{monthExpense ?? '--'}</span>
          <span className="module-sub">本月支出</span>
        </button>
        <button className="zl-num" onClick={() => go('zhijiji', 'twelve')} title="进入致知己·十二问题">
          <span className="zl-num-val">{twelve ?? '--'}</span>
          <span className="module-sub">十二问题</span>
        </button>
      </div>
```
new:
```tsx
      {/* 轻数字条（点击直达对应模块；260922 双模式拆源：学习=信息源未读，生活=本月支出+十二问题） */}
      <div className="zl-numbar">
        {mode === 'learn' && (
          <button className="zl-num" onClick={() => go('feed')} title="进入信息源">
            <span className="zl-num-val">{feedUnread ?? '--'}</span>
            <span className="module-sub">信息源未读</span>
          </button>
        )}
        {mode === 'life' && (
          <>
            <button className="zl-num" onClick={() => go('ledger')} title="进入记账本">
              <span className="zl-num-val">{monthExpense ?? '--'}</span>
              <span className="module-sub">本月支出</span>
            </button>
            <button className="zl-num" onClick={() => go('zhijiji', 'twelve')} title="进入致知己·十二问题">
              <span className="zl-num-val">{twelve ?? '--'}</span>
              <span className="module-sub">十二问题</span>
            </button>
          </>
        )}
      </div>
```

- [ ] **Step 6.10: 底部格言轮播包学习条件**

old:
```tsx
      {motto && (
```
new:
```tsx
      {mode === 'learn' && motto && (
```
（其闭合 `)}` 不变——条件表达式就地扩展）

- [ ] **Step 6.11: App.tsx 传 prop**

old:
```tsx
              {m.id === 'zonglan' && <OverviewModule />}
```
new:
```tsx
              {m.id === 'zonglan' && <OverviewModule mode={appMode} />}
```

- [ ] **Step 6.12: typecheck**

Run: `npm run typecheck`
Expected: 无错误

---

### Task 7: 全量验证 + 冒烟清单

- [ ] **Step 7.1: typecheck 双配置**

Run: `npm run typecheck`
Expected: 两个 tsconfig 均通过

- [ ] **Step 7.2: build**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 7.3: dev 冒烟（开发者人工，`npm run dev`）**

按设计 §6 验收清单逐项：
1. 学习模式左栏 9 项（总导览→**工作台**→学习库→万象库→文笔坊→信息源→音乐吧→回收站→个人档），工作台呼吸灯三态（工作脉动/暂停灰/空闲静默）；
2. 切到生活：左栏 9 项（总导览→致知己→灵感泉→图书馆→推理角→记账本→音乐吧→回收站→个人档），工作台不可见；
3. 右下角自下而上：模式（新）→主题→音源→终端→AI 活动；四面板任一展开全列隐藏；图标显目标（学习模式显 home、生活显 school）；
4. 切换落点：学习库→切生活→落生活 last（首次落总导览）→再切回→回学习库；总导览/音乐吧/回收站/个人档停留不记 last；
5. 重启：模式恢复、主栏仍落总导览；
6. 深链跟随：生活模式下点 debugzi 中学习库深挖完成 toast / 回收站恢复学习模块条目 → 自动切学习模式并跳转；总导览聚合块仅在学习模式显示；
7. 总导览：学习模式=今日学习/待学习区/超级工作台/挑战/我是谁/数字条(信息源未读)/热力图/格言轮播；生活模式=每日一题/挑战/我是谁/续读/数字条(本月支出+十二问题)/热力图；切换即时刷新；
8. keep-alive：学习模式触发学习库 AI 生成→切生活→切回，任务不中断；
9. 双主题目检各一遍。

---

### Task 8: 文档同步

- [ ] **Step 8.1: dev log**——`bash date "+%y%m%d %H:%M"` 取执行日（2026-09-23 起算应为 260923），新建/追加 `docs/log/<执行日>.md` 小节：

```markdown
## 学习/生活双模式 + 工作台入口迁移 + 总导览随模式换块（优化建议区第50轮）

> 创建于 <执行日 YYYY-MM-DD HH:MM>

（完成说明：设计/计划文档路径 `docs/project/全局/2026-09-22-学习生活双模式-{design,plan}.md`；
改动文件 = src/shared/types.ts、src/App.tsx、src/App.css、src/modules/zonglan/OverviewModule.tsx 四个；
settings 三键 app_mode/mode_last_learn/mode_last_life 零 DB 迁移零主进程；
typecheck 双配置 + build 通过；待冒烟清单见设计 §6 / 计划 Task 7——按实际实施结果如实记录）
```

- [ ] **Step 8.2: 优化建议区归档**——`docs/project/优化建议区.md`：待完成区保留第 4 条（双模式背景，后面再说），第 1/2/3/5 条移入归档区**顶部**新小节（轮次编号顺延第 49 轮 → **第50轮（执行日 YYMMDD）：学习/生活双模式**），四条各附完成说明（要点：模块归属拍板表、工作台迁移、settings 三键与切换行为、总导览换块清单；结尾「**待冒烟**：见计划 Task 7 九项」），格式对齐历轮。

- [ ] **Step 8.3: CLAUDE.md 同步**（三处语义更新，执行时以文件现状为准定位）：
  1. 「三栏式布局」bullet 的左栏顺序括注 → 改为双模式描述：常驻（总导览/音乐吧/回收站/个人档）+ 学习专属（工作台第 2 位/学习库/万象库/文笔坊/信息源）+ 生活专属（致知己/灵感泉/图书馆/推理角/记账本），切换按钮右下角最底位单按钮往返；
  2. 「总导览」架构 bullet → 增补 260922 随模式换块（公共/学习/生活三组）；
  3. 项目性质/架构段涉及左栏入口计数处 → 补一句 260922 双模式过滤（工作台为 2.0 批次 C 既有入口的位置变更，不新增 ModuleId）。

- [ ] **Step 8.4: 归档预告（非本计划步骤）**——开发者冒烟确认通过后，AI 将 `docs/project/全局/2026-09-22-学习生活双模式-{design,plan}.md` 移入 `docs/project/全局/archive/`（落地归档铁律）；git 提交由开发者完成。
