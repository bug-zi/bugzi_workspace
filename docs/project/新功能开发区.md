# 新功能开发区

说明：该文档相当于是idea文件夹的一个辅助文档，就是所有想要新增的大大小小的功能模块都先初步写在这里，如果是大功能模块再到idea文件夹下立项细化说明，小功能模块的化就直接让AI完成开发即可。

---

### 待完成

### 归档区

- [X]  学习区添加播客台模块（小宇宙或其他播客订阅）
- [X]  我想要再进一步试着丰富音乐台中的音乐资源：主要是丰富优化白噪音使用体验这一块和添加一些最近比较流行的助眠视频中的那些触发音之类的。请你看看如何设计升级方案。（260921 已实施，`左侧边栏/常驻模块/音乐吧/2026-09-21-触发音与白噪音体验升级-design.md` + 同日 plan；brainstorm 决策——触发音混合来源（内置合成近似 + 导入 mp3/wav/ogg 补耳语/刷头类真人录音）、并入白噪音混音层为跨场景叠加层（随机间隔 2–8s 单发，不做独立声板页签）、立体声声像（每层声像滑杆 + 触发音「游移」随机摆位，游移开时滑杆置灰）、新场景 ×4（雷雨/雪夜/风/溪流）、播放/暂停/切场景淡入淡出（增益 ramp + token 防串扰）、睡眠定时器本轮明确不做；DB v49 trigger_sounds（v48 被并行会话超级工作台批次A 占用顺延）+ 零新 settings 键（NoiseState JSON 扩展 pans/triggers）；主进程 trigger.ts 五 IPC + triggerSynth.ts 9 合成器（木桌轻叩/玻璃轻敲/翻书页/机械键盘/剪刀开合/泡泡纸/塑料袋窸窣/风铃/颂钵）+ noiseEngine 触发层调度/声像链路/自定义混音与播放队列携带触发层快照（出厂预设不动触发层）+ MixerPanel 触发音区（开关/音量/声像/游移/导入/改名/删除二次确认彻底删不入回收站）；typecheck 本任务文件全绿 + build 通过（收尾时余量错误均在并行会话半成品文件），运行时冒烟待开发者验证）
- [X]  在个人档-我的画像模块下再添加“我是谁”的功能模块——就是让AI基于当前项目中的数据对我的一个了解，每天对我进一步提出3-4个问题，来加深对我的了解——我可以选择回答，也可以选择不回答。AI只需要将我作出回答的问题整理好，添加到对我的记忆的相关库中就行。（260921 已实施，`左侧边栏/常驻模块/个人档/2026-09-21-我是谁-design.md` + 同日 plan；brainstorm 决策——每日 3-4 问当日随机定档、跨天自然作废（历史行永不删，供防重复提问）、回答提交即定稿不可改（要改去画像区改条目）、跳过为明确动作；AI 侧：11 源广谱聚合工作台数据出题一次调用 + 近 40 问防重复注入，每答提炼 0-2 条候选画像条目（只收稳定信息、不重复已有条目），逐条「加入画像/忽略」确认后入 profile_facts(source='ai')；DB v47 whoami_questions + settings 键 whoami_daily_date；IPC 五通道（get/generate/answer/extract/resolve）+ 启动 10s 簇与午夜排程双触发（均静默失败、LLM 未配置跳过不记日期）；渲染层 WhoamiZone 挂 ProfileModule 画像 zone 之下（题目行三态：待答/提炼中/候选待确认，提炼失败可重试）+ profile 深链 target='whoami' 自动展开 + 总导览挑战卡后紧凑状态行；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  文笔坊中再添加一个“经验书”的标签页模块——不需要AI功能，由我自己手动增删改查即可。我会记录一些自己认为有用的经验道理在这里面。（260917 已实施，`左侧边栏/学习模块/文笔坊/2026-09-17-经验书-design.md` + 同日 plan；brainstorm 决策——一句话短条目、平铺时间倒序、行内快记+行内编辑零弹窗、tab 位次第二（格言库｜经验书｜写作台｜浮生记，默认仍格言库）、SQLite 方案 A；DB v44 wenbi_experiences + 回收站 wenbi_exp 通用链路（恢复/3 天过期零特判）+ 四 IPC（expList/expCreate/expUpdate/expDiscard）+ ExperiencePanel（快记框 Enter 连记/Shift+Enter 换行/双击行内编辑/丢弃二次确认）；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  Ctrl+滚轮调整字体大小（先只做订阅源，我不知道整个项目是否好实现。请你和我分析商讨，再确认是否要把这个功能推广到整个项目）（260916 已实施，`左侧边栏/学习模块/信息源/2026-09-16-Ctrl滚轮字体缩放-design.md` + 同日 plan；brainstorm 决策——仅信息源阅读视图（全项目推广不做一刀切，epub iframe 桥接/pdf 缩放语义撞车留待按场景逐个接入）、全局记一档 settings `feed_reader_zoom`、80%–200% 步进 10、标题/元信息/AI 总结/正文一体缩放；ArticleView 原生非 passive wheel 监听 preventDefault 阻断 Chromium 整页缩放（React 合成 onWheel 是 passive）；底部浮条百分比点击复位、1.5s 淡出；settings 值损坏/越界回落 100；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  总导览添加热力图（仿GitHub热力图）——记录每日的一个任务完成情况吧。（完成度越高颜色越深）（260916 已实施，`左侧边栏/常驻模块/总导览/2026-09-16-每日挑战与热力图-design.md` + 同日 plan；brainstorm 决策——固定三任务口径（学习每日要求/每日一题/每日挑战，0-3 件四档色）、近一年 53 周整图起始对齐周一、置于格言轮播上方、方案 A 读时聚合零汇总表；新服务 overview.ts 三源聚合（learn_daily.done + wall_puzzles 非 answering + challenge_daily.done）+ overview:heatmap 单通道；HeatmapCard 纯自研 div grid、color-mix 主题色四档零彩亮、月标签/周一三五行标/图例/hover 详情固定标题行、窄窗横向滚动兜底；挑战打卡/撤销即时重拉联动；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  总览区添加每日挑战（去健身房锻炼，去跑2000m......具体挑战项目由用户自己填写，AI随机抽取）（260916 已实施，`左侧边栏/常驻模块/总导览/2026-09-16-每日挑战与热力图-design.md` + 同日 plan；brainstorm 决策——本地随机抽取（总导览保持零 AI 架构）、日定一条+打卡且换一条不重置（完成记在当天）、打卡可逆（误点可撤销，热力图随实时记录一致）、池管理总导览页内弹窗；DB v43 challenge_pool/challenge_daily 两表（v42 已被同日并行会话藏阅阁文件夹占用顺延）+ challenge.ts 服务（事务 check-and-set 定档）+ 7 通道；挑战卡置每日一题与续读之间（完成/撤销/换一条池<2 置灰/管理），ChallengeManageDialog 增改删+删除二次确认，删池条目 LEFT JOIN 兜底「已删除的挑战」done 历史保留，池空引导态；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  右侧接入一个可以打开文件夹，查看文件夹下文件和代码的资源管理器.（放在debugzi下面和草稿本上面之间的位置）（260916 已实施，`右侧边栏/资源管理器/2026-09-16-资源管理器-design.md` + 同日 plan；brainstorm 五决策——单个文件夹记住上次、纯只读、highlight.js 语法高亮、零 AI 零联动、自研轻量树；零 DB 迁移，settings 三键 explorer_root_path/last_file/width；主进程新服务 explorer.ts 四 IPC（root 逐调用传参 + resolve 越权校验 + null byte 二进制探测 + 1MB/5MB 上限）；FileExplorerSidebar 懒加载树 + 单栏两态（浏览态/查看态：返回+面包屑+分流——md 走 MdView、代码白名单 hljs 高亮+sticky 行号、图片预览、其余不支持）+ hljs 配色双主题色系 CSS 变量；右栏四面板互斥、细条图标序 debugzi→资源管理器→草稿本→画布；顺带修复 ipc.ts 存量游离重复 import generateWikiQuiz；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  白噪音更名为”音乐吧”——其中有轻音乐和白噪音两个标签页——轻音乐模块有可导入音乐的功能，我想要导入一些轻音乐（先支持mp3格式）（260915 已实施，`左侧边栏/常驻模块/音乐吧/2026-09-15-音乐吧-轻音乐-design.md` + 同日 plan；brainstorm 四决策——音源互斥、最小集+歌单分组、与白噪音播放队列完全独立、快捷控件控当前活跃音源；DB v39 music_playlists/music_tracks + mp3 复制入 userData/music/ + 独立 MusicEngine（HTMLAudioElement）+ audioExclusive 互斥协调；音乐吧页大 tab「轻音乐｜白噪音」默认轻音乐，白噪音子 tab 混音器/播放队列零改动；MusicPanel 歌单分组/删除二次确认彻底删连文件/时长懒回写；左栏更名音乐吧、右栏快捷钮活跃音源口径、MusicState 重启默认暂停；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  添加对学习库的每日要求——每日必须新学习8-10个新知识点（用户自己去解锁去打卡完成，不要随机推荐的），并且要完成题目练习测试（260915 已实施，`左侧边栏/学习模块/学习库/2026-09-15-每日要求-design.md` + 同日 plan；brainstorm 四决策——每日随机 8-10 定档不重抽、取消系统自动推荐改用户自己解锁、自动打卡无手动按钮、小测交卷即完成不看分数；DB v40 learn_nodes.learned_at（本地时间）+ learn_daily 重建（goal/review_ids/done）；done 两处 check-and-set（mark learn / quizSubmit）显式落库防删卡丢历史；learnStock 重写为备学池泵（todo 已生成保底 10 张轮转均摊）；今日学习 tab 改今日要求卡（进度条+小测状态+连胜）+ 今日已学动态列表，小测出卷口径改今日已学卡、onChanged 联动；总导览今日学习块改每日要求状态行；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  差不多就这样了。最后我还需要在左侧边栏最上方加一个总导览（具体放什么还没想好）。然后就打算把”收藏夹”和”藏书架”两个模块合并在一个，腾出一个位置放”总导览”。以后启动项目的时候初始默认显示”总导览”页面。（260912 全部实施：前半藏阅阁合并 + 后半总导览落地——今日驱动页（问候/今日格言/学习队列/待消化/每日一题/续读/轻数字条）左栏置顶、启动默认已切总导览、深链直达各模块内部视图；见 `左侧边栏/常驻模块/总导览/`）
- [X]  万象库中再加一个“问答”的标签页，在“致知己”模块的十二问题放的是一些自己对生活的理解的偏思考问题；而万象库的“问答”标签页中是放那种偏科普的知识问题，比如“我很好奇流量价格是怎么降下来的”这种问题——问AI，AI搜索回答我（260912 已实施，`左侧边栏/学习模块/万象库/2026-09-12-知识问答标签页-design.md` + 同日 plan；万象库三 tab 化（百科｜辩真｜问答），问答 = 输入偏科普知识问题，AI 强制 MCP 联网搜索后回答（无搜索工具不降级纯 LLM）；brainstorming 商定四决策：单轮问答存档（追问走右栏「万象·问答」频道不绑定记录）、安静模式（过程不推频道）、允许重复提问、不联动百科词条；DB v37 qa_records + runQaAnswer 复用辩真「关键词→检索→综合」骨架；删除入回收站万象库块（新 source qa）；MdDialog 划词 onHighlight 改可选（问答无词条载体仅「问 AI」）；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
- [X]  感觉差不多可以把项目封装成客户端了...看看封装成哪几种形态（网页端，桌面端）。还有就是如何同移动设备连接（在移动设备使用项目）（260912 最终态：桌面端 = electron-builder 打包 exe + 托盘常驻 + 开机自启，**已实施并保留**（期0，冒烟全过，托盘图标经 extraResources 修复）；移动连接（期1 Web 服务 + 扫码配对 + 手机 PWA 壳 + Tailscale）**当日撤销**——Tailscale 本机无法 login，手机连接暂不需要，相关代码/依赖（remoteServer/remotePairing/src/mobile/qrcode）与计划文档（`idea/手机端/`）已全部删除，期0 不受影响。期2-4（记录/信息源/知识）随之取消，devlog 260912 有全程记录）
- [X]  左侧边栏加一个预言家的模块（在致知己中创建预言家标签页），比如说根据流量价格的发展预测未来 Token 会越来越便宜，这个推断是否合理。然后需要AI联网搜索资料，然后给出我充分全面的说明。这里的AI还要和我进行多轮对话的能力，因为我可能会对AI给出的一些理由提出质疑什么的。最后有一个判断后可能还可以和致知己原初区域产生联动。（260912 已实施，`左侧边栏/生活模块/致知己/2026-09-12-预言家与十二问题-design.md` + 同日 plan；致知己三 tab 化（沉淀｜预言家｜十二问题），预言家 = 新增「致知己·预言家」AI 频道（预言分析师人设）+ runProphetAnalysis 复用辩真 MCP 检索骨架出六节充分说明（md 快照挂条目 + assistant 消息推频道作多轮起点）+ 频道内多轮质疑 + MdDialog footerBar 判断区三选一即点即判可改判；与沉淀区联动商定本期不做；DB v36 prophet_records；typecheck 双配置通过，运行时冒烟待开发者验证）
- [X]  关于输出：把自己对问题的想法写下来，记入到自己的思维库。【在致知己中创建"十二问题"标签页】（260912 已实施，同上设计/计划；费曼式自定义 ≤12 题（满额置灰+服务端 TWELVE_FULL 兜底）+ 每题多条带日期想法流（倒序、MdView 渲染、Enter 快记、单条删除二次确认彻底删）+「问 AI」带题目与最近想法 autoAsk 进「致知己·追问」频道（只帮想透不代笔）；默认零 AI 纯手写；DB v36 twelve_questions/twelve_thoughts，删题进回收站致知己块想法随题封存恢复；typecheck 双配置通过，运行时冒烟待开发者验证）
- [X]  给工作台加一个CLI命令行，目前想到的一个作用是在添加书架功能后，可以借助[github.com/heartleo/zlib](https://github.com/heartleo/zlib)这个项目然后在工作台快速下载书籍吧可能。（260912 已实施，`全局/archive/2026-09-12-内置终端-design.md` + 同日 plan；xterm.js + node-pty 内嵌系统终端（VS Code 同款栈）——全局底部面板 `Ctrl+J` 呼出/收起、`Ctrl+Shift+J` 新建标签、多标签上限 6、页签点击重命名、底边拖拽高度记忆、收起仅隐藏进程不死、选中输出发右栏 AI（钉助手频道）、双主题 ANSI 配色、退出条一键重开；主进程 data 合帧防刷屏 + `before-quit` 兜底杀 shell；个人档新增「终端」zone（默认 shell 三选 + 默认工作目录，settings JSON 零 DB 迁移）；构建经 externalizeDepsPlugin 外置 node-pty 原生模块免 rebuild。typecheck 双配置通过，CDP 冒烟 + 开发者冒烟通过（冒烟中发现并修复收起重开回显偏移 bug）；zlib 下载书籍为终端内的后续可选用法，不在本次范围内）
- [X]  左侧边栏上面新添加一个模块（放在第一个的位置）——关于我的专业知识的学习库。具体如何设计需要好好商议（260911 已实施，`左侧边栏/学习模块/学习库/2026-09-11-学习库-design.md` + 同日 plan；DB v33 四表 + AI 建树/卡片/展开三函数 + learnStock 每日队列定档与预生成泵 + LearnModule 三 tab（今日学习/知识树/高光笔记）+ MdDialog studyBar + 右栏「学习·问答」频道 + 回收站十块（learn 块居首）；typecheck 双配置通过，运行时冒烟待开发者验证）
- [X]  给书架中的书阅读的时候补充阅读背景图片（纯色背景图片/用户自上传背景图片）（260911 已实施，`左侧边栏/生活模块/图书馆/archive/书架/2026-09-11-阅读背景-design.md`；全局一份 settings 两键 + image:pick 扩展 reader-bg 单槽位 + 6 纸感预设色/上传图亮度采样自动配深浅字色 + 阅读条背景菜单（ActionMenu 扩展 iconStyle）；epub 注入字色/透明透底图、pdf 页外底色；零 DB 迁移。typecheck 双配置通过（中途两错为并行会话格言库重构半成品，其收尾后复核全绿）；运行时冒烟待开发者验证）
- [X]  丰富白噪音中的音效功能（在右侧边栏右下角，主题切换上方添加一个播放的组件，快速播放/暂停白噪音）（260911 已实施，`左侧边栏/常驻模块/音乐吧/2026-09-11-快捷按钮与新场景-design.md`；右下角快捷按钮随主题按钮显隐、播放中主题色高亮；新增森林/海浪/夜晚篝火/咖啡馆 4 场景（chirp/wave/crackle/clink 四种新事件合成器，各配 3 出厂预设），场景卡/混音/恢复自动生效零 UI 改动；纯渲染层。听感调参与冒烟待开发者试听反馈）
- [X]  万象库建立一个待学习区：每天自动随机生成5-10条知识卡片（具体数量每天生成一个随机数，由随机数决定。知识卡片要求尽可能平均到目前已有的板块），在启动项目的时候生成吧；然后当用户阅读待学习区中的卡片的时候，要有一个（学会了）的组件，点击才能把该知识卡片加到板块中，不然就还是放在待学习区中。然后点击随机来一条，手动输入关键词生成的这些卡片，也都一样，在没有点击”学会了“之前，先放到待学习区中。然后关于“学会了”组件，点击一下变成“已学会”，代表处于已学会状态，从已学会状态点击一下可以重新变回“学会了”，代表处于待学习状态。除了建立这个每日5-10条这个持续学习机制以外，我觉得可以像推理角的预生成机制一样，每个板块预生成3个知识卡片在后库等待，玩家点击随机来一张，则从后库立刻抽出一张来显示，用户不需要等待。然后后库发现存量小于3了，立刻补充。关于位置，笔记本板块在最下面，我们把待学习板块放到最上面吧，也就是经济学上方。（260911 已实施，`左侧边栏/学习模块/万象库/2026-09-10-待学习区与预生成-design.md`；DB v31 state 三态 + 后库泵 wikiStock.ts + 每日批次 5-10 张板块均摊 + MdDialog learnBar 学会了/已学会可逆切换 + 待学习区置顶板块列表；typecheck 通过，运行时冒烟待开发者验证）
- [X]  左侧边栏增加一个收藏夹模块（藏书架和信息源之间）：放置我收藏的网站，文章等等（网站和文章又可以分成好多类，URL+名称+简介等信息来记录）超链接文本的形式呈现吧，然后Ctrl+点击可以直接跳转，直接点击查看介绍，增删改查功能都需要，还要有多级分类这样子。我暂时想到放在收藏夹中的就这些东西，也可以帮我拓展一下思路，看看还有什么也比较适合放在这里面（260910 已实施，`左侧边栏/生活模块/图书馆/archive/收藏夹/designs-specs.md`；两级分类 + 头部大类 tab + 抓取 meta + 拖拽/置顶/搜索；同日第 33 轮补分类可发现性：tab 行尾「＋」一键建大类、「分类管理」更名、零分类引导）
- [X]  右侧边栏再接入画布功能（[excalidraw.com](https://excalidraw.com/)）（260909 已实施，`右侧边栏/画布/2026-09-09-画布侧边栏-design.md`）
- [X]  给工作台加入白噪音（参考 mynoise.net 雨声发生器这个资源）（260908 已实施，`左侧边栏/常驻模块/音乐吧/designs-specs.md`）
- [X]  左侧边栏增加一个账本的功能——记录资金流水变化。（260908 已实施，`左侧边栏/生活模块/记账本/designs-specs.md`）
- [X]  左侧边栏新增一个书架模块——支持导入.epub格式/.pdf格式的书籍。然后每次打开的时候都要记住上次的位置。（260908 已实施，`左侧边栏/生活模块/图书馆/archive/书架/designs-specs.md`）
- [X]  左侧边栏新增一个信息源（收集RSS订阅，AI读文章总结放在开头，然后我读原文。weekly.tw93.fun/rss.xml，aiznb.com/weekly/atom.xml，[ursb.me/blog/feed.xml](https://ursb.me/blog/feed.xml)。先接入这三个。然后需要可以随时添加导入。）（260908 已实施，`左侧边栏/学习模块/信息源/designs-specs.md`）
