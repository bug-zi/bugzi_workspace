# 总导览（左栏置顶 · 启动默认页）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 左栏顶部新增「总导览」模块并设为启动默认页——今日驱动聚合（问候/今日格言/学习队列/待消化/每日一题/续读/轻数字条）+ 深链直达各模块内部视图。

**Architecture:** 纯渲染层方案 A——新模块并行调用各模块既有 IPC 聚合展示（零 AI、零主进程改动）；深链经新全局事件 `MODULE_NAVIGATE_EVENT`（detail `{module, target, payload}`）+ 新 hook `useModuleNavigate`，六个目标模块各加一个常驻监听切内部视图。设计依据：同目录 `2026-09-12-总导览-design.md`。

**Tech Stack:** Electron 44 + React 19 + TypeScript（electron-vite）。无新依赖、无 AI、无 DB 迁移。

**项目纪律（覆盖技能默认）：**
- **本计划不含任何 git 步骤**——所有 git 操作由开发者完成。
- 项目无单测框架 → 验证 = `npm run typecheck`（双 tsconfig）+ `npm run build` + 开发者运行时冒烟。
- 设计/计划存放按项目惯例（模块目录 + archive/），非 docs/superpowers/。

**已核实的上下文（实施时以内容匹配为准，行号为 2026-09-12 深夜实况）：**

| 事项 | 实况 |
|---|---|
| `ModuleId` | `src/shared/types.ts:4-16` 与 `src/renderer/api.d.ts:2-15` 两处联合类型（260912 合并后无 'bookshelf'/'favorites'，已有 'zangyue'；api.d.ts 遗留 'verify' 不动） |
| App.tsx | `MODULES` 32-44 行（首行 learn 学习库）；`useState<MainView>('learn')` ≈77 行；`moduleRef = useRef<MainView>('learn')` ≈79 行；`MODULE_ACTIVATED_EVENT`/`MODULE_DEACTIVATED_EVENT` 导出 ≈70-72 行；`activateModule` 157-165 行（`(id: MainView) => void`，可直传作 `onNavigate`）；渲染区 ≈254 行起 `{m.id === 'learn' && ...}` |
| 学习库 | `LearnModule.tsx:81` `const [tab, setTab] = useState<'daily' \| 'tree' \| 'notes'>('daily')` |
| 万象库 | `WikiModule.tsx:40-42` `const [view, setView] = useState<View>(...)`（View 含 `{kind:'learn'}` 待学习区）+ `const [tab, setTab] = useState<'wiki' \| 'verify' \| 'qa'>('wiki')` |
| 推理角 | `ReasoningModule.tsx:9` `const [board, setBoard] = useState<'turtle' \| 'wall'>('turtle')`；WallPanel 无内部子页签（每日一题在思维墙板块顶部）→ 深链只需 `setBoard('wall')` |
| 文笔坊 | `WenbiModule.tsx:16` `const [board, setBoard] = useState<'mottos' \| 'writing' \| 'journal'>('mottos')` |
| 藏阅阁 | `ZangyueModule.tsx:12` `const [tab, setTab] = useState<'bookshelf' \| 'favorites'>('bookshelf')`；`BookshelfModule` 的 `openBook(b: BooksRecord)` 为组件内普通函数（不读外部 state，旧闭包调用安全） |
| 数据端点 | `learn.daily()`→`{new: LearnDailyRow[]; review: LearnDailyRow[]}`（LearnDailyRow extends LearnNode 有 `title`）；`wiki.learnEntries()`→`WikiLearnRow[]`；`wall.month(year, month)`（**月份 1 基**，ipc.ts:2438 前缀 `${year}-${pad2(month)}-%`）→`WallMonthInfo {streak, correct, wrong, days: WallDayCell[]}`（WallDayCell `{date:'YYYY-MM-DD', status:'correct'\|'wrong', ...}`，只含有记录的天）；`books.list()`（最近阅读在前）/`books.readStats()`→`{todaySeconds, streakDays, readingCount, rows}`；`feeds.list()`→`(FeedRecord & {unread:number})[]`；`ledger.stats('YYYY-MM')`→`{incomeCents, expenseCents, breakdown}`；`mottos.list('formal')`→`MottoRecord {content, source, ...}`（status 码 'draft'\|'settled'\|'formal'） |
| 用户名 | `settings[SettingsKeys.UserName]`（'user_name'，useAppSettings 读取，ProfileModule 同款） |
| 事件先例 | `useModuleActivated.ts` 全文 13 行（window CustomEvent 监听 + 按 detail 过滤），useModuleNavigate 照抄结构 |

---

### Task 1: 类型与事件基建（types + hook）

**Files:**
- Modify: `src/shared/types.ts`（ModuleId 联合类型 + 新增 ModuleNavDetail）
- Modify: `src/renderer/api.d.ts:2-15`（ModuleId 联合类型）
- Create: `src/hooks/useModuleNavigate.ts`

- [ ] **Step 1: shared/types.ts ModuleId 加 'zonglan'（放首位）+ 尾部追加 ModuleNavDetail**

联合类型改为（注释续写沿革）：
```ts
// 模块标识（260908 辩真阁并入万象库：'verify' 移除，其功能为万象库「辩真」板块；260911 格言库并入文笔坊：'mottos' 移除；260912 收藏夹+藏书架合并：两 id 移除，并为 'zangyue'；260912 新增总导览 'zonglan' 居首）
export type ModuleId =
  | 'zonglan'
  | 'learn'
  | 'wiki'
  | 'inspirations'
  | 'zhijiji'
  | 'reasoning'
  | 'wenbi'
  | 'zangyue'
  | 'feed'
  | 'ledger'
  | 'recycle'
  | 'profile'
```
在 ModuleId 定义之后追加：
```ts
// 模块深链导航事件 detail（260912 总导览）：App.tsx MODULE_NAVIGATE_EVENT 的载荷，
// 目标模块经 useModuleNavigate 监听后按 target 切内部视图
export interface ModuleNavDetail {
  module: ModuleId
  target: string
  payload?: Record<string, unknown>
}
```

- [ ] **Step 2: api.d.ts ModuleId 同步**

联合类型 `'learn'` 行之前加 `'zonglan'` 行（历史遗留 `'verify'` 不动），头注释追加「；260912 新增总导览 'zonglan'」。

- [ ] **Step 3: 新建 `src/hooks/useModuleNavigate.ts`**

```ts
// 模块深链导航监听 hook（配合 App.tsx 的 MODULE_NAVIGATE_EVENT，与 useModuleActivated 对称）
// 用法：useModuleNavigate('wiki', (target) => { if (target === 'learn-zone') setView({ kind: 'learn' }) })
// keep-alive 下组件常驻，藏于后台也接得住；onNavigate 内联箭头每渲染重订阅（useModuleActivated 同款，开销可忽略）
import { useEffect } from 'react'
import { MODULE_NAVIGATE_EVENT } from '../App'
import type { ModuleNavDetail } from '../shared/types'

export function useModuleNavigate(
  moduleId: string,
  onNavigate: (target: string, payload: Record<string, unknown> | undefined) => void
): void {
  useEffect(() => {
    const handler = (e: Event): void => {
      const detail = (e as CustomEvent<ModuleNavDetail>).detail
      if (detail && detail.module === moduleId) onNavigate(detail.target, detail.payload)
    }
    window.addEventListener(MODULE_NAVIGATE_EVENT, handler)
    return () => window.removeEventListener(MODULE_NAVIGATE_EVENT, handler)
  }, [moduleId, onNavigate])
}
```
（MODULE_NAVIGATE_EVENT 尚不存在——Task 3 App.tsx 导出后 typecheck 才恢复全绿；本任务末不做全库 typecheck。）

---

### Task 2: OverviewModule 组件 + 样式

**Files:**
- Create: `src/modules/zonglan/OverviewModule.tsx`
- Create: `src/modules/zonglan/zonglan.css`

- [ ] **Step 1: 新建 `src/modules/zonglan/OverviewModule.tsx`（全文）**

```tsx
// 总导览（左栏置顶 · 启动默认页，260912 新功能开发区）：今日驱动——问候 + 今日格言 +
// 学习队列 / 待消化 / 每日一题 / 续读 四今日块 + 轻数字条。纯渲染层并行聚合既有 IPC
// （零 AI、零主进程改动，逐块独立 catch）；深链 = 派发 MODULE_NAVIGATE_EVENT + onNavigate
// 切模块（设计 §五，目标模块 useModuleNavigate 监听切内部视图）。
import { useCallback, useEffect, useState } from 'react'
import { useAppSettings } from '../../theme/ThemeProvider'
import { useModuleActivated } from '../../hooks/useModuleActivated'
import { useModuleNavigate } from '../../hooks/useModuleNavigate'
import { SettingsKeys } from '../../shared/types'
import type { LearnDailyRow, ModuleId, MottoRecord } from '../../shared/types'
import { MODULE_NAVIGATE_EVENT } from '../../App'
import './zonglan.css'

export interface OverviewModuleProps {
  onNavigate: (module: ModuleId) => void
}

function pickRandom<T>(arr: T[]): T | null {
  return arr.length > 0 ? arr[Math.floor(Math.random() * arr.length)] : null
}

/** 今日 YYYY-MM-DD（wall.month 的 WallDayCell.date 同格式比对） */
function todayStr(now: Date): string {
  return `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`
}

export default function OverviewModule(props: OverviewModuleProps) {
  const { settings } = useAppSettings()

  // ----- 各块状态（null = 加载失败 → 该块显示「--」/失败态，不拖垮整页） -----
  const [formalMottos, setFormalMottos] = useState<MottoRecord[]>([])
  const [motto, setMotto] = useState<MottoRecord | null>(null)
  const [queue, setQueue] = useState<{ newItems: LearnDailyRow[]; reviewItems: LearnDailyRow[] } | null>(null)
  const [learnCount, setLearnCount] = useState<number | null>(null)
  const [wallState, setWallState] = useState<{ done: boolean; streak: number } | null>(null)
  const [reading, setReading] = useState<{ id: number; title: string; percent: number; todayLabel: string } | null>(null)
  const [readingStreak, setReadingStreak] = useState<number | null>(null)
  const [feedUnread, setFeedUnread] = useState<number | null>(null)
  const [monthExpense, setMonthExpense] = useState<string | null>(null)

  /** 深链：先派发导航事件（目标模块常驻监听切内部视图），再切模块 */
  const go = useCallback(
    (module: ModuleId, target?: string, payload?: Record<string, unknown>) => {
      window.dispatchEvent(new CustomEvent(MODULE_NAVIGATE_EVENT, { detail: { module, target, payload } }))
      props.onNavigate(module)
    },
    [props]
  )

  // ----- 逐块加载（各自 catch） -----
  const loadMottos = useCallback(async (): Promise<void> => {
    try {
      const formal = await window.api.mottos.list('formal')
      setFormalMottos(formal)
      setMotto(pickRandom(formal))
    } catch {
      setFormalMottos([])
      setMotto(null)
    }
  }, [])
  const loadDaily = useCallback(async (): Promise<void> => {
    try {
      const d = await window.api.learn.daily()
      setQueue({ newItems: d.new, reviewItems: d.review })
    } catch {
      setQueue(null)
    }
  }, [])
  const loadLearnCount = useCallback(async (): Promise<void> => {
    try {
      setLearnCount((await window.api.wiki.learnEntries()).length)
    } catch {
      setLearnCount(null)
    }
  }, [])
  const loadWall = useCallback(async (): Promise<void> => {
    try {
      const now = new Date()
      const info = await window.api.wall.month(now.getFullYear(), now.getMonth() + 1)
      const t = todayStr(now)
      setWallState({ done: info.days.some((d) => d.date === t), streak: info.streak })
    } catch {
      setWallState(null)
    }
  }, [])
  const loadReading = useCallback(async (): Promise<void> => {
    try {
      const [books, stats] = await Promise.all([window.api.books.list(), window.api.books.readStats()])
      setReadingStreak(stats.streakDays)
      const inProgress = books.find((b) => b.last_read_at != null && b.progress_percent < 100) ?? null
      if (!inProgress) {
        setReading(null)
        return
      }
      const mins = Math.floor(stats.todaySeconds / 60)
      setReading({
        id: inProgress.id,
        title: inProgress.title,
        percent: Math.round(inProgress.progress_percent),
        todayLabel: mins > 0 ? `今日 ${mins} 分钟` : stats.todaySeconds > 0 ? '今日 <1 分钟' : '今日尚未阅读'
      })
    } catch {
      setReading(null)
      setReadingStreak(null)
    }
  }, [])
  const loadNumbers = useCallback(async (): Promise<void> => {
    try {
      const feeds = await window.api.feeds.list()
      setFeedUnread(feeds.reduce((s, f) => s + f.unread, 0))
    } catch {
      setFeedUnread(null)
    }
    try {
      const now = new Date()
      const s = await window.api.ledger.stats(`${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`)
      setMonthExpense(`¥${(s.expenseCents / 100).toFixed(2)}`)
    } catch {
      setMonthExpense(null)
    }
  }, [])

  const loadAll = useCallback((): void => {
    void loadMottos()
    void loadDaily()
    void loadLearnCount()
    void loadWall()
    void loadReading()
    void loadNumbers()
  }, [loadMottos, loadDaily, loadLearnCount, loadWall, loadReading, loadNumbers])

  useEffect(() => {
    loadAll()
  }, [loadAll])
  useModuleActivated('zonglan', loadAll)

  // ----- 问候行（用户名取个人档；未设置退「你好」） -----
  const now = new Date()
  const name = settings[SettingsKeys.UserName]?.trim()
  const greetLine = `${name || '你好'}，${now.getMonth() + 1}月${now.getDate()}日 周${'日一二三四五六'[now.getDay()]}`
  const hasQueue = queue != null && (queue.newItems.length > 0 || queue.reviewItems.length > 0)

  return (
    <div className="module-page" style={{ maxWidth: 1200 }}>
      <div className="module-header">
        <span className="material-symbols-outlined">space_dashboard</span>
        <span className="module-title">总导览</span>
        <span className="module-sub">{greetLine}</span>
      </div>

      {/* 今日格言（正式区随机；换一条本地重摇不重拉；点击进格言库） */}
      {motto && (
        <div
          className="card zl-quote"
          onClick={() => go('wenbi', 'mottos')}
          title="进入格言库"
        >
          <div className="zl-quote-text">{motto.content}</div>
          <div className="zl-quote-foot">
            <span className="module-sub">{motto.source ? `—— ${motto.source}` : ''}</span>
            <button
              className="icon-btn"
              title="换一条"
              onClick={(e) => {
                e.stopPropagation()
                setMotto(pickRandom(formalMottos))
              }}
            >
              <span className="material-symbols-outlined">refresh</span>
            </button>
          </div>
        </div>
      )}

      {/* 今日学习（整卡深链「今日学习」tab，不逐卡定位） */}
      <button
        className="card zl-row"
        onClick={() => go('learn', 'today')}
        title="进入学习库·今日学习"
      >
        <span className="material-symbols-outlined">school</span>
        <div className="zl-row-main">
          <div className="zl-row-line">
            <span className="zl-row-title">今日学习</span>
            <span className="module-sub">
              {queue ? `新学 ${queue.newItems.length} · 复习 ${queue.reviewItems.length}` : '加载失败'}
            </span>
          </div>
          {hasQueue && queue && (
            <ul className="zl-list">
              {queue.newItems.slice(0, 5).map((n) => (
                <li key={`n${n.id}`}>
                  <span className="zl-tag">新学</span>
                  {n.title}
                </li>
              ))}
              {queue.reviewItems.slice(0, 5).map((n) => (
                <li key={`r${n.id}`}>
                  <span className="zl-tag">复习</span>
                  {n.title}
                </li>
              ))}
            </ul>
          )}
          {queue && !hasQueue && <div className="module-sub">队列为空（LLM 未配置或尚未生成）· 点击去学习库</div>}
        </div>
      </button>

      {/* 待消化（0 置灰不可点） */}
      <button
        className={`card zl-row${learnCount === 0 ? ' zl-muted' : ''}`}
        disabled={learnCount === 0}
        onClick={() => go('wiki', 'learn-zone')}
        title="进入万象库·待学习区"
      >
        <span className="material-symbols-outlined">public</span>
        <span className="zl-row-title">待学习区</span>
        <span className="module-sub">
          {learnCount == null ? '加载失败' : learnCount === 0 ? '今日无待消化' : `${learnCount} 张待消化`}
        </span>
      </button>

      {/* 每日一题（wall.month 被动查，绝不触发出题） */}
      <button
        className="card zl-row"
        onClick={() => go('reasoning', 'daily')}
        title="进入推理角·思维墙"
      >
        <span className="material-symbols-outlined">psychology</span>
        <span className="zl-row-title">每日一题</span>
        <span className="module-sub">
          {wallState
            ? wallState.done
              ? `已完成 · 连胜 ${wallState.streak} 天`
              : `未打卡 · 连胜 ${wallState.streak} 天`
            : '加载失败'}
        </span>
      </button>

      {/* 续读（无在读书显引导） */}
      {reading ? (
        <button
          className="card zl-row"
          onClick={() => go('zangyue', 'read', { bookId: reading.id })}
          title="继续阅读"
        >
          <span className="material-symbols-outlined">auto_stories</span>
          <span className="zl-row-title">继续阅读：{reading.title}</span>
          <span className="module-sub">
            {reading.percent}% · {reading.todayLabel}
          </span>
        </button>
      ) : (
        <button className="card zl-row" onClick={() => go('zangyue')} title="进入藏阅阁">
          <span className="material-symbols-outlined">auto_stories</span>
          <span className="zl-row-title">藏阅阁·书架</span>
          <span className="module-sub">没有在读的书 · 去导入一本</span>
        </button>
      )}

      {/* 轻数字条（点击直达对应模块） */}
      <div className="zl-numbar">
        <button className="zl-num" onClick={() => go('feed')} title="进入信息源">
          <span className="zl-num-val">{feedUnread ?? '--'}</span>
          <span className="module-sub">信息源未读</span>
        </button>
        <button className="zl-num" onClick={() => go('ledger')} title="进入记账本">
          <span className="zl-num-val">{monthExpense ?? '--'}</span>
          <span className="module-sub">本月支出</span>
        </button>
        <button className="zl-num" onClick={() => go('zangyue')} title="进入藏阅阁">
          <span className="zl-num-val">{readingStreak != null ? `${readingStreak} 天` : '--'}</span>
          <span className="module-sub">阅读连续</span>
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 新建 `src/modules/zonglan/zonglan.css`**

```css
/* ===== 总导览（260912）：今日驱动聚合页；全部双主题变量，无彩亮色 ===== */
.zl-quote {
  cursor: pointer;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.zl-quote-text {
  font-size: 1.1em;
  line-height: 1.8;
}
.zl-quote-foot {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.zl-row {
  display: flex;
  align-items: center;
  gap: 10px;
  width: 100%;
  padding: 12px 14px;
  cursor: pointer;
  text-align: left;
}
.zl-row > .material-symbols-outlined {
  color: var(--color-primary);
  flex-shrink: 0;
}
.zl-row-main {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.zl-row-line {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
}
.zl-row-title {
  font-weight: 500;
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.zl-muted {
  opacity: 0.55;
}
.zl-list {
  margin: 0;
  padding: 0 0 0 30px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.zl-list li {
  list-style: none;
  font-size: 0.92em;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.zl-tag {
  display: inline-block;
  margin-right: 6px;
  padding: 0 6px;
  border-radius: 8px;
  font-size: 0.8em;
  background: var(--color-surface-strong);
  border: 1px solid var(--color-border);
}
.zl-numbar {
  display: flex;
  gap: 12px;
}
.zl-num {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  padding: 10px;
  cursor: pointer;
}
.zl-num-val {
  font-size: 1.2em;
  font-weight: 500;
}
```

---

### Task 3: App.tsx 接线

**Files:**
- Modify: `src/App.tsx`（事件导出、import、MODULES、启动默认、渲染）

- [ ] **Step 1: MODULE_DEACTIVATED_EVENT 导出之后追加事件常量**

```tsx
// 模块深链导航事件（260912 总导览）：detail ModuleNavDetail——目标模块 useModuleNavigate 监听切内部视图
export const MODULE_NAVIGATE_EVENT = 'bugzi:module-navigate'
```

- [ ] **Step 2: import 区加 OverviewModule（LearnModule 之前）**

```tsx
import OverviewModule from './modules/zonglan/OverviewModule'
```

- [ ] **Step 3: MODULES 首位插一行 + 注释续写**

```tsx
// 左栏模块顺序（260908 重排；260911 格言库并入文笔坊 12→11 项；260911 学习库置顶 11→12 项；260912 优化建议区第41轮重排；260912 收藏夹+藏书架合并藏阅阁 12→11 项；260912 总导览置顶 11→12 项）
const MODULES: { id: ModuleId; label: string; icon: string }[] = [
  { id: 'zonglan', label: '总导览', icon: 'space_dashboard' },
  { id: 'learn', label: '学习库', icon: 'school' },
```
（其余行不动。）

- [ ] **Step 4: 启动默认两处 'learn' → 'zonglan'（useState 与 moduleRef 必须同改）**

```tsx
  const [module, setModule] = useState<MainView>('learn')
  // 当前模块 ref（失活事件需捕获旧模块 id；ref 方案防 strict-mode 双触发）
  const moduleRef = useRef<MainView>('learn')
```
改为：
```tsx
  const [module, setModule] = useState<MainView>('zonglan')
  // 当前模块 ref（失活事件需捕获旧模块 id；ref 方案防 strict-mode 双触发）
  const moduleRef = useRef<MainView>('zonglan')
```

- [ ] **Step 5: 渲染区 learn 行之前插总导览（onNavigate 直传 activateModule）**

```tsx
              {m.id === 'zonglan' && <OverviewModule onNavigate={activateModule} />}
              {m.id === 'learn' && <LearnModule onOpenAi={openAiWith} />}
```

---

### Task 4: 六个目标模块的深链监听（各 ~5 行）

**Files:**
- Modify: `src/modules/learn/LearnModule.tsx`
- Modify: `src/modules/wiki/WikiModule.tsx`
- Modify: `src/modules/reasoning/ReasoningModule.tsx`
- Modify: `src/modules/wenbi/WenbiModule.tsx`
- Modify: `src/modules/zangyue/ZangyueModule.tsx`
- Modify: `src/modules/bookshelf/BookshelfModule.tsx`

- [ ] **Step 1: LearnModule**——import 区加 `import { useModuleNavigate } from '../../hooks/useModuleNavigate'`；`useModuleActivated('learn', ...)` 调用附近（同组件体内）加：

```tsx
  // 总导览深链（260912）：切到今日学习
  useModuleNavigate('learn', (target) => {
    if (target === 'today') setTab('daily')
  })
```

- [ ] **Step 2: WikiModule**——import 区加同款 import；`useModuleActivated('wiki', ...)` 附近加：

```tsx
  // 总导览深链（260912）：切到百科 tab 的待学习区视图
  useModuleNavigate('wiki', (target) => {
    if (target === 'learn-zone') {
      setTab('wiki')
      setView({ kind: 'learn' })
    }
  })
```

- [ ] **Step 3: ReasoningModule**——import 区加同款 import；`useModuleActivated('reasoning', ...)` 之后加：

```tsx
  // 总导览深链（260912）：切到思维墙（每日一题在板块顶部，无子页签）
  useModuleNavigate('reasoning', (target) => {
    if (target === 'daily') setBoard('wall')
  })
```

- [ ] **Step 4: WenbiModule**——import 区加同款 import；组件体内（board state 之后）加：

```tsx
  // 总导览深链（260912）：切到格言库
  useModuleNavigate('wenbi', (target) => {
    if (target === 'mottos') setBoard('mottos')
  })
```

- [ ] **Step 5: ZangyueModule**——import 区加 `import { useModuleNavigate } from '../../hooks/useModuleNavigate'`；onReadingChange 定义之后加：

```tsx
  // 总导览深链（260912）：续读直达——先落书架页签（阅读器打开后页签条本就隐藏，返回时已在书架）
  useModuleNavigate('zangyue', (target) => {
    if (target === 'read') setTab('bookshelf')
  })
```

- [ ] **Step 6: BookshelfModule**——import 区加同款 import；items state（`const [items, setItems] = useState<BooksRecord[]>([])`）之后加 ref（防深链早于列表载入的旧闭包）：

```tsx
  /** items 最新值（总导览续读深链查书用） */
  const itemsRef = useRef(items)
  itemsRef.current = items
```
`useModuleActivated('zangyue', ...)` 之后加：

```tsx
  // 总导览深链（260912）：续读直达——找书 openBook 进阅读器（列表未载完则现拉一次）
  useModuleNavigate('zangyue', (target, payload) => {
    if (target !== 'read') return
    const bookId = payload?.bookId
    if (typeof bookId !== 'number') return
    void (async () => {
      const hit =
        itemsRef.current.find((b) => b.id === bookId) ??
        (await window.api.books.list()).find((b) => b.id === bookId)
      if (hit) void openBook(hit)
    })()
  })
```

---

### Task 5: 全库验证

- [ ] **Step 1: 双 tsconfig 类型检查**

Run: `npm run typecheck`
Expected: 两配置均无错误（若报 MODULE_NAVIGATE_EVENT 未导出，核对 Task 3 Step 1；若报 'zonglan' 缺失，核对两处联合类型）。

- [ ] **Step 2: 构建**

Run: `npm run build`
Expected: 通过。

- [ ] **Step 3: 开发者运行时冒烟清单**（对应设计 §七）

- [ ] 左栏 12 项、总导览居首、图标 `space_dashboard`；启动默认落总导览
- [ ] 问候行用户名/日期正确；今日格言显示、「换一条」只重摇、点击进文笔坊格言库
- [ ] 学习队列数字与学习库「今日学习」一致；点击直达今日学习 tab
- [ ] 待消化数与万象库待学习区一致；点击直达待学习区视图；0 张置灰
- [ ] 每日一题打卡状态/连胜与思维墙一致（总导览打开不出题、LLM 未配置不弹配置框）；点击直达思维墙
- [ ] 续读卡书名/进度/今日分钟正确；点击直接打开该书进阅读器；无在读书显引导
- [ ] 轻数字条三项数字正确、点击分别达信息源/记账本/藏阅阁
- [ ] 切走再切回总导览数据刷新；单块数据源异常仅该块显「--」/加载失败
- [ ] 双主题配色合规、无 emoji；其他模块零回归

---

### Task 6: 文档收尾

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/log/260912.md`（追加节）
- Modify: `docs/project/新功能开发区.md`（第 1 条整体完成，移入归档区顶部）
- Move: design + 本 plan → `docs/project/左侧边栏/总导览/archive/`

- [ ] **Step 1: CLAUDE.md 同步**（Grep `总导览|启动默认|十一模块|入口模块` 定位）：
  - 「三栏式布局」行：顺序改为 `总导览→学习库→…→个人档`（总导览居首）；「启动默认学习库」→「启动默认总导览」。
  - 「应用架构」引言：「左栏十一个入口模块」→「左栏十二个入口模块」（260912 总导览置顶）。
  - 架构列表最前加一条：`- **总导览**（260912 新增，左栏置顶 · 启动默认页）：今日驱动聚合——问候头、今日格言（正式区随机/换一条）、学习队列、待学习区待消化、每日一题打卡状态（wall.month 被动查不触发出题）、续读直达阅读器、轻数字条（未读/本月支出/阅读连续）；纯渲染层并行聚合既有 IPC 零 AI；深链经 MODULE_NAVIGATE_EVENT 事件 + useModuleNavigate 监听切各模块内部视图。`
  - 项目性质段「十一模块」→「十二模块」。

- [ ] **Step 2: devlog** `docs/log/260912.md` 追加小节（bash `date "+%Y-%m-%d %H:%M"` 取实际时间写「创建于」行）：记录决策四条、改动面（2 新文件 + 类型 + App 接线 + 六模块监听）、验证结果、冒烟待开发者。

- [ ] **Step 3: 新功能开发区.md**：待完成区第 1 条整行删除，归档区顶部插入：
```markdown
- [X]  差不多就这样了。最后我还需要在左侧边栏最上方加一个总导览（具体放什么还没想好）。然后就打算把“收藏夹”和“藏书架”两个模块合并在一个，腾出一个位置放“总导览”。以后启动项目的时候初始默认显示“总导览”页面。（260912 全部实施：前半藏阅阁合并 + 后半总导览落地——今日驱动页（问候/今日格言/学习队列/待消化/每日一题/续读/轻数字条）左栏置顶、启动默认已切总导览、深链直达各模块内部视图；见 `左侧边栏/总导览/`）
```

- [ ] **Step 4: 归档**：design 与 plan 移入 `docs/project/左侧边栏/总导览/archive/`（文件系统操作，不做 git）。

---

## Self-Review 记录（已核）

1. **Spec 覆盖**：设计 §二接线→Task 1/3；§三页面结构七要素→Task 2（问候头/格言卡/学习卡/待消化/每日一题/续读/轻数字条逐块对应）；§四数据流（并行 + 逐块 catch + 全部既有端点）→Task 2 loadX 函数；§五深链机制→Task 1 hook + Task 3 事件 + Task 4 六监听；§六明确不做→无越界改动；§七验收→Task 5 冒烟清单。
2. **无占位符**：全部步骤含精确代码/命令/预期；唯一「实施时核对」类表述已消除（wall.month 月份 1 基与日期格式已核 ipc.ts:2438、status 码 'formal' 已核）。
3. **类型一致性**：`ModuleNavDetail {module, target, payload?}` 在 shared/types 定义、hook 消费、OverviewModule 派发三处一致；`useModuleNavigate(moduleId: string, onNavigate: (target: string, payload?: Record<string, unknown> | undefined) => void)` 与六处调用一致；`onNavigate: (module: ModuleId) => void` 接 `activateModule: (id: MainView) => void`（MainView ⊇ ModuleId，参数逆变可赋值）；ReadStats 字段 `todaySeconds/streakDays`、LedgerStats `expenseCents`、WallMonthInfo `streak/days` 均与 api.d.ts 定义一致。
