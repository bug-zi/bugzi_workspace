# 学习库·深挖结果确认后写入知识卡 实施计划

> 创建于 2026-09-12 19:38（设计同日定稿，见 `2026-09-12-学习库深挖入卡-design.md`）
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 深挖改为卡片弹窗内闭环——生成 → 确认 → 主进程原子追加 `## 深挖（YYMMDD）` 小节进卡片 md；不再自动发右栏。

**Architecture:** 主进程两新 IPC（`learn:dig` 生成 / `learn:digApply` 原子写入）；MdDialog studyBar 扩展 `digPanel?: ReactNode` 渲染位（通用组件零破坏）；LearnModule 管理深挖状态并用既有 `mdVersion` 机制在写入后触发弹窗重读。

**Tech Stack:** Electron IPC + React 19 + 既有 chatCompletion/parseJsonObject/mdRead/mdWrite 基建。

**约定：**
- 本项目无自动化测试框架，验证 = `npm run typecheck`（双 tsconfig）+ `npm run build` + 冒烟清单；纯逻辑如有疑虑用临时断言脚本验证后删除（项目惯例）。
- **git 纪律（开发者硬性规则）：AI 不得执行任何 git 命令**，全部提交由开发者自行完成；每个任务完成后提示开发者可提交。
- LLM 未配置口径：按钮不置灰，点击时 `window.api.ai.configured()` 检查 + GoConfigDialog（LearnModule 已有 `goConfig` state）。

---

### Task 1: services.ts 新增 digLearnCard

**Files:**
- Modify: `electron/ai/services.ts`（在 `generateLearnTask` 函数后追加）

- [ ] **Step 1: 追加 digLearnCard 函数**

在 `reviewLearnTask` 函数结束后追加（与既有函数同风格；`chatCompletion`/`ensureNotCancelled`/`stripMdFence`/`mdRead`/`getDb` 本文件已在用）：

```ts
/** 深挖（优化建议区 260912）：四角度模板生成，返回结果 md；不落库，确认后经 learn:digApply 写入卡片 */
export async function digLearnCard(nodeId: number, signal?: AbortSignal): Promise<string> {
  const d = getDb()
  const n = d
    .prepare(
      `SELECT n.id, n.title, dom.name AS domain_name, t.title AS topic_title
       FROM learn_nodes n JOIN learn_domains dom ON n.domain_id = dom.id
       LEFT JOIN learn_nodes t ON n.parent_id = t.id
       WHERE n.id = ? AND n.deleted_at IS NULL`
    )
    .get(nodeId) as { id: number; title: string; domain_name: string; topic_title: string | null } | undefined
  if (!n) throw new Error('NOT_FOUND')
  let md = ''
  try {
    md = mdRead(`md/learn/${n.id}.md`)
  } catch {
    md = ''
  }
  if (!md.trim()) throw new Error('卡片内容为空，先打开卡片确认已生成')
  const prompt = `请深挖知识点「${n.title}」（领域：${n.domain_name} · 主题：${n.topic_title ?? '（无）'}）。结合下面的卡片内容，从四个角度展开：1）原理再进一层（卡片没讲到的机制细节）；2）常见误解与易错点；3）知识串联（同主题其他知识点与跨主题关联）；4）实际工作中的坑与经验。用简体中文 Markdown 输出，不要用代码围栏包裹整体，不要输出与深挖无关的开场白或结束语。

<卡片内容>
${md}
</卡片内容>`
  const res = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.6,
    scene: 'learn:dig',
    signal
  })
  ensureNotCancelled(signal)
  const out = stripMdFence(res.content)
  if (!out) throw new Error('LLM 未返回内容')
  return out
}
```

四角度模板文案与原前端 `onDig`（LearnModule.tsx:376）逐字一致，仅搬运进主进程。

- [ ] **Step 2: 验证编译**

Run: `npx tsc --noEmit -p electron/tsconfig.json`（或项目脚本对应主进程配置）
Expected: 无新增错误

---

### Task 2: ipc.ts 两通道 learn:dig / learn:digApply

**Files:**
- Modify: `electron/ipc.ts`（learn 分段末尾，`learn:deleteHighlight` 之后）

- [ ] **Step 1: import 行追加 digLearnCard**

`electron/ipc.ts:31` 附近 services import 列表中 `reviewLearnTask,` 后加一行：

```ts
  digLearnCard,
```

- [ ] **Step 2: 注册两通道**

在 `learn:deleteHighlight` handler 之后追加（`beginJob`/`endJob`/`mdRead`/`mdWrite` 均已在本文件使用；`yyMMdd` 已被 zhijiji 段使用，若未在作用域则从 `./db/db` 补 import）：

```ts
  // ---------- 深挖（优化建议区 260912）：生成 → 确认 → 原子写入卡片 md ----------
  ipcMain.handle('learn:dig', async (_e, jobId: string, nodeId: number) => {
    const ac = beginJob(jobId)
    try {
      return { content: await digLearnCard(nodeId, ac.signal) }
    } finally {
      endJob(jobId)
    }
  })
  // 原子写入：读全文 → 末尾追加日期小节 → 写回，收敛主进程单点防渲染层读改写竞态
  ipcMain.handle('learn:digApply', (_e, nodeId: number, content: string) => {
    const path = `md/learn/${nodeId}.md`
    let md = ''
    try {
      md = mdRead(path)
    } catch {
      md = ''
    }
    const c = String(content ?? '').trim()
    if (!c) throw new Error('EMPTY_CONTENT')
    const next = `${md.replace(/\s*$/, '')}\n\n## 深挖（${yyMMdd()}）\n\n${c}\n`
    mdWrite(path, next)
    return { md: next }
  })
```

- [ ] **Step 3: 验证编译**

Run: `npx tsc --noEmit -p electron/tsconfig.json`
Expected: 通过（若 `yyMMdd` 未导出则从 db.ts 补 import 后通过）

---

### Task 3: preload.ts + api.d.ts 透传

**Files:**
- Modify: `electron/preload.ts`（learn 分组，`taskDelete` 之后）
- Modify: `src/renderer/api.d.ts`（learn 接口，`taskDelete` 声明之后）

- [ ] **Step 1: preload.ts learn 分组追加**

```ts
    dig: (jobId: string, nodeId: number): Promise<{ content: string }> =>
      ipcRenderer.invoke('learn:dig', jobId, nodeId),
    digApply: (nodeId: number, content: string): Promise<{ md: string }> =>
      ipcRenderer.invoke('learn:digApply', nodeId, content)
```

注意给原 `taskDelete` 行尾补逗号。

- [ ] **Step 2: api.d.ts learn 接口追加声明**

```ts
    /** AI 深挖：四角度生成结果 md（不落库，确认后 digApply 写入）；可取消；LLM 未配置抛 LLM_NOT_CONFIGURED */
    dig(jobId: string, nodeId: number): Promise<{ content: string }>
    /** 深挖结果确认写入：卡片 md 末尾追加「## 深挖（YYMMDD）」小节，返回更新后全文 */
    digApply(nodeId: number, content: string): Promise<{ md: string }>
```

- [ ] **Step 3: 验证**

Run: `npm run typecheck`
Expected: 双配置通过

---

### Task 4: MdDialog 扩展 digPanel 渲染位

**Files:**
- Modify: `src/components/MdDialog.tsx`

- [ ] **Step 1: studyBar 接口加 digPanel**

`MdDialogProps` 内 `studyBar` 对象类型（约 51-58 行）追加可选字段：

```ts
    /** 深挖结果面板（优化建议区 260912，仅学习库传入）：渲染在操作条上方，内容由调用方构造 */
    digPanel?: ReactNode
```

若文件顶部未 import `ReactNode` 则补 `import type { ReactNode } from 'react'`。

- [ ] **Step 2: 渲染位插入**

在 `{studyBar && !editing && (` 的 `dialog-footer` 块（约 426 行）**之前**插入：

```tsx
        {studyBar && !editing && studyBar.digPanel && (
          <div className="dialog-footer learn-dig-panel">{studyBar.digPanel}</div>
        )}
```

其他模块调用方不传 `digPanel`，零影响。

- [ ] **Step 3: 验证**

Run: `npm run typecheck`
Expected: 通过

---

### Task 5: LearnModule 深挖状态重写

**Files:**
- Modify: `src/modules/learn/LearnModule.tsx`

- [ ] **Step 1: 新增 state 与回调（替换原 onDig，原 366-380 行）**

删除原 `onDig`（调 `props.onOpenAi` 自动发右栏的版本），新增：

```ts
  // 深挖（260912）：弹窗内生成 → 确认写入卡片；不再自动发右栏（右栏追问走手动问 AI）
  const [dig, setDig] = useState<{ phase: 'loading' | 'done'; content: string; jobId: string | null } | null>(
    null
  )

  const onDig = useCallback(
    async (c: LearnCardRow) => {
      if (dig?.phase === 'loading') return
      if (!(await window.api.ai.configured())) {
        setGoConfig(true)
        return
      }
      const jobId = crypto.randomUUID()
      setDig({ phase: 'loading', content: '', jobId })
      try {
        const r = await window.api.learn.dig(jobId, c.id)
        setDig({ phase: 'done', content: r.content, jobId: null })
      } catch (e) {
        setDig(null)
        handleAiError(e)
      }
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [dig]
  )

  const cancelDig = (): void => {
    if (dig?.jobId) void window.api.ai.cancel(dig.jobId)
    setDig(null)
  }

  const onDigApply = async (): Promise<void> => {
    if (!card || dig?.phase !== 'done') return
    try {
      await window.api.learn.digApply(card.id, dig.content)
      toast('已写入卡片')
      setDig(null)
      setMdVersion((v) => v + 1) // 触发 MdDialog 重挂重读（Task 6 key 扩展）
    } catch (e) {
      toast(`写入失败：${String((e as Error).message).slice(0, 80)}`)
    }
  }
```

注意：`handleAiError`/`toast`/`setGoConfig`/`setCard`/`setMdVersion` 均为组件既有成员；若 `MdView` 未 import 则补 `import MdView from '../../components/MdView'`。

- [ ] **Step 2: MdDialog 挂载处改造（约 795-805 行）**

`key` 扩展带上 `mdVersion`（写入后重挂重读），`studyBar` 传入 `digPanel`：

```tsx
      <MdDialog
        key={card ? `${card.id}-${mdVersion}` : 'none'}
        open={card != null}
        title={card?.title ?? ''}
        titleTag={card ? `${card.domain_name} · ${card.topic_title ?? ''}` : undefined}
        filePath={card ? `md/learn/${card.id}.md` : ''}
        onClose={() => {
          setCard(null)
          setDig(null)
        }}
        onChanged={() => setMdVersion((v) => v + 1)}
        selectionActions={{ onHighlight: (t) => void onHighlight(t), onAskAi }}
        studyBar={
          card
            ? {
                ...studyBarOf(card),
                onDig: () => void onDig(card),
                digPanel:
                  dig == null ? undefined : (
                    <div style={{ width: '100%', display: 'flex', flexDirection: 'column', gap: 8 }}>
                      {dig.phase === 'loading' ? (
                        <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
                          <span className="module-sub">AI 深挖中…</span>
                          <button className="btn" onClick={cancelDig} title="取消本次生成">
                            <span className="material-symbols-outlined">stop_circle</span>
                            取消
                          </button>
                        </div>
                      ) : (
                        <>
                          <div className="learn-dig-md">
                            <MdView md={dig.content} />
                          </div>
                          <div style={{ display: 'flex', gap: 8, justifyContent: 'flex-end' }}>
                            <button className="btn" onClick={() => setDig(null)}>
                              放弃
                            </button>
                            <button className="btn" onClick={() => void onDig(card)}>
                              重新生成
                            </button>
                            <button className="btn btn-primary" onClick={() => void onDigApply()}>
                              <span className="material-symbols-outlined">save</span>
                              写入卡片
                            </button>
                          </div>
                        </>
                      )}
                    </div>
                  )
              }
            : undefined
        }
      />
```

- [ ] **Step 3: App.css 追加深挖结果区样式**

在 App.css 学习库样式区（`learn-` 相关规则附近）追加：

```css
/* 深挖结果区（260912）：弹窗内预览，限高内部滚动 */
.learn-dig-panel { flex-direction: column; align-items: stretch; }
.learn-dig-md {
  max-height: 300px;
  overflow-y: auto;
  padding: 10px 12px;
  border: 1px solid var(--color-border);
  border-radius: 8px;
  background: var(--color-surface);
}
```

- [ ] **Step 4: 验证**

Run: `npm run typecheck && npm run build`
Expected: 双配置 + build 通过

---

### Task 6: 冒烟清单（开发者执行）

- [ ] 深挖 → loading 态可取消 → 重新深挖 → 结果预览滚动正常
- [ ] 「写入卡片」→ toast + 弹窗内容刷新，末尾出现 `## 深挖（260912）` 小节
- [ ] 再次深挖写入 → 第二节累加，日期正确
- [ ] 在卡片编辑态删改深挖小节正常保存
- [ ] 「放弃」关闭结果区；「重新生成」换新内容
- [ ] LLM 未配置：点深挖弹「去配置」
- [ ] 其他模块 MdDialog（格言笔记/万象卡/致知己版本等）外观与交互零变化
- [ ] 双主题目检深挖结果区
