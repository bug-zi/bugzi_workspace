# 灵感泉 AI 生成 prompt 规范 v2 实现计划

> **状态**：已归档——260907 执行完成（三批次实施 + 每批规格/质量双审查 + 终审 Approved，typecheck 双侧 PASS），spec 见 `specs/archive/2026-09-07-灵感泉生成prompt规范v2-design.md`，实施记录见 `docs/log/260907.md`。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 治第18轮两阶段管线的三个实测痛点——标题故弄玄虚、summary 谜语化、批次同质化：标题改「画面+实体锚点」、summary 改两句白话模板（≤100 字）、批内 form 配额 + 跨批随机风向标，toast 顺带展示本期风向。

**Architecture:** 全部改动收敛在 `electron/ai/services.ts` 的 `generateInspirations()` 及其解析辅助函数（prompt 重写 + form 字段容错 + 风向标常量池 + 配额降级挑选），渲染层仅 toast 文案与预览字数两行，IPC 三件套返回类型加 `winds`。无 DB 迁移、无 UI 结构改动、无 settings key。

**Tech Stack:** Electron 44 主进程 + TypeScript；LLM 调用复用现有 `chatCompletion`（两趟温度 0.95/0.4 不变）。

**Spec:** `docs/project/左侧边栏/灵感泉/archive/2026-09-07-灵感泉生成prompt规范v2-design.md`（260907 已批准，实施后已归档）

---

## 项目适配说明（优先于技能默认）

- **无测试框架**：本项目 v1.0 起只以 `npm run typecheck` + 人工冒烟为验证口径（package.json 无 test 脚本，20 轮优化皆如此），本计划沿用——每个任务以 typecheck 为机械验证，最后统一冒烟；不引入测试框架（YAGNI）。
- **Git 纪律**：禁止 AI 执行任何 git 操作（CLAUDE.md）。各任务的「提交检查点」只给出建议提交信息，**由开发者手动执行**，AI 不代跑。
- **工作区**：直接在 dev 分支工作区实施（项目 20+ 轮惯例，不开 worktree）。

## 文件结构

| 文件 | 职责 | 动作 |
| --- | --- | --- |
| `electron/ai/services.ts` | 灵感生成主战场：常量池、解析容错、两阶段 prompt、降级挑选、返回值 | 改 |
| `electron/preload.ts:197` | `inspirations.generate` 返回类型 | 改 |
| `src/renderer/api.d.ts:567` | 同上（渲染层类型镜像） | 改 |
| `src/modules/inspirations/InspirationsModule.tsx:54-60,188` | 预览 100→120 字、toast 风向从句 | 改 |
| `electron/ipc.ts:468` | handler 直接透传 `generateInspirations()` 返回值 | **不改**（自动携带 winds） |
| `docs/project/左侧边栏/灵感泉/designs-specs.md` §6.2 | prompt 描述同步（AI 维护职责） | 改 |
| `docs/project/优化建议区.md` | 任务2 移入归档区新轮次 | 改 |
| `docs/log/260907.md` | 当日开发日志追加 | 改 |

---

### Task 1: services.ts——form 枚举、风向标词池与抽取、解析容错

**Files:**
- Modify: `electron/ai/services.ts:419-441`（parseInspirationArray）+ 顶部常量区

- [ ] **Step 1: 在 `parseInspirationArray` 上方（约 `:416` 注释行前后）加常量与工具函数**

```ts
/** 灵感形态枚举（优化建议区任务2）：发散候选携 form 标签，自评按配额挑 5，跨阶段透传 */
const INSPIRATION_FORMS = [
  '实用工具',
  '游戏与玩具',
  '内容创作',
  '数据可视化',
  '实验探索',
  '艺术表达',
  '社群活动'
] as const

/** 风向标基调词池（优化建议区任务2）：每批随机抽 1~2 个注入发散 prompt，跨批换口味；与手动方向指引冲突时指引优先 */
const WIND_POOL = [
  '实用主义',
  '纸上原型',
  '数据控',
  '声音实验',
  '时间胶囊',
  '荒诞幽默',
  '城市观察',
  '怀旧电子',
  '桌面游戏',
  '手作实感',
  '极简主义',
  '社群之夜',
  '慢生活',
  '解谜推理'
] as const

/** 抽取本批风向：1~2 个不重复基调词（个数各半概率），池恒有 14 词不会抽空 */
function pickWinds(): string[] {
  const pool = [...WIND_POOL]
  const count = Math.random() < 0.5 ? 1 : 2
  const winds: string[] = []
  for (let i = 0; i < count && pool.length > 0; i++) {
    winds.push(pool.splice(Math.floor(Math.random() * pool.length), 1)[0])
  }
  return winds
}
```

- [ ] **Step 2: 改 `parseInspirationArray` 返回类型与 form 容错**

将 `electron/ai/services.ts:419-441` 整个函数替换为（必填仍只有 title/summary；form 缺失/非法落「未分类」，不丢条目）：

```ts
/** 解析 LLM 返回的灵感数组（兼容 ```json 与 {"inspirations":[...]} 对象包裹，同 parseJsonArray 容错）；form 非必填，非法落「未分类」（优化建议区任务2） */
function parseInspirationArray(raw: string): { title: string; summary: string; form: string }[] {
  const text = raw.replace(/^[\s\S]*?```(?:json)?\s*\n?/, '').replace(/\n?```\s*[\s\S]*$/, '').trim()
  let parsed: unknown = JSON.parse(text)
  if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
    parsed = Object.values(parsed).find((v) => Array.isArray(v)) ?? parsed
  }
  if (!Array.isArray(parsed)) throw new Error('LLM 未返回 JSON 数组')
  const out: { title: string; summary: string; form: string }[] = []
  for (const item of parsed) {
    // title/summary 任一缺失或空白即跳过（specs §6.2 解析容错）；form 只做归一不设卡
    if (
      item &&
      typeof item.title === 'string' &&
      typeof item.summary === 'string' &&
      item.title.trim() &&
      item.summary.trim()
    ) {
      const form =
        typeof item.form === 'string' && (INSPIRATION_FORMS as readonly string[]).includes(item.form)
          ? item.form
          : '未分类'
      out.push({ title: item.title.trim(), summary: item.summary.trim(), form })
    }
  }
  if (out.length === 0) throw new Error('LLM 返回数组为空或字段缺失')
  return out
}
```

- [ ] **Step 3: typecheck 验证**

Run: `npm run typecheck`
Expected: **FAIL**——`generateInspirations`（:576/:606 等）把带 form 的返回值赋给 `{ title: string; summary: string }[]` 报类型不匹配。这正是 Task 2~4 要接的线；若想任务边界干净，可先把 :574 与 :604 的声明改为 `{ title: string; summary: string; form: string }[]` 再跑（Task 2/3 一并会动到这两行）。**本步骤允许 FAIL 收尾，进入 Task 2。**

---

### Task 2: services.ts——发散阶段 prompt 重写（标题锚点 + summary 两句 + 风向注入）

**Files:**
- Modify: `electron/ai/services.ts:533-580`（generateInspirations 头部与 divergePrompt）

- [ ] **Step 1: 风向抽取与 windBlock（插在 `tasteWithGuide` 拼装之后、divergePrompt 之前）**

```ts
  // 风向标（优化建议区任务2）：每批随机 1~2 个基调词注入，跨批换口味；与方向指引冲突时指引优先
  const winds = pickWinds()
  const windBlock = `## 本批风向（每批随机轮换的基调倾向）\n${winds.join('、')}——12 条中 3~5 条向风向靠拢即可，其余自由发挥；若与「我的方向指引」冲突，以方向指引为准。`
```

- [ ] **Step 2: 用下方整体替换 `divergePrompt`（:551-567）**

```ts
  // 阶段一：发散（temperature 高，出 12 条粗点子；标题锚点/summary 两句/风向注入——优化建议区任务2）
  const divergePrompt = `你是一位挑剔的创意策展人，服务一位有独立品味的开发者。你的任务不是罗列平庸点子，也不是堆砌文艺概念，而是提出让人看了想立刻动手的项目灵感。

${tasteWithGuide}

${windBlock}

## 已有灵感清单（这些方向勿重复或高度雷同）
${avoidList}

## 生成规则（严格执行）
1. 恰好生成 12 条项目灵感。
2. 形态必须分散：每条标注 form 字段，从这 7 种里选——实用工具 / 游戏与玩具 / 内容创作 / 数据可视化 / 实验探索 / 艺术表达 / 社群活动；至少覆盖 4 种不同形态，严禁全部是「做一个工具/助手/平台」。
3. 每条必须具体可执行：说得清第一步做什么（最小版本要动手做的事）。
4. 禁止拼装式命名：「XX记录/追踪/打卡/复盘/管理 + 工具/助手/平台/工作台」这类句式模板一律不要。
5. 禁止平庸，也禁止故弄玄虚：安全但无聊的清单/CRUD 类点子不要；纯隐喻、读完不知道要做什么的概念装置也不要。
6. 与口味呼应：至少一半灵感能与口味材料（格言气质/求知领域/方向指引）看出真实连接，但严禁生搬硬套题材。
7. title：10~25 字，结构为「画面 + 实体锚点」——前半可以有画面感/氛围，后半必须落到看得见摸得着的实体（卡片、电台、程序、语音房、比赛、图鉴、博物馆、原型等）或具体数量（8张、7分钟、一晚）。正例：「税册里的雨神：8张证物卡」「匿名电台：只开七分钟，一生一次」；反例：「从税册里挖出一位失真的神」——纯隐喻，读完不知道要做什么。
8. summary：≤100 字，两句结构——第一句大白话说清「这是什么 + 好玩/值得在哪」，第二句以「第一步：」开头给出最小可动手动作；像跟朋友解释一样写，禁止「先…」开头句式，禁止文艺化压缩（每个名词都要落到具体动作/物件）。

以 JSON 对象返回，最外层是对象，格式：{"inspirations":[{"title":"...","summary":"...","form":"..."}]}，inspirations 数组内恰好 12 项，不要输出其他任何内容。`
```

- [ ] **Step 3: 候选变量声明改带 form（:574）**

```ts
  let candidates: { title: string; summary: string; form: string }[]
```

（`callDiverge` 与 try/catch 两行不动——parse 返回类型已在 Task 1 放宽。）

- [ ] **Step 4: typecheck 验证**

Run: `npm run typecheck`
Expected: 可能仍 FAIL（reviewPrompt 的 `list` 参数类型与 :604 `items` 声明未跟上）——留给 Task 3；若已 PASS 也可继续。

---

### Task 3: services.ts——自评阶段 prompt 改造（form 展示 + 批内配额 + 五维 + 两句打磨）

**Files:**
- Modify: `electron/ai/services.ts:582-614`（reviewPrompt 与 items 声明）

- [ ] **Step 1: 用下方整体替换 `reviewPrompt`（:583-597）**

```ts
  // 阶段二：自评筛选（temperature 低，评审收敛；批内 form 配额 + 标题可读性维度——优化建议区任务2）
  const reviewPrompt = (list: { title: string; summary: string; form: string }[]): string =>
    `你是同一位创意策展人，现在进行内部审稿：从下面 ${list.length} 条候选项目灵感中选出恰好 5 条最好的。

${tasteWithGuide}

## 候选灵感
${list.map((c, i) => `${i + 1}. [${c.form}] ${c.title}：${c.summary}`).join('\n')}

## 评审规则（严格执行）
1. 心中按五维给每条打分（1~5）：新颖度、契合口味、具体度、兴奋度、标题可读性（不看 summary 能否猜到要做什么）——分数不用输出，只用于取舍。
2. 批内多样性硬配额：5 条至少覆盖 3 种不同 form，同一种 form 至多 2 条。
3. 淘汰同质（多条同类只留最好一条）、平庸、空泛、与「不想要的方向」冲突的。
4. 恰好选出 5 条；合格不足 5 条时从剩余中挑相对好的补足，并把 summary 打磨到位。
5. 打磨每条 summary：≤100 字，两句结构——第一句大白话说清「这是什么 + 好玩/值得在哪」，第二句以「第一步：」开头给出最小可动手动作；禁止「先…」开头，禁止文艺化压缩。

以 JSON 对象返回，最外层是对象，格式：{"inspirations":[{"title":"...","summary":"...","form":"..."}]}，inspirations 数组内恰好 5 项，title 与 form 沿用候选原文，不要输出其他任何内容。`
```

（`callReview` 的参数类型同步为 `{ title: string; summary: string; form: string }[]`。）

- [ ] **Step 2: `items` 声明改带 form（:604）**

```ts
  let items: { title: string; summary: string; form: string }[]
```

- [ ] **Step 3: typecheck 验证**

Run: `npm run typecheck`
Expected: 仅剩 Task 4 的 `slice(0, 5)` 兜底与返回值缺 `winds` 的报错（若有）；或 PASS。

---

### Task 4: services.ts——配额降级挑选 + 返回 winds

**Files:**
- Modify: `electron/ai/services.ts:523-526`（接口）+ `:604-614`（降级路径）+ `:641`（返回值）

- [ ] **Step 1: 扩展返回接口（:523-526）**

```ts
export interface GenerateInspirationsResult {
  generated: number
  inserted: number
  /** 本批风向（1~2 个基调词），渲染层 toast 展示用（优化建议区任务2） */
  winds: string[]
}
```

- [ ] **Step 2: 在 `generateInspirations` 上方加配额挑选函数（与 pickWinds 相邻亦可，但保持导出函数在前辅助在后的现有文件习惯，放 inspirationTasteProfile 之后）**

```ts
/**
 * 降级路径的代码侧配额挑选（优化建议区任务2）：自评两趟均失败时按 form 多样性贪心取 5 条——
 * 同 form ≤2、每轮优先补出现次数最少的形态；候选全为「未分类」等配额不可满足场景按原顺序取前 5 条兜底。
 */
function pickDiverseFive(
  candidates: { title: string; summary: string; form: string }[]
): { title: string; summary: string; form: string }[] {
  const picked: { title: string; summary: string; form: string }[] = []
  const formCount = new Map<string, number>()
  const isPicked = (c: { title: string; summary: string; form: string }): boolean =>
    picked.some((p) => p === c)
  while (picked.length < 5) {
    let bestIdx = -1
    for (let i = 0; i < candidates.length; i++) {
      const c = candidates[i]
      if (isPicked(c)) continue
      const cnt = formCount.get(c.form) ?? 0
      if (cnt >= 2) continue // 该形态已满 2 条
      if (bestIdx === -1 || cnt < (formCount.get(candidates[bestIdx].form) ?? 0)) bestIdx = i
    }
    if (bestIdx === -1) break // 剩余候选形态均满额或已耗尽 → 走原顺序兜底
    picked.push(candidates[bestIdx])
    formCount.set(candidates[bestIdx].form, (formCount.get(candidates[bestIdx].form) ?? 0) + 1)
  }
  // 兜底：配额不可满足时按原顺序补满（含全「未分类」场景）
  for (const c of candidates) {
    if (picked.length >= 5) break
    if (!isPicked(c)) picked.push(c)
  }
  return picked.slice(0, 5)
}
```

- [ ] **Step 3: 降级路径换掉裸 slice（:610-612 的 catch 内）**

```ts
    } catch {
      // 自评两趟均失败 → 代码侧按 form 配额挑前 5，不空手而归也不带病降级（优化建议区任务2）
      items = pickDiverseFive(candidates)
    }
```

- [ ] **Step 4: 返回值带风向（:641）**

```ts
  return { generated: items.length, inserted, winds }
```

- [ ] **Step 5: typecheck 验证（services.ts 主线完成）**

Run: `npm run typecheck`
Expected: **PASS**（node 侧）。若 web 侧报 `winds` 不在渲染层类型里——正是 Task 5。

- [ ] **Step 6: 提交检查点（开发者手动执行）**

建议提交信息：`灵感泉生成管线 v2：标题实体锚点 + summary 两句白话 + form 配额 + 风向标（主进程侧）`

---

### Task 5: IPC 类型与渲染层（toast 风向 + 预览 120 字）

**Files:**
- Modify: `electron/preload.ts:197`
- Modify: `src/renderer/api.d.ts:567`
- Modify: `src/modules/inspirations/InspirationsModule.tsx:54-60,188`

- [ ] **Step 1: preload.ts 返回类型（:196-198）**

```ts
    /** 「来5条灵感」：两阶段生成（发散 12 → 配额自评 5）入草稿区；winds 为本批风向（specs §6.2 + 优化建议区任务2） */
    generate: (): Promise<{ generated: number; inserted: number; winds: string[] }> =>
      ipcRenderer.invoke('inspirations:generate'),
```

- [ ] **Step 2: api.d.ts 返回类型（:566-567）**

```ts
    /** 「来5条灵感」：两阶段生成（发散 12 → 配额自评 5）入草稿区；winds 为本批风向（specs §6.2 + 优化建议区任务2） */
    generate(): Promise<{ generated: number; inserted: number; winds: string[] }>
```

（`electron/ipc.ts:468` 透传返回值，无需改动。）

- [ ] **Step 3: 预览 100→120 字（InspirationsModule.tsx:54-60，注释与切片同步）**

```ts
    // 批量读 md 正文做预览：压平空白取前 120 字（summary 放宽 ≤100 字后「第一步」大概率可见；读取失败静默为空；v11 起正文不含标题行）
    const entries = await Promise.all(
      rows.map(async (it) => {
        let body = ''
        try {
          const raw = await window.api.md.read(it.md_path)
          body = raw.replace(/\s+/g, ' ').trim().slice(0, 120)
        } catch {
          /* 无正文则不显示预览 */
        }
        return [it.id, body] as const
      })
    )
```

- [ ] **Step 4: toast 追加风向从句（:188）**

```ts
      const r = await window.api.inspirations.generate()
      toast(
        r.winds?.length
          ? `已生成 ${r.inserted} 条灵感，已入草稿区；本期风向：${r.winds.join(' · ')}`
          : `已生成 ${r.inserted} 条灵感，已入草稿区`
      )
```

（`r.winds?.length` 防御 dev 模式下主进程/渲染层代码新旧不同步的瞬间。）

- [ ] **Step 5: typecheck 验证（全量）**

Run: `npm run typecheck`
Expected: **PASS**（node + web 双 tsconfig）。

- [ ] **Step 6: 提交检查点（开发者手动执行）**

建议提交信息：`灵感泉生成管线 v2：toast 展示本期风向 + 卡片预览放宽至 120 字`

---

### Task 6: 冒烟验证 + 文档同步与归档

**Files:**
- Modify: `docs/project/左侧边栏/灵感泉/designs-specs.md` §6.2
- Modify: `docs/project/优化建议区.md`
- Modify: `docs/log/260907.md`
- Move: `docs/project/左侧边栏/灵感泉/archive/2026-09-07-灵感泉生成prompt规范v2-design.md`（已执行；260907 又随目录规范化自 superpowers 迁入）

- [ ] **Step 1: 冒烟（开发者配合，App 需已配 LLM）**

`npm run dev` 启动 → 灵感泉连点两批「来5条灵感」，逐项核对：
1. 标题：每条含实体锚点，无纯隐喻（如「从税册里挖出一位失真的神」式）；
2. summary：两句结构、第二句「第一步：」开头、无「先…」开头、大白话不谜语；卡片预览能看到第一步（120 字内）；
3. 批内：5 条 form ≥3 种（点开 md 弹窗看正文风格分布即可肉眼判断），无单一形态 ≥3 条；
4. 跨批：两批 toast 风向不同，标题气质有轮换；
5. toast 末尾有「本期风向：X · Y」；
6. 无功能回归：入库位置草稿区末尾、AI 徽章、标题查重（同批无重复）、「灵感方向指引」弹窗正常。

- [ ] **Step 2: specs §6.2 prompt 段落同步（designs-specs.md :85-107 一带）**

将 §6.2 开头到「**返回**」前的正文与 prompt 代码块替换为下述内容（其余小节不动）：

````markdown
### 6.2 从零生成 generateInspirations（「来5条灵感」；优化建议区第18轮两阶段 + 任务2规范 v2）

- **口味注入**：手动「方向指引」（settings.inspiration_guide，置顶最高优先级，留空不注入）+ `inspirationTasteProfile()` 跨模块画像（个人画像摘要 / 格言正式区≤30条 / 万象词条按板块 / 手写灵感标题；AI 生成条不进画像只进避免清单）。
- **避免清单**：`SELECT title FROM inspirations ORDER BY updated_at DESC LIMIT 500`（**不过滤 deleted_at**，含回收站，防 prompt 超长）。
- **风向标（任务2）**：代码内置 14 个基调词池（实用主义/纸上原型/数据控/声音实验/时间胶囊/荒诞幽默/城市观察/怀旧电子/桌面游戏/手作实感/极简主义/社群之夜/慢生活/解谜推理），每批随机抽 1~2 个注入发散 prompt；12 条中 3~5 条靠拢即可；与方向指引冲突时指引优先。
- **两阶段管线**（jsonMode，对象包裹返回）：
  - 阶段一发散（temperature 0.95）：「挑剔的创意策展人」人设，恰好 12 条，每条 `{"title","summary","form"}`；form 从 7 种形态枚举选（实用工具/游戏与玩具/内容创作/数据可视化/实验探索/艺术表达/社群活动），至少覆盖 4 种；title 10~25 字「画面+实体锚点」结构（prompt 内置正反例，禁纯隐喻）；summary ≤100 字两句结构（第一句大白话「是什么+值得在哪」，第二句「第一步：」最小动作；禁「先…」开头、禁文艺化压缩）。
  - 阶段二自评（temperature 0.4）：五维打分（新颖度/契合口味/具体度/兴奋度/**标题可读性**），批内硬配额「5 条 ≥3 种 form、同 form ≤2 条」，恰好选 5 并按两句模板打磨 summary；title 与 form 沿用候选原文。
- **解析**：`parseInspirationArray`（兼容 ```json 与对象包裹）；title/summary 任一缺失或空白跳过；**form 非必填，非法/缺失落「未分类」**；0 条有效抛「LLM 未返回有效灵感」。两阶段解析失败各自动重试一次。
- **降级**：自评两趟均失败 → `pickDiverseFive()` 代码侧按 form 配额贪心取 5 条（同 form ≤2、优先补出现最少形态；配额不可满足按原顺序前 5 兜底），不空手而归。
- **入库**（解析成功后统一执行，任一环节失败整体不入库）：逐条精确查重（不过滤 deleted_at）命中跳过；`status='draft'`、`origin='ai'`、sort 取草稿区 `MAX(sort)+1` 递增；md 正文只存 summary（两句间单换行，第19轮起不写标题行）。
- **返回** `{ generated, inserted, winds }`；渲染层 toast「已生成 N 条灵感，已入草稿区；本期风向：X · Y」+ 列表刷新；卡片正文预览取前 120 字。
````

- [ ] **Step 3: 优化建议区归档**

`docs/project/优化建议区.md`：待完成区删除「任务2」条目；归档区顶部（现第20轮之上）插入新小节（轮次号以归档时最新轮次 +1，当前应为第21轮）：

```markdown
### 第21轮（260907）：灵感泉 AI 生成 prompt 规范 v2

- [X]  灵感泉中的AI生成点子需要好好prompt规范一下：当前：【title：灵感标题（10~25 字，具体、可执行，不要空泛口号）；summary：一句话简介（≤50 字，说明这是什么、有什么价值）】（260907 完成：第18轮两阶段管线的二轮反馈——实测 9/6 两批钟摆从「CRUD 工具」荡到「文艺概念装置」，开发者勾选三痛点：标题故弄玄虚/summary 谜语化/新型同质化。三项规范——**标题锚点**：10~25 字「画面+实体锚点」结构，前半可文艺、后半必须落到实体（卡片/电台/程序/8张/7分钟），prompt 内置正反例禁纯隐喻；**summary 白话化**：两句模板 ≤100 字（第一句大白话「是什么+值得在哪」，第二句「第一步：」最小动作），禁「先…」开头与文艺化压缩，卡片预览 100→120 字让第一步可见；**反同质化**：发散输出携 form 标签（7 形态枚举），自评加批内硬配额「5 条 ≥3 形态、同 form ≤2」+ 第五维「标题可读性」打分，跨批随机风向标（内置 14 基调词池每批抽 1~2 注入，3~5 条靠拢即可、与方向指引冲突指引优先），自评降级路径同步升级为代码侧配额贪心挑选；toast 顺带「本期风向：X · Y」。设计文档 `docs/project/左侧边栏/灵感泉/archive/`（已归档）。typecheck 通过，待冒烟看两批生成质量）
```

- [ ] **Step 4: 开发日志追加（docs/log/260907.md 末尾，沿用 `## 小节` + 要点列表格式）**

```markdown
## 灵感泉 AI 生成 prompt 规范 v2（优化建议区任务2）

- **背景**：第18轮两阶段管线实测仍不满意——9/6 两批 10 条全成「文艺概念装置」（标题纯隐喻、summary 压缩成谜语、批次同味）。brainstorming 逐项确认六决策（标题锚点/summary 两句/批内配额+风向标/风向静默+toast/预览 120/自学习闭环列 V2 预留），spec：`docs/project/左侧边栏/灵感泉/archive/2026-09-07-灵感泉生成prompt规范v2-design.md`（已随实施归档至 specs/archive/）。
- **改动**：`electron/ai/services.ts`——INSPIRATION_FORMS 七形态枚举 + WIND_POOL 14 基调词池 + pickWinds()；parseInspirationArray 增 form 容错（非法/缺失落「未分类」不丢条目）；发散 prompt 规则重写（title「画面+实体锚点」带正反例、summary 两句模板 ≤100 字禁「先…」、风向注入 3~5 条靠拢且指引优先）；自评 prompt 五维+批内硬配额（≥3 形态、同 form ≤2）+ form 沿用；降级路径 slice(0,5) → pickDiverseFive() 配额贪心；GenerateInspirationsResult 增 winds。渲染层——toast 追加「本期风向：X · Y」、卡片预览 100→120 字；preload/api.d.ts 类型同步（ipc.ts 透传无改动）。
- **不动**：口味注入/避免清单/查重入库/md 模板/refine/两趟温度与重试结构；无 DB 迁移。
- `npm run typecheck` 通过；**待人工冒烟**（两批生成核对：标题锚点、summary 两句白话、form 分布、风向轮换可见）。git 提交由开发者执行。
```

- [ ] **Step 5: spec 归档（superpowers 归档机制）**

`docs/project/左侧边栏/灵感泉/archive/2026-09-07-灵感泉生成prompt规范v2-design.md` → 移入 `docs/project/左侧边栏/灵感泉/archive/`，文件顶部加状态行：

```markdown
> **状态**：已归档——260907 实施完成（typecheck 通过），实施记录见 docs/log/260907.md；计划文档 plans/ 同名文件一并归档。
```

（plans 归档在执行完成后进行，与本计划文件自身一并处理。）

- [ ] **Step 6: 提交检查点（开发者手动执行）**

建议提交信息：`灵感泉生成 prompt 规范 v2 实施完成：文档同步与归档（优化建议区第21轮）`

---

## 自审记录（写计划时已核）

1. **Spec 覆盖**：spec §1（标题规则/summary 规则/风向标）→ Task 2；§2（form 标签/批内配额/五维/打磨同步）→ Task 1+3；§3（parse 容错/降级升级/winds 返回/toast/预览）→ Task 1+4+5；§4 不动项——各任务代码均为定点替换未触碰；§5 落点表与文件结构一致（ipc.ts 明确不改）。验证方式（spec §验证）→ Task 6 Step 1 逐条对应。
2. **占位符**：无 TBD/TODO；每个代码步骤含完整代码。
3. **类型一致性**：`{ title: string; summary: string; form: string }[]` 在 Task 1/2/3/4 间一致；`winds: string[]` 在 Task 4/5 一致；`pickWinds`/`pickDiverseFive`/`INSPIRATION_FORMS`/`WIND_POOL` 命名前后一致；`parseInspirationArray` 唯一调用方为 `generateInspirations`（codegraph 确认），改返回类型无外溢。
