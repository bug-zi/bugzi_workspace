# 藏阅阁（收藏夹 + 藏书架合并）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「收藏夹」与「藏书架」合并为单一左栏模块「藏阅阁」（书架｜收藏双页签），为后续「总导览」腾位；主进程/IPC/DB 零改动。

**Architecture:** 新建薄壳 `ZangyueModule`（module-page flex 列 + recycle-tabs 页签 + `module-live/module-hidden` 保活包裹），常驻嵌入现有 `BookshelfModule` 与 `FavoritesModule` 本体。书架进入阅读器（组件 early-return）时经新增可选 prop `onReadingChange` 上报，壳隐藏头部与页签条，阅读器满栏。设计依据：同目录 `2026-09-12-藏阅阁合并-design.md`。

**Tech Stack:** Electron 44 + React 19 + TypeScript（electron-vite）。无新依赖、无 AI、无 DB 迁移。

**项目纪律（覆盖技能默认）：**
- **本计划不含任何 git 步骤**——所有 git 操作由开发者完成。
- 项目无单测框架 → 每任务验证 = 编辑落盘 + 最终 `npm run typecheck`（双 tsconfig）+ `npm run build` + 开发者运行时冒烟。
- 设计文档写「懒加载注册」，实际代码库为静态 import（App.tsx 顶部）——**以实际代码库为准**，用静态 import。

**行号基准（2026-09-12 实况，实施时以内容匹配为准）：**
- `src/App.tsx`：模块 import 8-23 行（bookshelf=14、favorites=15）；顺序注释 31 行；`MODULES` 32-45 行（favorites=39、bookshelf=40）；MainView 注释 56 行；模块渲染 256-257 行。
- `src/shared/types.ts`：ModuleId 联合类型 4-16 行。
- `src/renderer/api.d.ts`：ModuleId 联合类型 2-15 行（另含历史遗留 `'verify'`，**不动它**）。
- `src/modules/bookshelf/BookshelfModule.tsx`：组件签名 74 行（无 props）；`useModuleActivated('bookshelf',…)` 165-168 行；`useReadingTimer(...)` 181-182 行；阅读视图 early-return `if (reading)` 443 行起；库视图根 `<div className="module-page">` 690-691 行。
- `src/modules/favorites/FavoritesModule.tsx`：`useModuleActivated('favorites',…)` 65 行。
- `src/modules/favorites/favorites.css`：`.fav-page { height: calc(100vh - 36px); … }` 4-9 行。
- 关键既有机制：`.module-live { display: contents }` / `.module-hidden { display: none }`（App.css 54-59）；`.module-page` 本身就是 `display:flex; flex-direction:column; gap:14px; max-width:980px; margin:0 auto`（App.css 62-68），且 **App.css 在模块 css 之后打包**（App.tsx 29 行 `import './App.css'` 晚于模块 import）——同特异度冲突时 App.css 赢，故 `.zy-page` 只补 `height` 这一 App.css 没有的属性，其余全复用。
- 阅读计时防虚计无需新 prop：`useReadingTimer` 的可见性检查含 `hostRef.getBoundingClientRect().height > 0`（useReadingTimer.ts 15-19 行），面板被 `display:none` 藏起时高度为 0 自动暂停——与 App 级 keep-alive 同一机制。

---

### Task 1: 新建 ZangyueModule 壳（tsx + css）

**Files:**
- Create: `src/modules/zangyue/ZangyueModule.tsx`
- Create: `src/modules/zangyue/zangyue.css`

- [ ] **Step 1: 写 `src/modules/zangyue/ZangyueModule.tsx`**

```tsx
// 藏阅阁（收藏夹 + 藏书架合并壳，260912 新功能开发区）：头部 + 页签「书架｜收藏」+ 双面板保活。
// 两面板常驻挂载、module-live/module-hidden 显隐（同 WenbiModule 合并先例）——阅读计时经
// rect 高度=0 天然暂停；书架进入阅读器（early-return）时经 onReadingChange 上报，壳隐藏
// 头部与页签条，阅读器满栏（与合并前行为一致）。tab 会话内保留、重启回默认书架。
import { useCallback, useState } from 'react'
import BookshelfModule from '../bookshelf/BookshelfModule'
import FavoritesModule from '../favorites/FavoritesModule'
import './zangyue.css'

export default function ZangyueModule() {
  const [tab, setTab] = useState<'bookshelf' | 'favorites'>('bookshelf')
  const [readerActive, setReaderActive] = useState(false)
  const onReadingChange = useCallback((v: boolean) => setReaderActive(v), [])
  return (
    <div className="module-page zy-page" style={{ maxWidth: 1200 }}>
      {!readerActive && (
        <>
          <div className="module-header">
            <span className="material-symbols-outlined">collections_bookmark</span>
            <span className="module-title">藏阅阁</span>
            <span className="module-sub">书架 · 收藏</span>
          </div>
          <div className="recycle-tabs">
            <button
              className={`recycle-tab${tab === 'bookshelf' ? ' active' : ''}`}
              onClick={() => setTab('bookshelf')}
            >
              书架
            </button>
            <button
              className={`recycle-tab${tab === 'favorites' ? ' active' : ''}`}
              onClick={() => setTab('favorites')}
            >
              收藏
            </button>
          </div>
        </>
      )}
      <div
        className={tab === 'bookshelf' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={tab !== 'bookshelf'}
      >
        <BookshelfModule onReadingChange={onReadingChange} />
      </div>
      <div
        className={tab === 'favorites' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={tab !== 'favorites'}
      >
        <FavoritesModule />
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 写 `src/modules/zangyue/zangyue.css`**

```css
/* ===== 藏阅阁（收藏夹+藏书架合并壳，260912）：定高 flex 列 =====
   module-page 已是 flex column（App.css 后置打包胜出同特异度），此处只补它没有的 height：
   36 = main-area 上下 padding（18×2，fav-page/bk-reader-page 同款口径）。
   头部与页签条不参与伸展；两面板经 display:contents 直接成为 flex 项。 */
.zy-page {
  height: calc(100vh - 36px);
}
.zy-page > .module-header,
.zy-page > .recycle-tabs {
  flex-shrink: 0;
}
```

- [ ] **Step 3: 确认落盘**

两文件创建后不接线（App.tsx 尚未引用），此时全库 typecheck 仍应通过（未引用不影响）。

---

### Task 2: BookshelfModule 加 onReadingChange + 激活 id 改 zangyue

**Files:**
- Modify: `src/modules/bookshelf/BookshelfModule.tsx:74`（签名）、`:165-168`（激活 id）、`:181-182`（新增上报 effect）

- [ ] **Step 1: 组件签名加可选 props（74 行）**

```tsx
export default function BookshelfModule() {
```
改为：
```tsx
export interface BookshelfModuleProps {
  /** 阅读态上报（藏阅阁壳据此隐藏头部与页签条；不传=行为不变） */
  onReadingChange?: (reading: boolean) => void
}

export default function BookshelfModule(props: BookshelfModuleProps) {
```

- [ ] **Step 2: 阅读态上报 effect（插在 useReadingTimer 之后，181-182 行处）**

```tsx
  // 阅读计时（三条件口径 + 30 秒 flush；书架 v2.0 §五）
  useReadingTimer(reading != null, reading?.id ?? null, readerPageRef)
```
其后追加：
```tsx
  // 阅读态上报（ref 转发防 inline prop 身份变化重触发；进/出阅读器各一次）
  const readingActive = reading != null
  const onReadingChangeRef = useRef(props.onReadingChange)
  onReadingChangeRef.current = props.onReadingChange
  useEffect(() => {
    onReadingChangeRef.current?.(readingActive)
  }, [readingActive])
```
（`useRef`/`useEffect` 该文件已 import，不新增 import。）

- [ ] **Step 3: 激活 id 改 zangyue（165-168 行）**

```tsx
  useModuleActivated('bookshelf', () => {
    void load()
    void refreshStats()
  })
```
改为：
```tsx
  // 260912 收藏夹+藏书架合并藏阅阁：激活 id 随壳（zangyue）
  useModuleActivated('zangyue', () => {
    void load()
    void refreshStats()
  })
```

---

### Task 3: FavoritesModule 激活 id 改 zangyue

**Files:**
- Modify: `src/modules/favorites/FavoritesModule.tsx:65`

- [ ] **Step 1: 改激活 id**

```tsx
  useModuleActivated('favorites', () => void load())
```
改为：
```tsx
  // 260912 收藏夹+藏书架合并藏阅阁：激活 id 随壳（zangyue）
  useModuleActivated('zangyue', () => void load())
```
其余零改动（无 props 需求；拖拽/弹窗/搜索全部不动）。

---

### Task 4: favorites.css 高度策略改 flex 填充

**Files:**
- Modify: `src/modules/favorites/favorites.css:3-9`

- [ ] **Step 1: `.fav-page` 改 flex 项填充**

收藏页从此只作为 zy-page 的 flex 项存在（display:contents 包裹下直接参与 zy-page 布局），固定 vh 计算会让页签条以内容溢出，改为填充剩余高度：

```css
/* 主视图：顶栏 + 列表体（260912 并入藏阅阁：原 height:calc(100vh - 36px) 改为
   填充 zy-page 剩余高度——页签条占了上方空间，固定 vh 会溢出） */
.fav-page {
  flex: 1 1 0;
  min-height: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
}
```
（即删 `height: calc(100vh - 36px);` 一行，加 `flex: 1 1 0; min-height: 0;` 两行，其余属性原样保留。）

---

### Task 5: App.tsx 接线 + ModuleId 类型改造

**Files:**
- Modify: `src/App.tsx:14-15`（import）、`:31`（顺序注释）、`:39-40`（MODULES）、`:56`（MainView 注释）、`:256-257`（渲染）
- Modify: `src/shared/types.ts:3-16`
- Modify: `src/renderer/api.d.ts:2-15`

- [ ] **Step 1: App.tsx import 替换（14-15 行两行并一行）**

```tsx
import BookshelfModule from './modules/bookshelf/BookshelfModule'
import FavoritesModule from './modules/favorites/FavoritesModule'
```
改为：
```tsx
import ZangyueModule from './modules/zangyue/ZangyueModule'
```

- [ ] **Step 2: MODULES 两行并一行（39-40 行）+ 顺序注释（31 行）**

```tsx
  { id: 'favorites', label: '收藏夹', icon: 'bookmark' },
  { id: 'bookshelf', label: '藏书架', icon: 'auto_stories' },
```
改为：
```tsx
  { id: 'zangyue', label: '藏阅阁', icon: 'collections_bookmark' },
```
31 行顺序注释追加一段（保留原有历史注记）：
```tsx
// 左栏模块顺序（260908 重排；260911 格言库并入文笔坊 12→11 项；260911 学习库置顶 11→12 项；260912 优化建议区第41轮重排；260912 收藏夹+藏书架合并藏阅阁 12→11 项）
```

- [ ] **Step 3: MainView 注释（56 行）模块计数改 11**

```tsx
/** 主栏视图：十三模块 + 白噪音混音器页（不进左栏模块列表，入口在左栏底部控件；specs §5.1） */
```
改为：
```tsx
/** 主栏视图：十一模块 + 白噪音混音器页（不进左栏模块列表，入口在左栏底部控件；specs §5.1） */
```

- [ ] **Step 4: 模块渲染两行并一行（256-257 行）**

```tsx
              {m.id === 'bookshelf' && <BookshelfModule />}
              {m.id === 'favorites' && <FavoritesModule />}
```
改为：
```tsx
              {m.id === 'zangyue' && <ZangyueModule />}
```

- [ ] **Step 5: shared/types.ts 联合类型（3-16 行）**

```ts
// 模块标识（260908 辩真阁并入万象库：'verify' 移除，其功能为万象库「辩真」板块；260911 格言库并入文笔坊：'mottos' 移除）
export type ModuleId =
  | 'learn'
  | 'wiki'
  | 'inspirations'
  | 'zhijiji'
  | 'reasoning'
  | 'wenbi'
  | 'bookshelf'
  | 'favorites'
  | 'feed'
  | 'ledger'
  | 'recycle'
  | 'profile'
```
改为：
```ts
// 模块标识（260908 辩真阁并入万象库：'verify' 移除，其功能为万象库「辩真」板块；260911 格言库并入文笔坊：'mottos' 移除；260912 收藏夹+藏书架合并：两 id 移除，并为 'zangyue'）
export type ModuleId =
  | 'learn'
  | 'wiki'
  | 'inspirations'
  | 'zhijiji'
  | 'reasoning'
  | 'wenbi'
  | 'zangyue'
  | 'feed'
  | 'ledger'
  | 'recycle'
  | 'profile'
```

- [ ] **Step 6: renderer/api.d.ts 联合类型（2-15 行）同款替换**

`'bookshelf'`、`'favorites'` 两行删去，原位加 `'zangyue'`，头注释补一句「260912 收藏夹+藏书架合并：'zangyue'」。**历史遗留的 `'verify'` 不动**（非本次范围）。

---

### Task 6: 全库验证

- [ ] **Step 1: 双 tsconfig 类型检查**

Run: `npm run typecheck`
Expected: 两配置均无错误（若报 `'bookshelf'`/`'favorites'` 不存在于 ModuleId，说明有遗漏引用点——全局搜索 `src/` 下 `'bookshelf'|'favorites'` 字符串清零后复跑）。

- [ ] **Step 2: 构建**

Run: `npm run build`
Expected: 通过。

- [ ] **Step 3: 开发者运行时冒烟清单**（对应设计 §六，AI 可用 chrome-devtools CDP 冒烟辅助，最终以开发者验证为准）

- [ ] 左栏 11 项；藏阅阁位于信息源与推理角之间；图标 `collections_bookmark`
- [ ] 进入藏阅阁默认落「书架」页签，页首有「藏阅阁 / 书架 · 收藏」头部
- [ ] 书架列表 ↔ 收藏 页签互切：两面板状态保留、数据正常
- [ ] 打开一本书：头部与页签条消失、阅读器满栏；返回书架：头部与页签条恢复
- [ ] 收藏页签激活期间阅读计时暂停（统计面板今日分钟不增长），切回书架阅读继续累计
- [ ] 收藏夹功能零回归：新增/抓取/详情/编辑/置顶/拖拽/分类管理/搜索/Ctrl+点跳转
- [ ] 书架功能零回归：导入/双引擎阅读/双模式/键盘导航/目录侧栏/笔记/书签/统计/阅读背景/每书偏好
- [ ] 双主题配色正常、无 emoji；其他模块（万象库/文笔坊/致知己等）切换无回归

---

### Task 7: 文档收尾

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/log/260912.md`（追加节）
- Modify: `docs/project/新功能开发区.md`（待完成区第 1 条注记）
- Move: `docs/project/左侧边栏/藏阅阁/2026-09-12-藏阅阁合并-design.md` → 同目录 `archive/`
- Move: 本 plan 文件 → 同目录 `archive/`

- [ ] **Step 1: CLAUDE.md 同步**（先 Grep `收藏夹|藏书架` 定位全部提及处，逐处更新）：
  - 「三栏式布局」全局规则中的左栏顺序：`信息源→收藏夹→藏书架→推理角` 改 `信息源→藏阅阁→推理角`，注明 260912 合并。
  - 「应用架构」开头的模块计数（「左栏十一个入口模块」→「左栏十个入口模块」）。
  - 「藏书架」条目扩写为「藏阅阁」条目：本地电子书阅读（原藏书架内容不动）+ 网站/文章收藏（两级分类、抓取 meta、拖拽/置顶/搜索、Ctrl+点跳转，原收藏夹能力并入）。
  - 其余历史沿革句（260908/260910 等）保留不改。

- [ ] **Step 2: devlog** `docs/log/260912.md` 追加小节（bash `date "+%Y-%m-%d %H:%M"` 取实际时间写「创建于」行）：记录合并实施、改动面（1 新壳 + 4 文件微改 + 类型改造）、验证结果、冒烟待开发者。

- [ ] **Step 3: 新功能开发区.md** 待完成区第 1 条原样保留（总导览未做），条目尾追加注记：`（260912 合并半项已实施：藏阅阁落地，见 左侧边栏/藏阅阁/；总导览待商议）`。

- [ ] **Step 4: 归档**：design 与 plan 两文件移入 `docs/project/左侧边栏/藏阅阁/archive/`（文件移动用文件系统操作，不做 git）。

---

## Self-Review 记录（已核）

1. **Spec 覆盖**：设计 §二命名接线→Task 5；§三壳结构→Task 1；§四面板改动→Task 2/3；高度策略（设计 §三「css 微调」具体化为 flex 填充）→Task 4；§六验收→Task 6；CLAUDE.md/devlog/归档→Task 7。设计 §三「visible prop 防虚计」在探索后升级：rect 高度检查已天然覆盖 display:none，**无需新 prop**（比设计更简，已在计划头部注明依据）。
2. **无占位符**：全部步骤含精确代码/命令/预期。
3. **类型一致性**：`BookshelfModuleProps.onReadingChange?: (reading: boolean) => void` 与壳 `useCallback((v: boolean) => setReaderActive(v))` 一致；`tab` 键 `'bookshelf'|'favorites'` 仅为壳内页签名（与被移除的 ModuleId 无关）；`'zangyue'` 在 types.ts / api.d.ts / 两面板 hook / App.tsx 四处一致。
