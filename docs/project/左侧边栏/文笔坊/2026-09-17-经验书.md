# 文笔坊「经验书」标签页 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **项目纪律（覆盖技能默认）**：本仓库**禁止 AI 执行任何 git 操作**——计划不含 commit 步骤，全部任务完成并验证后由开发者统一提交。本项目无单测框架，验证闸 = `npm run typecheck`（双 tsconfig）+ `npm run build`，功能验收走冒烟清单（见设计文档）。
>
> 设计依据：`docs/project/左侧边栏/文笔坊/2026-09-17-经验书-design.md`

**Goal:** 文笔坊新增第四个 tab「经验书」（格言库｜经验书｜写作台｜浮生记）——一句话经验道理手册，行内快记 + 行内编辑，零 AI，删除入回收站文笔坊块。

**Architecture:** SQLite 新表 `wenbi_experiences`（DB v44）存一句话条目；复用 `discardToRecycle` 通用回收站链路（新 source `wenbi_exp`，恢复/彻底删/3 天过期零特判）；新 IPC 四通道（list/create/update/discard）；新 `ExperiencePanel` 组件行内交互，WenbiModule 四 tab keep-alive 编排。

**Tech Stack:** Electron 44 + React 19 + TypeScript（electron-vite，node:sqlite）。

---

## 文件结构

| 文件 | 操作 | 职责 |
| --- | --- | --- |
| `electron/db/db.ts` | 修改 | v44 迁移：建 `wenbi_experiences` 表 + deleted_at 索引 |
| `src/shared/types.ts` | 修改 | `WenbiExperienceRecord` 接口 + `RecycleRow['source']` 加 `'wenbi_exp'` |
| `src/renderer/api.d.ts` | 修改 | 同上两处类型副本 |
| `electron/services/recycle.ts` | 修改 | RecycleSource/TABLES/MD_FIELDS 三注册 + restore 分支 |
| `electron/ipc.ts` | 修改 | `wenbi:expList/expCreate/expUpdate/expDiscard` 四 handler |
| `electron/preload.ts` | 修改 | `window.api.wenbi` 挂四方法 |
| `src/modules/recycle/RecycleModule.tsx` | 修改 | tabOf/backToOf/srcTag/summaryOf 四函数纳入 `wenbi_exp` |
| `src/modules/wenbi/ExperiencePanel.tsx` | 新建 | 经验书面板：快记框 + 平铺列表 + 行内编辑 + 丢弃确认 |
| `src/modules/wenbi/WenbiModule.tsx` | 修改 | board 加 `'exp'`、tab 顺序、面板挂载、副标题 |
| `src/App.css` | 修改 | `.exp-quick` / `.exp-content` / `.exp-edit` 样式 |
| `CLAUDE.md` | 修改 | 文笔坊条目同步四 tab |
| `docs/` 若干 | 修改 | devlog / 新功能开发区归档 / designs-specs 补章 |

---

### Task 1: DB v44 迁移

**Files:**
- Modify: `electron/db/db.ts`

- [ ] **Step 1: 在 v43 块后追加 v44 块**

定位 v43 块（`if (version < 43) { … d.exec('PRAGMA user_version = 43') }`，约 1041-1057 行），其后、`migrate` 函数收尾 `}` 之前插入：

```ts
  if (version < 44) {
    // v44：文笔坊经验书（2026-09-17-经验书-design.md §一）——一句话经验道理条目，
    // 零 AI 零 md 正文；删除走回收站 wenbi_exp（软删 + 快照，通用链路零特判）。
    d.exec(`CREATE TABLE wenbi_experiences (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      content TEXT NOT NULL,
      created_at TEXT NOT NULL,
      updated_at TEXT NOT NULL,
      deleted_at TEXT
    )`)
    d.exec(
      'CREATE INDEX IF NOT EXISTS idx_wenbi_experiences_deleted ON wenbi_experiences(deleted_at)'
    )
    d.exec('PRAGMA user_version = 44')
  }
```

- [ ] **Step 2: 语法自查**

Run: `npx tsc -p tsconfig.node.json --noEmit`
Expected: 可能报 ipc/preload 相关存量错误（后续任务补齐），本文件自身无新增错误即可；若担心隔离验证，仅确认 db.ts 无红线。

---

### Task 2: 类型定义（shared/types.ts + api.d.ts）

**Files:**
- Modify: `src/shared/types.ts`
- Modify: `src/renderer/api.d.ts`

- [ ] **Step 1: shared/types.ts 加 WenbiExperienceRecord**

定位 `WenbiJournalRecord` 接口（约 549-558 行），其后插入：

```ts
/** 经验书条目（wenbi_experiences 表，DB v44）：一句话经验道理，无标题无 md 正文；删除走回收站 wenbi_exp */
export interface WenbiExperienceRecord {
  id: number
  content: string
  created_at: string
  updated_at: string
  deleted_at: string | null
}
```

- [ ] **Step 2: shared/types.ts RecycleRow source 加 'wenbi_exp'**

定位 `RecycleRow` 接口的 source 联合（约 870-880 行，含 `| 'wenbi_journal'` / `| 'wenbi_article'`），在 `| 'wenbi_article'` 后加一行：

```ts
    | 'wenbi_exp'
```

- [ ] **Step 3: api.d.ts 同步两处**

定位 `WenbiJournalRecord` 接口（约 312-321 行），其后插入（同 Step 1 内容）：

```ts
/** 经验书条目（wenbi_experiences 表，DB v44）：一句话经验道理，无标题无 md 正文；删除走回收站 wenbi_exp */
export interface WenbiExperienceRecord {
  id: number
  content: string
  created_at: string
  updated_at: string
  deleted_at: string | null
}
```

定位 api.d.ts 的 `RecycleRow` source 联合（约 604-628 行，含 `| 'wenbi_journal'` / `| 'wenbi_article'`），在 `| 'wenbi_article'` 后加一行：

```ts
    | 'wenbi_exp'
```

---

### Task 3: 回收站服务接入（recycle.ts）

**Files:**
- Modify: `electron/services/recycle.ts`

- [ ] **Step 1: RecycleSource 联合加 'wenbi_exp'**

第 16-17 行区域：

```ts
  | 'wenbi_journal'
  | 'wenbi_article'
```

`| 'wenbi_article'` 后加：

```ts
  | 'wenbi_exp'
```

- [ ] **Step 2: TABLES 映射加表名**

`TABLES` 常量（第 27-46 行），`wenbi_article: 'wenbi_articles',`（第 37 行）后加：

```ts
  wenbi_exp: 'wenbi_experiences',
```

- [ ] **Step 3: MD_FIELDS 加 null（无附属 md）**

`MD_FIELDS` 常量（第 50-70 行），`wenbi_article: 'md_path',`（第 61 行）后加：

```ts
  wenbi_exp: null,
```

- [ ] **Step 4: restoreFromRecycle 加恢复分支**

`case 'wenbi_journal': … break`（第 168-171 行）后加：

```ts
    case 'wenbi_exp':
      // 回经验书列表：仅清标记（平铺倒序按 created_at 钉位，恢复即回原时间位）
      d.prepare('UPDATE wenbi_experiences SET deleted_at = NULL WHERE id = ?').run(rb.item_id)
      break
```

---

### Task 4: IPC 四通道 + preload 四方法

**Files:**
- Modify: `electron/ipc.ts`
- Modify: `electron/preload.ts`

- [ ] **Step 1: ipc.ts 加四个 handler**

定位 `wenbi:journalDiscard` handler（约 1660-1663 行），其后插入：

```ts
  // ---------- 经验书（DB v44，2026-09-17 新功能开发区）：一句话经验道理，零 AI ----------
  ipcMain.handle('wenbi:expList', () =>
    getDb()
      .prepare(
        'SELECT * FROM wenbi_experiences WHERE deleted_at IS NULL ORDER BY created_at DESC, id DESC'
      )
      .all()
  )
  /** 新建经验条目：服务端兜底 trim 校验（渲染层已先行拦截空内容），返回整行供插列表顶部 */
  ipcMain.handle('wenbi:expCreate', (_e, content: string) => {
    const c = String(content ?? '').trim()
    if (!c) throw new Error('EMPTY_CONTENT')
    const d = getDb()
    const now = nowIso()
    const r = d
      .prepare('INSERT INTO wenbi_experiences (content, created_at, updated_at) VALUES (?, ?, ?)')
      .run(c, now, now)
    return d.prepare('SELECT * FROM wenbi_experiences WHERE id = ?').get(Number(r.lastInsertRowid))
  })
  /** 改经验条目：回写 updated_at，返回更新后整行 */
  ipcMain.handle('wenbi:expUpdate', (_e, id: number, content: string) => {
    const c = String(content ?? '').trim()
    if (!c) throw new Error('EMPTY_CONTENT')
    const d = getDb()
    d.prepare('UPDATE wenbi_experiences SET content = ?, updated_at = ? WHERE id = ?').run(c, nowIso(), id)
    return d.prepare('SELECT * FROM wenbi_experiences WHERE id = ?').get(id)
  })
  ipcMain.handle('wenbi:expDiscard', (_e, id: number) => {
    discardToRecycle('wenbi_exp', id)
    return true
  })
```

注：`nowIso` / `discardToRecycle` 均为 ipc.ts 既有 import（journalCreate / journalDiscard 在用），无需新增。

- [ ] **Step 2: preload.ts wenbi 段加四方法**

定位 `journalDiscard`（第 533 行），其后插入：

```ts
    /** 经验书条目列表（created_at 倒序；零 AI 板块） */
    expList: (): Promise<import('../src/shared/types').WenbiExperienceRecord[]> =>
      ipcRenderer.invoke('wenbi:expList'),
    /** 新建经验条目（trim 后非空才建；返回整行，渲染层插列表顶部） */
    expCreate: (content: string): Promise<import('../src/shared/types').WenbiExperienceRecord> =>
      ipcRenderer.invoke('wenbi:expCreate', content),
    /** 改经验条目（回写 updated_at；返回更新后整行） */
    expUpdate: (id: number, content: string): Promise<import('../src/shared/types').WenbiExperienceRecord> =>
      ipcRenderer.invoke('wenbi:expUpdate', id, content),
    /** 经验条目入回收站（前端二次确认后调用） */
    expDiscard: (id: number): Promise<boolean> => ipcRenderer.invoke('wenbi:expDiscard', id),
```

- [ ] **Step 3: 主进程侧 typecheck**

Run: `npx tsc -p tsconfig.node.json --noEmit`
Expected: PASS（node 侧改动已闭环；web 侧渲染层尚未消费新类型，双配置全绿在 Task 6 后达成）。

---

### Task 5: 回收站 UI 纳入 wenbi_exp（RecycleModule.tsx）

**Files:**
- Modify: `src/modules/recycle/RecycleModule.tsx`

- [ ] **Step 1: tabOf 归组**

第 27 行：

```ts
  if (source === 'wenbi_journal' || source === 'wenbi_article') return 'wenbi'
```

改为：

```ts
  if (source === 'wenbi_journal' || source === 'wenbi_article' || source === 'wenbi_exp') return 'wenbi'
```

- [ ] **Step 2: backToOf 恢复去向文案**

第 62-65 行 `case 'wenbi_journal': … case 'wenbi_article': …` 后加：

```ts
    case 'wenbi_exp':
      return '经验书列表'
```

- [ ] **Step 3: srcTag 来源小字**

第 80-81 行两条 `if` 后加：

```ts
  if (source === 'wenbi_exp') return '经验书 · '
```

- [ ] **Step 4: summaryOf 摘要提取**

第 119 行 `if (row.source === 'wenbi_article') return String(p.title ?? '')` 后加：

```ts
    if (row.source === 'wenbi_exp') return String(p.content ?? '')
```

---

### Task 6: ExperiencePanel 组件 + CSS + WenbiModule 四 tab

**Files:**
- Create: `src/modules/wenbi/ExperiencePanel.tsx`
- Modify: `src/App.css`
- Modify: `src/modules/wenbi/WenbiModule.tsx`

- [ ] **Step 1: 新建 ExperiencePanel.tsx（完整文件）**

```tsx
// 经验书（文笔坊·2026-09-17 新功能开发区）：一句话经验道理手册——行内快记 + 行内编辑，零 AI 硬边界
import { useCallback, useEffect, useRef, useState, type KeyboardEvent } from 'react'
import type { WenbiExperienceRecord } from '../../shared/types'
import ConfirmDialog from '../../components/ConfirmDialog'
import { useToast } from '../../components/Toast'
import { useModuleActivated } from '../../hooks/useModuleActivated'

export default function ExperiencePanel(props: { active: boolean }) {
  const { toast } = useToast()
  const [rows, setRows] = useState<WenbiExperienceRecord[]>([])
  const [draft, setDraft] = useState('')
  // 行内编辑：editId 驱动渲染；ref 镜像供失焦保存读最新值（连点切行时闭包不串行）
  const [editId, setEditId] = useState<number | null>(null)
  const [editText, setEditText] = useState('')
  const editIdRef = useRef<number | null>(null)
  const editTextRef = useRef('')
  const [discardTarget, setDiscardTarget] = useState<WenbiExperienceRecord | null>(null)
  const quickRef = useRef<HTMLTextAreaElement>(null)

  const load = useCallback(async () => {
    try {
      setRows(await window.api.wenbi.expList())
    } catch {
      toast('经验列表加载失败')
    }
  }, [toast])

  useEffect(() => {
    void load()
  }, [load])
  // keep-alive：切回文笔坊模块时刷新（回收站恢复可能改动了列表）
  useModuleActivated('wenbi', () => void load())
  // 板块切回时刷新（四板块常驻挂载，board 切换不触发组件重挂）
  useEffect(() => {
    if (props.active) void load()
  }, [props.active, load])

  /** 快记新建：插列表顶部，焦点回输入框连记；空内容静默不建 */
  const doCreate = async (): Promise<void> => {
    const content = draft.trim()
    if (!content) return
    try {
      const rec = await window.api.wenbi.expCreate(content)
      setRows((rs) => [rec, ...rs])
      setDraft('')
      quickRef.current?.focus()
    } catch {
      toast('新建失败')
    }
  }

  const beginEdit = (it: WenbiExperienceRecord): void => {
    setEditId(it.id)
    editIdRef.current = it.id
    setEditText(it.content)
    editTextRef.current = it.content
  }

  /** 保存当前编辑（Enter / 失焦共用）：空内容视为取消 + 提示；内容未变跳过写库 */
  const commitEdit = async (): Promise<void> => {
    const id = editIdRef.current
    if (id == null) return
    const content = editTextRef.current.trim()
    setEditId(null)
    editIdRef.current = null
    if (!content) {
      toast('内容为空，未保存')
      return
    }
    if (content === rows.find((r) => r.id === id)?.content) return
    try {
      const rec = await window.api.wenbi.expUpdate(id, content)
      setRows((rs) => rs.map((r) => (r.id === id ? rec : r)))
    } catch {
      toast('保存失败')
    }
  }

  const cancelEdit = (): void => {
    setEditId(null)
    editIdRef.current = null
  }

  const onEditChange = (v: string): void => {
    setEditText(v)
    editTextRef.current = v
  }

  const doDiscard = async (): Promise<void> => {
    if (!discardTarget) return
    try {
      await window.api.wenbi.expDiscard(discardTarget.id)
      toast('已放入回收站')
    } catch {
      toast('丢弃失败')
    }
    setDiscardTarget(null)
    await load()
  }

  /** 快记键位：Enter 新建 / Shift+Enter 换行 / Esc 清空 */
  const onQuickKey = (e: KeyboardEvent<HTMLTextAreaElement>): void => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault()
      void doCreate()
    } else if (e.key === 'Escape') {
      setDraft('')
    }
  }

  /** 编辑键位：Enter 保存 / Shift+Enter 换行 / Esc 取消 */
  const onEditKey = (e: KeyboardEvent<HTMLTextAreaElement>): void => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault()
      void commitEdit()
    } else if (e.key === 'Escape') {
      cancelEdit()
    }
  }

  return (
    <div>
      <textarea
        ref={quickRef}
        className="exp-quick"
        placeholder="记一条经验道理…（Enter 保存，Shift+Enter 换行）"
        value={draft}
        rows={1}
        onChange={(e) => setDraft(e.target.value)}
        onKeyDown={onQuickKey}
      />

      {rows.length > 0 && (
        <section className="zone">
          <div className="zone-header">
            <span className="material-symbols-outlined" style={{ fontSize: 18 }}>
              menu_book
            </span>
            <span>经验</span>
            <span className="zone-count">{rows.length}</span>
          </div>
          <div className="zone-body">
            {rows.map((it) =>
              editId === it.id ? (
                <div className="row-item" key={it.id} style={{ cursor: 'default' }}>
                  <div className="row-main" style={{ display: 'flex', alignItems: 'baseline', gap: 10 }}>
                    <span className="journal-date">{fmtDate(it.created_at)}</span>
                    <textarea
                      className="exp-edit"
                      autoFocus
                      rows={1}
                      value={editText}
                      onChange={(e) => onEditChange(e.target.value)}
                      onKeyDown={onEditKey}
                      onBlur={() => void commitEdit()}
                    />
                  </div>
                </div>
              ) : (
                <div
                  className="row-item"
                  key={it.id}
                  style={{ cursor: 'default' }}
                  onDoubleClick={() => beginEdit(it)}
                  title="双击编辑"
                >
                  <div className="row-main" style={{ display: 'flex', alignItems: 'baseline', gap: 10 }}>
                    <span className="journal-date">{fmtDate(it.created_at)}</span>
                    <span className="exp-content">{it.content}</span>
                  </div>
                  <div className="row-actions" onClick={(e) => e.stopPropagation()}>
                    <button className="icon-btn" title="编辑" onClick={() => beginEdit(it)}>
                      <span className="material-symbols-outlined">edit</span>
                    </button>
                    <button className="icon-btn danger" title="丢弃" onClick={() => setDiscardTarget(it)}>
                      <span className="material-symbols-outlined">delete</span>
                    </button>
                  </div>
                </div>
              )
            )}
          </div>
        </section>
      )}
      {rows.length === 0 && (
        <div className="empty-state" style={{ padding: '40px 0' }}>
          <span className="material-symbols-outlined">menu_book</span>
          把踩过的坑和悟出的道理记在这里
        </div>
      )}

      <ConfirmDialog
        open={discardTarget != null}
        title="放入回收站"
        confirmText="丢弃"
        danger
        onConfirm={() => void doDiscard()}
        onCancel={() => setDiscardTarget(null)}
      >
        放入回收站，3 天后自动彻底删除。
      </ConfirmDialog>
    </div>
  )
}

function fmtDate(iso: string): string {
  if (!iso) return ''
  const d = new Date(iso)
  return `${d.getFullYear()}/${d.getMonth() + 1}/${d.getDate()}`
}
```

- [ ] **Step 2: App.css 加样式**

定位 `.journal-date` 块（第 1337-1342 行），其后插入：

```css
/* ===== 文笔坊：经验书（2026-09-17 新功能开发区）：行内快记 + 行内编辑 ===== */
.exp-quick {
  display: block;
  width: 100%;
  min-height: 42px;
  max-height: 160px;
  field-sizing: content;
  resize: none;
  overflow-y: auto;
  border: 1px solid var(--color-border);
  border-radius: 8px;
  background: var(--color-surface);
  color: var(--color-text);
  font: inherit;
  line-height: 1.5;
  padding: 9px 12px;
  margin-bottom: 12px;
}
.exp-quick:focus {
  outline: none;
  border-color: var(--color-primary);
}
.exp-content {
  flex: 1;
  white-space: pre-wrap;
  word-break: break-word;
}
.exp-edit {
  flex: 1;
  min-height: 1.6em;
  max-height: 160px;
  field-sizing: content;
  resize: none;
  overflow-y: auto;
  border: 1px solid var(--color-primary);
  border-radius: 6px;
  background: transparent;
  color: var(--color-text);
  font: inherit;
  line-height: 1.5;
  padding: 1px 6px;
}
.exp-edit:focus {
  outline: none;
}
```

- [ ] **Step 3: WenbiModule.tsx 改四 tab**

整文件替换为：

```tsx
// 文笔坊模块（优化建议区第36轮：格言库并入三 tab；260917 新功能开发区：新增经验书四 tab，默认格言库）
import { useState } from 'react'
import type { AiChannel } from '../../shared/types'
import MottosModule from '../mottos/MottosModule'
import { useModuleNavigate } from '../../hooks/useModuleNavigate'
import ExperiencePanel from './ExperiencePanel'
import JournalPanel from './JournalPanel'
import WritingPanel from './WritingPanel'

export interface WenbiModuleProps {
  /** 透传 App.openAiWith 给格言面板（AI 解读显式带 channel: 'motto'） */
  onOpenAi: (prefill?: string, opts?: { auto?: boolean; channel?: AiChannel }) => void
  bumpAi: () => void
}

export default function WenbiModule(props: WenbiModuleProps) {
  // 默认格言库（承袭其曾是启动首页的地位）；module-hidden 保活，tab 状态会话内保留、重启回默认
  const [board, setBoard] = useState<'mottos' | 'exp' | 'writing' | 'journal'>('mottos')
  // 总导览深链（260912）：切到格言库
  useModuleNavigate('wenbi', (target) => {
    if (target === 'mottos') setBoard('mottos')
  })
  return (
    <div className="module-page" style={{ maxWidth: 1200 }}>
      <div className="module-header">
        <span className="material-symbols-outlined">history_edu</span>
        <span className="module-title">文笔坊</span>
        <span className="module-sub">格言库 · 经验书 · 写作台 · 浮生记</span>
      </div>
      <div className="recycle-tabs">
        <button
          className={`recycle-tab${board === 'mottos' ? ' active' : ''}`}
          onClick={() => setBoard('mottos')}
        >
          格言库
        </button>
        <button
          className={`recycle-tab${board === 'exp' ? ' active' : ''}`}
          onClick={() => setBoard('exp')}
        >
          经验书
        </button>
        <button
          className={`recycle-tab${board === 'writing' ? ' active' : ''}`}
          onClick={() => setBoard('writing')}
        >
          写作台
        </button>
        <button
          className={`recycle-tab${board === 'journal' ? ' active' : ''}`}
          onClick={() => setBoard('journal')}
        >
          浮生记
        </button>
      </div>
      <div
        className={board === 'mottos' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={board !== 'mottos'}
      >
        <MottosModule active={board === 'mottos'} onOpenAi={props.onOpenAi} bumpAi={props.bumpAi} />
      </div>
      <div
        className={board === 'exp' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={board !== 'exp'}
      >
        <ExperiencePanel active={board === 'exp'} />
      </div>
      <div
        className={board === 'writing' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={board !== 'writing'}
      >
        <WritingPanel active={board === 'writing'} />
      </div>
      <div
        className={board === 'journal' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={board !== 'journal'}
      >
        <JournalPanel active={board === 'journal'} />
      </div>
    </div>
  )
}
```

- [ ] **Step 4: 双配置 typecheck**

Run: `npm run typecheck`
Expected: PASS（两 tsconfig 全绿）。

---

### Task 7: 构建 + 文档收尾

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/project/新功能开发区.md`
- Create: `docs/log/260917.md`
- Modify: `docs/project/左侧边栏/文笔坊/designs-specs.md`

- [ ] **Step 1: 构建**

Run: `npm run build`
Expected: 成功产出，无错误。

- [ ] **Step 2: CLAUDE.md 文笔坊条目同步**

「应用架构（规划）」中 **文笔坊** 条目改为（保留格言库/浮生记/写作台既有描述措辞，插入经验书）：

```markdown
- **文笔坊**：格言库 + 写作生活板块——格言库（260911 并入，见上条）+ 经验书（260917 新增：一句话经验道理手动增删改查，行内快记 + 行内编辑零弹窗，零 AI，删除入回收站文笔坊块；DB v44 wenbi_experiences）+ 浮生记（周记/月记/随心记自由记录，零 AI 纯私密）+ 写作台（博客文章四区流转：构思/写作/完稿/已发布，Copilot 式 AI 协笔——起稿/续写/润色/改写以建议卡呈现、逐段采纳）。
```

同条目开头「tab 顺序格言库｜写作台｜浮生记，默认格言库」类表述若存在则改为「四 tab 格言库｜经验书｜写作台｜浮生记，默认格言库」。

- [ ] **Step 3: devlog**

用 `date "+%Y-%m-%d %H:%M"` 取实际时间后创建 `docs/log/260917.md`，记录：需求来源（新功能开发区待完成区）、brainstorm 决策（一句话条目/平铺倒序/行内交互/tab 位次第二/SQLite 方案 A/回收站 wenbi_exp）、DB v44、四 IPC、ExperiencePanel、四 tab 编排、验证结果（typecheck + build）、冒烟待开发者。**每个小节标题下附 `> 创建于 YYYY-MM-DD HH:MM`（取实际时间，不估不编）。**

- [ ] **Step 4: 新功能开发区.md 归档**

该条从「待完成」移入「归档区」顶部，格式对齐既有条目：

```markdown
- [X]  文笔坊中再添加一个“经验书”的标签页模块……（260917 已实施，`左侧边栏/文笔坊/2026-09-17-经验书-design.md` + 同日 plan；brainstorm 决策——一句话短条目、平铺时间倒序、行内快记+行内编辑零弹窗、tab 位次第二（格言库｜经验书｜写作台｜浮生记）、SQLite 方案 A；DB v44 wenbi_experiences + 回收站 wenbi_exp 通用链路（恢复/3 天过期零特判）+ 四 IPC（expList/expCreate/expUpdate/expDiscard）+ ExperiencePanel（快记框 Enter 连记/双击行内编辑/丢弃二次确认）；typecheck 双配置 + build 通过，运行时冒烟待开发者验证）
```

- [ ] **Step 5: designs-specs.md 补章**

在 `docs/project/左侧边栏/文笔坊/designs-specs.md` 末尾追加「经验书」章节（AI 维护文档）：定位（一句话经验道理，零 AI）、数据表结构、四 IPC 通道、行内交互规则（Enter/Shift+Enter/Esc/失焦）、回收站接入、样式类名——以已批准设计文档为准做精简转述，不新造决策。

- [ ] **Step 6: 冒烟移交**

向开发者交付冒烟清单（设计文档「验证」节六条：四 tab 顺序与保活 / 快记连记换行 / 行内编辑四键位与空内容提示 / 丢弃确认与回收站恢复 / 3 天过期回归 / 双主题目检），等待开发者运行 `npm run dev` 实测反馈。

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 §一 DB v44（Task 1）、§二 recycle 接入（Task 3）与四 IPC（Task 4）、§三 渲染层（Task 5/6）、§四 边界（Task 6 组件内 trim/ref 守卫/失焦保存）、§五 文档收尾（Task 7）——逐条有任务对应。
- **占位符扫描**：无 TBD/TODO；所有代码步骤含完整代码。
- **类型一致性**：`WenbiExperienceRecord` 五字段（id/content/created_at/updated_at/deleted_at）在 types.ts、api.d.ts、preload、ipc、Panel 六处一致；source 字面量 `'wenbi_exp'` 五处一致；通道名 `wenbi:expList/expCreate/expUpdate/expDiscard` ipc 与 preload 一致。
