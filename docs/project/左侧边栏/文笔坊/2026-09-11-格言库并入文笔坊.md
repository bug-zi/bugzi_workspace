# 格言库并入文笔坊 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **项目纪律（覆盖技能默认）**：本仓库**禁止 AI 执行任何 git 操作**——计划不含 commit 步骤，全部任务完成并验证后由开发者统一提交。本项目无单测框架，验证闸 = `npm run typecheck`（双 tsconfig）+ `npm run build`，功能验收走冒烟清单（见设计文档）。
>
> 设计依据：`docs/project/左侧边栏/文笔坊/2026-09-11-格言库并入文笔坊-design.md`

**Goal:** 格言库从左栏独立模块移除，成为文笔坊第一块 tab（格言库、写作台、浮生记），App 启动默认落万象库。

**Architecture:** MottosModule 原地面板化（去 module-page/module-header 外壳、加 active prop、刷新事件改挂 wenbi、AI 解读显式传 channel: 'motto'）；WenbiModule 扩三 tab 并透传 onOpenAi/bumpAi；App.tsx 删模块项与挂载、改启动默认；ModuleId 联合类型删 'mottos' 后 typecheck 驱动清理。

**Tech Stack:** Electron 44 + React 19 + TypeScript（electron-vite）。

---

## 文件结构

| 文件 | 操作 | 职责 |
| --- | --- | --- |
| `src/modules/mottos/MottosModule.tsx` | 修改 | 面板化：去外壳、active 刷新、channel 显式传 |
| `src/modules/wenbi/WenbiModule.tsx` | 修改 | 三 tab 编排 + props 透传 |
| `src/App.tsx` | 修改 | MODULES 删项、挂载切换、启动默认、频道映射清理 |
| `src/shared/types.ts` | 修改 | ModuleId 删 'mottos' |
| `src/renderer/api.d.ts` | 修改 | ModuleId 副本删 'mottos' |
| `src/components/AiSidebar.tsx` | 修改 | moduleLabel 死条目清理 |
| `CLAUDE.md` | 修改 | 左栏顺序/模块数描述同步 |

---

### Task 1: MottosModule 面板化

**Files:**
- Modify: `src/modules/mottos/MottosModule.tsx`

- [ ] **Step 1: 扩展 props 类型（active + onOpenAi 带 channel）**

第 10 行 `import { SettingsKeys } from '../../shared/types'` 改为同时引入 AiChannel：

```tsx
import { SettingsKeys, type AiChannel } from '../../shared/types'
```

第 12-16 行 `MottosModuleProps` 整体替换为：

```tsx
export interface MottosModuleProps {
  /** 文笔坊 tab 激活态：从写作台/浮生记切回格言库 tab 时刷新列表 */
  active?: boolean
  /** opts.auto：模块动作直发（切频道后自动发送），同 App.openAiWith；
   *  channel 由格言面板显式传 'motto' 直达「格言·解读」频道（当前模块已变 wenbi，模块级映射不再命中） */
  onOpenAi: (prefill?: string, opts?: { auto?: boolean; channel?: AiChannel }) => void
  bumpAi: () => void
}
```

- [ ] **Step 2: 刷新改双路径（wenbi 激活 + tab active）**

第 107-108 行：

```tsx
  // keep-alive：切回格言库时刷新（定时任务可能在后台已生成）
  useModuleActivated('mottos', () => void load())
```

替换为：

```tsx
  // keep-alive：格言库已并入文笔坊——切回文笔坊模块时刷新（定时任务可能在后台已生成）
  useModuleActivated('wenbi', () => void load())
  // tab 激活：从写作台/浮生记切回格言库 tab 时刷新（与模块激活两路径重叠时幂等查询，无副作用）
  useEffect(() => {
    if (props.active) void load()
  }, [props.active, load])
```

- [ ] **Step 3: 去掉页面外壳（module-page / module-header）**

第 427-433 行 JSX 根：

```tsx
  return (
    <div className="module-page mottos-page">
      <div className="module-header">
        <span className="material-symbols-outlined">format_quote</span>
        <span className="module-title">格言库</span>
        <span className="module-sub">三级流转：草稿 → 沉淀 → 正式</span>
      </div>
```

替换为（页面容器与标题由 WenbiModule 持有；保留 `mottos-page` class——App.css `.mottos-page .zone` 选择器依赖它，不依赖 `.module-page` 前缀）：

```tsx
  return (
    <div className="mottos-page">
```

其余 JSX（从吸顶导航栏 `zone-nav` 起）全部不动。

- [ ] **Step 4: AI 解读调用点显式传频道**

第 313 行：

```tsx
    props.onOpenAi(text, { auto: true })
```

替换为：

```tsx
    props.onOpenAi(text, { auto: true, channel: 'motto' })
```

（全文件仅此一处 onOpenAi 调用；context 为菜单「AI 解读」项。）

- [ ] **Step 5: typecheck 中间验证**

Run: `npm run typecheck`
Expected: PASS（本任务向后兼容：active 可选、App.tsx 旧挂载照常编译）

---

### Task 2: WenbiModule 扩三 tab 并透传 props

**Files:**
- Modify: `src/modules/wenbi/WenbiModule.tsx`

- [ ] **Step 1: 整文件替换为三 tab 版本**

```tsx
// 文笔坊模块（格言库并入——优化建议区第36轮：三 tab「格言库｜写作台｜浮生记」，默认格言库）
import { useState } from 'react'
import type { AiChannel } from '../../shared/types'
import MottosModule from '../mottos/MottosModule'
import JournalPanel from './JournalPanel'
import WritingPanel from './WritingPanel'

export interface WenbiModuleProps {
  /** 透传 App.openAiWith 给格言面板（AI 解读显式带 channel: 'motto'） */
  onOpenAi: (prefill?: string, opts?: { auto?: boolean; channel?: AiChannel }) => void
  bumpAi: () => void
}

export default function WenbiModule(props: WenbiModuleProps) {
  // 默认格言库（承袭其曾是启动首页的地位）；module-hidden 保活，tab 状态会话内保留、重启回默认
  const [board, setBoard] = useState<'mottos' | 'writing' | 'journal'>('mottos')
  return (
    <div className="module-page" style={{ maxWidth: 1200 }}>
      <div className="module-header">
        <span className="material-symbols-outlined">history_edu</span>
        <span className="module-title">文笔坊</span>
        <span className="module-sub">格言库 · 写作台 · 浮生记</span>
      </div>
      <div className="recycle-tabs">
        <button
          className={`recycle-tab${board === 'mottos' ? ' active' : ''}`}
          onClick={() => setBoard('mottos')}
        >
          格言库
        </button>
        <button
          className={`recycle-tab${board === 'writing' ? ' active' : ''}`}
          onClick={() => setBoard('writing')}
        >
          写作台
        </button>
        <button
          className={`recycle-tab${board === 'journal' ? ' active' : ''}`}
          onClick={() => setBoard('journal')}
        >
          浮生记
        </button>
      </div>
      <div
        className={board === 'mottos' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={board !== 'mottos'}
      >
        <MottosModule active={board === 'mottos'} onOpenAi={props.onOpenAi} bumpAi={props.bumpAi} />
      </div>
      <div
        className={board === 'writing' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={board !== 'writing'}
      >
        <WritingPanel active={board === 'writing'} />
      </div>
      <div
        className={board === 'journal' ? 'module-live' : 'module-live module-hidden'}
        aria-hidden={board !== 'journal'}
      >
        <JournalPanel active={board === 'journal'} />
      </div>
    </div>
  )
}
```

注意：原文件 JournalPanel/WritingPanel 的 import 路径、props 形态（`active` 布尔）与上面一致，直接替换整文件即可；若原文件还有本计划未显示的行（如额外 import），以原文件为基准做增删而非盲覆盖。

- [ ] **Step 2: typecheck 中间验证**

Run: `npm run typecheck`
Expected: PASS

---

### Task 3: App.tsx 切换挂载与启动默认

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 删格言库独立模块项**

第 8 行 `import MottosModule from './modules/mottos/MottosModule'` 删除。

MODULES 数组（29-42 行）删第 30 行：

```tsx
  { id: 'mottos', label: '格言库', icon: 'format_quote' },
```

第 28 行注释同步更新为：

```tsx
// 左栏模块顺序（260908 重排；260911 格言库并入文笔坊，左栏 12 → 11 项、启动默认万象库）
```

- [ ] **Step 2: 频道映射删 mottos 条目**

CHANNEL_BY_MODULE（46-50 行）删 `mottos: 'motto',` 行；其上方注释补一句：

```tsx
/** 模块 → AI 边栏频道映射（频道制，致知己 specs §4）：其余模块默认助手频道；
 *  辩真阁已并入万象库，'verify' 频道由万象库辩真板块经 openAiWith 的 channel 覆盖直达；
 *  格言库已并入文笔坊，'motto' 频道由文笔坊格言面板显式传 channel 覆盖直达 */
```

- [ ] **Step 3: 启动默认模块改万象库**

第 72 与 74 行 `'mottos'` 均改 `'wiki'`：

```tsx
  const [module, setModule] = useState<MainView>('wiki')
  // 当前模块 ref（失活事件需捕获旧模块 id；ref 方案防 strict-mode 双触发）
  const moduleRef = useRef<MainView>('wiki')
```

- [ ] **Step 4: 挂载区切换**

第 200 行删除：

```tsx
              {m.id === 'mottos' && <MottosModule onOpenAi={openAiWith} bumpAi={() => setAiVersion((v) => v + 1)} />}
```

第 209 行：

```tsx
              {m.id === 'wenbi' && <WenbiModule />}
```

替换为：

```tsx
              {m.id === 'wenbi' && (
                <WenbiModule onOpenAi={openAiWith} bumpAi={() => setAiVersion((v) => v + 1)} />
              )}
```

- [ ] **Step 5: typecheck 中间验证**

Run: `npm run typecheck`
Expected: PASS

---

### Task 4: ModuleId 类型收缩与死条目清理

**Files:**
- Modify: `src/shared/types.ts`、`src/renderer/api.d.ts`、`src/components/AiSidebar.tsx`

- [ ] **Step 1: shared/types.ts 删 'mottos'**

第 3-16 行 ModuleId 定义：删 `  | 'mottos'` 行，注释更新：

```ts
// 模块标识（260908 辩真阁并入万象库：'verify' 移除，其功能为万象库「辩真」板块；260911 格言库并入文笔坊：'mottos' 移除）
export type ModuleId =
  | 'wiki'
```

（其余成员不变。）

- [ ] **Step 2: api.d.ts 的 ModuleId 副本同步删**

`src/renderer/api.d.ts` 第 2-3 行起的 ModuleId 联合删 `| 'mottos'` 行。

注意：api.d.ts 第 393 行与 648 行附近的 `'mottos'` 字面量属于 `item:discard` 的表名联合与回收站 source 联合（`'mottos' | 'wiki_entries' | …`），是**独立类型、与 ModuleId 无关，不动**。

- [ ] **Step 3: AiSidebar moduleLabel 删死条目**

`src/components/AiSidebar.tsx` 689-704 行 `moduleLabel` 函数内删 `    mottos: '格言库',` 行（map 有 wenbi 条目，currentModule 为 string 类型，删行不影响编译；行为上 mottos 已不可能是当前模块）。

- [ ] **Step 4: typecheck 驱动残余引用清理**

Run: `npm run typecheck`
Expected: PASS。若报 `'mottos' 不能赋值给 ModuleId` 类错误，逐处按上下文清理（预期为 0 处——App.tsx 已在 Task 3 移除全部 ModuleId 语境的 mottos 引用；若出现新位置，属 ModuleId 联合的真实使用点，同样删除该分支即可）。

---

### Task 5: 文档同步 + 全量验证

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: CLAUDE.md 左栏顺序描述同步**

「全局硬性规则」节三栏式布局条目中：

```
三栏式布局：左侧边栏（图标+模块名，260908 重排后顺序：格言库→万象库→藏书架→信息源→文笔坊→致知己→灵感泉→推理角→记账本→白噪音→回收站→个人档；白噪音为非模块的全局功能入口）
```

替换为（按 App.tsx MODULES 现状含收藏夹，并去掉格言库）：

```
三栏式布局：左侧边栏（图标+模块名，260908 重排 + 260911 格言库并入文笔坊后顺序：万象库→藏书架→收藏夹→信息源→文笔坊→致知己→灵感泉→推理角→记账本→白噪音→回收站→个人档；白噪音为非模块的全局功能入口）
```

- [ ] **Step 2: CLAUDE.md 架构段同步**

「应用架构（规划）」节：

「十一个功能模块 + 白噪音全局功能（260908 辩真阁并入万象库为「辩真」板块）」替换为：

```
无需账号登录注册。左栏十一个入口模块 + 白噪音全局功能（260908 辩真阁并入万象库为「辩真」板块；260911 格言库并入文笔坊为「格言库」板块）
```

「**格言库**：」条目开头追加并入说明（条目内容保留，功能描述不变）：

```
- **格言库**（260911 并入文笔坊为其第一块 tab，左栏不再有独立入口）：名言格言三级管理（草稿区/沉淀区/正式区）+ 回收站流转。……（原内容不变）
```

「**文笔坊**：」条目的「写作生活双板块——浮生记（……）+ 写作台（……）」开头改为：

```
- **文笔坊**：格言库 + 写作生活双板块——格言库（260911 并入，tab 顺序：格言库｜写作台｜浮生记，默认格言库）+ 浮生记（……）+ 写作台（……）
```

（若 CLAUDE.md 实际行文与上面引用有出入，以就近语义等价替换为准。）

- [ ] **Step 3: 双闸验证**

Run: `npm run typecheck`
Expected: PASS（双 tsconfig）

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: 冒烟清单（交开发者）**

重启 `npm run dev` 后按设计文档「冒烟清单」逐项验收：左栏 11 项/启动落万象库、文笔坊默认格言库 tab、格言全功能回归、AI 解读直达频道、定时生成后刷新、回收站照常、双主题目检。

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 5 节改动清单 ↔ Task 1（格言面板化）、Task 2（三 tab）、Task 3（App 四处）、Task 4（类型/死条目）、Task 5（CLAUDE.md + 验证）一一对应；「tab 会话内保持」「零改动区」无需任务（不动即实现）。
- **占位符扫描**：无 TBD/「适当处理」类表述；所有代码步骤给出完整代码。
- **类型一致性**：`WenbiModuleProps.onOpenAi` 签名与 `MottosModuleProps.onOpenAi`、App `openAiWith` 三处一致（`channel?: AiChannel`）；`active` 在两文件中均为可选布尔。
