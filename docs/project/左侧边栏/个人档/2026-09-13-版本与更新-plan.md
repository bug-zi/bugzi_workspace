# 个人档「版本与更新」实施计划

> 创建于 2026-09-13 00:01
> 依据：同目录 `2026-09-13-版本与更新-design.md`（已批准）

> **For agentic workers:** 按任务顺序执行，步骤用 checkbox 跟踪。每步给出完整代码与锚点（Edit 用 old_string 均为当前文件真实内容），照抄即可。

**Goal:** 个人档新增「版本」小节——显示当前版本、启动静默检查更新、手动下载、一键「重启并安装」，更新源为 GitHub Releases（electron-updater）。

**Architecture:** 主进程新建 `electron/services/updater.ts` 状态机（idle/checking/available/downloading/downloaded/disabled，全内存），经 `app:update*` 四个 invoke 通道 + `app:updateEvent` 推送通道连接渲染层；ProfileModule 新增一个 zone 镜像快照渲染。零 DB、零 settings、零新表。

**Tech Stack:** electron-updater（新增运行时依赖）+ electron-builder `publish: github`。

## 本仓库执行约定（覆盖 superpowers 默认）

- **禁止 AI 执行任何 git 命令**——计划不含 commit 步骤；开发者自行择机提交。
- **无测试框架**——验证手段为 `npm run typecheck`（双 tsconfig）+ `npm run build` + dev/打包冒烟。
- IPC 三处同步铁律：`electron/ipc.ts` + `electron/preload.ts` + `src/renderer/api.d.ts`。

## 设计勘误（计划期核实，已回写设计文档）

托盘兼容**无需** `isInstallingUpdate()` 标志：`quitAndInstall()` 内部走 `app.quit()` → `before-quit`（main.ts:133 置 `quitting = true`）→ close 拦截（main.ts:52 判 `!quitting`）天然放行。零改动。

---

### Task 1: 依赖安装 + 打包配置

**Files:**
- Modify: `package.json`（npm 自动写入 dependencies）
- Modify: `electron-builder.yml:2`（productName 行后插入）

- [ ] **Step 1.1: 安装 electron-updater**

```bash
npm install electron-updater
```

预期：成功写入 dependencies（`.npmrc` 已配 legacy-peer-deps + 国内镜像，直接装）。装完检查 `package.json` 出现 `"electron-updater": "^6.x"`。

- [ ] **Step 1.2: electron-builder.yml 加 publish 配置**

在 `productName: bugzi's workspace` 行后插入：

```yaml
# 应用内更新（个人档「版本与更新」）：打包自动产出 latest.yml + .blockmap 到 release/；
# 本地 dist 不自动发布（无 CI 时 electron-builder 不触发 publish），发版走 gh release create（见 Task 8）
publish:
  provider: github
  owner: bug-zi
  repo: bugzi_workspace
```

- [ ] **Step 1.3: 验证**

```bash
npm run typecheck
```

预期：无错误（新依赖暂未被引用，纯确认环境无损）。

---

### Task 2: 共享类型（src/shared/types.ts）

**Files:**
- Modify: `src/shared/types.ts:871`（文件末尾 ErrCode 块之后追加）

- [ ] **Step 2.1: 追加类型**

Edit：old_string 取文件末尾：

```ts
export const ErrCode = {
  LlmNotConfigured: 'LLM_NOT_CONFIGURED',
  McpNotEnabled: 'MCP_NOT_ENABLED',
  NotFound: 'NOT_FOUND',
  Conflict: 'CONFLICT'
} as const
```

new_string 在其后追加（保留原块不动）：

```ts

// ---------- 应用更新（个人档「版本与更新」，260913） ----------

export type UpdatePhase = 'idle' | 'checking' | 'available' | 'downloading' | 'downloaded' | 'disabled'

/** 更新状态快照（主进程全内存；app:updateState 返回值 / app:updateEvent 推送载荷） */
export interface UpdateSnapshot {
  currentVersion: string
  phase: UpdatePhase
  /** available/downloaded 态的新版本号 */
  availableVersion: string | null
  /** 新版本发布时间（ISO 串，UI slice(0,10) 显日期） */
  releaseDate: string | null
  /** downloading 态 0-100 */
  percent: number
  /** 一次性提示文案（仅启动静默检查发现新版时置一次；渲染层 toast 用） */
  notice: string | null
}

export interface UpdateCheckResult {
  status: 'up-to-date' | 'available' | 'error' | 'disabled'
  version?: string
  releaseDate?: string
}
```

- [ ] **Step 2.2: 验证**

```bash
npm run typecheck
```

预期：无错误。

---

### Task 3: 主进程更新服务（新建 electron/services/updater.ts）

**Files:**
- Create: `electron/services/updater.ts`

- [ ] **Step 3.1: 写入完整文件**

```ts
// 更新服务（个人档「版本与更新」）：electron-updater 状态机封装，状态全内存、不落库不落 settings。
// 自动化策略（260913 设计定稿）：启动静默检查一次仅提示；下载/安装由用户在个人档手动触发。
import { app, type BrowserWindow } from 'electron'
import { autoUpdater, type UpdateInfo } from 'electron-updater'
import type { UpdateCheckResult, UpdateSnapshot } from '../../src/shared/types'

let getWin: () => BrowserWindow | null = () => null
let snapshot: UpdateSnapshot = {
  currentVersion: app.getVersion(),
  phase: 'idle',
  availableVersion: null,
  releaseDate: null,
  percent: 0,
  notice: null
}
let inited = false
/** 并发检查去重：启动静默检查与手动检查共用同一 in-flight Promise */
let checkingPromise: Promise<UpdateCheckResult> | null = null

/** 三段数字版本号比较：remote 更大返回 true（版本号自控，无预发布段） */
function isNewer(remote: string, current: string): boolean {
  const a = remote.split('.').map(Number)
  const b = current.split('.').map(Number)
  for (let i = 0; i < 3; i++) {
    if ((a[i] ?? 0) !== (b[i] ?? 0)) return (a[i] ?? 0) > (b[i] ?? 0)
  }
  return false
}

function push(): void {
  getWin()?.webContents.send('app:updateEvent', snapshot)
}

function setPhase(phase: UpdateSnapshot['phase'], extra?: Partial<UpdateSnapshot>): void {
  snapshot = { ...snapshot, phase, ...extra }
  push()
}

export function initUpdater(winGetter: () => BrowserWindow | null): void {
  getWin = winGetter
  // dev 模式无 app-update.yml，checkForUpdates 必抛：直接置 disabled（UI 点检查 toast「开发模式下不可用」）
  if (!app.isPackaged) {
    snapshot = { ...snapshot, phase: 'disabled' }
    push()
    return
  }
  if (inited) return
  inited = true
  // 一切显式控制：不自动下载、退出不自动装（退出自动装在托盘关闭行为下易意外，设计否决）
  autoUpdater.autoDownload = false
  autoUpdater.autoInstallOnAppQuit = false
  // electron-updater 6.x 未暴露 requestTimeout（默认超时即可）；国内波动由检查静默/回退、下载可重试兜底
  autoUpdater.on('update-available', (info: UpdateInfo) => {
    setPhase('available', { availableVersion: info.version, releaseDate: info.releaseDate, percent: 0 })
  })
  autoUpdater.on('update-not-available', () => setPhase('idle'))
  autoUpdater.on('download-progress', (p: { percent: number }) => {
    setPhase('downloading', { percent: Math.min(100, Math.round(p.percent)) })
  })
  autoUpdater.on('update-downloaded', () => setPhase('downloaded', { percent: 100 }))
  autoUpdater.on('error', () => {
    // 检查失败回 idle；下载失败回 available 可重试（blockmap 差量续传）
    if (snapshot.phase === 'checking') setPhase('idle')
    else if (snapshot.phase === 'downloading') setPhase('available', { percent: 0 })
  })
  // 启动静默检查：错开启动高峰；发现新版置一次性 notice，任何失败静默（首个 Release 发布前 404 同静默）
  setTimeout(() => {
    void checkForUpdates()
      .then((r) => {
        if (r.status === 'available') {
          snapshot = { ...snapshot, notice: `发现新版本 v${r.version}` }
          push()
        }
      })
      .catch(() => {})
  }, 5_000).unref()
}

export function getUpdateState(): UpdateSnapshot {
  return snapshot
}

/** 检查更新（手动按钮与启动静默检查共用；并发去重） */
export function checkForUpdates(): Promise<UpdateCheckResult> {
  if (!app.isPackaged) return Promise.resolve({ status: 'disabled' })
  if (checkingPromise) return checkingPromise
  setPhase('checking')
  checkingPromise = (async () => {
    try {
      const r = await autoUpdater.checkForUpdates()
      const info = r?.updateInfo
      // update-not-available 时 updateInfo 为远端（≤当前）版本：isNewer 判否即「已最新」
      if (info && isNewer(info.version, app.getVersion())) {
        return { status: 'available' as const, version: info.version, releaseDate: info.releaseDate }
      }
      return { status: 'up-to-date' as const }
    } catch {
      return { status: 'error' as const }
    } finally {
      checkingPromise = null
    }
  })()
  return checkingPromise
}

/** 开始下载（仅 available 态受理）；进度/失败经事件推送，无取消（设计决策：失败回 available 可重试） */
export function downloadUpdate(): void {
  if (snapshot.phase !== 'available') return
  setPhase('downloading', { percent: 0 })
  void autoUpdater.downloadUpdate().catch(() => {
    if (snapshot.phase === 'downloading') setPhase('available', { percent: 0 })
  })
}

/** 重启并安装（仅 downloaded 态受理）：静默安装 + 装完自动重启。
 *  退出链路：quitAndInstall → app.quit() → before-quit（main.ts 置 quitting=true）→ close 拦截放行，托盘模式零改动兼容 */
export function installUpdate(): void {
  if (snapshot.phase !== 'downloaded') return
  autoUpdater.quitAndInstall(true, true)
}
```

- [ ] **Step 3.2: 验证**

```bash
npm run typecheck
```

预期：无错误（此时 updater.ts 尚无人引用，tsc 仍检查其内部自洽）。

---

### Task 4: IPC 通道 + 主进程接线

**Files:**
- Modify: `electron/ipc.ts:128`（import 区末尾）与 `electron/ipc.ts:151-157`（app:setLaunchOnBoot 块后）
- Modify: `electron/main.ts:14`（import 区）与 `electron/main.ts:112`（whenReady 内）

- [ ] **Step 4.1: ipc.ts 加 import**

Edit：old_string = `import type { TerminalCreateOpts } from './services/terminal'`
new_string：

```ts
import type { TerminalCreateOpts } from './services/terminal'
import { getUpdateState, checkForUpdates, downloadUpdate, installUpdate } from './services/updater'
```

- [ ] **Step 4.2: ipc.ts 注册四通道**

Edit：old_string（现有 app:setLaunchOnBoot 块）：

```ts
  ipcMain.handle('app:setLaunchOnBoot', (_e, on: boolean) => {
    setSetting(SettingsKeys.LaunchOnBoot, on ? '1' : '0')
    app.setLoginItemSettings({ openAtLogin: on })
    refreshTrayMenu()
    return true
  })
```

new_string 在其后追加：

```ts

  // ---------- 更新（个人档「版本与更新」） ----------
  ipcMain.handle('app:updateState', () => getUpdateState())
  ipcMain.handle('app:updateCheck', () => checkForUpdates())
  ipcMain.handle('app:updateDownload', () => {
    downloadUpdate()
    return true
  })
  ipcMain.handle('app:updateInstall', () => {
    installUpdate()
    return true
  })
```

- [ ] **Step 4.3: main.ts 接线 initUpdater**

Edit 1：old_string = `import { createTray } from './services/tray'`
new_string：

```ts
import { createTray } from './services/tray'
import { initUpdater } from './services/updater'
```

Edit 2：old_string：

```ts
    createTray(appIcon, showMainWindow)
    app.setLoginItemSettings({ openAtLogin: getSetting(SettingsKeys.LaunchOnBoot) === '1' })
```

new_string：

```ts
    createTray(appIcon, showMainWindow)
    app.setLoginItemSettings({ openAtLogin: getSetting(SettingsKeys.LaunchOnBoot) === '1' })
    // 应用内更新（个人档「版本与更新」）：启动静默检查在 updater.ts 内部错峰 5s 触发，失败静默
    initUpdater(() => BrowserWindow.getAllWindows()[0] ?? null)
```

- [ ] **Step 4.4: 验证**

```bash
npm run typecheck
```

预期：无错误。

---

### Task 5: preload 桥 + api.d.ts（三处同步的后两处）

**Files:**
- Modify: `electron/preload.ts:13`（app 块后插入 updater 块）
- Modify: `src/renderer/api.d.ts:814`（app 接口块后插入 updater 接口）

- [ ] **Step 5.1: preload.ts**

Edit：old_string：

```ts
  app: {
    /** 开机自启：写 settings + 立即生效 + 托盘菜单勾选态刷新 */
    setLaunchOnBoot: (on: boolean): Promise<boolean> => ipcRenderer.invoke('app:setLaunchOnBoot', on)
  },
```

new_string 在其后插入：

```ts
  updater: {
    /** 状态快照（个人档「版本」小节挂载时同步） */
    getState: (): Promise<import('../src/shared/types').UpdateSnapshot> => ipcRenderer.invoke('app:updateState'),
    /** 手动检查（与启动静默检查同内核，主进程并发去重） */
    check: (): Promise<import('../src/shared/types').UpdateCheckResult> => ipcRenderer.invoke('app:updateCheck'),
    download: (): Promise<boolean> => ipcRenderer.invoke('app:updateDownload'),
    install: (): Promise<boolean> => ipcRenderer.invoke('app:updateInstall'),
    /** 状态机任何变化实时推送，返回取消订阅 */
    onUpdateEvent: (cb: (s: import('../src/shared/types').UpdateSnapshot) => void): (() => void) => {
      const listener = (_e: unknown, s: import('../src/shared/types').UpdateSnapshot): void => {
        cb(s)
      }
      ipcRenderer.on('app:updateEvent', listener)
      return () => ipcRenderer.removeListener('app:updateEvent', listener)
    }
  },
```

- [ ] **Step 5.2: api.d.ts**

Edit：old_string：

```ts
  app: {
    /** 开机自启：写 settings + 立即生效 + 托盘菜单勾选态刷新 */
    setLaunchOnBoot(on: boolean): Promise<boolean>
  }
  md: {
```

new_string（注意 api.d.ts 从 renderer 出发用 `../shared/types`，与 preload 的 `../src/shared/types` 不同）：

```ts
  app: {
    /** 开机自启：写 settings + 立即生效 + 托盘菜单勾选态刷新 */
    setLaunchOnBoot(on: boolean): Promise<boolean>
  }
  updater: {
    /** 状态快照（个人档「版本」小节挂载时同步） */
    getState(): Promise<import('../shared/types').UpdateSnapshot>
    /** 手动检查（与启动静默检查同内核，主进程并发去重） */
    check(): Promise<import('../shared/types').UpdateCheckResult>
    download(): Promise<boolean>
    install(): Promise<boolean>
    /** 状态机任何变化实时推送，返回取消订阅 */
    onUpdateEvent(cb: (s: import('../shared/types').UpdateSnapshot) => void): () => void
  }
  md: {
```

- [ ] **Step 5.3: 验证**

```bash
npm run typecheck
```

预期：无错误（此时 window.api.updater 已可被渲染层使用）。

---

### Task 6: ProfileModule「版本」小节

**Files:**
- Modify: `src/modules/profile/ProfileModule.tsx:2`（import 加 useRef）
- Modify: `src/modules/profile/ProfileModule.tsx:10`（类型 import 加 UpdateSnapshot）
- Modify: `src/modules/profile/ProfileModule.tsx:70`（closeAction 行后加状态/副作用/回调）
- Modify: `src/modules/profile/ProfileModule.tsx:768-770`（数据存储 zone 与 LLM 配置注释之间插 zone）

- [ ] **Step 6.1: 改 import**

Edit 1：old_string = `import { Fragment, useCallback, useEffect, useMemo, useState } from 'react'`
new_string = `import { Fragment, useCallback, useEffect, useMemo, useRef, useState } from 'react'`

Edit 2：old_string = `import type { LlmUsageRecord, LlmUsageStats, TerminalSettings } from '../../shared/types'`
new_string = `import type { LlmUsageRecord, LlmUsageStats, TerminalSettings, UpdateSnapshot } from '../../shared/types'`

- [ ] **Step 6.2: 加状态、订阅与回调**

Edit：old_string：

```ts
  const [closeAction, setCloseAction] = useState<'tray' | 'exit'>('tray')
```

new_string：

```ts
  const [closeAction, setCloseAction] = useState<'tray' | 'exit'>('tray')
  // 版本与更新（260913）：快照唯一数据源为主进程事件推送，本组件仅镜像；keep-alive 下挂载一次订阅常驻
  const [update, setUpdate] = useState<UpdateSnapshot | null>(null)
  const lastNoticeRef = useRef('')
  useEffect(() => {
    void window.api.updater.getState().then(setUpdate)
    return window.api.updater.onUpdateEvent(setUpdate)
  }, [])
  // 一次性提示（启动静默检查发现新版）；ref 守卫防重复推送多次弹
  useEffect(() => {
    if (update?.notice && update.notice !== lastNoticeRef.current) {
      lastNoticeRef.current = update.notice
      toast(update.notice)
    }
  }, [update, toast])
```

- [ ] **Step 6.3: 加 checkUpdate 回调**

放在 Step 6.2 块之后同一位置（组件体内、return 之前任意靠前处均可，紧随 6.2 块即符合「设置区函数就近」习惯）：

```ts
  const checkUpdate = useCallback(async (): Promise<void> => {
    try {
      const r = await window.api.updater.check()
      if (r.status === 'up-to-date') toast('已是最新版本')
      else if (r.status === 'error') toast('检查失败，请稍后再试')
      else if (r.status === 'disabled') toast('开发模式下不可用')
    } catch {
      toast('检查失败，请稍后再试')
    }
  }, [toast])
```

- [ ] **Step 6.4: 插入「版本」zone**

Edit：old_string：

```tsx
      {/* LLM 配置 */}
```

new_string：

```tsx
      {/* 版本与更新（260913）：GitHub Releases · 启动静默检查 + 手动下载 */}
      <section className="zone">
        <div className="zone-header"><span>版本</span></div>
        <div className="zone-body" style={{ padding: 0 }}>
          <div className="setting-row">
            <span className="setting-label">当前版本</span>
            <span className="grow" style={{ fontSize: '0.85em', color: 'var(--color-text-secondary)' }}>
              v{update?.currentVersion ?? '…'}
            </span>
            {(update?.phase === 'idle' || update?.phase === 'disabled') && (
              <button className="btn" onClick={() => void checkUpdate()}>
                <span className="material-symbols-outlined">system_update_alt</span>
                检查更新
              </button>
            )}
            {update?.phase === 'checking' && (
              <button className="btn" disabled>
                <span className="material-symbols-outlined">progress_activity</span>
                检查中…
              </button>
            )}
            {update?.phase === 'available' && (
              <button className="btn" onClick={() => window.api.updater.download()}>
                <span className="material-symbols-outlined">download</span>
                下载更新
              </button>
            )}
            {update?.phase === 'downloaded' && (
              <button className="btn" onClick={() => window.api.updater.install()}>
                <span className="material-symbols-outlined">restart_alt</span>
                重启并安装
              </button>
            )}
          </div>
          {update?.phase === 'downloading' && (
            <div style={{ padding: '2px 16px 12px' }}>
              <div style={{ fontSize: '0.85em', color: 'var(--color-text-secondary)', marginBottom: 6 }}>
                正在下载更新… {update.percent}%
              </div>
              <div style={{ height: 4, borderRadius: 2, background: 'var(--color-primary-soft)', overflow: 'hidden' }}>
                <div
                  style={{
                    width: `${update.percent}%`,
                    height: '100%',
                    background: 'var(--color-primary)',
                    transition: 'width .3s'
                  }}
                />
              </div>
            </div>
          )}
          {update?.phase === 'available' && (
            <div className="setting-row">
              <span className="setting-label">发现新版本</span>
              <span className="grow module-sub">
                v{update.availableVersion}（{update.releaseDate ? update.releaseDate.slice(0, 10) : ''}），
                下载完成后可一键更新
              </span>
            </div>
          )}
        </div>
      </section>

      {/* LLM 配置 */}
```

（代码量 ~70 行 < 80 行阈值，按设计不抽独立组件。）

- [ ] **Step 6.5: 验证**

```bash
npm run typecheck && npm run build
```

预期：双 tsconfig 无错误，build 产物正常。

---

### Task 7: dev 冒烟（本地可验证部分）

- [ ] **Step 7.1: dev 模式目检**

```bash
npm run dev
```

预期：
1. 进入「个人档」→ 设置区「数据存储」之后出现「版本」小节，显示 `v1.0.0` 与「检查更新」按钮。
2. 点「检查更新」→ toast「开发模式下不可用」，状态不异常。
3. 双主题切换目检小节样式（主题色系、无异常颜色）。

- [ ] **Step 7.2: 全量回归确认**

```bash
npm run typecheck && npm run build
```

预期：通过。零 DB 迁移/零 settings 新键，其他模块无需逐个回归。

---

### Task 8: 真实更新链路发版冒烟（开发者手动执行；AI 至多提供命令）

前置：Task 1-7 全部完成，`git` 工作区由开发者自行处理（提交/推送均由开发者执行）。

- [ ] **Step 8.1: 升版本号**

改 `package.json` `"version": "1.0.0"` → `"1.0.1"`（或 `npm version patch --no-git-tag-version`；**不要**裸跑 `npm version patch`——它会自动 commit + tag）。

- [ ] **Step 8.2: 打包**

```bash
npm run dist
```

预期：`release/` 产出 `bugzi-workspace-Setup-1.0.1.exe`、`bugzi-workspace-Setup-1.0.1.exe.blockmap`、`latest.yml`（差量更新依赖 exe + blockmap + latest.yml 三件齐全）。

- [ ] **Step 8.3: 本机安装含更新功能的首版（260913 勘误：原写「装旧版 1.0.0 验证检测」为自举错误——更新代码随首版才存在，更早版本无更新能力、永远检测不到，属一次性手动迁移）**

运行 `release/bugzi-workspace-Setup-1.0.1.exe`（首个内置更新功能的版本）。此后所有新版均可应用内自动更新，无需再手动装。

- [ ] **Step 8.4: 发布 Release（git/gh 操作由开发者执行）**

```bash
gh release create v1.0.1 "release/bugzi-workspace-Setup-1.0.1.exe" "release/bugzi-workspace-Setup-1.0.1.exe.blockmap" "release/latest.yml" --title "v1.0.1" --notes "更新说明"
```

- [ ] **Step 8.5: 再发一版做端到端验收（自举要求：必须存在比本机更新的版本可检测）**

```bash
npm version patch --no-git-tag-version   # 1.0.1 → 1.0.2
npm run dist
gh release create v1.0.2 "release/bugzi-workspace-Setup-1.0.2.exe" "release/bugzi-workspace-Setup-1.0.2.exe.blockmap" "release/latest.yml" --title "v1.0.2" --notes "更新链路验证"
```

启动已装的 1.0.1 App：
1. 启动 ~5s 后 toast「发现新版本 v1.0.2」，个人档「版本」小节进 available 态（新版本行 + 下载更新按钮）。
2. 点「下载更新」→ 进度条推进到 100% → 出「重启并安装」。
3. 点「重启并安装」→ 静默安装、App 自动重启 → 个人档显示 `v1.0.2`，用户数据（各模块数据/背景图/头像）完好。
4. 再点「检查更新」→ toast「已是最新版本」。
5. 断网点「检查更新」→ toast「检查失败，请稍后再试」。
6. 关闭行为=最小化到托盘时重复 2-3，更新重启不卡死。
7. 二次发版（如 v1.0.2 改个版本号即可）验证**差量更新**：下载进度明显快于全量（blockmap 生效）。

---

## 自审记录（写完计划即查）

- **spec 覆盖**：设计 §1-§7 ↔ Task 1（打包）/§2 publish ↔ Task 1/§3 状态机 ↔ Task 3/§4 IPC+UI ↔ Task 4-6/§5 发版 ↔ Task 8/§6 错误安全 ↔ Task 3 事件处理/§7 冒烟 ↔ Task 7-8。无缺口。
- **占位符**：无 TBD/「适当处理」类语句；所有代码完整给出。
- **类型一致性**：`UpdateSnapshot/UpdateCheckResult`（Task 2）↔ updater.ts（Task 3）↔ preload/api.d.ts（Task 5）↔ ProfileModule（Task 6）字段逐一核对一致；通道名 `app:update*` 五处一致。
