# 背景素材库 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **项目纪律（覆盖技能默认）**：本仓库**禁止 AI 执行任何 git 操作**——计划不含 commit 步骤，全部任务完成并验证后由开发者统一提交。本项目无单测框架，验证闸 = `npm run typecheck`（双 tsconfig）+ `npm run build`，功能验收走冒烟清单（见设计文档）。
>
> 设计依据：`docs/project/左侧边栏/个人档/2026-09-11-背景素材库-design.md`

**Goal:** 个人档背景图从「每主题一张覆盖式」升级为素材库——浅色/深色两组独立、多张上传、弹窗管理切换；旧图自动迁移。

**Architecture:** 文件系统为真相源（`userData/bg/light/`、`userData/bg/dark/` 两目录、唯一文件名、列表=扫目录），当前使用仍走现有 settings 键 `bg_bg-light`/`bg_bg-dark`（存纯文件名）；bzres 协议零改动，ThemeProvider 的 applyBg 拼 URL 须带组段 `bzres://bg/<group>/<file>`（260911 修复：原 `bzres://bg/<file>` 在迁移后指向 `bg/` 根旧路径 404 回退纯色，详见 docs/log/260911.md）；主进程新增 4 个 `image:bg*` IPC + 一次性幂等迁移；渲染层新 BgLibraryDialog 弹窗 + ProfileModule 替换原两行按钮。

**Tech Stack:** Electron 44（ipcMain/dialog/node:fs）+ React 19 + TypeScript。

---

## 文件结构

| 文件 | 操作 | 职责 |
| --- | --- | --- |
| `electron/ipc.ts` | 修改 | 4 个 bg 通道 + migrateBgLibrary + image:pick 收窄为 avatar |
| `electron/preload.ts` | 修改 | image 段暴露 bgList/bgUpload/bgUse/bgDelete |
| `src/renderer/api.d.ts` | 修改 | image 段类型声明同步 |
| `src/modules/profile/BgLibraryDialog.tsx` | 新建 | 素材库管理弹窗（两组切换/网格/上传/删除） |
| `src/modules/profile/BgLibraryDialog.css` | 新建 | 弹窗样式 |
| `src/modules/profile/ProfileModule.tsx` | 修改 | 删 pickBg/原两行，换「管理素材库」入口 + 挂弹窗 |

---

### Task 1: 主进程——素材目录工具 + 4 个 IPC 通道 + 迁移

**Files:**
- Modify: `electron/ipc.ts`

- [ ] **Step 1: 扩展 node:fs import**

第 108 行：

```ts
import { copyFileSync, unlinkSync, writeFileSync } from 'node:fs'
```

替换为：

```ts
import { copyFileSync, unlinkSync, writeFileSync, readdirSync, mkdirSync, renameSync } from 'node:fs'
```

- [ ] **Step 2: 素材库工具函数（放在「头像/背景图上传」注释块之前）**

在 `// ---------- 头像/背景图上传 ----------`（第 149 行）之前插入：

```ts
// ---------- 背景素材库（优化建议区第36轮：文件系统为真相源，light/dark 两组独立） ----------
const BG_EXTS = ['png', 'jpg', 'jpeg', 'webp', 'gif', 'bmp']
/** 每组素材上限 */
const BG_MAX_PER_GROUP = 20

const bgGroupDir = (group: 'light' | 'dark'): string => join(userDataDir(), 'bg', group)

/** 列出某组素材文件名（新上传在前；只认图片扩展；目录缺失自动创建） */
function bgListFiles(group: 'light' | 'dark'): string[] {
  const dir = bgGroupDir(group)
  mkdirSync(dir, { recursive: true })
  return readdirSync(dir)
    .filter((f) => BG_EXTS.includes(f.split('.').pop()?.toLowerCase() ?? ''))
    .sort((a, b) => b.localeCompare(a))
}

/** 一次性迁移（幂等，registerIpc 注册时执行）：旧单图 bg/bg-light.ext → bg/light/bg-light.ext，dark 同理；
 *  文件名保持原名（settings 已指向它，零额外改动）；组目录已有文件即跳过；失败静默（旧图原地不动，现有行为不受影响） */
function migrateBgLibrary(): void {
  for (const group of ['light', 'dark'] as const) {
    const dir = bgGroupDir(group)
    try {
      if (readdirSync(dir).length > 0) continue
    } catch {
      /* 目录不存在 → 继续迁移 */
    }
    for (const ext of BG_EXTS) {
      const oldFile = `bg-${group}.${ext}`
      try {
        mkdirSync(dir, { recursive: true })
        renameSync(join(userDataDir(), 'bg', oldFile), join(dir, oldFile))
        break // 每组最多一个旧文件
      } catch {
        /* 无此扩展旧文件，试下一个 */
      }
    }
  }
}
```

- [ ] **Step 3: registerIpc 体开头执行迁移**

找到 `registerIpc` 函数定义（ipc.ts 尾部导出函数，注册全部 handle 的容器），在函数体第一行插入：

```ts
  // 背景素材库一次性迁移（幂等；失败静默不影响任何现有行为）
  try {
    migrateBgLibrary()
  } catch {
    /* 静默 */
  }
```

- [ ] **Step 4: image:pick 收窄为仅头像 + 新增 4 个通道**

原 `image:pick` handler（149-190 行）整体替换为：

```ts
  // ---------- 头像上传 / 背景素材库 ----------
  ipcMain.handle('image:pick', async (_e, kind: 'avatar') => {
    const r = await dialog.showOpenDialog(win()!, {
      title: '选择图片',
      filters: [{ name: '图片', extensions: ['png', 'jpg', 'jpeg', 'webp', 'gif', 'bmp'] }],
      properties: ['openFile']
    })
    if (r.canceled || r.filePaths.length === 0) return null
    const src = r.filePaths[0]
    const ext = src.split('.').pop()?.toLowerCase() ?? 'png'
    const dest = join(userDataDir(), `avatar.${ext}`)
    // 清旧头像（不同扩展名）
    for (const old of BG_EXTS) {
      if (old !== ext) {
        try {
          unlinkSync(join(userDataDir(), `avatar.${old}`))
        } catch {
          /* 无旧文件 */
        }
      }
    }
    copyFileSync(src, dest)
    setSetting(SettingsKeys.UserAvatar, `avatar.${ext}`)
    return true
  })

  ipcMain.handle('image:bgList', (_e, group: 'light' | 'dark'): string[] => bgListFiles(group))

  // 多选上传入组；单张自动设为当前背景（applied=文件名），多张只入库（applied=null）
  ipcMain.handle(
    'image:bgUpload',
    async (_e, group: 'light' | 'dark'): Promise<{ list: string[]; applied: string | null } | null> => {
      const r = await dialog.showOpenDialog(win()!, {
        title: '选择背景图片（可多选）',
        filters: [{ name: '图片', extensions: BG_EXTS }],
        properties: ['openFile', 'multiSelections']
      })
      if (r.canceled || r.filePaths.length === 0) return null
      const dir = bgGroupDir(group)
      if (bgListFiles(group).length + r.filePaths.length > BG_MAX_PER_GROUP) {
        throw new Error(`素材库已达上限（每组 ${BG_MAX_PER_GROUP} 张），请先删除部分素材`)
      }
      let applied: string | null = null
      for (const src of r.filePaths) {
        const ext = src.split('.').pop()?.toLowerCase() ?? 'png'
        const name = `${Date.now()}-${Math.random().toString(36).slice(2, 6)}.${ext}`
        copyFileSync(src, join(dir, name))
        if (r.filePaths.length === 1) applied = name
      }
      if (applied) setSetting(`bg_bg-${group}`, applied)
      return { list: bgListFiles(group), applied }
    }
  )

  // 设某张为当前使用（校验在库 + 文件名安全）
  ipcMain.handle('image:bgUse', (_e, group: 'light' | 'dark', file: string): boolean => {
    if (!/^[\w.-]+$/.test(file)) return false
    if (!bgListFiles(group).includes(file)) return false
    setSetting(`bg_bg-${group}`, file)
    return true
  })

  // 删某张；删的是当前使用图时置空 settings 回退纯色（wasUsing 供渲染层 refreshBg）
  ipcMain.handle(
    'image:bgDelete',
    (_e, group: 'light' | 'dark', file: string): { list: string[]; wasUsing: boolean } => {
      if (!/^[\w.-]+$/.test(file)) throw new Error('非法文件名')
      unlinkSync(join(bgGroupDir(group), file))
      const wasUsing = getSetting(`bg_bg-${group}`) === file
      if (wasUsing) setSetting(`bg_bg-${group}`, '')
      return { list: bgListFiles(group), wasUsing }
    }
  )
```

（`getSetting`/`setSetting` 已在文件头 import；`win()` 为文件内既有 BrowserWindow 获取函数。）

- [ ] **Step 5: typecheck 中间验证**

Run: `npm run typecheck`
Expected: FAIL——`preload.ts`/`api.d.ts`/`ProfileModule.tsx` 仍引用 `image.pick('bg-light'…)` 旧签名（Task 2/4 修复）。若仅 ipc.ts 自身错误则需修复后再进 Task 2。

---

### Task 2: preload 与类型声明同步

**Files:**
- Modify: `electron/preload.ts`、`src/renderer/api.d.ts`

- [ ] **Step 1: preload image 段替换**

preload.ts 第 16-20 行 image 段替换为：

```ts
  image: {
    /** 选择并落盘头像 */
    pick: (kind: 'avatar'): Promise<boolean | null> => ipcRenderer.invoke('image:pick', kind),
    /** 背景素材库（第36轮）：列出某组素材文件名（新在前） */
    bgList: (group: 'light' | 'dark'): Promise<string[]> => ipcRenderer.invoke('image:bgList', group),
    /** 对话框多选上传入组；单张自动设为当前（applied=文件名），多张 applied=null；取消返回 null */
    bgUpload: (group: 'light' | 'dark'): Promise<{ list: string[]; applied: string | null } | null> =>
      ipcRenderer.invoke('image:bgUpload', group),
    /** 设某张为当前使用 */
    bgUse: (group: 'light' | 'dark', file: string): Promise<boolean> =>
      ipcRenderer.invoke('image:bgUse', group, file),
    /** 删某张；wasUsing=删的是当前使用图（已回退纯色） */
    bgDelete: (group: 'light' | 'dark', file: string): Promise<{ list: string[]; wasUsing: boolean }> =>
      ipcRenderer.invoke('image:bgDelete', group, file)
  },
```

- [ ] **Step 2: api.d.ts image 段同步**

api.d.ts 第 642-643 行 image 段替换为：

```ts
  image: {
    pick(kind: 'avatar'): Promise<boolean | null>
    bgList(group: 'light' | 'dark'): Promise<string[]>
    bgUpload(group: 'light' | 'dark'): Promise<{ list: string[]; applied: string | null } | null>
    bgUse(group: 'light' | 'dark', file: string): Promise<boolean>
    bgDelete(group: 'light' | 'dark', file: string): Promise<{ list: string[]; wasUsing: boolean }>
  }
```

- [ ] **Step 3: typecheck 中间验证**

Run: `npm run typecheck`
Expected: 仅 `ProfileModule.tsx` 的 `pickBg`（`pick('bg-light')` 旧调用）报错——Task 4 删除该调用后消除。

---

### Task 3: BgLibraryDialog 组件

**Files:**
- Create: `src/modules/profile/BgLibraryDialog.tsx`
- Create: `src/modules/profile/BgLibraryDialog.css`

- [ ] **Step 1: 写组件**

```tsx
// 背景素材库管理弹窗（优化建议区第36轮）：浅色/深色两组独立，上传/切换/删除
import { useCallback, useEffect, useState } from 'react'
import ConfirmDialog from '../../components/ConfirmDialog'
import { useToast } from '../../components/Toast'
import { useAppSettings } from '../../theme/ThemeProvider'
import './BgLibraryDialog.css'

export interface BgLibraryDialogProps {
  open: boolean
  onClose: () => void
}

export default function BgLibraryDialog(props: BgLibraryDialogProps) {
  const { toast } = useToast()
  // settings 由 refreshBg 全量刷新——use/delete 后 using 值随之更新
  const { settings, refreshBg, theme } = useAppSettings()
  // 打开时默认选中当前主题对应的组
  const [group, setGroup] = useState<'light' | 'dark'>(theme)
  const [files, setFiles] = useState<string[]>([])
  const [delTarget, setDelTarget] = useState<string | null>(null)
  const using = settings[`bg_bg-${group}`] ?? ''

  const load = useCallback(async () => {
    try {
      setFiles(await window.api.image.bgList(group))
    } catch {
      setFiles([]) // 扫描异常 → 空态
    }
  }, [group])

  useEffect(() => {
    if (props.open) void load()
  }, [props.open, load])

  if (!props.open) return null

  const doUpload = async (): Promise<void> => {
    try {
      const r = await window.api.image.bgUpload(group)
      if (!r) return // 用户取消系统对话框
      setFiles(r.list)
      if (r.applied) {
        await refreshBg()
        toast('已上传并设为当前背景')
      } else {
        toast('已加入素材库')
      }
    } catch (e) {
      toast((e as Error).message || '上传失败')
    }
  }

  const doUse = async (file: string): Promise<void> => {
    const ok = await window.api.image.bgUse(group, file)
    if (ok) {
      await refreshBg()
      toast('已设为当前背景')
    } else {
      toast('设置失败')
      void load()
    }
  }

  const doDelete = async (): Promise<void> => {
    if (!delTarget) return
    try {
      const r = await window.api.image.bgDelete(group, delTarget)
      setFiles(r.list)
      if (r.wasUsing) {
        await refreshBg()
        toast('已删除，回退纯色背景')
      } else {
        toast('已删除')
      }
    } catch {
      toast('删除失败')
    }
    setDelTarget(null)
  }

  return (
    <div className="dialog-overlay" onMouseDown={(e) => e.target === e.currentTarget && props.onClose()}>
      <div className="dialog bg-lib-dialog" style={{ width: 640 }}>
        <div className="dialog-header">背景素材库</div>
        <div className="dialog-body">
          <div className="recycle-tabs">
            <button
              className={`recycle-tab${group === 'light' ? ' active' : ''}`}
              onClick={() => setGroup('light')}
            >
              浅色模式
            </button>
            <button
              className={`recycle-tab${group === 'dark' ? ' active' : ''}`}
              onClick={() => setGroup('dark')}
            >
              深色模式
            </button>
          </div>
          {files.length === 0 ? (
            <div className="bg-lib-empty">
              <span>还没有背景素材，上传一张试试</span>
              <button className="btn btn-primary" onClick={() => void doUpload()}>
                <span className="material-symbols-outlined">upload</span>
                上传
              </button>
            </div>
          ) : (
            <div className="bg-lib-grid">
              {files.map((f) => (
                <div key={f} className={`bg-lib-item${using === f ? ' using' : ''}`}>
                  <img src={`bzres://bg/${group}/${f}`} alt={f} onClick={() => void doUse(f)} />
                  {using === f && <span className="bg-lib-using">使用中</span>}
                  <button className="bg-lib-del" title="删除" onClick={() => setDelTarget(f)}>
                    <span className="material-symbols-outlined">delete</span>
                  </button>
                </div>
              ))}
            </div>
          )}
        </div>
        <div className="dialog-footer">
          <button className="btn" onClick={props.onClose}>
            关闭
          </button>
          <button className="btn btn-primary" onClick={() => void doUpload()}>
            <span className="material-symbols-outlined">upload</span>
            上传
          </button>
        </div>
      </div>
      <ConfirmDialog
        open={delTarget != null}
        title="删除背景素材"
        danger
        confirmText="删除"
        onCancel={() => setDelTarget(null)}
        onConfirm={() => void doDelete()}
      >
        {delTarget != null && using === delTarget
          ? '该图正在使用，删除后回退纯色背景。确定删除？'
          : '确定删除这张背景素材？'}
      </ConfirmDialog>
    </div>
  )
}
```

- [ ] **Step 2: 写样式**

```css
/* 背景素材库弹窗（优化建议区第36轮）：浅/深两组网格，当前使用主题色描边 */
.bg-lib-dialog .recycle-tabs {
  margin-bottom: 12px;
}

.bg-lib-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
}

.bg-lib-item {
  position: relative;
  aspect-ratio: 16 / 9;
  border: 2px solid var(--color-border);
  border-radius: 8px;
  overflow: hidden;
}

.bg-lib-item.using {
  border-color: var(--color-primary);
}

.bg-lib-item img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  cursor: pointer;
}

.bg-lib-using {
  position: absolute;
  left: 6px;
  bottom: 6px;
  padding: 2px 8px;
  border-radius: 999px;
  font-size: 0.72em;
  color: var(--color-primary-deep);
  background: var(--color-primary-soft);
  pointer-events: none;
}

.bg-lib-del {
  position: absolute;
  top: 4px;
  right: 4px;
  display: none;
  padding: 2px;
  border: none;
  border-radius: 6px;
  background: var(--color-surface-strong);
  color: var(--color-text);
  cursor: pointer;
}

.bg-lib-item:hover .bg-lib-del {
  display: inline-flex;
}

.bg-lib-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 28px 0;
  opacity: 0.75;
}
```

（CSS 变量 `--color-primary`/`--color-primary-deep`/`--color-primary-soft`/`--color-surface-strong`/`--color-border`/`--color-text` 均为项目既有全局变量——MdDialog 标题胶囊与 Toast 已在用。）

- [ ] **Step 3: typecheck 中间验证**

Run: `npm run typecheck`
Expected: 新文件零错误；仍余 ProfileModule 旧调用错误（Task 4 消除）。

---

### Task 4: ProfileModule 接入素材库入口

**Files:**
- Modify: `src/modules/profile/ProfileModule.tsx`

- [ ] **Step 1: import 与状态**

文件头部 import 区加入：

```tsx
import BgLibraryDialog from './BgLibraryDialog'
```

组件 state 区（`pickBg` 函数附近）加入：

```tsx
  const [bgLibOpen, setBgLibOpen] = useState(false)
```

- [ ] **Step 2: 删旧 pickBg 与旧两行，换新入口**

第 416-422 行 `pickBg` 函数整体删除：

```tsx
  const pickBg = async (kind: 'bg-light' | 'bg-dark'): Promise<void> => {
    const r = await window.api.image.pick(kind)
    if (r) {
      await refreshBg()
      toast('背景图已更新')
    }
  }
```

第 34 行解构中 `refreshBg` 若无其他使用处则一并移除（typecheck/noUnused 会兜底提示）。

第 640-653 行两个 setting-row（背景图浅色/深色）整体替换为：

```tsx
          <div className="setting-row">
            <span className="setting-label">背景素材库</span>
            <button className="btn" onClick={() => setBgLibOpen(true)}>
              <span className="material-symbols-outlined">wallpaper</span>
              管理素材库
            </button>
          </div>
```

- [ ] **Step 3: 挂载弹窗**

ProfileModule JSX 根级末尾（与其他弹窗同级）加入：

```tsx
      {/* 背景素材库管理弹窗（优化建议区第36轮） */}
      <BgLibraryDialog open={bgLibOpen} onClose={() => setBgLibOpen(false)} />
```

- [ ] **Step 4: typecheck 验证**

Run: `npm run typecheck`
Expected: PASS（双 tsconfig）

---

### Task 5: 全量验证与冒烟

- [ ] **Step 1: build**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 2: 冒烟清单（交开发者）**

重启 `npm run dev` 后按设计文档「冒烟清单」逐项验收：旧图迁移且幂等、两组独立、单张上传即生效/多张只入库、上限 21 张拒绝、点图即时切换、双主题互不影响、删除在用图回退纯色、重启保持、双主题弹窗目检。

---

## Self-Review 记录

- **Spec 覆盖**：设计文档「存储与迁移」↔ Task 1 Step 2/3；「IPC 四通道表」↔ Task 1 Step 4 + Task 2；「UI（弹窗）」↔ Task 3/4；「错误处理」↔ 各 catch 分支（上传失败 toast 保留已成功张数=不回滚复制；扫描异常空态；image:pick bg 分支删除）；「零改动区」无需任务。
- **占位符扫描**：无 TBD；全部代码步骤给出完整代码与确切行号锚点。
- **类型一致性**：`{ list: string[]; applied: string | null } | null`（bgUpload）与 `{ list: string[]; wasUsing: boolean }`（bgDelete）在 ipc.ts / preload.ts / api.d.ts / BgLibraryDialog.tsx 四处一致；`group: 'light' | 'dark'` 贯穿一致；settings 键拼接 `bg_bg-${group}` 与 ThemeProvider 现有键 `bg_bg-light`/`bg_bg-dark` 一致。
