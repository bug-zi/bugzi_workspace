# 我是谁·「来一问」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 或 superpowers:executing-plans。设计 `2026-09-23-我是谁来一问-design.md` §1 即任务分解，六步：①`whoami.ts` 新 `whoamiAskOne`（放 `whoamiGenerateIpc` 之后；代码见设计 §1，BUSY 预检/未配置预检/parseQuestions 取首条空抛 PARSE_EMPTY/INSERT 返回 toView）；②`shared/types.ts` LLM_SCENE_LABELS 补 `whoami:daily`/`whoami:extract`/`whoami:askOne` 三行（`other` 之前）；③`ipc.ts` import + `whoami:askOne` handle（`whoami:suggestionUpdate` 之后）；④`preload.ts` whoami 命名空间 `askOne()`（suggestionUpdate 之后，返回 `Promise<WhoamiQuestionView>`）；⑤`api.d.ts` whoami 接口 `askOne()`；⑥`WhoamiZone.tsx` `.whoami-toolbar` 按钮（askingOne state、成功 setData 追加 today、LLM_NOT_CONFIGURED→「请先在本页上方配置 LLM」、BUSY 静默）+ `WhoamiZone.css` `.whoami-toolbar{display:flex;justify-content:flex-end;margin-bottom:8px}`。验证：typecheck 双配置 + build；文档：dev log 260923 追加小节。

**纪律**：禁 git；types.ts 等热点文件串行编辑、失配重读；验证口径 typecheck+build+开发者冒烟。
