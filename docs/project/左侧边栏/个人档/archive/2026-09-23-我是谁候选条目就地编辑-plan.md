# 我是谁·候选画像条目就地编辑 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 我是谁 WhoamiZone 候选画像条目支持行内就地编辑（类别 select + 内容输入框），保存持久化写回候选记录。

**Architecture:** 新 IPC `whoami:suggestionUpdate` 主进程原位替换 suggestions JSON；`PROFILE_CATEGORIES` 提升到 `src/shared/types.ts` 三处共用；WhoamiItem 行内编辑态局部 state。零 DB 迁移。

**Tech Stack:** Electron 44 + React 19 + TypeScript（electron-vite）。

**依据设计:** `docs/project/左侧边栏/个人档/2026-09-23-我是谁候选条目就地编辑-design.md`。

**项目纪律:** 禁止 AI 执行 git 操作（无 commit 步骤）；验证口径 = typecheck 双配置 + build + 开发者冒烟。`src/shared/types.ts` 为并行会话热点文件——编辑严格串行、失败重读。

---

### Task 1: shared/types.ts 导出 PROFILE_CATEGORIES

- [ ] **Step 1.1:** Grep 定位 `WhoamiQuestionView` 接口在 `src/shared/types.ts` 的位置，在其后插入：

```ts
// 画像类别预设十类（个人档 datalist、我是谁候选提炼校验与候选编辑下拉三处共用；260923 收敛为单一来源）
export const PROFILE_CATEGORIES = [
  '基本档案', '性格特质', '擅长能力', '兴趣爱好', '生活方式', '社交出行',
  '学习与技能', '职业规划', '价值观', '其他'
]
```

### Task 2: whoami.ts — 改 import + 删本地清单 + 新函数

- [ ] **Step 2.1:** `import { SettingsKeys } from '../../src/shared/types'` → `import { PROFILE_CATEGORIES, SettingsKeys } from '../../src/shared/types'`
- [ ] **Step 2.2:** 删除本地 `/** 候选类别清单…*/ const PROFILE_CATEGORIES = […]` 整块（L12-16）
- [ ] **Step 2.3:** `whoamiResolve` 之后追加：

```ts
/** 就地编辑候选条目（优化建议区第51轮）：类别须在清单内、内容 trim 非空（≤120 字与提炼口径一致）；原位替换写回候选记录 */
export function whoamiSuggestionUpdate(id: number, index: number, category: string, content: string): WhoamiQuestionView {
  const row = getRow(id)
  if (!row) throw new Error('NOT_FOUND')
  const items = row.suggestions ? (JSON.parse(row.suggestions) as { category: string; content: string }[]) : []
  if (index < 0 || index >= items.length) throw new Error('INDEX_OUT_OF_RANGE')
  const cat = category.trim()
  const text = content.trim().slice(0, 120)
  if (!PROFILE_CATEGORIES.includes(cat)) throw new Error('BAD_CATEGORY')
  if (!text) throw new Error('CONTENT_EMPTY')
  items[index] = { category: cat, content: text }
  getDb()
    .prepare('UPDATE whoami_questions SET suggestions = ?, updated_at = ? WHERE id = ?')
    .run(JSON.stringify(items), nowIso(), id)
  return toView(getRow(id)!)
}
```

### Task 3: ProfileModule.tsx — 改 import + 删本地清单

- [ ] **Step 3.1:** 定位其 shared/types import 行，加入 `PROFILE_CATEGORIES`（注意该文件用的是值导入还是 type 导入——是值导入，需并入既有值 import 或新开一行 `import { PROFILE_CATEGORIES } from '../../shared/types'`）
- [ ] **Step 3.2:** 删除本地 `/** 画像类别预设…*/ const PROFILE_CATEGORIES = […]` 整块（L17-21，含注释行）

### Task 4: IPC 三件套

- [ ] **Step 4.1:** `electron/ipc.ts` L30 import 增 `whoamiSuggestionUpdate`；L2975-2976 `whoami:resolve` handle 之后追加：

```ts
  ipcMain.handle('whoami:suggestionUpdate', (_e, id: number, index: number, category: string, content: string) =>
    whoamiSuggestionUpdate(id, index, category, content)
  )
```

- [ ] **Step 4.2:** `electron/preload.ts` whoami 命名空间（L1040 resolve 之后）追加：

```ts
    suggestionUpdate: (
      id: number,
      index: number,
      category: string,
      content: string
    ): Promise<import('../src/shared/types').WhoamiQuestionView> =>
      ipcRenderer.invoke('whoami:suggestionUpdate', id, index, category, content),
```

（实际格式对齐该命名空间既有成员风格——多为单行；若 resolve 是单行则此条也压成项目惯用两行式）

- [ ] **Step 4.3:** `src/renderer/api.d.ts` whoami 接口（L1717 resolve 之后）追加：

```ts
    suggestionUpdate(id: number, index: number, category: string, content: string): Promise<import('../shared/types').WhoamiQuestionView>
```

### Task 5: WhoamiZone.tsx + CSS

- [ ] **Step 5.1:** 父组件 `doResolve` 之后加 handler：

```tsx
  /** 就地编辑候选条目（第51轮）：成功 patch 回写返回 true；失败 toast 保持编辑态 */
  const doUpdateSuggestion = async (
    q: WhoamiQuestionView,
    index: number,
    category: string,
    content: string
  ): Promise<boolean> => {
    try {
      patch(await window.api.whoami.suggestionUpdate(q.id, index, category, content))
      return true
    } catch (e) {
      toast((e as Error).message || '保存失败')
      return false
    }
  }
```

- [ ] **Step 5.2:** `WhoamiItem` props 增 `onUpdateSuggestion: (index: number, category: string, content: string) => Promise<boolean>`；两处渲染点（today L140 附近 / pending L157 附近）各传 `onUpdateSuggestion={(i, c, t) => doUpdateSuggestion(q, i, c, t)}`。
- [ ] **Step 5.3:** `WhoamiItem` 内加局部编辑 state 与保存函数（组件顶部）：

```tsx
  const [editIdx, setEditIdx] = useState<number | null>(null)
  const [editCat, setEditCat] = useState('')
  const [editText, setEditText] = useState('')
  const saveEdit = async (i: number): Promise<void> => {
    const text = editText.trim()
    if (!text || !editCat) return
    if (await props.onUpdateSuggestion(i, editCat, text)) setEditIdx(null)
  }
```

（`import { useState } from 'react'` 若缺则补；`PROFILE_CATEGORIES` 从 `../../shared/types` import）

- [ ] **Step 5.4:** `q.suggestions.map` 渲染改为三目——`editIdx === i` 出编辑行（select + input + 保存/取消），否则默认行（action 区铅笔在「加入画像」左）：

```tsx
          {q.suggestions.map((s, i) =>
            editIdx === i ? (
              <div key={i} className="whoami-suggestion whoami-suggestion-editing">
                <select
                  className="whoami-select"
                  value={editCat}
                  onChange={(e) => setEditCat(e.target.value)}
                >
                  {PROFILE_CATEGORIES.map((c) => (
                    <option key={c} value={c}>
                      {c}
                    </option>
                  ))}
                </select>
                <input
                  className="whoami-input"
                  value={editText}
                  placeholder="一句话概括…"
                  onChange={(e) => setEditText(e.target.value)}
                  onKeyDown={(e) => {
                    if (e.key === 'Enter') void saveEdit(i)
                    else if (e.key === 'Escape') setEditIdx(null)
                  }}
                />
                <span className="whoami-suggestion-actions">
                  <button className="btn btn-primary" disabled={!editText.trim()} onClick={() => void saveEdit(i)}>
                    保存
                  </button>
                  <button className="btn" onClick={() => setEditIdx(null)}>
                    取消
                  </button>
                </span>
              </div>
            ) : (
              <div key={i} className="whoami-suggestion">
                <span className="whoami-cat">{s.category}</span>
                <span className="whoami-suggestion-content">{s.content}</span>
                <span className="whoami-suggestion-actions">
                  <button
                    className="icon-btn"
                    title="编辑此条"
                    onClick={() => {
                      setEditIdx(i)
                      setEditCat(s.category)
                      setEditText(s.content)
                    }}
                  >
                    <span className="material-symbols-outlined">edit</span>
                  </button>
                  <button className="btn btn-primary" onClick={() => props.onResolve(i, true)}>
                    加入画像
                  </button>
                  <button className="btn" onClick={() => props.onResolve(i, false)}>
                    忽略
                  </button>
                </span>
              </div>
            )
          )}
```

- [ ] **Step 5.5:** `WhoamiZone.css` 末尾追加：

```css
/* 候选条目就地编辑（优化建议区第51轮）：编辑态实线描边 + 实底区分默认虚线行 */
.whoami-suggestion-editing {
  border-style: solid;
  background: var(--color-surface-strong);
}
.whoami-select {
  flex-shrink: 0;
  padding: 4px 6px;
  border: 1px solid var(--color-border);
  border-radius: 8px;
  background: var(--color-surface-strong);
  color: var(--color-text);
}
```

### Task 6: 验证

- [ ] **Step 6.1:** `npm run typecheck`——双配置无错误
- [ ] **Step 6.2:** `npm run build`——构建成功

### Task 7: 文档同步

- [ ] **Step 7.1:** `docs/log/260923.md` 追加小节（`> 创建于` 用 bash date 实际时间）：改动五文件（types/whoami/ipc/preload/api.d.ts/WhoamiZone×2 计七处）、清单收敛说明、typecheck+build、待冒烟清单
- [ ] **Step 7.2:** 优化建议区：待完成删该条，归档区顶部插「### 第51轮（260923）：我是谁候选画像条目就地编辑」+ 完成说明 + 待冒烟
