# 信息源——显式已读 + 文章收藏 + 30 天彻底清理 实施计划

> **执行方式**：本计划任务逐个执行（checkbox 勾选跟踪）。**项目纪律：AI 不做任何 git 操作**；每任务验证 = `npm run typecheck`（本项目无测试框架，最终以开发者 dev 冒烟为准）。
>
> **执行状态**：260911 12:21 内联执行完毕——10 任务全过、typecheck 六连绿、零返工（仅 Task 5 因 import 依赖提前合并验证，见文末记录）；Task 10 Step 4（文档归档）留待开发者冒烟通过后。
>
> **typecheck 前必清增量缓存**（260911 排障结论，tsbuildinfo 会重放旧诊断）：
> `rm -f tsconfig.node.tsbuildinfo tsconfig.web.tsbuildinfo && npm run typecheck`
>
> **并行会话警示**：db.ts / api.d.ts / types.ts 当前有其他会话未提交改动（学习库/背景素材库），每个任务动手前先重读目标文件对应区域的磁盘现状，Edit 失败即重读再试。

**Goal**：信息源已读判定改显式按钮（可逆）、文章可收藏（独立收藏页、永不被清理）、已读未收藏文章超 30 天整行彻底删除。

**Architecture**：articles 表加 `favorited_at` 一列（DB v34）承载收藏态，单一数据源；`articles:setRead`/`articles:setFavorite` 两个新 IPC 通道；列表查询按三视图分流；清理函数从「置空正文保轻行」改为「整行 DELETE」。

**Tech Stack**：Electron 44 主进程（node:sqlite）+ React 19 渲染层，无新依赖。

**设计依据**：`docs/project/左侧边栏/信息源/2026-09-11-信息源已读收藏与清理-design.md`（下称 design.md）。

---

### Task 1: DB v34 迁移

**Files:**
- Modify: `electron/db/db.ts`（v33 块之后、函数收尾 `}` 之前，约 872 行处）

- [x] **Step 1: 在 `if (version < 33) { … }` 块之后追加 v34 块**

```ts
  if (version < 34) {
    // v34：信息源显式已读与文章收藏（2026-09-11-信息源已读收藏与清理-design.md §1）——
    // articles 加 favorited_at（NULL=未收藏）：收藏文章只出现在收藏页、永不被清理
    // （清理只删已读+未收藏；未读与收藏都保留）。无新表、无索引（数据量小）。
    d.exec(`ALTER TABLE articles ADD COLUMN favorited_at TEXT`)
    d.exec('PRAGMA user_version = 34')
  }
```

- [x] **Step 2: typecheck**

`rm -f tsconfig.node.tsbuildinfo tsconfig.web.tsbuildinfo && npm run typecheck` → 0 错误。

---

### Task 2: 共享类型（types.ts + api.d.ts 双份手工同步）

**Files:**
- Modify: `src/shared/types.ts:522-556`（ArticleRecord / ArticleSummary / FeedView）
- Modify: `src/renderer/api.d.ts:263-295`（同名三份声明——该文件不 import shared，是手工镜像）

> 本任务后**暂不跑 typecheck**：`ArticleSummary.favorited` 必使 feed.ts 现有映射报缺失，与 Task 3 一并验证。

- [x] **Step 1: types.ts 三处**

`ArticleRecord`（`read_at` 行后插一行）：

```ts
  read_at: string | null
  favorited_at: string | null
```

`ArticleSummary`（`read_at` 行后插）：

```ts
  read_at: string | null
  /** 已收藏（260911 收藏页/星标实心态） */
  favorited: boolean
```

`FeedView` 整行替换：

```ts
/** 文章列表视图（260911 扩三值）：unread=收件箱（未读）；archive=已归档（已读按时间翻）；favorite=收藏（收藏时间倒序全量、不筛源） */
export type FeedView = 'unread' | 'archive' | 'favorite'
```

- [x] **Step 2: api.d.ts 同名三份做完全相同的修改**（ArticleRecord 加 `favorited_at: string | null`、ArticleSummary 加 `favorited: boolean`、FeedView 扩 `'favorite'`）

---

### Task 3: 服务层 feed.ts（核心改造）

**Files:**
- Modify: `electron/services/feed.ts`（listFeeds/addFeed 未读子查询、listArticles、openArticle、markAllRead、新函数 ×2、清理函数替换）

- [x] **Step 1: listFeeds 与 addFeed 两处未读子查询排除收藏**（约 198 与 235 行，同一子查询出现两次，都改）

```sql
(SELECT COUNT(*) FROM articles a WHERE a.feed_id = f.id AND a.read_at IS NULL AND a.favorited_at IS NULL) AS unread
```

- [x] **Step 2: listArticles 整函数替换 + 新增 toSummary 映射**

```ts
/** 文章列表：unread/archive 排除收藏、30 天窗口起按 sinceDays 加载（remaining=窗口外计数，「加载更早」用）；
 *  favorite 全量按收藏时间倒序，不筛源不设窗口（design.md §5）。
 *  260911：视图查询排除收藏（收藏文章只出现在收藏页）。 */
export function listArticles(feedId: number | null, view: FeedView, sinceDays: number): FeedListView {
  const d = getDb()
  if (view === 'favorite') {
    const rows = d
      .prepare('SELECT * FROM articles WHERE favorited_at IS NOT NULL ORDER BY favorited_at DESC, id DESC')
      .all() as unknown as ArticleRecord[]
    return { articles: rows.map(toSummary), remaining: 0 }
  }
  const readClause = view === 'unread' ? 'read_at IS NULL' : 'read_at IS NOT NULL'
  const sinceIso = new Date(Date.now() - sinceDays * 86_400_000).toISOString()
  const feedClause = feedId == null ? '' : ' AND feed_id = ?'
  const params: (string | number)[] = [sinceIso, ...(feedId == null ? [] : [feedId])]
  const rows = d
    .prepare(
      `SELECT * FROM articles WHERE ${readClause} AND favorited_at IS NULL AND COALESCE(published_at, fetched_at) >= ?${feedClause}
       ORDER BY COALESCE(published_at, fetched_at) DESC, id DESC`
    )
    .all(...params) as unknown as ArticleRecord[]
  const remainingRow = d
    .prepare(
      `SELECT COUNT(*) AS c FROM articles WHERE ${readClause} AND favorited_at IS NULL AND COALESCE(published_at, fetched_at) < ?${feedClause}`
    )
    .get(...params) as { c: number }
  return { articles: rows.map(toSummary), remaining: remainingRow.c }
}

/** 轻量行映射（三视图共用；剥标签预览 120 字） */
function toSummary(r: ArticleRecord): ArticleSummary {
  return {
    id: r.id,
    feed_id: r.feed_id,
    title: r.title,
    url: r.url,
    author: r.author,
    published_at: r.published_at,
    fetched_at: r.fetched_at,
    read_at: r.read_at,
    favorited: !!r.favorited_at,
    has_summary: !!r.summary_text,
    preview: stripTags(r.content_fetched_html || r.content_feed_html || '').slice(0, 120)
  }
}
```

- [x] **Step 3: openArticle 去掉自动标已读**（整函数替换）

```ts
/** 打开文章：懒抓正文 + 全量返回（260911 起不再自动标已读——已读判定唯一入口是显式「已读」按钮） */
export async function openArticle(id: number): Promise<ArticleRecord> {
  const d = getDb()
  const row = d.prepare('SELECT * FROM articles WHERE id = ?').get(id) as unknown as ArticleRecord | undefined
  if (!row) throw new Error('NOT_FOUND')
  await extractArticle(id)
  return (d.prepare('SELECT * FROM articles WHERE id = ?').get(id) as unknown as ArticleRecord) ?? row
}
```

- [x] **Step 4: openArticle 之后新增两函数、markAllRead 排除收藏**

```ts
/** 显式已读/再看看（design.md §2）：true→read_at=now；false→清空回未读（行回收件箱） */
export function setArticleRead(id: number, read: boolean): void {
  getDb().prepare('UPDATE articles SET read_at = ? WHERE id = ?').run(read ? nowIso() : null, id)
}

/** 收藏/取消收藏（design.md §3）：收藏文章只出现在收藏页且永不被清理；
 *  取消后按已读状态自然回流收件箱（未读）或已归档（已读） */
export function setArticleFavorite(id: number, fav: boolean): void {
  getDb().prepare('UPDATE articles SET favorited_at = ? WHERE id = ?').run(fav ? nowIso() : null, id)
}
```

`markAllRead` 两条 SQL 的 `read_at IS NULL` 后各追加 `AND favorited_at IS NULL`：

```ts
/** 全部标已读（feedId 空=全部源；不波及收藏——收藏未读不在收件箱展示，design.md §2） */
export function markAllRead(feedId: number | null): void {
  const d = getDb()
  if (feedId == null)
    d.prepare('UPDATE articles SET read_at = ? WHERE read_at IS NULL AND favorited_at IS NULL').run(nowIso())
  else
    d.prepare('UPDATE articles SET read_at = ? WHERE feed_id = ? AND read_at IS NULL AND favorited_at IS NULL').run(
      nowIso(),
      feedId
    )
}
```

- [x] **Step 5: cleanupOldArticleBodies 整函数替换为 cleanupOldArticles**（含注释）

```ts
/** 30 天已读文章彻底清理（design.md §4，替代第28轮「清正文保轻行」）：已读+未收藏+超 30 天整行 DELETE；
 *  未读文章无论多老都保留（待读积压由用户自主处理），收藏文章永不清除。
 *  启动 + 每日零点调用（scheduler.ts），静默，返回清理条数。 */
export function cleanupOldArticles(): number {
  const cutoff = new Date(Date.now() - 30 * 86_400_000).toISOString()
  const r = getDb()
    .prepare(
      `DELETE FROM articles
       WHERE read_at IS NOT NULL AND favorited_at IS NULL
         AND COALESCE(published_at, fetched_at) < ?`
    )
    .run(cutoff)
  return Number(r.changes)
}
```

- [x] **Step 6: typecheck**（连同 Task 2 的类型改动一并验证）

`rm -f tsconfig.node.tsbuildinfo tsconfig.web.tsbuildinfo && npm run typecheck` → 0 错误（此时尚有 ipc.ts 对 `cleanupOldArticleBodies` 的死 import 引用——若报错，先到 Task 4 Step 1 一并处理再回来跑）。

---

### Task 4: IPC 三处桥接（ipc.ts / preload.ts / api.d.ts）

**Files:**
- Modify: `electron/ipc.ts:68-80`（import）、`electron/ipc.ts:1030-1041`（handler 区）
- Modify: `electron/preload.ts:550-564`（articles 桥）
- Modify: `src/renderer/api.d.ts:937-946`（articles API 声明）

- [x] **Step 1: ipc.ts import 区**——删除死 import `cleanupOldArticleBodies`（全文件仅 import 行引用它，真正的清理调用在 scheduler.ts），加两个新函数：

```ts
import {
  listFeeds,
  fetchAllFeeds,
  probeFeed,
  addFeed,
  renameFeed,
  removeFeed,
  listArticles,
  openArticle,
  markAllRead,
  setArticleRead,
  setArticleFavorite,
  summarizeArticle
} from './services/feed'
```

- [x] **Step 2: ipc.ts handler 区**（`articles:open` 与 `articles:markAllRead` 之间插入，`articles:open` 上方注释同步改）

```ts
  /** 打开文章：懒抓正文 + 全量返回（260911 起不再自动标已读——已读判定唯一入口是 setRead） */
  ipcMain.handle('articles:open', (_e, id: number) => openArticle(id))
  /** 显式已读/再看看（read=false 清空 read_at，行回收件箱） */
  ipcMain.handle('articles:setRead', (_e, id: number, read: boolean) => {
    setArticleRead(id, read)
    return true
  })
  /** 收藏/取消收藏（取消后按已读状态回流收件箱/已归档） */
  ipcMain.handle('articles:setFavorite', (_e, id: number, fav: boolean) => {
    setArticleFavorite(id, fav)
    return true
  })
```

- [x] **Step 3: preload.ts articles 桥**（`open` 行注释同步改，其后插两方法）

```ts
    /** 打开文章：懒抓正文 + 全量返回（260911 起不再自动标已读） */
    open: (id: number): Promise<import('../src/shared/types').ArticleRecord> => ipcRenderer.invoke('articles:open', id),
    /** 显式已读/再看看（read=false 回退收件箱） */
    setRead: (id: number, read: boolean): Promise<boolean> => ipcRenderer.invoke('articles:setRead', id, read),
    /** 收藏/取消收藏（取消后按已读状态回流收件箱/已归档） */
    setFavorite: (id: number, fav: boolean): Promise<boolean> => ipcRenderer.invoke('articles:setFavorite', id, fav),
```

- [x] **Step 4: api.d.ts articles 声明镜像**（`open` 注释同改，其后插两方法）

```ts
    /** 打开文章：懒抓正文 + 全量返回（260911 起不再自动标已读） */
    open(id: number): Promise<ArticleRecord>
    /** 显式已读/再看看（read=false 回退收件箱） */
    setRead(id: number, read: boolean): Promise<boolean>
    /** 收藏/取消收藏（取消后按已读状态回流收件箱/已归档） */
    setFavorite(id: number, fav: boolean): Promise<boolean>
```

- [x] **Step 5: typecheck** → 0 错误。

---

### Task 5: 定时器 scheduler.ts

**Files:**
- Modify: `electron/services/scheduler.ts`（import + 两处调用 + 文件头注释）

- [x] **Step 1: 改 import 与两处调用**

```ts
import { cleanupOldArticles } from './feed'
```

零点块内：

```ts
      const a = cleanupOldArticles()
      if (a > 0) console.log(`[scheduler] 信息源 30 天已读文章清理 ${a} 条`)
```

启动块内：

```ts
    const a = cleanupOldArticles()
    if (a > 0) console.log(`[scheduler] 启动清理信息源 30 天已读文章 ${a} 条`)
```

文件头第 1 行注释「信息源老文章正文清理」改「信息源 30 天已读文章清理」。

- [x] **Step 2: typecheck** → 0 错误。

---

### Task 6: 样式 feed.css

**Files:**
- Modify: `src/modules/feed/feed.css`（文章行结构改造 + 新按钮样式）

- [x] **Step 1: `.feed-art-row` 块（现 93-105 行）替换并追加新类**

```css
.feed-art-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 12px;
  border-radius: var(--radius-sm);
  background: var(--color-surface-strong);
  border: 1px solid transparent;
  cursor: pointer;
}
.feed-art-row:hover {
  border-color: var(--color-border);
}
/* 主体列（标题/预览/meta）+ 右侧常显操作区（260911 显式已读+收藏） */
.feed-art-main {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 3px;
}
.feed-art-actions {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-shrink: 0;
}
/* 已读/再看看双态小按钮（主题色系、无彩亮——全局硬性规则） */
.feed-read-btn {
  display: flex;
  align-items: center;
  gap: 4px;
  padding: 4px 10px;
  border-radius: 14px;
  border: 1px solid var(--color-border);
  background: transparent;
  color: var(--color-text-secondary);
  font-size: 0.78em;
  cursor: pointer;
}
.feed-read-btn:hover {
  border-color: var(--color-primary);
  color: var(--color-primary-deep);
}
:root[data-theme='dark'] .feed-read-btn:hover {
  color: #fff;
}
.feed-read-btn .material-symbols-outlined {
  font-size: 15px;
}
/* 星标收藏态：实心主题色（基类 FILL 0，此处覆盖 FILL 轴） */
.feed-star.on .material-symbols-outlined {
  font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24;
  color: var(--color-primary);
}
```

（`--color-primary-deep` 在深色主题下白字的写法沿用本文件 `.feed-load-more:hover` 先例。）

---

### Task 7: 列表渲染 FeedModule.tsx

**Files:**
- Modify: `src/modules/feed/FeedModule.tsx`

- [x] **Step 1: openArticle 去 load 调用与注释**（整函数替换——open 已不写库，列表无需刷新；阅读视图内总结生成会经 `onChanged` 刷新）

```ts
  /** 打开文章（open 只懒抓正文不再标已读——已读判定唯一入口是「已读」按钮，design.md §2） */
  const openArticle = async (id: number): Promise<void> => {
    try {
      const full = await window.api.articles.open(id)
      setReadingArticle(full)
      setReadingId(id)
    } catch (e) {
      toast(`打开失败：${(e as Error).message}`)
    }
  }
```

- [x] **Step 2: 新增两个流转 handler**（放 openArticle 之后）

```ts
  /** 已读/再看看（260911）：点击即流转，行随列表刷新消失/回归 */
  const toggleRead = async (a: ArticleSummary): Promise<void> => {
    try {
      await window.api.articles.setRead(a.id, !a.read_at)
      await load()
    } catch (e) {
      toast(`操作失败：${(e as Error).message}`)
    }
  }

  /** 收藏/取消收藏（260911）：收藏进收藏页，取消按已读状态回流收件箱/已归档 */
  const toggleFav = async (a: ArticleSummary): Promise<void> => {
    try {
      await window.api.articles.setFavorite(a.id, !a.favorited)
      await load()
    } catch (e) {
      toast(`操作失败：${(e as Error).message}`)
    }
  }
```

- [x] **Step 3: 顶部 tab 加第三枚「收藏」**（recycle-tabs 内、已归档按钮后）

```tsx
          <button
            className={`recycle-tab${view === 'favorite' ? ' active' : ''}`}
            onClick={() => switchView('favorite')}
          >
            收藏
          </button>
```

- [x] **Step 4: 空态文案扩 favorite 分支**（empty-state 三元改四分支）

```tsx
            {fetching
              ? '正在拉取…'
              : view === 'favorite'
                ? '还没有收藏的文章（点文章行右侧星标收藏）'
                : view === 'unread'
                  ? activeFeedRow
                    ? '该源没有未读文章'
                    : '没有未读文章，点「刷新」拉取新内容'
                  : activeFeedRow
                    ? '该源近 30 天没有已读文章'
                    : '近 30 天没有已读文章'}
```

- [x] **Step 5: 文章行重构**（现 269-277 行的 map 体整体替换）

```tsx
              {articles.map((a) => (
                <div
                  key={a.id}
                  className={`feed-art-row${a.read_at ? '' : ' unread'}`}
                  onClick={() => void openArticle(a.id)}
                >
                  <div className="feed-art-main">
                    <div className="feed-art-title">{a.title}</div>
                    {a.preview && <div className="feed-art-preview">{a.preview}</div>}
                    <div className="feed-art-meta">
                      {(activeFeed === null || view === 'favorite') && (
                        <span>{feeds.find((f) => f.id === a.feed_id)?.title ?? ''}</span>
                      )}
                      <span>{relTime(a.published_at ?? a.fetched_at)}</span>
                    </div>
                  </div>
                  <div className="feed-art-actions">
                    <button
                      className={`icon-btn feed-star${a.favorited ? ' on' : ''}`}
                      title={a.favorited ? '取消收藏' : '收藏'}
                      onClick={(e) => {
                        e.stopPropagation()
                        void toggleFav(a)
                      }}
                    >
                      <span className="material-symbols-outlined">star</span>
                    </button>
                    <button
                      className="feed-read-btn"
                      title={a.read_at ? '清除已读，回到收件箱' : '标记已读，移入已归档'}
                      onClick={(e) => {
                        e.stopPropagation()
                        void toggleRead(a)
                      }}
                    >
                      <span className="material-symbols-outlined">
                        {a.read_at ? 'move_to_inbox' : 'mark_email_read'}
                      </span>
                      {a.read_at ? '再看看' : '已读'}
                    </button>
                  </div>
                </div>
              ))}
```

（收藏页跨源，meta 源名在 `view === 'favorite'` 时恒显。）

- [x] **Step 6: 「加载更早」只在收件箱显示**（归档天然 ≤30 天，design.md §4；收藏页无窗口）

```tsx
              {view === 'unread' && remaining > 0 && (
                <button className="feed-load-more" onClick={() => setSinceDays((s) => s + 30)}>
                  加载更早 30 天 · 还剩 {remaining} 篇
                </button>
              )}
```

- [x] **Step 7: 顶部注释与第 17-19 行状态注释顺带更新**（「读完即消失」口径改「显式已读流转」——一行文字，防误导后来者）

- [x] **Step 8: typecheck** → 0 错误。

---

### Task 8: 阅读视图 ArticleView.tsx

**Files:**
- Modify: `src/modules/feed/ArticleView.tsx`

- [x] **Step 1: 加状态与两个 handler**（state 声明区加两行；handler 放 runSummarize 之后）

```ts
  const [read, setRead] = useState(!!props.article.read_at)
  const [fav, setFav] = useState(!!props.article.favorited_at)
```

```ts
  /** 已读/再看看（260911）：与列表行双入口，读完好顺手点 */
  const toggleRead = async (): Promise<void> => {
    try {
      await window.api.articles.setRead(props.article.id, !read)
      setRead(!read)
      props.onChanged()
    } catch (e) {
      toast(`操作失败：${(e as Error).message}`)
    }
  }

  /** 收藏/取消收藏（260911） */
  const toggleFav = async (): Promise<void> => {
    try {
      await window.api.articles.setFavorite(props.article.id, !fav)
      setFav(!fav)
      props.onChanged()
    } catch (e) {
      toast(`操作失败：${(e as Error).message}`)
    }
  }
```

- [x] **Step 2: 既有 useEffect（props.article.id 重置）补两行复位**

```ts
  useEffect(() => {
    setSummary(props.article.summary_text)
    setGenError('')
    setRead(!!props.article.read_at)
    setFav(!!props.article.favorited_at)
    if (!props.article.summary_text) void runSummarize(crypto.randomUUID())
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [props.article.id])
```

- [x] **Step 3: 头部条插两按钮**（`bk-reader-title` 与「去原文」之间）

```tsx
        <button
          className={`icon-btn feed-star${fav ? ' on' : ''}`}
          title={fav ? '取消收藏' : '收藏'}
          onClick={() => void toggleFav()}
        >
          <span className="material-symbols-outlined">star</span>
        </button>
        <button className="btn" onClick={() => void toggleRead()}>
          <span className="material-symbols-outlined">{read ? 'move_to_inbox' : 'mark_email_read'}</span>
          {read ? '再看看' : '已读'}
        </button>
```

- [x] **Step 4: 文件头注释补一句**（「260911：头部加显式已读/再看看与收藏星标」）

- [x] **Step 5: typecheck** → 0 错误。

---

### Task 9: designs-specs.md 增量注记

**Files:**
- Modify: `docs/project/左侧边栏/信息源/designs-specs.md`（头部注记 + 冲突小节）

- [x] **Step 1: 头部 `260909 增量注记` 之前插 `260911 增量注记`**（沿用第28轮格式，指明旧口径作废点）

> 260911 增量注记：已读判定改**显式按钮**（打开文章不再自动标已读，第28轮「读完即消失」与「无标未读回退」口径作废）——行/阅读视图双入口「已读｜再看看」双态可逆；新增**文章收藏**（DB v34 `favorited_at`，「收藏」第三 tab，收藏时间倒序全量不筛源，永不被清理；取消按已读状态回流收件箱/已归档）；老数据清理改**30 天整行彻底删除**（已读+未收藏，第28轮 180 天清正文保轻行口径作废；归档页「加载更早」移除）。详见 §2.4/§2.5/§3/§4 与 `2026-09-11-信息源已读收藏与清理-design.md`。

- [x] **Step 2: 修订冲突小节**（§2.4 收件箱查询、§2.5 open 标已读描述、§3 列表交互「读完即消失」句、§4 IPC 表加两通道、§6 验收清单两条）——以 design.md 口径为准，改动处标注（260911）。

---

### Task 10: 全量验证 + 日志 + 冒烟清单

- [x] **Step 1: 清缓存全量 typecheck**

`rm -f tsconfig.node.tsbuildinfo tsconfig.web.tsbuildinfo && npm run typecheck` → 0 错误。

- [x] **Step 2: devlog 补记**（`docs/log/260911.md` 末尾追加，时间 `date '+%Y-%m-%d %H:%M'` 取实际值）——记录实施完成、涉及文件、typecheck 结果、「运行时冒烟待开发者」。

- [x] **Step 3: 冒烟清单交开发者**（dev 运行）：

  1. 迁移：旧库启动无报错，已有文章/订阅完好（v33→v34）
  2. 打开一封未读：阅读视图正常、返回列表**行仍在**（不再自动消失）
  3. 点行上「已读」：行消失→已归档页可见；已归档点「再看看」：回 收件箱
  4. 阅读视图头部按钮同验一遍（已读/再看看/星标）
  5. 星标收藏：行消失→收藏页可见（实心星）；收藏页点开阅读视图正常（正文/总结在）
  6. 取消收藏：未读收藏→回收件箱；已读收藏→回已归档
  7. 收藏页不受左栏选源影响；meta 恒显源名；收藏页空态文案
  8. 「全部标已读」后：收件箱清空、收藏页未读文章仍为未读；源徽标数不含收藏
  9. 已归档页无「加载更早」按钮；收件箱文章多时仍可加载更早
  10. SQL 造数验证清理（如 `UPDATE articles SET read_at='2026-07-01', published_at='2026-07-01' WHERE id=<某已读未收藏id>` 后重启 dev）：该行被删；同时造一篇同日期未读与一篇已读已收藏作对照——两者保留
  11. 双主题下按钮配色正常（无彩亮、星标实心主题色）

- [ ] **Step 4（可选，开发者冒烟通过后）**：把 `2026-09-11-信息源已读收藏与清理-design.md` 与本计划移入 `archive/` 子目录（第28轮文档至今仍在模块根目录，跟随开发者归档节奏即可）。

---

## 自查记录（writing-plans Self-Review）

- **Spec 覆盖**：design.md §1→Task 1；§2→Task 3/7/8；§3→Task 1/3/4/7/8；§4→Task 3/5/7；§5→Task 2/3/7；§6→Task 6；验证口径→Task 10。无遗漏。
- **占位符**：无 TBD/TODO；所有代码块完整可粘。
- **类型一致性**：`setArticleRead`/`setArticleFavorite`（feed.ts）↔ IPC 通道 `articles:setRead`/`articles:setFavorite` ↔ preload `setRead`/`setFavorite` ↔ api.d.ts 同名；`toSummary` 返回 `ArticleSummary`（含 `favorited`）；`FeedView` 三值双文件同步。
- **已知风险**：①api.d.ts 与 types.ts 双份类型必须同改（本计划已拆进 Task 2）；②并行会话交叉 db.ts/api.d.ts/types.ts——每任务前重读磁盘现状；③FILL 轴实心星依赖 npm material-symbols 变量字体（global.css 基类已用同轴声明，可行）；④`mark_email_read`/`move_to_inbox` 图标名以 fonts.google.com/icons 实存为准，冒烟时确认渲染。
