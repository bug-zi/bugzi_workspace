# 信息源——收件箱模式 + 时间窗口加载 + 老数据瘦身 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 主列表改收件箱模式（只显未读、读完即消失、已归档视图可翻已读）、30 天时间窗口加载（窗口外「加载更早」触达、顺带治全量 SELECT 性能病）、180 天前老文章清正文保轻行（定时清理）。

**Architecture:** 查询层重构 `listArticles`（增 view/sinceDays 参数、返回 `{ articles, remaining }`）+ IPC 三件套透传新参数；主进程新增 `cleanupOldArticleBodies()` 挂进既有 scheduler（启动 + 零点）；渲染层 FeedModule 加视图切换/窗口状态/加载更早。无 DB 迁移（零新表新列）。

**Tech Stack:** Electron 44 主进程 + React 19 + TypeScript；SQLite（node:sqlite）。

**Spec:** `docs/project/左侧边栏/信息源/2026-09-09-信息源收件箱与时间窗口-design.md`（260909 已批准）

---

## 项目适配说明（优先于技能默认）

- **无测试框架**：本项目只以 `npm run typecheck` + 人工冒烟为验证口径，本计划沿用；不引入测试框架（YAGNI）。
- **Git 纪律**：禁止 AI 执行任何 git 操作（CLAUDE.md）。各任务「提交检查点」只给建议提交信息，**由开发者手动执行**。
- **工作区**：直接在 dev 分支工作区实施（项目惯例，不开 worktree）。
- **行号锚点**：行号以 260909 工作区为准，实施时按代码内容定位漂移。
- **执行顺序**：本计划在「灵感泉完整点子文档」计划之后执行（Task 6 归档两条目共用第 28 轮小节）；单独执行亦可，Task 6 只归档已实施条目。

## 文件结构

| 文件 | 职责 | 动作 |
| --- | --- | --- |
| `src/shared/types.ts:309-322` 后 | `FeedView` / `FeedListView` 新类型 | 增 |
| `electron/services/feed.ts:248-275` | `listArticles` 重构（view/sinceDays/remaining） | 改 |
| `electron/services/feed.ts` 文件尾 | `cleanupOldArticleBodies()` 老数据瘦身 | 增 |
| `electron/ipc.ts:57-68,776` | import 增补 + `articles:list` 透传 | 改 |
| `electron/preload.ts:460-463` | `articles.list` 签名升级 | 改 |
| `src/renderer/api.d.ts:782-791` | 渲染层类型镜像同步 | 改 |
| `electron/services/scheduler.ts` | 零点 + 启动挂正文清理 | 改 |
| `src/modules/feed/FeedModule.tsx` | 收件箱/已归档切换、窗口状态、加载更早、空状态 | 改 |
| `src/modules/feed/feed.css` 文件尾 | `.feed-load-more` 样式 | 增 |
| `docs/project/左侧边栏/信息源/designs-specs.md` | §2/§3/§4/§6/§7 同步 | 改 |
| `docs/project/优化建议区.md` | 第 28 轮归档（含灵感泉条目） | 改 |
| `docs/log/260909.md` | 当日开发日志追加 | 改 |

---

### Task 1: shared/types.ts + feed.ts——FeedListView 类型与 listArticles 重构

**Files:**
- Modify: `src/shared/types.ts:309-322`（ArticleSummary 定义之后）
- Modify: `electron/services/feed.ts:248-275`（listArticles）

- [ ] **Step 1: shared/types.ts 增两个类型**

在 `ArticleSummary` 接口（`:309-322`）之后新增：

```ts
/** 文章列表视图（优化建议区第28轮）：unread=收件箱（只显未读，读完即消失）；archive=已归档（已读按时间翻） */
export type FeedView = 'unread' | 'archive'

/** 文章列表返回体（优化建议区第28轮）：窗口内轻量行 + 窗口外剩余计数（「加载更早」按钮展示） */
export interface FeedListView {
  articles: ArticleSummary[]
  /** 同条件（视图 + 源筛选）去时间窗口的 COUNT——窗口外还有多少篇可加载 */
  remaining: number
}
```

- [ ] **Step 2: 重写 listArticles**

`electron/services/feed.ts:248-275` 的 `listArticles` 整体替换为：

```ts
/** 文章列表（优化建议区第28轮）：view 分未读收件箱/已归档，30 天窗口起按 sinceDays 加载；
 *  remaining = 同条件窗口外计数，「加载更早」按钮展示。轻量行 + 剥标签预览只算窗口内文章。 */
export function listArticles(feedId: number | null, view: FeedView, sinceDays: number): FeedListView {
  const d = getDb()
  const readClause = view === 'unread' ? 'read_at IS NULL' : 'read_at IS NOT NULL'
  const sinceIso = new Date(Date.now() - sinceDays * 86_400_000).toISOString()
  const feedClause = feedId == null ? '' : ' AND feed_id = ?'
  const params: unknown[] = [sinceIso, ...(feedId == null ? [] : [feedId])]
  const rows = d
    .prepare(
      `SELECT * FROM articles WHERE ${readClause} AND COALESCE(published_at, fetched_at) >= ?${feedClause}
       ORDER BY COALESCE(published_at, fetched_at) DESC, id DESC`
    )
    .all(...params) as unknown as ArticleRecord[]
  const remainingRow = d
    .prepare(
      `SELECT COUNT(*) AS c FROM articles WHERE ${readClause} AND COALESCE(published_at, fetched_at) < ?${feedClause}`
    )
    .get(...params) as { c: number }
  return {
    articles: rows.map((r) => ({
      id: r.id,
      feed_id: r.feed_id,
      title: r.title,
      url: r.url,
      author: r.author,
      published_at: r.published_at,
      fetched_at: r.fetched_at,
      read_at: r.read_at,
      has_summary: !!r.summary_text,
      preview: stripTags(r.content_fetched_html || r.content_feed_html || '').slice(0, 120)
    })),
    remaining: remainingRow.c
  }
}
```

要点：feed.ts 顶部 import 行（`:9`）补 `FeedListView` 与 `FeedView`：

```ts
import type { ArticleRecord, ArticleSummary, FeedFetchResult, FeedListView, FeedRecord, FeedView } from '../../src/shared/types'
```

- [ ] **Step 3: feed.ts 文件尾新增清理函数**

在 `summarizeArticle` 之后（文件末尾）新增：

```ts
/** 老文章正文清理（优化建议区第28轮）：180 天前文章置空正文/总结（最占空间的字段），保留标题/链接/已读状态——
 *  归档区仍可翻到、可「去原文」；被点开时 extractArticle 懒抓兜底（正文空+有 url 会重新抓网页）。
 *  启动 + 每日零点调用（scheduler.ts），静默，返回清理条数。 */
export function cleanupOldArticleBodies(): number {
  const cutoff = new Date(Date.now() - 180 * 86_400_000).toISOString()
  const r = getDb()
    .prepare(
      `UPDATE articles SET content_feed_html = NULL, content_fetched_html = NULL, summary_text = NULL, summary_at = NULL
       WHERE COALESCE(published_at, fetched_at) < ?
         AND (content_feed_html IS NOT NULL OR content_fetched_html IS NOT NULL OR summary_text IS NOT NULL)`
    )
    .run(cutoff)
  return Number(r.changes)
}
```

- [ ] **Step 4: typecheck 验证**

Run: `npm run typecheck`
Expected: 主进程侧可能因 ipc.ts 未同步报 `listArticles` 参数错误——若报错先继续 Task 2 再统一验证（此步允许 FAIL，错误应仅指向 ipc.ts 的调用点）

- [ ] **Step 5: 提交检查点（开发者手动执行）**

建议信息：`信息源：listArticles 收件箱/时间窗口重构 + 180 天正文清理函数（第28轮）`

---

### Task 2: IPC 三件套同步

**Files:**
- Modify: `electron/ipc.ts:57-68,776`
- Modify: `electron/preload.ts:460-463`
- Modify: `src/renderer/api.d.ts:782-791`

- [ ] **Step 1: ipc.ts import 补类型 + handler 透传**

`:57-68` 的 import 块替换为：

```ts
import {
  listFeeds,
  fetchAllFeeds,
  probeFeed,
  addFeed,
  renameFeed,
  removeFeed,
  listArticles,
  openArticle,
  markAllRead,
  summarizeArticle,
  cleanupOldArticleBodies
} from './services/feed'
import type { FeedView } from '../src/shared/types'
```

（`cleanupOldArticleBodies` 此步先引入，Task 3 接线使用；若希望零未使用告警也可放到 Task 3 再加，二选一。）

`:776` 的 handler 替换为：

```ts
  ipcMain.handle(
    'articles:list',
    (_e, feedId: number | null, view: FeedView, sinceDays: number) => listArticles(feedId, view, sinceDays)
  )
```

`markAllRead` handler **不改**——现 SQL 本就不带时间过滤，天然满足「作用于全部未读」。

- [ ] **Step 2: preload.ts 签名升级**

`:460-463` 替换为：

```ts
  articles: {
    /** 文章列表（feedId=null 全部；view=unread 收件箱/archive 已归档；sinceDays 时间窗口；返回轻量行+窗口外剩余数） */
    list: (
      feedId: number | null,
      view: import('../src/shared/types').FeedView,
      sinceDays: number
    ): Promise<import('../src/shared/types').FeedListView> =>
      ipcRenderer.invoke('articles:list', feedId, view, sinceDays),
```

（`open` / `markAllRead` / `summarize` 三行不动。）

- [ ] **Step 3: api.d.ts 镜像同步**

先在 api.d.ts 中 `ArticleSummary` 相关类型声明区（与 `FeedRecord` 同区，按文件内现有 import/声明方式）引入 `FeedView` / `FeedListView`：若该文件顶部有 `/// <reference>` 或直接从 types 导入则跟随现状追加；若类型是内联声明则内联复制上述两类型的等价声明。

`:782-791` 的 `articles` 块中 `list` 行替换为：

```ts
    /** 文章列表（feedId=null 全部；view=unread 收件箱/archive 已归档；sinceDays 时间窗口；返回轻量行+窗口外剩余数） */
    list(feedId: number | null, view: FeedView, sinceDays: number): Promise<FeedListView>
```

- [ ] **Step 4: typecheck 验证**

Run: `npm run typecheck`
Expected: 渲染层 FeedModule 尚未传新参，可能报调用参数错误——错误应仅指向 FeedModule.tsx 的 `articles.list` 调用点（Task 4 消除）；主进程与 preload 侧应 PASS

- [ ] **Step 5: 提交检查点（开发者手动执行）**

建议信息：`信息源：IPC 三件套透传 view/sinceDays（第28轮）`

---

### Task 3: scheduler.ts——挂接正文清理

**Files:**
- Modify: `electron/services/scheduler.ts:1-6,71-95`

- [ ] **Step 1: import 行补清理函数**

`:6` 后追加一行 import：

```ts
import { cleanupOldArticleBodies } from './feed'
```

- [ ] **Step 2: 零点清理块并入正文清理**

`:71-83` 的 `scheduleMidnightCleanup` 内 try 块替换为：

```ts
    try {
      const n = cleanupExpired()
      if (n > 0) console.log(`[scheduler] 回收站零点清理 ${n} 条`)
      const a = cleanupOldArticleBodies()
      if (a > 0) console.log(`[scheduler] 信息源老文章正文清理 ${a} 条`)
    } catch (e) {
```

- [ ] **Step 3: 启动清理并入**

`:86-95` 的 `startSchedulers` 首个 try 块替换为：

```ts
  try {
    const n = cleanupExpired()
    if (n > 0) console.log(`[scheduler] 启动清理回收站 ${n} 条`)
    const a = cleanupOldArticleBodies()
    if (a > 0) console.log(`[scheduler] 启动清理信息源老文章正文 ${a} 条`)
  } catch (e) {
```

（`:1` 文件头注释同步改为：`// 定时任务（主进程）：格言定时生成 + 回收站每日零点清理 + 信息源老文章正文清理`）

- [ ] **Step 4: typecheck 验证**

Run: `npm run typecheck`
Expected: 主进程侧 PASS（Task 1 引入的未使用 import 就此消解）

- [ ] **Step 5: 提交检查点（开发者手动执行）**

建议信息：`信息源：定时清理 180 天前文章正文（第28轮）`

---

### Task 4: FeedModule.tsx——收件箱切换 + 窗口加载 + 空状态

**Files:**
- Modify: `src/modules/feed/FeedModule.tsx`（状态区 `:20-33`、源行点击 `:174-186`、头部 `:145-168`、列表 `:220-239`）

- [ ] **Step 1: 状态与 load 升级**

`:20-33` 状态区（`activeFeed` 声明之后）追加：

```tsx
  // 收件箱模式（优化建议区第28轮）：unread=只显未读读完即消失；archive=已读按时间翻；30 天窗口，「加载更早」前移
  const [view, setView] = useState<FeedView>('unread')
  const [sinceDays, setSinceDays] = useState(30)
  const [remaining, setRemaining] = useState(0)
```

（文件顶部 type import 处补 `FeedView`。）

`load` 回调替换为：

```tsx
  const load = useCallback(async () => {
    const [fs, lv] = await Promise.all([
      window.api.feeds.list(),
      window.api.articles.list(activeFeed, view, sinceDays)
    ])
    setFeeds(fs)
    setArticles(lv.articles)
    setRemaining(lv.remaining)
  }, [activeFeed, view, sinceDays])
```

- [ ] **Step 2: 切视图/切源重置窗口**

在 `load` 之后追加两个 handler（源行/全部行的 `onClick` 改调 `switchFeed`）：

```tsx
  /** 切视图/切源窗口重置 30 天（优化建议区第28轮） */
  const switchView = (v: FeedView): void => {
    if (view === v) return
    setView(v)
    setSinceDays(30)
  }
  const switchFeed = (id: number | null): void => {
    if (activeFeed === id) return
    setActiveFeed(id)
    setSinceDays(30)
  }
```

`:175`「全部文章」行 `onClick={() => setActiveFeed(null)}` 改 `onClick={() => switchFeed(null)}`；`:185` 源行 `onClick={() => setActiveFeed(f.id)}` 改 `onClick={() => switchFeed(f.id)}`。

- [ ] **Step 3: 头部加「收件箱 | 已归档」切换（复用全局 recycle-tab 样式）**

`:148-152` 头部标题区替换为：

```tsx
        <div className="module-title">信息源</div>
        <div className="recycle-tabs" style={{ marginLeft: 14 }}>
          <button
            className={`recycle-tab${view === 'unread' ? ' active' : ''}`}
            onClick={() => switchView('unread')}
          >
            收件箱
          </button>
          <button
            className={`recycle-tab${view === 'archive' ? ' active' : ''}`}
            onClick={() => switchView('archive')}
          >
            已归档
          </button>
        </div>
        <div className="module-sub">{feeds.length} 个源{totalUnread > 0 ? ` · ${totalUnread} 篇未读` : ''}</div>
```

（`recycle-tabs`/`recycle-tab` 为 App.css 全局类，万象库「百科|辩真」同款，零新样式。）

- [ ] **Step 4: 列表空状态分视图 + 底部「加载更早」**

`:222-238` 列表块替换为：

```tsx
          {articles.length === 0 && remaining === 0 ? (
            <div className="empty-state">
              <span className="material-symbols-outlined">rss_feed</span>
              {fetching
                ? '正在拉取…'
                : view === 'unread'
                  ? activeFeedRow
                    ? '该源没有未读文章'
                    : '没有未读文章，点「刷新」拉取新内容'
                  : activeFeedRow
                    ? '该源近 30 天没有已读文章'
                    : '近 30 天没有已读文章'}
            </div>
          ) : (
            <>
              {articles.map((a) => (
                <div key={a.id} className={`feed-art-row${a.read_at ? '' : ' unread'}`} onClick={() => void openArticle(a.id)}>
                  <div className="feed-art-title">{a.title}</div>
                  {a.preview && <div className="feed-art-preview">{a.preview}</div>}
                  <div className="feed-art-meta">
                    {activeFeed === null && <span>{feeds.find((f) => f.id === a.feed_id)?.title ?? ''}</span>}
                    <span>{relTime(a.published_at ?? a.fetched_at)}</span>
                  </div>
                </div>
              ))}
              {remaining > 0 && (
                <button className="feed-load-more" onClick={() => setSinceDays((s) => s + 30)}>
                  加载更早 30 天 · 还剩 {remaining} 篇
                </button>
              )}
            </>
          )}
```

注意：收件箱视图下 `a.read_at` 恒为 null（行必未读），`unread` 类名恒命中——保留写法与已归档视图共用行组件不改逻辑。

- [ ] **Step 5: typecheck 验证**

Run: `npm run typecheck`
Expected: 双侧 PASS（Task 2 遗留的 FeedModule 调用点错误就此消除）

- [ ] **Step 6: 提交检查点（开发者手动执行）**

建议信息：`信息源：收件箱/已归档切换 + 30 天窗口加载更早（第28轮）`

---

### Task 5: feed.css——加载更早按钮样式

**Files:**
- Modify: `src/modules/feed/feed.css`（文件尾追加）

- [ ] **Step 1: 追加样式**

文件尾追加：

```css
/* 加载更早（收件箱/已归档 30 天窗口外触达——优化建议区第28轮） */
.feed-load-more {
  margin: 6px auto 14px;
  padding: 8px 18px;
  border-radius: 16px;
  border: 1px dashed var(--color-border);
  background: transparent;
  color: var(--color-text-secondary);
  cursor: pointer;
  font-size: 0.85em;
}
.feed-load-more:hover {
  border-color: var(--color-primary);
  color: var(--color-primary-deep);
}
:root[data-theme='dark'] .feed-load-more:hover {
  color: #fff;
}
```

- [ ] **Step 2: 提交检查点（开发者手动执行）**

建议信息：`信息源：加载更早按钮样式（第28轮）`

---

### Task 6: specs 同步 + 优化建议区第 28 轮归档 + 开发日志

**Files:**
- Modify: `docs/project/左侧边栏/信息源/designs-specs.md`
- Modify: `docs/project/优化建议区.md`
- Modify: `docs/log/260909.md`

- [ ] **Step 1: designs-specs.md 五处同步**

1. 文件头引用行（`:3`）后追加一行：

```md
>
> 260909 增量注记（第28轮）：主列表改**收件箱模式**（只显未读、读完即消失；「已归档」视图翻已读）+ **30 天时间窗口**加载（窗口外「加载更早 30 天 · 还剩 N 篇」触达，切视图/切源窗口重置）+ **180 天老文章清正文保轻行**（启动+零点定时置空正文/总结，保留标题轻行可跳原文，点开懒抓兜底）。详见 `2026-09-09-信息源收件箱与时间窗口-design.md`。
```

2. §2 末尾（`:72` 之后）新增小节：

```md
### 2.4 收件箱查询与老数据清理（第28轮）

- `listArticles(feedId, view, sinceDays)`：`view='unread'`（收件箱，`read_at IS NULL`）| `'archive'`（已归档，`read_at IS NOT NULL`）；时间条件 `COALESCE(published_at, fetched_at) >= now - sinceDays 天`；返回 `FeedListView { articles, remaining }`，remaining 为同条件去时间窗口 COUNT。
- `cleanupOldArticleBodies()`：`UPDATE` 置空 180 天前文章的 `content_feed_html`/`content_fetched_html`/`summary_text`/`summary_at`（仅清理仍有内容者），保留标题/链接/已读轻行；挂 scheduler 启动 + 每日零点；老文章点开时 §2.2 懒抓兜底。
```

3. §3.1 文章列表行（`:80`）「摘要前 50 字」之后接续改为：`；顶部「收件箱 | 已归档」切换（recycle-tab 同款，第28轮）——收件箱只显未读、读完即消失，已归档显已读；默认 30 天窗口，底部「加载更早 30 天 · 还剩 N 篇」按钮连续前移，切视图/切源重置`
4. §4 表 `articles:list` 行（`:107`）替换为：

```md
| `articles:list` | `(feedId: number \| null, view: 'unread' \| 'archive', sinceDays: number) => Promise<FeedListView>` | null=全部；view 分收件箱/已归档；30 天窗口起 + remaining 窗口外计数 |
```

5. §6「明确不做」追加两条：`已读不进回收站（现口径）；无「标未读」回退操作`；§7 验收清单追加：`- [ ]  收件箱模式：读完消失、已归档可翻、加载更早触达窗口外、全部标已读清空收件箱（含窗口外未读）`

- [ ] **Step 2: 优化建议区第 28 轮归档**

`docs/project/优化建议区.md` 待完成区**删除**两条（灵感泉 + 信息源），归档区顶部（`### 第27轮` 之上）插入新小节：

````md
### 第28轮（260909）：灵感泉完整点子文档 + 信息源收件箱与时间窗口

- [X]  灵感泉生成的prompt还需要再优化（260909 完成：经 brainstorming 摸底——第18轮两阶段管线 + 第22轮规范 v2 后唯一病灶为 **summary 两句模板太薄**（「是什么+第一步」100 字装不下点子本身，卡片与 md 正文均无完整内容载体，看不出值不值得立项），标题/腔调/风向/配额均无恙不动。定稿方案 A「完整点子文档」——自评阶段升级：选中 5 条各产出 body 完整 md 文档（引子段 + ## 这是什么 / ## 核心机制 / ## 最小版本，300~400 字，白话具体禁文艺化压缩、「第一步」并入最小版本小节），md 正文即完整文档；发散 12 条粗点子与口味注入/避免清单/风向标不动；`InspirationIdea` 类型统一（body 可选）、`parseInspirationArray` 容错升级（title 必填 + summary/body 至少其一）；降级路径 pickDiverseFive 无 body 退单行 summary；卡片预览改取首段（首个空行前压平 120 字，不再混 ## 标记符）；存量 AI 条不迁移、零 DB/IPC 改动。设计文档 `docs/project/左侧边栏/灵感泉/2026-09-09-灵感泉完整点子文档-design.md`。typecheck 通过，待冒烟看 body 质量与批间多样性）
- [X]  信息源需要好好优化一下——比如说已读之后的归档机制，选择性加载机制（按时间来规定）（260909 完成：经 brainstorming 三项定稿——**收件箱模式**：主列表只显未读、读完即从列表消失，头部「收件箱 | 已归档」切换（recycle-tab 同款）翻已读，「全部标已读」本就不带时间过滤天然清空全部未读（不改动）；**30 天时间窗口**：`listArticles(feedId, view, sinceDays)` 重构返回 `{articles, remaining}`（同条件去窗口 COUNT），底部「加载更早 30 天 · 还剩 N 篇」连续前移、切视图/切源窗口重置 30——顺带治「进模块全量 SELECT + 逐条剥标签」性能病（预览只算窗口内）；**180 天清正文保轻行**：`cleanupOldArticleBodies()` 置空老文章正文 HTML/AI 总结、保留标题/链接/已读轻行，挂 scheduler 启动 + 零点，点开时 extractArticle 懒抓兜底真想看还能抓回；IPC 三件套透传新参；零 DB 迁移。设计文档 `docs/project/左侧边栏/信息源/2026-09-09-信息源收件箱与时间窗口-design.md`。typecheck 通过，待冒烟）
````

- [ ] **Step 3: 开发日志追加**

`docs/log/260909.md` 新增小节（标题下附创建时间行，bash `date '+%Y-%m-%d %H:%M'` 取实际时间）：

````md
## 信息源优化（第28轮之二）：收件箱模式 + 时间窗口 + 老数据瘦身

> 创建于 YYYY-MM-DD HH:MM

（记录：Task 1-5 概要、typecheck 结果、冒烟待办清单——读完消失/已归档翻阅/加载更早/全部标已读/源筛选两视图/清理日志观察）
````

- [ ] **Step 4: 提交检查点（开发者手动执行）**

建议信息：`信息源：specs 同步 + 第28轮归档 + 开发日志`

---

## 验证与冒烟（实施完成后）

1. `npm run typecheck` 双侧 PASS
2. `npm run dev` 冒烟：
   - 进信息源 → 默认收件箱只显未读；点开一篇文章 → 返回列表该行消失；「已归档」视图能翻到它
   - 源筛选（全部/单源）在两个视图下都生效
   - 制造窗口外数据（临时把 sinceDays 初值改 1 验证后改回，或等自然积累）→「加载更早 30 天 · 还剩 N 篇」出现、点击后窗口前移、连续点可触达全部
   - 「全部标已读」→ 收件箱清空（含窗口外未读），全部转入已归档
   - 主进程控制台观察启动/零点清理日志（有 180 天前老文章时显示「信息源老文章正文清理 N 条」）；清理后的老文章在已归档可翻到标题、「去原文」可跳、正文为空不报错

## Self-Review 记录

- **Spec 覆盖**：spec §1 收件箱模式（Task 4）/ §2 时间窗口（Task 1+4）/ §3 老数据瘦身（Task 1 Step 3 + Task 3）/ §4 不做的事（markAllRead 不改注记、明确不做条目进 specs §6）——全覆盖；「顺带治性能病」由窗口内才算 preview 落实
- **占位符扫描**：无 TBD/TODO；api.d.ts 的类型引入给了两种跟随现状的明确做法（非二义性「适当处理」）
- **类型一致性**：`FeedView`/`FeedListView` 在 Task 1 定义、Task 2 三件套与 Task 4 渲染层统一引用；`listArticles` 五处调用签名一致；`cleanupOldArticleBodies` Task 1 定义、Task 2 引入 import、Task 3 使用
- **跨计划依赖**：Task 6 Step 2 归档含灵感泉条目——若灵感泉计划未实施，该条保留在待完成区不写入归档小节（计划头部「执行顺序」已注明）
