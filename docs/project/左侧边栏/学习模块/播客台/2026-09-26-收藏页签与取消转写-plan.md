# 播客台收藏页签 + 取消转写 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 播客台新增收藏页签（收藏出流，信息源同口径）与转写取消入口（排队摘队/在跑 abort）。

**Architecture:** DB v61 加 `podcast_episodes.starred_at` 列；服务层四视图查询口径 + unread/清空收件箱排除收藏；`PodcastEpisodeView` 全链路启用并扩 `'starred'`；渲染层星标按钮 + 三忙碌态取消按钮。

**Tech Stack:** Electron 44 + React 19 + TypeScript + node:sqlite。

**设计文档:** `docs/project/左侧边栏/学习模块/播客台/2026-09-26-收藏页签与取消转写-design.md`

**项目纪律（覆盖 skill 默认）：**
- **AI 禁止执行任何 git 操作**——不含 commit 步骤，每任务以 `npm run typecheck` 收尾。
- 无测试框架，验证 = typecheck 双配置 + dev 桩实测/开发者冒烟。
- 并行会话热点文件（types.ts / api.d.ts / 优化建议区 / dev log）Edit 失配先重读。

---

### Task 1: DB v61 收藏列

**Files:**
- Modify: `electron/db/db.ts`（v60 块之后、迁移函数收尾之前）

- [ ] **Step 1.1: 追加 v61 块**（锚点 = 第 56 轮的 v60 块）

```ts
  if (version < 61) {
    // v61：播客台收藏页签（优化建议区第57轮，2026-09-26-收藏页签与取消转写-design.md §3.1）——
    // 单集加 starred_at 收藏列（信息源 favorited_at 同款）：收藏出流（收件箱不含、未读计数不计），
    // 收藏页签按收藏时间倒序；取消收藏按已读态回流。
    d.exec('ALTER TABLE podcast_episodes ADD COLUMN starred_at TEXT')
    d.exec('PRAGMA user_version = 61')
  }
```

- [ ] **Step 1.2: typecheck 验证**

Run: `npm run typecheck` — Expected: 零错误。

---

### Task 2: 类型定义（types.ts）

**Files:**
- Modify: `src/shared/types.ts:932-949`（PodcastEpisodeView + PodcastEpisodeSummary）

- [ ] **Step 2.1: 两处类型改**

```ts
/** 单集流视图（260925 收件箱制；260926 增收藏）：inbox=未读未收藏（默认），archived=已读未收藏，all=全部含收藏，starred=收藏（按收藏时间倒序） */
export type PodcastEpisodeView = 'inbox' | 'archived' | 'all' | 'starred'
```

`PodcastEpisodeSummary` 内 `read_at: string | null` 之后追加：

```ts
  /** 收藏时间（NULL=未收藏；收藏出流——收件箱不含、未读计数不计） */
  starred_at: string | null
```

注意：类型字段名用 `starred_at`（原始列名透传），渲染层判断布尔用 `ep.starred_at != null`（不加单独 boolean 字段，与 `read_at` 同风格）。

- [ ] **Step 2.2: typecheck 验证**

Run: `npm run typecheck`
Expected: **报错若干**——podcast.ts 的 mapEpisodeSummary 未返回 starred_at（下一任务修）；这是预期中间态，Task 3 完成后应全绿。若还有其他文件报错一并核对。

---

### Task 3: 主进程 podcast.ts 服务层

**Files:**
- Modify: `electron/services/podcast.ts`

- [ ] **Step 3.1: import 扩展**

顶部 `import { beginJob, endJob } from '../ai/jobs'` 改为：

```ts
import { beginJob, cancelJob, endJob } from '../ai/jobs'
```

types import 块（`import type { ... } from '../../src/shared/types'`）追加 `PodcastEpisodeView`：

```ts
import type {
  AsrConfig,
  ItunesPodcast,
  PodcastEpisodeDetail,
  PodcastEpisodeSummary,
  PodcastEpisodeView,
  PodcastFeed
} from '../../src/shared/types'
```

- [ ] **Step 3.2: listPodcastFeeds unread 子查询排除收藏**

```ts
      `SELECT f.*,
        (SELECT COUNT(*) FROM podcast_episodes e WHERE e.feed_id = f.id AND e.read_at IS NULL AND e.starred_at IS NULL) AS unread,
```

（untranscribed / rss_transcripts 两个子查询不动。）

- [ ] **Step 3.3: markAllEpisodesRead 排除收藏**

```ts
/** 收件箱一键清空：全部（或指定节目）未读置为已读 = 归档（已转写与否无关；收藏的集不动） */
export function markAllEpisodesRead(feedId: number | null): number {
  const r = getDb()
    .prepare('UPDATE podcast_episodes SET read_at = ? WHERE read_at IS NULL AND starred_at IS NULL AND (? IS NULL OR feed_id = ?)')
    .run(nowIso(), feedId, feedId)
  return Number(r.changes)
}
```

- [ ] **Step 3.4: mapEpisodeSummary 加 starred**

`read_at: (r.read_at as string | null) ?? null` 之后追加一行：

```ts
    starred_at: (r.starred_at as string | null) ?? null
```

- [ ] **Step 3.5: listPodcastEpisodes 四视图口径**（整体替换该函数）

```ts
/** 单集流（收件箱制 260925，收藏出流 260926）：view=inbox 未读且未收藏（默认首屏）/ archived 已读未收藏 /
 *  starred 收藏（按收藏时间倒序）/ all 全部含收藏。发布倒序，无日期退 fetched_at。
 *  归档/收藏只动视图分类，转写状态/文字稿不受影响 */
export function listPodcastEpisodes(feedId: number | null, view: PodcastEpisodeView = 'all'): PodcastEpisodeSummary[] {
  const cond =
    view === 'inbox'
      ? 'AND e.read_at IS NULL AND e.starred_at IS NULL'
      : view === 'archived'
        ? 'AND e.read_at IS NOT NULL AND e.starred_at IS NULL'
        : view === 'starred'
          ? 'AND e.starred_at IS NOT NULL'
          : ''
  const order =
    view === 'starred'
      ? 'e.starred_at DESC, e.id DESC'
      : 'COALESCE(e.published_at, e.fetched_at) DESC, e.id DESC'
  const rows = getDb()
    .prepare(
      `SELECT e.id, e.feed_id, f.title AS feed_title, e.title, e.published_at, e.duration_sec,
              e.transcript_state, e.transcript_url, e.transcript_error, e.read_at, e.starred_at, e.fetched_at
       FROM podcast_episodes e JOIN podcast_feeds f ON f.id = e.feed_id
       WHERE (? IS NULL OR e.feed_id = ?) ${cond}
       ORDER BY ${order}`
    )
    .all(feedId, feedId) as unknown as FeedRow[]
  return rows.map(mapEpisodeSummary)
}
```

- [ ] **Step 3.6: setEpisodeStarred + cancelTranscribe**（在 `setEpisodeRead` 之后、`deleteEpisode` 之前插入）

```ts
/** 收藏/取消收藏（收藏出流：收件箱不含、未读计数不计；取消收藏按已读态回流） */
export function setEpisodeStarred(id: number, starred: boolean): void {
  getDb().prepare('UPDATE podcast_episodes SET starred_at = ? WHERE id = ?').run(starred ? nowIso() : null, id)
}

/** 取消转写（第57轮）：在跑的走全局 jobId 取消（processTranscribe catch 置回 none 可重试）；
 *  排队的摘内存队列并置回 none（防徽章卡 queued）；异常卡住态顺带自愈。
 *  polishing 不提供取消（转写成果已落库，取消反而丢） */
export function cancelTranscribe(episodeId: number): void {
  const active = activeTranscribes.get(episodeId)
  if (active) {
    cancelJob(active.jobId)
    return
  }
  dequeueTranscribe(episodeId)
  getDb()
    .prepare("UPDATE podcast_episodes SET transcript_state = 'none' WHERE id = ? AND transcript_state = 'queued'")
    .run(episodeId)
  notifyTaskChanged()
}
```

- [ ] **Step 3.7: typecheck 验证**

Run: `npm run typecheck`
Expected: 仅剩 preload/api.d.ts/渲染层可能的 view 类型/新通道报错（Task 4/5 修），podcast.ts 自身零错。

---

### Task 4: IPC 三件套

**Files:**
- Modify: `electron/ipc.ts`（podcast 段约 2431-2474）
- Modify: `electron/preload.ts`（podcast 段 1263-1316）
- Modify: `src/renderer/api.d.ts`（podcast 段 2003-2029+）

- [ ] **Step 4.1: ipc.ts**

`podcast:episodes` handler 签名内联联合换类型（`(_e, feedId: number | null, view?: 'inbox' | 'archived' | 'all')` → `(_e, feedId: number | null, view?: PodcastEpisodeView)`）；在 `podcast:transcribe` handler 之后追加两通道：

```ts
  ipcMain.handle('podcast:episodeStar', (_e, id: number, starred: boolean) => {
    setEpisodeStarred(id, starred)
    return true
  })
  ipcMain.handle('podcast:transcribeCancel', (_e, id: number) => {
    cancelTranscribe(id)
    return true
  })
```

import 核对：podcast 服务 import 列表追加 `setEpisodeStarred, cancelTranscribe`；`PodcastEpisodeView` 若未在 types import 中则追加（ipc.ts 现有 `from '../src/shared/types'` import 块内加）。若 ipc.ts 的 episodes handler 原签名未带 view 参数则补上（以现状为准重读锚点）。

- [ ] **Step 4.2: preload.ts**（podcast 段，episodes 签名换类型 + transcribe 之后插两行）

```ts
    /** 单集流（feedId=null 全部订阅；view 四视图，缺省 all） */
    episodes: (
      feedId: number | null,
      view?: import('../src/shared/types').PodcastEpisodeView
    ): Promise<import('../src/shared/types').PodcastEpisodeSummary[]> =>
      ipcRenderer.invoke('podcast:episodes', feedId, view),
```

transcribe 行之后追加：

```ts
    /** 收藏/取消收藏（收藏出流：收件箱不含、未读计数不计） */
    episodeStar: (id: number, starred: boolean): Promise<boolean> =>
      ipcRenderer.invoke('podcast:episodeStar', id, starred),
    /** 取消转写（排队摘队/在跑 abort，回 none 可重试） */
    transcribeCancel: (id: number): Promise<boolean> => ipcRenderer.invoke('podcast:transcribeCancel', id),
```

- [ ] **Step 4.3: api.d.ts**（podcast 段同款）

`episodes` 签名改：

```ts
    /** 单集流（feedId=null 全部订阅；view=inbox 未读收件箱 / archived 已读归档 / all 全部 / starred 收藏，缺省 all） */
    episodes(feedId: number | null, view?: import('../shared/types').PodcastEpisodeView): Promise<import('../shared/types').PodcastEpisodeSummary[]>
```

`transcribe` 声明之后追加：

```ts
    /** 收藏/取消收藏（收藏出流） */
    episodeStar(id: number, starred: boolean): Promise<boolean>
    /** 取消转写（排队摘队/在跑 abort，回 none 可重试） */
    transcribeCancel(id: number): Promise<boolean>
```

- [ ] **Step 4.4: typecheck 验证**

Run: `npm run typecheck`
Expected: 仅渲染层 PodcastModule 的 view state 内联联合不兼容（赋 'starred' 前不会报——此步可能全绿也可能渲染层残留，Task 5 修）。

---

### Task 5: 渲染层 PodcastModule.tsx

**Files:**
- Modify: `src/modules/podcast/PodcastModule.tsx`

- [ ] **Step 5.1: view state 换类型**

`const [view, setView] = useState<'inbox' | 'archived' | 'all'>('inbox')` 改：

```tsx
  const [view, setView] = useState<PodcastEpisodeView>('inbox')
```

types import 行追加 `PodcastEpisodeView`：

```tsx
import type { PodcastEpisodeSummary, PodcastEpisodeView, PodcastFeed } from '../../shared/types'
```

- [ ] **Step 5.2: 视图 chips 追加「收藏」**（「全部」chip 之后）

```tsx
              <button
                className={`pc-filter-chip${view === 'starred' ? ' active' : ''}`}
                onClick={() => setView('starred')}
                title="收藏的单集（收件箱不再显示，未读计数不计）"
              >
                收藏
              </button>
```

- [ ] **Step 5.3: toggleStar / cancelTranscribe 处理函数**（`toggleRead` 之后插入）

```tsx
  const toggleStar = async (ep: PodcastEpisodeSummary): Promise<void> => {
    try {
      await window.api.podcast.episodeStar(ep.id, ep.starred_at == null)
      // 收藏出流：按当前视图重取（inbox 收藏 = 出流消失；starred 取消收藏 = 行消失）
      await loadEpisodes(feedFilter, view)
      void loadFeeds()
    } catch (e) {
      toast(`操作失败：${(e as Error).message}`)
    }
  }

  const cancelTranscribe = async (ep: PodcastEpisodeSummary): Promise<void> => {
    try {
      await window.api.podcast.transcribeCancel(ep.id)
      setEpisodes((arr) =>
        arr.map((e) => (e.id === ep.id ? { ...e, transcript_state: 'none' as const, transcript_error: null } : e))
      )
      toast('已取消转写，可重新触发')
    } catch (e) {
      toast(`操作失败：${(e as Error).message}`)
    }
  }
```

- [ ] **Step 5.4: 行操作区**（星标按钮放 actions 首位；取消按钮在转写按钮之后、删除之前）

行 actions 区改为（星标在「标已读」按钮之前；取消按钮插在转写 speech_to_text 按钮的条件块之后）：

```tsx
                <div className="pc-ep-actions" onClick={(e) => e.stopPropagation()}>
                  <button
                    className="icon-btn"
                    title={ep.starred_at ? '取消收藏（按已读状态回流）' : '收藏（移入收藏页签，收件箱不再显示）'}
                    style={ep.starred_at ? { color: 'var(--color-primary)', fontVariationSettings: "'FILL' 1" } : undefined}
                    onClick={() => void toggleStar(ep)}
                  >
                    <span className="material-symbols-outlined">star</span>
                  </button>
                  <button
                    className="icon-btn"
                    title={ep.read_at ? '标未读' : '标已读'}
                    onClick={() => void toggleRead(ep)}
                  >
                    <span className="material-symbols-outlined">{ep.read_at ? 'mark_email_unread' : 'mark_email_read'}</span>
                  </button>
                  {(ep.transcript_state === 'none' || ep.transcript_state === 'failed') && (
                    <button className="icon-btn" title="转写本集" onClick={() => void openEpisode(ep)}>
                      <span className="material-symbols-outlined">speech_to_text</span>
                    </button>
                  )}
                  {(ep.transcript_state === 'queued' ||
                    ep.transcript_state === 'downloading' ||
                    ep.transcript_state === 'transcribing') && (
                    <button
                      className="icon-btn"
                      title="取消转写（回到待转写，可重新触发）"
                      onClick={() => void cancelTranscribe(ep)}
                    >
                      <span className="material-symbols-outlined">stop_circle</span>
                    </button>
                  )}
                  <button className="icon-btn danger" title="删除单集" onClick={() => setDelEpTarget(ep)}>
                    <span className="material-symbols-outlined">delete</span>
                  </button>
                </div>
```

- [ ] **Step 5.5: 空态文案加 starred 分支**

空态嵌套三元中 `view === 'archived'` 分支之后插入：

```tsx
                    : view === 'starred'
                      ? '还没有收藏的单集——点单集行内的星标收藏'
```

（三元链最终形态：feeds 空 → inbox 空 → archived 空 → starred 空 → feedFilter 分支。）

- [ ] **Step 5.6: typecheck 验证**

Run: `npm run typecheck`
Expected: 双配置全绿。

---

### Task 6: dev 桩实测

- [ ] **Step 6.1: 桩数据验证**（dev server 5183 已在跑则直接用；Chrome initScript 注入桩）

桩的 `feedsList` 返回含 `unread` 数（3/12）即可；`episodes` 返回 3 行覆盖三态：

```js
episodes: async (feedId, view) => {
  const rows = [
    { id: 1, feed_id: 1, feed_title: '示例节目A', title: '未读集（无星）', published_at: '2026-09-25T10:00:00Z', duration_sec: 3600, transcript_state: 'done', has_rss_transcript: true, transcript_error: null, read_at: null, starred_at: null },
    { id: 2, feed_id: 1, feed_title: '示例节目A', title: '转写排队中的集', published_at: '2026-09-24T10:00:00Z', duration_sec: 1800, transcript_state: 'queued', has_rss_transcript: false, transcript_error: null, read_at: null, starred_at: null },
    { id: 3, feed_id: 2, feed_title: '示例节目B', title: '已收藏的集', published_at: '2026-09-20T10:00:00Z', duration_sec: 7200, transcript_state: 'transcribing', has_rss_transcript: false, transcript_error: null, read_at: '2026-09-21T00:00:00Z', starred_at: '2026-09-22T08:00:00Z' }
  ]
  if (view === 'inbox') return rows.filter((r) => r.read_at == null && r.starred_at == null)
  if (view === 'archived') return rows.filter((r) => r.read_at != null && r.starred_at == null)
  if (view === 'starred') return rows.filter((r) => r.starred_at != null)
  return rows
}
episodeStar: async () => {},
transcribeCancel: async () => {}
```

验证点：
1. 四 chips（收件箱 15｜已归档｜全部｜收藏）切活、收藏视图只显已收藏行、星标行 FILL 填充主题色；
2. queued/transcribing 行出 `stop_circle` 取消钮，done 行无；点击取消 → 徽章回退 + toast；
3. 收件箱 chips 计数、清空收件箱按钮位置回归（上轮零位移不破）；
4. 双主题目检。

- [ ] **Step 6.2: 开发者冒烟**（重启 dev 触发 v61）

按设计 §五冒烟清单全项（真实库：收藏出流/计数/清空排除/取消转写回退/重触发/AI 面板取消回归/删除墓碑联动）。

---

### Task 7: 文档同步

**Files:**
- Modify: `docs/project/优化建议区.md`
- Modify: `docs/log/260926.md`
- Check: `README.md`

- [ ] **Step 7.1: 优化建议区**——待完成区两条新建议保留不动（副本库目录/其他，非本轮）；归档区顶部（第 56 轮之上）插入第 57 轮小节，含原始描述 + 完成说明；**并订正第 56 轮说明中的顺延注记**：`顺延 v61` 改 `顺延 v62`（v61 已被本轮收藏列占用）。
- [ ] **Step 7.2: dev log 260926**——播客台第 56 轮小节的「⚠ DB 版本号互备」段同步订正 v62；新增第 57 轮小节（`> 创建于` 用 `date "+%Y-%m-%d %H:%M"` 实际时间）。
- [ ] **Step 7.3: README 核对**——grep 播客台描述，功能面变化（新增收藏页签）若 README 模块表未细化到页签级则不动；有出入才更新。

---

## Self-Review 记录

- **Spec 覆盖**：design §3.1 迁移/四视图/联动口径/星标/UI → Task 1-5；§3.2 cancelTranscribe 三分支/UI/费用口径 → Task 3.6 + 5.4；§3.3 版本互备订正 → Task 7；冒烟 §五 → Task 6。
- **占位符扫描**：无 TBD/TODO；Task 4 ipc.ts import 以现状重读为准属锚点核对指令非占位。
- **类型一致性**：`PodcastEpisodeView`（types.ts 定义 → podcast.ts/ipc.ts/preload/api.d.ts/PodcastModule 统一 import）；`starred_at` 字段名全链一致（类型/映射/SQL/渲染判断 `ep.starred_at`）；`episodeStar`/`transcribeCancel` 通道名三件套一致。
