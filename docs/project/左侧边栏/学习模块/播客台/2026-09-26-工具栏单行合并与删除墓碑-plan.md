# 播客台工具栏单行合并 + 删除单集复活修复 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复「删除单集后切模块复活甚至自动入转写」bug（墓碑表 DB v60），并把单集流双行工具栏合并为单行（视图 chips 钉位零位移 + 订阅筛选收束为下拉）。

**Architecture:** 主进程墓碑表 `podcast_deleted_eps(feed_id, guid)` 三处接线（删单集写墓碑 / 拉源跳过 / 退订清墓碑）；渲染层 PodcastModule 单行工具栏 + ActionMenu 下拉筛选，纯渲染层改动零 IPC 新增。

**Tech Stack:** Electron 44 + React 19 + TypeScript + node:sqlite（better 无框架直用）；构建 electron-vite。

**设计文档:** `docs/project/左侧边栏/学习模块/播客台/2026-09-26-工具栏单行合并与删除墓碑-design.md`

**项目纪律（覆盖 skill 默认）：**
- **AI 禁止执行任何 git 操作**——所有 commit 由开发者完成，本计划不含 commit 步骤；每任务以 `npm run typecheck` 收尾验证。
- 本项目无测试框架，验证 = typecheck 双配置（electron + renderer 两 tsconfig）+ dev 实测/冒烟。
- 文档同步（优化建议区归档、dev log、README 核对）在 Task 6。

---

### Task 1: DB v60 墓碑表迁移

**Files:**
- Modify: `electron/db/db.ts`（v59 块之后、迁移函数收尾 `}` 之前，约 1598 行处）

- [ ] **Step 1.1: 在 v59 块后追加 v60 块**

定位锚点（v59 块，其后即函数收尾）：

```ts
  if (version < 59) {
    // v59：副本库 v2.1（260925 开发者指令）——「未读」只属于待读区：收藏库存量中的
    // unread（含 DIY 测试生成物）全部划入待读区；DIY 生成自此直接落 pool（读毕处置与其他
    // 库存一致：存档 / 丢弃）。收藏库页签只剩 进行中 + 已读人生 两区。
    d.prepare("UPDATE copies SET state = 'pool' WHERE state = 'unread' AND deleted_at IS NULL").run()
    d.exec('PRAGMA user_version = 59')
  }
}
```

在 `}` （函数收尾）之前插入：

```ts

  if (version < 60) {
    // v60：播客台删除墓碑（优化建议区第56轮，2026-09-26-工具栏单行合并与删除墓碑-design.md §3.1）——
    // 手动删除的单集按 (feed_id, guid) 留墓碑，拉源入库前跳过，根治「删除后切模块触发
    // fetchAll 复活（自动转写节目还会误入转写队列）」；退订时随删本 feed 墓碑（重订全新开始）。
    d.exec(`CREATE TABLE podcast_deleted_eps (
      feed_id INTEGER NOT NULL,
      guid TEXT NOT NULL,
      deleted_at TEXT NOT NULL,
      PRIMARY KEY (feed_id, guid)
    )`)
    d.exec('PRAGMA user_version = 60')
  }
```

- [ ] **Step 1.2: typecheck 验证**

Run: `npm run typecheck`
Expected: 两个 tsconfig 均无错误（迁移是纯字符串 DDL，此处主要验证无语法/类型破坏）。

---

### Task 2: podcast.ts 墓碑三处接线

**Files:**
- Modify: `electron/services/podcast.ts`（`deleteEpisode` / `fetchPodcastFeed` / `deletePodcastFeed` 三函数）

- [ ] **Step 2.1: `deleteEpisode` 写墓碑**

替换现有函数（原注释「删单集（硬删，不入回收站；在队列中的顺带摘除）」一并更新）：

```ts
/** 删单集（硬删，不入回收站；在队列中的顺带摘除；写墓碑防拉源复活） */
export function deleteEpisode(id: number): void {
  const d = getDb()
  const row = d.prepare('SELECT feed_id, guid FROM podcast_episodes WHERE id = ?').get(id) as
    | { feed_id: number; guid: string }
    | undefined
  dequeueTranscribe(id)
  if (row) {
    d.prepare('INSERT OR REPLACE INTO podcast_deleted_eps (feed_id, guid, deleted_at) VALUES (?, ?, ?)').run(
      row.feed_id,
      row.guid,
      nowIso()
    )
  }
  d.prepare('DELETE FROM podcast_episodes WHERE id = ?').run(id)
}
```

- [ ] **Step 2.2: `fetchPodcastFeed` 跳过墓碑**

在 `fetchPodcastFeed` 内、`const ins = d.prepare(`（INSERT OR IGNORE 语句）之前插入墓碑集合加载：

```ts
    // 删除墓碑：用户删过的 (feed_id, guid) 不再回库（防 fetchAll 复活与自动转写误入队）
    const tombstones = new Set(
      (
        d.prepare('SELECT guid FROM podcast_deleted_eps WHERE feed_id = ?').all(feedId) as unknown as {
          guid: string
        }[]
      ).map((r) => r.guid)
    )
```

入库循环 `for (const it of items) {` 的**第一行**加：

```ts
      if (tombstones.has(it.guid)) continue
```

- [ ] **Step 2.3: `deletePodcastFeed` 退订清墓碑**

在现有两条 DELETE 之后追加一条（函数最终形态）：

```ts
/** 退订连删该节目全部单集与文字稿（二次确认在渲染层；不入回收站；随删墓碑——重订全新开始） */
export function deletePodcastFeed(id: number): void {
  const d = getDb()
  const eps = d.prepare('SELECT id FROM podcast_episodes WHERE feed_id = ?').all(id) as { id: number }[]
  for (const e of eps) dequeueTranscribe(e.id)
  d.prepare('DELETE FROM podcast_episodes WHERE feed_id = ?').run(id)
  d.prepare('DELETE FROM podcast_feeds WHERE id = ?').run(id)
  d.prepare('DELETE FROM podcast_deleted_eps WHERE feed_id = ?').run(id)
}
```

- [ ] **Step 2.4: typecheck 验证**

Run: `npm run typecheck`
Expected: 两配置零错误。

---

### Task 3: PodcastModule.tsx 单行工具栏

**Files:**
- Modify: `src/modules/podcast/PodcastModule.tsx`

- [ ] **Step 3.1: 引入 ActionMenu**

在现有 import 区（`ConfirmDialog` import 附近）加：

```tsx
import ActionMenu from '../../components/ActionMenu'
```

- [ ] **Step 3.2: 新增下拉锚定 state**

在 `const [asrConfigOpen, setAsrConfigOpen] = useState(false)` 之后加：

```tsx
  // 订阅筛选下拉锚定（ActionMenu anchorEl；点项时 ActionMenu 先 onClose 再 onClick，自动收起）
  const [filterMenuAnchor, setFilterMenuAnchor] = useState<HTMLElement | null>(null)
```

- [ ] **Step 3.3: 两行工具栏合并为单行**

将 `{tab === 'episodes' ? (` 分支内的 `.pc-toolbar` 块（刷新/添加订阅/订阅 chips）与 `.pc-viewbar` 块（视图 chips/清空收件箱）**整体替换**为：

```tsx
          {/* 单行工具栏：操作按钮 + 视图 chips（钉位零位移）+ 清空收件箱 + 订阅筛选下拉（右对齐收束） */}
          <div className="pc-toolbar">
            <button className="btn" onClick={() => void refresh()} disabled={refreshing} title="拉取全部订阅的新集">
              <span className="material-symbols-outlined">{refreshing ? 'progress_activity spin' : 'refresh'}</span>
              刷新
            </button>
            <button className="btn" onClick={() => setAddOpen(true)}>
              <span className="material-symbols-outlined">add</span>添加订阅
            </button>
            <div className="pc-views">
              <button
                className={`pc-filter-chip${view === 'inbox' ? ' active' : ''}`}
                onClick={() => setView('inbox')}
                title="未读单集（标已读即归档出流）"
              >
                收件箱{inboxTotal > 0 ? ` ${inboxTotal}` : ''}
              </button>
              <button
                className={`pc-filter-chip${view === 'archived' ? ' active' : ''}`}
                onClick={() => setView('archived')}
              >
                已归档
              </button>
              <button className={`pc-filter-chip${view === 'all' ? ' active' : ''}`} onClick={() => setView('all')}>
                全部
              </button>
              {view === 'inbox' && inboxTotal > 0 && (
                <button
                  className="btn"
                  onClick={() => void doMarkAllRead()}
                  title="全部标已读（归档到「已归档」，可随时「再看看」恢复）"
                >
                  <span className="material-symbols-outlined">done_all</span>
                  清空收件箱
                </button>
              )}
            </div>
            <button
              className={`pc-filter-chip pc-feed-select${feedFilter != null ? ' active' : ''}`}
              onClick={(e) => setFilterMenuAnchor(e.currentTarget)}
              title="按节目筛选单集流"
            >
              <span className="material-symbols-outlined">filter_list</span>
              <span className="pc-feed-select-label">
                {feedFilter == null ? '全部节目' : (filterFeed?.title ?? '全部节目')}
              </span>
              <span className="material-symbols-outlined">expand_more</span>
            </button>
          </div>

          {filterMenuAnchor && (
            <ActionMenu
              anchorEl={filterMenuAnchor}
              onClose={() => setFilterMenuAnchor(null)}
              items={[
                {
                  key: 'all',
                  icon: feedFilter == null ? 'check' : undefined,
                  label: '全部节目',
                  onClick: () => setFeedFilter(null)
                },
                ...feeds.map((f) => ({
                  key: String(f.id),
                  icon: feedFilter === f.id ? 'check' : undefined,
                  label: f.unread > 0 ? `${f.title}（${f.unread} 未读）` : f.title,
                  onClick: () => setFeedFilter(f.id)
                }))
              ]}
            />
          )}
```

注意：原两块中的 `pc-filter` chips 行（全部 + 各节目平铺）整体删除，不再有平铺形态；`filterFeed`/`inboxTotal`/`doMarkAllRead` 均为既有变量函数，零新增逻辑。

- [ ] **Step 3.4: typecheck 验证**

Run: `npm run typecheck`
Expected: 两配置零错误（ActionMenuItem.onClick 必填——每个 item 都已带 onClick）。

---

### Task 4: podcast.css 样式增删

**Files:**
- Modify: `src/modules/podcast/podcast.css`

- [ ] **Step 4.1: 删除死样式块**

删除 `.pc-viewbar` 整块（注释「收件箱视图行」+ 规则）与 `.pc-filter` 整块（`margin-left: auto` 的容器类，合并后无使用方）。删除前 grep 确认无他人使用：

Run: 在 `src/` 下全局搜索 `pc-viewbar` 与 `pc-filter`（注意排除 `pc-filter-chip`——该类保留继续用）
Expected: 仅 `PodcastModule.tsx`（改后应已无引用）与 `podcast.css` 命中。

- [ ] **Step 4.2: 更新 `.pc-toolbar` 注释并新增两类**

`.pc-toolbar` 规则本身不变（flex-wrap 保留作窄窗降级），注释更新；在其后新增：

```css
/* 单行工具栏：刷新 / 添加订阅 / 视图 chips（钉位零位移）/ 清空收件箱 / 订阅筛选下拉（右对齐收束） */
.pc-toolbar {
  display: flex;
  align-items: center;
  gap: 10px;
  flex-wrap: wrap;
  margin-bottom: 10px;
}
/* 视图 chips 组：紧跟固定宽度按钮之后，切视图零位移（不带 auto margin） */
.pc-views {
  display: flex;
  align-items: center;
  gap: 6px;
}
/* 订阅筛选下拉按钮：复用 chip 样式语言，右对齐；标题超长省略 */
.pc-feed-select {
  margin-left: auto;
  max-width: 260px;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}
.pc-feed-select .material-symbols-outlined {
  font-size: 1em;
  flex-shrink: 0;
}
.pc-feed-select-label {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

（`.pc-filter-chip` 块原样保留——max-width 160px 被上面 `.pc-feed-select` 的 260px 覆盖需注意 CSS 顺序：`.pc-feed-select` 规则必须写在 `.pc-filter-chip` **之后**才能同特异度覆盖 max-width。）

- [ ] **Step 4.3: typecheck 验证**

Run: `npm run typecheck`
Expected: 零错误。

---

### Task 5: dev 实测（布局桩验证 + 迁移触发）

- [ ] **Step 5.1: dev 环境布局桩验证**

`npm run dev`（renderer 固定 5183 端口）后，用 chrome-devtools 连 `http://localhost:5183`，经 `navigate_page` 的 `initScript` 在页面脚本前注入 window.api 桩（dev 普通 Chrome 无 preload）：

```js
window.api = {
  podcast: {
    feedsList: async () => [
      { id: 1, title: '示例节目A', artist: '', artwork_url: null, feed_url: '', auto_transcribe: true, created_at: '2026-09-26T00:00:00Z', last_fetched_at: null, fetch_error: null, unread: 3, untranscribed: 0, rss_transcripts: 0 },
      { id: 2, title: '示例节目B（名称特别长用于验证下拉按钮省略号与菜单滚动效果）', artist: '', artwork_url: null, feed_url: '', auto_transcribe: false, created_at: '2026-09-26T00:00:00Z', last_fetched_at: null, fetch_error: null, unread: 12, untranscribed: 0, rss_transcripts: 2 }
    ],
    episodes: async () => [],
    fetchAll: async () => 0,
    onTaskChanged: () => () => {}
  },
  settings: { get: async () => null }
}
```

左栏进「播客台」实测要点：
1. 单行工具栏渲染：刷新/添加订阅/三视图 chips/下拉按钮同一行，无第二行工具栏残留；
2. 收件箱↔已归档↔全部 反复切换：chips 与下拉按钮**零位移**；收件箱+有未读时「清空收件箱」出现/消失不挤动前项；
3. 下拉：点击弹 ActionMenu（右缘对齐按钮）、当前项打勾、节目项带未读数、长标题省略；选节目后按钮 active 态、再选「全部」恢复；
4. 窗口缩窄：下拉按钮换行不破版；
5. 双主题（浅粉/深蓝）切换目检无彩亮。

- [ ] **Step 5.2: 迁移触发（开发者侧）**

开发者重启 dev/应用触发 v60 迁移后冒烟（AI 无法替代真实库）：
1. **墓碑闭环**：删除单集 → 切走模块再切回（触发 fetchAll）→ 不复活；手动刷新多次 → 不复活；
2. **自动转写节目**：开「自动转写新集」的节目里删除在转写中的集 → 不复活、不入转写队列；
3. **退订重订**：退订 → 重新订阅 → 首拉最近 10 集全新开始；
4. **已归档**：「再看看」回收件箱回归；
5. DB `PRAGMA user_version` = 60。

---

### Task 6: 文档同步

**Files:**
- Modify: `docs/project/优化建议区.md`
- Modify: `docs/log/260926.md`（不存在则创建）
- Check: `README.md`

- [ ] **Step 6.1: 优化建议区归档**

待完成区勾掉该条并移除；归档区顶部新增（置于第 55 轮之上）：

```markdown
### 第56轮（260926）：播客台工具栏单行合并 + 删除单集复活修复

- [X]  播客台的导航栏组件需要重新设计一下……（保留原始描述全文）+ bug 描述（260926 完成：……完成说明，实施后据实撰写）
```

- [ ] **Step 6.2: dev log**

`docs/log/260926.md` 追加小节（当天文件已存在则追加、不存在则新建），小节标题下附真实时间行（`date "+%Y-%m-%d %H:%M"` 取实际时间，不估不编）：

```markdown
## 播客台：工具栏单行合并 + 删除单集复活修复（优化建议区第56轮）

> 创建于 YYYY-MM-DD HH:MM

（根因排查 / 墓碑 DB v60 / 单行工具栏 / 冒烟状态，据实撰写）
```

- [ ] **Step 6.3: README 核对**

grep `README.md` 中「播客台」描述：本轮不改模块定位与功能面（仅工具栏形态与 bug 修复），如描述无出入则不动；有出入才更新。

---

## Self-Review 记录

- **Spec 覆盖**：design §3.1 墓碑四点 → Task 1（表）+ Task 2（三函数）；§3.2 单行工具栏三要素 → Task 3 + Task 4；§五冒烟 → Task 5；归档纪律 → Task 6。无缺口。
- **占位符扫描**：无 TBD/TODO；Task 6 的「据实撰写」为实施后填实结果的既定流程（dev log 归档纪律），非占位。
- **类型一致性**：`podcast_deleted_eps(feed_id, guid, deleted_at)` Task 1/2 一致；`filterMenuAnchor`/`pc-views`/`pc-feed-select(-label)` Task 3/4 一致；ActionMenuItem 必填 onClick 均已带。
