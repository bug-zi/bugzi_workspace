# 信息源·阅读视图 Ctrl+滚轮字体缩放 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **项目约束（覆盖技能默认行为）：**
> - **禁止一切 git 操作**——本仓库所有 git 命令由开发者本人执行，计划不含 commit 步骤。
> - **项目无测试基建**，验证 = `npm run typecheck`（双 tsconfig）+ `npm run build` + 开发者运行时冒烟。
> - 设计正本：`docs/project/左侧边栏/学习模块/信息源/2026-09-16-Ctrl滚轮字体缩放-design.md`（实施前先读一遍）。
> - 文档收尾（新功能开发区归档、当日 devlog）由同日另一 plan `docs/project/左侧边栏/常驻模块/总导览/2026-09-16-每日挑战与热力图-plan.md` Task 10 统一处理，本计划不含。

**Goal:** 信息源阅读视图（ArticleView）支持 Ctrl+滚轮缩放文章字号（80%–200%、步进 10%、全局记一档），普通滚动与其他页面零影响。

**Architecture:** 纯渲染层改动——ArticleView 挂原生非 passive wheel 监听（阻断 Chromium 默认整页缩放），缩放值存 settings 键 `feed_reader_zoom`，应用于文章容器 fontSize；底部浮条显示当前百分比、点击复位。

**Tech Stack:** React 19 函数组件 + 原生 addEventListener（`{ passive: false }`）；ThemeProvider 的 `useAppSettings().setSetting` 落库；CSS 变量双主题。

---

### Task 1: SettingsKeys 新增 FeedZoom

**Files:**
- Modify: `src/shared/types.ts`（`SettingsKeys` 常量对象内，`FontWeight: 'font_weight',` 行后）

- [ ] **Step 1: 追加一行键**

```ts
  /** 信息源阅读视图 Ctrl+滚轮缩放百分比（80-200，全局一档；260916 新功能开发区） */
  FeedZoom: 'feed_reader_zoom',
```

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: 通过。

---

### Task 2: ArticleView 缩放逻辑 + 指示条 + 样式

**Files:**
- Modify: `src/modules/feed/ArticleView.tsx`
- Modify: `src/modules/feed/feed.css`（文件末尾追加）

- [ ] **Step 1: 替换文件头部 import 块（第 1-7 行区域）**

原：

```tsx
import { useCallback, useEffect, useState } from 'react'
import DOMPurify from 'dompurify'
import type { ArticleRecord } from '../../shared/types'
import GoConfigDialog from '../../components/GoConfigDialog'
import { useToast } from '../../components/Toast'
```

改为：

```tsx
import { useCallback, useEffect, useRef, useState } from 'react'
import DOMPurify from 'dompurify'
import { SettingsKeys } from '../../shared/types'
import type { ArticleRecord } from '../../shared/types'
import { useAppSettings } from '../../theme/ThemeProvider'
import GoConfigDialog from '../../components/GoConfigDialog'
import { useToast } from '../../components/Toast'
```

- [ ] **Step 2: 在 `relTime` 函数之后（`export default function ArticleView` 之前）追加模块级常量与工具**

```tsx
// ===== Ctrl+滚轮字体缩放（260916 新功能开发区）：仅阅读视图，全局记一档（settings feed_reader_zoom）=====
const ZOOM_MIN = 80
const ZOOM_MAX = 200
const ZOOM_STEP = 10
const ZOOM_DEFAULT = 100

function clampZoom(v: number): number {
  return Math.min(ZOOM_MAX, Math.max(ZOOM_MIN, v))
}

/** settings 原始值解析：非数字/非正/越界回落 100，并对齐 10 步进 */
function parseZoom(raw: string | undefined): number {
  const n = Number(raw)
  if (!Number.isFinite(n) || n <= 0) return ZOOM_DEFAULT
  return clampZoom(Math.round(n / ZOOM_STEP) * ZOOM_STEP)
}
```

- [ ] **Step 3: 组件体内加状态（`const { toast } = useToast()` 之后）**

```tsx
  const { settings, setSetting } = useAppSettings()
  const scrollRef = useRef<HTMLDivElement | null>(null)
  const [zoom, setZoom] = useState<number>(() => parseZoom(settings[SettingsKeys.FeedZoom]))
  const [zoomBarVisible, setZoomBarVisible] = useState(false)
```

（ThemeProvider 在 App 挂载时已把 settings 读全，此处惰性初始化可安全读到已存缩放值。）

- [ ] **Step 4: 组件体内加三个 effect（`toggleFav` 函数之后、「打开文章」useEffect 之前）**

```tsx
  // Ctrl+滚轮缩放：原生非 passive 监听（React 合成 onWheel 是 passive，preventDefault 无效）
  useEffect(() => {
    const el = scrollRef.current
    if (!el) return
    const onWheel = (e: WheelEvent): void => {
      if (!e.ctrlKey) return
      e.preventDefault() // 阻断 Chromium/Electron 默认整页缩放
      setZoom((z) => clampZoom(z + (e.deltaY < 0 ? ZOOM_STEP : -ZOOM_STEP)))
    }
    el.addEventListener('wheel', onWheel, { passive: false })
    return () => el.removeEventListener('wheel', onWheel)
  }, [])

  // 缩放落库（全局一档记忆；挂载时回写同值无副作用）
  useEffect(() => {
    void setSetting(SettingsKeys.FeedZoom, String(zoom))
  }, [zoom, setSetting])

  // 指示条：变化浮出，1.5s 无变化淡出
  useEffect(() => {
    setZoomBarVisible(true)
    const t = window.setTimeout(() => setZoomBarVisible(false), 1500)
    return () => window.clearTimeout(t)
  }, [zoom])
```

- [ ] **Step 5: JSX 三处改动**

① 滚动容器挂 ref：

```tsx
      <div className="feed-reader-scroll" ref={scrollRef}>
```

② 文章容器应用缩放（标题/元信息/AI 总结/正文一体）：

```tsx
        <article className="feed-article" style={{ fontSize: `${zoom}%` }}>
```

③ 指示条浮条（`<GoConfigDialog ... />` 之前插入）：

```tsx
      {zoomBarVisible && (
        <button className="feed-zoom-bar" title="点击复位 100%" onClick={() => setZoom(ZOOM_DEFAULT)}>
          <span className="material-symbols-outlined">text_increase</span>
          {zoom}%
        </button>
      )}
```

- [ ] **Step 6: feed.css 末尾追加样式**

```css
/* ===== Ctrl+滚轮字体缩放（260916）：阅读页相对定位承载指示条，颜色走主题变量 ===== */
.feed-reader-page {
  position: relative; /* 若该类已有 position 声明则跳过本条 */
}
.feed-zoom-bar {
  position: absolute;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 4px 12px;
  border-radius: var(--radius);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  box-shadow: 0 4px 14px color-mix(in srgb, var(--color-primary) 18%, transparent);
  cursor: pointer;
  z-index: 5;
  font-size: 0.9em;
  color: var(--color-text);
}
```

- [ ] **Step 7: 类型检查**

Run: `npm run typecheck`
Expected: 通过。

---

### Task 3: 构建验证

- [ ] **Step 1: 全量类型检查 + 构建**

Run: `npm run typecheck && npm run build`
Expected: 双 tsconfig 通过，vite 构建成功无错误。

- [ ] **Step 2: 冒烟清单移交开发者（设计 §七）**

Ctrl+上/下滚缩放与 80/200 边界、不按 Ctrl 滚动不变、界面其他部分字号不变、浮条浮出/点击复位/1.5s 淡出、重启记住缩放、AI 总结生成中缩放不干扰。
