# 万象库·测一测题库制 实施计划

> 创建于 2026-09-16 01:12
> 依据：`2026-09-16-测一测题库制-design.md`（同目录，已获开发者批准）。
> 纪律：git 操作一律由开发者完成，本计划不含任何 git 步骤；本项目无测试框架，验证口径 = `npm run typecheck` 双 tsconfig + `npm run build` 通过 + 开发者冒烟。

**目标**：测一测由「点按钮现场 LLM 生成 5 题」改为「后台泵维持题库（每 learned 词条常备 2 题）+ 点按钮秒抽 5 题」，作答提交即记账（答对 +1 达 2 毕业删题、答错 −1 地板 0），词条毕业累计次数即熟练度，卡片弹窗与答题页双展示。

**架构**：新表 `wiki_quiz_bank`（DB v41）+ `wiki_entries.quiz_graduated` 列；新泵服务 `electron/services/wikiQuizStock.ts`（仿 wikiStock.ts 静默惯例，三触发：启动 10s / 抽题消耗后 / 进模块 stockCheck）；`generateWikiQuiz` 降级为池空兜底（内部保留，兜底题入库）；渲染层仅 `WikiModule.tsx` 测一测链路 + `MdDialog` titleTag 多胶囊扩展。

**任务顺序**：Task 1 DB 迁移 → Task 2 类型 → Task 3 出题服务函数 → Task 4 泵服务 → Task 5 IPC/preload/触发点/级联删除 → Task 6 MdDialog 多胶囊 → Task 7 WikiModule 改造 → Task 8 验证。

---

### Task 1: DB v41 迁移（题库表 + 熟练度列）

**Files:**
- Modify: `electron/db/db.ts`（迁移函数尾部，`if (version < 40)` 块之后、函数收尾 `}` 之前）

- [ ] **Step 1: 在 `if (version < 40) { ... }` 块后追加 v41 块**

```ts
  if (version < 41) {
    // v41：万象库测一测题库制（2026-09-16-测一测题库制-design.md §二）——题库表 +
    // 词条熟练度列（quiz_graduated = 题目毕业移除累计次数，永续不清零）。
    // 题不入回收站（派生产物：毕业即删、词条彻底删时级联删）。
    d.exec(`CREATE TABLE wiki_quiz_bank (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      entry_id INTEGER NOT NULL,
      question TEXT NOT NULL,
      options TEXT NOT NULL,
      answer INTEGER NOT NULL,
      explanation TEXT NOT NULL,
      correct_count INTEGER NOT NULL DEFAULT 0,
      created_at TEXT NOT NULL
    )`)
    d.exec('CREATE INDEX IF NOT EXISTS idx_wiki_quiz_bank_entry ON wiki_quiz_bank(entry_id)')
    d.exec('ALTER TABLE wiki_entries ADD COLUMN quiz_graduated INTEGER NOT NULL DEFAULT 0')
    d.exec('PRAGMA user_version = 41')
  }
```

- [ ] **Step 2: 验证迁移无语法错**

Run: `npm run typecheck`
Expected: 双 tsconfig 均无输出（通过）。运行时迁移在重启 dev 时触发，冒烟阶段验证。

---

### Task 2: 类型同步（shared/types.ts + api.d.ts）

**Files:**
- Modify: `src/shared/types.ts`（WikiEntry 约 350 行起）
- Modify: `src/renderer/api.d.ts`（WikiEntry 约 71 行、WikiQuizQuestion 约 97 行、wiki API 段约 640 行附近 quiz 方法）

- [ ] **Step 1: `src/shared/types.ts` 的 `WikiEntry` 加 `quiz_graduated`，并在其后新增题库题接口**

```ts
export interface WikiEntry {
  id: number
  section_id: number
  term: string
  summary: string
  md_path: string
  origin: 'ai' | 'manual'
  /** 熟练度：该词条题目被「答对 2 次毕业」移除的累计次数（DB v41） */
  quiz_graduated: number
  created_at: string
  updated_at: string
  deleted_at: string | null
}

/** 测一测题库题（wiki:quizDraw 返回）：题面 + 记账句柄 + 来源词条熟练度 */
export interface WikiQuizBankQuestion {
  /** 题库行 id（wiki:quizRecord 记账用） */
  bankId: number
  /** 来源词条 id */
  entryId: number
  /** 来源词条名 */
  term: string
  question: string
  options: string[]
  /** 正确选项下标 0..3 */
  answer: number
  /** 题目解析（提交后无论对错都展示） */
  explanation: string
  /** 来源词条熟练度（毕业移除累计次数，>0 时展示「测毕 N」胶囊） */
  graduated: number
}
```

- [ ] **Step 2: `src/renderer/api.d.ts` 同步**——`WikiEntry` 在 `state: WikiEntryState` 行后加：

```ts
  /** 熟练度：该词条题目被「答对 2 次毕业」移除的累计次数（DB v41） */
  quiz_graduated: number
```

`WikiQuizQuestion` 接口保留不动（services 内部 + 兜底生成仍用），在其后新增：

```ts
/** 测一测题库题（wiki.quizDraw 返回）：题面 + 记账句柄 + 来源词条熟练度 */
export interface WikiQuizBankQuestion {
  /** 题库行 id（wiki.quizRecord 记账用） */
  bankId: number
  /** 来源词条 id */
  entryId: number
  /** 来源词条名 */
  term: string
  question: string
  options: string[]
  /** 正确选项下标 0..3 */
  answer: number
  /** 题目解析（提交后无论对错都展示） */
  explanation: string
  /** 来源词条熟练度（毕业移除累计次数，>0 时展示「测毕 N」胶囊） */
  graduated: number
}
```

（Task 5 会改 preload 与 api.d.ts 的 wiki 方法段，此处先落类型。）

---

### Task 3: 出题服务函数 `generateEntryQuiz`（services.ts）

**Files:**
- Modify: `electron/ai/services.ts`（`generateWikiQuiz` 函数之后、`learnTreeDocPath` 之前）

- [ ] **Step 1: 追加单词条出题函数（卡文注入 + 现有题干避重；scene 沿用 `wiki:quiz` 记账同桶）**

```ts
/** 单词条出 2 题（测一测题库泵用，260916 题库制）：卡文注入 + 已有题干避重（新题换考查角度）。
 *  兜底现场生成的 generateWikiQuiz 保留为 quizDraw 池空路径。 */
export async function generateEntryQuiz(
  term: string,
  entryId: number,
  cardMd: string,
  existingStems: string[],
  signal?: AbortSignal
): Promise<WikiQuizQuestion[]> {
  const avoid =
    existingStems.length > 0
      ? `\n该词条已有以下题目，新题必须换考查角度，不得与已有题目问法或考点重复：\n${existingStems.map((s) => `- ${s}`).join('\n')}\n`
      : ''
  const prompt = `${profileDigest()}${profileDigest() ? '\n\n' : ''}以下是一张知识卡片（词条：${term}）。请基于卡片内容出恰好 2 道四选一选择题，考查对核心知识点的掌握（不要直接抄卡片原句，干扰项要有迷惑性但明显错误）。${avoid}以 JSON 对象返回，最外层是对象，格式：{"questions":[{"term":"${term}","question":"题干","options":["选项一","选项二","选项三","选项四"],"answer":0,"explanation":"题目解析（一两句话：讲清正确答案为什么对、干扰项错在哪，60~120字）"}]}，answer 为正确选项的下标（0-3），questions 数组内恰好 2 项，不要输出其他任何内容。\n\n【词条：${term}】\n${cardMd.slice(0, 1200)}`
  const res = await chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.7,
    jsonMode: true,
    scene: 'wiki:quiz',
    signal
  })
  return parseQuizArray(res.content, new Map([[term, entryId]]))
}
```

（`parseQuizArray` 同文件既有函数，`Map([[term, entryId]])` 直接命中单词条校验；`profileDigest` 同文件既有。）

---

### Task 4: 泵服务 `electron/services/wikiQuizStock.ts`

**Files:**
- Create: `electron/services/wikiQuizStock.ts`

- [ ] **Step 1: 新建文件，全文如下**

```ts
// 万象库测一测题库泵（2026-09-16-测一测题库制-design.md §三/§四，仿 wikiStock.ts 静默惯例）：
// 每个 learned 词条常备 QUIZ_PER_ENTRY(2) 题在库，毕业（答对 2 次）即删题 + 词条熟练度 +1。
// 三触发：main.ts 启动延迟 10s / wiki:quizDraw 抽题消耗后 / 进万象库模块（wiki:stockCheck 挂靠）。
// fire-and-forget、永不抛错、永不弹窗；LLM 未配置静默跳过。
import { BrowserWindow } from 'electron'
import { getDb, nowIso } from '../db/db'
import { isLlmConfigured, generateEntryQuiz, type WikiQuizQuestion } from '../ai/services'
import { runPumpJob } from '../ai/jobs'
import { mdRead } from './files'

/** 每 learned 词条在库题数目标（设计定稿：2 题，同词条新题换角度防背答案） */
export const QUIZ_PER_ENTRY = 2
/** 毕业线：答对净次数达此值即删题 + 词条熟练度 +1 */
export const QUIZ_GRADUATE_AT = 2
/** 每次触发最多补几个词条（防启动风暴） */
const QUIZ_BATCH_ENTRIES = 5

/** 单飞标志：防并发重入（三触发点叠投时只跑一趟） */
let pumping = false

/** 每批入库后推送（复用 wiki:stockChanged，渲染层渐进刷新） */
function notifyStockChanged(): void {
  BrowserWindow.getAllWindows()[0]?.webContents.send('wiki:stockChanged')
}

/** 词条在库题数 */
export function bankCountOf(entryId: number): number {
  return (
    getDb()
      .prepare('SELECT COUNT(*) AS n FROM wiki_quiz_bank WHERE entry_id = ?')
      .get(entryId) as { n: number }
  ).n
}

/** 抽题（wiki:quizDraw 用）：低熟练度词条优先（graduated 升序）、同级词条间随机、
 *  同词条一轮最多 1 题（组内随机取 1）；排除软删在站词条的题。 */
export function drawWikiQuiz(limit = 5): {
  bankId: number
  entryId: number
  term: string
  question: string
  options: string[]
  answer: number
  explanation: string
  graduated: number
}[] {
  const d = getDb()
  const rows = d
    .prepare(
      `SELECT b.id AS bank_id, b.entry_id, b.question, b.options, b.answer, b.explanation,
              e.term AS term, e.quiz_graduated AS graduated
       FROM wiki_quiz_bank b JOIN wiki_entries e ON e.id = b.entry_id
       WHERE e.deleted_at IS NULL AND e.state = 'learned'
       ORDER BY e.quiz_graduated ASC`
    )
    .all() as {
    bank_id: number
    entry_id: number
    question: string
    options: string
    answer: number
    explanation: string
    term: string
    graduated: number
  }[]
  // 同词条最多 1 题：按词条分组、组内随机取 1
  const byEntry = new Map<number, typeof rows>()
  for (const r of rows) {
    const g = byEntry.get(r.entry_id)
    if (g) g.push(r)
    else byEntry.set(r.entry_id, [r])
  }
  const picked = [...byEntry.values()].map((g) => g[Math.floor(Math.random() * g.length)])
  // 同熟练度档内洗牌（同级随机），档间保持低熟练度优先
  const byGrad = new Map<number, typeof picked>()
  for (const p of picked) {
    const g = byGrad.get(p.graduated)
    if (g) g.push(p)
    else byGrad.set(p.graduated, [p])
  }
  const ordered = [...byGrad.keys()]
    .sort((a, b) => a - b)
    .flatMap((k) => byGrad.get(k)!.sort(() => Math.random() - 0.5))
  return ordered.slice(0, limit).map((r) => ({
    bankId: r.bank_id,
    entryId: r.entry_id,
    term: r.term,
    question: r.question,
    options: JSON.parse(r.options) as string[],
    answer: r.answer,
    explanation: r.explanation,
    graduated: r.graduated
  }))
}

/** 题面入库（兜底生成与泵共用；兜底题入库使记账全链路单一口径——设计 §四） */
export function insertQuizBankRows(qs: WikiQuizQuestion[]): void {
  const d = getDb()
  const ins = d.prepare(
    'INSERT INTO wiki_quiz_bank (entry_id, question, options, answer, explanation, correct_count, created_at) VALUES (?, ?, ?, ?, ?, 0, ?)'
  )
  for (const q of qs) {
    ins.run(q.entryId, q.question, JSON.stringify(q.options), q.answer, q.explanation, nowIso())
  }
}

/** 作答记账（wiki:quizRecord，每题提交即调）：答对 +1 达毕业线删题 + 词条熟练度 +1（同事务）；
 *  答错 −1 地板 0。 */
export function recordQuizAnswer(bankId: number, isCorrect: boolean): void {
  const d = getDb()
  if (!isCorrect) {
    d.prepare('UPDATE wiki_quiz_bank SET correct_count = MAX(0, correct_count - 1) WHERE id = ?').run(bankId)
    return
  }
  d.exec('BEGIN')
  try {
    const row = d.prepare('SELECT entry_id, correct_count FROM wiki_quiz_bank WHERE id = ?').get(bankId) as
      | { entry_id: number; correct_count: number }
      | undefined
    if (!row) {
      d.exec('ROLLBACK')
      return
    }
    if (row.correct_count + 1 >= QUIZ_GRADUATE_AT) {
      d.prepare('DELETE FROM wiki_quiz_bank WHERE id = ?').run(bankId)
      d.prepare('UPDATE wiki_entries SET quiz_graduated = quiz_graduated + 1 WHERE id = ?').run(row.entry_id)
    } else {
      d.prepare('UPDATE wiki_quiz_bank SET correct_count = ? WHERE id = ?').run(row.correct_count + 1, bankId)
    }
    d.exec('COMMIT')
  } catch (e) {
    d.exec('ROLLBACK')
    throw e
  }
}

/** 题库补充泵（唯一入口）：挑在库题数 < 目标的 learned 词条（按 id 序，每批最多 5 个），
 *  逐词条读卡文调 LLM 各出 2 题入库；单词条失败跳过不废批。 */
export async function ensureWikiQuizStock(): Promise<void> {
  if (pumping) return
  pumping = true
  try {
    if (!isLlmConfigured()) {
      console.warn('[wikiQuizStock] LLM 未配置，测一测题库泵跳过')
      return
    }
    const needy = getDb()
      .prepare(
        `SELECT e.id, e.term, e.md_path FROM wiki_entries e
         WHERE e.deleted_at IS NULL AND e.state = 'learned'
           AND (SELECT COUNT(*) FROM wiki_quiz_bank b WHERE b.entry_id = e.id) < ${QUIZ_PER_ENTRY}
         ORDER BY e.id LIMIT ${QUIZ_BATCH_ENTRIES}`
      )
      .all() as { id: number; term: string; md_path: string }[]
    if (needy.length === 0) return
    console.info(`[wikiQuizStock] 题库补充开始：${needy.map((e) => e.term).join(' · ')}`)
    for (const entry of needy) {
      try {
        // 并发触发下该词条可能已被上一轮补齐
        if (bankCountOf(entry.id) >= QUIZ_PER_ENTRY) continue
        let md = ''
        try {
          md = await mdRead(entry.md_path)
        } catch {
          /* md 缺失的词条跳过 */
        }
        if (!md) continue
        const existing = (
          getDb().prepare('SELECT question FROM wiki_quiz_bank WHERE entry_id = ?').all(entry.id) as {
            question: string
          }[]
        ).map((r) => r.question)
        const qs = await runPumpJob('wikiQuiz', (sig) =>
          generateEntryQuiz(entry.term, entry.id, md, existing, sig)
        )
        insertQuizBankRows(qs)
        notifyStockChanged()
        console.info(`[wikiQuizStock]「${entry.term}」题库 +${qs.length}（现 ${bankCountOf(entry.id)}）`)
      } catch (e) {
        console.warn(`[wikiQuizStock]「${entry.term}」出题失败，跳过：`, (e as Error).message)
      }
    }
    console.info('[wikiQuizStock] 题库补充结束')
  } finally {
    pumping = false
  }
}
```

（`mdRead` 来自 `electron/services/files.ts`，与 ipc.ts / recycle.ts 同款导入路径 `./files`。）

---

### Task 5: IPC / preload / 触发点 / 级联删除

**Files:**
- Modify: `electron/ipc.ts`（wiki:stockCheck 约 702 行、wiki:quiz 约 717 行、wiki:deleteForeverEntry 约 726 行）
- Modify: `electron/services/recycle.ts`（hardDelete wiki 分支约 224 行）
- Modify: `electron/preload.ts`（wiki 段 quiz 方法约 226 行）
- Modify: `src/renderer/api.d.ts`（wiki API 方法段）
- Modify: `electron/main.ts`（启动触发约 107-109 行）

- [ ] **Step 1: `electron/ipc.ts` 头部导入**——把

```ts
import { ensureWikiStock, drawPoolCard } from './services/wikiStock'
```

改为

```ts
import { ensureWikiStock, drawPoolCard } from './services/wikiStock'
import {
  ensureWikiQuizStock,
  drawWikiQuiz,
  insertQuizBankRows,
  recordQuizAnswer
} from './services/wikiQuizStock'
import { generateWikiQuiz } from './ai/services'
```

（若 `generateWikiQuiz` 已在别处导入则并入既有导入行。已核实：ipc.ts:57 有 `import type { TurtleSoupMaterial, WallPuzzleType } from './ai/services'`，是纯类型导入行，函数导入须**另起一行** value import。）

- [ ] **Step 2: `wiki:stockCheck` 挂靠泵触发**——把

```ts
  ipcMain.handle('wiki:stockCheck', () => {
    void ensureWikiStock()
    return true
  })
```

改为

```ts
  ipcMain.handle('wiki:stockCheck', () => {
    void ensureWikiStock()
    void ensureWikiQuizStock()
    return true
  })
```

- [ ] **Step 3: 旧 `wiki:quiz` 通道替换为 `wiki:quizDraw` + `wiki:quizRecord`**——把

```ts
  ipcMain.handle('wiki:quiz', async (_e, jobId: string) => {
    const ac = beginJob(jobId)
    try {
      return await generateWikiQuiz(ac.signal)
    } finally {
      endJob(jobId)
    }
  })
```

改为

```ts
  // 测一测抽题（260916 题库制）：秒抽秒回；池空兜底现场生成并入库（记账全链路单一口径）；
  // 消耗后 fire-and-forget 触发补充泵
  ipcMain.handle('wiki:quizDraw', async (_e, jobId: string) => {
    const ac = beginJob(jobId)
    try {
      let rows = drawWikiQuiz(5)
      if (rows.length === 0) {
        const qs = await generateWikiQuiz(ac.signal)
        insertQuizBankRows(qs)
        rows = drawWikiQuiz(5)
      }
      void ensureWikiQuizStock()
      return rows
    } finally {
      endJob(jobId)
    }
  })
  ipcMain.handle('wiki:quizRecord', (_e, bankId: number, isCorrect: boolean) => {
    recordQuizAnswer(bankId, isCorrect)
    return true
  })
```

- [ ] **Step 4: `wiki:deleteForeverEntry` 级联删题**——在 `d.prepare('DELETE FROM wiki_highlights WHERE entry_id = ?').run(id)` 行后加：

```ts
    d.prepare('DELETE FROM wiki_quiz_bank WHERE entry_id = ?').run(id)
```

- [ ] **Step 5: `electron/services/recycle.ts` hardDelete wiki 分支级联删题**——把

```ts
  if (rb.source === 'wiki') {
    d.prepare('DELETE FROM wiki_highlights WHERE entry_id = ?').run(rb.item_id)
  }
```

改为

```ts
  if (rb.source === 'wiki') {
    d.prepare('DELETE FROM wiki_highlights WHERE entry_id = ?').run(rb.item_id)
    d.prepare('DELETE FROM wiki_quiz_bank WHERE entry_id = ?').run(rb.item_id)
  }
```

- [ ] **Step 6: `electron/preload.ts` wiki 段**——把

```ts
    quiz: (jobId: string): Promise<
      import('../src/shared/types').WikiQuizQuestion[]
    > => ipcRenderer.invoke('wiki:quiz', jobId),
```

（以文件实际内容为准，两行可能排为一行）改为

```ts
    quizDraw: (jobId: string): Promise<import('../src/shared/types').WikiQuizBankQuestion[]> =>
      ipcRenderer.invoke('wiki:quizDraw', jobId),
    quizRecord: (bankId: number, isCorrect: boolean): Promise<boolean> =>
      ipcRenderer.invoke('wiki:quizRecord', bankId, isCorrect),
```

- [ ] **Step 7: `src/renderer/api.d.ts` wiki 方法段**——删旧 `quiz` 方法声明，原位加两行声明（api.d.ts 是手写声明文件，方法只写签名，参照同文件其他方法）：

```ts
    quizDraw: (jobId: string): Promise<WikiQuizBankQuestion[]>
    quizRecord: (bankId: number, isCorrect: boolean): Promise<boolean>
```

- [ ] **Step 8: `electron/main.ts` 启动触发**——在学习库泵行后加：

```ts
    // 万象库测一测题库泵（260916 题库制）：错开启动高峰，静默失败
    setTimeout(() => void ensureWikiQuizStock(), 10_000).unref()
```

并在头部 import 行 `import { ensureDailyLearn, ensureWikiStock } from './services/wikiStock'` 之后加：

```ts
import { ensureWikiQuizStock } from './services/wikiQuizStock'
```

---

### Task 6: MdDialog titleTag 多胶囊

**Files:**
- Modify: `src/components/MdDialog.tsx`（props 类型约 13 行、渲染约 280 行）

- [ ] **Step 1: props 类型**——`titleTag?: string` 改为

```ts
  titleTag?: string | string[]
```

- [ ] **Step 2: 渲染**——把

```tsx
              {titleTag && <span className="dialog-title-tag">{titleTag}</span>}
```

改为

```tsx
              {(Array.isArray(titleTag) ? titleTag : titleTag ? [titleTag] : []).map(
                (t) =>
                  t && (
                    <span key={t} className="dialog-title-tag">
                      {t}
                    </span>
                  )
              )}
```

（字符串用法对既有使用方零影响；数组渲染多胶囊。）

---

### Task 7: WikiModule 测一测链路改造

**Files:**
- Modify: `src/modules/wiki/WikiModule.tsx`（类型导入 6 行、quiz 状态 88-99 行、startQuiz 304-327 行、submitAnswer 336-340 行、总览按钮 425-427 行、答题页 651-763 行、卡片弹窗 titleTag 769 行）
- Modify: `src/App.css`（`.quiz-meta` 附近约 895 行）

- [ ] **Step 1: 类型导入**——第 6 行 `WikiQuizQuestion` 改为 `WikiQuizBankQuestion`

```ts
import type { WikiEntry, WikiSection, WikiHighlightRow, WikiQuizBankQuestion, WikiLearnRow } from '../../renderer/api'
```

- [ ] **Step 2: quiz 状态类型**——`useState<WikiQuizQuestion[]>` 改为 `useState<WikiQuizBankQuestion[]>`（91 行），注释同步为「测一测（260916 题库制）：题库秒抽 5 题逐题反馈」

- [ ] **Step 3: startQuiz**——`window.api.wiki.quiz(jobId)` 改 `window.api.wiki.quizDraw(jobId)`，成功分支不再 toast（秒开无「出题完成」提示价值），删除 `toast('出题完成，点击开始作答', { onClick: jumpToQuiz })` 一行；`jumpToQuiz` 回调保留（深链 quiz 分支仍用——若 `useCallback` 仅剩此一处使用且深链不再需要，可改普通函数；保留现状最稳）：

```tsx
  const startQuiz = async (): Promise<void> => {
    if (quizJob) return
    const jobId = crypto.randomUUID()
    setView({ kind: 'quiz' })
    setQuizJob(jobId)
    setQuizQuestions([])
    setQuizIdx(0)
    setQuizPick(null)
    setQuizPicks([])
    setQuizSubmitted(false)
    setQuizFinished(false)
    try {
      setQuizQuestions(await window.api.wiki.quizDraw(jobId))
    } catch (e) {
      const msg = String((e as Error).message)
      if (msg.includes('已取消')) toast('已取消')
      else if (msg.includes('LLM_NOT_CONFIGURED')) setGoConfig('llm')
      else toast(`出题失败：${msg.slice(0, 80)}`)
      setView({ kind: 'overview' })
    } finally {
      setQuizJob(null)
    }
  }
```

- [ ] **Step 4: submitAnswer 记账**——提交即 fire-and-forget 记账：

```tsx
  /** 提交答案：正式作答 + 题库记账（答对 +1 达 2 毕业删题、答错 −1），亮出对错与解析 */
  const submitAnswer = (): void => {
    if (quizPick == null || quizSubmitted) return
    const q = quizQuestions[quizIdx]
    void window.api.wiki.quizRecord(q.bankId, quizPick === q.answer)
    setQuizPicks((arr) => [...arr, quizPick])
    setQuizSubmitted(true)
  }
```

- [ ] **Step 5: 答题页熟练度胶囊**——quiz-meta 行（674 行）改为：

```tsx
                <div className="quiz-meta">
                  第 {quizIdx + 1} / {quizQuestions.length} 题 · 来源词条「{quizQuestions[quizIdx].term}」
                  {quizQuestions[quizIdx].graduated > 0 && (
                    <span className="quiz-mastery">测毕 {quizQuestions[quizIdx].graduated}</span>
                  )}
                </div>
```

- [ ] **Step 6: 加载文案**——答题页 loading（664 行）`出题中，约需数秒…` 改 `题库见底，现场出题中…`（仅兜底路径可见，照推理角第 30 轮文案惯例；秒抽路径该态一闪即过）。

- [ ] **Step 7: 卡片弹窗熟练度胶囊**——titleTag（769 行）改为：

```tsx
        titleTag={
          cardEntry
            ? [
                sections.find((s) => s.id === cardEntry.section_id)?.name,
                cardEntry.quiz_graduated > 0 ? `测毕 ${cardEntry.quiz_graduated}` : ''
              ].filter((x): x is string => !!x)
            : undefined
        }
```

- [ ] **Step 8: `src/App.css`**——`.quiz-meta` 规则后追加：

```css
.quiz-mastery {
  display: inline-flex;
  align-items: center;
  margin-left: 8px;
  padding: 1px 10px;
  border-radius: 999px;
  font-size: 0.75em;
  background: var(--color-primary-soft);
  border: 1px solid var(--color-primary);
  color: var(--color-primary-deep);
}
```

（变量已核实与 `MdDialog.css:64` 的 `.dialog-title-tag` 规则同款：`--color-primary-soft` / `--color-primary` / `--color-primary-deep`。）

---

### Task 8: 验证

- [ ] **Step 1: typecheck**

Run: `npm run typecheck`
Expected: 双 tsconfig 均通过无输出。

- [ ] **Step 2: build**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 3: 全局残留检查**

Run: 在仓库根 grep `wiki:quiz'` 与 `api.wiki.quiz(`（精确串），确认旧通道无残留引用（`wiki:quizDraw`、`wiki:quizRecord`、scene 字符串 `'wiki:quiz'` 与 shared/types.ts SCENE_LABELS 的 `'wiki:quiz': '万象库·测一测'` 属正常保留）。

---

### 冒烟清单（开发者执行，重启 dev 触发 v41）

1. 启动后约 10s 泵开补（控制台 `[wikiQuizStock]` 日志）；learned 词条各 2 题入库
2. 点「测一测」秒出题（不再有「出题完成」toast）；低熟练度词条优先、同轮无同词条两题
3. 提交制交互回归：预选改选/提交锁定/解析框/查看知识点卡片
4. 答对 2 次（跨轮）→ 该题消失（题库删行）；答错 → 再答对需 2 次净对（地板 0 验证：连错后首对不毕业）
5. 毕业后泵补新题换角度（同词条题干与旧题不同）
6. 卡片弹窗标题旁「测毕 N」胶囊（N=0 不显示）；答题页词条旁同款
7. 词条「直接删除」与回收站彻底删除 → 其题库行消失；软删在站期间抽不到其题、恢复后回归
8. 清空题库场景（如新装无 learned 词条）→ 点测一测走兜底现场生成且题入库
9. LLM 未配置：泵静默跳过；池空点测一测弹「去配置」
10. 双主题目检（胶囊与既有解析框配色）
