# 思维墙题型换血 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 思维墙三数学题型完全替换为四类思维游戏（侦探断案/情境谜题/文字谜题/生活逻辑），难度标尺上移，精选题库追加 8 道新种子（DB v22）。

**Architecture:** 纯 prompt 层重写 + 常量换血：出题/审题/判答两阶段管线机制不动，只换题型定义与口径文本；渲染层改练习场题型下拉与类型中文名映射（保留旧映射兼容存量记录）；种子库走独立附加数组 + v22 迁移只追加不动旧。

**Tech Stack:** Electron 44 + TypeScript，node:sqlite。验证手段：`npm run typecheck`（项目无单测框架，正则类逻辑用 node 脚本正反验证——本轮无新正则）。

**设计文档:** `docs/project/左侧边栏/推理角/2026-09-08-思维墙题型换血-design.md`（题目全文以 design 附录为准，本计划 Task 7 完整内联）。

**Git 纪律:** 全程不执行任何 git 操作，提交由开发者完成。

---

### Task 1: services.ts 类型与常量换血

**Files:**
- Modify: `electron/ai/services.ts:1710-1733`

- [ ] **Step 1: 替换 WallPuzzleDraft 联合类型与题型池**

把（services.ts:1710-1733）：

```typescript
export interface WallPuzzleDraft {
  type: 'insight_invariant' | 'strategy_protocol' | 'counter_probability'
  puzzle: string
  answer: string
  /** 标准论证全文（两阶段审题与判答讲解共用，随题落库） */
  reasoning: string
  hints: string[]
  difficulty: 'easy' | 'medium' | 'hard'
}

export type WallPuzzleType = WallPuzzleDraft['type']

/** 洞察题型池（v1.2 题型池换血：旧四类模板题全部退池，真假话推理开发者明令删除） */
export const WALL_TYPE_LIST: readonly WallPuzzleType[] = [
  'insight_invariant',
  'strategy_protocol',
  'counter_probability'
]

const WALL_TYPE_ZH_FULL: Record<WallPuzzleType, string> = {
  insight_invariant: '不变量与构造',
  strategy_protocol: '策略协议设计',
  counter_probability: '反直觉概率'
}
```

替换为：

```typescript
export interface WallPuzzleDraft {
  type: 'detective_case' | 'lateral_puzzle' | 'word_logic' | 'life_logic'
  puzzle: string
  answer: string
  /** 标准论证全文（两阶段审题与判答讲解共用，随题落库） */
  reasoning: string
  hints: string[]
  difficulty: 'easy' | 'medium' | 'hard'
}

export type WallPuzzleType = WallPuzzleDraft['type']

/** 思维游戏题型池（v1.6 题型换血：三个数学题型全部退池，不出数学理论题） */
export const WALL_TYPE_LIST: readonly WallPuzzleType[] = [
  'detective_case',
  'lateral_puzzle',
  'word_logic',
  'life_logic'
]

const WALL_TYPE_ZH_FULL: Record<WallPuzzleType, string> = {
  detective_case: '侦探断案',
  lateral_puzzle: '情境谜题',
  word_logic: '文字谜题',
  life_logic: '生活逻辑'
}
```

- [ ] **Step 2: 更新 generateWallPuzzle 上方注释**

services.ts:1735-1740 的 JSDoc 注释「三题型（不变量与构造 / 策略协议设计 / 反直觉概率），难度按洞察链深度标定…」改为：

```typescript
/**
 * 思维墙出题（specs §3，v1.6 思维游戏题型换血）：四题型（侦探断案 / 情境谜题 /
 * 文字谜题 / 生活逻辑），难度按线索复杂度与误导强度标定，脑筋急转弯与模板套皮
 * 题任何档位不合格。两阶段管线不变：出题 → 审题（独立验证结论正确 / 唯一 /
 * 可解 / 难度达标，不过打回重出一次，仍不过抛错不落库）。避免清单注入近期题面
 * 摘要防同构重复。出题注入画像摘要。
 */
```

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: **会报错**——`ipc.ts` 的每日一题/练习场仍引用旧类型无碍（类型是字符串子集），但若有旧类型字面量残留于此处之外的赋值点会暴露。**预期 0 错或仅 Task 4/6 将修的下游报错；若出现 services.ts 内部报错立即修复再继续。**

---

### Task 2: generateWallPuzzle 出题 prompt 重写

**Files:**
- Modify: `electron/ai/services.ts:1754-1775`（basePrompt）

- [ ] **Step 1: 整体替换 basePrompt 模板**

把 basePrompt 赋值（`const basePrompt = \`...\``）替换为：

```typescript
  const basePrompt = `${profileDigest()}${profileDigest() ? '\n\n' : ''}你是顶尖的思维游戏命题人，为思维墙出一道「答案需要被解题者推理出来」的思维游戏题。核心禁令：不出数学理论题（不变量/概率/数论/组合计算一律不合格），不出「套路 + 表面装饰」的题——纯真假话列表逐条排除、纯网格排除、看破套路后机械操作即解的题不合格。

## 题型（本次出：${WALL_TYPE_ZH_FULL[type]}）
- detective_case 侦探断案：案情/场景叙述，破绽藏在细节里——证词矛盾、时间线不对、不可能知道的信息；任务是还原真相或指认说谎者。题面需给出多方证词与物证细节
- lateral_puzzle 情境谜题：一个奇怪的现象问「为什么」，答案需要换掉一个默认假设才能想通；必须有唯一合理解（题面约束充分，独立推理可解，不是开放猜测）。与脑筋急转弯的区别：答案推得出来，不是靠冷知识
- word_logic 文字谜题：字谜、密码、索引、藏字、语言逻辑、文字规律——纯语言文字游戏，零数学符号。解码方式必须可被题面唯一确定，索引无歧义
- life_logic 生活逻辑：生活情境里的推理——谁先推断出什么、信息在人与人之间如何传递、沉默与行为本身携带信息；题面是故事不是符号。真假话列表换生活皮的模板题仍不合格

## 难度标定（严格执行，难度来自推理链深度与误导强度，而非信息量堆砌）
- 简单：单一关键洞察，但伪装到位——题面有一个「顺理成章的错误解释」挡在真相前面，戳破它才能解题。说破就懂、没有推理过程的脑筋急转弯不合格
- 中等：多条线索交织，至少一条主动埋设的误导；或需要连续两次转换默认假设；表面解释与真相要有真实的迷惑性竞争
- 困难：完整推理链（需整合 4 处以上细节）+ 强误导；单条线索都不致命，合起来才能唯一锁定真相；或需要发明一个解释框架统一所有异常（侦探的「唯一假设」方法）
${avoidBlock}
## 硬性质量标准（输出前逐条自检，不合格重写）
1. 答案唯一可判定（明确结论/指认/谜底），存在另一个同样说得通的答案即不合格
2. 题面自包含，无需外部知识；所有关键线索都在题面中给出
3. 禁止可被「标准模板」直接套解；禁止脑筋急转弯（答案靠冷知识而非推理）
4. 至少一条「看似可行的错误解释/误导线索」，走进去会得到错误结论
5. hints 恰好 3 条递进：一级指方向 → 二级点方法 → 三级给关键一步，任何一级都不直接给出答案
6. reasoning 为标准论证全文（可分步，200~500 字），完整闭合、无跳步
7. puzzle 用 Markdown 纯文本（可分行、可列表），不要用表格；本次目标难度：${DIFF_ZH[difficulty]}

只输出 JSON：{"puzzle":"题面全文","answer":"标准结论（简短明确）","reasoning":"标准论证全文","hints":["提示1","提示2","提示3"]}，不要输出其他任何内容。`
```

注意：`avoidBlock`、`DIFF_ZH`、`WALL_TYPE_ZH_FULL` 引用不变；函数其余逻辑（composeWallPuzzle 调用、打回重出）零改动。

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: PASS（0 错）

---

### Task 3: verifyWallPuzzle 审题口径适配

**Files:**
- Modify: `electron/ai/services.ts`（verifyWallPuzzle 的审题 prompt，`审查四关` 段）

- [ ] **Step 1: 替换审题四关文本**

把审题 prompt 中：

```
审查四关：
1. 结论正确：独立推演，标准结论是否真的成立
2. 结论唯一：是否存在另一个同样成立的答案或实质不同的等价结论，使题面有歧义
3. 条件自洽完备：仅凭题面能否推出结论，有无缺条件或内部矛盾
4. 难度达标：按「简单=单工具不显然映射 / 中等=两层链或含诱饵 / 困难=三层链或需发明构造」评估实际难度档；能否被「标准模板」直接套解（能则不合格）；难度来自洞察还是计算量（纯计算量不合格）
```

替换为：

```
审查四关：
1. 结论正确：独立推演（文字题须逐字数、逐索引核对），标准结论是否真的成立
2. 结论唯一：是否存在另一个同样说得通的答案或实质不同的等价结论；情境谜题重点审「题面约束是否足以排除其他合理解」，文字谜题重点审「解码方式是否唯一无歧义」
3. 条件自洽完备：仅凭题面能否推出结论，有无缺条件或内部矛盾
4. 难度达标：按「简单=单一洞察+伪装到位（脑筋急转弯不合格）/ 中等=多线索交织或含主动误导 / 困难=完整推理链+强误导+唯一假设法」评估实际难度档；能否被「标准模板」直接套解（能则不合格）；是否数学理论题或冷知识题（是则不合格）
```

其余（半盲设定、JSON 返回格式、温度 0.2）不动。

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: PASS

---

### Task 4: judgeWallAnswer 判答口径适配

**Files:**
- Modify: `electron/ai/services.ts:1892-1920`（judgeWallAnswer）

- [ ] **Step 1: 扩宽松等价口径并注入题型语境**

函数 JSDoc（1886-1891）同步更新为：

```typescript
/**
 * 思维墙判答（specs §3，v1.6 题型语境版）：宽松等价——结论实质相同即算对
 * （侦探断案=同一真相/说谎者指认；情境谜题=同一机制解释；文字谜题=同一谜底；
 * 生活逻辑=同一结论或等价推理结果）；只给结论不论证也算对，
 * 附带论证时讲解中顺带点评但不因论证简陋判错。无论对错都输出完整讲解
 * （判定理由 + 标准论证）。判答不注入画像。
 */
```

函数签名追加 `typeZh: string` 参数（调用方传入题型中文名）：

```typescript
export async function judgeWallAnswer(
  puzzleText: string,
  standardAnswer: string,
  standardReasoning: string,
  myAnswer: string,
  typeZh: string,
  signal?: AbortSignal
): Promise<WallAnswerResult> {
```

prompt 首行段替换为：

```
你是思维墙的判答员，本题题型为「${typeZh}」。宽松等价判定：玩家答案与标准答案表述不同但实质等价（侦探断案=同一真相指认或同一说谎者指认 / 情境谜题=同一机制解释 / 文字谜题=同一谜底（谐音字酌情算对）/ 生活逻辑=同一结论或等价推理结果）即算对；仅当结论实质不同才判错。
```

- [ ] **Step 2: 同步三处调用方（Grep `judgeWallAnswer(` 定位，行号以现场为准）**

1. **每日一题** `wall:answer`（ipc.ts:1261 附近）：从 `wall_puzzles` 行取 `puzzle_type` 经 WALL_TYPE_ZH 映射传入：

```typescript
const verdict = await judgeWallAnswer(
  row.puzzle_text,
  row.answer_standard,
  row.standard_reasoning ?? '',
  a,
  WALL_TYPE_ZH[row.puzzle_type] ?? row.puzzle_type,
  ac.signal
)
```

2. **练习场** `wall:practiceAnswer`（ipc.ts:1390 附近）：`entry` 为主进程内存题记录，含 `type` 字段，同样经映射传入（`entry.type` 为 `WallPuzzleType`，直接 `WALL_TYPE_ZH[entry.type] ?? entry.type`）。

3. **精选题库** `wall:bankAnswer`（ipc.ts:1490 附近）：`wall_bank` 表无 `puzzle_type` 字段，但其 `tag` 字段即题型/内容标签（新种子题的 tag 就是四题型中文名），直接传 `row.tag`。

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: PASS

---

### Task 5: ipc.ts 类型中文名映射扩容

**Files:**
- Modify: `electron/ipc.ts:1647-1656`（WALL_TYPE_ZH）

- [ ] **Step 1: 保留全部旧映射 + 追加新四类**

替换为：

```typescript
const WALL_TYPE_ZH: Record<string, string> = {
  logic_grid: '逻辑网格',
  truth_lie: '真假话推理',
  sequence: '序列推理',
  verbal_trap: '文字逻辑陷阱',
  // v1.2 洞察题型池（仅历史行显示用，不再生成）
  insight_invariant: '不变量与构造',
  strategy_protocol: '策略协议设计',
  counter_probability: '反直觉概率',
  // v1.6 思维游戏题型池（现行）
  detective_case: '侦探断案',
  lateral_puzzle: '情境谜题',
  word_logic: '文字谜题',
  life_logic: '生活逻辑'
}
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: PASS

---

### Task 6: PracticePanel 题型下拉换血

**Files:**
- Modify: `src/modules/reasoning/PracticePanel.tsx:17-23`（TYPE_PREFS）

- [ ] **Step 1: 替换 TYPE_PREFS**

```typescript
/** 题型自选（v1.6 思维游戏题型池；默认随机，单次有效不持久化） */
const TYPE_PREFS = [
  { id: 'random', label: '随机题型' },
  { id: 'detective_case', label: '侦探断案' },
  { id: 'lateral_puzzle', label: '情境谜题' },
  { id: 'word_logic', label: '文字谜题' },
  { id: 'life_logic', label: '生活逻辑' }
] as const
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: PASS

---

### Task 7: 种子题库追加 8 题（wallBankSeed.ts）

**Files:**
- Modify: `electron/db/wallBankSeed.ts`（文件末尾追加导出数组）

- [ ] **Step 1: 追加 WALL_BANK_SEED_V22 附加数组**

在文件末尾（`WALL_BANK_SEED` 数组之后）追加。**四道定稿题（1-4）来自 design 附录（开发者已校准），四道补题（5-8）为本计划新起草、待开发者人工验证；source 统一标「AI 起草」：**

```typescript
// v1.6 题型换血追加种子（DB v22 迁移插入；WALL_BANK_SEED 保持 v13 原样不动，
// 防全新用户在 v13 全量插入时重复）。四道经开发者例题校准，四道待人工验证。
export const WALL_BANK_SEED_V22: WallBankSeed[] = [
  {
    title: '雨夜失窃的怀表',
    tag: '侦探断案',
    difficulty: 'hard',
    puzzle: `暴雨夜 21:35，别墅主人报案：书房书桌上的收藏怀表失窃。警察到场，分开讯问四人。

别墅两层：二楼书房、客房，一楼客厅、厨房、洗衣房；老宅有前后两道楼梯——前梯开口正对客厅，后梯出口紧邻书房门口、楼下通洗衣房。

**主人**：20:00 起在二楼书房看书。21:00 整经前梯下楼到厨房烧水泡茶，21:30 端茶回书房，发现怀表不见了。

**夫人**：20:00–20:40 在客厅与妹妹视频通话（妹妹证实），随后在客厅沙发看书，位置正对前梯。她表示：20:40 之后没有任何人经过前梯。

**管家**：20:30–21:35 一直在一楼洗衣房熨衣服，洗衣房门口能看到后梯。警察随口问「失窃大概发生在什么时候」，她想了想说：「应该是 21:10 大雨下起来那阵——雨声那么响，小偷挑的就是那个时候。」

**客人（留宿的朋友）**：在二楼客房备明日的课，客房与书房相邻。21:00–21:30 间他听到走廊里两回脚步声：21:00 许（远去）、21:30 前后（走近）。21:10 暴雨响起后他戴上耳机，没再留意走廊动静。

**物证**：① 气象记录：21:10 起暴雨，此前全天无雨；② 书房门窗完好，怀表展架平放桌面，无翻动；③ 书房窗从内闩死、窗台无攀爬痕迹，但窗台**内侧有一小片洇湿**；④ 厨房水壶尚温，泡了两杯茶，一杯在书房，一杯还在灶台——主人习惯泡两杯，一杯给管家；⑤ 主人整晚未提过失窃时间，21:35 喊人时只喊了「怀表不见了」。

**问：窃贼是谁？给出完整推理链，并解释窗台洇湿的真正来历。**`,
    answer:
      '管家。她持有不可能得知的失窃时间信息（21:10），只有当时动手的人才能把失窃时间与雨起时刻挂钩；窗台洇湿是老宅窗框不严、暴雨随风渗入的自然痕迹，与贼无关。',
    solution: `1. **不可能知识（硬矛盾）**：失窃时间从头到尾无人宣布——主人 21:30 才发现，喊人也只喊「怀表不见了」。失窃发生于 21:00–21:30 之间这件事，只有两个人知道：主人（事后）与窃贼（当时）。管家全程在一楼熨衣，看不见二楼书房，却把失窃时间准确挂到「21:10 大雨下起来那阵」。能拥有这个信息的，只有 21:10 动手的人。

2. **时机自洽**：21:00 主人经前梯下楼（管家从洗衣房门口看得见后梯动静——确认主人离开书房）。她不动手，专等 21:10 暴雨：雨声盖住后梯的脚步与门响，也让隔壁客人戴上耳机。21:10–21:20 间她经后梯上楼（出口紧邻书房门）取表后原路返回。

3. **排除误导一（窗台洇湿）**：洇湿看似「有人雨中进出」，但窗从内闩死、无攀爬痕迹，外贼不成立。实情是老宅窗框不严，21:10 后暴雨随风渗入——自然洇湿。

4. **排除误导二（主人离房）**：主人唯一的 30 分钟空档使他成为头号嫌疑。但水壶尚温、两杯茶（含管家的那杯）坐实烧水属实；客人听到的两回脚步（21:00 远去、21:30 走近）与主人一下一上完全吻合——若主人中途折返，脚步应是四回。

5. **唯一假设**：全部细节在「管家雨中行窃」下无一例外；在其它任何人身上都至少撞上第 1 条的死结。`,
    hints: [
      '失窃时间从头到尾没人宣布过——把四人证词里所有提到「时间」的地方与物证对一遍。',
      '窗台洇湿和两回脚步声都是「支线」，不影响主案定人；先放下它们。',
      '管家那句「小偷挑的就是 21:10 雨下起来那阵」预设了她知道什么？谁才可能知道？'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  },
  {
    title: '1208 房',
    tag: '情境谜题',
    difficulty: 'medium',
    puzzle: `午后两点，酒店服务员小周敲响 1208 房的门准备清扫，里面一个男人应声：「不必了，谢谢。」她转去清扫隔壁 1209。14:35 收工时，她发现 1208 的门**虚掩着**——敲门无人应，推门进去，房内无人。房间干净整齐，床铺平整得像没人碰过；书桌上放着半杯咖啡，还冒着热气；窗户大开，窗帘翻飞；一根手指粗的绳索从房内穿出窗外——一端系在卫生间的承重管上，绳结打得又紧又齐整，另一端垂在窗外，坠着一块深蓝色木板。

小周冲到窗边往下看了一眼，立刻拨打了报警电话。

**问：她看到了什么？实际上发生了什么？**`,
    answer:
      '她看见一个人悬在十二楼窗外，以为有人坠楼而报警；实际上那是高楼外墙清洗工（蜘蛛人）正在作业——他开 1208 房是当作业平台：安全绳锚在卫生间承重管上，踩着悬吊的深蓝色木板（作业踏板）下放清洗外玻璃。',
    solution: `所有反常细节被「房间用途＝作业平台」一个假设统一解释：床铺平整＝他从没打算住宿；咖啡冒着热气＝刚刚还在房间（开工前提神）；绳结紧而齐整＝专业承重锚点，非临时起意；木板＝作业踏板（自缢或攀逃都不需要踏板和承重锚）；门虚掩＝方便收工翻回、给绳留通道。

小周冲到窗边往下看，看见「一个人悬在十二楼窗外」，第一反应是有人坠楼——受惊报警。

两次翻转：①房间的用途（住宿→工作平台）；②报警的性质（发现案件→她自己看错了）。`,
    hints: [
      '系在承重管上的绳结为什么打得那么齐整？什么人才需要这样的锚点？',
      '床没动过、咖啡还冒着热气——这位客人「住」过吗？他开房是干什么来的？',
      '如果你站在十二楼的窗边往下看，看见一个人挂在楼外的空中，你的第一反应会是什么？'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  },
  {
    title: '祖父的信',
    tag: '文字谜题',
    difficulty: 'medium',
    puzzle: `祖父去世后，家人在他留给孙子的信里发现正文之外、角落多了一行谁也看不懂的字——三对数字：

「2-13　2-14　3-7」

信的正文按一句一行誊抄如下：

　我们一起生活过的院子里，槐树比房檐还高。
　每逢六月，白花开满枝头，落在石桌上，浸进你奶奶的茶碗里。
　你总爱蹲在树下数蚂蚁，数着数着就睡着了，阳光透过叶隙洒在你的发梢上。
　如今石桌还在，槐树也还在，只是比房檐更高了。
　你回来的时候去看看吧，今年的花，应该又开了。

**问：祖父想告诉孙子什么？给出完整解法。**`,
    answer: '「石桌下」——有东西埋在院子里那座石桌下面。数字对是（行-字位）索引密码。',
    solution: `数字对是（行-字位）索引：每对取「第 X 行的第 Y 个字」。

- 2-13 → 第 2 行第 13 字 = **石**（每(1)逢(2)六(3)月(4)白(5)花(6)开(7)满(8)枝(9)头(10)落(11)在(12)石(13)）
- 2-14 → 第 2 行第 14 字 = **桌**
- 3-7 → 第 3 行第 7 字 = **下**（你(1)总(2)爱(3)蹲(4)在(5)树(6)下(7)）

连读＝「石桌下」。信的通篇都在写石桌与槐树，与暗号互相印证。计数细节：只数汉字、标点不计、一句一行——这是解密的第二道坎（起草时原索引误为 3-6，人工数数复核修正为 3-7，恰好说明这道坎真实存在）。`,
    hints: [
      '一对数字有两个数——一句话恰好有两个可数的属性：第几句，第几个字。',
      '2-13 和 2-14 同行相邻：取出来的两个字应该挨得很近。',
      '按索引逐字取出来连读（只数汉字，标点不算），别忘了一句一行。'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  },
  {
    title: '额头上的红漆',
    tag: '生活逻辑',
    difficulty: 'hard',
    puzzle: `郊游途中，老师带四个孩子路过一处工地。一阵风起，四个孩子中有若干人额头沾上了红漆。每个人都能看见别人的额头，却看不见自己的；没有镜子、没有互相告知。四个孩子都是完美的逻辑学家，并且彼此知道这一点。

老师对四人宣布：「我确定，你们当中**至少有一个人**额头上有漆。」随后老师说：「谁能**确定**自己额头上有漆，请举手。」老师一共问了四轮。

前三轮：无人举手。
第四轮：四人**同时**举手。

**问：到底几个孩子额头上有漆？给出完整推理——前三轮的沉默各排除了什么，第四轮的集体举手又是如何被逼出来的？**`,
    answer: '四个孩子全部有漆。',
    solution: `- **归纳奠基**：若只有一个孩子有漆，他看见三张干净额头，老师的「至少一个」只能落在他自己头上——第一轮就该举手。第一轮无人举手 ⇒ 至少两人有漆。
- **归纳递推**：若恰有两人有漆，每个有漆者看见一张有漆的脸，心想「若我是干净的，他在第一轮就会举手」；他没有 ⇒「我也有漆」——两人会在第二轮举手。第二轮无人 ⇒ 至少三人。同理，第三轮无人 ⇒ 至少四人——总共只有四个孩子，于是人人有漆。
- **第四轮**：每个孩子看见三张有漆的额头，推理「若我是干净的，全场只有三人有漆，他们仨第三轮就该举手；第三轮的沉默戳破了这个假设 ⇒ 我也有漆」——四人同时举手。
- **点睛**：老师的「至少一人有漆」对每个孩子（人人看见三张花脸）是废话——但它把这句话变成了「人人都知道人人都知道……」的公共知识，归纳链条由此才能一环扣一环；每一轮的沉默不是没有信息，是信息本身：它宣告「上一轮的推断上限被击穿」。（结构上与种子库「蓝眼睛的岛民」同源——共同知识归纳，但本题由行为轮次反推人数，方向相反。）`,
    hints: [
      '从最小的情形想起：如果只有一个孩子有漆，第几轮会有人举手？',
      '每一轮「没人举手」本身也是信息——它排除了什么？',
      '老师那句「至少一个人有漆」人人亲眼可见（人人看见三张花脸），像是废话——那它改变了什么？'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  },
  {
    title: '碎玻璃',
    tag: '侦探断案',
    difficulty: 'easy',
    puzzle: `便利店早班店员交接时发现：冷藏货架上的一瓶橙汁碎了，玻璃碴散在隔板上——奇怪的是，碎片和隔板都是干的，地面也没有任何液体痕迹。

监控显示昨夜九点多，有个孩子曾在这排货架前停留过几分钟。店员本想按「顾客打碎商品」调监控找家长赔偿，店长摆摆手：「不用找，这瓶是我们自己的问题。」

店员这才想起，昨夜打烊后，冷藏柜的压缩机确实响得反常。

**问：这瓶橙汁是怎么碎的？**`,
    answer:
      '瓶内橙汁冻成冰后体积膨胀，把瓶子从内部胀裂了——汁仍以冰的形态冻着，所以碎片是干的、没有液体流出，与孩子无关。',
    solution: `1. **「碰碎说」排除**：人为碰倒摔碎，瓶内液体必然当场流出——碎片湿、地面有渍。现场却全干。

2. **「干碎片 + 无液体」指向**：瓶里的液体没有流出——因为它冻成了冰。冷藏柜压缩机故障（昨夜异响）导致温度过低，橙汁结冰膨胀（水结冰体积增大约 9%），从内部胀裂瓶身；碎裂时内容物仍是固态，故无液体痕迹。

3. **孩子只是巧合**：他在货架前停留（买零食/看玩具），被「恰好出现在监控里」推到了嫌疑位——这正是题面埋的伪装。

4. 店长知道压缩机昨夜异常，一眼锁定自家设备问题。`,
    hints: [
      '如果是被人碰碎的，地面上应该有什么？现在为什么没有？',
      '液体去哪了——它一定还在这瓶「碎掉的东西」里，只是换了形态。',
      '昨夜冷藏柜压缩机的异响意味着什么？水结冰时体积会怎么变？'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  },
  {
    title: '周日晚上的电话',
    tag: '情境谜题',
    difficulty: 'easy',
    puzzle: `筒子楼里，302 的年轻人每周日晚上八点都会给 501 的独居老人打一个电话——铃响两声就挂断，从不说话。老人从不应答，也从不抱怨。这样的电话，打了整整三年。

直到某个周日晚上，铃声没有响起。老人看了看挂钟，又等了十分钟，立刻披衣下楼去敲 302 的门——年轻人正发着高烧躺在床上，几乎晕了过去。老人及时叫来了救护车。

**问：这通从不说话的电话，是一个什么约定？为什么铃「没响」反而让老人立刻行动？**`,
    answer:
      '铃声是「平安信号」：两声铃＝「我这周一切安好」。年轻人独居，老人重听（听不清通话人声但听得见座机振铃）且不会用手机，两人约定以振铃报平安；某周铃未响＝年轻人可能出了事，老人按约定上门查看——因此救了他。',
    solution: `1. **「从不说话」是题眼**：通话内容毫无意义——说明铃声本身就是全部信息，电话被当作信号装置而非通讯工具。

2. **老人「不应答、不抱怨」且「一听没响就行动」**：说明双方事先有明确约定——响＝平安，没响＝异常。

3. **为什么需要这么绕的约定**：老人独居、重听（听不清人声，但座机铃声足够响亮）、不会用手机——短信、微信、语音通话都不可行，振铃是唯一可靠信道。

4. **验证事件**（发烧获救）恰好证明约定的存在与用途：年轻人每周报平安，老人是他的紧急守望人。`,
    hints: [
      '打电话却从不说话、对方也从不接——这通电话传递信息的部分到底是什么？',
      '老人等的不是「电话」，是「铃声」本身：响代表什么，不响代表什么？',
      '为什么不用短信或微信？想想老人有什么生理条件限制。'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  },
  {
    title: '老先生的对联',
    tag: '文字谜题',
    difficulty: 'medium',
    puzzle: `老先生走得突然。整理遗物时，家人在他书桌的抽屉里发现一副他生前亲笔写好、尚未装裱的对联：

　　钱塘旧巷一口井，
　　在水之湄缓缓东。

家人反复念了念，觉得对得并不工整，意思也只是寻常写景，不明白他为什么特意写好这样一副对联郑重留在抽屉里。老先生一生谨慎，钱物上的事从不明说。

**问：这副对联想告诉家人什么？**`,
    answer:
      '「钱在井东」——四字分藏于上下联首尾：上联首「钱」、下联首「在」、上联尾「井」、下联尾「东」，按「上首—下首—上尾—下尾」连读即得。积蓄藏在（老宅）井的东面。',
    solution: `1. **「对得不工整」是破题点**：老先生写得一手好字却留下不工整的对联，说明重点不在文采——对联另有用途。

2. **检查对联的「四角」**：上联首字「钱」、下联首字「在」、上联尾字「井」、下联尾字「东」。

3. **连读试序**：「钱、在、井、东」（上首→下首→上尾→下尾）成句；「钱、井、在、东」（上首→上尾→下首→下尾）不成句——前者即答案。

4. **伪装层**：「钱塘」是天然掩护——家人首先会把「钱塘」当地名读过去，藏字因此不显眼；「在水之湄」出自《诗经·蒹葭》，添书卷气降低戒心。`,
    hints: [
      '一生谨慎的人把一副「不工整」的对联郑重留在抽屉里——它的价值不在文采，在别处。',
      '把上下联各自的第一个字和最后一个字，一共四个字，单独抄出来。',
      '四个字试着连读成句：如果一种排列不成句，换「上联头、下联头、上联尾、下联尾」的顺序再试。'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  },
  {
    title: '消失的坚果',
    tag: '生活逻辑',
    difficulty: 'medium',
    puzzle: `周一上午，公司行政发现茶水间公共零食柜（零食放在第二层——站着才够得到的高度）少了一包坚果。调监控：当天上午只有三人进出茶水间——

- 小周 10:00 进，10:01 出；
- 小郑 10:05 进，10:08 出；
- 小吴 10:10 进，10:11 出（小吴坐轮椅）。

10:02 有同事路过茶水间门口，看见坚果还在柜里；10:15 行政去拿东西，发现没了。

三人分别说：
小周：「我就进去接了杯水。」
小郑：「我进去的时候，坚果还好好地在柜子里。」
小吴：「我进去时第二层已经空了，我还以为是被谁领走了。」

行政听完三句话，直接找小郑单独谈了话。

**问：为什么是小郑？**`,
    answer:
      '坚果只能消失在「有人在场」的时段：10:02 还在、10:08–10:10 与 10:11 之后均无人进出（东西不会自己消失），10:08 前唯一的在场窗口就是小郑的 10:05–10:08；小吴坐轮椅够不到第二层、小周离开时坚果尚未丢。小郑说「还在柜子里」是谎话。',
    solution: `1. **时间线排除**：10:02 同事看见还在 ⇒ 小周（10:00–10:01，在此之前出场）无关；10:15 发现没了，窗口锁定 10:02–10:15。

2. **窗口内的在场时段**只有两个：小郑 10:05–10:08、小吴 10:10–10:11；两段之间（10:08–10:10）与之后（10:11–10:15）均无人进出（监控保证），零食不会自行消失。

3. **小吴 10:10 看到「第二层已空」若是真话**，消失必发生在 10:10 之前——只剩小郑的在场时段。若小吴说谎（他拿了），物理上不成立：他坐轮椅，够不到第二层。

4. **唯一可能**：坚果在小郑 10:05–10:08 在场期间消失，而他说「还在柜子里」——说谎者即拿走者。

5. **迷惑点**：直觉上最可疑的是自曝「进去时已经空了」的小吴（像贼喊捉贼），轮椅细节不动声色地排除了他。`,
    hints: [
      '10:08 到 10:10 之间，茶水间里有几个人？',
      '先别急着怀疑说「已经空了」的小吴——他碰得到放坚果的那层货架吗？',
      '零食不会自己消失：消失必须发生在某个「有人在场」的时段里。把三个时段摆到时间线上逐一对照三人证词。'
    ],
    source: 'AI 起草（2026-09-08，bugzi workspace）'
  }
]
```

- [ ] **Step 2: 人工核验文字题字位**

对《祖父的信》与《老先生的对联》两题，**逐字数一遍**（只数汉字、标点不计、一句一行）：2-13=石、2-14=桌、3-7=下；上联「钱塘旧巷一口井」首钱尾井、下联「在水之湄缓缓东」首在尾东。任何一处不符，修正 puzzle 或索引后再继续（先例：设计阶段 3-6 数错由开发者复核发现）。

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: PASS

---

### Task 8: DB v22 迁移

**Files:**
- Modify: `electron/db/db.ts`（v21 迁移块之后追加；`electron/db/db.ts:6` 已 import WALL_BANK_SEED）

- [ ] **Step 1: 追加 import 与迁移块**

db.ts 顶部 import 行改为：

```typescript
import { WALL_BANK_SEED, WALL_BANK_SEED_V22 } from './wallBankSeed'
```

在 `if (version < 21) { ... }` 块之后追加（⚠️ 若实施时已存在更高版本迁移，按实际顺延版本号并同步本计划与设计文档的 v22 表述）：

```typescript
  if (version < 22) {
    // v22：思维墙题型换血（v1.6）——精选题库追加 8 道思维游戏题
    // （4 道经开发者例题校准 + 4 道待人工验证，source 标「AI 起草」），
    // 只追加不动旧；WALL_BANK_SEED 保持 v13 原样防全新用户重复插入。
    // （v1.3 规划的 wall_pool 题池未实施，无池存量旧题需清理——
    // 每日一题/练习场均现场两阶段生成，换血后自动全走新 prompt。）
    const now22 = new Date().toISOString()
    const insBank22 = d.prepare(
      'INSERT INTO wall_bank (title, tag, difficulty, puzzle_text, answer_standard, solution, source, status, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)'
    )
    for (const b of WALL_BANK_SEED_V22) {
      insBank22.run(b.title, b.tag, b.difficulty, b.puzzle, b.answer, b.solution, b.source, 'todo', now22, now22)
    }
    d.exec('PRAGMA user_version = 22')
  }
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: PASS

---

### Task 9: 推理角 specs v1.6 增量同步

**Files:**
- Modify: `docs/project/左侧边栏/推理角/designs-specs.md`

- [ ] **Step 1: 头部版本注记追加 v1.6 段**

在头部版本注记区（v1.5 注记之前/最新位置，按现有「从新到旧」惯例）追加：

```markdown
> **v1.6（260908）思维墙题型换血**：三数学题型（不变量/策略协议/反直觉概率）全部退池，新四类思维游戏题型上线——detective_case 侦探断案 / lateral_puzzle 情境谜题 / word_logic 文字谜题 / life_logic 生活逻辑；难度标尺改为「线索复杂度 + 误导强度」（简单=单一洞察+伪装到位、中等=多线索+主动误导、困难=完整推理链+唯一假设法），脑筋急转弯/模板套皮/数学理论题任何档位不合格；出题审题判答 prompt 全部换血（两阶段管线机制不变），判答签名增 typeZh 题型语境；精选题库 v22 迁移追加 8 题（WALL_BANK_SEED_V22，source「AI 起草」，4 道已校准 4 道待验证），WALL_TYPE_ZH 保留全部旧类型映射兼容存量行。设计文档：`docs/project/左侧边栏/推理角/2026-09-08-思维墙题型换血-design.md`。
```

- [ ] **Step 2: §3 出题/审题/判答段落就地更新**

定位 specs §3 中描述三题型/难度标定/判答口径的段落，将题型列表与口径改为新四类表述（与 Task 2/3/4 的 prompt 内容一致的摘要）。**注意**：specs v1.3 注记中的 wall_pool 题池预生成/补充泵机制实际未实施（代码无 wall_pool/reasoningStock），v1.6 注记与 §3 改写均不得引用「题池」机制，也不修改 v1.3 注记本身（历史规划态非本轮范围）。

- [ ] **Step 3: §1 DB 注记追加 v22**

在 DB 版本清单（v13/v14/v15/v16/v18/v21 附近）追加 v22 行：追加 wall_bank 种子 8 题（wallBankSeed.ts WALL_BANK_SEED_V22）。

---

### Task 10: 总验证 + 冒烟清单 + 日志

- [ ] **Step 1: typecheck 终验**

Run: `npm run typecheck`
Expected: PASS（0 错）

- [ ] **Step 2: 冒烟（需重启 dev 触发 v22 迁移）**

1. 重启 dev → 数据库迁移 v22 → 题库列表新增 8 题（已破 0 / 共 19）。
2. 逐题打开核对：题面/难度标签显示正常；《祖父的信》《老先生的对联》**亲自动笔逐字数一遍索引**（上轮 3-6 的教训）。
3. 每日一题（当日无题时）：出新四类之一，旧记录（数学类型）月份墙/详情显示中文名正常。
4. 练习场：四类下拉各出至少一题；三级提示递进正常。
5. 判答：接近真相的表述（如《1208 房》答「清洁工在窗外作业」）应判对；完全不对的判错并给完整讲解。
6. 旧每日一题记录 md/详情打开无异常。

- [ ] **Step 3: 开发日志追加**

`docs/log/260908.md` 顶部追加小节（标题下带 `> 创建于 YYYY-MM-DD HH:MM`，bash `date` 取实际时间），记录：实施范围、v22 迁移、种子 8 题（4 道待验证清单）、冒烟结果、遗留事项（4 道补题待开发者验证后可删改；旧数学种子题保留）。

- [ ] **Step 4: 优化建议区归档（冒烟通过后）**

「思维墙的题目还需要进一步升级」条目移入归档区第 27 轮小节（与格言条目同轮，若格言已先实施则并入同节），保留原始描述 + 完成说明。
