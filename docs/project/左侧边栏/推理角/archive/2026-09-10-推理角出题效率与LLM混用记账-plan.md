# 推理角出题效率与 LLM 混用/记账 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `chatCompletion` 咽喉点上实现模型池溢出路由（glm 满载溢出 gpt）+ llm_usage 记账（DB v27）+ 补充泵三级并行化 + 个人档「AI 使用」统计区 + 左栏 AI 实时活动指示。

**Architecture:** 全仓 27 处 LLM 调用唯一咽喉点 `electron/ai/llm.ts` 内做路由（每配置在途计数 + `maxConcurrent` 上限）、记账（每次逻辑调用落 `llm_usage` 一行）、活动广播（`llm:activity`）；补充泵与汤批审题并行化纯属调度改动，prompt/审题/打回逻辑零改动（质量红线）。spec 见同目录 `2026-09-10-推理角出题效率与LLM混用记账-design.md`。

**Tech Stack:** Electron 44 主进程（node:sqlite / ipcMain）、React 19 + TypeScript 渲染层、OpenAI 兼容 chat/completions。

**项目纪律（覆盖默认流程）：**
- **禁止任何 git 操作**（add/commit 等全部由开发者手动完成）——本计划无 commit 步骤。
- 本项目无单测框架：验证 = `npm run typecheck`（双 tsconfig）+ 一次性验证脚本（跑完即删，第 23/27 轮先例）+ 冒烟清单。若 typecheck 出现疑似旧诊断重放，先删 `*.tsbuildinfo` 再跑（第 23 轮先例）。
- IPC 新增需**三处手工同步**：`electron/ipc.ts`（handler）+ `electron/preload.ts`（桥）+ `src/renderer/api.d.ts`（类型）。

**实测基线（写计划当日）：** glm-5.3-flash TTFT 0.7s / 4 并发全 200 / usage 完整；gpt-5.6 中转 TTFT 11.7s。glm 已是默认配置。

---

### Task 1: DB v27 —— llm_usage 表

**Files:**
- Modify: `electron/db/db.ts`（migrate() 末尾，v26 块之后）

- [ ] **Step 1: 在 `if (version < 26) {...}` 块之后、`migrate()` 收尾大括号之前追加 v27 块**

```ts
  if (version < 27) {
    // v27：LLM 使用记账（260910 推理角效率优化）——chatCompletion 咽喉点每次逻辑调用一行；
    // 429 重试只记最终趟（耗时含重试等待）；usage 缺失时按字符估算并打 tokens_estimated 标记。
    // 只增不删（一天几十行，不做清理任务——设计定稿 YAGNI）。
    d.exec(`CREATE TABLE IF NOT EXISTS llm_usage (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      created_at TEXT NOT NULL,
      scene TEXT NOT NULL,
      config_name TEXT NOT NULL,
      model TEXT NOT NULL,
      ok INTEGER NOT NULL,
      duration_ms INTEGER NOT NULL,
      prompt_tokens INTEGER NOT NULL DEFAULT 0,
      completion_tokens INTEGER NOT NULL DEFAULT 0,
      tokens_estimated INTEGER NOT NULL DEFAULT 0,
      error_brief TEXT
    )`)
    d.exec('CREATE INDEX IF NOT EXISTS idx_llm_usage_created ON llm_usage(created_at)')
    d.exec('PRAGMA user_version = 27')
  }
```

- [ ] **Step 2: 验证**：`npm run typecheck` 通过（此时仅加表无引用，不应有新错误）。

---

### Task 2: 共享类型 —— maxConcurrent / 场景标签 / 统计类型

**Files:**
- Modify: `src/shared/types.ts`（LlmConfig 接口处 + 文件合适位置）

- [ ] **Step 1: `LlmConfig` 加可选字段**（现有定义约 77-83 行）：

```ts
export interface LlmConfig {
  id: string
  name: string
  apiUrl: string
  apiKey: string
  model: string
  /** 模型池溢出路由并发上限（260910）：>0 生效，缺省 = 不限（永不溢出，行为同旧版） */
  maxConcurrent?: number
}
```

- [ ] **Step 2: 文件末尾追加统计类型与场景标签表**：

```ts
// ---------- LLM 使用统计（260910 推理角效率优化配套） ----------

/** llmUsage:stats 单场景聚合行 */
export interface LlmUsageSceneRow {
  scene: string
  calls: number
  failures: number
  avgMs: number
  tokens: number
  /** 存在估算 token 的行数（>0 时统计页 token 列带 ≈ 标记） */
  estRows: number
}

/** llmUsage:stats 返回 */
export interface LlmUsageStats {
  totals: { calls: number; failures: number; tokens: number }
  rows: LlmUsageSceneRow[]
}

/** LLM 场景中文标签（统计页与活动指示共用；未收录的 scene 直接显示原串） */
export const LLM_SCENE_LABELS: Record<string, string> = {
  'reasoning:wall-compose': '推理角·出题',
  'reasoning:wall-review': '推理角·审题',
  'reasoning:wall-judge': '推理角·判答',
  'reasoning:soup-compose': '推理角·出汤',
  'reasoning:soup-review': '推理角·审汤',
  'reasoning:soup-ask': '推理角·判问',
  'reasoning:soup-guess': '推理角·判汤底',
  'reasoning:soup-report': '推理角·复盘',
  'reasoning:soup-backfill': '推理角·诡计回填',
  'ai:chat': 'debugzi·对话',
  'ai:compact': 'AI·上下文压缩',
  'motto:generate': '格言库·生成',
  'wiki:card': '万象库·知识卡',
  'wiki:suggest': '万象库·词条构思',
  'wiki:quiz': '万象库·测一测',
  'inspiration:diverge': '灵感泉·发散',
  'inspiration:refine': '灵感泉·自评',
  'verify:check': '万象库·辩真核查',
  'zhijiji:v0': '致知己·AI 初始化',
  'wenbi:copilot': '文笔坊·协笔',
  'feed:summary': '信息源·总结',
  'mcp:research': 'MCP·配置研究',
  other: '其他'
}
```

- [ ] **Step 3: 验证**：`npm run typecheck` 通过。

---

### Task 3: llm.ts 咽喉点改造 —— 路由 + 记账 + 活动广播

**Files:**
- Modify: `electron/ai/llm.ts`（整文件重构，`normalizeModelsUrl`/`listUpstreamModels`/`testLlmConnection`/`sleepOrAbort` 原样保留）

- [ ] **Step 1: 重构文件为下述内容**（保留原有函数不动者标注「原样」；新逻辑全部就位）：

```ts
// LLM 客户端（主进程）：OpenAI 兼容 chat/completions。
// v2（260910 推理角效率优化）：chatCompletion 保持唯一咽喉点，调用方透明获得
// 模型池溢出路由 + llm_usage 记账 + llm:activity 活动广播；
// 手动诊断路径（测试连接/拉模型列表）不经咽喉点，不路由不记账。
import { BrowserWindow } from 'electron'
import { getDb, nowIso } from '../db/db'
import { getJsonSetting, getSetting } from '../db/settings'
import type { LlmConfig } from '../../src/shared/types'
import { SettingsKeys } from '../../src/shared/types'
import { ensureNotCancelled } from './jobs'

export class LlmNotConfiguredError extends Error {
  constructor() {
    super('LLM_NOT_CONFIGURED')
  }
}

/** 取默认 LLM 配置；无配置或默认项缺失 → LlmNotConfiguredError（原样） */
export function getDefaultLlm(): LlmConfig {
  const configs = getJsonSetting<LlmConfig[]>(SettingsKeys.LlmConfigs, [])
  if (configs.length === 0) throw new LlmNotConfiguredError()
  const defaultId = getSetting(SettingsKeys.LlmDefaultId)
  const def = configs.find((c) => c.id === defaultId) ?? configs[0]
  if (!def) throw new LlmNotConfiguredError()
  return def
}

// ---------- 模型池溢出路由（260910 设计 §1） ----------

/** 在途调用登记：key = 自增序号；value 含路由计数（按 configId）与活动指示（scene/配置名）所需字段 */
const activeCalls = new Map<number, { scene: string; configId: string; configName: string }>()
let callSeq = 0

function countInflight(configId: string): number {
  let n = 0
  for (const v of activeCalls.values()) if (v.configId === configId) n++
  return n
}

/** 在途快照广播（活动指示，设计 §5）：调用起止各发一次，payload 只含 scene 与配置名 */
function broadcastActivity(): void {
  const items = [...activeCalls.values()].map((v) => ({ scene: v.scene, configName: v.configName }))
  BrowserWindow.getAllWindows()[0]?.webContents.send('llm:activity', { items })
}

/** 路由纯函数（无副作用，便于脚本验证）：默认未满用默认；否则挑在途最低的可用备选；
 *  全满仍回默认（不排队，429 三层兜底收尾）。maxConcurrent 缺省/非正 = 不限。 */
export function pickConfigFrom(
  configs: LlmConfig[],
  defaultId: string,
  inflightOf: (id: string) => number
): LlmConfig {
  const def = configs.find((c) => c.id === defaultId) ?? configs[0]
  if (!def) throw new LlmNotConfiguredError()
  const cap = (c: LlmConfig): number =>
    typeof c.maxConcurrent === 'number' && c.maxConcurrent > 0 ? c.maxConcurrent : Infinity
  if (inflightOf(def.id) < cap(def)) return def
  const spare = configs.filter((c) => c.id !== def.id && inflightOf(c.id) < cap(c))
  if (spare.length === 0) return def
  return spare.sort((a, b) => inflightOf(a.id) - inflightOf(b.id))[0]
}

// ---------- usage 记账（260910 设计 §2；静默容错，绝不影响调用本身） ----------

function recordUsage(row: {
  scene: string
  configName: string
  model: string
  ok: boolean
  durationMs: number
  promptTokens: number
  completionTokens: number
  tokensEstimated: boolean
  errorBrief: string | null
}): void {
  try {
    getDb()
      .prepare(
        'INSERT INTO llm_usage (created_at, scene, config_name, model, ok, duration_ms, prompt_tokens, completion_tokens, tokens_estimated, error_brief) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)'
      )
      .run(
        nowIso(),
        row.scene,
        row.configName,
        row.model,
        row.ok ? 1 : 0,
        row.durationMs,
        row.promptTokens,
        row.completionTokens,
        row.tokensEstimated ? 1 : 0,
        row.errorBrief
      )
  } catch (e) {
    console.warn('[llm] usage 记账失败（忽略）：', e)
  }
}

/** 标准化 API URL：自动拼 /v1/chat/completions（调用方给 base 或完整路径均可）（原样） */
export function normalizeChatUrl(apiUrl: string): string {
  const u = apiUrl.trim().replace(/\/+$/, '')
  if (/\/chat\/completions$/.test(u)) return u
  if (/\/v\d+($|\/)/.test(u)) return `${u}/chat/completions`
  return `${u}/v1/chat/completions`
}

export interface ChatOptions {
  messages: { role: 'system' | 'user' | 'assistant'; content: string }[]
  temperature?: number
  jsonMode?: boolean
  signal?: AbortSignal
  /** 场景标签（260910 记账/活动指示用，两段式 模块:动作；缺省 other） */
  scene?: string
}

export interface ChatResult {
  content: string
}

/** 429 限流自动重试次数（中转服务账号并发超限属瞬态错误，等待后重试即可）（原样） */
const RATE_LIMIT_RETRIES = 3

/** 调用 LLM 的唯一咽喉点（260910 v2）：路由选模型 → 在途登记/广播 → 调用 → 记账。
 *  429 重试在内部完成，一次逻辑调用只记一行（耗时含重试等待）。
 *  失败抛带 message 的 Error（渲染层 toast 展示）；signal 取消 → 抛「已取消」。 */
export async function chatCompletion(opts: ChatOptions): Promise<ChatResult> {
  const cfg = pickConfigFrom(
    getJsonSetting<LlmConfig[]>(SettingsKeys.LlmConfigs, []),
    getSetting(SettingsKeys.LlmDefaultId) ?? '',
    countInflight
  )
  const scene = opts.scene ?? 'other'
  const seq = ++callSeq
  activeCalls.set(seq, { scene, configId: cfg.id, configName: cfg.name })
  broadcastActivity()
  const t0 = Date.now()
  try {
    const res = await chatWithConfig(cfg, opts)
    // usage 提取：上游报了用真的；任一侧缺失按字符粗估（中文 ~2 字符/token）并打估算标记
    const pt = res.usage?.prompt_tokens
    const ct = res.usage?.completion_tokens
    let promptTokens = typeof pt === 'number' ? pt : 0
    let completionTokens = typeof ct === 'number' ? ct : 0
    let tokensEstimated = false
    if (typeof pt !== 'number') {
      promptTokens = Math.ceil(opts.messages.reduce((n, m) => n + m.content.length, 0) / 2)
      tokensEstimated = true
    }
    if (typeof ct !== 'number') {
      completionTokens = Math.ceil(res.content.length / 2)
      tokensEstimated = true
    }
    recordUsage({
      scene,
      configName: cfg.name,
      model: cfg.model,
      ok: true,
      durationMs: Date.now() - t0,
      promptTokens,
      completionTokens,
      tokensEstimated,
      errorBrief: null
    })
    return { content: res.content }
  } catch (e) {
    recordUsage({
      scene,
      configName: cfg.name,
      model: cfg.model,
      ok: false,
      durationMs: Date.now() - t0,
      promptTokens: 0,
      completionTokens: 0,
      tokensEstimated: false,
      errorBrief: String((e as Error)?.message ?? e).slice(0, 200)
    })
    throw e
  } finally {
    activeCalls.delete(seq)
    broadcastActivity()
  }
}

/** 实际网络调用（原 chatCompletion 主体固定单一配置版；额外透传上游 usage） */
async function chatWithConfig(
  cfg: LlmConfig,
  opts: ChatOptions
): Promise<{ content: string; usage?: { prompt_tokens?: number; completion_tokens?: number } }> {
  const url = normalizeChatUrl(cfg.apiUrl)
  const body: Record<string, unknown> = {
    model: cfg.model,
    messages: opts.messages,
    stream: false
  }
  if (opts.temperature != null) body.temperature = opts.temperature
  if (opts.jsonMode) body.response_format = { type: 'json_object' }

  let res: Response
  for (let attempt = 0; ; attempt++) {
    ensureNotCancelled(opts.signal)
    try {
      res = await fetch(url, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${cfg.apiKey}`
        },
        body: JSON.stringify(body),
        signal: opts.signal
      })
    } catch (e) {
      if (opts.signal?.aborted) throw new Error('已取消')
      throw new Error(`网络请求失败：${(e as Error).message}`)
    }
    // 429 账号并发/限流：尊重 Retry-After 头，否则指数退避 2s/4s/8s 后自动重试
    if (res.status !== 429 || attempt >= RATE_LIMIT_RETRIES) break
    const retryAfter = Number(res.headers.get('retry-after'))
    const backoff = [2000, 4000, 8000]
    const waitMs =
      Number.isFinite(retryAfter) && retryAfter > 0
        ? Math.min(retryAfter * 1000, 15_000)
        : (backoff[attempt] ?? 8000)
    await sleepOrAbort(waitMs, opts.signal)
  }
  if (!res.ok) {
    const text = await res.text().catch(() => '')
    throw new Error(`LLM 服务返回 ${res.status}：${text.slice(0, 300)}`)
  }
  const data = (await res.json()) as {
    choices?: { message?: { content?: string } }[]
    usage?: { prompt_tokens?: number; completion_tokens?: number }
  }
  const content = data?.choices?.[0]?.message?.content
  if (typeof content !== 'string') throw new Error('LLM 返回格式异常（无 choices[0].message.content）')
  return { content, usage: data?.usage }
}

/** 退避等待：与 signal abort race（原样） */
function sleepOrAbort(ms: number, signal?: AbortSignal): Promise<void> {
  if (!signal) return new Promise((r) => setTimeout(r, ms))
  if (signal.aborted) return Promise.reject(new Error('已取消'))
  return new Promise((resolve, reject) => {
    const t = setTimeout(() => {
      signal.removeEventListener('abort', onAbort)
      resolve()
    }, ms)
    const onAbort = (): void => {
      clearTimeout(t)
      reject(new Error('已取消'))
    }
    signal.addEventListener('abort', onAbort, { once: true })
  })
}

// normalizeModelsUrl / listUpstreamModels / testLlmConnection 三函数原样保留，不路由不记账
```

（`normalizeModelsUrl`、`listUpstreamModels`、`testLlmConnection` 从现文件原样复制保留。）

- [ ] **Step 2: 验证**：`npm run typecheck` 通过。此时行为与旧版等价（无人传 scene、无人设 maxConcurrent → 永不溢出、记账默默开始落库）。

---

### Task 4: 27 处调用点 scene 标注

**Files:**
- Modify: `electron/ipc.ts`（1 处）、`electron/ai/mcpResearch.ts`（3 处）、`electron/ai/services.ts`（22 处）、`electron/services/feed.ts`（1 处）

- [ ] **Step 1: 按下表在每处的 `chatCompletion({...})` 选项对象里加一行 `scene: '<值>'`**（三种代码形态示例见后）：

| 文件:行 | 所属函数 | scene 值 |
|---|---|---|
| ipc.ts:945 | zhijiji createQuestion AI 初始化 | `zhijiji:v0` |
| mcpResearch.ts:146 | MCP 研究 | `mcp:research` |
| mcpResearch.ts:189 | MCP 研究 | `mcp:research` |
| mcpResearch.ts:241 | MCP 研究 | `mcp:research` |
| services.ts:109 | compactAiSession | `ai:compact` |
| services.ts:259/267 | chatWithProfileLookup（首轮+补画像轮） | `ai:chat` |
| services.ts:425 | generateMottos callAndParse | `motto:generate` |
| services.ts:777 | generateInspirations 发散 | `inspiration:diverge` |
| services.ts:815 | generateInspirations 自评 | `inspiration:refine` |
| services.ts:872 | refineInspiration | `inspiration:refine` |
| services.ts:912 | copilotWriting | `wenbi:copilot` |
| services.ts:955 | suggestWikiTerm | `wiki:suggest` |
| services.ts:989 | generateWikiCard | `wiki:card` |
| services.ts:1077 | generateWikiQuiz | `wiki:quiz` |
| services.ts:1108 | runVerification 关键词步 | `verify:check` |
| services.ts:1142 | runVerification 分析步 | `verify:check` |
| services.ts:1450 | composeSoups（箭头包装） | `reasoning:soup-compose` |
| services.ts:1490 | reviewSoup | `reasoning:soup-review` |
| services.ts:1594 | backfillTrickNotes | `reasoning:soup-backfill` |
| services.ts:1658 | judgeSoupQuestion | `reasoning:soup-ask` |
| services.ts:1710 | judgeSoupGuess | `reasoning:soup-guess` |
| services.ts:1753 | soupReview（终局点评/复盘） | `reasoning:soup-report` |
| services.ts:1862 | composeWallPuzzle | `reasoning:wall-compose` |
| services.ts:1892 | verifyWallPuzzle | `reasoning:wall-review` |
| services.ts:1960 | judgeWallAnswer | `reasoning:wall-judge` |
| feed.ts:356 | 文章 AI 总结 | `feed:summary` |

三种形态的写法（行号以当前文件为准，重构后会漂移，按函数名定位）：

```ts
// 形态一：await 直接调用（如 services.ts:1862 composeWallPuzzle）
const res = await chatCompletion({
  messages: [{ role: 'user', content: prompt }],
  temperature: 0.9,
  jsonMode: true,
  scene: 'reasoning:wall-compose',
  signal
})

// 形态二：箭头包装（如 services.ts:425 generateMottos 的 callAndParse）
const call = () =>
  chatCompletion({
    messages: [{ role: 'user', content: prompt }],
    temperature: 0.9,
    jsonMode: true,
    scene: 'motto:generate',
    signal
  })

// 形态三：变参透传（services.ts:254 chatWithProfileLookup）——req 类型加 scene 字段，
// 两处 chatCompletion(req)/chatCompletion({...req, messages}) 自动携带：
async function chatWithProfileLookup(req: {
  messages: { role: 'system' | 'user' | 'assistant'; content: string }[]
  temperature: number
  signal?: AbortSignal
  scene?: string
}): Promise<{ content: string }> {
  const first = await chatCompletion(req)   // scene 已在 req 内
  ...
```

（形态三调用方 `aiChat` 传入处加 `scene: 'ai:chat'`。）

- [ ] **Step 2: 验证**：`npm run typecheck` 通过。抽查：`grep -c "scene: 'reasoning" electron/ai/services.ts` 应为 **9**（wall 3 + soup 6，含 soup-backfill）；全仓 scene 标注共 27 处。

---

### Task 5: llmUsage:stats IPC + preload + api.d.ts（三处同步）

**Files:**
- Modify: `electron/ipc.ts`（llm:test / llm:models handler 附近）
- Modify: `electron/preload.ts`（llm 桥对象之后加 llmUsage 桥）
- Modify: `src/renderer/api.d.ts`（Api 接口 llm 段之后加 llmUsage 段）

- [ ] **Step 1: ipc.ts 新增 handler**（放在 `llm:test`/`llm:models` 同区）：

```ts
  // ---------- AI 使用统计（260910 推理角效率优化） ----------
  ipcMain.handle('llmUsage:stats', (_e, range: 'today' | 'month' | 'all') => {
    const now = new Date()
    let since: string | null = null
    if (range === 'today') {
      since = new Date(now.getFullYear(), now.getMonth(), now.getDate()).toISOString()
    } else if (range === 'month') {
      since = new Date(now.getFullYear(), now.getMonth(), 1).toISOString()
    }
    const sql = `SELECT scene,
        COUNT(*) AS calls,
        SUM(CASE WHEN ok = 0 THEN 1 ELSE 0 END) AS failures,
        CAST(AVG(duration_ms) AS INTEGER) AS avg_ms,
        SUM(prompt_tokens + completion_tokens) AS tokens,
        SUM(tokens_estimated) AS est_rows
      FROM llm_usage${since ? ' WHERE created_at >= ?' : ''}
      GROUP BY scene ORDER BY tokens DESC`
    const rows = (
      since ? getDb().prepare(sql).all(since) : getDb().prepare(sql).all()
    ) as {
      scene: string
      calls: number
      failures: number
      avg_ms: number
      tokens: number
      est_rows: number
    }[]
    const mapped = rows.map((r) => ({
      scene: r.scene,
      calls: r.calls,
      failures: r.failures,
      avgMs: r.avg_ms,
      tokens: r.tokens,
      estRows: r.est_rows
    }))
    return {
      totals: {
        calls: mapped.reduce((n, r) => n + r.calls, 0),
        failures: mapped.reduce((n, r) => n + r.failures, 0),
        tokens: mapped.reduce((n, r) => n + r.tokens, 0)
      },
      rows: mapped
    }
  })
```

（`getDb` 在 ipc.ts 已有导入；返回结构即 `LlmUsageStats`。）

- [ ] **Step 2: preload.ts 加桥**（`llm: { test, models }` 对象之后）：

```ts
  llmUsage: {
    /** AI 使用统计（260910）：range = today | month | all */
    stats: (range: 'today' | 'month' | 'all'): Promise<import('../src/shared/types').LlmUsageStats> =>
      ipcRenderer.invoke('llmUsage:stats', range),
    /** AI 实时活动（在途调用起止广播；空闲 items 为空数组） */
    onActivity: (cb: (payload: { items: { scene: string; configName: string }[] }) => void): (() => void) => {
      const listener = (_e: unknown, payload: { items: { scene: string; configName: string }[] }): void => {
        cb(payload)
      }
      ipcRenderer.on('llm:activity', listener)
      return () => ipcRenderer.removeListener('llm:activity', listener)
    }
  },
```

- [ ] **Step 3: api.d.ts 的 Api 接口加同构类型**（llm 段之后）：

```ts
  llmUsage: {
    /** AI 使用统计（260910）：range = today | month | all */
    stats(range: 'today' | 'month' | 'all'): Promise<import('../shared/types').LlmUsageStats>
    /** AI 实时活动（在途调用起止广播；空闲 items 为空数组） */
    onActivity(cb: (payload: { items: { scene: string; configName: string }[] }) => void): () => void
  }
```

- [ ] **Step 4: 验证**：`npm run typecheck` 通过。

---

### Task 6: 补充泵并行化（顶层双路 + 题池配对）

**Files:**
- Modify: `electron/services/reasoningStock.ts`

- [ ] **Step 1: 用下述代码替换 `pumpPuzzles`、`ensureReasoningStock` 两函数，并新增 `pickPairSpecs`/`buildAvoidList` 辅助，删除不再使用的 `randomOf`**：

```ts
/** 并列稀缺随机化：先洗牌再稳定排序，等计数组内顺序随机（保持原单道 randomOf 语义的配对版） */
function shuffled<T>(list: readonly T[]): T[] {
  return list.slice().sort(() => Math.random() - 0.5)
}

/** 配对挑选（互异）：难度 = 池内最稀缺档、题型 = 池内 + 近 2 日最少见（口径与原单道一致），
 *  两道强制难度与题型都不同（防两路读到同一稀缺档各出同构题） */
function pickPairSpecs(need: number): { difficulty: 'easy' | 'medium' | 'hard'; type: WallPuzzleType }[] {
  const d = getDb()
  const diffCount: Record<string, number> = { easy: 0, medium: 0, hard: 0 }
  for (const r of d
    .prepare('SELECT difficulty, COUNT(*) AS n FROM wall_pool GROUP BY difficulty')
    .all() as { difficulty: string; n: number }[]) {
    diffCount[r.difficulty] = r.n
  }
  const diffs = shuffled(['easy', 'medium', 'hard'] as const).sort(
    (a, b) => diffCount[a] - diffCount[b]
  )
  const typeCount = new Map<string, number>(WALL_TYPE_LIST.map((t) => [t, 0]))
  for (const r of d
    .prepare('SELECT puzzle_type, COUNT(*) AS n FROM wall_pool GROUP BY puzzle_type')
    .all() as { puzzle_type: string; n: number }[]) {
    typeCount.set(r.puzzle_type, (typeCount.get(r.puzzle_type) ?? 0) + r.n)
  }
  // 近 2 日已出题型计入（与 ensureToday 题型轮换同口径：转正时也优先避开）
  for (const r of d
    .prepare('SELECT puzzle_type FROM wall_puzzles ORDER BY date DESC LIMIT 2')
    .all() as { puzzle_type: string }[]) {
    typeCount.set(r.puzzle_type, (typeCount.get(r.puzzle_type) ?? 0) + 1)
  }
  const types = shuffled(WALL_TYPE_LIST).sort(
    (a, b) => (typeCount.get(a) ?? 0) - (typeCount.get(b) ?? 0)
  )
  return Array.from({ length: need }, (_, i) => ({
    difficulty: diffs[i % diffs.length],
    type: types[i % types.length]
  }))
}

/** 避免清单快照：近 20 题 wall_puzzles 题面摘要 + 池内全部题面摘要（配对前取一次，两路共用） */
function buildAvoidList(): string[] {
  const d = getDb()
  const avoid = (
    d.prepare('SELECT puzzle_text FROM wall_puzzles ORDER BY date DESC LIMIT 20').all() as {
      puzzle_text: string
    }[]
  ).map((r) => summarize(r.puzzle_text))
  avoid.push(
    ...(d.prepare('SELECT puzzle_text FROM wall_pool').all() as { puzzle_text: string }[]).map((r) =>
      summarize(r.puzzle_text)
    )
  )
  return avoid
}

/** 补题：池数 < PUZZLE_LOW 时**两道并行**补到目标（260910 效率优化）。
 *  单道非瞬态失败不废整泵（另一道照常入库，错误携带到循环结束再上抛走现有 warn 路径）；
 *  整配对全败 = 零进展，立即上抛防死循环。 */
async function pumpPuzzles(): Promise<void> {
  let carried: unknown = null
  while (poolCount() < PUZZLE_TARGET) {
    const specs = pickPairSpecs(Math.min(2, PUZZLE_TARGET - poolCount()))
    const avoid = buildAvoidList()
    const results = await Promise.allSettled(
      specs.map((s) =>
        retryTransient('题池生成', () => generateWallPuzzle(s.difficulty, { type: s.type, avoid }))
      )
    )
    let failed = 0
    let firstErr: unknown = null
    results.forEach((r) => {
      if (r.status === 'fulfilled') {
        const draft = r.value
        getDb()
          .prepare(
            'INSERT INTO wall_pool (title, puzzle_text, answer_standard, standard_reasoning, hints, puzzle_type, difficulty, created_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?)'
          )
          .run(
            draft.title ?? '',
            draft.puzzle,
            draft.answer,
            draft.reasoning,
            JSON.stringify(draft.hints),
            draft.type,
            draft.difficulty,
            nowIso()
          )
        notifyStockChanged()
        console.info(`[reasoningStock] 题池 +1（${draft.difficulty}/${draft.type}，现 ${poolCount()}）`)
      } else {
        failed++
        if (firstErr === null) firstErr = r.reason
      }
    })
    if (failed === specs.length) throw firstErr // 整对全败：零进展立即跳出（同原单道失败语义）
    if (firstErr !== null && carried === null) carried = firstErr // 单道失败：继续补，结束再上抛
  }
  if (carried !== null) throw carried
}
```

`ensureReasoningStock` 中两路改并发（`pumping` 单例、LLM 未配置检查、日志全保留）：

```ts
    console.info(`[reasoningStock] 补充开始：汤库 fresh ${soups}/${SOUP_LOW} · 题池 ${puzzles}/${PUZZLE_LOW}（目标各 ${SOUP_TARGET}/${PUZZLE_TARGET}）`)
    // 双路并行（260910 效率优化）：题池与汤库同时补；两路各自容错互不阻断（原语义保留）
    await Promise.all([
      pumpPuzzles().catch((e) => {
        console.warn('[reasoningStock] 题池补充中断：', e)
        // 通道类错误（过载/瞬断）→ 自愈重排，几分钟后自动再试；质量类失败等下次用户侧触发
        if (isTransientLlmError(e)) schedulePostponeReRun()
      }),
      pumpSoups().catch((e) => {
        console.warn('[reasoningStock] 汤库补充中断：', e)
        if (isTransientLlmError(e)) schedulePostponeReRun()
      })
    ])
    console.info(`[reasoningStock] 补充结束：汤库 fresh ${freshSoupCount()} · 题池 ${poolCount()}`)
```

同时更新文件头部注释第 3 行附近：补充泵现为**题池/汤库双路并行、题池内两道并行**。

- [ ] **Step 2: 验证**：`npm run typecheck` 通过（`randomOf` 删除后无引用报错即确认干净）。

---

### Task 7: generateSoups 首轮审题并行

**Files:**
- Modify: `electron/ai/services.ts`（generateSoups 内 `// 逐碗串行：审题…` 至 `for (const draft of drafts) {...}` 整段）

- [ ] **Step 1: 用下述代码替换原「逐碗串行审题 + 重出」循环**（`MAX_REDO` 常量与 `finals` 声明保留；prompt 构造、审题内容、打回上限语义零改动——首轮 3 碗并行（半盲审题互相独立），打回重出保持串行（互避清单正确性））：

```ts
  // 首轮审题并行（260910 效率优化）：审题互相独立（半盲、各看各题）；
  // 打回重出保持串行——重出要带「同批已保留碗」互避清单，串行才能保证清单正确。
  // 首轮即过（多数情况）时零重出等待；此改动对「来 3 碗汤」手动入口同样生效。
  const firstReviews = await Promise.all(
    drafts.map((draft) =>
      reviewSoupSafe(
        draft,
        {
          recentTricks,
          peerTricks: drafts.filter((o) => o !== draft).map((o) => o.trick_note),
          requiredDifficulty
        },
        signal
      )
    )
  )
  const redos: { draft: TurtleSoupDraft; target: 'easy' | 'medium' | 'hard'; problems: string[] }[] = []
  drafts.forEach((draft, i) => {
    const review = firstReviews[i]
    if (!review) return // 审题通道两次故障 → 弃碗
    if (review.qualityOk && review.difficultyOk) {
      finals.push({ soup: draft, difficulty: review.ratedDifficulty })
      return
    }
    // 首轮未过（质量或难度）→ 串行重出（原 attempt 0 语义；难度不符在重出轮放宽）
    redos.push({ draft, target: requiredDifficulty ?? review.ratedDifficulty, problems: review.problems })
  })

  const MAX_REDO = 2
  for (const r of redos) {
    ensureNotCancelled(signal)
    let current = r.draft
    let target = r.target
    let problems = r.problems
    for (let attempt = 1; attempt <= MAX_REDO; attempt++) {
      // 重出：random 模式补位档 = 审题人重评档（维持批内错开）；指定模式 = 偏好档；
      // 携当次问题清单与已保留碗 trick_note（批内互避），不给原碗内容（防锚定修补）
      try {
        const [redone] = await composeSoups(
          buildSoupPrompt({
            count: 1,
            difficultyText: `本碗按「${DIFF_ZH[target]}」难度出题`,
            avoidList,
            recentTrickList,
            redoProblems: problems,
            keepTricks: finals.map((f) => f.soup.trick_note)
          }),
          1,
          signal
        )
        current = redone
      } catch (e) {
        // 取消异常已在 composeSoups 内先抛「已取消」，此处如实上抛不被吞
        if (signal?.aborted) throw e
        break // 重出失败 → 弃碗
      }
      const review2 = await reviewSoupSafe(
        current,
        { recentTricks, peerTricks: finals.map((f) => f.soup.trick_note), requiredDifficulty },
        signal
      )
      if (!review2) break // 弃碗
      if (review2.qualityOk) {
        // 重出过的碗（attempt≥1）复审无硬伤即按重评档如实落库（260907 二调口径）
        finals.push({ soup: current, difficulty: review2.ratedDifficulty })
        break
      }
      problems = review2.problems
      target = requiredDifficulty ?? review2.ratedDifficulty
    }
  }
```

注意：原循环上方的 `const finals: ... = []` 声明保留在原处（首轮审题结果先入 finals，redos 引用它）；`MAX_REDO` 原声明位置在循环前，按上面代码随段迁移即可，勿重复声明。

- [ ] **Step 2: 验证**：`npm run typecheck` 通过；`grep -n "逐碗串行" electron/ai/services.ts` 无结果（旧注释已换）。

---

### Task 8: 个人档 LLM 表单 —— 并发上限输入

**Files:**
- Modify: `src/modules/profile/ProfileModule.tsx`（LLM 表单，约 741-780 行 `llmForm && (...)` 弹层内）

- [ ] **Step 1: model 输入行之后追加并发上限输入**：

```tsx
<input
  className="field"
  type="number"
  min={1}
  placeholder="并发上限（选填，如 3；留空不限，满载不溢出）"
  value={llmForm.maxConcurrent ?? ''}
  onChange={(e) => {
    const v = e.target.value.trim()
    setLlmForm({
      ...llmForm,
      maxConcurrent: v && Number(v) > 0 ? Math.floor(Number(v)) : undefined
    })
  }}
/>
```

（保存路径无需改动——表单对象整体 JSON 落 `settings.llm_configs`，`undefined` 字段序列化时自然省略。）

- [ ] **Step 2: 验证**：`npm run typecheck` 通过。

---

### Task 9: 个人档「AI 使用」统计区

**Files:**
- Modify: `src/modules/profile/ProfileModule.tsx`（LLM 配置 zone `</section>` 与 MCP zone 之间插入新 zone）
- Modify: `src/App.css`（文件末尾追加样式）

- [ ] **Step 1: 组件顶部加状态与加载逻辑**（与其他 state 并列；`LlmUsageStats`/`LLM_SCENE_LABELS` 从 `../../shared/types` 导入）：

```tsx
  const [usageRange, setUsageRange] = useState<'today' | 'month' | 'all'>('today')
  const [usage, setUsage] = useState<LlmUsageStats | null>(null)
  const [usageLoading, setUsageLoading] = useState(false)
  const loadUsage = useCallback(async () => {
    setUsageLoading(true)
    try {
      setUsage(await window.api.llmUsage.stats(usageRange))
    } catch {
      /* 静默保留上次数据 */
    } finally {
      setUsageLoading(false)
    }
  }, [usageRange])
  useEffect(() => {
    void loadUsage()
  }, [loadUsage])
  // AI 空闲时自动刷新（后台生成结束数字即新）
  useEffect(() => {
    return window.api.llmUsage.onActivity((p) => {
      if (p.items.length === 0) void loadUsage()
    })
  }, [loadUsage])
  const fmtTokens = (n: number): string => (n >= 10000 ? `${(n / 10000).toFixed(1)}万` : String(n))
```

- [ ] **Step 2: LLM 配置 zone 之后插入统计 zone**：

```tsx
      {/* AI 使用统计（260910 推理角效率优化） */}
      <section className="zone">
        <div className="zone-header">
          <span>AI 使用</span>
          <div className="zone-actions llm-usage-range">
            {(['today', 'month', 'all'] as const).map((r) => (
              <button
                key={r}
                className={`btn chip${usageRange === r ? ' active' : ''}`}
                onClick={() => setUsageRange(r)}
              >
                {r === 'today' ? '今日' : r === 'month' ? '本月' : '全部'}
              </button>
            ))}
            <button className="btn" title="刷新" onClick={() => void loadUsage()} disabled={usageLoading}>
              <span className="material-symbols-outlined">refresh</span>
            </button>
          </div>
        </div>
        <div className="zone-body">
          {usage && (
            <>
              <div className="llm-usage-cards">
                <div className="llm-usage-card">
                  <div className="llm-usage-num">{usage.totals.calls}</div>
                  <div className="llm-usage-sub">调用次数（失败 {usage.totals.failures}）</div>
                </div>
                <div className="llm-usage-card">
                  <div className="llm-usage-num">{fmtTokens(usage.totals.tokens)}</div>
                  <div className="llm-usage-sub">token 消耗</div>
                </div>
              </div>
              {usage.rows.length === 0 ? (
                <div className="empty-state">
                  <span className="material-symbols-outlined">bar_chart</span>
                  该时间范围还没有 AI 调用
                </div>
              ) : (
                <table className="llm-usage-table">
                  <thead>
                    <tr>
                      <th>场景</th>
                      <th>次数</th>
                      <th>失败</th>
                      <th>平均耗时</th>
                      <th>token</th>
                    </tr>
                  </thead>
                  <tbody>
                    {usage.rows.map((r) => (
                      <tr key={r.scene}>
                        <td>{LLM_SCENE_LABELS[r.scene] ?? r.scene}</td>
                        <td>{r.calls}</td>
                        <td>{r.failures || '-'}</td>
                        <td>
                          {r.avgMs >= 1000 ? `${(r.avgMs / 1000).toFixed(1)}s` : `${r.avgMs}ms`}
                        </td>
                        <td>
                          {fmtTokens(r.tokens)}
                          {r.estRows > 0 ? ' ≈' : ''}
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              )}
            </>
          )}
        </div>
      </section>
```

- [ ] **Step 3: App.css 末尾追加样式**（若变量名与实际主题变量有出入，以 App.css 既有变量为准就近替换——`--color-primary-soft` 等先例见 `.dialog-title-tag`）：

```css
/* ===== AI 使用统计（260910）：个人档 zone 内卡片/表格/范围 pill ===== */
.llm-usage-range {
  display: flex;
  gap: 4px;
  align-items: center;
}
.llm-usage-range .btn.chip {
  padding: 2px 10px;
  font-size: 12px;
}
.llm-usage-range .btn.chip.active {
  background: var(--color-primary);
  color: var(--color-primary-deep);
  border-color: var(--color-primary-deep);
}
.llm-usage-cards {
  display: flex;
  gap: 10px;
  margin-bottom: 10px;
}
.llm-usage-card {
  flex: 1;
  border: 1px solid var(--color-border);
  border-radius: 10px;
  padding: 10px 14px;
  background: var(--color-surface-strong);
}
.llm-usage-num {
  font-size: 20px;
  font-weight: 600;
  color: var(--color-primary-deep);
}
.llm-usage-sub {
  font-size: 12px;
  opacity: 0.7;
  margin-top: 2px;
}
.llm-usage-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}
.llm-usage-table th,
.llm-usage-table td {
  text-align: left;
  padding: 5px 8px;
  border-bottom: 1px solid var(--color-border);
}
.llm-usage-table th {
  font-size: 12px;
  opacity: 0.65;
}
```

- [ ] **Step 4: 验证**：`npm run typecheck` 通过。

---

### Task 10: AI 实时活动指示组件 + 挂载

**Files:**
- Create: `src/components/LlmActivity.tsx`
- Modify: `src/App.tsx`（sidebar 内 `<div className="sidebar-spacer" />` 之后）
- Modify: `src/App.css`（末尾追加）

- [ ] **Step 1: 新建 `src/components/LlmActivity.tsx`**：

```tsx
// AI 实时活动指示（260910 推理角效率优化）：主进程 llm:activity 广播驱动，
// 空闲（items 空）不渲染；同场景聚合计数（如「推理角·出题 ×2」）。纯指示不可点。
import { useEffect, useState } from 'react'
import { LLM_SCENE_LABELS } from '../shared/types'

interface ActivityItem {
  scene: string
  configName: string
}

export default function LlmActivity() {
  const [items, setItems] = useState<ActivityItem[]>([])
  useEffect(() => {
    return window.api.llmUsage.onActivity((payload) => setItems(payload.items))
  }, [])
  if (items.length === 0) return null
  const counts = new Map<string, number>()
  for (const it of items) counts.set(it.scene, (counts.get(it.scene) ?? 0) + 1)
  const text = [...counts.entries()]
    .map(([scene, n]) => `${LLM_SCENE_LABELS[scene] ?? scene}${n > 1 ? ` ×${n}` : ''}`)
    .join(' · ')
  return (
    <div className="llm-activity" title={text}>
      <span className="llm-activity-dot" />
      <span className="llm-activity-text">{text}</span>
    </div>
  )
}
```

- [ ] **Step 2: App.tsx 挂载**（import + sidebar 底部）：

```tsx
import LlmActivity from './components/LlmActivity'
```

```tsx
          <div className="sidebar-spacer" />
          {/* AI 实时活动指示（260910）：左栏底部常驻，空闲不渲染 */}
          <LlmActivity />
        </nav>
```

- [ ] **Step 3: App.css 末尾追加**：

```css
/* ===== AI 实时活动指示（260910）：左栏底部，空闲不渲染 ===== */
.llm-activity {
  display: flex;
  align-items: center;
  gap: 6px;
  margin: 8px 10px;
  padding: 6px 10px;
  border-radius: 8px;
  background: var(--color-primary-soft);
  color: var(--color-primary-deep);
  font-size: 12px;
}
.llm-activity-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--color-primary);
  flex-shrink: 0;
  animation: llm-activity-breath 1.6s ease-in-out infinite;
}
@keyframes llm-activity-breath {
  0%,
  100% {
    opacity: 0.35;
  }
  50% {
    opacity: 1;
  }
}
.llm-activity-text {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

- [ ] **Step 4: 验证**：`npm run typecheck` 通过。

---

### Task 11: 算法验证脚本 + 全量 typecheck

**Files:**
- Create then delete: `tmp-pick-config-check.mjs`（项目根，跑完即删——第 23/27 轮一次性脚本惯例）

- [ ] **Step 1: 写验证脚本**（`pickConfigFrom` 函数体**逐字复制** Task 3 终稿——先例：第 27 轮「正则终稿与脚本逐字一致」）：

```js
// 临时脚本：验证 pickConfigFrom 路由算法（函数体逐字复制自 electron/ai/llm.ts 终稿）。用完即删。
class LlmNotConfiguredError extends Error {
  constructor() {
    super('LLM_NOT_CONFIGURED')
  }
}
function pickConfigFrom(configs, defaultId, inflightOf) {
  const def = configs.find((c) => c.id === defaultId) ?? configs[0]
  if (!def) throw new LlmNotConfiguredError()
  const cap = (c) => (typeof c.maxConcurrent === 'number' && c.maxConcurrent > 0 ? c.maxConcurrent : Infinity)
  if (inflightOf(def.id) < cap(def)) return def
  const spare = configs.filter((c) => c.id !== def.id && inflightOf(c.id) < cap(c))
  if (spare.length === 0) return def
  return spare.sort((a, b) => inflightOf(a.id) - inflightOf(b.id))[0]
}

const cfg = (id, maxConcurrent) => ({ id, name: id, apiUrl: '', apiKey: '', model: 'm', maxConcurrent })
let pass = 0
const assertEq = (label, actual, expected) => {
  const ok = actual === expected
  console.log(`${ok ? 'PASS' : 'FAIL'} ${label}${ok ? '' : `（实际 ${actual?.id ?? actual}，期望 ${expected?.id ?? expected}）`}`)
  if (ok) pass++
}
// 案例池：pool = glm 上限 2 / gpt 上限 3 / cli 不限；capped 全员有上限（「全部满载」场景用）。
// 断言用 === 比较，期望值必须取池内同一引用。
const pool = [cfg('glm', 2), cfg('gpt', 3), cfg('cli')]
const capped = [cfg('glm', 2), cfg('gpt', 3), cfg('cli', 1)]
const run = (configs, inflight) => pickConfigFrom(configs, 'glm', (id) => inflight[id] ?? 0)

assertEq('默认未满 → 默认', run(pool, { glm: 1 }), pool[0])
assertEq('默认满 → 溢出到在途最低备选（cli 0 < gpt 1）', run(pool, { glm: 2, gpt: 1, cli: 0 }), pool[2])
assertEq('备选满载剔除（gpt 满，只剩 cli 可选）', run(pool, { glm: 2, gpt: 3, cli: 1 }), pool[2])
assertEq('全部满载 → 仍回默认（不排队）', run(capped, { glm: 2, gpt: 3, cli: 1 }), capped[0])
const noCap = [cfg('glm'), cfg('gpt')]
assertEq('上限缺省 = 不限 → 在途 99 也不溢出', pickConfigFrom(noCap, 'glm', () => 99), noCap[0])
try {
  pickConfigFrom([], '', () => 0)
  console.log('FAIL 空配置应抛 LlmNotConfiguredError')
} catch (e) {
  assertEq('空配置 → 抛 LlmNotConfiguredError', e.constructor.name, 'LlmNotConfiguredError')
}
console.log(pass === 6 ? `\nALL PASS (${pass}/6)` : `\n有失败 (${pass}/6)`)
if (pass < 6) process.exit(1)
```

- [ ] **Step 2: 跑脚本**：`node tmp-pick-config-check.mjs` → 输出 `ALL PASS (6/6)`。

- [ ] **Step 3: 删除脚本**：`rm tmp-pick-config-check.mjs`。

- [ ] **Step 4: 全量验证**：`npm run typecheck` 通过（如报疑似旧诊断，删 `*.tsbuildinfo` 后重跑）。

---

### Task 12: 冒烟清单 + 开发日志 + 归档

**Files:**
- Create/Modify: `docs/log/260910.md`（每小节标题下 `> 创建于 YYYY-MM-DD HH:MM`，时间用 `date '+%Y-%m-%d %H:%M'` 实取）
- Modify: `docs/project/优化建议区.md`（待完成条目移入归档区，新增「### 第31轮（260910）：推理角出题效率与 LLM 混用记账」小节，插归档区顶部）
- Move: 设计与计划两文档移入 `docs/project/左侧边栏/推理角/archive/`（落地后归档规则）

- [ ] **Step 1: 开发日志**：记录本轮实施内容（实测基线、路由/记账/并行/统计/指示五件套、涉及文件、typecheck 结果、待冒烟），遵守创建时间行规则。

- [ ] **Step 2: 优化建议区归档**：把「待完成」里本轮条目（推理角出题速度慢 + token 不可见）打勾并写入第 31 轮小节（保留原始描述 + 完成说明），小节插归档区顶部。

- [ ] **Step 3: 文档归档**：`2026-09-10-推理角出题效率与LLM混用记账-design.md` 与本计划文档移入 `archive/` 子目录。

- [ ] **Step 4: 冒烟清单**（需重启 dev 触发 DB v27 迁移；交开发者执行）：
  1. 启动后空库/低库存触发补充泵：左栏底部出现「AI · 推理角·出题 ×2」类指示，结束归零消失。
  2. 个人档 → AI 使用：今日范围出现分场景行（推理角·出题/审题等），token 非 0；切今日/本月/全部正常。
  3. glm 配置并发上限设 1，触发「来 3 碗汤」+ 每日一题：统计页出现 gpt 行（溢出生效），观察混用。
  4. 清空 wall_pool + 汤库 fresh 后计时建池：对比提速（预期显著）。
  5. 「来 3 碗汤」成功率与质量无回归（打回重出仍生效）；汤库列表渐进刷新正常。
  6. 取消一次生成（停止键）：活动指示归零、无残留计数。
  7. LLM 配置删除到只剩一个 + 不设上限：行为与旧版一致（不溢出）。

---

## Self-Review 记录

- **Spec 覆盖**：设计 §1→Task 3/8；§2→Task 1/2/3/4/5；§3→Task 6/7；§4→Task 5/9；§5→Task 3/10；边界容错→Task 3（finally 记账减计数）/Task 5/11；验收要点→Task 11/12。无遗漏。
- **占位符**：无（Task 11 验证脚本经二次修订为可直接照抄的完整版本，6 断言全覆盖路由语义）。
- **类型一致性**：`LlmUsageStats`/`LlmUsageSceneRow`（Task 2 定义，Task 5 IPC 返回、Task 9 渲染消费）；`pickConfigFrom`（Task 3 定义、Task 11 逐字复制）；`llmUsage` 桥三处（ipc/preload/api.d.ts）签名一致；`maxConcurrent`（Task 2 定义、Task 3 路由消费、Task 8 表单写入）。
- **已知偏差**：设计文档估 25 处调用点，实际 27 处（已勘误入设计文档）；汤批审题的 `MAX_REDO` 语义在并行化重构中保持（首轮 attempt 0 + 重出 1..2）。
