# 海龟汤生成质量优化 Implementation Plan

> **状态：已归档（260907 六任务全部执行完毕 + 终审 Approved，待人工冒烟）**。设计 spec：`docs/project/左侧边栏/推理角/archive/2026-09-07-海龟汤质量优化-design.md`；执行记录见 `docs/log/260907.md`。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 依据 `docs/project/左侧边栏/推理角/archive/2026-09-07-海龟汤质量优化-design.md`，把 `generateSoups` 升级为「出题标准 v2 + 逐碗并行审题」两阶段管线，并以 `trick_note`（核心诡计一句话）实现跨批避免同构。

**Architecture:** 出题一趟产 3 碗（新四条质量标准 + 巧思总纲，产出 trick_note）→ 3 趟审题并行（半盲六查，温度 0.2）→ 不合格碗携问题清单重出一次、复审 → 合格碗以审题人难度评定落库（弃碗不弃批，3 碗全灭才抛错）。启动后一次性 LLM 回填存量汤的 trick_note。

**Tech Stack:** Electron 44 主进程（node:sqlite），无新依赖。

---

## 仓库纪律（执行前必读）

1. **禁止 AI 执行任何 git 命令**（本项目 CLAUDE.md 硬性规则）。每个任务末尾的「提交点」只把建议提交信息展示给开发者，由开发者自行提交。
2. **本项目无测试基建**（package.json 无 test script，不要引入测试框架）。每个任务的自动验证 = `npm run typecheck`；计划末尾 `npm run smoke` + 开发者手动验收（出题链路依赖真实 LLM，只能实机验证）。
3. **DB 版本**：当前 `user_version = 14`（草稿本已占 v14；题库预生成 spec 里「v14」的表述已过期）。本计划迁移占 **v15**。若题库预生成 spec 恰好先落地占了 v15，本迁移改为 v16（`if (version < 16)`，其余不变）。
4. 所有 prompt 文本为中文，与文件内既有 prompt 风格一致；不要改动本计划未提到的函数。

## 文件结构总览

| 文件 | 职责 |
| --- | --- |
| `electron/db/db.ts` | v15 迁移：`turtle_soups` 加 `trick_note` 列 |
| `electron/ai/services.ts` | 出题 v2（draft 类型/解析/组 prompt/调用）、审题 `reviewSoup`、`generateSoups` 管线重写、`backfillTrickNotes` 回填 |
| `electron/main.ts` | 启动延迟 10s 触发回填（一次性 fire-and-forget） |
| `src/modules/reasoning/TurtlePanel.tsx` | loading 文案一句 |
| `docs/project/左侧边栏/推理角/designs-specs.md` | 实施后同步增量（AI 维护） |
| `docs/log/260907.md` | 开发日志追加 |

背景参考（执行时不必重读）：spec 全文在 `docs/project/左侧边栏/推理角/archive/2026-09-07-海龟汤质量优化-design.md`；被替换的旧代码在 `electron/ai/services.ts:1038-1123`。

---

### Task 1: DB v15 迁移——turtle_soups 加 trick_note 列

**Files:**
- Modify: `electron/db/db.ts:467`（v14 块结束的 `}` 之后、迁移函数收尾 `}` 之前）

- [ ] **Step 1: 在 v14 块后追加 v15 块**

找到 `electron/db/db.ts` 中 v14 块的结尾（约 466-468 行）：

```ts
    d.exec('PRAGMA user_version = 14')
  }
}
```

将末尾的 `}` 前插入 v15 块，改为：

```ts
    d.exec('PRAGMA user_version = 14')
  }

  if (version < 15) {
    // v15：海龟汤质量优化（海龟汤修改反馈）——汤库存「核心诡计一句话」概括（trick_note），
    // 供出题/审题注入避免清单防跨批同构；纯内部机制，不进任何 UI（防剧透）。
    // 存量回填由启动后一次性 LLM 任务完成（services.backfillTrickNotes），迁移只加列。
    d.exec('ALTER TABLE turtle_soups ADD COLUMN trick_note TEXT')
    d.exec('PRAGMA user_version = 15')
  }
}
```

（若题库预生成 spec 已先落地占了 v15，则本块写 `if (version < 16)` 并置 `user_version = 16`。）

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: 两个 tsconfig 均无错误退出（exit 0）。

- [ ] **Step 3: 提交点（开发者执行）**

建议提交信息：`feat(db): v15——turtle_soups 加 trick_note 列（核心诡计概括，内部防同构用）`

---

### Task 2: 出题 v2——draft 类型、解析、组 prompt、出题段重写（此任务完成时管线尚无审题，行为可用）

**Files:**
- Modify: `electron/ai/services.ts:1024-1123`（`TurtleSoupDraft` 接口、`generateSoups`、`parseSoupArray`）

- [ ] **Step 1: 扩展 TurtleSoupDraft，新增 trick_note 字段**

将 `electron/ai/services.ts:1024` 的接口：

```ts
export interface TurtleSoupDraft {
  title: string
  surface: string
  bottom: string
  analysis: string
  difficulty: 'easy' | 'medium' | 'hard'
  theme: string
}
```

替换为：

```ts
export interface TurtleSoupDraft {
  title: string
  surface: string
  bottom: string
  analysis: string
  difficulty: 'easy' | 'medium' | 'hard'
  theme: string
  /** 核心诡计一句话概括（如「用前一天的旧录像伪造离场记录」）——落库防跨批同构，不进 UI */
  trick_note: string
}
```

- [ ] **Step 2: 参数化 parseSoupArray 并校验 trick_note**

将现有 `parseSoupArray`（`electron/ai/services.ts:1091` 起）整体替换为：

```ts
/** 解析出汤返回（数量与三件套齐全才算合格，否则抛错不落库；出题 3 碗、打回重出 1 碗共用） */
function parseSoupArray(raw: string, expected = 3): TurtleSoupDraft[] {
  const parsed = parseJsonObject(raw)
  const arr = Array.isArray(parsed.soups)
    ? (parsed.soups as unknown[])
    : (Object.values(parsed).find((v) => Array.isArray(v)) as unknown[] | undefined)
  if (!Array.isArray(arr)) throw new Error('LLM 未返回汤数组')
  const out: TurtleSoupDraft[] = []
  for (const item of arr as Record<string, unknown>[]) {
    if (
      item &&
      typeof item.title === 'string' &&
      typeof item.surface === 'string' &&
      typeof item.bottom === 'string' &&
      typeof item.analysis === 'string' &&
      typeof item.trick_note === 'string' &&
      item.title.trim() &&
      item.surface.trim() &&
      item.bottom.trim() &&
      item.analysis.trim() &&
      item.trick_note.trim()
    ) {
      out.push({
        title: item.title.trim(),
        surface: item.surface.trim(),
        bottom: item.bottom.trim(),
        analysis: item.analysis.trim(),
        difficulty: normDifficulty(item.difficulty),
        theme: typeof item.theme === 'string' && item.theme.trim() ? item.theme.trim() : '本格',
        trick_note: item.trick_note.trim()
      })
    }
  }
  if (out.length !== expected) {
    throw new Error(`LLM 返回 ${out.length} 碗合格汤（应为 ${expected} 碗）`)
  }
  return out
}
```

- [ ] **Step 3: 新增 buildSoupPrompt（出题 prompt v2）与 composeSoups（调用+解析+重试一次）**

在 `parseSoupArray` 之后新增两个函数（完整代码）：

```ts
/** 出题 prompt v2 组装（海龟汤修改反馈：四条新标准 + 巧思总纲 + trick_note 产出 + 摘要避免清单） */
function buildSoupPrompt(opts: {
  count: number
  /** 难度要求句（如「三碗均按『中等』难度出题」/「本碗按『困难』难度出题」） */
  difficultyText: string
  avoidList: string
  recentTrickList: string
  /** 打回重出：审题人问题清单（不给原碗内容，防锚定修补——照思维墙打回惯例） */
  redoProblems?: string[]
  /** 打回重出：同批已保留碗的 trick_note（批内互避） */
  keepTricks?: string[]
}): string {
  const redoBlock =
    opts.redoProblems && opts.redoProblems.length > 0
      ? `\n## 上一次出的这碗汤被审题人打回，问题如下（重新出题必须全部规避）\n${opts.redoProblems
          .map((p) => `- ${p}`)
          .join('\n')}\n`
      : ''
  const keepBlock =
    opts.keepTricks && opts.keepTricks.length > 0
      ? `\n## 本碗需与以下同批已保留汤的核心诡计思路明显不同\n${opts.keepTricks
          .map((t) => `- ${t}`)
          .join('\n')}\n`
      : ''
  return `${profileDigest()}${profileDigest() ? '\n\n' : ''}你是一位资深海龟汤出题人，为一位喜欢推理的玩家原创出题。

## 已有汤清单（汤名与题材组合请避开，不得重复或高度雷同）
${opts.avoidList}

## 近期已用过的核心诡计思路（新汤的核心思路必须与它们明显不同）
${opts.recentTrickList}
${redoBlock}${keepBlock}
## 出题要求（严格执行）
1. 恰好原创 ${opts.count} 碗海龟汤。禁止搬运网传经典汤。好汤的标志是：真相揭示后，玩家回看汤面，发现每个寻常细节都另有含义——核心诡计应让多个寻常细节咬合成一个不寻常的真相。不设类型清单，思路不受任何分类束缚。
2. 每碗产出：title（汤名，2~6 字）、surface（汤面）、bottom（汤底）、analysis（裁判解析——把汤底展开讲透的完整背景：人物、时间线、动机、每个关键细节的因果，供裁判判答用）、trick_note（核心诡计一句话概括，15~40 字，如「用前一天的旧录像伪造离场记录」——只描述诡计思路本身，不复述案情）。
3. ${opts.difficultyText}；每碗自评难度（easy/medium/hard）并打一枚题材标签（theme，4~8 字，如「本格·罪案」「现代·亲情」「诡计·日常」）。
4. surface 80~200 字；bottom 150~400 字；analysis 200~500 字。${
    opts.count > 1 ? '\n5. 各碗的核心诡计思路（trick_note）彼此明显不同。' : ''
  }

## 难度定义（自评难度必须对照给出）
- easy：单层反转，关键线索在汤面中较显眼，玩家盘问较少即可逼近汤底
- medium：双层反转，或关键线索有伪装，需先排除一个误导方向
- hard：多层反转或强误导，关键线索全部隐性，需玩家自己想到盘问方向

## 质量标准（输出前逐碗自检，不合格的碗重写替换后再输出）
① 常识可解：解题所需知识限于日常生活常识；专业知识可作佐证、不得作解题钥匙。自检：一个观察敏锐的普通人，不查任何资料，只凭盘问能否逼近汤底？
② 对手不降智：若汤涉及对手/作案者的计划，破绽必须源于其固有盲区（信息差、无法预料的变量、成本权衡），不得是与其缜密程度不相称的低级疏忽。自检：对手再谨慎一点，这个诡计还会漏吗？若「多检查一步就不会漏」，不合格。
③ 线索自然隐藏：汤面至少 3 个可供盘问的具体事实（日期、物件、身份、动作、位置等），以平常口吻织入叙事，不得集中罗列异常；其中至多一半呈现为显性异常，至少一条是隐性线索——表面完全平常，汤底揭示后含义才反转。自检：把汤面里的异常挑出来，如果一眼能挑出全部，就不合格。
④ 汤底逐一回收：汤面出现的每个元素在汤底都有解释，无悬空元素。
⑤ 本格自洽：无超自然、无巧合堆砌，因果链在现实逻辑内成立。
⑥ 可判定性：事实链封闭，玩家的判断类问题都能明确答「是 / 否 / 与汤无关」。

以 JSON 对象返回，最外层是对象，格式：{"soups":[{"title":"...","surface":"...","bottom":"...","analysis":"...","trick_note":"...","difficulty":"easy|medium|hard","theme":"..."}]}，soups 数组内恰好 ${opts.count} 项，不要输出其他任何内容。`
}

/** 单趟出题调用 + 解析（解析失败自动重试一次，同 generateMottos 惯例；仍失败抛错不落库） */
async function composeSoups(prompt: string, count: number): Promise<TurtleSoupDraft[]> {
  const call = () =>
    chatCompletion({ messages: [{ role: 'user', content: prompt }], temperature: 0.9, jsonMode: true })
  try {
    return parseSoupArray((await call()).content, count)
  } catch {
    return parseSoupArray((await call()).content, count)
  }
}
```

- [ ] **Step 4: 重写 generateSoups（本任务只换出题段与落库，暂不接审题）**

将现有 `generateSoups` 整体替换为：

```ts
/**
 * 「来 3 碗汤」：原创出 3 碗海龟汤入库（specs §3；海龟汤修改反馈——出题标准 v2）。
 * 出题注入画像摘要 + 已有汤避免清单 + 近 9 碗诡计摘要清单（防跨批同构）。
 * （Task 3 将在此函数中接入逐碗审题管线。）
 */
export async function generateSoups(
  preference: 'random' | 'easy' | 'medium' | 'hard'
): Promise<{ generated: number; inserted: number }> {
  const d = getDb()
  const existing = d
    .prepare('SELECT title, theme_tag FROM turtle_soups ORDER BY id DESC LIMIT 200')
    .all() as { title: string; theme_tag: string }[]
  const avoidList = existing.length
    ? existing.map((r) => `- 《${r.title}》（${r.theme_tag}）`).join('\n')
    : '（暂无）'
  const recentTricks = (
    d
      .prepare('SELECT trick_note FROM turtle_soups WHERE trick_note IS NOT NULL ORDER BY id DESC LIMIT 9')
      .all() as { trick_note: string }[]
  ).map((r) => r.trick_note)
  const recentTrickList = recentTricks.length
    ? recentTricks.map((s, i) => `${i + 1}. ${s}`).join('\n')
    : '（暂无）'
  const difficultyText =
    preference === 'random'
      ? '难度不限，三碗难度错开为佳'
      : `三碗均按「${DIFF_ZH[preference]}」难度出题`

  const soups = await composeSoups(
    buildSoupPrompt({ count: 3, difficultyText, avoidList, recentTrickList }),
    3
  )

  const now = nowIso()
  const ins = d.prepare(
    "INSERT INTO turtle_soups (title, surface, bottom, analysis, difficulty, theme_tag, trick_note, status, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, 'fresh', ?, ?)"
  )
  for (const s of soups) {
    ins.run(s.title, s.surface, s.bottom, s.analysis, s.difficulty, s.theme, s.trick_note, now, now)
  }
  return { generated: soups.length, inserted: soups.length }
}
```

- [ ] **Step 5: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。`parseSoupArray` 旧调用已随函数体替换消失；若报「soup 文档/类型未用」之类错误，检查 `TurtleSoupDraft` 其他消费点是否需要补 `trick_note`（用 Grep 确认该类型只在 services.ts 内使用）。

- [ ] **Step 6: 提交点（开发者执行）**

建议提交信息：`feat(turtle): 出题 prompt v2——常识可解/反降智/线索自然/难度定义四标准 + trick_note 产出落库`

---

### Task 3: 审题函数 + generateSoups 接入逐碗并行审题管线

**Files:**
- Modify: `electron/ai/services.ts`（Task 2 后的 `composeSoups` 之后插入审题函数；`generateSoups` 函数体再次重写）

- [ ] **Step 1: 在 composeSoups 之后新增 SoupReview 接口与 reviewSoup 函数**

```ts
export interface SoupReview {
  ok: boolean
  problems: string[]
  /** 审题人独立重评的难度档（落库以此为准） */
  ratedDifficulty: 'easy' | 'medium' | 'hard'
}

/**
 * 审题（半盲验证，海龟汤修改反馈）：审题人先只读汤面以玩家视角记录第一手证据，
 * 再读汤底六查——常识可解 / 对手不降智 / 难度复核 / 线索自然 / 回收自洽可判定 / 同构。
 * 玩家指定难度偏好时，重评档与偏好档不符即不合格（代码硬校验，不信 LLM 自觉）。
 * 调用或解析失败由调用方按「弃碗」处理（故障与汤质量无关）。
 */
async function reviewSoup(
  draft: TurtleSoupDraft,
  ctx: {
    recentTricks: string[]
    peerTricks: string[]
    /** 玩家指定的难度偏好（random 模式为 undefined：重评落库但不影响合格判定） */
    requiredDifficulty?: 'easy' | 'medium' | 'hard'
  }
): Promise<SoupReview> {
  const targetLine = ctx.requiredDifficulty
    ? `本轮玩家指定了难度偏好「${DIFF_ZH[ctx.requiredDifficulty]}」：你的重评档与偏好档不符即整碗不合格。`
    : '本轮难度不限：按定义独立重评即可，重评档不影响合格判定。'
  const res = await chatCompletion({
    messages: [
      {
        role: 'user',
        content: `你是苛刻的海龟汤审题人。下面这碗汤将发给一位喜欢推理的玩家，请独立严格审查——必须自己重新推演，不得默认出题人正确。

第一步：只读【汤面】，以玩家视角记下：第一直觉的猜测方向、最想盘问的 3 个问题、一眼注意到的异常。
第二步：读【汤底】【裁判解析】与【出题人自评】，逐项审查六关。

【汤面】
${draft.surface}

【汤底】
${draft.bottom}

【裁判解析】
${draft.analysis}

【出题人自评】难度：${DIFF_ZH[draft.difficulty]}；题材：${draft.theme}；核心诡计思路：${draft.trick_note}

审查六关：
1. 常识可解：你在第一步的推理方向是否全部走在日常生活常识内？若关键一环必须用到专业知识（天文、地理、法医、化学、密码学等），不合格——专业知识可作佐证，不得作解题钥匙。
2. 对手不降智：若汤涉及对手/作案者的计划——「对手再谨慎一点，这个诡计还会漏吗？」若破绽只是「多检查一步就不会漏」式的低级疏忽，而非固有盲区（信息差、无法预料的变量、成本权衡），不合格。
3. 难度复核：对照「easy=单层反转线索较显影 / medium=双层反转或线索有伪装需排除误导 / hard=多层反转或强误导关键线索全隐性」独立重评实际难度档。${targetLine}
4. 线索自然：结合第一步「一眼注意到的异常」——若汤面的异常一眼即可全部挑出（报菜名式罗列），或不存在任何隐性线索（表面平常、汤底揭示后含义才反转的细节），不合格；同时检查汤面是否至少有 3 个可供盘问的具体事实。
5. 回收/自洽/可判定：汤面元素在汤底逐一有交代、无悬空；无巧合堆砌、因果在现实逻辑内成立；玩家的判断类问题都能明确作答。
6. 同构：核心诡计思路与「近期已用思路清单」或「同批其他汤」是否明显不同？高度同构则不合格。

## 近期已用思路清单
${ctx.recentTricks.length ? ctx.recentTricks.map((s, i) => `${i + 1}. ${s}`).join('\n') : '（暂无）'}

## 同批其他汤的核心思路
${ctx.peerTricks.length ? ctx.peerTricks.map((s) => `- ${s}`).join('\n') : '（无）'}

只输出 JSON：{"verdict":"pass|fail","ratedDifficulty":"easy|medium|hard","problems":["问题1","问题2"]}。六关全过才 pass；problems 仅在 fail 时非空。`
      }
    ],
    temperature: 0.2,
    jsonMode: true
  })
  const parsed = parseJsonObject(res.content)
  const problems = Array.isArray(parsed.problems)
    ? parsed.problems.filter((p): p is string => typeof p === 'string' && p.trim() !== '')
    : []
  const rated = normDifficulty(parsed.ratedDifficulty)
  const verdictPass = parsed.verdict === 'pass' && problems.length === 0
  const diffOk = !ctx.requiredDifficulty || rated === ctx.requiredDifficulty
  if (verdictPass && !diffOk) {
    problems.push(
      `难度复核为「${DIFF_ZH[rated]}」，与玩家指定的「${DIFF_ZH[ctx.requiredDifficulty!]}」不符`
    )
  }
  return { ok: verdictPass && diffOk, problems, ratedDifficulty: rated }
}
```

- [ ] **Step 2: generateSoups 出题段之后接入并行审题/打回/弃碗管线**

将 Task 2 版 `generateSoups` 中从 `const soups = await composeSoups(...)` 到函数结尾的部分，替换为如下完整函数体（全函数重写，直接整体替换）：

```ts
export async function generateSoups(
  preference: 'random' | 'easy' | 'medium' | 'hard'
): Promise<{ generated: number; inserted: number }> {
  const d = getDb()
  const existing = d
    .prepare('SELECT title, theme_tag FROM turtle_soups ORDER BY id DESC LIMIT 200')
    .all() as { title: string; theme_tag: string }[]
  const avoidList = existing.length
    ? existing.map((r) => `- 《${r.title}》（${r.theme_tag}）`).join('\n')
    : '（暂无）'
  const recentTricks = (
    d
      .prepare('SELECT trick_note FROM turtle_soups WHERE trick_note IS NOT NULL ORDER BY id DESC LIMIT 9')
      .all() as { trick_note: string }[]
  ).map((r) => r.trick_note)
  const recentTrickList = recentTricks.length
    ? recentTricks.map((s, i) => `${i + 1}. ${s}`).join('\n')
    : '（暂无）'
  const requiredDifficulty = preference === 'random' ? undefined : preference
  const difficultyText =
    preference === 'random'
      ? '难度不限，三碗难度错开为佳'
      : `三碗均按「${DIFF_ZH[preference]}」难度出题`

  const drafts = await composeSoups(
    buildSoupPrompt({ count: 3, difficultyText, avoidList, recentTrickList }),
    3
  )

  // 逐碗并行审题；审题调用/解析失败 = 该碗放弃（故障与汤质量无关），不阻塞其余碗
  const reviews = await Promise.allSettled(
    drafts.map((s) =>
      reviewSoup(s, {
        recentTricks,
        peerTricks: drafts.filter((o) => o !== s).map((o) => o.trick_note),
        requiredDifficulty
      })
    )
  )

  /** 最终入碗：落库难度一律以最后一次通过审题的评定为准 */
  const finals: { soup: TurtleSoupDraft; difficulty: 'easy' | 'medium' | 'hard' }[] = []
  const redoJobs: Promise<void>[] = []

  const reviewOne = async (draft: TurtleSoupDraft): Promise<SoupReview> =>
    reviewSoup(draft, {
      recentTricks,
      peerTricks: finals.map((f) => f.soup.trick_note),
      requiredDifficulty
    })

  const redoBowl = async (
    draft: TurtleSoupDraft,
    problems: string[],
    target: 'easy' | 'medium' | 'hard'
  ): Promise<void> => {
    try {
      const [redone] = await composeSoups(
        buildSoupPrompt({
          count: 1,
          difficultyText: `本碗按「${DIFF_ZH[target]}」难度出题`,
          avoidList,
          recentTrickList,
          redoProblems: problems,
          keepTricks: finals.map((f) => f.soup.trick_note)
        }),
        1
      )
      const re = await reviewOne(redone)
      if (re.ok) finals.push({ soup: redone, difficulty: re.ratedDifficulty })
      // 复审仍不过 → 弃碗不弃批
    } catch {
      // 重出/复审故障 → 弃碗
    }
  }

  for (let i = 0; i < drafts.length; i++) {
    const r = reviews[i]
    const draft = drafts[i]
    if (r.status === 'rejected') continue // 审题故障 → 弃碗
    if (r.value.ok) {
      finals.push({ soup: draft, difficulty: r.value.ratedDifficulty })
      continue
    }
    // 打回重出一次：random 模式补位档 = 审题人重评档（维持批内错开）；指定模式 = 偏好档
    const target = requiredDifficulty ?? r.value.ratedDifficulty
    redoJobs.push(redoBowl(draft, r.value.problems, target))
  }
  await Promise.all(redoJobs)

  if (finals.length === 0) throw new Error('本批 3 碗均未通过审题（重出后仍不合格），未落库')

  const now = nowIso()
  const ins = d.prepare(
    "INSERT INTO turtle_soups (title, surface, bottom, analysis, difficulty, theme_tag, trick_note, status, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, 'fresh', ?, ?)"
  )
  for (const f of finals) {
    ins.run(
      f.soup.title,
      f.soup.surface,
      f.soup.bottom,
      f.soup.analysis,
      f.difficulty,
      f.soup.theme,
      f.soup.trick_note,
      now,
      now
    )
  }
  return { generated: finals.length, inserted: finals.length }
}
```

- [ ] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

- [ ] **Step 4: 提交点（开发者执行）**

建议提交信息：`feat(turtle): 逐碗并行审题管线——半盲六查、打回重出、弃碗不弃批、审题人难度评定落库`

---

### Task 4: 存量汤诡计摘要一次性回填 + main.ts 启动挂钩

**Files:**
- Modify: `electron/ai/services.ts`（`generateSoups` 之后新增 `backfillTrickNotes`）
- Modify: `electron/main.ts:7`（import 区）与 `electron/main.ts:75`（`whenReady` 块内）

- [ ] **Step 1: services.ts 末段（`generateSoups`/审题区之后）新增回填函数**

```ts
/**
 * 存量汤诡计摘要一次性回填（海龟汤修改反馈）：启动后 fire-and-forget，
 * 全量（含已玩过/回收站软删——软删汤思路同样算「已用过」）为 trick_note 为 NULL
 * 的汤补一句核心诡计概括。失败静默（console.warn）下次启动再试；LLM 未配置跳过；
 * 跑完即无、不建定时器。
 */
export async function backfillTrickNotes(): Promise<void> {
  try {
    const d = getDb()
    const rows = d
      .prepare('SELECT id, title, bottom FROM turtle_soups WHERE trick_note IS NULL')
      .all() as { id: number; title: string; bottom: string }[]
    if (rows.length === 0) return
    const res = await chatCompletion({
      messages: [
        {
          role: 'user',
          content: `为下列海龟汤各写一句「核心诡计思路」概括（trick_note）：只描述诡计思路本身（如「用前一天的旧录像伪造离场记录」），不复述案情、不评价质量，每句 15~40 字。

${rows.map((r) => `# ${r.id}《${r.title}》\n汤底：${r.bottom}`).join('\n\n')}

以 JSON 对象返回：{"notes":[{"id":1,"trick_note":"..."}]}，数组恰好 ${rows.length} 项，id 与上表一致，不要输出其他任何内容。`
        }
      ],
      temperature: 0.2,
      jsonMode: true
    })
    const parsed = parseJsonObject(res.content)
    if (!Array.isArray(parsed.notes)) return
    const upd = d.prepare('UPDATE turtle_soups SET trick_note = ? WHERE id = ? AND trick_note IS NULL')
    for (const n of parsed.notes as Record<string, unknown>[]) {
      if (n && typeof n.id === 'number' && typeof n.trick_note === 'string' && n.trick_note.trim()) {
        upd.run(n.trick_note.trim(), n.id)
      }
    }
  } catch (e) {
    console.warn('[backfillTrickNotes] 存量汤诡计摘要回填失败，下次启动再试：', e)
  }
}
```

- [ ] **Step 2: main.ts 接线**

`electron/main.ts:7` import 区现有：

```ts
import { startSchedulers } from './services/scheduler'
```

其后新增一行：

```ts
import { backfillTrickNotes } from './ai/services'
```

`whenReady` 块内（`electron/main.ts:71-76`），在 `startSchedulers()` 与 `createWindow()` 之间插入：

```ts
    // 存量汤诡计摘要一次性回填（海龟汤质量优化 spec）：错开启动高峰，失败静默（内部自 catch）
    setTimeout(() => void backfillTrickNotes(), 10_000).unref()
```

即该块变为：

```ts
  void app.whenReady().then(() => {
    registerBzresProtocol()
    initDb()
    registerIpc()
    startSchedulers()
    // 存量汤诡计摘要一次性回填（海龟汤质量优化 spec）：错开启动高峰，失败静默（内部自 catch）
    setTimeout(() => void backfillTrickNotes(), 10_000).unref()
    createWindow()
```

- [ ] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: exit 0（`setTimeout` 在主进程 Node 环境返回 `NodeJS.Timeout`，`.unref()` 可用）。

- [ ] **Step 4: 提交点（开发者执行）**

建议提交信息：`feat(turtle): 存量汤诡计摘要一次性回填（启动延迟 10s 触发，静默失败）`

---

### Task 5: TurtlePanel loading 文案

**Files:**
- Modify: `src/modules/reasoning/TurtlePanel.tsx:584`

- [ ] **Step 1: 替换按钮 loading 文案**

`TurtlePanel.tsx:584` 现有：

```tsx
          {generating ? 'AI 出汤中…' : '来 3 碗汤'}
```

替换为：

```tsx
          {generating ? '出题中，审题人正在验汤…' : '来 3 碗汤'}
```

（toast `已出 N 碗汤` 已展示实际入库数，零改动。）

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

- [ ] **Step 3: 提交点（开发者执行）**

建议提交信息：`ui(turtle): 「来3碗汤」loading 文案体现审题环节`

---

### Task 6: 全链路验证 + specs 增量同步 + 开发日志

**Files:**
- Modify: `docs/project/左侧边栏/推理角/designs-specs.md`（文件末尾追加增量节）
- Modify: `docs/log/260907.md`（当日日志追加；若文件不存在则按 `docs/log/` 下最近一份日志的格式新建）

- [ ] **Step 1: 构建冒烟**

Run: `npm run smoke`
Expected: 构建成功，Electron 启动自检通过退出（无迁移报错、无启动异常）。

- [ ] **Step 2: 开发者手动验收（需已配置 LLM；AI 不可代验，逐项请开发者确认）**

清单（来自 spec 验收节）：

1. `npm run dev` → 推理角 → 海龟汤 → 点「来 3 碗汤」：等待文案为「出题中，审题人正在验汤…」，约 2~3 轮 LLM 往返后 toast「已出 N 碗汤」；
2. 抽查新汤：汤面异常不扎眼罗列、存在隐性线索；汤底对手行为经得起「再谨慎一点」追问；
3. 指定难度偏好（如「困难」）再出一批：不出「货不对板」（审题打回机制生效）；
4. 连续两批：核心诡计思路无同构、批内 3 碗互异（可用 SQLite 查 `SELECT id, title, trick_note FROM turtle_soups ORDER BY id DESC` 检查 trick_note）；
5. 重启 App 一次：主进程 console 无 `[backfillTrickNotes]` 报错，10s 后存量汤 trick_note 补齐（同上 SQL 查 NULL 数为 0）；
6. 全程 trick_note 不出现在任何界面（防剧透）。

- [ ] **Step 3: designs-specs.md 追加增量节**

先读该文件末尾的既有增量节格式（形如「v1.x 增量（YYMMDD …）」），照格式在文件末尾追加一节，内容要点：

```markdown
## v1.x 增量（260907 海龟汤质量优化——海龟汤修改反馈）

- 「来 3 碗汤」升级两阶段管线：出题（标准 v2）→ 逐碗并行审题（半盲六查）→ 不合格打回重出一次 → 弃碗不弃批，3 碗全灭才报错不落库
- 出题标准 v2：①常识可解（专业知识不得作解题钥匙）②对手不降智（破绽须源于固有盲区）③线索自然隐藏（至少一条隐性线索，禁报菜名）④难度三档定义（单层/双层/多层反转），另保留回收/自洽/可判定与巧思总纲
- turtle_soups 新增 trick_note（核心诡计一句话，DB v15）：出题/审题注入近 9 碗摘要清单防跨批同构；纯内部机制不进 UI；存量汤启动后一次性 LLM 回填
- 难度落库以审题人评定为准；指定难度偏好时重评档不符即打回
- 「来 3 碗汤」loading 文案改为「出题中，审题人正在验汤…」；toast 已出 N 碗不变
```

- [ ] **Step 4: 开发日志追加**

读 `docs/log/260907.md`（今天已有日志则追加一节，无则按 `docs/log/` 最近一份格式新建），记录：海龟汤质量优化实施（两阶段审题管线 + trick_note 防同构 + 存量回填，DB v15），验证情况（typecheck/smoke/手动验收结果），并注明设计文档与计划文档路径。

- [ ] **Step 5: 提交点（开发者执行）**

建议提交信息：`docs: 推理角 specs v1.x 增量同步 + 260907 开发日志（海龟汤质量优化）`

---

## Self-Review 记录

- **Spec 覆盖**：出题标准 v2/巧思总纲（Task 2）、trick_note 产出+落库+不进 UI（Task 2/3）、避免清单升级（Task 3 查询+prompt）、审题半盲六查并行（Task 3）、难度冲突双口径（Task 3 代码硬校验+落库）、打回不给原稿（Task 2 redoProblems 设计）、弃碗不弃批/全灭抛错（Task 3）、出题解析重试一次（Task 2 composeSoups）、DB 加列与版本协调（Task 1+纪律 3）、回填全量含软删（Task 4）、LLM 未配置边界（手动入口走现有 handleErr 规则 8；回填自 catch）、UI 文案（Task 5）、specs/日志（Task 6）——全部有对应任务。
- **占位符**：无 TBD/TODO；所有代码步骤给出完整代码。
- **类型一致性**：`TurtleSoupDraft.trick_note: string`、`parseSoupArray(raw, expected)`、`composeSoups(prompt, count)`、`reviewSoup(draft, ctx)` / `SoupReview{ok,problems,ratedDifficulty}`、`buildSoupPrompt(opts)` 各任务间一致。
- **已知取舍**：`parseSoupArray` 由固定 3 碗参数化为 `expected`（spec「恰好 3 碗口径不变」指初始出题，重出场景传 1，语义不变）；多个失败碗并行重出时互避清单只含已通过碗（同批失败碗会被替换，无互避必要）。
