# 格言生成句式约束 实施计划

> **状态**：已实施并归档（260907）——Task 1/2/3 全部完成（Task 3 含计划外补充：渲染层 `src/renderer/api.d.ts` 第三处类型声明同步）；Task 4 冒烟由开发者执行。spec 见 `docs/project/左侧边栏/格言库/archive/2026-09-07-格言生成句式约束-design.md`。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 格言库「来 10 条格言」的 AI 编撰条禁用 7 类对仗套话句式——prompt 禁令 + 代码侧正则兜底剔除，摘录条豁免。

**Architecture:** 三处小改——主进程 `generateMottos()`（prompt 追加禁令段 + 入库循环正则校验剔除 + 返回 `patternRejected` 计数）→ `preload.ts` 返回类型透传 → `MottosModule` toast 附加提示。设计依据 `docs/project/左侧边栏/格言库/archive/2026-09-07-格言生成句式约束-design.md`（优化建议区第 23 轮）。

**Tech Stack:** Electron 主进程 TypeScript，node:sqlite，正则匹配。无测试框架（项目惯例：typecheck + 冒烟），正则行为用 node 内联脚本验证。

**项目纪律（务必遵守）：**
- **禁止执行任何 git 命令**（add/commit/push 等全部由开发者手动完成）——本计划不含 commit 步骤。
- 摘录条（excerpt）完全豁免；命中剔除后不补足；不做两阶段自评；清单写死为代码常量。

---

### Task 1: 禁用句式正则常量 + 行为验证

**Files:**
- Modify: `electron/ai/services.ts:347`（`mottoKind` 函数之后、`generateMottos` 注释之前插入）

- [ ] **Step 1: 在 services.ts 插入正则常量**

在 `mottoKind` 函数结束（第 347 行 `}`）与 `/** 「来10条格言」... */` 注释（第 349 行）之间插入：

```ts

/**
 * 编撰条禁用句式（优化建议区第23轮）：与 generateMottos prompt 中的禁令一一对应，
 * 仅校验 composed 条；摘录条（真实名言）豁免。命中即剔除、不补足。
 */
const COMPOSED_BANNED_PATTERNS: RegExp[] = [
  /(?:不是|并非)[^，,。；;！？\n]{1,24}[，,][^，,。；;！？\n]{0,16}而是/, // 1 不是/并非A，而是B
  /与其说?[^，,。；;！？\n]{0,24}[，,][^，,。；;！？\n]{0,12}不如说?/, // 2 与其（说）A，不如（说）B
  /真正的[^，,。；;！？\n]{1,16}[，,][^。；;！？\n]{0,10}(?:从来)?(?:不是|是|都是)/, // 3 真正的A，（从来）是/不是/都是B
  /所谓[^，,。；;！？\n]{1,16}[，,](?:不过是|只是)/, // 4 所谓A，不过是/只是B
  /[，,](?:才算|才配)/, // 5a ……，才算/才配
  /唯有[^，,。；;！？\n]{1,16}[，,](?:才|方可)/, // 5b 唯有A，才/方可B
  /(?:所有|一切)[^，,。；;！？\n]{1,20}[，,]都/, // 6 所有/一切A，都B（全称断言）
  /愿你/ // 7 愿你祝福腔（中文格言出现即祝福腔，误伤率极低，从宽抓）
]
```

- [ ] **Step 2: 运行行为验证脚本（10 正例应各自命中对应正则，6 反例应全部放行）**

在项目根目录运行：

```bash
node -e '
const P = [
  /(?:不是|并非)[^，,。；;！？\n]{1,24}[，,][^，,。；;！？\n]{0,16}而是/,
  /与其说?[^，,。；;！？\n]{0,24}[，,][^，,。；;！？\n]{0,12}不如说?/,
  /真正的[^，,。；;！？\n]{1,16}[，,][^。；;！？\n]{0,10}(?:从来)?(?:不是|是|都是)/,
  /所谓[^，,。；;！？\n]{1,16}[，,](?:不过是|只是)/,
  /[，,](?:才算|才配)/,
  /唯有[^，,。；;！？\n]{1,16}[，,](?:才|方可)/,
  /(?:所有|一切)[^，,。；;！？\n]{1,20}[，,]都/,
  /愿你/
];
const hits = [
  ["成长不是妥协，而是懂得取舍", 0],
  ["自由并非放纵，而是自我约束", 0],
  ["与其追逐风口，不如深耕自己", 1],
  ["与其说这是天赋，不如说这是习惯", 1],
  ["真正的强大，从来不是无视眼泪", 2],
  ["所谓成熟，不过是把哭声调成静音", 3],
  ["熬过去，才算是一个人的成人礼", 4],
  ["唯有热爱，方可抵岁月漫长", 5],
  ["所有的惊喜，都在路上", 6],
  ["愿你走出半生，归来仍是少年", 7]
];
const misses = [
  "读书使人充实，讨论使人机智，写作使人精确",
  "知识就是力量",
  "走自己的路，让别人去说吧",
  "认识你自己",
  "人是万物的尺度",
  "简单是长期努力工作的结果"
];
let fail = 0;
for (const [s, i] of hits) { const ok = P[i].test(s); if (!ok) fail++; console.log(ok ? "PASS" : "FAIL", "命中#" + i, s); }
for (const s of misses) { const ok = P.every(p => !p.test(s)); if (!ok) fail++; console.log(ok ? "PASS" : "FAIL", "放行", s); }
console.log(fail === 0 ? "ALL PASS" : fail + " FAILED");
'
```

Expected: 16 行全部 `PASS`，末行 `ALL PASS`。
若有 FAIL：按失败案例调整对应正则的容差（如字数区间、标点集），改完同步更新 services.ts 中的常量与脚本，直到 ALL PASS。调整仅限容差，不得删减句式家族。

---

### Task 2: prompt 禁令段 + 入库校验 + patternRejected 计数

**Files:**
- Modify: `electron/ai/services.ts:334-341`（`GenerateMottosResult`）、`:365`（prompt 字符串）、`:398-413`（计数声明、入库循环、返回值）

- [ ] **Step 1: GenerateMottosResult 加 patternRejected 字段**

现（services.ts:334-341）：

```ts
export interface GenerateMottosResult {
  generated: number
  inserted: number
  /** 入库的摘录条数（v2.0 §7.3） */
  excerptInserted: number
  /** 入库的编撰条数（v2.0 §7.3） */
  composedInserted: number
}
```

改为：

```ts
export interface GenerateMottosResult {
  generated: number
  inserted: number
  /** 入库的摘录条数（v2.0 §7.3） */
  excerptInserted: number
  /** 入库的编撰条数（v2.0 §7.3） */
  composedInserted: number
  /** 因句式禁令被剔除的编撰条数（优化建议区第23轮） */
  patternRejected: number
}
```

- [ ] **Step 2: prompt 追加句式禁令段**

services.ts:365 的 prompt 字符串中，找到片段（生成指令句尾）：

```
恰好 5 条由你自行编撰（kind 为 "composed"，source 标「debugzi」）。\n\n以下是我已有的全部格言清单
```

在两者之间插入禁令段，改为：

```
恰好 5 条由你自行编撰（kind 为 "composed"，source 标「debugzi」）。\n\n你自行编撰的 5 条额外遵守句式禁令——以下 7 类对仗套话一律禁止：\n1. 「不是A，而是B」「并非A，而是B」\n2. 「与其A，不如B」「与其说A，不如说B」\n3. 「真正的A，是/从来不是B」\n4. 「所谓A，不过是B」\n5. 「……，才算……」「唯有A，才B」类排他强调\n6. 「所有/一切A，都B」全称断言\n7. 「愿你……」祝福腔\n编撰条请像正式区样本那样平实、具体、有画面，靠内容本身立住，不要靠句式端着。摘录条（kind 为 "excerpt"）不受此限，如实引用原文。\n\n以下是我已有的全部格言清单
```

（其余部分一字不动。）

- [ ] **Step 3: 计数声明加 patternRejected**

services.ts:398-400，现：

```ts
  let inserted = 0
  let excerptInserted = 0
  let composedInserted = 0
```

改为：

```ts
  let inserted = 0
  let excerptInserted = 0
  let composedInserted = 0
  let patternRejected = 0
```

- [ ] **Step 4: 入库循环加正则校验剔除**

services.ts:401-412，现：

```ts
  for (const it of items) {
    const key = normalizeText(it.content)
    if (!key || isDupMotto(allNorms, key)) continue
    allNorms.push(key)
    headSort -= 1
    // gen_kind 落库（DB v6）：驱动 AI 徽章仅编撰条显示、摘录条不打
    const kind = mottoKind(it.kind, it.source)
    ins.run(it.content, it.source, kind, headSort, now, now)
    if (kind === 'composed') composedInserted++
    else excerptInserted++
    inserted++
  }
  return { generated: items.length, inserted, excerptInserted, composedInserted }
```

改为（kind 判定提前到 `allNorms.push` 之前，被剔除条不占用查重集合）：

```ts
  for (const it of items) {
    const key = normalizeText(it.content)
    if (!key || isDupMotto(allNorms, key)) continue
    // gen_kind 落库（DB v6）：驱动 AI 徽章仅编撰条显示、摘录条不打
    const kind = mottoKind(it.kind, it.source)
    // 句式禁令兜底（优化建议区第23轮）：仅编撰条，命中剔除不补足；摘录条豁免（真实名言不受限）
    if (kind === 'composed' && COMPOSED_BANNED_PATTERNS.some((p) => p.test(it.content))) {
      patternRejected++
      continue
    }
    allNorms.push(key)
    headSort -= 1
    ins.run(it.content, it.source, kind, headSort, now, now)
    if (kind === 'composed') composedInserted++
    else excerptInserted++
    inserted++
  }
  return { generated: items.length, inserted, excerptInserted, composedInserted, patternRejected }
```

- [ ] **Step 5: typecheck 确认无类型错误**

Run: `npm run typecheck`
Expected: 双 tsconfig 均无错误，exit 0。
（此时 preload 的返回类型声明还没有 `patternRejected`，若 typecheck 因 preload 类型不匹配报错属预期，Task 3 修复后复跑；正常情况下 preload 是结构化子集声明、不会报错。）

---

### Task 3: preload 类型透传 + toast 附加提示

**Files:**
- Modify: `electron/preload.ts:142-146`（mottos.generate 返回类型）
- Modify: `src/modules/mottos/MottosModule.tsx:192-195`（generate 的 toast）

- [ ] **Step 1: preload.ts 返回类型加字段**

现（preload.ts:141-146）：

```ts
    generate: (): Promise<{
      generated: number
      inserted: number
      excerptInserted: number
      composedInserted: number
    }> => ipcRenderer.invoke('mottos:generate'),
```

改为：

```ts
    generate: (): Promise<{
      generated: number
      inserted: number
      excerptInserted: number
      composedInserted: number
      patternRejected: number
    }> => ipcRenderer.invoke('mottos:generate'),
```

- [ ] **Step 2: MottosModule toast 在有剔除时附加提示**

现（MottosModule.tsx:192-195）：

```ts
      const r = await window.api.mottos.generate()
      toast(
        `本次生成 ${r.generated} 条（摘录 ${r.excerptInserted} + 编撰 ${r.composedInserted}），去重后入库 ${r.inserted} 条`
      )
```

改为：

```ts
      const r = await window.api.mottos.generate()
      toast(
        `本次生成 ${r.generated} 条（摘录 ${r.excerptInserted} + 编撰 ${r.composedInserted}），去重后入库 ${r.inserted} 条` +
          (r.patternRejected > 0 ? `，${r.patternRejected} 条因句式违规未入库` : '')
      )
```

（IPC handler `ipcMain.handle('mottos:generate', () => generateMottos())` 直接透传返回值，无需改动。）

- [ ] **Step 3: typecheck 全量通过**

Run: `npm run typecheck`
Expected: 双 tsconfig 均无错误，exit 0。

---

### Task 4: 冒烟验证（需开发者配合）

**Files:** 无代码改动。

- [ ] **Step 1: 开发者冒烟**

`npm run dev` 启动，格言库点「来 10 条格言」若干批，验证：

1. 编撰条（带 AI 徽章的草稿区新条目）不再出现 7 类句式（不是…而是 / 与其…不如 / 真正的… / 所谓…不过是 / …才算 / 唯有…才 / 所有…都 / 愿你）
2. 摘录条不受影响——真实名言仍可正常出现（包括本身是「不是…而是」句式的真名言）
3. 若有剔除，toast 显示「，N 条因句式违规未入库」
4. 偶发剔除后该批编撰条不足 5 条属预期（不补足，已确认的设计决策）

- [ ] **Step 2: 冒烟通过后由开发者手动完成**

- git 提交（AI 不执行）
- 优化建议区待完成条目移入归档区「### 第23轮（260907）：格言生成句式约束」小节（保留原始描述 + 完成说明），spec 与本 plan 已随 260907 目录规范化迁入 `docs/project/左侧边栏/格言库/archive/`
- `docs/log/260907.md` 追加本轮开发记录（若当日已有日志则追加小节）

---

## Self-Review 记录

- **Spec 覆盖**：7 类禁用清单（Task 1 正则 + Task 2 prompt 段）、只约束编撰条（Task 2 Step 4 `kind === 'composed'` 判定）、剔除不补足（`continue`，无补生成逻辑）、patternRejected 计数透明（Task 2 Step 1/4 + Task 3 toast）、「不做的事」均未引入 ✓
- **Placeholder 扫描**：全部步骤含完整代码/命令/预期输出，无 TBD ✓
- **类型一致性**：`patternRejected` 在 GenerateMottosResult、返回值字面量、preload 声明、toast 引用四处同名同义；`COMPOSED_BANNED_PATTERNS` Task 1 定义、Task 2 引用同名 ✓
