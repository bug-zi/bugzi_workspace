# 藏书架书签优化（章节名 label + 改名 + 备注）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 书签默认 label 改为可靠解析的「章节名 · 位置」（epub 区间锚点修复），支持侧栏行内改名与写备注。

**Architecture:** DB v30 单列迁移（book_marks.note）；主进程 BookService 增 updateMark（部分更新）+ 1 条 IPC 四处同步；渲染层 ReaderTocItem/ReaderLocate 增 spineIndex（EpubReader 解析上报）→ 模块层 epubChapterAt 区间判定（与 pdf tocAnchorAt 同构）→ addBookmark label 重写；ReaderSidebar 书签行增备注小字 + 行内展开编辑。设计依据：同目录 `2026-09-10-书架书签章节名与备注-design.md`（下称「设计」）。

**Tech Stack:** Electron 44 + React 19 + TypeScript，node:sqlite（prepare API），epub.js 0.3.93。

**项目特例（开发者指令，覆盖 skill 默认）：**
- **AI 禁止一切 git 操作**——计划内无 commit 步骤，提交由开发者自行完成。
- **项目无测试框架**——每任务以 `npm run typecheck` 为自动门禁，最终 `npm run build` + 开发者手测验收清单（设计文档末节）。
- 类型双处惯例：`src/shared/types.ts` 与 `src/renderer/api.d.ts` 各一份同构声明。

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `electron/db/db.ts` | 修改 | v30 迁移（book_marks 加 note 列） |
| `src/shared/types.ts` | 修改 | BookMark 加 note 字段 |
| `src/renderer/api.d.ts` | 修改 | BookMark 同构 + books 方法区 markUpdate |
| `electron/services/books.ts` | 修改 | updateMark（部分更新 + 回读整行） |
| `electron/ipc.ts` | 修改 | books:markUpdate handler + import |
| `electron/preload.ts` | 修改 | books 块 markUpdate 方法 |
| `src/modules/bookshelf/ReaderSidebar.tsx` | 修改 | spineIndex 两类型 + epubChapterAt + 书签行备注/行内编辑（MarkItem） |
| `src/modules/bookshelf/EpubReader.tsx` | 修改 | 目录/locate 上报带 spineIndex |
| `src/modules/bookshelf/BookshelfModule.tsx` | 修改 | addBookmark label 口径 + updateMark 编排接线 |
| `src/modules/bookshelf/bookshelf.css` | 修改 | 行内编辑态样式 |

---

### Task 1: DB v30 迁移

**Files:**
- Modify: `electron/db/db.ts`（v29 块之后、`migrate()` 收尾 `}` 之前，约 801 行处）

- [ ] **Step 1: 追加 v30 迁移块**

在 `d.exec('PRAGMA user_version = 29')` 与其闭括号之后插入：

```ts
  if (version < 30) {
    // v30：书架书签优化（2026-09-10-书架书签章节名与备注-design.md §三）——书签加备注列。
    // label 改「章节名 · 位置」口径由渲染层生成，存量行不迁移（想要精修手动改名）。
    d.exec(`ALTER TABLE book_marks ADD COLUMN note TEXT NOT NULL DEFAULT ''`)
    d.exec('PRAGMA user_version = 30')
  }
```

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: 双 tsconfig 无错误退出（exit 0）。

---

### Task 2: BookMark 类型加 note（两处同构）

**Files:**
- Modify: `src/shared/types.ts`（BookMark 接口，353-364 行）
- Modify: `src/renderer/api.d.ts`（BookMark 接口，168-179 行）

- [ ] **Step 1: shared/types.ts——BookMark 整体替换为**

```ts
/** 书架手动书签（book_marks 表，书架 v2.0 §二 + 书签优化轮）：epub 存 cfi、pdf 存 page（1 基）；label 默认「章节名 · 位置」可手动改 */
export interface BookMark {
  id: number
  book_id: number
  /** epub 定位（epub 行非空） */
  cfi: string | null
  /** pdf 页码（1 基；pdf 行非空） */
  page: number | null
  /** 显示名（默认「章节名 · 42%」/「章节名 · 第 N 页」，用户可改） */
  label: string
  /** 备注；空字符串 = 无备注 */
  note: string
  created_at: string
}
```

- [ ] **Step 2: api.d.ts——BookMark 整体替换为（同构，注释精简）**

```ts
/** 书架手动书签（book_marks 表，书架 v2.0 + 书签优化轮）：epub 存 cfi、pdf 存 page（1 基）；label 可手动改 */
export interface BookMark {
  id: number
  book_id: number
  /** epub 定位（epub 行非空） */
  cfi: string | null
  /** pdf 页码（1 基；pdf 行非空） */
  page: number | null
  /** 显示名（默认「章节名 · 42%」/「章节名 · 第 N 页」，用户可改） */
  label: string
  /** 备注；空字符串 = 无备注 */
  note: string
  created_at: string
}
```

注：`listMarks`/`addMark` 的 `SELECT *` 自然带出 note（列默认 ''），服务层零改动即类型自洽。

- [ ] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: exit 0（addMark 调用方不传 note，DB 列默认值兜底）。

---

### Task 3: 主进程链路——updateMark 服务 + IPC + preload + api.d.ts（四处同步）

**Files:**
- Modify: `electron/services/books.ts`（addMark 之后、removeMark 之前，约 300 行处）
- Modify: `electron/ipc.ts`（import 块 59 行 `removeMark,` 附近 + markRemove handler 之后，约 839 行处）
- Modify: `electron/preload.ts`（books 块 markRemove 之后，约 484 行处）
- Modify: `src/renderer/api.d.ts`（books 方法区 markRemove 之后，约 889 行处）

- [ ] **Step 1: books.ts——addMark 与 removeMark 之间插入**

```ts
/** 更新书签 label/备注（只传要改的字段；书签优化轮 §三），回读整行返回 */
export function updateMark(markId: number, m: { label?: string; note?: string }): BookMark {
  if (m.label !== undefined) {
    getDb().prepare('UPDATE book_marks SET label = ? WHERE id = ?').run(m.label, markId)
  }
  if (m.note !== undefined) {
    getDb().prepare('UPDATE book_marks SET note = ? WHERE id = ?').run(m.note, markId)
  }
  return getDb()
    .prepare('SELECT * FROM book_marks WHERE id = ?')
    .get(markId) as unknown as BookMark
}
```

- [ ] **Step 2: ipc.ts——import 与 handler**

import 块 `addMark,` 与 `removeMark,` 之间插入一行：

```ts
  updateMark,
```

`books:markRemove` handler 闭括号之后插入：

```ts
  ipcMain.handle(
    'books:markUpdate',
    (_e, markId: number, m: { label?: string; note?: string }) => updateMark(markId, m)
  )
```

- [ ] **Step 3: preload.ts——markRemove 之后插入**

```ts
    /** 书签改名/备注（只传要改的字段，返回更新后整行） */
    markUpdate: (
      markId: number,
      m: { label?: string; note?: string }
    ): Promise<import('../src/shared/types').BookMark> =>
      ipcRenderer.invoke('books:markUpdate', markId, m),
```

- [ ] **Step 4: api.d.ts——books 方法区 markRemove 之后插入**

```ts
    /** 书签改名/备注（只传要改的字段，返回更新后整行） */
    markUpdate(markId: number, m: { label?: string; note?: string }): Promise<BookMark>
```

- [ ] **Step 5: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

---

### Task 4: ReaderSidebar 纯增量——spineIndex 类型 + epubChapterAt

**Files:**
- Modify: `src/modules/bookshelf/ReaderSidebar.tsx`（ReaderTocItem 6-13 行、ReaderLocate 15-19 行、tocAnchorAt 之后 70 行处）

- [ ] **Step 1: ReaderTocItem 加 spineIndex 字段（href 之后）**

```ts
export interface ReaderTocItem {
  label: string
  /** epub：章节 href（rendition.display 直达） */
  href?: string
  /** epub：spine 序号（书签优化轮 §一，「当前章」区间判定用；解析失败缺省） */
  spineIndex?: number
  /** pdf：目标页码（1 基） */
  page?: number
  children?: ReaderTocItem[]
}
```

- [ ] **Step 2: ReaderLocate 加 spineIndex 字段（href 之后）**

```ts
export interface ReaderLocate {
  href?: string
  /** epub：当前 spine 序号（书签优化轮 §一） */
  spineIndex?: number
  page?: number
}
```

- [ ] **Step 3: tocAnchorAt 函数之后新增 epubChapterAt**

```ts
/** 当前 spine 序号落在的目录章节（DFS 序最后一个 spineIndex ≤ 当前的节点；书签优化轮 §一，与 tocAnchorAt 同构） */
export function epubChapterAt(items: ReaderTocItem[], spineIndex: number): ReaderTocItem | null {
  let hit: ReaderTocItem | null = null
  for (const it of flattenToc(items)) {
    if (it.spineIndex != null && it.spineIndex <= spineIndex) hit = it
    else if (it.spineIndex != null && hit) break
  }
  return hit
}
```

注：内部已 flattenToc，调用方直接传树根；无 spineIndex 的目录项（解析失败/纯标签节点）在区间判定中自然跳过。

- [ ] **Step 4: 类型检查**

Run: `npm run typecheck`
Expected: exit 0（新字段全部可选、新函数暂无调用方，纯增量）。

---

### Task 5: EpubReader——目录/locate 上报带 spineIndex

**Files:**
- Modify: `src/modules/bookshelf/EpubReader.tsx`（convertToc 43-52 行、navigation 上报 382-384 行、relocated 320-336 行）

- [ ] **Step 1: convertToc 改签名解析 spineIndex（整体替换 42-52 行）**

```tsx
/** 目录 href → spine 序号（去锚点解析；解析失败 undefined → 区间判定跳过该项。书签优化轮 §一） */
function resolveSpineIndex(spine: Book['spine'], href: string | undefined): number | undefined {
  if (!href) return undefined
  const file = href.split('#')[0]
  if (!file) return undefined
  try {
    return spine.get(file)?.index ?? spine.get(href)?.index ?? undefined
  } catch {
    return undefined
  }
}

/** NavItem → 侧栏目录树（subitems 递归，深于 4 层剪枝；书签优化轮：增 spineIndex 供「当前章」区间判定） */
function convertToc(items: NavItem[] | undefined, spine: Book['spine'], depth = 0): ReaderTocItem[] {
  if (!items || depth > 4) return []
  return items
    .map((it) => ({
      label: (it.label ?? '').trim(),
      href: it.href,
      spineIndex: resolveSpineIndex(spine, it.href),
      children: convertToc(it.subitems, spine, depth + 1)
    }))
    .filter((it) => it.label || it.children.length > 0)
}
```

- [ ] **Step 2: navigation 上报等 spine 就绪（替换 382-384 行）**

```tsx
      // 目录上报（navigation + spine 就绪后一次；书签优化轮：spine 用于解析章节 spineIndex）
      void Promise.all([bk.loaded.navigation, bk.loaded.spine])
        .then(([nav]) => props.onToc(convertToc(nav.toc, bk.spine)))
        .catch(() => undefined)
```

- [ ] **Step 3: relocated 上报补 spineIndex（替换 325 行）**

```tsx
      if (l.start?.href) props.onLocate({ href: l.start.href, spineIndex: l.start.index })
```

（`l.start.index` 上一行已收进 `spineIdxRef`，此处只是随 locate 一并上报，ReaderLocate 类型 Task 4 已扩。）

- [ ] **Step 4: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

---

### Task 6: 侧栏书签行内编辑（MarkItem）+ 模块编排接线

**Files:**
- Modify: `src/modules/bookshelf/ReaderSidebar.tsx`（import 行、Props、marks 页签渲染 136-158 行）
- Modify: `src/modules/bookshelf/BookshelfModule.tsx`（jumpMark 附近 + ReaderSidebar 挂载处 393-407 行）

- [ ] **Step 1: ReaderSidebar——顶部补 React hook import（第 3 行 import type 之前）**

```tsx
import { useState } from 'react'
```

- [ ] **Step 2: Props 加 onUpdateMark（onDeleteMark 之后）**

```tsx
  onJumpMark: (m: BookMark) => void
  onDeleteMark: (m: BookMark) => void
  /** 书签改名/备注保存（模块层走 IPC；书签优化轮 §四） */
  onUpdateMark: (id: number, m: { label: string; note: string }) => void
```

- [ ] **Step 3: 组件解构补 onUpdateMark（103 行）**

```tsx
  const { tab, onTabChange, toc, locate, notes, marks, onJumpToc, onJumpNote, onDeleteNote, onJumpMark, onDeleteMark, onUpdateMark, onOverview, onCollapse } = props
```

- [ ] **Step 4: ReaderSidebar 函数之前新增 MarkItem 子组件（TocTree 之后）**

```tsx
/** 单条书签行：常态（label + 备注小字 + hover 编辑/删除）与行内编辑态（书签优化轮 §四） */
function MarkItem(props: {
  mark: BookMark
  onJump: (m: BookMark) => void
  onDelete: (m: BookMark) => void
  onUpdate: (id: number, m: { label: string; note: string }) => void
}) {
  const { mark, onJump, onDelete, onUpdate } = props
  const [editing, setEditing] = useState(false)
  const [label, setLabel] = useState('')
  const [note, setNote] = useState('')
  const startEdit = (): void => {
    setLabel(mark.label)
    setNote(mark.note)
    setEditing(true)
  }
  const save = (): void => {
    const t = label.trim()
    if (!t) return // 空名兜底（保存按钮已禁用）
    onUpdate(mark.id, { label: t, note: note.trim() })
    setEditing(false)
  }
  if (editing) {
    return (
      <div className="bk-note-item bk-mark-editing" onClick={(e) => e.stopPropagation()}>
        <input
          className="bk-mark-label-input"
          value={label}
          onChange={(e) => setLabel(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === 'Enter') save()
            else if (e.key === 'Escape') setEditing(false)
          }}
          placeholder="书签名"
          autoFocus
        />
        <textarea
          className="bk-mark-note-input"
          value={note}
          onChange={(e) => setNote(e.target.value)}
          placeholder="写点备注…可留空"
          rows={2}
        />
        <div className="bk-mark-edit-row">
          <button className="btn" disabled={!label.trim()} onClick={save}>
            保存
          </button>
          <button className="btn btn-ghost" onClick={() => setEditing(false)}>
            取消
          </button>
        </div>
      </div>
    )
  }
  return (
    <div className="bk-note-item" onClick={() => onJump(mark)} title="点击跳回书签位置">
      <div className="bk-note-quote">{mark.label}</div>
      {mark.note && <div className="bk-note-text">{mark.note}</div>}
      <div className="bk-note-foot">
        <span>书签 · {relTime(mark.created_at)}</span>
        <span className="bk-mark-actions">
          <button
            className="bk-mark-edit-btn"
            title="编辑书签（改名 / 备注）"
            onClick={(e) => {
              e.stopPropagation()
              startEdit()
            }}
          >
            <span className="material-symbols-outlined">edit</span>
          </button>
          <button
            className="bk-note-del"
            title="删除书签"
            onClick={(e) => {
              e.stopPropagation()
              onDelete(mark)
            }}
          >
            <span className="material-symbols-outlined">delete</span>
          </button>
        </span>
      </div>
    </div>
  )
}
```

- [ ] **Step 5: marks 页签渲染改用 MarkItem（替换 140-157 行的 marks.map 块）**

```tsx
            marks.map((m) => (
              <MarkItem key={m.id} mark={m} onJump={onJumpMark} onDelete={onDeleteMark} onUpdate={onUpdateMark} />
            ))
```

（外层 `marks.length === 0 ? 空态 : (...)` 结构不变。）

- [ ] **Step 6: BookshelfModule——jumpMark 之后新增 updateMark 编排**

```tsx
  /** 书签改名/备注保存（书签优化轮 §四）：整行回填本地 */
  const updateMark = (id: number, m: { label: string; note: string }): void => {
    void window.api.books
      .markUpdate(id, m)
      .then((row) => {
        setMarks((prev) => prev.map((x) => (x.id === id ? row : x)))
        toast('已保存')
      })
      .catch(() => toast('保存失败'))
  }
```

- [ ] **Step 7: BookshelfModule——ReaderSidebar 挂载传 prop（onDeleteMark 之后）**

```tsx
              onUpdateMark={updateMark}
```

- [ ] **Step 8: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

---

### Task 7: addBookmark label 口径重写

**Files:**
- Modify: `src/modules/bookshelf/BookshelfModule.tsx`（import 行 13 + addBookmark 236-252 行）

- [ ] **Step 1: import 补 epubChapterAt（ReaderSidebar 导入行）**

```tsx
import ReaderSidebar, { flattenToc, relTime, tocAnchorAt, epubChapterAt, type ReaderLocate, type ReaderTocItem, type SidebarTab } from './ReaderSidebar'
```

- [ ] **Step 2: epub 分支 label 生成替换（236-237 行）**

```tsx
      const pct = Math.round(cur.percent * 100)
      const chapter =
        locate?.spineIndex != null ? (epubChapterAt(toc, locate.spineIndex)?.label ?? null) : null
      const label = chapter ? `${chapter} · ${pct}%` : `约 ${pct}%`
```

- [ ] **Step 3: pdf 分支 label 生成替换（248 行）**

```tsx
      const anchor = tocAnchorAt(toc, page)?.label
      const label = anchor ? `${anchor} · 第 ${page} 页` : `第 ${page} 页`
```

（cfi/page 去重、markAdd 调用、toast 全部不动。）

- [ ] **Step 4: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

---

### Task 8: bookshelf.css 行内编辑样式

**Files:**
- Modify: `src/modules/bookshelf/bookshelf.css`（`.bk-note-del:hover` 之后、划词气泡注释之前，约 372 行处）

- [ ] **Step 1: 追加样式块**

```css
/* ===== 书签行内编辑（书签优化轮 §四） ===== */
.bk-mark-actions {
  display: flex;
  gap: 2px;
}
.bk-mark-edit-btn {
  border: none;
  background: transparent;
  color: var(--color-text-secondary);
  padding: 2px;
  border-radius: 4px;
  display: flex;
}
.bk-mark-edit-btn:hover {
  background: var(--color-primary);
  color: var(--color-text);
}
.bk-mark-editing {
  cursor: default;
  gap: 6px;
}
.bk-mark-label-input,
.bk-mark-note-input {
  width: 100%;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  background: var(--color-surface);
  color: var(--color-text);
  font: inherit;
  padding: 4px 8px;
}
.bk-mark-label-input:focus,
.bk-mark-note-input:focus {
  outline: none;
  border-color: var(--color-primary);
}
.bk-mark-note-input {
  resize: vertical;
  min-height: 44px;
  line-height: 1.55;
}
.bk-mark-edit-row {
  display: flex;
  gap: 6px;
}
```

注：全局 `.field { width: 100% }` 与本类名无冲突（收藏夹第33轮同款坑，新类名避开）；双主题全走既有变量零新色。

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

---

### Task 9: 总验证

- [ ] **Step 1: 类型检查**

Run: `npm run typecheck`
Expected: exit 0。

- [ ] **Step 2: 构建**

Run: `npm run build`
Expected: 构建成功退出。

- [ ] **Step 3: 冒烟清单（开发者手测；含 DB 迁移需重启 dev 触发 v30）**

对照设计文档「验收清单」逐项：

1. 重启 dev → DB 迁 v30（旧书签 note 为空串不报错）；
2. epub 打开 → 跨章移动后加书签，label 为「章节名 · N%」，章节随位置变化；目录带锚点/稀疏的书也能解析；同章节加两个书签 label 可区分；无目录/解析失败书兜底「约 N%」；
3. pdf 打开 → label「章节名 · 第 N 页」；无目录书兜底「第 N 页」；
4. 侧栏书签页签：hover 出编辑铅笔 → 行内改名/写备注 → 保存后 label 更新、备注小字显示；清空备注保存后小字消失；重开书仍在；label 清空时保存禁用；Enter 保存 / Esc 取消；编辑态点行不跳转；
5. 同位置去重 toast、删除二次确认、点击跳转回归正常；
6. 双主题下编辑态配色正常，图标全 Material Symbols。

---

## Self-Review 记录

- **Spec 覆盖**：设计 §一（Task 4/5）、§二（Task 7）、§三（Task 1/2/3）、§四（Task 6/8）、验收清单（Task 9）——全覆盖；「明确不做」未越界。
- **占位扫描**：无 TBD/TODO/「适当处理」类措辞，所有代码步骤含完整代码。
- **类型一致性**：`updateMark(markId, { label?, note? })` 四处签名一致；`onUpdateMark(id, { label: string, note: string })` 在 ReaderSidebar Props / MarkItem / BookshelfModule 三处一致；`epubChapterAt(items: ReaderTocItem[], spineIndex: number)` 定义与调用（`epubChapterAt(toc, locate.spineIndex)`）一致；`BookMark.note` 两处同构。
- **任务序依赖**：Task 4（类型）先于 Task 5（EpubReader 上报）与 Task 7（调用）；Task 6 侧栏必传 prop 与模块接线同任务闭环，各任务 typecheck 均可独立通过。
