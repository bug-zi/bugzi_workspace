# 阅读器字体选择与划词气泡修复 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **本项目纪律（覆盖技能默认）**：禁止 AI 执行任何 git 操作（add/commit 等全部留给开发者手动完成），任务步骤不含 commit；本项目无单测基建，验证手段 = `npm run typecheck`（双 tsconfig）+ 末尾人工冒烟清单。改动含 electron/（DB 迁移），冒烟前须重启 dev。

**Goal:** 藏书架 epub 阅读器新增书级字体选择（12 项浮层，顺带修复打包字体进不了 iframe 的静默回落），修复划词气泡取消划词不消失（iframe 事件不冒泡），高光/批注两按钮改横排。

**Architecture:** 数据链路完全镜像现有字号倍率（`books.font_family` 列 NULL=跟随全局 → `setReadingPref` IPC 透传 → `EpubReader.applyTheme` 注入）；打包字体经 `rendition.on('rendered')` 向每个 iframe 注入 `@font-face`；气泡修复挂 `rendition.on('mousedown'/'relocated')` 与滚动容器 scroll。

**Tech Stack:** Electron 44 + React 19 + TypeScript + epub.js（rendition/themes/annotations）。设计文档：`docs/project/左侧边栏/书架/2026-09-11-阅读器字体选择与划词气泡修复-design.md`（含根因取证，实施前先通读）。

---

### Task 1: 共享字体定义模块 `src/theme/fonts.ts`

**Files:**
- Create: `src/theme/fonts.ts`
- Modify: `src/modules/profile/ProfileModule.tsx`（删本地 `FONT_FAMILIES` 定义，改 import）

- [ ] **Step 1: 新建 `src/theme/fonts.ts`**

```ts
// 字体定义共享模块（字体选择轮 §1.1）：个人档 FONT_FAMILIES 自 ProfileModule 迁出共用，
// 阅读器 READER_FONTS 供书架字体浮层与 iframe @font-face 注入
import liyuUrl from '../renderer/assets/fonts/liyu-shoushu.ttf?url'
import hongleiUrl from '../renderer/assets/fonts/honglei-banshu.ttf?url'
import lxgwUrl from '../renderer/assets/fonts/lxgw-wenkai.ttf?url'
import xuanzongUrl from '../renderer/assets/fonts/xuanzongti.otf?url'
import youranUrl from '../renderer/assets/fonts/youran-xiaokai.ttf?url'

export interface FontOption {
  label: string
  /** CSS font-family 串（含回落链） */
  value: string
}

/** 个人档全局字体（原 ProfileModule 私有，内容不变；对应 global.css @font-face，楷体为系统字体） */
export const FONT_FAMILIES: FontOption[] = [
  { label: '默认（系统）', value: "system-ui, -apple-system, 'Segoe UI', 'Microsoft YaHei', sans-serif" },
  { label: '漓雨手书', value: "'Liyu Shoushu', 'KaiTi', serif" },
  { label: '鸿雷板书简体', value: "'Honglei Banshu', sans-serif" },
  { label: '霞鹜文楷', value: "'LXGW WenKai', 'KaiTi', serif" },
  { label: '玄宗体', value: "'XuanZong Ti', serif" },
  { label: '演示悠然小楷', value: "'Youran Xiaokai', 'KaiTi', serif" },
  { label: '楷体', value: "'KaiTi', 'STKaiti', serif" }
]

export interface ReaderFont extends FontOption {
  /** @font-face family 名（打包项注入 iframe 用） */
  family: string
  /** 打包字体文件 URL（Vite ?url 资产；缺省 = 系统字体无需注入） */
  url?: string
}

/** 阅读器字体清单（字体选择轮 §1.1，系统 6 + 打包 5；「跟随全局」项由调用方动态拼、不在此列） */
export const READER_FONTS: ReaderFont[] = [
  { label: '系统默认（雅黑）', value: "system-ui, -apple-system, 'Segoe UI', 'Microsoft YaHei', sans-serif", family: 'system-ui' },
  { label: '微软雅黑', value: "'Microsoft YaHei', 'PingFang SC', sans-serif", family: 'Microsoft YaHei' },
  { label: '宋体', value: "'SimSun', 'STSong', serif", family: 'SimSun' },
  { label: '楷体', value: "'KaiTi', 'STKaiti', serif", family: 'KaiTi' },
  { label: '黑体', value: "'SimHei', 'STHeiti', sans-serif", family: 'SimHei' },
  { label: '仿宋', value: "'FangSong', 'STFangsong', serif", family: 'FangSong' },
  { label: '漓雨手书', value: "'Liyu Shoushu', 'KaiTi', serif", family: 'Liyu Shoushu', url: liyuUrl },
  { label: '鸿雷板书简体', value: "'Honglei Banshu', sans-serif", family: 'Honglei Banshu', url: hongleiUrl },
  { label: '霞鹜文楷', value: "'LXGW WenKai', 'KaiTi', serif", family: 'LXGW WenKai', url: lxgwUrl },
  { label: '玄宗体', value: "'XuanZong Ti', serif", family: 'XuanZong Ti', url: xuanzongUrl },
  { label: '演示悠然小楷', value: "'Youran Xiaokai', 'KaiTi', serif", family: 'Youran Xiaokai', url: youranUrl }
]

/** 打包字体（@font-face 注入 iframe 用；设计 §1.4，EpubReader 消费） */
export const BUNDLED_FONTS = READER_FONTS.filter((f): f is ReaderFont & { url: string } => f.url != null)
```

- [ ] **Step 2: ProfileModule 改用共享定义**

`src/modules/profile/ProfileModule.tsx` 顶部 import 区（现有 React/共享类型 import 之后）加：

```ts
import { FONT_FAMILIES } from '../../theme/fonts'
```

删除本地定义（原 19-27 行的 `const FONT_FAMILIES = [...]` 及其上方注释行「// 对应 global.css @font-face；楷体为系统字体保留」——注释语义已并入 fonts.ts）。函数体内 `FONT_FAMILIES` 三处引用（约 491/493/620 行）零改动。

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: 双配置 0 error（`*?url` 模块由 `src/assets.d.ts` 声明覆盖，`.ttf/.otf?url` import 无需新增声明）。

---

### Task 2: DB v32 迁移（books.font_family 列）

**Files:**
- Modify: `electron/db/db.ts`（v31 块之后、迁移函数末尾）

- [ ] **Step 1: 追加 v32 块**

在 `if (version < 31) { ... }` 块（现 810-817 行）之后追加（与其同缩进）：

```ts
  if (version < 32) {
    // v32：书架字体选择（2026-09-11-阅读器字体选择与划词气泡修复-design.md §1.2）——
    // 书级字体族（NULL=跟随全局，存量书零迁移）。（设计初稿写 v31，被并行会话万象库
    // 待学习区占用 v31，实际落 v32——版本号实况条款同 v18/v19、v29 先例。）
    d.exec(`ALTER TABLE books ADD COLUMN font_family TEXT`)
    d.exec('PRAGMA user_version = 32')
  }
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: 0 error。（迁移执行属运行时，冒烟重启 dev 时触发。）

---

### Task 3: fontFamily 全链透传（service → IPC → preload → api.d.ts → types）

**Files:**
- Modify: `electron/services/books.ts:319-330`（setReadingPref）
- Modify: `electron/ipc.ts:885-891`（books:setReadingPref handler）
- Modify: `electron/preload.ts:508-512`（setReadingPref 桥）
- Modify: `src/renderer/api.d.ts:905`
- Modify: `src/shared/types.ts`（BooksRecord，font_scale 字段之后）

- [ ] **Step 1: books.ts setReadingPref 加分支**

```ts
/** 书级阅读偏好（书架 v2.0 §四）：只传要改的字段；fontScale/fontFamily 传 null 清除（回落全局） */
export function setReadingPref(
  bookId: number,
  p: { mode?: 'scroll' | 'page'; fontScale?: number | null; fontFamily?: string | null }
): void {
  if (p.mode != null) {
    getDb().prepare('UPDATE books SET reading_mode = ? WHERE id = ?').run(p.mode, bookId)
  }
  if (p.fontScale !== undefined) {
    getDb().prepare('UPDATE books SET font_scale = ? WHERE id = ?').run(p.fontScale, bookId)
  }
  if (p.fontFamily !== undefined) {
    getDb().prepare('UPDATE books SET font_family = ? WHERE id = ?').run(p.fontFamily ?? null, bookId)
  }
}
```

（仅注释行与签名、末尾新增 if 块三处变化，前两分支原样保留。）

- [ ] **Step 2: ipc.ts handler 签名扩字段**

```ts
  ipcMain.handle(
    'books:setReadingPref',
    (
      _e,
      bookId: number,
      p: { mode?: 'scroll' | 'page'; fontScale?: number | null; fontFamily?: string | null }
    ) => {
      setReadingPref(bookId, p)
      return true
    }
  )
```

- [ ] **Step 3: preload.ts 桥签名扩字段**

```ts
    /** 书级阅读偏好（只传要改的字段；fontScale/fontFamily null 清除回落全局） */
    setReadingPref: (
      bookId: number,
      p: { mode?: 'scroll' | 'page'; fontScale?: number | null; fontFamily?: string | null }
    ): Promise<boolean> => ipcRenderer.invoke('books:setReadingPref', bookId, p),
```

- [ ] **Step 4: api.d.ts 类型同步**

```ts
    setReadingPref(
      bookId: number,
      p: { mode?: 'scroll' | 'page'; fontScale?: number | null; fontFamily?: string | null }
    ): Promise<boolean>
```

- [ ] **Step 5: types.ts BooksRecord 加字段**

在 `font_scale: number | null` 字段（现 331 行）后追加：

```ts
  /** epub 书级字体族 CSS 串（字体选择轮 §1.2）；NULL=跟随个人档全局字体 */
  font_family: string | null
```

（books 全链 `SELECT *` 取行，列自动流入记录，无 SELECT 改动。）

- [ ] **Step 6: typecheck**

Run: `npm run typecheck`
Expected: 0 error。

---

### Task 4: ActionMenu 组件扩展（icon 可选 + 项级 style + 菜单过高滚动）

**Files:**
- Modify: `src/components/ActionMenu.tsx`
- Modify: `src/components/ActionMenu.css`

- [ ] **Step 1: ActionMenuItem 接口扩展**

```ts
import { useEffect, useRef, useState, type CSSProperties } from 'react'

export interface ActionMenuItem {
  key: string
  /** material symbols 图标名（禁 emoji）；可选——不传不渲染图标位（字体选择轮：字体项无勾不占位） */
  icon?: string
  label: string
  danger?: boolean
  separatorAbove?: boolean
  /** 项级样式透传（字体选择轮：fontFamily 自渲染预览） */
  style?: CSSProperties
  onClick: () => void
}
```

- [ ] **Step 2: 菜单项渲染透传**

渲染处（`items.map` 内 button）改为：

```tsx
          <button
            className={`action-menu-item${it.danger ? ' danger' : ''}`}
            style={it.style}
            onClick={() => {
              onClose()
              it.onClick()
            }}
          >
            {it.icon && <span className="material-symbols-outlined">{it.icon}</span>}
            {it.label}
          </button>
```

- [ ] **Step 3: css 加 max-height（12 项菜单过长兜底；格言菜单 7 项不受影响）**

`.action-menu` 规则内追加两行（现有属性不动）：

```css
  max-height: 70vh;
  overflow-y: auto;
```

- [ ] **Step 4: typecheck**

Run: `npm run typecheck`
Expected: 0 error（icon 变可选向后兼容，现有唯一使用方格言库全传 icon，行为不变）。

---

### Task 5: EpubReader 字体注入（prop + applyTheme + @font-face 进 iframe）

**Files:**
- Modify: `src/modules/bookshelf/EpubReader.tsx`

- [ ] **Step 1: Props 加 fontFamily + import 共享字体**

import 区加：

```ts
import { BUNDLED_FONTS } from '../../theme/fonts'
```

（`BUNDLED_FONTS` 已由 Task 1 导出。）

Props 接口 `fontScale: number | null` 后加：

```ts
  /** 书级字体族 CSS 串（null=跟随全局；字体选择轮 §1.3） */
  fontFamily: string | null
```

组件解构 `const { book, theme, mode, notes, fontScale } = props` 改为含 `fontFamily`。

- [ ] **Step 2: applyTheme 加第三参**

```ts
function applyTheme(rendition: Rendition | null, fontScale: number | null, fontFamily: string | null): void {
```

body 对象内 `'font-family'` 一行改为：

```ts
      'font-family': fontFamily ?? body.fontFamily,
```

三处调用点同步：`createRendition` 内 `applyTheme(rendition, fontScale)` → `applyTheme(rendition, fontScale, fontFamily)`；`[theme]` effect 与 `[fontScale]` effect 同改（后者 deps 改为 `[fontScale, fontFamily]`，注释补「字体族同机制即时重注入」）。

- [ ] **Step 3: @font-face 注入器（模块级函数，组件外）**

放在 `applyTheme` 函数之后：

```ts
/** 打包字体注入 iframe（设计 §1.4）：父文档 @font-face 进不了 iframe（独立 document），
 *  正文按 family 名解析不到打包字体会静默回落楷体——rendered 时注入修复。
 *  ?url 产物 dev 为根相对 / build 为相对路径，blob iframe 无基准可解析 → new URL 绝对化。 */
const FONT_FACE_STYLE_ID = 'bz-reader-fonts'
function injectFontFaces(doc: Document): void {
  if (doc.getElementById(FONT_FACE_STYLE_ID)) return
  const css = BUNDLED_FONTS.map(({ family, url }) => {
    const abs = new URL(url, window.location.href).href
    const fmt = url.endsWith('.otf') ? 'opentype' : 'truetype'
    return `@font-face{font-family:'${family}';src:url('${abs}') format('${fmt}');font-weight:100 900;font-display:swap;}`
  }).join('\n')
  const s = doc.createElement('style')
  s.id = FONT_FACE_STYLE_ID
  s.textContent = css
  doc.head.appendChild(s)
}
```

- [ ] **Step 4: createRendition 挂 rendered 钩子**

`createRendition` 内 `rendition.on('selected', ...)` 注册之前加：

```ts
    // 每个章节视图就绪（iframe 重建）注入打包字体（幂等）
    rendition.on('rendered', (_section: unknown, contents: Contents) => {
      try {
        injectFontFaces(contents.document)
      } catch {
        /* document 未就绪等，忽略 */
      }
    })
```

- [ ] **Step 5: typecheck**

Run: `npm run typecheck`
Expected: 0 error。若 `rendered` 事件类型报参签名不符，以 epub.js 实际类型（`(section: Section, contents: Contents)`）为准调整形参标注。

---

### Task 6: EpubReader 气泡修复 + 高光/批注横排

**Files:**
- Modify: `src/modules/bookshelf/EpubReader.tsx`
- Modify: `src/modules/bookshelf/bookshelf.css`

- [ ] **Step 1: rendition 三挂点收气泡**

`createRendition` 内、`rendered` 钩子之后加：

```ts
    // 收气泡（设计 §二）：iframe 内事件不冒泡到父文档——正文点击取消划词气泡残留的主根因；
    // 翻页/跳章坐标失效、滚动脱锚顺带即收
    rendition.on('mousedown', () => closeBubble())
    rendition.on('relocated', () => closeBubble())
```

`createRendition` 的 `rendition.display(...).then(...)` 块内（resize 逻辑之后）加滚动监听：

```ts
      // 滚动即收气泡（scrolled 模式气泡 absolute 挂 host 不随内容滚，脱锚悬空——设计 §二顺带项）
      const scroller = scrollerOf(host2)
      scroller.addEventListener('scroll', closeBubble, { passive: true })
```

（`.epub-container` 由 rendition 创建后常驻、章节 iframe 在其内换血，监听一次即可；模式切换父级换 key 全量重挂无泄漏。）

- [ ] **Step 2: 高光/批注横排**

`rendition.on('selected', ...)` 回调内，原 `b.append(mkBtn('高光', ...), mkBtn('批注', ...))` 改为：

```ts
      const row = document.createElement('div')
      row.className = 'bk-sel-row bk-sel-row-center'
      row.append(
        mkBtn('高光', () => {
          props.onAddNote({ cfiRange, quote: selText, note: '' })
          clearSel()
          closeBubble()
        }),
        mkBtn('批注', () =>
          mountNoteEditor(b, '', (text) => {
            props.onAddNote({ cfiRange, quote: selText, note: text })
            clearSel()
            closeBubble()
          })
        )
      )
      b.append(row)
```

- [ ] **Step 3: css 加居中修饰类**

`bookshelf.css` 的 `.bk-sel-row` 规则块后追加：

```css
.bk-sel-row-center {
  justify-content: center;
}
```

- [ ] **Step 4: typecheck**

Run: `npm run typecheck`
Expected: 0 error。

---

### Task 7: BookshelfModule 字体按钮 + 浮层

**Files:**
- Modify: `src/modules/bookshelf/BookshelfModule.tsx`

- [ ] **Step 1: import 与状态**

import 区加（React 既有、ActionMenu 与共享字体为新增）：

```ts
import ActionMenu from '../../components/ActionMenu'
import { FONT_FAMILIES, READER_FONTS } from '../../theme/fonts'
```

`useAppSettings()` 解构改 `const { theme, settings } = useAppSettings()`；状态区加：

```ts
  /** 字体浮层锚点（null=关） */
  const [fontAnchor, setFontAnchor] = useState<HTMLButtonElement | null>(null)
```

- [ ] **Step 2: setBookFont + 全局字体名**

`stepFont`/`resetFont` 之后加：

```ts
  /** 书级字体设置（null=跟随全局；字体选择轮 §1.2，镜像 stepFont） */
  const setBookFont = (value: string | null): void => {
    if (!reading || reading.format !== 'epub') return
    void window.api.books.setReadingPref(reading.id, { fontFamily: value }).then(() => {
      setReading((r) => (r && r.id === reading.id ? { ...r, font_family: value } : r))
    })
  }

  /** 「跟随全局（X）」动态名：反查个人档全局字体；未设置=系统 */
  const globalFontName = (): string => {
    const hit = FONT_FAMILIES.find((f) => f.value === settings[SettingsKeys.FontFamily])
    return hit ? hit.label.split('（')[0] : '系统'
  }
```

- [ ] **Step 3: 阅读条加字体按钮（epub 组、「默认」按钮之后）**

`reading.font_scale != null && (...)` 的「默认」按钮块之后、书签按钮之前插入：

```tsx
              <button
                className="btn bk-bar-btn"
                onClick={(e) => setFontAnchor(e.currentTarget)}
                title="字体（每书独立记忆）"
              >
                <span className="material-symbols-outlined">font_download</span>
              </button>
```

（冒烟若 `font_download` 连字在本地打包 material-symbols 0.47 拆成双图标——第 19 轮 notebook 先例——换 `text_fields`。）

- [ ] **Step 4: 浮层渲染（EpubReader 之前、阅读条 div 之后）**

`<div className="bk-reader-flex...">` 之前插：

```tsx
          {fontAnchor && (
            <ActionMenu
              anchorEl={fontAnchor}
              onClose={() => setFontAnchor(null)}
              items={[
                {
                  key: 'follow',
                  icon: reading.font_family == null ? 'check' : undefined,
                  label: `跟随全局（${globalFontName()}）`,
                  onClick: () => setBookFont(null)
                },
                ...READER_FONTS.map((f) => ({
                  key: f.family,
                  icon: reading.font_family === f.value ? 'check' : undefined,
                  label: f.label,
                  style: { fontFamily: f.value },
                  separatorAbove: f.family === 'Liyu Shoushu',
                  onClick: () => setBookFont(f.value)
                }))
              ]}
            />
          )}
```

（打包组首项 separatorAbove 分组；每项 `style.fontFamily` 即自渲染预览。）

- [ ] **Step 5: EpubReader 传 fontFamily**

`<EpubReader ... fontScale={reading.font_scale} />` 处加一 prop：

```tsx
                fontFamily={reading.font_family}
```

- [ ] **Step 6: typecheck + build**

Run: `npm run typecheck`
Expected: 0 error。

Run: `npm run build`
Expected: 构建成功（字体资产 `?url` 打包路径正常产出）。

---

### Task 8: 冒烟清单（开发者，重启 dev 触发 v32）

重启 `npm run dev` 后按设计文档 §四逐项验证（8 条）：字体浮层 12 项/自渲染/打勾 → 切霞鹜文楷正文真渲染（与楷体字形肉眼可辨）→ 书级记忆重开保留、「跟随全局」回落且 DB 为 NULL → 翻章/切模式字体保持（iframe 重注入）→ **划词后正文内点击气泡即收（主 bug）**、滚动/翻页收气泡、标注点击气泡正常开合 → 高光/批注并排居中 → 双主题目检 → 个人档字体设置页照常。

---

## Self-Review 记录

- **Spec 覆盖**：设计 §1.1 清单→Task 1/7；§1.2 链路→Task 2/3；§1.3 注入→Task 5；§1.4 @font-face→Task 5；§1.5 UI→Task 4/7；§2 气泡→Task 6；§3 横排→Task 6；§四验证→Task 7 build + Task 8。无缺口。
- **占位符**：无 TBD/「适当处理」类表述；所有代码步骤含完整代码。
- **类型一致性**：`setReadingPref` 五处签名一致（mode/fontScale/fontFamily）；`ReaderFont.family/url` 与注入器、菜单 items 的 `key: f.family` 一致；`BUNDLED_FONTS` 由 Task 1 导出、Task 5 消费，`filter` 谓词含类型收窄。
- **版本号**：v32（设计文档已同步修订并注明实况条款）。
