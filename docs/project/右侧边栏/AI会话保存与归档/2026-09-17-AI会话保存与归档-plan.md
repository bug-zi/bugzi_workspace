# AI 会话保存与归档 + 终端悬浮整窗宽 实施计划

> **For agentic workers:** 按任务顺序逐个执行，步骤用 `- [ ]` 勾选跟踪。**本项目 git 操作由开发者自理，计划内一律不含 git 步骤**；验证闸门 = `npm run typecheck`（双 tsconfig）+ `npm run build`（项目无测试框架，纯函数用临时断言脚本验证后即删——项目惯例）。

**Goal:** ①终端面板改悬浮整窗宽（左右各留 8px）；②学习/万象/致知己三频道每轮 AI 回复后出「继续对话/保存对话/归档对话」三选条，保存落模块原生载体、归档入回收站新「AI 会话」块（DB v45）。

**Architecture:** 设计正本 `docs/project/右侧边栏/AI会话保存与归档/2026-09-17-AI会话保存与归档-design.md`。三选条 + 保存小窗抽共享组件 `ChatDisposition.tsx` 供 AiSidebar 与 ChannelChatPanel 两承载面复用；归档复用 `discardToRecycle` 通用软删链路；zhijiji 保存复用既有 createQuestion/overwriteVersion/saveNewVersion 三通道零新增；learn/wiki 各新增一个免 AI 落卡通道。

**Tech Stack:** Electron 44 + React 19 + TypeScript + node:sqlite；主进程 `electron/`，渲染层 `src/`。

---

## 文件结构总览

| 动作 | 文件 | 职责 |
|---|---|---|
| Modify | `src/App.css` | 终端面板悬浮样式 + 三选条样式（.chat-disposition-*） |
| Modify | `src/components/terminal/TerminalPanel.tsx` | 仅顶部注释补一行悬浮说明（可选） |
| Modify | `electron/db/db.ts` | v45：ai_sessions.deleted_at |
| Modify | `electron/services/recycle.ts` | RecycleSource/TABLES/MD_FIELDS + restore/hardDelete 分支 |
| Modify | `electron/ai/services.ts` | listAiSessions 过滤软删 |
| Modify | `electron/ipc.ts` | aiSession:archive + learn:saveChatCard + wiki:saveChatCard |
| Modify | `src/preload.ts` | 三通道透传 |
| Modify | `src/renderer/api.d.ts` | RecycleRow.source 联合 + aiSession.archive + learn/wiki saveChatCard 声明 |
| Create | `src/components/ChatDisposition.tsx` + `src/components/ChatDisposition.css` | md 封装纯函数 + 三选条 + 保存落点小窗 |
| Modify | `src/components/AiSidebar.tsx` | 接入三选条/保存/归档/终结切新会话 |
| Modify | `src/components/ChannelChatPanel.tsx` | 同上（拓展坞面） |
| Modify | `src/modules/recycle/RecycleModule.tsx` | 「AI 会话」页签 |
| Modify | `docs/log/260917.md`、`docs/project/优化建议区.md`、`CLAUDE.md` | 文档收尾 |

---

### Task 1: 终端悬浮整窗宽（纯 CSS）

**Files:**
- Modify: `src/App.css:1604-1611`（`.terminal-panel` 规则块）

- [ ] **Step 1.1: 改 `.terminal-panel` 为悬浮层**

将现规则块：

```css
.terminal-panel {
  display: flex;
  flex-direction: column;
  border-top: 1px solid var(--color-border);
  background: var(--color-surface);
  min-height: 0;
  overflow: hidden;
}
```

整体替换为：

```css
/* 悬浮整窗宽（优化建议区第47轮）：左右下各留 8px 居中铺满，覆盖底部内容不挤压布局 */
.terminal-panel {
  position: fixed;
  left: 8px;
  right: 8px;
  bottom: 8px;
  z-index: 50; /* 盖主栏内容（右栏 5-20），低于全局弹窗（100+），ConfirmDialog 仍在其上 */
  display: flex;
  flex-direction: column;
  border: 1px solid var(--color-border);
  border-radius: 10px;
  background: var(--color-surface);
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.18);
  min-height: 0;
  overflow: hidden;
}
```

说明：组件 TSX 的内联 `style={{ height: open ? height : 0, display: open ? 'flex' : 'none' }}` 不动（fixed 元素 height 生效、display:none 照旧、pty 不死）；拖拽高度用 `window.innerHeight` 计算与 fixed 无冲突；悬浮尺寸变化由 TerminalTab 既有 ResizeObserver 自动 refit。页签条 `border-bottom` 与圆角经 `overflow: hidden` 自然裁齐。

- [ ] **Step 1.2: typecheck**

Run: `npm run typecheck`
Expected: 双 tsconfig 全绿（本任务纯 CSS，理论上必过，作回归闸）。

---

### Task 2: DB v45 + 归档后端链路

**Files:**
- Modify: `electron/db/db.ts:1059-1074`（v44 块之后追加）
- Modify: `electron/services/recycle.ts`
- Modify: `electron/ai/services.ts:30-34`
- Modify: `electron/ipc.ts:566` 后（aiSession:clear 之后）

- [ ] **Step 2.1: db.ts 追加 v45 迁移**

在 `if (version < 44) { ... }` 块之后、函数收尾 `}` 之前追加：

```ts
  if (version < 45) {
    // v45：AI 会话归档（优化建议区第47轮）——ai_sessions 软删标记；归档 = 置 deleted_at +
    // 写 recycle_bin('ai_session')，恢复清标记，彻底删连全部消息。v44 已被同日经验书占用顺延。
    d.exec('ALTER TABLE ai_sessions ADD COLUMN deleted_at TEXT')
    d.exec('PRAGMA user_version = 45')
  }
```

- [ ] **Step 2.2: recycle.ts 三处扩展**

① `RecycleSource` 联合类型（`electron/services/recycle.ts:7`）末尾 `| 'qa'` 后加一行：

```ts
  | 'ai_session'
```

② `TABLES` 映射（:28）：把末行 `qa: 'qa_records'` 改为带逗号的 `qa: 'qa_records',`，其下新增一行（对齐既有缩进）：

```ts
  ai_session: 'ai_sessions'
```

③ `MD_FIELDS` 映射（:52）`qa: 'md_path',` 行后新增（对齐既有缩进）：

```ts
  ai_session: null, // 会话消息为 DB 行，hardDelete 特判清理
```

④ `restoreFromRecycle` switch（:103）在 `case 'qa'` 块后加：

```ts
    case 'ai_session':
      // 回 AI 边栏原频道会话列表：仅清标记（激活指向失效由渲染层 load 兜底）
      d.prepare('UPDATE ai_sessions SET deleted_at = NULL WHERE id = ?').run(rb.item_id)
      break
```

⑤ `hardDelete`（:216）在 `if (rb.source === 'zhijiji')` 块之前加早退分支：

```ts
  if (rb.source === 'ai_session') {
    // 归档会话彻底删除：连全部消息一并删（无 md 附属）
    d.prepare('DELETE FROM ai_messages WHERE session_id = ?').run(rb.item_id)
    d.prepare('DELETE FROM ai_sessions WHERE id = ?').run(rb.item_id)
    d.prepare('DELETE FROM recycle_bin WHERE id = ?').run(recycleId)
    return
  }
```

- [ ] **Step 2.3: ai/services.ts 会话列表过滤软删**

`listAiSessions`（:30-34）SQL 改：

```ts
    .prepare('SELECT * FROM ai_sessions WHERE channel = ? AND deleted_at IS NULL ORDER BY updated_at DESC, id DESC')
```

（`getActiveSessionId` 不动——激活指向已归档会话时返回的 id 不在列表里，渲染层既有「active 不在列表则兜底」逻辑天然覆盖。`deleteAiSession` 内的「同频道自动切」子查询也不加过滤——删除是物理删，行为不变。）

- [ ] **Step 2.4: ipc.ts 归档通道**

在 `aiSession:clear` handler（:563-566）之后加（`discardToRecycle` 已在 ipc.ts import——learn:nodeDelete 在用）：

```ts
  // 归档会话（优化建议区第47轮）：软删 + 入回收站 ai_session 块；恢复/彻底删走 recycle 通用链路
  ipcMain.handle('aiSession:archive', (_e, id: number) => {
    discardToRecycle('ai_session', id)
    return true
  })
```

（`discardToRecycle` 通用路径对 ai_sessions 成立：UPDATE deleted_at + 整行 JSON 入 payload——title/channel 快照随行。）

- [ ] **Step 2.5: preload + api.d.ts**

`src/preload.ts` aiSession 段（`clear:` 之后）加：

```ts
    /** 归档会话到回收站「AI 会话」块（软删；恢复=回频道列表，彻底删=连消息） */
    archive: (id: number): Promise<boolean> => ipcRenderer.invoke('aiSession:archive', id),
```

`src/renderer/api.d.ts`：①aiSession 接口 `clear(...)` 后加 `archive(id: number): Promise<boolean>`；②`RecycleRow.source` 联合（:615）`| 'qa'` 后加 `| 'ai_session'`。

- [ ] **Step 2.6: typecheck**

Run: `npm run typecheck` → 全绿。

---

### Task 3: learn / wiki 免 AI 落卡通道

**Files:**
- Modify: `electron/ipc.ts`（learn:nodeDelete 附近与 wiki:updateEntry 附近各加一 handler；mdCreate/mdWrite/nowIso 均已 import）

- [ ] **Step 3.1: learn:saveChatCard**

在 `learn:nodeAdd` handler 之后加（对齐其插列口径，但不调 generateLearnCard、不碰 learn_daily，state 走表默认 'todo'）：

```ts
  // 对话保存落卡（优化建议区第47轮）：免 AI 知识点卡，md=对话封装；todo 态进学习队列由用户学会计数
  ipcMain.handle('learn:saveChatCard', (_e, topicId: number, title: string, md: string) => {
    const d = getDb()
    const t = title.trim()
    if (!t) throw new Error('TITLE_REQUIRED')
    const topic = d
      .prepare('SELECT domain_id FROM learn_nodes WHERE id = ? AND level = 1')
      .get(topicId) as { domain_id: number } | undefined
    if (!topic) throw new Error('NOT_FOUND')
    const dup = d
      .prepare('SELECT id FROM learn_nodes WHERE parent_id = ? AND title = ? AND deleted_at IS NULL')
      .get(topicId, t)
    if (dup) throw new Error('CONFLICT:' + t)
    const r = d
      .prepare(
        "INSERT INTO learn_nodes (domain_id, parent_id, level, title, source, created_at) VALUES (?, ?, 2, ?, 'manual', ?)"
      )
      .run(topic.domain_id, topicId, t, nowIso())
    const id = Number(r.lastInsertRowid)
    mdCreate(`md/learn/${id}.md`, md)
    return id
  })
```

- [ ] **Step 3.2: wiki:saveChatCard**

在 `wiki:updateEntry` handler 之后加（插列/两段式 md 路径对齐 generateWikiCard，ai/services.ts:1026-1034；state='learned' 不进待学习区）：

```ts
  // 对话保存落卡（优化建议区第47轮）：免 AI 词条卡，已学态直接入板块（不混待学习区批次）
  ipcMain.handle('wiki:saveChatCard', (_e, sectionId: number, title: string, md: string) => {
    const d = getDb()
    const t = title.trim()
    if (!t) throw new Error('TITLE_REQUIRED')
    if (!d.prepare('SELECT id FROM wiki_sections WHERE id = ?').get(sectionId)) throw new Error('NOT_FOUND')
    const dup = d.prepare('SELECT id FROM wiki_entries WHERE term = ? AND deleted_at IS NULL').get(t)
    if (dup) throw new Error('CONFLICT:' + t)
    const bodyLine = md.split('\n').find((l) => l.trim() && !l.trimStart().startsWith('#') && !l.trimStart().startsWith('>'))
    const summary = (bodyLine ?? '').replace(/\s+/g, ' ').trim().slice(0, 60) || `${t}（AI 对话）`
    const now = nowIso()
    const r = d
      .prepare(
        "INSERT INTO wiki_entries (section_id, term, summary, md_path, origin, state, created_at, updated_at) VALUES (?, ?, ?, 'PENDING', 'ai', 'learned', ?, ?)"
      )
      .run(sectionId, t, summary, now, now)
    const id = Number(r.lastInsertRowid)
    const mdPath = `md/wiki/${id}.md`
    d.prepare('UPDATE wiki_entries SET md_path = ? WHERE id = ?').run(mdPath, id)
    mdWrite(mdPath, md)
    return id
  })
```

- [ ] **Step 3.3: preload + api.d.ts**

preload.ts：learn 段加 `saveChatCard: (topicId: number, title: string, md: string): Promise<number> => ipcRenderer.invoke('learn:saveChatCard', topicId, title, md),`；wiki 段加同构 `saveChatCard: (sectionId: number, title: string, md: string): Promise<number> => ipcRenderer.invoke('wiki:saveChatCard', sectionId, title, md),`。api.d.ts 对应接口各加一行同签名声明。

- [ ] **Step 3.4: typecheck**

Run: `npm run typecheck` → 全绿。

---

### Task 4: 共享组件 ChatDisposition

**Files:**
- Create: `src/components/ChatDisposition.tsx`
- Create: `src/components/ChatDisposition.css`
- Modify: `src/App.css`（三选条样式追加到文件尾也可——**决策：样式放 ChatDisposition.css，App.css 不动**）

- [ ] **Step 4.1: ChatDisposition.tsx 全量**

```tsx
// AI 会话三选（优化建议区第47轮）：每轮 AI 回复后的「继续对话/保存对话/归档对话」处置条 +
// 保存落点小窗（按频道变体：learn→知识点卡 / wiki→词条卡 / zhijiji→问题版本）。
// AiSidebar 与 ChannelChatPanel 两承载面共用；终结式——保存/归档后由调用方切新会话。
import { useEffect, useState } from 'react'
import type { AiMessageRow } from '../renderer/api'
import { AI_NAME } from '../shared/types'
import { useToast } from './Toast'
import './ChatDisposition.css'

export type DispositionChannel = 'learn' | 'wiki' | 'zhijiji'

const DISPOSITION_CHANNELS: readonly string[] = ['learn', 'wiki', 'zhijiji']

export const isDispositionChannel = (ch: string): boolean =>
  DISPOSITION_CHANNELS.includes(ch)

const CHANNEL_LABEL: Record<DispositionChannel, string> = {
  learn: '学习·问答',
  wiki: '万象·问答',
  zhijiji: '致知己·追问'
}

/** 保存成功 toast 用的模块名 */
const MODULE_LABEL: Record<DispositionChannel, string> = {
  learn: '学习库',
  wiki: '万象库',
  zhijiji: '致知己'
}

/** 会话消息 → md 封装（原样完整对话，双击可再编辑；system 行剔除） */
export function buildChatTranscriptMd(
  title: string,
  channel: DispositionChannel,
  messages: AiMessageRow[]
): string {
  const now = new Date()
  const pad = (n: number): string => String(n).padStart(2, '0')
  const stamp = `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())} ${pad(now.getHours())}:${pad(now.getMinutes())}`
  const parts: string[] = [`# ${title}`, '', `> 保存于 ${stamp} · 来自「${CHANNEL_LABEL[channel]}」对话`, '']
  for (const m of messages) {
    if (m.role === 'system') continue
    parts.push(`## ${m.role === 'user' ? '用户' : AI_NAME}`, '', m.content.trim(), '')
  }
  return parts.join('\n')
}

/** 三选条（仅挂在最新一条 AI 回复下方；由调用方控制显隐条件） */
export function ChatDispositionBar(props: {
  onContinue: () => void
  onSave: () => void
  onArchive: () => void
}) {
  return (
    <div className="chat-disposition-bar">
      <button className="chat-disposition-btn" onClick={props.onContinue} title="收起选择，保留上下文继续对话">
        <span className="material-symbols-outlined">forum</span>
        继续对话
      </button>
      <button className="chat-disposition-btn primary" onClick={props.onSave} title="封装为 md 卡片存入对应模块">
        <span className="material-symbols-outlined">save</span>
        保存对话
      </button>
      <button className="chat-disposition-btn" onClick={props.onArchive} title="会话移入回收站「AI 会话」（3 天后自动清理）">
        <span className="material-symbols-outlined">archive</span>
        归档对话
      </button>
    </div>
  )
}

/** 最小结构行：领域/板块用 name，主题/问题用 title */
interface IdNameRow {
  id: number
  name: string
}
interface IdTitleRow {
  id: number
  title: string
}

export interface SaveChatDialogProps {
  open: boolean
  channel: DispositionChannel
  defaultTitle: string
  md: string
  onCancel: () => void
  /** 保存成功（target = 模块名，供 toast） */
  onSaved: (target: string) => void
}

export function SaveChatDialog(props: SaveChatDialogProps) {
  const { open, channel, defaultTitle, md, onCancel, onSaved } = props
  const { toast } = useToast()
  const [title, setTitle] = useState(defaultTitle)
  const [busy, setBusy] = useState(false)
  // learn：领域›主题 两级（主题含「＋ 新建主题…」）
  const [domains, setDomains] = useState<IdNameRow[]>([])
  const [domainId, setDomainId] = useState<number | null>(null)
  const [topics, setTopics] = useState<IdTitleRow[]>([])
  const [topicId, setTopicId] = useState<number | 'new' | null>(null)
  const [newTopic, setNewTopic] = useState('')
  // wiki：板块
  const [sections, setSections] = useState<IdNameRow[]>([])
  const [sectionId, setSectionId] = useState<number | null>(null)
  // zhijiji：新建问题 / 附加既有
  const [mode, setMode] = useState<'new' | 'append'>('new')
  const [questions, setQuestions] = useState<IdTitleRow[]>([])
  const [questionId, setQuestionId] = useState<number | null>(null)

  // 打开时重置表单 + 预载落点候选
  useEffect(() => {
    if (!open) return
    setTitle(defaultTitle)
    setBusy(false)
    setDomainId(null)
    setTopics([])
    setTopicId(null)
    setNewTopic('')
    setSectionId(null)
    setMode('new')
    setQuestionId(null)
    void (async () => {
      try {
        if (channel === 'learn') {
          const ds = (await window.api.learn.domains()) as IdNameRow[]
          setDomains(ds)
          if (ds.length > 0) setDomainId(ds[0].id)
        } else if (channel === 'wiki') {
          const ss = (await window.api.wiki.sections()) as IdNameRow[]
          setSections(ss)
          if (ss.length > 0) setSectionId(ss[0].id)
        } else {
          const qs = (await window.api.zhijiji.list()) as IdTitleRow[]
          setQuestions(qs)
        }
      } catch {
        /* 候选载入失败：提交时按空态拦截 */
      }
    })()
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [open, channel])

  // 选领域 → 载其主题
  useEffect(() => {
    if (channel !== 'learn' || domainId == null) {
      setTopics([])
      setTopicId(null)
      return
    }
    void (async () => {
      try {
        const ts = (await window.api.learn.tree(domainId)) as IdTitleRow[]
        setTopics(ts)
        setTopicId(ts.length > 0 ? ts[0].id : 'new')
      } catch {
        setTopics([])
        setTopicId('new')
      }
    })()
  }, [channel, domainId])

  if (!open) return null

  const canSubmit = (): boolean => {
    if (!title.trim()) return false
    if (channel === 'learn') {
      if (domainId == null) return false
      if (topicId === 'new') return !!newTopic.trim()
      return topicId != null
    }
    if (channel === 'wiki') return sectionId != null
    return mode === 'append' ? questionId != null : true
  }

  const submit = async (): Promise<void> => {
    if (busy || !canSubmit()) return
    setBusy(true)
    try {
      if (channel === 'learn') {
        let tid = topicId
        if (tid === 'new') tid = await window.api.learn.topicCreate(domainId as number, newTopic.trim())
        await window.api.learn.saveChatCard(tid as number, title.trim(), md)
      } else if (channel === 'wiki') {
        if (sectionId == null) {
          toast('暂无板块，请先在万象库创建板块')
          setBusy(false)
          return
        }
        await window.api.wiki.saveChatCard(sectionId, title.trim(), md)
      } else if (mode === 'append') {
        if (questionId == null) {
          toast('请选择要附加到的问题')
          setBusy(false)
          return
        }
        await window.api.zhijiji.saveNewVersion(questionId, md)
      } else {
        const r = await window.api.zhijiji.createQuestion(crypto.randomUUID(), title.trim())
        await window.api.zhijiji.overwriteVersion(r.versionId, md)
      }
      onSaved(MODULE_LABEL[channel])
    } catch (e) {
      const msg = String((e as Error).message)
      toast(msg.startsWith('CONFLICT:') ? `「${msg.slice(9)}」已存在，换个标题` : `保存失败：${msg.slice(0, 80)}`)
      setBusy(false)
    }
  }

  return (
    <div className="scd-overlay" onMouseDown={onCancel}>
      <div className="scd-panel" onMouseDown={(e) => e.stopPropagation()}>
        <div className="scd-head">
          <span className="material-symbols-outlined">save</span>
          <span>保存对话到{MODULE_LABEL[channel]}</span>
          <button className="icon-btn" onClick={onCancel} title="取消">
            <span className="material-symbols-outlined">close</span>
          </button>
        </div>
        <div className="scd-body">
          <label className="scd-field">
            <span className="scd-label">卡片标题</span>
            <input value={title} maxLength={60} onChange={(e) => setTitle(e.target.value)} autoFocus />
          </label>

          {channel === 'learn' && (
            <>
              <label className="scd-field">
                <span className="scd-label">领域</span>
                {domains.length === 0 ? (
                  <span className="scd-hint">知识树还没有领域，请先到学习库·知识树创建</span>
                ) : (
                  <select value={domainId ?? ''} onChange={(e) => setDomainId(Number(e.target.value))}>
                    {domains.map((d) => (
                      <option key={d.id} value={d.id}>
                        {d.name}
                      </option>
                    ))}
                  </select>
                )}
              </label>
              {domains.length > 0 && (
                <label className="scd-field">
                  <span className="scd-label">主题</span>
                  <select
                    value={topicId == null ? '' : String(topicId)}
                    onChange={(e) =>
                      setTopicId(e.target.value === 'new' ? 'new' : Number(e.target.value))
                    }
                  >
                    {topics.map((t) => (
                      <option key={t.id} value={t.id}>
                        {t.title}
                      </option>
                    ))}
                    <option value="new">＋ 新建主题…</option>
                  </select>
                </label>
              )}
              {topicId === 'new' && (
                <label className="scd-field">
                  <span className="scd-label">新主题名</span>
                  <input
                    value={newTopic}
                    maxLength={40}
                    placeholder="输入主题名"
                    onChange={(e) => setNewTopic(e.target.value)}
                  />
                </label>
              )}
              <div className="scd-hint">卡片以「待学习」进入该主题，由你点「学会了」计入每日新学。</div>
            </>
          )}

          {channel === 'wiki' && (
            <>
              <label className="scd-field">
                <span className="scd-label">落点板块</span>
                {sections.length === 0 ? (
                  <span className="scd-hint">万象库还没有板块，请先创建板块</span>
                ) : (
                  <select value={sectionId ?? ''} onChange={(e) => setSectionId(Number(e.target.value))}>
                    {sections.map((s) => (
                      <option key={s.id} value={s.id}>
                        {s.name}
                      </option>
                    ))}
                  </select>
                )}
              </label>
              <div className="scd-hint">卡片以「已学」直接进入板块，不占用每日待学习批次。</div>
            </>
          )}

          {channel === 'zhijiji' && (
            <>
              <div className="scd-modes">
                <button
                  className={`scd-mode${mode === 'new' ? ' active' : ''}`}
                  onClick={() => setMode('new')}
                >
                  新建问题
                </button>
                <button
                  className={`scd-mode${mode === 'append' ? ' active' : ''}`}
                  onClick={() => setMode('append')}
                >
                  附加到既有问题
                </button>
              </div>
              {mode === 'new' ? (
                <div className="scd-hint">以标题新建一个沉淀区问题，对话封装为它的首个版本。</div>
              ) : questions.length === 0 ? (
                <div className="scd-hint">沉淀区还没有问题，请改选「新建问题」</div>
              ) : (
                <label className="scd-field">
                  <span className="scd-label">选择问题</span>
                  <select value={questionId ?? ''} onChange={(e) => setQuestionId(Number(e.target.value))}>
                    <option value="" disabled>
                      选择要附加到的问题…
                    </option>
                    {questions.map((q) => (
                      <option key={q.id} value={q.id}>
                        {q.title}
                      </option>
                    ))}
                  </select>
                </label>
              )}
              <div className="scd-hint">对话将作为一个「对话存档」版本追加，原有版本不受影响。</div>
            </>
          )}
        </div>
        <div className="scd-foot">
          <button className="btn" onClick={onCancel}>
            取消
          </button>
          <button className="btn btn-primary" disabled={!canSubmit() || busy} onClick={() => void submit()}>
            {busy ? '保存中…' : '保存'}
          </button>
        </div>
      </div>
    </div>
  )
}
```

注意：`window.api.learn.domains/tree/topicCreate`、`window.api.wiki.sections`、`window.api.zhijiji.list` 均为 preload 既有通道（learn:domains :865 / learn:tree :973 / learn:topicCreate :1001 / wiki:sections :711 / zhijiji:list :2036），**不新增**。已核实形状：`learn.domains()` 返回 `LearnDomain[]`（显示字段 **name**）；`learn.tree(domainId)` 直接返回 `LearnTopicView[]`（**title**，非 `{topics}` 包裹）；`wiki.sections()` 返回 `WikiSection[]`（**name**）；`zhijiji.list()` 返回 `ZhijijiQuestion[]`（title）。

- [ ] **Step 4.2: ChatDisposition.css 全量**

```css
/* AI 会话三选条（AiSidebar 与 ChannelChatPanel 共用） */
.chat-disposition-bar {
  display: flex;
  justify-content: flex-end;
  gap: 6px;
  padding: 2px 4px 10px;
}
.chat-disposition-btn {
  display: flex;
  align-items: center;
  gap: 4px;
  border: 1px solid var(--color-border);
  border-radius: 999px;
  background: var(--color-surface);
  color: var(--color-text-secondary);
  font-size: 12px;
  padding: 3px 10px;
  cursor: pointer;
}
.chat-disposition-btn .material-symbols-outlined {
  font-size: 14px;
}
.chat-disposition-btn:hover {
  border-color: var(--color-primary);
  color: var(--color-text);
  background: var(--color-primary-soft);
}
.chat-disposition-btn.primary {
  border-color: var(--color-primary);
  color: var(--color-primary-deep, var(--color-primary));
}

/* 保存落点小窗（覆盖层与 MdDialog 同档，需压过弹窗内拓展坞） */
.scd-overlay {
  position: fixed;
  inset: 0;
  z-index: 310;
  background: rgba(0, 0, 0, 0.35);
  display: flex;
  align-items: center;
  justify-content: center;
}
.scd-panel {
  width: min(420px, 92vw);
  max-height: 80vh;
  overflow: auto;
  background: var(--color-surface-strong, var(--color-surface));
  border: 1px solid var(--color-border);
  border-radius: 10px;
  box-shadow: 0 16px 40px rgba(0, 0, 0, 0.22);
}
.scd-head {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 10px 14px;
  border-bottom: 1px solid var(--color-border);
  font-weight: 600;
}
.scd-head .material-symbols-outlined {
  font-size: 18px;
  color: var(--color-primary);
}
.scd-head .icon-btn {
  margin-left: auto;
}
.scd-body {
  padding: 12px 14px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.scd-field {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.scd-label {
  font-size: 12px;
  color: var(--color-text-secondary);
}
.scd-field input,
.scd-field select {
  border: 1px solid var(--color-border);
  border-radius: 6px;
  background: var(--color-surface);
  color: var(--color-text);
  padding: 6px 8px;
  outline: none;
}
.scd-field input:focus,
.scd-field select:focus {
  border-color: var(--color-primary);
}
.scd-hint {
  font-size: 12px;
  color: var(--color-text-secondary);
}
.scd-modes {
  display: flex;
  gap: 6px;
}
.scd-mode {
  border: 1px solid var(--color-border);
  border-radius: 999px;
  background: var(--color-surface);
  color: var(--color-text-secondary);
  font-size: 12px;
  padding: 3px 12px;
  cursor: pointer;
}
.scd-mode.active {
  border-color: var(--color-primary);
  background: var(--color-primary-soft);
  color: var(--color-text);
}
.scd-foot {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  padding: 10px 14px;
  border-top: 1px solid var(--color-border);
}
```

- [ ] **Step 4.3: typecheck（此时组件尚无使用方，应无未用导出告警；若 lint 报 unused 随接入任务消除）**

Run: `npm run typecheck` → 全绿。

---

### Task 5: 接入 AiSidebar（右栏承载面）

**Files:**
- Modify: `src/components/AiSidebar.tsx`

- [ ] **Step 5.1: import 与状态**

文件头 import 区加：

```tsx
import {
  ChatDispositionBar,
  SaveChatDialog,
  buildChatTranscriptMd,
  isDispositionChannel,
  type DispositionChannel
} from './ChatDisposition'
```

组件 state 区（`const [needConfig, ...]` 附近）加：

```tsx
  // 三选条（优化建议区第47轮）：本轮 AI 回复后待处置的会话 id；null=已消费/无
  const [dispositionSid, setDispositionSid] = useState<number | null>(null)
  const [saveOpen, setSaveOpen] = useState(false)
  const [archiveAsk, setArchiveAsk] = useState(false)
```

- [ ] **Step 5.2: 发送链路挂载/清除三选态**

`sendText` 普通发送路径（非斜杠命令分支），在乐观上屏 `setMessages(...)` 之前加一行：

```tsx
      setDispositionSid(null) // 新一轮开始，上一轮的三选条收起
```

成功路径 `await loadMessages(sid)` 之后加：

```tsx
      if (isDispositionChannel(channel) && sid != null) setDispositionSid(sid)
```

`loadForChannel` 与 `switchSession` 开头各加 `setDispositionSid(null)`（切频道/切会话即收起）；`doDelete` 的 session 分支开头加同款。

- [ ] **Step 5.3: 渲染三选条**

`.ai-list` 容器内、`{sending && (...)}` 块之后加：

```tsx
          {!sending &&
            dispositionSid != null &&
            dispositionSid === activeId &&
            isDispositionChannel(activeChannel) && (
              <ChatDispositionBar
                onContinue={() => setDispositionSid(null)}
                onSave={() => setSaveOpen(true)}
                onArchive={() => setArchiveAsk(true)}
              />
            )}
```

- [ ] **Step 5.4: 保存/归档流 + 弹窗挂载**

组件内（`doDelete` 之后）加：

```tsx
  /** 归档：会话入回收站「AI 会话」块，终结式切新会话 */
  const doArchive = async (): Promise<void> => {
    const sid = activeIdRef.current
    if (sid == null) return
    setArchiveAsk(false)
    await window.api.aiSession.archive(sid)
    setDispositionSid(null)
    toast('会话已移入回收站「AI 会话」')
    await newSession()
  }

  /** 保存成功：卡片已落模块，终结式切新会话 */
  const onChatSaved = (target: string): void => {
    setSaveOpen(false)
    setDispositionSid(null)
    toast(`已保存到${target}`)
    void newSession()
  }
```

组件 JSX 末尾 `<ConfirmDialog ...>` 之前加：

```tsx
      {(() => {
        const cur = sessions.find((s) => s.id === activeId) ?? null
        return (
          <SaveChatDialog
            open={saveOpen && activeId != null && isDispositionChannel(activeChannel)}
            channel={activeChannel as DispositionChannel}
            defaultTitle={cur?.title ?? '新对话'}
            md={buildChatTranscriptMd(
              cur?.title ?? '新对话',
              activeChannel as DispositionChannel,
              messages
            )}
            onCancel={() => setSaveOpen(false)}
            onSaved={onChatSaved}
          />
        )
      })()}
      <ConfirmDialog
        open={archiveAsk}
        title="归档对话"
        confirmText="归档"
        danger
        onConfirm={() => void doArchive()}
        onCancel={() => setArchiveAsk(false)}
      >
        将把当前会话移入回收站「AI 会话」（3 天后自动彻底删除，期间可恢复），并切换到新会话。确定归档吗？
      </ConfirmDialog>
```

- [ ] **Step 5.5: typecheck**

Run: `npm run typecheck` → 全绿。

---

### Task 6: 接入 ChannelChatPanel（拓展坞承载面）

**Files:**
- Modify: `src/components/ChannelChatPanel.tsx`

- [ ] **Step 6.1: import 与状态**

import 区加：

```tsx
import {
  ChatDispositionBar,
  SaveChatDialog,
  buildChatTranscriptMd,
  isDispositionChannel,
  type DispositionChannel
} from './ChatDisposition'
```

state 区加：

```tsx
  const [dispositionSid, setDispositionSid] = useState<number | null>(null)
  const [saveOpen, setSaveOpen] = useState(false)
  const [archiveAsk, setArchiveAsk] = useState(false)
```

- [ ] **Step 6.2: 发送链路**

`sendText` 普通路径：乐观上屏前加 `setDispositionSid(null)`；`if (sidRef.current === sid) await load()` 之后加：

```tsx
      if (isDispositionChannel(channel) && sidRef.current === sid) setDispositionSid(sid)
```

`switchSession` 与 `newSession` 开头各加 `setDispositionSid(null)`。

- [ ] **Step 6.3: 渲染三选条**

`.dlg-chat-list` 内 `{sending && (...)}` 块之后加：

```tsx
        {!sending &&
          dispositionSid != null &&
          dispositionSid === sidRef.current &&
          isDispositionChannel(channel) && (
            <ChatDispositionBar
              onContinue={() => setDispositionSid(null)}
              onSave={() => setSaveOpen(true)}
              onArchive={() => setArchiveAsk(true)}
            />
          )}
```

- [ ] **Step 6.4: 保存/归档流 + 弹窗挂载**

`doDeleteSession` 之后加：

```tsx
  /** 终结式切新会话（保存/归档后共用） */
  const concludeToNewSession = async (): Promise<void> => {
    const s = await window.api.aiSession.create(channel)
    await window.api.settings.set(sessionKey, String(s.id))
    sidRef.current = s.id
    setSessions((arr) => [s, ...arr])
    setMessages([])
  }

  const doArchive = async (): Promise<void> => {
    const sid = sidRef.current
    if (sid == null) return
    setArchiveAsk(false)
    await window.api.aiSession.archive(sid)
    setDispositionSid(null)
    toast('会话已移入回收站「AI 会话」')
    await concludeToNewSession()
  }

  const onChatSaved = (target: string): void => {
    setSaveOpen(false)
    setDispositionSid(null)
    toast(`已保存到${target}`)
    void concludeToNewSession()
  }
```

组件 JSX 末尾 `<ConfirmDialog ...>`（删除会话那个）之前加：

```tsx
      <SaveChatDialog
        open={saveOpen && sidRef.current != null && isDispositionChannel(channel)}
        channel={channel as DispositionChannel}
        defaultTitle={curSession?.title ?? '新对话'}
        md={buildChatTranscriptMd(curSession?.title ?? '新对话', channel as DispositionChannel, messages)}
        onCancel={() => setSaveOpen(false)}
        onSaved={onChatSaved}
      />
      <ConfirmDialog
        open={archiveAsk}
        title="归档对话"
        confirmText="归档"
        danger
        onConfirm={() => void doArchive()}
        onCancel={() => setArchiveAsk(false)}
      >
        将把当前会话移入回收站「AI 会话」（3 天后自动彻底删除，期间可恢复），并切换到新会话。确定归档吗？
      </ConfirmDialog>
```

（`curSession` 变量组件已有：`const curSession = sessions.find(...)`。）

- [ ] **Step 6.5: typecheck**

Run: `npm run typecheck` → 全绿。

---

### Task 7: 回收站「AI 会话」页签

**Files:**
- Modify: `src/modules/recycle/RecycleModule.tsx`

- [ ] **Step 7.1: 三处映射 + 页签数组**

① 来源→页签分组函数（:26 附近，`if (source === 'qa') return 'wiki'` 之后）加：

```tsx
  if (source === 'ai_session') return 'ai'
```

② 前缀标签函数（:81 附近）加：

```tsx
  if (source === 'ai_session') return 'AI 会话 · '
```

③ 摘要提取（:102 附近，payload 解析分支内）加：

```tsx
    if (row.source === 'ai_session') {
      const ch = String(p.channel ?? '')
      const chLabel =
        ch === 'learn' ? '学习' : ch === 'wiki' ? '万象' : ch === 'zhijiji' ? '致知己' : ch === 'assistant' ? '助手' : ch || 'AI'
      return `${p.title ?? ''}（${chLabel}频道）`
    }
```

④ 页签数组（`recycle-tabs` 渲染 :201 上方的 TABS 定义，执行时定位）**末位追加** `{ key: 'ai', label: 'AI 会话' }`（跨模块块置末位，不打乱既有「学习库居首」序）。

- [ ] **Step 7.2: typecheck**

Run: `npm run typecheck` → 全绿。

---

### Task 8: 构建验证 + 文档收尾

**Files:**
- Modify: `docs/log/260917.md`（不存在则创建）
- Modify: `docs/project/优化建议区.md`
- Modify: `CLAUDE.md`

- [ ] **Step 8.1: build**

Run: `npm run build` → 成功。

- [ ] **Step 8.2: dev log**

`docs/log/260917.md` 追加（文件已由同日经验书会话创建则续写；每个小节标题下附 `> 创建于 YYYY-MM-DD HH:MM`（用 `date` 取实际时间））：第 47 轮两实施小节（终端悬浮 / AI 会话三选），含关键决策（悬浮 fixed+8px、终结式、DB v45 顺延、zhijiji 零新通道、笔记本勘误）与待冒烟清单。

- [ ] **Step 8.3: 优化建议区归档第 47 轮**

`docs/project/优化建议区.md`：两条移出「待完成」，在归档区顶部（`### 第46轮` 之前）插入：

```markdown
### 第47轮（260917）：终端悬浮整窗宽 + AI 会话三选（保存/归档）

- [ ]  终端显示窗口需要能够铺满整个底部导航栏（横向长度不够，需要左右各留5-10px即可，居中布局显示）（260917 完成：需求确认为面板改**悬浮整窗宽**——澄清拍板悬浮覆盖而非文档流挤压；`.terminal-panel` 改 `position: fixed; left/right/bottom: 8px`、z-index 50 低于全局弹窗、圆角+描边+柔和投影走既有主题变量零彩亮；DOM 与 TSX 零改动（fixed 相对视口、拖高用 window.innerHeight、ResizeObserver 自动 refit），open=false 仍 display:none 进程不死。**待冒烟**：整窗宽居中、拖高、收起重开、弹窗压终端之上、双主题）

- [ ]  右侧和AI对话交互的时候……（原始描述全文保留）……其他模块你看是否需要统一补充；（260917 完成：brainstorming 四决策定稿——**终结式**三选（每轮回复后仅最新一条 assistant 下方挂「继续对话/保存对话/归档对话」条，继续=收起；保存/归档=动作后自动切新会话，旧会话保留历史列表/入回收站）+ **原生载体选落点**（学习库→领域›主题下拉（含新建主题）落 todo 态知识点卡不静默计每日新学；万象库→板块下拉落 learned 态词条卡不混待学习区〔澄清勘误：笔记本是划词高光页签非板块，默认落第一板块〕；致知己→新建问题（对话为首版）或既有问题追加「对话存档」版本，三路全部复用/新增主进程单点写 IPC）+ **归档入回收站新「AI 会话」块**（DB v45 ai_sessions.deleted_at〔v44 被同日经验书占用顺延〕；恢复=回频道会话列表，彻底删=连消息，3 天自动清理照常；RecycleSource/TABLES/restore/hardDelete 四处扩展+页签末位）+ **范围仅三频道**（学习·问答/万象·问答/致知己·追问；助手无归属、辩真有验证记录、格言解读产出短不补）。共享组件 ChatDisposition（md 封装+三选条+落点小窗）AiSidebar 与 ChannelChatPanel（学习库拓展坞/致知己追问栏）两承载面复用。typecheck 双配置 + build 通过。**待冒烟**：三频道×两面挂条位置、保存三路落卡与状态口径、归档→回收站新页签→恢复/彻底删、终结切新会话两面同步、LLM 未配置无条、其余频道回归、双主题。设计/计划见 `右侧边栏/AI会话保存与归档/2026-09-17-AI会话保存与归档-{design,plan}.md`）
```

（第二条归档条目内「原始描述全文保留」处，实施时把优化建议区原文整段复制进去——归档规则要求保留原始描述。）

- [ ] **Step 8.4: CLAUDE.md 同步**

①「全局硬性规则」三栏布局条目的右侧边栏括注补一句：`；学习/万象/致知己三频道每轮 AI 回复后有三选条（继续对话/保存对话〔落模块原生载体选落点〕/归档对话〔回收站「AI 会话」块〕，终结式切新会话，260917）`。②「回收站」条目补：`另有「AI 会话」块（归档的 AI 频道会话，恢复回边栏，260917）`，顺带把陈旧「按来源分八块」改为「按来源分块」。

---

## 冒烟清单（实施完成后交开发者）

1. **终端**：Ctrl+J 打开悬浮面板——左右下各 ~8px 居中铺满；拖高；收起重开回显正常（老 bug 回归点）；ConfirmDialog/弹窗压终端之上；双主题。
2. **三选条**：learn/wiki/zhijiji 三频道 × 右栏/拓展坞两面——AI 回复后仅最新一条下方挂条；点「继续对话」收起；再问一轮重现；切频道/切会话/删会话不残留；assistant/motto/verify/prophet 频道与「学习库划词问 AI 经拓展坞」回归。
3. **保存**：学习库（选主题落卡 todo 态、进队列、点学会了才计每日新学；新建主题路径；重名报错）；万象库（选板块落卡 learned 态、不进待学习区、重名报错）；致知己（新建问题=对话首版；附加=新版本不覆盖旧版）；卡片 md 双击可编辑；保存后自动切新会话且旧会话在历史列表可回看、两面（右栏/拓展坞）激活一致。
4. **归档**：确认弹窗 → 回收站「AI 会话」页签可见（标题+频道徽章+时间）→ 恢复回对应频道列表 → 彻底删连消息（重开 App 会话消失）→ 3 天自动清理口径（代码路径同既有 cleanupExpired）。
5. **边界**：LLM 未配置无回复无条；生成失败/取消不挂条；重启后历史消息不挂条。
