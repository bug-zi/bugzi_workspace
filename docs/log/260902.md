# 260902 开发日志

今日工作：与开发者交互式完善全部 8 份 design.md 文档（会话开始时 /init 生成项目级 CLAUDE.md）。

## 跨模块统一规则（本次确定）

1. 回收站按来源分四板块：格言库 / 万象库 / 灵感泉 / 辩真阁。
2. 「恢复」统一回到来源模块的最初级区（格言库/灵感泉→草稿区，万象库→原板块，辩真阁→历史列表）。
3. 主题切换双入口：左侧边栏底部图标 + 个人中心 App 设置。
4. 3 天自动彻底删除的检查时机：App 启动时 + 运行期间每日零点。

## 各模块文档变更

- **回收站**：由 3 行扩写为完整 v1.0（四板块、恢复/删除规则、附属内容封存、剩余存活时间展示）。
- **格言库**：开发者手动修复——移除「归档」概念，三区统一为「丢弃」→ 回收站。AI 补充：各区操作明细、正式区手动新增+批量导入、生成前查重、失败提示可重试、定时设置入口在模块内、笔记弹窗双击切换编辑态。
- **万象库**：补全板块管理（增/改名/删空板块）、知识卡片固定模板（定义+解释+举例+启示）、划词浮动气泡（高光/问AI）、笔记本独立页签可跳回。高光规则定为：高亮标记只保留在卡片中，笔记本只记录文本。
- **灵感泉**：列表行=标题+更新时间、每区「+」新建、拖拽+菜单双移动方式、AI 生成灵感定为 v1 不做。
- **辩真阁**：由一句话扩写为完整 v1.0（自动验证+边栏可见过程、分析文字+可信度百分比、保留来源链接、重复验证提示）。
- **个人中心**：头像仅上传、LLM 多服务商+默认、字体设置即时生效自动保存、MCP 地址+开关。**删除了拼写笔误文件 desing.md**（内容已并入 design.md）。
- **样式**：写入参考色值（浅 #E8A0BF/#F8BBD0，深 #1A237E/#283593）、背景图统一命名 bg-light/bg-dark、图标定 Material Symbols Outlined、背景铺满全窗口+半透明卡片。

## 统一组件约定（跨模块）

md 弹窗（格言笔记 / 万象卡片 / 灵感文档 / 辩真记录详情）为同一组件：默认渲染态、双击切换编辑态。

## 待办 / 建议

- 所有 designs-specs.md 仍为空，为下一步工作。
- 建议（待开发者确认）：将本次确定的跨模块规则（回收站四板块、恢复规则、主题双入口）补充进《总需求文档.md》，该文档由开发者维护，AI 不擅自修改。

## 信息同步（260902 补充）

- 权责更新：`总需求文档.md` 为开发者主导、AI 可协助完善（需审核通过）的共同维护文档。
- 新增 `docs/project/问题疑惑区.md`：开发者记录问题疑惑处，AI 不主动查阅，开发者指明时才阅读答疑。

## 设计完备性审查（260902 第二轮）

与开发者确认了 12 项全局决策，已写入《总需求文档.md》（AI 起草、待审核）：技术栈 Electron+React+TS、SQLite+md 文件存储、App 名 bugzi's workspace、回收站四板块/恢复规则/3天删除检查时机、md 弹窗统一组件、主题双入口、LLM 未配置统一降级（提示+直达配置）、AI 助手自由对话+模块感知（v1 单会话）、定时任务错过即跳过、首次启动欢迎引导、侧边栏模块顺序、背景图可替换（个人中心新增背景图设置项）。

结论：设计层面已完备，可进入 designs-specs.md 生成阶段。

## designs-specs.md 生成（260902 第三轮）

应开发者要求生成全部 7 份 designs-specs.md（此前答疑确认：AI 能力由 App 直连 LLM 服务商 API，主进程发起，无自建后端——已写入总需求文档第 2 条）：

| specs | 覆盖内容 |
|---|---|
| 样式 | 工程骨架（electron-vite、目录结构、主/渲染进程 IPC 架构、SQLite+md 存储布局）、主题系统（CSS 变量双套、双入口切换、背景图机制）、Material Symbols Outlined 图标、三栏布局、AI 助手边栏（自由对话+模块感知+单会话持久）、全局 MdDialog 组件 |
| 回收站 | recycle_bin 表（软删除+快照兜底）、四板块页签、恢复/删除、3 天自动清理（启动+零点）、各模块接入约定 |
| 格言库 | mottos 表、三区流转、AI 生成（prompt 构造/查重/定时/失败处理/错过跳过）、正式区手动新增+批量导入、笔记 md |
| 万象库 | 板块/词条/高光三表、固定模板卡片生成（随机+手动、已存在拦截）、划词气泡（高光 `==标记==` 入卡片+笔记本、问AI 推边栏）、笔记本页签 |
| 灵感泉 | inspirations 表、四区看板、每区新建、拖拽+菜单移动、MdDialog、归档区不自动删除 |
| 辩真阁 | verify_records 表、自动验证流程（重复检测→前置检查→MCP 检索→过程推边栏）、可信度+来源链接、详情 md |
| 个人中心 | settings 表 key 清单、字体即时生效、主题/背景图设置、LLM 多服务商+默认、MCP 配置、首次启动三步引导 |

骨架归属决策：应用工程骨架/主题/三栏/AI 边栏/MdDialog 归入样式 specs；首次启动引导归入个人中心 specs；回收站表全局定义在回收站 specs。

下一步：开发者简单审核 7 份 specs → 按 样式 → 格言库 → 万象库 → 灵感泉 → 辩真阁 → 个人中心 → 回收站 顺序开发。

## 1.0 开发（260902 第四轮，AI 执行）

应开发者要求「完成项目的 1.0 开发」，基于 7 份 designs-specs.md 初始化全部代码。

### 技术选型落地

- **数据库**：node:sqlite（DatabaseSync）+ WAL + 外键，PRAGMA user_version 版本化迁移。弃 better-sqlite3 以避开 Windows 下原生 ABI 重编译；为此将 Electron 从 33 升至 44（需 Node ≥22.13，内置 node:sqlite）。
- **进程架构**：渲染层只做 UI，全部 IO（SQLite/md/LLM HTTP/MCP）在主进程；contextBridge 类型化 IPC，contextIsolation 开、nodeIntegration 关、sandbox 关（preload 需要 require）。
- **图片资源**：bzres:// 特权协议（app ready 前注册 scheme + protocol.handle）服务 userData 内头像/背景图，含路径穿越防护。
- **md 渲染**：marked + DOMPurify，`==高光==` 后处理为 `<mark>`；外链 shell.openExternal。
- **图标**：material-symbols npm 包本地 woff2（遵守禁 emoji 规则）。
- **LLM 客户端**：OpenAI 兼容 /v1/chat/completions，主进程 fetch + Bearer；jsonMode（response_format）；URL 规范化自动补 /v1/chat/completions。
- **MCP 客户端**：仅 Streamable HTTP 传输（JSON-RPC POST + mcp-session-id + SSE 响应解析）。
- **依赖坑**：vite@8 与 electron-vite@5 peer 声明（≤^7）冲突 + React 19 与 plugin-react peer 冲突，`.npmrc` 固化 `legacy-peer-deps=true` 解决（裸 npm install 即可）。

### 代码结构（v1.0 全量）

```
electron/           主进程：main/ipc/preload/db(db,settings)/services(files,bzres,recycle,scheduler)/ai(llm,mcp,services)
src/shared/         types.ts（SettingsKeys/接口）、api.d.ts（window.api 类型）
src/renderer/       api.d.ts 同步
src/theme/          global.css、ThemeProvider（双主题+背景图+字体即时生效+首启判断）
src/components/     MdDialog（渲染/双击编辑/划词气泡）、AiSidebar、Toast、ConfirmDialog、GoConfigDialog
src/modules/        mottos / wiki / inspirations / verify / recycle / profile（含 WelcomeGuide）
src/App.tsx/.css    三栏布局与六模块挂载
```

- DB v1 建 9 表：settings、mottos（draft/settled/formal + note_path）、wiki_sections（唯一名）、wiki_entries、wiki_highlights、inspirations（status+sort）、verify_records、ai_messages、recycle_bin；初始 5 板块与默认设置。
- IPC 全通道：settings/md/image:pick/item:discard/recycle/ai/mottos/wiki/inspirations/verify/llm:test/shell；`recycle:changed`、`ai:message` 主→渲染推送。
- 定时器：格言生成（settings 可配 hh:mm，错过即跳过）、零点回收清理（重排 setTimeout，unref）。
- 全部「丢弃/删除」走 ConfirmDialog 二次确认（全局硬性规则）。

### 验证结果

- `npm run typecheck`（tsconfig.node + tsconfig.web）**通过**
- `npm run build`（electron-vite build）**成功**：34 modules，renderer index.js 749.89 kB
- 依赖安装：`.npmrc` 修复后裸 `npm install` 通过（vite 8.2.2 / electron-vite 5.0.0 / plugin-react 6.1.1）

### 待办（交下轮）

- `npm run dev` 人工冒烟走查（首启引导/模块切换/主题/弹窗交互）；AI 生成、辩真验证需真实 LLM/MCP 配置实测。
- 设计文档与实现的差异复核（specs 验收清单逐条过）。
- git 提交。

## 优化建议实施（260902 第五轮，AI 执行）

开发者新建 `docs/project/优化建议区.md`（开发者记录使用中遇到的优化建议，AI 据此实施，完成后移入归档区）；已登记进 CLAUDE.md 文档体系权责表。

### 建议 #1：LLM 配置支持从上游获取模型列表

- 主进程 `llm.ts` 新增 `listUpstreamModels()`：OpenAI 兼容 `GET /v1/models`（URL 规范化复用 chat 同款逻辑），解析 `data[].id` 并排序返回。
- IPC 新增 `llm:models` 通道，preload 暴露 `window.api.llm.models()`，`api.d.ts` 同步类型。
- 个人中心 LLM 编辑弹窗：模型名输入框旁新增「获取模型」按钮（需先填 API 地址），拉取成功后出现下拉列表供选择；修改 API 地址自动清空已拉取列表。手动输入仍可用。

### 建议 #2：深色主题配色优化

参考图（深色模式颜色参考.png）实测采样：深海军蓝底 `#0E1D3C`、点缀蓝 `#66A4FF`/`#1C6B98`、白字。原配色饱和靛蓝（#1a237e/#283593）偏紫、观感刺眼，替换为去饱和海军蓝色系：

| 变量 | 旧值 | 新值 |
|---|---|---|
| `--color-primary` | #1a237e | #2e5fa3 |
| `--color-primary-soft` | #283593 | #1f3a6b |
| `--color-primary-deep`（链接/强调） | #9fa8f5 | #8ab0f0 |
| `--color-text-secondary` | #c3c9f0 | #9aa8c7 |
| `--color-surface` | rgba(40,53,147,.55) | rgba(21,38,74,.6) |
| `--color-surface-strong` | rgba(26,35,126,.92) | rgba(13,23,47,.92) |
| `--color-border` | rgba(159,168,245,.35) | rgba(102,164,255,.25) |
| `--color-danger` | #ff8fab | #e87a93 |
| `.app-bg` 兜底色 | #10154a | #0e1d3c |
| 欢迎引导深色预览块 | #283593→#1a237e 渐变 | #1b3a6b→#0d1930 渐变 |

叠在主色上的 `color:#fff` 前景（badge/user 消息气泡等）对比度仍充足，未动。

### 验证

- `npm run typecheck` 通过（本轮建议 #1 涉及 TS 改动）。

### 待办

- `npm run dev` 切深色主题人工观察配色效果；LLM「获取模型」对真实上游实测（不同服务商 /models 返回格式可能有差异）。
- 优化建议区第 3 条内容未写完（仅一个「给」字），待开发者补充后实施。
- git 提交（由开发者执行）。

## 问题疑惑区问题修复（260902 第六轮，AI 执行）

开发者首启冒烟后在《问题疑惑区.md》登记 3 个问题，全部定位修复：

1. **头像不显示**：bzres:// 协议处理器把 host（root）错误拼进磁盘路径，`bzres://root/avatar.png` 被解析为 `userData/root/avatar.png` → 404。重写解析：host 仅作命名空间（root=根目录、bg=背景目录），不计入路径。
2. **背景图更换不生效**（两个叠加 bug）：① image:pick 存背景图目标路径漏扩展名（存成 `bg/bg-light`）而 settings 记 `bg-light.png` → 永远 404，已修正为带扩展名存储；② index.html CSP `connect-src 'self'` 拦截 ThemeProvider 的 bzres: fetch 探测导致回退纯色，已放行 `bzres:`。另做 DB v2 迁移自愈：旧无扩展文件自动改名规范名 + 从项目根种子默认背景图（bg-light.png/bg-dark.png，不覆盖用户已传图）。
3. **格言笔记弹窗关闭键失效**：MdDialog 缺 onClose prop（关闭按钮 onClick 只处理编辑态而非编辑态渲染为空操作），遮罩/Esc 同样失效。补 onClose prop 并统一 close()（编辑态先保存草稿再关），5 处调用方（格言/万象/灵感/辩真×2）全部接上。

### 验证

- typecheck 双 project 通过；重启 dev 后实测：DB 迁移至 v2，bg 目录出现 bg-light.png（旧文件改名）+ bg-dark.png（种子），settings 键值正确，窗口正常打开。

### 待办

- 开发者肉眼复核：头像显示、浅/深色背景图渲染、弹窗关闭（含编辑态关闭自动保存）。

## 背景图问题第二层根因修复（260902 第七轮，AI 执行）

开发者反馈背景图仍未显示，二轮排查定案：scheme 未注册 `corsEnabled` 特权。dev 页面源为 http://localhost:5173，ThemeProvider 用 fetch(bzres://bg/...) 探测 404 属跨源请求，Chromium 直接拒绝（"Cross origin requests are only supported for protocol schemes: chrome, http, https..."）→ 探测 catch 回退纯色。头像走 <img> 标签不受 CORS 限制，故头像正常、背景异常，与现象吻合。

修复：`electron/services/bzres.ts` privileges 增加 `corsEnabled: true`；`src/theme/ThemeProvider.tsx` 探测逻辑容错（fetch 异常不再清空 url，CSS url() 加载本就不走 CORS）。

验证方法记录：写独立复现脚本 scripts/test-bzres.cjs（本地 http 页面源 + 可切换 privileges），复现 FETCH_FAIL → 加 corsEnabled 后 FETCH_OK，锁定根因；再以 `REMOTE_DEBUGGING_PORT=9222 npm run dev` 起真实 app，经 CDP（scripts/cdp-check.cjs）在渲染进程实测：cssVar 与 computedStyle 均为 url("bzres://bg/bg-light.png")、fetch status=200、IMG_OK。开发者重启实测确认背景正常显示。

清理：scripts/ 下 test-bzres.cjs、cdp-check.cjs、check-bg.cjs、capture-screen.ps1 为本次排查临时脚本，可留作诊断工具或删除。typecheck 通过。所有改动未提交，git 由开发者操作。

## 优化建议实施·第二批（260902 第八轮，AI 执行）

实施优化建议区第 3、4 条（完成后已移入归档区）。

### 建议 #3：格言列表改单行显示

- `MottosModule.tsx` 列表行由「格言上 / 出处下」两行改为单行 `.motto-line`（flex）：格言居左、出处 `margin-left:auto` 右对齐；两侧均超长省略（出处限宽 40%）。
- `App.css` 新增 `.motto-line / .motto-content / .motto-source` 样式；`.row-sub` 保留（其他模块仍在用）。

### 建议 #4：数据存储位置改 D 盘默认 + 个人中心数据管理

- 新增 `electron/services/storage.ts`：
  - 内置默认数据目录 `D:\Paper\app_output\bugzi_workspace`；`applyDataDirAtStartup()` 在 app ready 前 `app.setPath('userData', ...)` 切换（D 盘不可用时回退系统默认，保证可启动）。
  - 自定义位置经系统默认目录（`%APPDATA%\bugzi_workspace`）下的 `data_home.json` 指针文件定位；无指针/指针目标不存在 → 用内置默认。
  - `migrateDataDir()`：closeDb（WAL checkpoint 落盘）→ 复制应用自有数据（bugzi.db 三件套 / md / bg / avatar.*）→ 校验 → 写/删指针 → 清理旧目录数据文件。目标已有 bugzi.db 拒绝（TARGET_HAS_DATA），防误覆盖。
- `db.ts` 新增 `closeDb()`（checkpoint + close，迁移专用）。
- IPC：`storage:currentDir / pickDir / migrate / openDir / relaunch`；preload 与 api.d.ts 同步。
- 个人中心新增「数据存储」区块（App 设置下方）：显示当前路径、资源管理器打开、修改存储位置（选目录 → 二次确认弹窗 → 迁移 → toast → 自动 relaunch 重启生效）。

### 一次性迁移（已执行）

- 排查发现 D 盘目标目录已有一份 12:43 产生的残缺库（0 条格言、无用户设置）——先删其三件库文件再迁移，md/bg 保留复用。
- 关键坑：C 盘 bugzi.db 主文件仅 4KB 空壳，**真实数据（6 条格言、12 项设置）全在 506KB 的 WAL 里**；直接复制主库得到空库。先对 C 盘库 `PRAGMA wal_checkpoint(TRUNCATE)` 合并（主库变 69KB）再复制，校验通过（mottos=6 / settings=12 / sections=5 / user_name=bug子，md/bg/avatar 逐文件 cmp 全同）。
- 迁移确认后删除 `C:\Users\Administrator\AppData\Roaming\bugzi_workspace` 整个目录（Roaming 已无残留；进程排查确认当时无 electron 实例运行；bugzia.exe 为另一 Tauri 应用，未动）。

### 验证

- `npm run typecheck` 通过。
- D 盘库以 node:sqlite 只读直开校验内容完整。

### 待办

- 开发者 `npm run dev` 冒烟：① 格言列表单行+出处右对齐观感；② 应用现在直接读 D 盘数据（无需 data_home.json，D 盘即默认）；③ 个人中心数据存储区块：路径显示/打开/修改迁移全流程（迁移会自动重启）。
- 优化建议区新增第 5 条（整理项目根目录文件）待下轮实施。
- git 提交（由开发者执行）。

## 优化建议实施·第三批：根目录文件整理（260902 第九轮，AI 执行）

实施优化建议区第 5 条（完成后已移入归档区）。盘点根目录散落文件后与开发者确认两项决策：配色参考图移入 docs 归档、调试脚本删截图留脚本。

改动明细：

- **背景图种子资源**：新建 `resources/`，`bg-light.png` / `bg-dark.png` 移入（根目录减 2 个约 2MB 大文件）。`electron/db/db.ts` v2 迁移的种子路径同步改为 `join(app.getAppPath(), 'resources', ...)`，注释更新。仅影响新装用户首次启动种子复制，已装用户数据目录不受影响。
- **配色参考图归档**：`深色模式颜色参考.png` → `docs/project/样式/深色模式颜色参考.png`（与样式 design.md 同处，作为历史设计依据）；优化建议区归档项中的绝对路径引用同步更新为相对路径。
- **调试产物清理**：删除 `scripts/screen-check.png`、`scripts/window-check.png`（一次性排查截图）；5 个调试脚本（test-bzres.cjs / cdp-check.cjs / cdp-shot.cjs / check-bg.cjs / capture-screen.ps1）保留，日志引用其作为验证方法记录。
- **`.gitignore` 增加 `*.tsbuildinfo`**：根目录 tsconfig.node/web.tsbuildinfo 是被误 git 跟踪的 tsc 增量缓存。本地文件保留（增量编译加速），建议开发者执行 `git rm --cached tsconfig.node.tsbuildinfo tsconfig.web.tsbuildinfo` 取消跟踪。
- README.md 项目结构节补 `resources/` 一行。

整理后根目录（不含目录）：.gitignore / .npmrc / CLAUDE.md / README.md / package.json / package-lock.json / electron.vite.config.ts / tsconfig.json / tsconfig.node.json / tsconfig.web.json + 2 个 tsbuildinfo（已被 ignore，待取消跟踪）。

### 验证

- `npm run typecheck` 通过（db.ts 路径改动无类型影响）。

### 待办

- 开发者 `npm run dev` 冒烟：新环境首启种子背景图仍能复制成功（已装环境无影响，可跳过）；更关键的是打包场景下 `app.getAppPath()` 指向 asar 时 resources 路径需随打包配置确认（v1 暂未配置打包，暂无影响）。
- 开发者 git 操作：移动/删除的文件（含两张 png 的 rename 检测）+ `git rm --cached` 两个 tsbuildinfo，一并提交。

## 优化建议实施·第四批：格言复制/序号拖拽/划词主题色（260902 第十轮，AI 执行）

实施优化建议区待完成三条（完成后已移入归档区）。三项设计决策先与开发者确认：序号每区独立编号、新格言插到区首、复制格式「格言 —— 出处」（无出处只复制正文）。

### 建议 #6：格言条目复制按键

- IPC 新增 `clipboard:writeText`（`electron/ipc.ts` 主进程 clipboard 模块，dev http 页面源下比渲染层 navigator.clipboard 可靠）；preload、api.d.ts 同步暴露 `window.api.clipboard.writeText`。
- `MottosModule.tsx` 三区每条格言操作区加 `content_copy` 图标按钮（删除键左侧、编辑键右侧，三区统一），复制后 toast「已复制」。

### 建议 #7：格言序号 + 拖拽排序（DB v3）

- **DB v3 迁移**：mottos 加 `sort REAL NOT NULL DEFAULT 0`；存量按原显示顺序（区内 id 倒序）逐区赋 0..n-1，迁移后观感不变。
- **排序规则**：`mottos:list` 改 `ORDER BY sort, id`；新 IPC `mottos:reorder`（moves 全量覆盖 0..n-1，与灵感泉 reorder 同构）。
- **插到区首**（四个入口全覆盖）：`mottos:create`（新增/批量导入）与 `mottos:setStatus`（流转）取目标区 MIN(sort)-1；`generateMottos`（AI 生成 10 条）headSort 逐条递减，先插入的排更前；`restoreFromRecycle`（回收站恢复回草稿区）同样取 MIN-1。
- **UI**：行首 `.row-index` 序号（每区独立 1..n，tabular-nums 右对齐）；行 HTML5 拖拽区内排序（拖到某行上插入该位置，纯前端重排数组后 reorder 全量写回，无半差 sort 残留）；跨区仍走流转按钮（「按钮=流转、拖拽=排序」语义清晰）；`.row-item.dragging` 半透明（原 `.kanban-card.dragging` 泛化，两模块共用）+ `.motto-row { cursor: grab }`。
- MottoRecord 类型补 `sort: number`。

### 建议 #8：划词颜色主题化

- `global.css` 两主题块新增 `--color-selection` 变量 + 全局 `::selection { background: var(--color-selection); color: var(--color-text) }`。
- 浅色：樱花粉半透明 `rgba(232,160,191,0.45)`；深色：参考图点缀蓝 `rgba(102,164,255,0.45)`（深色下用亮蓝保证选中区与背景区分度）。

### 验证

- `npm run typecheck` 双 project 通过。

### 待办

- 开发者 `npm run dev` 冒烟：① 复制键（有出处/无出处两种格言）；② 三区各自序号 1..n；③ 区内拖拽排序（拖 A 到 B 上松手 → 插到 B 位置）；④ 新增/AI 生成/回收站恢复的格言出现在区首；⑤ 两主题下任意文本划词，选中色为粉/亮蓝而非系统蓝。
- 开发者 git 提交（由开发者执行）。


## 问题疑惑区三项修复（第三轮）

### 1. 格言生成「LLM 未返回 JSON 数组」

- 根因：所配 LLM 中转（token.aiedulab.cn）json_object 模式强制顶层为对象，与「返回数组」指令冲突时模型固定输出 `{"error":"无法按要求生成内容"}`（合法 JSON 非数组），命中 `parseJsonArray` 报错，重试也同。经真实请求取证定位（json_object 失败 → 去 response_format 成功 → json_object+对象包裹成功，三组对照）。
- 修复（electron/ai/services.ts）：①prompt 改要求 `{"mottos":[...]}` 对象包裹；②`parseJsonArray` 兼容对象形态（自动取第一个数组字段），保留数组/\`\`\`json 兼容。真实 LLM 端到端验证 10 条解析成功。

### 2. 万象库生成「半天生成不出来」验证

- 用真实配置复现两场景全通：随机（词条名 2.8s + 卡片 11.6s）、「博弈论+纳什平衡」10.7s，模板四标题全命中。**主进程无 bug**，慢体验实为问题 3 的表现。

### 3. 切页生成失效 → 全模块 keep-alive

- 根因：App.tsx 条件渲染切模块即卸载组件，生成状态与结果回调丢失。
- 修复：六模块常驻挂载仅切换可见性（`.module-live` display:contents / `.module-hidden` display:none），AI 生成切页不中断、回来直接见结果，格言/万象/辩真三处受益。
- 次生问题配套：①MdDialog window 级 Esc 会误关隐藏模块的不可见弹窗 → 改为 getBoundingClientRect 探测可见后再响应（dialog-overlay 是 fixed，offsetParent 恒 null 不可用）；②失去「切回重挂即刷新」→ 新增 `MODULE_ACTIVATED_EVENT`（App 层 activateModule 派发），`useModuleActivated` hook（src/hooks/）五模块接入切回刷新。
- 验证：typecheck + build 通过。UI 实测需重启（当前运行实例为旧代码，且单实例锁，AI 不便代杀开发者进程）。

### 待办

- 开发者重启 App 实测：①格言库「来10条格言」；②万象库随机来一条（等待期切走再切回，卡片应正常弹出）；③手动输入博弈论+纳什平衡；④各模块切回数据刷新正常；⑤隐藏模块中开着弹窗时按 Esc 无异常。
- 开发者 git 提交（由开发者执行）。

## 优化建议实施·第五批：六条待办清零（260902 第十一轮，AI 执行）

实施优化建议区全部 6 条待办（完成后已移入归档区，待完成区清空）。

### 建议 #9：回收站时间显示改造

`src/modules/recycle/RecycleModule.tsx`

- 「入站 xxx ｜ 剩 N 天」改为「放入回收站时间 ｜ 彻底删除时间」，均精确到分钟（`2026/9/2 13:51 ｜ 2026/9/5 13:51`）。
- 彻底删除时间 = 入站时间 + 3 天（`purgeTime()`），与 `cleanupExpired` 清理口径一致；行悬停 title 标注字段含义。

### 建议 #10：万象库手动生成占位词条名跟随板块

`src/modules/wiki/WikiModule.tsx`

- 新增 `SECTION_TERM_EXAMPLES`：经济学→通货膨胀、法学→无罪推定、心理学→认知失调、博弈论→囚徒困境、历史神话→特洛伊战争；自定义板块回退「词条名」。
- 手动生成弹窗 placeholder 按当前选中板块实时联动。

### 建议 #11：字体列表更换（内置 5 款手写/楷体，约 110MB 随应用打包）

- 字体文件自 `D:\APRA\Resource\字体\` 复制入 `src/renderer/assets/fonts/`（规范英文命名）。
- `global.css` 新增 5 条 `@font-face`（`font-weight: 100 900` + `font-display: swap`），用户无需安装字体。
- `FONT_FAMILIES`：删宋体/黑体/等线，增 5 款（漓雨手书/鸿雷板书简体/霞鹜文楷/玄宗体/演示悠然小楷），保留系统楷体；存量旧字体值自动回退「默认（系统）」。

### 建议 #12：字体粗细 100-900 每 100 一档

- select（400/500/700）改 range 滑块，9 档实时生效。

### 建议 #13：字体大小默认 16px、范围 10-24px

- `--font-size` 默认 15→16px；滑块 min 12→10、max 20→24。

### 建议 #14：格言库快速导航栏

- `MottosModule.tsx` 模块头下新增三区 pill 导航（带条目计数，样式同回收站页签）；`jumpToZone` 点击后自动展开折叠区（双 rAF 等渲染）+ `scrollIntoView` 平滑滚动。
- 开发者反馈迭代：跳到正式区后导航滚出视野无法跳回 → 改为 **sticky 吸顶 + 整条磨砂底**（`--color-surface-strong` + `backdrop-filter: blur(6px)`，解决此前放弃吸顶时半透明背景穿透穿帮的问题）；跳转定位以 `.mottos-page .zone { scroll-margin-top: 50px }` 补偿吸顶高度，区标题不被盖住。
- 二次反馈：吸顶后与主栏顶部有 18px 空隙（Chromium 滚动容器的上内边距不随滚动消失）→ `top: -18px` 负偏移抵消，吸顶时贴住主栏真正顶边。
- 三次反馈（材质）：导航条改**半透明亚克力**——两主题各新增 `--acrylic-bg / --acrylic-border / --acrylic-highlight` 变量（浅色白玻璃、深色海军蓝玻璃），条体 `blur(14px) saturate(1.4)` + 顶部 inset 高光 + 底部投影；按钮改透明底玻璃钮扣（悬停高光层），磨砂从整条透出而非「实心底板贴钮扣」。

### 验证

- `npm run typecheck` 双 project 通过；`npm run build` 通过（5 款字体以 hash 文件名正确打入 out/renderer/assets/）。

### 待办

- 开发者 `npm run dev` 冒烟：①回收站两时间点格式；②手动生成弹窗切板块看 placeholder；③5 款字体切换渲染（含粗细滑块全档）；④字体大小 10-24 范围与默认 16；⑤格言库导航跳转（含折叠区展开）。
- 开发者 git 提交（由开发者执行）。

## 问题疑惑区第四轮：两个 bug 修复（260902）

1. **万象库卡「生成中」**：`wiki:generate` handler 同步返回 `{ok, data: Promise}`，嵌套 Promise 过不了 IPC 结构化克隆（v8 serialize 实测 `#<Promise> could not be cloned`），invoke 永不 settle，`setGenerating(false)` 永不执行。修复：handler 改 async + `await generateWikiCard(...)`（ipc.ts，同 `mottos:generate` 构型）。
2. **灵感泉 md 首开空白**：MdDialog 渲染 effect 竞态——loading 分支卸载 bodyRef，`setContent` 触发 effect 时 ref 为 null 早退，`setLoading(false)` 重挂 bodyRef 后 deps 无新变化 effect 不重跑；进编辑态（editing 是 deps）才重跑。修复：渲染 effect deps 补 `loading` + `!loading` 守卫。四个模块共用组件一并受益。

验证：typecheck 通过；v8 结构化克隆实验确证根因 A。UI 实测待开发者重启（万象库两种生成方式完成后按钮弹回并自动开卡片；灵感泉首开即渲染正文）。

### 待办

- MCP 疑惑（Tavily vs firecrawl 选型 + AI 辅助 MCP 配置功能方案）本轮未处理，待下一轮。
- 开发者 git 提交。

## 问题疑惑区第五轮：MCP 疑惑解答 + AI 辅助 MCP 配置功能实施（260902）

### 疑惑解答（详见问题疑惑区.md「疑惑区（已解答）」）

- **选型**：Tavily 主（`?tavilyApiKey=` 拼 URL 零改动、1000 credits/月、搜索定位正对口辩真阁）；Firecrawl 备（keyless 免账号试用）。
- **配置字段**：McpConfig 需扩展 authType + apiKey（Bearer 鉴权服务无法靠名称+URL 覆盖）。

### 功能实施（开发者确认后开工）

1. **类型扩展**（`src/shared/types.ts` + `src/renderer/api.d.ts`）：`McpConfig` 加可选 `authType('none'|'bearer')` + `apiKey`；新增 `McpResearch`/`McpResearchKey` 研究结果类型（urlTemplate 含 `{apiKey}` 占位符语义）。
2. **bearer 支持**（`electron/ai/mcp.ts`）：McpSession 抽出统一 `headers()`（rpc 与 initialized 通知都带 `Authorization: Bearer`），向后兼容（无 authType 行为不变）。
3. **研究服务**（`electron/ai/mcpResearch.ts` 新文件）：三步降级——①官方 Registry API（`registry.modelcontextprotocol.io/v0/servers?search=`，取 `remotes[].url` 远程端点 + `environmentVariables` 密钥声明，实测可用）→ ②LLM 生成候选文档 URL + llms.txt 惯例路径 → fetch → LLM 提取结构化配置（Tavily/Firecrawl llms.txt 实测可抓）→ ③LLM 自有知识兜底（不确定时拒绝编造）。全失败抛带指引的中文错误。
4. **IPC + preload**：`mcp:research` / `mcp:test`（initialize + tools/list 返回工具名列表）。
5. **个人中心 UI**（ProfileModule）：MCP 区新增「AI 帮我配置」入口 → 两阶段弹窗（输入名称 → 研究结果表单：来源徽章/端点预览/密钥填写+申请跳转/鉴权方式/文档链接/研究 notes）→「测试连接」（列工具名）→ 需密钥服务强制「填 key + 测试通过」才能保存；「重新研究」可回退。MCP 列表行加 Bearer 徽章。global.css 补 `.spin` 旋转动画（研究中图标）。

### 验证

- `npm run typecheck` 双 project 通过。
- Registry API 与两家 llms.txt 的真实数据结构已 curl 实测核对（解析代码非猜测）。
- UI 冒烟待开发者重启：个人中心 → MCP 配置 → AI 帮我配置 → 输入 Tavily → 研究应命中 Registry（io.github.tavily-ai/tavily-mcp）；Firecrawl 走文档/LLM 路径。

### 待办

- 开发者 git 提交。

## 问题疑惑区第六轮：AI 辅助 MCP 配置三处修复（260902）

开发者实测「AI 帮我配置 Tavily」暴露三个症状（同一条因果链）：

1. **研究选错条目**：Registry 里官方 tavily-ai 条目只有 stdio 包（无 remote），唯一带 remote 的是第三方网关 pipeworx——旧逻辑「见 remote 就用」让它当选 → 显示「无需鉴权」、无 TAVILY_API_KEY 申请入口。
2. **测试连接假成功**：pipeworx 网关 initialize 成功但返回 0 工具，旧逻辑视为「连接成功」。
3. **辩真阁报错**：保存的 0 工具配置在 `findSearchTool` 找不到 search 类工具 →「未找到可用的搜索工具」。

修复（`electron/ai/mcpResearch.ts`，typecheck 通过）：

- **官方优先分类**：`researchViaRegistry` 返回 `{official | officialRepo + fallback}`——owner 含关键词判官方；官方+remote 直接用；官方仅 stdio → 拿官方仓库 URL 转第②步查文档（GitHub 仓库自动转 raw README 抓取，实测 Tavily README 含 `mcp.tavily.com/mcp/?tavilyApiKey=<your-api-key>`）；第三方 remote 仅当全路径失败时兜底且标注「非官方，自担风险」。
- **占位符归一** `normalizeUrlTemplate`：`<your-api-key>`/`YOUR_API_KEY`/`<tavilyApiKey>` 等文档写法自动归一为 `{apiKey}`，测试连接与保存时自动拼入用户 key。
- **0 工具防线**：`testMcpConnection` 返回 0 工具抛「连接成功但未提供任何工具（可能是占位网关或需订阅），请换官方端点」。

验证：真实 Registry 数据跑分类单测（tavily-ai 官方条目当选 officialRepo、pipeworx 进 fallback）；占位符归一 4 用例通过；raw README 抓取含端点确认。UI 冒烟待开发者重启。

### 待办

- 开发者重启 dev 后复测：AI 帮我配置 → Tavily → 应给出 mcp.tavily.com 端点 + TAVILY_API_KEY 申请入口；填 key 测试连接应列出工具；辩真阁验证可用。
- 开发者 git 提交。

## 问题疑惑区第七轮：MCP 客户端 JSON-RPC 信封解包修复（260902）

开发者实测 Tavily：研究/密钥入口正常，填 key 后测试连接仍报「连接成功但未提供任何工具」——上一轮 0 工具防线被触发，暴露真正根因：

**根因（v1.0 遗留 bug，首次对真实 MCP 服务器跑通即现形）**：`mcp.ts` 的 `rpc()` 把 SSE data 解析后直接返回，但真实响应的 data 是 **JSON-RPC 完整信封** `{"result":{...}}`（用 Firecrawl keyless 端点实测确认），工具在 `.result.tools`；`listTools` 取 `res?.tools` 少解一层信封 → 恒为空数组。上一轮 pipeworx「0 工具」十有八九也是此解析 bug 而非网关问题。

**修复**（`electron/ai/mcp.ts` rpc()，typecheck 通过）：payload 解包——`{"error"}` 抛错、`{"result"}` 返回 `p.result`、其他原样返回。所有 MCP 调用（listTools/callTool/initialize）统一受益。

**验证**：复刻修复后 rpc 流程对 Firecrawl keyless 端点全链路实测——initialize ✓（firecrawl-fastmcp）、tools/list ✓（3 工具：firecrawl_scrape/search/parse）、`findSearchTool` 正则命中 firecrawl_search ✓、tools/call 真实搜索返回结果文本 ✓（即辩真阁完整链路可用）。

待开发者重启复测：Tavily 填 key 测试连接应列出工具；辩真阁验证全流程。git 提交由开发者执行。

## 问题疑惑区第八轮：LLM 429 限流自动重试（260902）

开发者辩真阁验证报 `LLM 服务返回 429：Concurrency limit exceeded for account, please retry later`。

**定位**：`runVerification` 流程本身串行（关键词 LLM → 3 次 MCP 搜索 → 综合 LLM），无自身并发；429 为中转服务（token.aiedulab.cn）**账号级并发限制**——同 key 其他设备/进程占用额度即触发，且服务明示「please retry later」＝瞬态错误，等待重试即可，非代码 bug。

**修复**（`electron/ai/llm.ts` chatCompletion，typecheck 通过）：429 自动退避重试最多 3 次——优先尊重 `Retry-After` 响应头（上限 15s），否则 2s/4s/8s 指数退避；重试耗尽仍 429 才抛错给用户。全部 AI 功能（辩真阁/格言库/万象库/AI 边栏/MCP 研究）统一受益，无需逐处改动。

待开发者重启复测辩真阁。git 提交由开发者执行。

## 问题疑惑区第九轮：万象库划词气泡「高光/问 AI」点击无响应修复（260902）

开发者报：万象库划词后「高光」「问 AI」两个气泡正常弹出，但点击均无响应，两个功能全不可用。

**根因**（`src/components/MdDialog.tsx` 划词气泡 effect）：document 级 `mouseup` 监听在每次 mouseup 开头无条件 `removeBubble()`。DOM 事件顺序为 mousedown→mouseup→click——点击气泡按钮时，mouseup 阶段按钮即被移出 DOM，Chromium 不向脱离文档的元素派发 click，按钮监听永不触发。气泡能弹出（划词检测/定位正常）只有点击失效，与现象完全吻合。CSS 已排除（fixed/z-index 300，无 pointer-events 问题）。

**验证先行**（Playwright 真实鼠标事件序列，复刻页 `output/playwright/sel-bubble-repro/`）：1:1 复刻气泡交互模式——现状模式拖拽划词后真实点击按钮，回调计数 =0（复现 click 被吞）；加气泡内守卫后 =1（修复确认）。

**修复**（MdDialog.tsx `onMouseUp` 加守卫，typecheck 通过）：mouseup 目标在气泡内时直接 return 不移除气泡，按钮 click 派发后由自身监听完成动作（onHighlight/onAskAi）+ 移除气泡 + 清空选区。

待开发者重启复测：万象卡片正文划词 → 点「高光」应弹「已加入高光」toast、正文高亮、笔记本新增记录；点「问 AI」右侧边栏应展开并预填提问。git 提交由开发者执行。

## 优化建议区：AI 助手多会话对话记录管理（260902）

开发者唯一待完成事项「侧边栏的AI助手的对话记录管理（开启新对话，删除对话记录等等）」。brainstorming 流程逐项澄清：会话模型=多会话可切换；删除粒度=会话+单条消息均可删；命名=首条用户消息自动命名（前 20 字）+ 双击改名；系统消息进当前激活会话；UI=头部按钮+边栏内浮层。方案对比（SQLite 会话表 / JSON 文件 / JSON 大列）后定 SQLite 方案，设计经开发者确认后实施。

**实现**（typecheck 通过，8 处改动）：

- **DB v4**（`electron/db/db.ts`）：新建 `ai_sessions` 表（id/title/created_at/updated_at），`ai_messages` 加 `session_id` 列（REFERENCES + 索引）；存量消息回填进「历史对话」会话并设为激活——升级后原记录可见可续聊。
- **服务层**（`electron/ai/services.ts`）：会话 CRUD（list 按 updated_at DESC / create / rename 截 50 字 / delete 连消息同删且删激活时自动切换）；`listAiMessages(sessionId)`、`appendAiMessage` 带 sessionId 并 bump 会话 updated_at；`aiChat` 上下文改为该会话最近 30 条非系统消息、首条用户消息自动命名；新增 `appendSystemToActiveSession`（无激活会话时自动新建接收，标题取消息前缀）。
- **IPC**（`electron/ipc.ts` + `electron/preload.ts` + `src/renderer/api.d.ts`）：`ai:messages`/`ai:chat` 加 sessionId；新增 `ai:deleteMessage`、`aiSession:list/create/rename/delete/active`；激活会话存 settings 新键 `ai_active_session_id`（切换复用 settings:set）；`ai:message` 推送带 session_id。
- **渲染层**（`src/components/AiSidebar.tsx` + `.css`）：头部新增会话列表按钮（list 图标）；浮层含「+ 新对话」、会话项（单击切换/双击行内改名 Enter 确认 Esc 取消/hover 删除键）、相对时间；消息 hover 右上角删除键；删除会话与删除消息均走 ConfirmDialog 二次确认（danger）；点浮层外部收起；启动恢复上次激活会话；无激活会话时发送自动新建。

边界处理：全删光后发送/系统消息推送均自动建会话；发送失败的本地临时消息（负 id）不显示删除键；自动命名/改名回退默认标题「新对话」。

待开发者 `npm run dev` 冒烟：旧记录在「历史对话」；新建/切换/改名/删除会话；删单条消息；辩真阁推送落当前会话。git 提交由开发者执行。

## 格言库 v2.0 实施（260902 第十轮，AI 执行）

应开发者要求实现 design.md v2.0 四项（标签分类/搜索/5+5 生成/去重完善）。specs 增量已先写入 designs-specs.md §7（交互决策：标签行内管理+顶部筛选条、搜索全局三区、去重三项全做，待开发者审核）。

### 主进程

- **DB v5**（`electron/db/db.ts`）：`mottos` 加 `tags TEXT NOT NULL DEFAULT '[]'`（JSON 字符串数组）。注意磁盘上 v4 已被 AI 多会话迁移占用，tags 顺延为 v5。新增导出 `isDupMotto(existing, norm)`：规范化后完全一致，或互为子串且双方规范化长度 ≥ 6 → 判重（§7.4.2 包含判重，门槛防短句误杀）。
- **IPC**（`electron/ipc.ts`）：`mottos:list` 返回前把 tags JSON 列解析为数组；`mottos:create`/`mottos:update` 加可选 tags 参数（update 不传=不动标签，传数组=覆盖）；新增 `mottos:setTags`（覆盖式）、`mottos:checkDuplicate`（未删除区内判重，供手动新增/批量导入复用）。
- **生成逻辑**（`electron/ai/services.ts` `generateMottos`）：
  - **5+5**（§7.3）：prompt 要求恰好 5 条摘录（kind=excerpt，真实出处）+ 5 条编撰（kind=composed，出处标「AI 编撰」）；kind 缺失/非法按出处含「AI 编撰」推断；不强制校验比例。
  - **LLM 主动避开**（§7.4.1）：prompt 附三区+回收站全部已有格言清单（按 updated_at DESC 最多 500 条防超长），明确禁止重复或微小改写。
  - **去重增强**（§7.4.2/§7.4.3）：比对集合去掉 `deleted_at` 过滤（含回收站），判重规则换 `isDupMotto`（一致或互为子串），批内互斥随插入扩充集合；返回值加 `excerptInserted`/`composedInserted`。
- **preload + api.d.ts**：同步 `MottoRecord.tags`、create/update 可选 tags、`setTags`、`checkDuplicate`、generate 新返回类型。

### 渲染层（MottosModule.tsx + App.css）

- **搜索**（§7.2）：模块标题行右侧搜索图标 → 展开输入框（自动聚焦，Esc 清空失焦），实时过滤三区（匹配正文/出处/标签，不区分大小写）；再点图标收起清空。
- **标签筛选条**（§7.1）：快速导航下方，聚合全部标签（使用数降序、同数按名称），点击单选筛选、再点取消；无标签不渲染。
- **行内标签编辑**：每行新增「sell」图标键，行下方展开编辑条——已有标签胶囊（点 × 删除）+ 输入框回车添加（trim/去空/同名去重）；行上出处右侧显示前 3 个标签胶囊 + `+n` 溢出（title 全量）。
- **组合逻辑**：过滤 = 搜索 AND 标签；区计数过滤时显示 `命中/总数`；区内有数据但无匹配显示「无匹配」空态（区别于「暂无格言」）。
- **弹窗**：新增/编辑弹窗加标签输入框（逗号/顿号分隔）；批量导入查重改走 `mottos:checkDuplicate`（原为渲染层简单空白归一比对）。
- **生成 toast**：`本次生成 10 条（摘录 X + 编撰 Y），去重后入库 N 条`。

### 验证

- `npm run typecheck`（双 project）通过。

### 待办（交开发者）

- `npm run dev` 冒烟：重启后 DB 自动迁移 v5（存量 tags='[]'）；打标签/筛选/搜索/5+5 生成实测（生成需真实 LLM）。
- designs-specs.md §7 交互决策审核（标签行内管理等三处 AI 按惯例定的点）。
- git 提交（由开发者执行）。

## 格言库 v2.0 导航栏整合 + 快速添加标签（260902 第十一轮，AI 执行）

开发者对 v2.0 提三点完善，全部实施：

1. **搜索移入吸顶导航栏右侧**：原模块标题行的搜索入口移除，改到与「草稿/沉淀/正式」区导航同一条吸顶栏的右侧（滚动时常驻随时可用）；图标展开输入框逻辑不变（实时过滤、Esc 清空、再点收起）。
2. **标签入口入导航栏**：右侧新增标签图标（sell），点击弹出亚克力浮层列出全部已建标签（含计数），点选即筛选并收起；筛选生效时图标按钮高亮并显示当前标签名；点浮层外部收起（mousedown 判定，开关按钮与浮层同容器不误关）。顶部标签汇总条保留（两入口同数据源、单选联动）。
3. **快速添加已有标签**：行内标签编辑条在输入框前排「虚线胶囊」（其他格言已用而本条未打的标签，最多 8 个，点击即加）；新增/编辑弹窗的标签输入框下方同款胶囊，点击以顿号衔接追加到输入内容。

specs §7.1/§7.2 已同步（筛选双入口、导航栏搜索入口、快速添加胶囊）。typecheck 双 project 通过。

补充（同日，开发者反馈）：导航栏标签浮层排版改为**单行显示**——`flex-wrap: nowrap` + 胶囊不压缩（flex-shrink:0）+ 超宽横向滚动（max-width min(640px,62vw)，细滚动条），不再折行堆叠。纯 CSS 改动，无 TS 变更。

## 格言库：AI 徽章仅编撰条显示（260902 第十二轮，AI 执行）

开发者指令：「来10条格言」生成的 10 条不应全打 AI 徽章——5 条 AI 编撰的打，5 条现实摘录的不打。

- **DB v6**（`electron/db/db.ts`）：`mottos` 加 `gen_kind TEXT`（'excerpt' 现实摘录 | 'composed' AI 编撰 | NULL 手动录入）；存量 `origin='ai'` 行按出处含「AI…编撰」回填 composed、其余回填 excerpt（v1 prompt 约定编撰条出处标「AI 编撰」，回填后存量摘录条徽章同步消失）。
- **生成落库**（`electron/ai/services.ts`）：`generateMottos` INSERT 增落 `gen_kind`（`mottoKind` 判定结果）。
- **渲染层**（`MottosModule.tsx`）：徽章条件改 `origin==='ai' && gen_kind !== 'excerpt'`；类型同步 `api.d.ts`（`gen_kind` 可空）。

typecheck 双 project 通过。待开发者重启 dev：DB 迁移 v6 后，已有生成条中出处非「AI 编撰」的徽章消失；新生成 10 条仅编撰 5 条带徽章。git 提交由开发者执行。

待开发者重启 dev 冒烟：滚动后导航栏搜索/标签筛选仍可达；标签浮层点选筛选、外部点击收起；行内与弹窗的虚线胶囊快速添加。git 提交由开发者执行。

## 优化建议区（第二轮）：边栏五项优化（260902）

开发者补充五条待办（拖拽调宽 / AI 消息排版渲染 / 会话改名 / 删消息滚动位置 / app 图标），合并呈现方案确认后实施，typecheck 通过。

1. **边栏拖拽调宽**：左缘 7px 拖拽条（col-resize），拖动实时写 `:root` 的 `--ai-width`（280–560px，默认 320），松手持久化 settings 新键 `ai_width`，启动恢复；收起态不渲染。
2. **AI 消息 md 渲染**：新建共享组件 `src/components/MdView.tsx`（marked+DOMPurify+==高光==+外链事件委托走系统浏览器），`MdDialog` 删除本地 renderMd 改为引用同一实现（行为不变）；边栏 AI 回复用 MdView 渲染，用户/系统消息保持纯文本；CSS 覆写 pre-wrap 并收紧块间距适配气泡。
3. **会话改名修复**：上轮「双击改名」实为不可达——列表项单击即切换会话并收起浮层，双击事件无从触发（开发者实测反馈印证）。改为 hover 铅笔按钮进入行内编辑（Enter 确认/Esc/失焦取消），任意会话可改名。
4. **删消息保持滚动位置**：删除前记录 scrollTop 至 keepScrollRef，重载后恢复原位置而非跳底；发送/推送等场景仍自动滚底。
5. **app 图标**：根因是 `main.ts` 引用的 `renderer/assets/icon.png` 从不存在（一直落到 Electron 默认图标）。`BrowserWindow` 图标改指 `resources/app.png`（541×537）：dev 走 `__dirname/../../resources/`，打包态预留 `process.resourcesPath`（将来接 electron-builder 配 extraResources 即可）。

改动文件：`electron/main.ts`、`src/shared/types.ts`（AiWidth 键）、`src/components/MdView.tsx`（新）、`MdDialog.tsx`、`AiSidebar.tsx`、`AiSidebar.css`。

待开发者 `npm run dev` 冒烟：拖拽调宽+重启恢复；让 AI 回条带列表/代码块的回复看渲染；会话改名；删消息不跳底；任务栏图标。git 提交由开发者执行。

## 文档规则：优化建议区归档改为按轮分组（260902）

应开发者要求，优化建议区归档区由扁平列表改为按轮分组：每轮一个小节「### 第N轮（YYMMDD）：本轮主题」，条目列于其下，新轮次插顶部。历史 20 条按本日志「优化建议实施·第N批」记录反推为第 1–7 轮（第1轮 LLM模型列表+深色配色 → 第2轮 单行显示+数据迁移 → 第3轮 根目录整理 → 第4轮 复制/序号拖拽/划词色 → 第5轮 六条清零 → 第6轮 AI 多会话 → 第7轮 边栏五项）。规则已写入文档归档区标题下方，并记入 AI 持久记忆，后续归档沿用。

## 文档规则：问题疑惑区已完成改为按轮分组（260902）

应开发者要求，问题疑惑区.md「已完成」由扁平列表改为按轮分组（同优化建议区规则）：第1轮 首启三连修（头像/背景图/弹窗关闭）→ 第2轮 格言JSON+万象验证+切页keep-alive → 第3轮 万象卡「生成中」+灵感泉首开空白 → 第4轮 MCP 选型疑惑解答（含 AI 辅助配置实施）→ 第5轮 划词气泡点击无响应。全部条目原文保留；「疑惑区（已解答）」并入第4轮；已完成区提升为与待完成平级的二级标题（原嵌在待完成下）；规则写入文档标题下方并更新 AI 持久记忆。

## 优化建议区第八轮：格言直接删除 / 板块菜单锚定 / 指定板块随机生成（260903）

待办前三项实施（第四项「万象库升级拓展探讨」留在待完成，等开发者发起）：

1. **格言直接删除**：`ConfirmDialog` 加可选 `extraAction` 第三按钮（渲染在 footer 左侧）；「放入回收站」弹窗新增「直接删除」→ 第二层确认（文案提示含笔记 md 一并删除）→ 新 IPC `mottos:deleteForever`（ipc.ts：查 note_path → DELETE → mdDelete，同回收站 hardDelete 口径）。preload/api.d.ts 同步。
2. **板块 ... 菜单优化**（方案经开发者选定：锚定按钮+加生成项）：①根因 `.ctx-menu` 写死 `top:60px;left:300px`（App.css），菜单不跟随按钮 → 删写死定位，调用处传按钮 rect（fixed，top=rect.bottom+4，left=rect.right-菜单宽，clamp≥8），万象库+灵感泉两处菜单一并修复；②存量 bug：板块改名弹窗条件 `renameOpen && sectionMenu` 但点改名即 `setSectionMenu(null)` → 永不显示，新增独立 `renameTarget` 修复；③菜单按钮加图标，新增「本板块随机来一条」。
3. **指定板块随机生成**：`services.ts` 抽 `suggestWikiTerm(sectionId)`（指定板块用指定、未指定随机挑，规避全部已有词条），`generateWikiCard` 随机模式改走它并尊重传入板块；新 IPC `wiki:suggestTerm`（只构思词条名）。UI 三入口（开发者三选全要）：板块详情页 zone-header 右侧骰子 icon-btn（generating 时 disabled+spin）、「...」菜单项、手动弹窗词条框旁骰子（suggesting 状态，填入后可改再生成）。

验证：`npm run typecheck` 通过。冒烟点：格言删除两层弹窗与彻底删除（含笔记消失）；板块 ... 菜单贴按钮弹出+改名可用（原为坏）+菜单内随机生成；灵感泉移动菜单贴按钮；进板块点骰子只出该板块词条；手动弹窗骰子填词。git 提交由开发者执行。
