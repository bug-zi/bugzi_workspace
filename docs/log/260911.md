# 260911 开发日志

## 万象库待学习区与预生成后库落地（新功能开发区待办，全栈）

> 创建于 2026-09-11 00:05

**需求**：`docs/project/新功能开发区.md` 待完成区唯一条目——万象库建待学习区：每日自动随机 5-10 条卡片（板块均摊，启动时生成）；读卡「学会了」才进板块、可逆切换；随机/手动生成也先进待学习区；仿推理角预生成——每板块后库 3 张，点「随机来一条」立刻抽出不等 AI，存量 < 3 立刻补；位置置顶板块列表（经济学上方）。

**设计定稿**（`docs/project/左侧边栏/万象库/2026-09-10-待学习区与预生成-design.md`）：

- **数据模型 DB v31**：wiki_entries 加 `state` 三态列（`pool` 后库储备用户不可见 / `learn` 待学习区 / `learned` 已学会正式词条），存量行默认 learned 零行为变化；settings 新 key `wiki_daily_learn_date`（每日批次幂等标记）。
- **后库泵**（新文件 `electron/services/wikiStock.ts`，仿 reasoningStock）：每板块目标 3 张；三触发（启动延迟 10s / 抽卡消耗后 / 进模块 wiki:stockCheck）；板块间并行板块内串行；LLM 未配置静默跳过；不做退避重试（wiki 触发点比推理角密，YAGNI）；每张入库推 `wiki:stockChanged` 渐进刷新。
- **每日批次**（同文件）：数量 `5+rand(6)`、板块洗牌轮转均摊（7 张 5 板块→2,2,1,1,1，轮转交错出卡防中途失败偏科）；启动 + 午夜零点排程双触发（App 跨天常驻不断供）；LLM 未配置跳过**不记日期**（当天配置好下次触发仍可生成）；≥1 张成功即记日期防重试撞词、零成功不记下次重试。
- **抽卡池优先**（wiki:generate 改造）：term=null 先抽池（指定板块最旧/未指定均匀随机，UPDATE 带 state='pool' 守卫防并发重抽）原地转 learn 态秒回；池空兜底现场生成；结果一律 state='learn'。
- **学会了组件**（MdDialog 新 prop `learnBar`）：learn 态三钮「直接删除/丢弃/学会了」，learned 态单钮「已学会」，可逆切换；关闭/遮罩/Esc=留在待学习区不强制三选一（区别于旧 review 流，头部关闭钮保留）——**旧「生成审核三选」流下线**。
- **连带口径修正**：wiki:entries/板块计数只含 learned；quiz 只考 learned；撞词 CONFLICT 分支主进程带回 conflictId（原卡可能在待学习区，板块列表找不到，渲染层「查看原卡片」直达；池卡跳过不泄漏）；deleteSection 先物理清池卡（用户不可见的池卡不能造成「看起来空却删不掉」死局），learn/learned 仍阻删；回收站恢复仅清 deleted_at → 待学习卡恢复自然回待学习区。

**实施**（10 任务全过）：db.ts v31 迁移 + SettingsKeys → ai/services.ts（generateWikiCard 加 state 参数默认 learn、quiz 过滤）→ wikiStock.ts（泵 + 每日批次 + drawPoolCard）→ ipc.ts（generate 池优先、learnEntries/setLearned/stockCheck、entries 过滤、deleteSection 清池、conflictId）→ main.ts + scheduler.ts 触发 → preload + api.d.ts 桥接 → MdDialog learnBar → WikiModule UI（待学习行置顶 dashed、learn 视图、toggleLearned、onStockChanged 订阅、reviewing 流移除）。

**验证**：`npm run typecheck` 双 tsconfig 通过。运行时行为未实测（LLM 泵/批次需开发者真机冒烟——尤其首启 10s 后后库首填约 30 趟 LLM 调用的体感与 wiki:stockChanged 渐进刷新）。

**涉及文件**：`electron/db/db.ts`、`src/shared/types.ts`、`electron/ai/services.ts`、`electron/services/wikiStock.ts`（新）、`electron/ipc.ts`、`electron/main.ts`、`electron/services/scheduler.ts`、`electron/preload.ts`、`src/renderer/api.d.ts`、`src/components/MdDialog.tsx`、`src/modules/wiki/WikiModule.tsx`、万象库 `designs-specs.md`（§头注/§1/§2/§3.1/§3.2/§5/§6 修订）、`docs/project/新功能开发区.md`（条目勾选归档）。

## 藏书架阅读器字体选择 + 划词气泡修复 + 气泡横排（优化建议区第 35 轮，全栈）

> 创建于 2026-09-11 00:14

**需求**：`docs/project/优化建议区.md` 待完成区唯一条目三件事——丰富书架字体选择；划词高光/批注气泡在取消划词时不能正常消失；两气泡按钮改横向排列。

**根因取证**（epub.js node_modules 源码级）：

- **气泡残留**：epub 正文渲染在 iframe 内，iframe 内 DOM 事件不冒泡到父文档——现有收气泡仅挂 host（父文档）mousedown，正文内点击取消划词根本不触发 → 气泡残留。epub.js `rendition.passEvents`（rendition.js:876）会把 iframe 内 DOM_EVENTS（含 mousedown）转发为 rendition 事件；`triggerSelectedEvent`（contents.js）只在非 collapsed 选区发 `selected`、无「取消选区」事件。
- **字体隐藏 bug（顺带修）**：全局字体选打包手写体时书内实际早已静默回落楷体——@font-face 定义在父文档（global.css），iframe（blob/srcdoc）独立 document 按 family 名解析不到。

**设计定稿**（brainstorming 三问 + 浮层预览拍板；`docs/project/左侧边栏/书架/2026-09-11-阅读器字体选择与划词气泡修复-design.md`）：

- **字体书级记忆**：与「阅读模式/字号」每书独立口径完全同构——DB v32 `books.font_family TEXT`（NULL=跟随全局，存量零迁移；设计初稿写 v31，被并行会话万象库待学习区占用，实际落 v32——版本号实况条款）；`setReadingPref` 增 fontFamily 五处透传（books.ts/ipc.ts/preload.ts/api.d.ts/types.ts + **api.d.ts 内第二处 BooksRecord**，计划漏计 typecheck 拦下后补）。
- **清单 12 项**：跟随全局（动态显示当前全局字体名）+ 系统 6（雅黑默认/微软雅黑/宋体/楷体/黑体/仿宋）+ 打包 5（沿用现有字体文件零新增打包）；字体定义收敛共享模块 `src/theme/fonts.ts`（FONT_FAMILIES 自 ProfileModule 迁出 + READER_FONTS + BUNDLED_FONTS）。
- **打包字体进 iframe**：`rendition.on('rendered')` 向每个 iframe head 注入 `@font-face`（Vite `?url` 资产，`new URL(url, location.href)` 绝对化——根相对/相对路径在 blob iframe 内无基准可解析；幂等 style#bz-reader-fonts；翻章重建 iframe 自动重注入）。
- **UI**：阅读条 epub 组加字体按钮（font_download 图标）→ ActionMenu 浮层，每项 `style.fontFamily` 用自身字体渲染自己的名字（所见即所得）、当前项打勾、打包组 separatorAbove 分组；ActionMenu 组件通用扩展（icon 改可选 + style 项级透传 + max-height 70vh 滚动）。
- **气泡修复**：`rendition.on('mousedown', closeBubble)`（主修复，正文点击即收，划词气泡与标注点击气泡同治；点标注场景 mousedown 收旧先于 click 开新顺序天然安全）+ relocated（翻页/跳章坐标失效）+ scrolled 滚动容器 scroll（脱锚悬空）三挂点；host mousedown 保留管正文外围。
- **横排**：高光/批注两键包 `.bk-sel-row bk-sel-row-center`（复用横排类加居中修饰），气泡其余形态不动。

**实施**（7 任务全过）：fonts.ts 新建 + ProfileModule 迁移 → db v32 → 五处透传 → ActionMenu 扩展 → EpubReader（fontFamily prop / applyTheme 三参 / injectFontFaces / rendered 钩子）→ 气泡三挂点 + 横排 + css → BookshelfModule（字体按钮 + 浮层 + setBookFont + globalFontName 反查 + EpubReader 传参）。

**验证**：`npm run typecheck` 双配置通过（途中两坑：api.d.ts 第二处 BooksRecord 漏改 typecheck 拦下补齐；tsbuildinfo 增量缓存重放旧诊断——第 23/31 轮同款，清缓存即过）；`npm run build` 通过，5 个字体 ttf/otf 资产正常产出 hash 文件。

**待冒烟**（重启 dev 触发 v32）：字体浮层 12 项自渲染预览/打勾 → 选霞鹜文楷正文真渲染（与楷体字形肉眼可辨，验证 iframe @font-face 注入）→ 书级记忆重开保留、「跟随全局」回落且列存 NULL → 翻章/切模式字体保持 → 划词后正文内点击气泡即收（主 bug）→ 滚动/翻页收气泡、标注点击气泡正常开合 → 高光/批注并排 → 双主题目检 → 个人档字体设置页照常。font_download 连字若在本地 material-symbols 0.47 拆字（第 19 轮 notebook 先例）换 text_fields。

**涉及文件**：`src/theme/fonts.ts`（新）、`src/modules/profile/ProfileModule.tsx`、`electron/db/db.ts`、`electron/services/books.ts`、`electron/ipc.ts`、`electron/preload.ts`、`src/renderer/api.d.ts`、`src/shared/types.ts`、`src/components/ActionMenu.tsx` + `.css`、`src/modules/bookshelf/EpubReader.tsx`、`src/modules/bookshelf/bookshelf.css`、`src/modules/bookshelf/BookshelfModule.tsx`、`docs/project/优化建议区.md`（第 35 轮归档）。

## 藏书架阅读背景 + 白噪音快捷按钮与新场景（新功能开发区待办，渲染层为主）

> 创建于 2026-09-11 01:15

**来源**：`docs/project/新功能开发区.md` 待完成区两条（开发者 260911 提出）。brainstorming 澄清定案：阅读背景**全局一份**（所有书共用，不动 books 表）+ 自定义图**单张槽位** + 图上字色**按图片明暗自动采样适配**；白噪音范围 = 右下角快捷按钮（**显隐跟随主题按钮规则**：三面板收起才显示）+ **新增 4 场景全选**（森林/海浪/夜晚篝火/咖啡馆）。设计文档：`docs/project/左侧边栏/书架/2026-09-11-阅读背景-design.md`、`docs/project/左侧边栏/白噪音/2026-09-11-快捷按钮与新场景-design.md`（均已开发者审核通过）。澄清环节开发者反馈 AskUserQuestion 交互组件在其环境显示异常，改纯文字提问完成。

**阅读背景**（零 DB 迁移）：

- settings 两新键：`books_reading_bg`（JSON `{kind:'theme'|'color'|'image', color?, textColor?}`）+ `reader_bg_image`（文件名，存 `userData/bg/reader-bg.<ext>`，`bzres://bg/` 加载）。
- `image:pick` IPC 扩展 kind `'reader-bg'`（镜像 bg-light 分支：复制/记名/清其他扩展旧文件）；「清除」只清引用回落主题，文件由下次上传覆盖清理（零新增 IPC）。
- 6 预设纸感色（纸黄/米白/护眼绿/青瓷/暖灰/夜读黑）+ 跟随主题默认；正文字色统一深 `#2b2b2b` / 浅 `#d8d8d3` 两值。上传后 32×32 缩略图 Rec.709 加权亮度采样 <0.5 判深图配浅字。
- UI：阅读条 palette 按钮（epub/pdf 都显示，书签按钮前）→ ActionMenu（跟随主题/6 色块/上传/自定义图片/清除）；**ActionMenu 通用扩展 `iconStyle`**（色块只染图标不染文字——style 染整行会让浅色项文字不可读）。
- 应用：epub `applyTheme` 增 bg 参（color=注入色值+配对字色 / image=body 透明透出宿主底图 / theme=现状），bg 变化即时重注入不重载书；容器 `bk-reader-body` 按 kind 设底色/底图（cover 居中），pdf 页面白底 canvas 不变（纸放桌上）。

**白噪音快捷按钮 + 新场景**（纯渲染层）：

- `noiseScenes.ts` 注册 4 场景：森林（风叶/阵风/林涛 + 鸟鸣 chirp ×2 变体）、海浪（浪涌事件/碎沫/海风/鸥鸣长滑音）、夜晚篝火（火苗幅度快抖/噼啪/炭火/夜风）、咖啡馆（人声两段滤波近似/杯盘 clink/杂响/底噪）——各配 3 出厂预设；`EventLayerDef` 增可选 `spawnCfg`（freq/dur/chirpsMax 覆盖，鸟与鸥同用 chirp 差异化）。场景卡/滑杆/自定义混音按 sceneId 自动生效，NoisePage 零改动。
- `noiseEngine.ts` spawnEvent 加 4 合成器：`chirp`（正弦滑音 1..n 连发、鸥鸣低频长下滑）、`wave`（棕噪带通慢起慢落包络 1.2~3s/3~6s）、`crackle`（高通窄脉冲四成概率双响）、`clink`（谐振 ping+噪声瞬态）。
- App.tsx 右下角快捷按钮：LlmActivity 之下、主题按钮之上，同款钉宽样式与显隐规则（三面板收起）；`graphic_eq` 图标播放中主题色高亮，title 带场景名与动作，点击 `noiseEngine.toggle()`（catch toast 同混音器页口径）。

**验证**：首跑 `npm run typecheck` 报两错——`App.tsx MottosModule` / `App.tsx WenbiModule 缺 props`，均为**并行会话「格言库并入文笔坊」重构半成品**（import 已删/组件签名已改但渲染处未跟上，期间 App.tsx 与 BookshelfModule.tsx 均出现过磁盘变化通知），非本次改动引入；本次涉及文件（noiseScenes/noiseEngine/App 噪音按钮段/types/ipc/api.d.ts/ActionMenu/EpubReader/BookshelfModule）无错误。01:19 复核：并行会话已收尾（MottosModule 渲染分支已删、WenbiModule 已接 onOpenAi/bumpAi、启动默认万象库），`npm run typecheck` 双配置全绿通过。运行时冒烟待开发者：5 场景卡切换与听感调参、快捷按钮显隐/切换/高亮、背景菜单预设/上传采样/清除、深色底高光可见性、epub 图片模式双模式透出。

**待办归档**：`docs/project/新功能开发区.md` 待完成区两条已勾选移入归档区。

**涉及文件**：`src/services/noiseScenes.ts`、`src/services/noiseEngine.ts`、`src/App.tsx`、`src/App.css`、`src/shared/types.ts`、`electron/ipc.ts`、`src/renderer/api.d.ts`、`src/components/ActionMenu.tsx`、`src/modules/bookshelf/EpubReader.tsx`、`src/modules/bookshelf/BookshelfModule.tsx`、`docs/project/新功能开发区.md`、两份设计文档。

## 格言库并入文笔坊 + 背景素材库（优化建议区第 36 轮，全栈）

> 创建于 2026-09-11 01:26

**需求**：`docs/project/优化建议区.md` 待完成区两条——①把格言库放到文笔坊中（tab 从左往右：格言库、写作台、浮生记）；②个人中心背景图升级为素材库（多张上传切换、浅色/深色独立）。

**设计定稿**（brainstorming 澄清四决策 + 双维度选型；两份设计文档入 `左侧边栏/文笔坊/` 与 `左侧边栏/个人档/`）：

- **任务1四决策**：文笔坊默认 tab **格言库**（承袭其曾是启动首页的地位）；App 启动默认落**万象库**（左栏 12→11 项新第一项）；tab 会话内 module-hidden 保活（重启回默认）；AI 解读显式传 `channel: 'motto'` 直达频道（当前模块已变 wenbi，模块级映射不再命中——万象库辩真同款先例）。
- **任务2四决策**：素材组织**两个独立素材组**（浅/深分开管理）；存储**文件系统为真相源**（`userData/bg/light|dark/`、唯一文件名 `<时间戳>-<随机4位>.<ext>`、列表=扫目录新在前；当前使用仍走现有 settings 键 `bg_bg-light|dark` → ThemeProvider/applyBg/bzres 协议零改动）；UI**弹窗管理器**；单张上传自动设为当前/多张只入库、删除在用图回退纯色、每组上限 20、旧图一次性幂等迁移（保持原名 settings 已指向零额外改动）。

**实施**（两计划 10 任务全过）：

- 任务1：MottosModule 原地面板化（去 module-page/module-header 外壳保留 `mottos-page` class——App.css `.mottos-page .zone` 选择器依赖；props 增 active；刷新双路径 `useModuleActivated('wenbi')` + active 变 true；onOpenAi 调用点带 channel）→ WenbiModule 三 tab（默认格言库、副标题「格言库 · 写作台 · 浮生记」、onOpenAi/bumpAi 透传）→ App.tsx（MODULES 删项、挂载切换、启动默认 'wiki'、CHANNEL_BY_MODULE 清理）→ ModuleId 删 'mottos'（shared/types.ts + api.d.ts）+ AiSidebar moduleLabel 死条目 → CLAUDE.md 左栏顺序/架构段同步。零改动：格言数据/IPC/DB、回收站格言页签与恢复、定时生成、频道、浮生记/写作台、文章页层级。
- 任务2：ipc.ts 工具（BG_EXTS/BG_MAX_PER_GROUP=20/bgGroupDir/bgListFiles/migrateBgLibrary——**迁移调用置于工具 const 初始化后防 TDZ**，计划初稿放函数首行会在 const 求值前执行抛 ReferenceError，实施时自查修正）+ 四通道（bgUpload 多选超限 throw 拒整批；bgUse/bgDelete 文件名 `^[\w.-]+$` 安全校验）+ `image:pick` 收窄 avatar|reader-bg（**保留并行会话同日新增的 reader-bg 单槽位**，preload 顺带补齐其漏标）→ preload.ts/api.d.ts 同步 → 新组件 `BgLibraryDialog.tsx+css`（浅/深 pill 默认随当前主题、16:9 缩略图网格、在用图描边+角标、点图即设当前、hover 删除二次确认在用图加提示）→ ProfileModule 两行按钮换「管理素材库」入口。

**排障两笔**（本会话环境）：①tsbuildinfo 增量缓存**两次重放旧诊断**（第35轮同款问题）——EpubReader「缺第 4 参」假错误与 ProfileModule 旧签名漏报，均 rm 清缓存后暴露真实状态；后续 typecheck 一律先清。②并行会话（白噪音 5 场景 + 藏书架阅读背景）与本轮同文件交叉（App.tsx/ipc.ts/api.d.ts/BookshelfModule）——途中一次 Edit 拼坏 import 换行、image:pick 盲替换险些抹掉 reader-bg 分支，均靠重读磁盘现状修正；BookshelfModule 漏传的 `bg={readingBg}` 一行已代补（该会话 devlog 01:19 复核确认三方收尾后 typecheck 全绿）。

**验证**：`npm run typecheck` 双 tsconfig + `npm run build` 通过（清缓存后跑）。运行时冒烟待开发者（两份设计文档「冒烟清单」各 7 项：任务1——左栏 11 项/启动万象库/默认格言 tab/格言全功能/AI 解读直达/定时生成刷新/回收站/双主题；任务2——旧图迁移幂等/两组独立/单张即生效/21 张拒传/点图切换/删在用图回退纯色/重启保持）。

**待办归档**：`docs/project/优化建议区.md` 待完成区两条已勾选移入归档区第 36 轮小节（新轮插顶部）。

**涉及文件**：`src/modules/mottos/MottosModule.tsx`、`src/modules/wenbi/WenbiModule.tsx`、`src/App.tsx`、`src/shared/types.ts`、`src/renderer/api.d.ts`、`src/components/AiSidebar.tsx`、`CLAUDE.md`、`electron/ipc.ts`、`electron/preload.ts`、`src/modules/profile/BgLibraryDialog.tsx`（新）、`src/modules/profile/BgLibraryDialog.css`（新）、`src/modules/profile/ProfileModule.tsx`、`src/modules/bookshelf/BookshelfModule.tsx`（代补 bg 一行）、`docs/project/优化建议区.md`、设计文档与实施计划各两份。
