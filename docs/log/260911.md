# 260911 开发日志

## 万象库待学习区与预生成后库落地（新功能开发区待办，全栈）

> 创建于 2026-09-11 00:05

**需求**：`docs/project/新功能开发区.md` 待完成区唯一条目——万象库建待学习区：每日自动随机 5-10 条卡片（板块均摊，启动时生成）；读卡「学会了」才进板块、可逆切换；随机/手动生成也先进待学习区；仿推理角预生成——每板块后库 3 张，点「随机来一条」立刻抽出不等 AI，存量 < 3 立刻补；位置置顶板块列表（经济学上方）。

**设计定稿**（`docs/project/左侧边栏/万象库/2026-09-10-待学习区与预生成-design.md`）：

- **数据模型 DB v31**：wiki_entries 加 `state` 三态列（`pool` 后库储备用户不可见 / `learn` 待学习区 / `learned` 已学会正式词条），存量行默认 learned 零行为变化；settings 新 key `wiki_daily_learn_date`（每日批次幂等标记）。
- **后库泵**（新文件 `electron/services/wikiStock.ts`，仿 reasoningStock）：每板块目标 3 张；三触发（启动延迟 10s / 抽卡消耗后 / 进模块 wiki:stockCheck）；板块间并行板块内串行；LLM 未配置静默跳过；不做退避重试（wiki 触发点比推理角密，YAGNI）；每张入库推 `wiki:stockChanged` 渐进刷新。
- **每日批次**（同文件）：数量 `5+rand(6)`、板块洗牌轮转均摊（7 张 5 板块→2,2,1,1,1，轮转交错出卡防中途失败偏科）；启动 + 午夜零点排程双触发（App 跨天常驻不断供）；LLM 未配置跳过**不记日期**（当天配置好下次触发仍可生成）；≥1 张成功即记日期防重试撞词、零成功不记下次重试。
- **抽卡池优先**（wiki:generate 改造）：term=null 先抽池（指定板块最旧/未指定均匀随机，UPDATE 带 state='pool' 守卫防并发重抽）原地转 learn 态秒回；池空兜底现场生成；结果一律 state='learn'。
- **学会了组件**（MdDialog 新 prop `learnBar`）：learn 态三钮「直接删除/丢弃/学会了」，learned 态单钮「已学会」，可逆切换；关闭/遮罩/Esc=留在待学习区不强制三选一（区别于旧 review 流，头部关闭钮保留）——**旧「生成审核三选」流下线**。
- **连带口径修正**：wiki:entries/板块计数只含 learned；quiz 只考 learned；撞词 CONFLICT 分支主进程带回 conflictId（原卡可能在待学习区，板块列表找不到，渲染层「查看原卡片」直达；池卡跳过不泄漏）；deleteSection 先物理清池卡（用户不可见的池卡不能造成「看起来空却删不掉」死局），learn/learned 仍阻删；回收站恢复仅清 deleted_at → 待学习卡恢复自然回待学习区。

**实施**（10 任务全过）：db.ts v31 迁移 + SettingsKeys → ai/services.ts（generateWikiCard 加 state 参数默认 learn、quiz 过滤）→ wikiStock.ts（泵 + 每日批次 + drawPoolCard）→ ipc.ts（generate 池优先、learnEntries/setLearned/stockCheck、entries 过滤、deleteSection 清池、conflictId）→ main.ts + scheduler.ts 触发 → preload + api.d.ts 桥接 → MdDialog learnBar → WikiModule UI（待学习行置顶 dashed、learn 视图、toggleLearned、onStockChanged 订阅、reviewing 流移除）。

**验证**：`npm run typecheck` 双 tsconfig 通过。运行时行为未实测（LLM 泵/批次需开发者真机冒烟——尤其首启 10s 后后库首填约 30 趟 LLM 调用的体感与 wiki:stockChanged 渐进刷新）。

**涉及文件**：`electron/db/db.ts`、`src/shared/types.ts`、`electron/ai/services.ts`、`electron/services/wikiStock.ts`（新）、`electron/ipc.ts`、`electron/main.ts`、`electron/services/scheduler.ts`、`electron/preload.ts`、`src/renderer/api.d.ts`、`src/components/MdDialog.tsx`、`src/modules/wiki/WikiModule.tsx`、万象库 `designs-specs.md`（§头注/§1/§2/§3.1/§3.2/§5/§6 修订）、`docs/project/新功能开发区.md`（条目勾选归档）。

## 藏书架阅读器字体选择 + 划词气泡修复 + 气泡横排（优化建议区第 35 轮，全栈）

> 创建于 2026-09-11 00:14

**需求**：`docs/project/优化建议区.md` 待完成区唯一条目三件事——丰富书架字体选择；划词高光/批注气泡在取消划词时不能正常消失；两气泡按钮改横向排列。

**根因取证**（epub.js node_modules 源码级）：

- **气泡残留**：epub 正文渲染在 iframe 内，iframe 内 DOM 事件不冒泡到父文档——现有收气泡仅挂 host（父文档）mousedown，正文内点击取消划词根本不触发 → 气泡残留。epub.js `rendition.passEvents`（rendition.js:876）会把 iframe 内 DOM_EVENTS（含 mousedown）转发为 rendition 事件；`triggerSelectedEvent`（contents.js）只在非 collapsed 选区发 `selected`、无「取消选区」事件。
- **字体隐藏 bug（顺带修）**：全局字体选打包手写体时书内实际早已静默回落楷体——@font-face 定义在父文档（global.css），iframe（blob/srcdoc）独立 document 按 family 名解析不到。

**设计定稿**（brainstorming 三问 + 浮层预览拍板；`docs/project/左侧边栏/书架/2026-09-11-阅读器字体选择与划词气泡修复-design.md`）：

- **字体书级记忆**：与「阅读模式/字号」每书独立口径完全同构——DB v32 `books.font_family TEXT`（NULL=跟随全局，存量零迁移；设计初稿写 v31，被并行会话万象库待学习区占用，实际落 v32——版本号实况条款）；`setReadingPref` 增 fontFamily 五处透传（books.ts/ipc.ts/preload.ts/api.d.ts/types.ts + **api.d.ts 内第二处 BooksRecord**，计划漏计 typecheck 拦下后补）。
- **清单 12 项**：跟随全局（动态显示当前全局字体名）+ 系统 6（雅黑默认/微软雅黑/宋体/楷体/黑体/仿宋）+ 打包 5（沿用现有字体文件零新增打包）；字体定义收敛共享模块 `src/theme/fonts.ts`（FONT_FAMILIES 自 ProfileModule 迁出 + READER_FONTS + BUNDLED_FONTS）。
- **打包字体进 iframe**：`rendition.on('rendered')` 向每个 iframe head 注入 `@font-face`（Vite `?url` 资产，`new URL(url, location.href)` 绝对化——根相对/相对路径在 blob iframe 内无基准可解析；幂等 style#bz-reader-fonts；翻章重建 iframe 自动重注入）。
- **UI**：阅读条 epub 组加字体按钮（font_download 图标）→ ActionMenu 浮层，每项 `style.fontFamily` 用自身字体渲染自己的名字（所见即所得）、当前项打勾、打包组 separatorAbove 分组；ActionMenu 组件通用扩展（icon 改可选 + style 项级透传 + max-height 70vh 滚动）。
- **气泡修复**：`rendition.on('mousedown', closeBubble)`（主修复，正文点击即收，划词气泡与标注点击气泡同治；点标注场景 mousedown 收旧先于 click 开新顺序天然安全）+ relocated（翻页/跳章坐标失效）+ scrolled 滚动容器 scroll（脱锚悬空）三挂点；host mousedown 保留管正文外围。
- **横排**：高光/批注两键包 `.bk-sel-row bk-sel-row-center`（复用横排类加居中修饰），气泡其余形态不动。

**实施**（7 任务全过）：fonts.ts 新建 + ProfileModule 迁移 → db v32 → 五处透传 → ActionMenu 扩展 → EpubReader（fontFamily prop / applyTheme 三参 / injectFontFaces / rendered 钩子）→ 气泡三挂点 + 横排 + css → BookshelfModule（字体按钮 + 浮层 + setBookFont + globalFontName 反查 + EpubReader 传参）。

**验证**：`npm run typecheck` 双配置通过（途中两坑：api.d.ts 第二处 BooksRecord 漏改 typecheck 拦下补齐；tsbuildinfo 增量缓存重放旧诊断——第 23/31 轮同款，清缓存即过）；`npm run build` 通过，5 个字体 ttf/otf 资产正常产出 hash 文件。

**待冒烟**（重启 dev 触发 v32）：字体浮层 12 项自渲染预览/打勾 → 选霞鹜文楷正文真渲染（与楷体字形肉眼可辨，验证 iframe @font-face 注入）→ 书级记忆重开保留、「跟随全局」回落且列存 NULL → 翻章/切模式字体保持 → 划词后正文内点击气泡即收（主 bug）→ 滚动/翻页收气泡、标注点击气泡正常开合 → 高光/批注并排 → 双主题目检 → 个人档字体设置页照常。font_download 连字若在本地 material-symbols 0.47 拆字（第 19 轮 notebook 先例）换 text_fields。

**涉及文件**：`src/theme/fonts.ts`（新）、`src/modules/profile/ProfileModule.tsx`、`electron/db/db.ts`、`electron/services/books.ts`、`electron/ipc.ts`、`electron/preload.ts`、`src/renderer/api.d.ts`、`src/shared/types.ts`、`src/components/ActionMenu.tsx` + `.css`、`src/modules/bookshelf/EpubReader.tsx`、`src/modules/bookshelf/bookshelf.css`、`src/modules/bookshelf/BookshelfModule.tsx`、`docs/project/优化建议区.md`（第 35 轮归档）。

## 藏书架阅读背景 + 白噪音快捷按钮与新场景（新功能开发区待办，渲染层为主）

> 创建于 2026-09-11 01:15

**来源**：`docs/project/新功能开发区.md` 待完成区两条（开发者 260911 提出）。brainstorming 澄清定案：阅读背景**全局一份**（所有书共用，不动 books 表）+ 自定义图**单张槽位** + 图上字色**按图片明暗自动采样适配**；白噪音范围 = 右下角快捷按钮（**显隐跟随主题按钮规则**：三面板收起才显示）+ **新增 4 场景全选**（森林/海浪/夜晚篝火/咖啡馆）。设计文档：`docs/project/左侧边栏/书架/2026-09-11-阅读背景-design.md`、`docs/project/左侧边栏/白噪音/2026-09-11-快捷按钮与新场景-design.md`（均已开发者审核通过）。澄清环节开发者反馈 AskUserQuestion 交互组件在其环境显示异常，改纯文字提问完成。

**阅读背景**（零 DB 迁移）：

- settings 两新键：`books_reading_bg`（JSON `{kind:'theme'|'color'|'image', color?, textColor?}`）+ `reader_bg_image`（文件名，存 `userData/bg/reader-bg.<ext>`，`bzres://bg/` 加载）。
- `image:pick` IPC 扩展 kind `'reader-bg'`（镜像 bg-light 分支：复制/记名/清其他扩展旧文件）；「清除」只清引用回落主题，文件由下次上传覆盖清理（零新增 IPC）。
- 6 预设纸感色（纸黄/米白/护眼绿/青瓷/暖灰/夜读黑）+ 跟随主题默认；正文字色统一深 `#2b2b2b` / 浅 `#d8d8d3` 两值。上传后 32×32 缩略图 Rec.709 加权亮度采样 <0.5 判深图配浅字。
- UI：阅读条 palette 按钮（epub/pdf 都显示，书签按钮前）→ ActionMenu（跟随主题/6 色块/上传/自定义图片/清除）；**ActionMenu 通用扩展 `iconStyle`**（色块只染图标不染文字——style 染整行会让浅色项文字不可读）。
- 应用：epub `applyTheme` 增 bg 参（color=注入色值+配对字色 / image=body 透明透出宿主底图 / theme=现状），bg 变化即时重注入不重载书；容器 `bk-reader-body` 按 kind 设底色/底图（cover 居中），pdf 页面白底 canvas 不变（纸放桌上）。

**白噪音快捷按钮 + 新场景**（纯渲染层）：

- `noiseScenes.ts` 注册 4 场景：森林（风叶/阵风/林涛 + 鸟鸣 chirp ×2 变体）、海浪（浪涌事件/碎沫/海风/鸥鸣长滑音）、夜晚篝火（火苗幅度快抖/噼啪/炭火/夜风）、咖啡馆（人声两段滤波近似/杯盘 clink/杂响/底噪）——各配 3 出厂预设；`EventLayerDef` 增可选 `spawnCfg`（freq/dur/chirpsMax 覆盖，鸟与鸥同用 chirp 差异化）。场景卡/滑杆/自定义混音按 sceneId 自动生效，NoisePage 零改动。
- `noiseEngine.ts` spawnEvent 加 4 合成器：`chirp`（正弦滑音 1..n 连发、鸥鸣低频长下滑）、`wave`（棕噪带通慢起慢落包络 1.2~3s/3~6s）、`crackle`（高通窄脉冲四成概率双响）、`clink`（谐振 ping+噪声瞬态）。
- App.tsx 右下角快捷按钮：LlmActivity 之下、主题按钮之上，同款钉宽样式与显隐规则（三面板收起）；`graphic_eq` 图标播放中主题色高亮，title 带场景名与动作，点击 `noiseEngine.toggle()`（catch toast 同混音器页口径）。

**验证**：首跑 `npm run typecheck` 报两错——`App.tsx MottosModule` / `App.tsx WenbiModule 缺 props`，均为**并行会话「格言库并入文笔坊」重构半成品**（import 已删/组件签名已改但渲染处未跟上，期间 App.tsx 与 BookshelfModule.tsx 均出现过磁盘变化通知），非本次改动引入；本次涉及文件（noiseScenes/noiseEngine/App 噪音按钮段/types/ipc/api.d.ts/ActionMenu/EpubReader/BookshelfModule）无错误。01:19 复核：并行会话已收尾（MottosModule 渲染分支已删、WenbiModule 已接 onOpenAi/bumpAi、启动默认万象库），`npm run typecheck` 双配置全绿通过。运行时冒烟待开发者：5 场景卡切换与听感调参、快捷按钮显隐/切换/高亮、背景菜单预设/上传采样/清除、深色底高光可见性、epub 图片模式双模式透出。

**待办归档**：`docs/project/新功能开发区.md` 待完成区两条已勾选移入归档区。

**涉及文件**：`src/services/noiseScenes.ts`、`src/services/noiseEngine.ts`、`src/App.tsx`、`src/App.css`、`src/shared/types.ts`、`electron/ipc.ts`、`src/renderer/api.d.ts`、`src/components/ActionMenu.tsx`、`src/modules/bookshelf/EpubReader.tsx`、`src/modules/bookshelf/BookshelfModule.tsx`、`docs/project/新功能开发区.md`、两份设计文档。

## 格言库并入文笔坊 + 背景素材库（优化建议区第 36 轮，全栈）

> 创建于 2026-09-11 01:26

**需求**：`docs/project/优化建议区.md` 待完成区两条——①把格言库放到文笔坊中（tab 从左往右：格言库、写作台、浮生记）；②个人中心背景图升级为素材库（多张上传切换、浅色/深色独立）。

**设计定稿**（brainstorming 澄清四决策 + 双维度选型；两份设计文档入 `左侧边栏/文笔坊/` 与 `左侧边栏/个人档/`）：

- **任务1四决策**：文笔坊默认 tab **格言库**（承袭其曾是启动首页的地位）；App 启动默认落**万象库**（左栏 12→11 项新第一项）；tab 会话内 module-hidden 保活（重启回默认）；AI 解读显式传 `channel: 'motto'` 直达频道（当前模块已变 wenbi，模块级映射不再命中——万象库辩真同款先例）。
- **任务2四决策**：素材组织**两个独立素材组**（浅/深分开管理）；存储**文件系统为真相源**（`userData/bg/light|dark/`、唯一文件名 `<时间戳>-<随机4位>.<ext>`、列表=扫目录新在前；当前使用仍走现有 settings 键 `bg_bg-light|dark` → ThemeProvider/applyBg/bzres 协议零改动）；UI**弹窗管理器**；单张上传自动设为当前/多张只入库、删除在用图回退纯色、每组上限 20、旧图一次性幂等迁移（保持原名 settings 已指向零额外改动）。

**实施**（两计划 10 任务全过）：

- 任务1：MottosModule 原地面板化（去 module-page/module-header 外壳保留 `mottos-page` class——App.css `.mottos-page .zone` 选择器依赖；props 增 active；刷新双路径 `useModuleActivated('wenbi')` + active 变 true；onOpenAi 调用点带 channel）→ WenbiModule 三 tab（默认格言库、副标题「格言库 · 写作台 · 浮生记」、onOpenAi/bumpAi 透传）→ App.tsx（MODULES 删项、挂载切换、启动默认 'wiki'、CHANNEL_BY_MODULE 清理）→ ModuleId 删 'mottos'（shared/types.ts + api.d.ts）+ AiSidebar moduleLabel 死条目 → CLAUDE.md 左栏顺序/架构段同步。零改动：格言数据/IPC/DB、回收站格言页签与恢复、定时生成、频道、浮生记/写作台、文章页层级。
- 任务2：ipc.ts 工具（BG_EXTS/BG_MAX_PER_GROUP=20/bgGroupDir/bgListFiles/migrateBgLibrary——**迁移调用置于工具 const 初始化后防 TDZ**，计划初稿放函数首行会在 const 求值前执行抛 ReferenceError，实施时自查修正）+ 四通道（bgUpload 多选超限 throw 拒整批；bgUse/bgDelete 文件名 `^[\w.-]+$` 安全校验）+ `image:pick` 收窄 avatar|reader-bg（**保留并行会话同日新增的 reader-bg 单槽位**，preload 顺带补齐其漏标）→ preload.ts/api.d.ts 同步 → 新组件 `BgLibraryDialog.tsx+css`（浅/深 pill 默认随当前主题、16:9 缩略图网格、在用图描边+角标、点图即设当前、hover 删除二次确认在用图加提示）→ ProfileModule 两行按钮换「管理素材库」入口。

**排障两笔**（本会话环境）：①tsbuildinfo 增量缓存**两次重放旧诊断**（第35轮同款问题）——EpubReader「缺第 4 参」假错误与 ProfileModule 旧签名漏报，均 rm 清缓存后暴露真实状态；后续 typecheck 一律先清。②并行会话（白噪音 5 场景 + 藏书架阅读背景）与本轮同文件交叉（App.tsx/ipc.ts/api.d.ts/BookshelfModule）——途中一次 Edit 拼坏 import 换行、image:pick 盲替换险些抹掉 reader-bg 分支，均靠重读磁盘现状修正；BookshelfModule 漏传的 `bg={readingBg}` 一行已代补（该会话 devlog 01:19 复核确认三方收尾后 typecheck 全绿）。

**验证**：`npm run typecheck` 双 tsconfig + `npm run build` 通过（清缓存后跑）。运行时冒烟待开发者（两份设计文档「冒烟清单」各 7 项：任务1——左栏 11 项/启动万象库/默认格言 tab/格言全功能/AI 解读直达/定时生成刷新/回收站/双主题；任务2——旧图迁移幂等/两组独立/单张即生效/21 张拒传/点图切换/删在用图回退纯色/重启保持）。

**待办归档**：`docs/project/优化建议区.md` 待完成区两条已勾选移入归档区第 36 轮小节（新轮插顶部）。

**涉及文件**：`src/modules/mottos/MottosModule.tsx`、`src/modules/wenbi/WenbiModule.tsx`、`src/App.tsx`、`src/shared/types.ts`、`src/renderer/api.d.ts`、`src/components/AiSidebar.tsx`、`CLAUDE.md`、`electron/ipc.ts`、`electron/preload.ts`、`src/modules/profile/BgLibraryDialog.tsx`（新）、`src/modules/profile/BgLibraryDialog.css`（新）、`src/modules/profile/ProfileModule.tsx`、`src/modules/bookshelf/BookshelfModule.tsx`（代补 bg 一行）、`docs/project/优化建议区.md`、设计文档与实施计划各两份。

## 背景素材库重启后背景不显示修复（问题疑惑区第 10 轮）

> 创建于 2026-09-11 11:25

**现象**：260911 01:30 背景素材库落地后重启项目，素材标记「使用中」但背景图不显示、回退纯色。

**根因**（代码 + 本机双重实证）：设计阶段误判「ThemeProvider 零改动」——混淆了 *bzres 协议支持子目录* 与 *applyBg 拼 URL 时会带子目录*。一次性迁移把旧图 `bg/bg-light.png` 挪进 `bg/light/`，`bgUse/bgUpload` 存**纯文件名**进 settings；弹窗缩略图走 `bzres://bg/<group>/<file>` 正常显示、「使用中」徽标比较纯文件名正常命中；但 ThemeProvider.applyBg（64536f1 提交未涉及该文件）仍拼旧格式 `bzres://bg/<file>` → 指向已无文件的 `bg/` 根路径 → fetch 探测 404 → `--bg-image: none`。本机实证（数据目录 `D:\Paper\app_output\bugzi_workspace`）：`bg/` 根仅剩 light/dark 两子目录，`bg/light/bg-light.png`、`bg/dark/bg-dark.png` 在位，settings `bg_bg-light=bg-light.png` / `bg_bg-dark=bg-dark.png`；一次性脚本复刻 bzres 协议映射断言——旧 URL 两组皆不存在、新 URL 皆存在。该设计文档冒烟清单第 1 条「重启后旧背景仍为当前使用」正是能拦住此 bug 的用例，实施当晚未做重启冒烟。

**修复**（`src/theme/ThemeProvider.tsx` applyBg，最小改动）：URL 改为组目录优先 `bzres://bg/<theme>/<file>`；旧根路径降为第二兜底候选（保住设计文档「迁移失败静默——旧图原地不动、现有行为不受影响」的承诺）；探测 404 换下一候选、fetch 异常保留当前 url 交 CSS 层决定的旧语义不变。

**验证**：一次性脚本断言（上述）+ `npm run typecheck` 双配置通过。待开发者重启 dev 冒烟：浅/深背景正常显示、切主题两组独立、弹窗点图即时生效、再重启仍保持。

**涉及文件**：`src/theme/ThemeProvider.tsx`、`docs/project/左侧边栏/个人档/2026-09-11-背景素材库.md` 与 `-design.md`（「零改动」误判勘误）、`docs/project/问题疑惑区.md`（第 10 轮归档）、本文档。

## 信息源显式已读双态按钮 + 文章收藏 + 30 天彻底清理（设计定稿，未实施）

> 创建于 2026-09-11 12:03

**需求**（开发者会话直提三项）：①归档保留期 180 天改 30 天且超期彻底删除（不再保留轻行）；②已读判定改显式——收件箱文章加「已读」双态按钮（已读态变「再看看」点击回退收件箱），打开文章不再自动标已读；③新增文章收藏功能（A 模块内收藏页 / B 并入收藏夹模块 / C 两都做，开发者选定 **A**）。

**两澄清决定**：删除范围 = 已读 + 未收藏 + 超 30 天（未读与收藏永不清）；收藏走 articles 表加 `favorited_at` 单一数据源（B/C 否决理由：fav_items 纯链接与文章行无关联、取消收藏靠 URL 反查、双份数据同步成本）。

**设计要点**（全文见 `docs/project/左侧边栏/信息源/2026-09-11-信息源已读收藏与清理-design.md`）：

- **DB v34**：articles 加 `favorited_at TEXT` 一列，无新表无索引。
- **显式已读**：`openArticle` 去掉 read_at 写入；新 IPC `articles:setRead(id, read)`；「已读/再看看」按钮布三处（收件箱行/已归档行/收藏页行）+ 阅读视图头部；「全部标已读」保留但排除收藏未读。
- **收藏**：新 IPC `articles:setFavorite(id, fav)`；星标按钮（实心 FILL 1 / 空心）同行布点；tab 三页「收件箱|已归档|收藏」，收藏页按收藏时间倒序全量、不筛源、无加载更早；未读徽标排除收藏。
- **30 天清理**：`cleanupOldArticleBodies` 改造为 `cleanupOldArticles` 整行 DELETE（read + 未收藏 + COALESCE(published_at,fetched_at) < 30 天），时机仍启动 + 零点；已归档页「加载更早」移除（天然 ≤30 天）、收件页保留（未读老文章不删）。

**状态**：设计文档已写入模块目录并自查（修正一处设问式表述）；实施未开始，待开发者审核设计后走写计划 → 实施。

**涉及文件**：`docs/project/左侧边栏/信息源/2026-09-11-信息源已读收藏与清理-design.md`（新）、本文档。

## 信息源显式已读 + 文章收藏 + 30 天清理（实施完成，全栈）

> 创建于 2026-09-11 12:21

**需求来源**：上一节设计定稿（12:01 会话延续），实施计划 `docs/project/左侧边栏/信息源/2026-09-11-信息源已读收藏与清理.md`（10 任务内联执行全过）。

**实施**（10 任务，一次通过无返工）：

- **DB v34**（db.ts）：`ALTER TABLE articles ADD COLUMN favorited_at TEXT`，无新表无索引。
- **类型层双文件**（shared/types.ts + renderer/api.d.ts 手工镜像）：`ArticleRecord.favorited_at`、`ArticleSummary.favorited`、`FeedView` 扩 `'favorite'`。
- **服务层**（feed.ts）：未读子查询两处排除收藏；`listArticles` 三视图（favorite 全量按收藏时间倒序不筛源；unread/archive 排除收藏）+ 抽 `toSummary` 共用映射；`openArticle` 去掉 `read_at` 写入（显式已读语义核心变更）；新增 `setArticleRead`/`setArticleFavorite`；`markAllRead` 加 `AND favorited_at IS NULL`；`cleanupOldArticleBodies`（180 天置空正文）整函数替换为 `cleanupOldArticles`（30 天整行 DELETE：已读+未收藏）。
- **IPC 三处**（ipc.ts + preload.ts + api.d.ts）：新通道 `articles:setRead`/`articles:setFavorite`；顺手清掉 ipc.ts 对旧清理函数的死 import（全文件仅 import 行引用）。
- **定时器**（scheduler.ts）：两处调用改 `cleanupOldArticles`，日志文案「信息源 30 天已读文章清理」。
- **渲染层**（FeedModule.tsx + ArticleView.tsx + feed.css）：三 tab「收件箱｜已归档｜收藏」；文章行改横排（主体列 + 常显操作区：星标 `star`〔实心 FILL 轴〕+「已读/再看看」双态按钮 `mark_email_read`/`move_to_inbox`，stopPropagation 不触发打开）；阅读视图头部同款两按钮（read/fav 本地态即时翻转 + onChanged 刷新）；「加载更早」仅收件箱显示；空态四分支；收藏页 meta 恒显源名。
- **specs 增量注记**（designs-specs.md）：头部 260911 注记（指明第28轮三个作废口径）+ §2.4/§3.1/§3.2/§4 表（+2 通道 +markAllRead 行）/§6（收藏与标未读回退划线落地）/§7 验收清单修订。

**执行偏差**：Task 3 后 typecheck 必报 scheduler.ts 旧 import（计划已预判），把 Task 5 提前合并验证，其余按计划顺序零偏差。每任务前重读磁盘现状防并行会话交叉（本轮未实际冲突，仅 FeedModule 空态一处缩进不匹配重读修正）。

**验证**：`npm run typecheck` 双 tsconfig 通过（每任务清 tsbuildinfo 缓存跑，共 6 次——第35/36 轮缓存重放教训执行到位）。运行时冒烟待开发者：迁移 v33→v34、打开文章行不消失、双态按钮双向流转（行+阅读视图）、收藏/取消回流、收藏页不筛源、全部标已读不波及收藏、归档无加载更早、SQL 造 40 天前已读文验证启动删除（未读/收藏对照保留）、双主题配色。

**涉及文件**：`electron/db/db.ts`、`src/shared/types.ts`、`electron/services/feed.ts`、`electron/ipc.ts`、`electron/preload.ts`、`src/renderer/api.d.ts`、`electron/services/scheduler.ts`、`src/modules/feed/FeedModule.tsx`、`src/modules/feed/ArticleView.tsx`、`src/modules/feed/feed.css`、信息源 `designs-specs.md`、实施计划文档（勾选记录）。

## 信息源拉取入库 7 天窗口（开发者冒烟反馈，微改）

> 创建于 2026-09-11 13:01

**需求**：拉取订阅源会把历史文章全部拉进来（`fetchFeed` 入库循环无时间门槛，全量 INSERT）；要求最多只拉最近 7 天。

**实施**（设计沟通确认三要点后直接改，`electron/services/feed.ts` 单文件）：新常量 `FETCH_WINDOW_DAYS = 7`，入库循环跳过 `publishedIso` 早于 now−7 天的条目；无日期条目视作新发照常进（防无 pubDate 源永远拉不到文章，guid 去重防重复）；已在库老文章不动、「添加订阅」立即拉取同生效。

**验证**：`npm run typecheck` 通过（清缓存）。冒烟待开发者：首次订阅一个历史较长的源，只进 7 天内文章；无日期源正常出文章；刷新不产生重复。

**涉及文件**：`electron/services/feed.ts`、设计文档「补充需求」节、本文档。

## 信息源订阅网络层修复 + 首页自动发现（优化建议区第 37 轮）

> 创建于 2026-09-11 13:29

**需求**：开发者接入 `feeds.feedburner.com/ruanyifeng` 显示 Error，反馈「获取订阅的方式有点窄」，要求优化升级订阅。

**根因**（systematic-debugging 四阶段取证）：①URL 本体正常——curl 直取返回标准 RSS 2.0（200 / text/xml / 137KB），项目同款 fast-xml-parser 配置解析成功（6 条目），排除内容与解析层；②复现 Error——node 全局 fetch（undici）直连该域名 `ConnectTimeoutError`，同机 curl 秒通；③差异在代理——开发者机器系统代理 127.0.0.1:7890（注册表 ProxyEnable=1），feedburner 属 Google 服务需代理可达，curl 走 `https_proxy` 环境变量而 **undici 既不读系统代理也不读环境变量**；④三个预置源均国内直连可达故洞从未暴露；⑤修法验证——Electron 隔离脚本对照 global fetch（复现超时）vs **net.fetch OK 200**（Chromium 网络栈自动跟随系统代理），且 llm.ts 260909 已因同理由全量迁 net.fetch，信息源当时被遗漏。

**实施**（开发者拍板：网络层 + 首页自动发现 + 错误友好化）：
- **网络层**：feed.ts 三处拉取（fetchFeed/probeFeed/extractArticle）统一走新内部件 `httpGet()`（net.fetch + 原浏览器 UA + 15s AbortSignal.timeout），对齐 llm.ts 约定。
- **首页自动发现**：probeFeed 双路径——HTML 页面解析 `<link rel="alternate">`（type 含 rss/atom/feed 优先、缺 type 退 href 关键词启发，linkedom 相对地址归一）发现 feed 直链逐个试解析（最多前 3 个）；非 HTML 原路径零变化，解析失败兜底当 HTML 探测（content-type 标错误的怪站）；返回 `FeedProbe { title, siteUrl, feedUrl }`，**addFeed 存发现的 feedUrl**（首页地址存库后续拉取永远打 HTML），首页/直链两输法归一去重。
- **错误友好化**：`friendlyError()` 映射连接超时/域名解析/代理失败/拒绝 → 可读中文 +「应用自动跟随系统代理」提示；HTTP/格式错误透传；probe 抛出与 fetch_error 入库两处统一。
- 同步：preload.ts + api.d.ts probe 类型补 feedUrl；FeedModule placeholder 放宽「RSS/Atom 链接或网站首页」+「实际订阅源：…」行（feedUrl ≠ 输入时显示，复用 .feed-probe-line）。零 DB 迁移零新依赖。

**验证**：typecheck 双配置 + build 通过；Electron E2E（esbuild 打包真实 feed.ts 实跑）三用例全绿——feedburner 直链 0.9s 通过（用户原始失败用例）、博客首页 0.8s 自动发现同源、坏域名 0.2s 可读文案。冒烟待开发者：直链/首页两路各订一次、看「实际订阅源」行、关代理验错误文案、已有三源拉取回归。

**涉及文件**：`electron/services/feed.ts`、`electron/preload.ts`、`src/renderer/api.d.ts`、`src/modules/feed/FeedModule.tsx`、信息源 `designs-specs.md`（第37轮增量注记）、新增 `2026-09-11-信息源订阅网络层修复与自动发现-design.md`、优化建议区第37轮归档。

## 学习库模块实施（左栏第一位新模块）

> 创建于 2026-09-11 13:53

- 依据 `左侧边栏/学习库/2026-09-11-学习库-design.md` 与同日 plan 完成：DB v33（learn_domains / learn_nodes / learn_daily / learn_highlights + 出厂 8 领域 seed）、AI 三函数（建树/知识卡/主题展开）、learnStock（每日队列定档纯 SQL + 预生成泵）、learn:* IPC 全量（20 handler + preload 21 方法）、LearnModule 三 tab、MdDialog studyBar、右栏「学习·问答」频道、回收站十块（learn 块居首）、启动/午夜触发接线。
- 三处实施偏差已回写设计文档：卡片 md 扁平路径 md/learn/<id>.md；未生成卡点开等待生成（原骨架预览简化）；md 读取失败沿用 MdDialog 既有提示（原重生成入口简化）。
- 质量审查追加的实施加固（计划逐字代码之外）：v33 四条建表补 IF NOT EXISTS；AI 三函数补 ensureNotCancelled 延迟取消检查；MODULE_LABELS（ai/services.ts）/AiSidebar moduleLabel 补 learn 标签；频道恢复校验补 motto；learn:highlights 查询 JOIN 过滤 deleted_at 修复删节点后高光虚影行；LearnModule 快速路径 try/catch + 行内 spin 图标；learnStock 头注释补午夜触发记载。
- `npm run typecheck` 双配置通过；运行时冒烟由开发者验证（清单见 plan 文末，需重启 dev 触发 DB v33 迁移）。

## 藏书架 epub 阅读器翻页键卡死修复（问题疑惑区第 11 轮）

> 创建于 2026-09-11 21:09

**需求**：问题疑惑区待完成区唯一条目——「翻页键有时候会卡死，需要通过点击一下其他键（比如放大/缩小字体）才能恢复正常」。

**根因取证**（systematic-debugging 四阶段 + Chrome DevTools 真实键盘探针）：

- EpubReader 键盘监听（方向键/WASD → `rendition.next()/prev()` 或按住滚动）只挂父 window；epub 正文渲染在 sandbox 同源 iframe 内（epub.js iframe.js），在正文上的任何点击（划词高光/批注、点高光标注、点书内链接、随手点正文）都会把文档焦点移进 iframe。
- KeyboardEvent 只派发给焦点所在文档、不跨 frame 边界 → 父 window 监听收不到 → 翻页处理器不触发 →「卡死」；点击 A-/A+ 等父层控件把焦点带回父文档后恢复——与开发者 workaround 完全吻合。epub.js 自身只把 iframe 内 DOM_EVENTS（含 keydown/keyup）以 passive 转发为 rendition 事件（contents.js addEventListeners → rendition.passEvents），项目未使用该通道。
- 浏览器层实证（1:1 复刻页：父 window 监听 + iframe document 监听，真实点击 + 真实按键）：焦点在父文档按键 → 父 window 收到；点击 iframe 正文后按键 → 只进 iframe document、父 window 零事件（bug 复现）；iframe document 转发 → 翻页处理器被调（修复方向确认）。

**修复**（最小改动，`EpubReader.tsx` 三处）：

- 新增 `keyHandlersRef`（现行键盘处理器，供 iframe 转发）+ `wiredDocsRef`（WeakSet 防同文档重复挂载——防一次按键翻两页）。
- 键盘 effect 把 `onKey`/`onKeyUp` 存入 ref、卸载置 null（旧 document 残留监听转 no-op）。
- 现有 rendered 钩子（原注入打包字体处）追加：每个章节视图 document 挂 keydown/keyup 转发到 ref 处理器。焦点在父文档走 window 监听、在 iframe 走 document 转发，物理按键只派发到一侧不会双触发；keyup 转发同时保证滚动模式按住 W/S 在正文内松开也能停滚动。
- PdfReader 无 iframe（pdf.js canvas 渲染在主文档）焦点不会被抢，无需改动。

**验证**：`npm run typecheck` 双 tsconfig 通过。运行时冒烟待开发者：重启 dev 后 epub 阅读中点击正文任意处（或划一次词）直接按 ←/→/A/D 翻页应立即响应；滚动模式点击正文后按住 W/S 流式滚动、在正文内松开能停。

**涉及文件**：`src/modules/bookshelf/EpubReader.tsx`、问题疑惑区第 11 轮归档。

## 白噪音播放队列（优化建议区第 38 轮）

> 创建于 2026-09-11 22:13

**需求**：优化建议区待完成区唯一条目——定义「音效」为组合音（出厂预设如篝火·围炉噼啪、用户自定义混音），以音效为单位增加播放队列，每项自定义时长（如 5/10 分钟），播放模式列表循环 + 单曲循环 + 列表随机。

**brainstorming 四项定稿**：①打断规则 = 队列是可选「接管模式」——队列运行中任何手动混音操作（拖滑杆/点预设/切场景/召回混音）自动退出队列、声音不断；主音量不打断；播放/暂停与队列正交（暂停冻结倒计时）。②UI = 页面顶部胶囊 tab「混音器 | 播放队列」双视图（recycle-tab 范式），混音器零改动。③时长 = 预设档 1/5/10/15/20/30/45/60 分钟 +「自定义…」数字输入（clamp 1–480）。④补第四模式「顺序播完即停」（默认，睡前定时听；另三模式维持循环语义）。架构拍板队列控制器**并入 NoiseEngine 单例**（否掉独立服务/渲染层驱动）——打断规则要求引擎区分「用户操作 vs 程序性切歌」，此信息只在引擎内部最清楚：公开 setLayer/setScene = 用户操作触发打断，队列切歌走私有 applySoundProgrammatic 不触发。

**实施**（plan 七任务全部完成）：

- `src/services/queueOrder.ts` 新纯逻辑模块：shuffleIds（Fisher-Yates + exclude 头部去重防连播）+ nextValidId（sequence 不回绕 null=播完即停 / list-loop 回绕含自身）；临时断言脚本 13 条 ALL PASS 后删（一处用例自身写错修正——「尾部全失效」用例漏排 c，实现行为正确）。
- `noiseEngine.ts` 并入队列控制器：getQueue/setCustomMixes/setQueueConfig/loadQueue/startQueue/stopQueue + 私有 playSoundAt 链（淡出 1.2s → 私有切场景配比 → 淡入回用户主音量 → 起计时，过渡不占播放时长）、advanceQueue 四模式推进、nextShuffleId 整轮洗牌耗尽重洗、finishQueue 播完淡出自动暂停 + master 归位（防下次手动播放静音）；queueToken 代际令牌——停止/打断后旧 fade 链自废，fadeMaster 墙钟计时（ctx 挂起音频时钟冻结的边界已注释）；play/pause 各挂一行队列计时恢复/冻结；activeId 按**行 id** 跟踪——运行中增删改/排序/换模式不乱（删运行行立即按模式推进）。
- 运行态（activeId/remainMs）不持久化——重启默认暂停从头开始；队列程序性切歌不写 noise_state（重启自由混音回到进队列前）。
- `shared/types.ts` SettingsKeys.NoisePlayQueue + `App.tsx` 启动恢复 effect（与 NoiseState 同构）。
- UI：NoisePage 薄壳化（tab + 播放大按钮共用 + 自定义混音上提两面板共用并同步引擎 setCustomMixes + onNotice→toast）；MixerPanel 原主体搬运 + 队列运行提示条（点击跳队列 tab）；QueuePanel 新组件——引擎为唯一状态源（getQueue 快照渲染、setQueueConfig 写入 + 落 settings，无本地镜像），运行态状态行（正在播放 N. 名 · 倒计时 mm:ss + 停止队列）、添加音效弹窗（按场景分组预设+混音多选 + 统一时长）、模式四选 pill、队列行（时长下拉/自定义输入/上下移/移除二次确认、运行行高亮、失效灰显「已删除」）；noise.css 队列样式块。
- 零主进程改动零 DB 迁移（settings JSON 新 key noise_play_queue）。

**验证**：typecheck 双配置 + build 通过；断言 13 条全过。**待冒烟**（dev 热更即生效）：建跨场景混合队列、四模式各验（末项播完自动暂停/回绕/单曲无缝重置计时/随机不连播同首）、切歌淡入淡出无爆音（1 分钟时长快速验证）、打断三路 toast「已退出队列播放」且声音不断、主音量不打断、暂停冻结恢复、切模块不断、删被引用混音行灰显跳过、重启配置在默认暂停、双主题目检。

**涉及文件**：`src/services/queueOrder.ts`（新）、`src/services/noiseEngine.ts`、`src/shared/types.ts`、`src/App.tsx`、`src/modules/noise/{NoisePage,MixerPanel,QueuePanel}.tsx`、`src/modules/noise/noise.css`；设计 `左侧边栏/白噪音/2026-09-11-白噪音播放队列-design.md` + 同名 plan。

### 第38轮反馈修订：混音器 chip 快捷加入队列（260911 22:42）

> 创建于 2026-09-11 22:42

**需求**：开发者冒烟前反馈——「在混音器页面就希望每种音效有一个可以"添加到播放列表"的效果」。原实现加队列唯一入口在 QueuePanel 添加弹窗，混音器页看到心仪音效要切 tab 再勾选，路径长。

**实施**：

- 混音器页两类 chip 各加「添加到播放队列」图标按钮（`playlist_add`，悬停 title 提示）：出厂预设 chip 由单按钮改 `tag-chip removable` 群组（名称点击=应用预设不变 + 图标按钮追加队列）；「我的混音」chip 在召回与删除之间插入同款按钮。
- 点击行为：以默认 30 分钟**追加队尾** + toast「已加入队列：场景 · 名称」；队列运行中也可加（activeId 按行 id 跟踪，list 模式顺序自然到达、random 下轮重洗纳入）。时长与排序之后在队列 tab 逐行调。
- 配置写入收敛：新 `queueStore.ts` 小件（`saveQueueConfig` = setQueueConfig + settings 落盘、`genQueueItemId`、`QUEUE_DEFAULT_MINUTES`、`QueueSoundRef` 类型），QueuePanel 的 update/commitAdd 改用之（去本地 DEFAULT_MINUTES/直写 settings），NoisePage 新增 `addToQueue`（拼 label、toast）传 MixerPanel——队列配置写入口唯一，两面板共用。
- 样式：`.tag-chip.removable button:hover` 全局默认 danger 红（删除语义），chip 追加按钮加 `.q-chip-add` 提特异度覆盖为主题色 hover。
- 图标选型说明：`playlist_add` 为 Material 早期经典稳定图标（第 19 轮 `notebook` 拆字是新图标个例）；woff2 压缩态无法 grep 验证连字，冒烟目检兜底——若拆字一行换图标。

**验证**：typecheck 双配置 + build 通过。**待冒烟补**：预设/混音 chip 的 + 按钮点击后 toast 出现、队列 tab 行尾多出该项（30 分钟档）、运行中追加可达、hover 主题色不红、`playlist_add` 渲染为单图标。

**涉及文件**：`src/modules/noise/queueStore.ts`（新）、`NoisePage.tsx`、`MixerPanel.tsx`、`QueuePanel.tsx`、`noise.css`。

### 第38轮反馈修订二：音效小组件改整行横条式（260911 22:46）

> 创建于 2026-09-11 22:46

**需求**：开发者反馈——把音效从小组件（chip 胶囊）样式改成一整行横条式。

**实施**：混音器页「出厂预设」与「我的混音」两区由 tag-chip 胶囊云改为横条行列表（`.noise-queue-row` 几何复用 + 新 `.noise-sound-row`：整行可点 + hover 主题色描边）——整行点击 = 应用预设/召回混音（stopPropagation 保护行尾按钮），行尾 ➕ 加队列、我的混音再加 ✕ 删除（原 chip 交互全部保留，只换形态）；行容器规则 `.noise-queue-rows` 扩为与 `.noise-sound-rows` 共用；撤下修订一引入的 `.q-chip-add` hover 覆盖（chip 已不存在），`.noise-chips` 规则留档标注暂无使用方。

**验证**：typecheck 双配置 + build 通过。**待冒烟补**：整行点击应用/召回、行尾按钮不触发行点击、hover 描边、两区横条观感与队列 tab 一致。

**涉及文件**：`src/modules/noise/MixerPanel.tsx`、`src/modules/noise/noise.css`。
