# 260921 开发日志

## 补办归档：内置终端 / AI 生成全局取消 落地归档（含 idea/ 归位纠错与规则固化）

> 创建于 2026-09-21 00:18
> 更正于 2026-09-21 00:29——初版归档落位有误，依开发者纠正重做，本节为最终态记录

**起因**：开发者指出内置终端（260912 已实施完成并冒烟通过）的 design/plan 一直未归档——260912 日终时顺带清点被漏。

**纠错经过**：AI 补办时误将两文档留在 `idea/内置终端/archive/`（把「移入所属目录 archive/」误解为原地建 archive）。开发者二次纠正 **idea/ 的定位语义**：`idea/` 只放**尚未完成甚至尚未立项、停留在想法阶段的点子**；已落地的文档不得留在 idea/ 下，必须移出并归入所属**正式目录**的 `archive/`。内置终端为全局功能（非左栏模块，正式归属 `docs/project/全局/`），故最终落位：

- `idea/内置终端/{2026-09-12-内置终端-design.md, -plan.md}` → **`全局/archive/`**（文件名不变，状态行补归档记录并注明纠错缘由），`idea/内置终端/` 目录已删除；
- 同类遗漏顺带补办：`全局/` 平铺的 **AI 生成全局取消** design+plan（260908 已实施，Task 1-19 全链路）一并移入 `全局/archive/` 并补状态行；
- 引用更新：终端 plan「设计依据」、全局取消 plan「设计依据」、`新功能开发区.md` 对应条目、`electron/ai/jobs.ts` 头部注释，旧路径全部改指 `全局/archive/`；CLAUDE.md / designs-specs 无其他旧路径引用；历史日志按规则不改。

**规则固化**（防再犯，均当次生效）：

1. `docs/project/README.md`（机制正本）：存放位置表 idea 行改为「idea 阶段（未立项、未落地的想法；**落地后必须移出 idea/**）」、新增「全局功能 → `全局/`」行；归档一节明确「idea/ 内文档落地后移出 idea/ 再归档，不得留在 `idea/<模块>/archive/`」；
2. `CLAUDE.md` 文档体系表 spec/plan 行同步修订（按归属存放 + 全局功能归 `全局/` + 落地即移出 idea/）；
3. AI 侧记忆新增「归档清点要实际执行」条目（find 全量列清单逐份判定，不得凭印象跳过），并记入 idea/ 语义教训。

## 我是谁（个人档·我的画像之下）

> 创建于 2026-09-21 01:44

- 来源：新功能开发区待完成区任务（260921 brainstorm 商定，见 `左侧边栏/个人档/2026-09-21-我是谁-design.md` + 同日 plan）。
- 决策回放：启动后台生成每日任务、总导览紧凑状态行+直达、广谱跨模块聚合出题、自动提炼+逐条确认入档（同 PROFILE_SUGGEST 范式）、跨天未答作废/待确认保留、实现方案 A（新表 + 独立服务）。
- 实施：DB v47 `whoami_questions`（历史行永不删，显示只取当日）+ settings 键 `whoami_daily_date`；新服务 `electron/services/whoami.ts`（11 源广谱聚合 + 出题一次调用 + 提炼每答一次调用，JSON 解析同 parseJsonObject 加固范式）；IPC 五通道（whoami:get/generate/answer/extract/resolve）+ preload + api.d.ts；启动 10s 簇 + 午夜排程双触发（均静默失败、LLM 未配置跳过不记日期）；渲染层 `WhoamiZone`（ProfileModule 画像 zone 之下，题目行三态：待答/提炼中/候选待确认，跳过收起、提炼失败可重试）+ profile 深链 target='whoami' 自动展开 + 总导览挑战卡后紧凑状态行（未答主题色提示、候选待确认计数、当日无题不显示）。
- 中途修正：`chatCompletion` 实际导出自 `ai/llm.ts`（非 services）；node:sqlite `.all()` 返回类型需经 `unknown` 中转断言。
- 验证：typecheck 双配置 + build 通过；运行时冒烟（出题/答/提炼/加入画像/总导览直达/跨天作废/LLM 未配置提示）待开发者验证。

## debugzi 统一会话流——验证与归档（补上一会话漏记）

> 创建于 2026-09-21 20:10

- 背景：优化建议区「debugzi 子模块繁杂沉重」条目于上一会话 brainstorming 定稿并实施完毕（随 5e101c2 提交，提交说明标「效果待验证」），当时 dev log 漏记，本节补记实施要点与本次验证结论。
- 实施回放（上一会话）：常驻 7 频道 pill 条删除，频道降级为会话属性标签；头部场景小 pill（助手会话不显示）；会话浮层全量跨场景平铺+场景徽章；「＋」只开助手会话；激活键双轨（新 `ai_active_session_global` 启动恢复最后聊过的会话 + 7 个 per-channel 键保留服务模块动作与拓展坞，双键落库）；辩真/预言家推送跟随切换到接收会话；主进程 `aiChat` 人设按会话 id 查 channel；三选条判断源改当前会话 channel、保存/归档后切助手新会话；零 DB 迁移。设计/计划 `右侧边栏/debugzi/2026-09-21-debugzi统一会话流-{design,plan}.md`。
- 验证（本次）：逐项核对 design 7 决策全部落地；`AiActiveChannel` 删除无死引用（DraftSidebar 的 switchChannel 为草稿本自有频道概念，无关）；typecheck 双配置通过。开发者确认效果，条目归档为优化建议区**第48轮**；design/plan 移入 `debugzi/archive/` 并补状态行。
- 遗留提示：`debugzi/design.md` 与 `右侧边栏.md` 的「频道制」描述已过时——该两份为开发者主导文档，AI 未擅改，建议开发者审核后更新。

## 触发音与白噪音体验升级（音乐吧）

> 创建于 2026-09-21 20:42

- 来源：新功能开发区待完成区任务（260921 brainstorm 商定，见 `左侧边栏/音乐吧/2026-09-21-触发音与白噪音体验升级-design.md` + 同日 plan）。
- 决策回放：触发音混合来源（内置合成近似 + 导入 mp3/wav/ogg 补耳语/刷头类真人录音——耳语类合成做不像）；并入白噪音混音层为**跨场景叠加**（不做独立声板页签）；立体声声像（每层滑杆 + 触发音「游移」随机摆位，游移开时滑杆置灰）；新场景 ×4（雷雨/雪夜/风/溪流）；播放/暂停/切场景淡入淡出；睡眠定时器本轮明确不做。
- 实施：DB **v49** `trigger_sounds`（v48 已被并行会话超级工作台 2.0 批次A 占用，按 v43 先例顺延）+ 零新 settings 键（`NoiseState` JSON 扩展 `pans`/`triggers`，旧数据容错回落）；主进程新服务 `electron/services/trigger.ts`（导入/改名/删除/读字节五通道，文件入 `userData/triggers/<id>.<ext>`，删除二次确认彻底删不入回收站）；`noiseScenes.ts` 追加 4 场景 + `BUILTIN_TRIGGERS` 注册表；新文件 `src/services/triggerSynth.ts`（9 合成器：木桌轻叩/玻璃轻敲/翻书页/机械键盘/剪刀开合/泡泡纸/塑料袋窸窣/风铃/颂钵，参数随机微变防机械感）；`noiseEngine.ts`——场景层 StereoPannerNode 声像链路、触发层跨场景实例（2–8s 随机间隔单发、游移每次发声随机左右、导入项 decodeAudioData 预解码缓存/丢文件提示一次）、`play` 0.8s 淡入（`immediate` 选项供队列路径）、`pause` 0.45s 淡出后挂起（pauseToken 防快切误挂起）、`applyScene` 统一入口（播放中 0.4s 淡出→换建→0.45s 淡入，sceneToken 连续快切旧链自废）、自定义混音/播放队列自动携带触发层快照（出厂预设不动触发层）；`MixerPanel` 新增触发音区（内置+导入统一行：开关/音量/声像/游移/改名/删除）+ 分层滑杆加声像条 + 持久化扩展 + 保存混音携带触发层快照；`noise.css` 触发音行样式（accent-color 对齐既有 `--color-primary` 惯例）。
- 验证：本任务全部改动文件 typecheck 零错误 + `npm run build` 通过。收尾时 typecheck 尚余 13 个错误**全部位于 `AgentSettingsSection.tsx`**（并行会话超级工作台 2.0 批次A 半成品，`window.api.agent` 未接线），与本任务无关。运行时冒烟（内置 9 触发音听感/游移声像耳机测试/导入三格式/改名删除/新场景 ×4/淡入淡出/队列携带触发层/重启恢复）待开发者验证。

## 触发音与白噪音体验升级——冒烟反馈第 1 轮（单独播放 / 删除体系 / 预设合并 / 默认混音）

> 创建于 2026-09-21 23:27

- 来源：开发者首轮冒烟后四条反馈（口头，未走优化建议区），需求明确直接实施（第43/44轮同款惯例）。
- 四决策口径：①触发音单独播放=开启触发音行时引擎未播自动开播（淡入；纯触发音把场景层拉 0；批量应用〔默认混音/混音召回/队列〕`autoPlay:false` 不顺带开播）；②删除体系=场景/内置触发音/出厂混音行均可删（settings 新键 `noise_hidden` JSON 三名单 `{scenes, builtinTriggers, factoryMixes}`，删=进名单不显示不复活，零 DB 迁移，场景至少保留一个、删后当前场景自动逃逸到首可见场景）；③出厂预设并入「我的混音」=每次加载按 factoryKey 幂等播种为出厂行（可删不复活，删除二次确认注明），「出厂预设」区移除，旧队列 preset 项解析保留兼容；④默认混音=每场景可星标一条（同场景唯一，点星可取消），`setScene` 时应用其层配比+触发层替代出厂 defaults（队列切歌不走默认混音）。
- 实施：`noiseEngine`——EngineMixRef/setCustomMixes 增 isDefault、setScene 默认混音分支、setTrigger 增 autoPlay 选项、forgetTrigger 重构为 forgetTriggerKey（内置/导入通用）；`NoisePage`——CustomMix 增 factoryKey/isDefault、parseHidden/seedFactories、NoiseHidden 状态与透传；`MixerPanel`——场景条按名单过滤 + 分层混音头删场景钮（末场景禁用）、触发音内置行过滤+删除（二次确认）、我的混音行加默认星标 + 出厂行删除进名单、召回混音整组恢复触发层；`noise.css` 星标色。
- 验证：本任务文件 typecheck 零错误 + build 通过。**待冒烟**：停播状态开触发音行自动开播（纯触发音听感）、批量应用不误开播、删场景/删内置触发音/删出厂混音行（重启不复活）、末场景删除禁用、星标默认混音切场景生效、出厂预设行入队列、旧自定义混音与队列兼容。

## 万象库/学习库划词高光补全：卡片内取消高光 + 即时渲染（优化建议区第49轮）

> 创建于 2026-09-21 20:10

- 来源：优化建议区「万象库模块，需要能划出添加高光，也要能给已经高光的句子删除高光的功能」。需求明确，未开 brainstorming（第43/44轮同款直接实施惯例）。
- 排查定位：添加高光链路本就存在（划词气泡「高光」→ md 包裹 `==标记==` + wiki_highlights 入笔记本）；真实缺口二——①已高光句子无法取消（笔记本删除按 design.md 约定仅移除记录不动卡片标记，该约定保留）；②高光写盘后 MdDialog 因 filePath 未变不重载，高亮要重开弹窗才可见。
- 实施：MdDialog `selectionActions` 增可选 `onUnhighlight`——选区整体落在既有 `<mark>` 内时气泡出「取消高光」（按整段标记文本取消，局部选中同段整体取消；顺带堵住高光内再划高光产生嵌套 `==标记==` 的脏写），未传或非 mark 选区行为不变；新 IPC `wiki:removeHighlight`（按词条+文本删记录，preload/api.d.ts 同步）；WikiModule `onUnhighlight`＝md `replaceAll` 解除标记 + 删记录 + 刷新；WikiModule MdDialog key 扩为 `${id}-${mdVersion}`（学习库同款重挂范式）高光/取消即时渲染。零 AI 零 DB 迁移。
- 范围扩展（同日开发者指令）：学习库补齐同款「取消高光」——其 `onHighlight` 本就带 mdVersion 重挂（即时渲染本无缺），仅补取消链路：IPC `learn:removeHighlight`（按节点+文本删记录，preload/api.d.ts 同步）+ LearnModule `onUnhighlight`（`md/learn/<id>.md` 解除标记+删记录+重挂，万象库同款）+ 气泡传入；MdDialog 通用层零改动；高光笔记删除口径不变（仅移除记录）。typecheck 双配置 + build 通过。
- 验证：typecheck 双配置 + build 通过。**待冒烟**：划词高光即时见高亮、划选已高光句子出「取消高光」点击后标记与笔记本记录同步消失、重开卡片不再高亮、取消后再划可重新高光、跨 mark 混合选区仍出「高光」、笔记本删除仍仅移除记录、学习库划词回归、双主题。

## 超级工作台 2.0：一期实施计划产出

> 创建于 2026-09-21 20:06

- 开发者指令查阅 `idea/超级工作台2.0/` 全套文档并启动全面升级，逐段推进、每段停下验证。
- 产出 `idea/超级工作台2.0/260921-超级工作台2.0-一期实施计划.md`（总纲 §9 流程第 2 步）：现状锚点表（DB v47 / chatCompletion+llm_usage 按 scene 记账可复用预算 / findSearchTool 渠道 / AiChannel 四处注册点 / MainView 临时视图先例）+ 四段节奏（本计划→批次 A 底座→批次 B 深读线→批次 C 感知层→二期另计划）。
- 关键落地决策：一期渠道锁定 MCP 搜索 + arXiv API（GLM 原生联网搜索留扩展点不阻塞）；DB v48 一次建 7 表（science_articles 留二期新版本）；引擎目录 `electron/services/agent/` 七文件（guardrails/embedding/loadGuard/budget/queue/sources/engine）；`agent_enabled` 默认关、冷启动向导完成才开；精译按章幂等续跑；全文抓取失败降级 meta_only + 手动传 PDF；零新依赖（fast-xml-parser/readability/pdfjs legacy/Ollama embed 全现成）。
- 待办：开发者审阅计划后，按批生成 designs-specs（A 引擎底座 → B 深读线 → C 感知层）再逐批实现；git 全程开发者自理。

## 超级工作台 2.0 批次A：引擎底座落地

> 创建于 2026-09-21 20:45

- 依据：`idea/超级工作台2.0/260921-超级工作台2.0-designs-specs-批次A-引擎底座.md`（本日生成，开发者过目后实施）。
- 存储层：DB **v48** 七表纯新增（agent_domains / discover_items / papers / interpretations / task_runs / knowledge_links / embeddings + 三索引）；initDb 新增 `papers/`、`md/interpretations/` 目录；science_articles 留二期新版本号。
- 类型与键：SettingsKeys 增 9 键（agent_enabled 默认关、负载阈值、预算、隐私白名单、embedding_config、literature 会话键占位）；shared/types 与 api.d.ts 双侧新增 AgentTrack/AgentPhase/AgentDomainRow/DiscoverItemRow/PaperRow/InterpretationRow/TaskRunRow/AgentStatusSnapshot/EmbeddingConfig/AgentConfigView；LLM_SCENE_LABELS 增工作台四场景（agent:collect/digest/lecture/translate）。
- 引擎层 `electron/services/agent/` 八文件：guardrails（wrapMaterial 防注入包裹 + politeFetch 同 host 3s 限速/UA/20s 超时/退避重试）、embedding（Ollama /api/embed 客户端 + Float32 BLOB 存取 + 余弦暴力扫描 + ensureOllama 一键拉起）、loadGuard（5s 采样 os.cpus() 差分，>阈值连续 3 窗暂停/<恢复阈值连续 6 窗续，迁移写 task_runs 台账）、budget（llm_usage 按 agent:% 场景当日汇总，只限搜集类）、queue（task_runs 台账 + 内存池并发 2 + 单飞行 + 优先级 + 指数退避重试逐次留痕 + recoverOnBoot 终态化 + cancelRun）、domains（CRUD + last_scan_at 存本地日期）、sources/arxiv（Atom API + fast-xml-parser）、sources/mcpSearch（findSearchTool 动态 import）。
- 装配与通道：engine.ts startAgent 自判 agent_enabled（关闭态零副作用）→ recoverOnBoot + startLoadGuard + 错峰巡检（5s 起每 90s 一领域，24h 重排；收集器钩子留批次 B 注册）+ 状态广播（500ms 节流 push `agent:status`；负载迁移联动 setDispatchPaused）；main.ts 启动挂载；IPC 九通道（agent:statusGet/configGet/configSet 键白名单/domains/domainSave/domainDelete/tokensToday + embedding:test/serve）+ preload agent/embedding 命名空间 + api.d.ts 同步。
- 个人档：新组件 AgentSettingsSection（part 双挂载）——Embedding 配置块（LLM 配置区下方，测试连接/拉起 Ollama）+ 超级工作台区（MCP 区后：引擎开关/深读·科普领域管理〔删除二次确认〕/负载阈值〔恢复须小于暂停〕/每日预算/隐私白名单默认全关）；复用 profile zone 体系与主题色，零 emoji 图标。
- 中途排障：web 侧 tsc 报 api.agent 不存在为 **composite 增量缓存陈旧**（tsbuildinfo 未感知 api.d.ts 变更），删缓存后全绿；无代码问题。
- 验证：typecheck 双配置 + build 通过。**待冒烟**：个人档两新区块呈现与交互、Embedding 测试连接报 1024 维、领域增删改启停重启不丢、引擎开关控制台 [agent] 日志起停、typecheck/build 复核；任务中心/呼吸灯等感知层在批次 C。

## 超级工作台 2.0 批次B：深读线全管道落地

> 创建于 2026-09-21 23:32

- 依据：`idea/超级工作台2.0/260921-超级工作台2.0-designs-specs-批次B-深读线.md`（23:18 生成后实施）。开发者确认批次 A 后继续。
- 管道服务（`electron/services/agent/` 新四文件）：**collectDeep**（arXiv Atom + MCP 搜索双渠道 allSettled 并行 → 海选 LLM jsonMode 单调用〔材料一律 wrapMaterial 包裹；url 白名单防编造；无结果合法〕→ url_hash + embedding 近重复 >0.92 去重 → discover_items，向量顺带入库）；**papers**（acceptDiscover 事务转正 + make_digest 自动入队；ensureFulltext——arXiv 规范 id 拉 PDF 存 `papers/{id}.pdf` → pdfjs-dist legacy 抽文本存 `.txt` / 非 arXiv 走 politeFetch+readability 抽正文；失败降级 meta_only 不炸；importManualPdf 手动传 PDF 兜底；paperRelated/pendingDiscoverCount 查询）；**interpret**（导读卡固定六节骨架自动生成〔meta_only 卡头注明〕；精讲/精译按章 ~10k 切块逐章 LLM 落盘、按 `## 第 N 部分/章` 计数幂等续跑；精译术语表先行存 papers.glossary 全文统一；token 记账 = llm_usage 场景差值经 ctx.addTokens 回写 task_runs；runInterpret 已 done 未 force 抛 INTERPRET_EXISTS）；**embedIndex**（导读卡/摘要向量化 + 万象库词条每触发补一批索引 + 余弦 ≥0.6 top3 写 knowledge_links；Ollama 不可达静默跳过）。bootstrap.ts side-effect 装配（collector 仅 deep 入队、science 留二期提示），main.ts import。
- 任务注册：collect_deep（p10 单飞行 重试2）/ make_digest（p10 单飞行 重试1）/ lecture、translate（p5 单飞行 重试0 按需）/ embed_index（p20 单飞行 重试1）；搜集类派发前 collectionAllowed() 预算检查、LLM 未配置 failed 留痕。
- 通道与 UI：IPC 增 agent:discoverList/discoverAccept/discoverReject/papers/paperDetail/runCollect/runInterpret/importPaperPdf（dialog 选 PDF）/pendingCounts + preload + api.d.ts（DiscoverItemRow/PaperRow/InterpretationRow 渲染层副本）；信息源升双页签「订阅 | 文献」（mainTab 会话内态；文献态头部隐订阅操作显待终选数）；新 LiteraturePanel（发现箱〔接受/拒绝/选领域跑海选〕+ 正式文献列表 + 详情弹窗〔产物三按钮生成/查看、meta_only 手动传 PDF、相关内容〕+ MdDialog 查看产物可双击编辑 + 重生成二次确认）+ literature.css 主题变量色系；feed 深链 target='literature' 先行铺好（批次 C 任务中心直达用）。
- 中途排障：pdfjs v6 类型变化两处（DocumentInitParameters 无 isEvalSupported、destroy 移至 loadingTask）改 loadingTask.destroy()；LiteraturePanel kind 'translate'→'translation' 映射修正。
- 验证：typecheck 双配置 + build 全绿。**待冒烟**：文献页签海选→发现箱条目带推荐理由→接受→导读卡自动生成→精讲/精译按需（含中断续跑与重生成确认）→meta_only 手动传 PDF→订阅页签零变化回归。
