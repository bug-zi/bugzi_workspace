# 260922 开发日志

## 超级工作台 2.0 批次B 验证反馈四项修复

> 创建于 2026-09-22 00:10

- 来源：开发者 260921 深夜验证批次 B 后反馈四问题：①海选/解读产物完成后前端不刷新；②「跑一轮海选」组件框样式需优化；③海选结果时间需精确到日；④正式文献缺删除功能。
- ①实时刷新：queue.ts 事件化（`QueueEvent {type, status: enqueued|done|failed|skipped}`，入队/终态各 notify 一次）→ engine.ts 记录 `lastQueueEvent` 随 `agent:status` 推送（`AgentStatusSnapshot.lastEvent` 新字段，双侧类型同步）；LiteraturePanel 订阅 `onAgentStatus`——collect_deep 终态 → 刷发现箱+toast；make_digest/lecture/translate 终态 → 刷文献列表+详情（detailIdRef 防闭包过期）+toast；详情内按钮依 `runningTypes` 显示「生成中…」旋转态；「查看导读卡/精讲/精译」改走 MdDialog 打开产物，重生成独立为旁置 refresh 小按钮（原「查看即触发生成确认」的 UX 缺口一并修正）。
- ②样式：海选控件升为独立「AI 海选」卡（lit-collect-bar：说明文案 + 领域选择器 + 主按钮，theme 变量描边卡底）；修正 CSS 变量名为项目真实变量（--color-border/--color-surface，原 --border-color/--panel-bg 不存在靠 fallback 侥幸显示）。
- ③日期精确到日：DB **v50**（discover_items/papers 各加 `date TEXT` 列；v49 已被并行会话触发音占用顺延）；arxiv.ts 候选带 published 前 10 位；collectDeep 建 origin+path 归一映射，插入时**直接取渠道原始日期不经 LLM 转述**（source 同口径）；转正平移进 papers；列表/详情显示 date ?? year。
- ④删除：papers.ts 增 `deletePaper`（彻底删：interpretations 行+产物 md+papers/{id}.txt/.pdf+embeddings+knowledge_links 双向，先库后文件文件失败仅留痕）与 `deleteDiscover`（条目+其向量）；IPC `agent:paperDelete`/`agent:discoverDelete` + preload + api.d.ts；渲染层发现箱行/文献行/详情 footer 三处入口，均 ConfirmDialog 二次确认（删除文献文案列明连带范围）。
- 验证：typecheck 双配置 + build 全绿。**待冒烟**：海选完成后发现箱自动出现条目（不手动刷新）、接受后导读卡完成自动可「查看」、生成中按钮转圈、日期显 YYYY-MM-DD、三处删除入口二次确认后连产物删除、订阅页签回归。

## 超级工作台 2.0 批次C：感知层落地（一期收官）

> 创建于 2026-09-22 00:37

- 依据：`idea/超级工作台2.0/260921-超级工作台2.0-designs-specs-批次C-感知层.md`（00:28 生成后实施）。开发者确认批次 B 四项修复后继续。
- **文献·追问场景（按 260921 统一会话流接入）**：AiChannel 增 'literature'（shared/types + api.d.ts 双侧）；ACTIVE_SESSION_KEYS 两处（主/渲染）接批次 A 占位键；CHANNEL_PERSONAS 增论文研读助手人设；AiSidebar CHANNELS 场景清单增「文献·追问」（auto_stories 图标）。上下文注入新服务 `agent/ask.ts`：定位/新建 literature 激活会话 → 注入导读卡||摘要 + 全文节选 40k（wrapMaterial 包裹）+ `<<<LIT:{id}>>>` 幂等标记 → 'ai:message' 推送自动跟随；**关键改造**：aiChat 的 history 过滤掉 system 行（辩真进度只展示），literature 通道特例——取会话最新一条 LIT 上下文作为额外 system 交 LLM（其他通道零变化）。入口：文献详情「追问」按钮（askLiterature → FeedModule 新 prop onOpenAi=openAiWith 显式 channel）。
- **呼吸灯 + 任务中心**：MainView 增 'agent'（openAiWith 的 CHANNEL_BY_MODULE 守卫同步扩）；新组件 AgentBreathLight（nav-item 形态列于记账本与音乐吧之间，working=主题色缓脉动/paused=灰常驻悬停原因/idle=静默，CSS 追加 App.css 走主题变量）；新页 AgentCenterPage（相位 chip+CPU/内存+今日额度+引擎快捷开关、待终选直达〔MODULE_NAVIGATE_EVENT go('feed','literature')〕、台账表 task_runs 倒序 100 条含 load_pause 行）；onAgentStatus 终态事件自动刷新台账。
- **总导览聚合块**：OverviewModule 增「超级工作台」卡（每日一题之后）——昨日海选/解读计数 + 待终选 + 相位，未启用引导文案；点击派发独立事件 `bugzi:open-agent-center`（App 监听 activateModule('agent')；不走 MODULE_NAVIGATE_EVENT，'agent' 非 ModuleId 不动公共类型）。
- **冷启动向导**：AgentColdStartGuide 三步弹窗（定位+信任契约+隐私说明 → 领域预置勾选/自建可跳过 → 可选立即首跑）；`agent:coldStartFinish(runFirst)` 置 agent_enabled=1 + cold_start_done=1 + runFirst 时全启用 deep 领域入队首跑；App 挂载时 firstLaunch===false 且 cold_start_done 空 → 弹（首装走 WelcomeGuide 不叠加）。
- 新 IPC：agent:runsList / agent:dayStats / agent:askLiterature / agent:coldStartFinish + 新服务 center.ts（dayStats 本地日界统计）；preload + api.d.ts 同步（TaskRunRow/AgentDayStats 副本 + AgentDayStats 入 shared/types 单源）。
- 验证：typecheck 双配置 + build 全绿。**待冒烟（一期收官验证）**：文献追问带上下文问答与场景徽章、换文献注入新上下文、呼吸灯三态与暂停原因、任务中心台账与直达、总导览块数字、冷启动向导一次性和首跑、既有场景回归。全部通过后 2.0 一期完，进二期（科普线/书籍解读/A1）。

## 超级工作台 2.0 一期代码全部落地（当日收尾状态记录）

> 创建于 2026-09-22 00:44

- 状态：批次 A（引擎底座，已验证通过）→ 批次 B（深读线，已验证并修复四项反馈）→ 批次 C（感知层，**代码完成待验证**）。一期全链路代码至此全部落地，typecheck 双配置 + build 全绿。
- 待办（开发者）：按批次 C 验证清单冒烟——文献·追问带上下文问答、呼吸灯三态与负载暂停、任务中心台账与直达、总导览聚合块、冷启动向导一次性、既有场景回归；git 提交由开发者自行操作。
- 下次继续：一期验证通过后进入**二期**（科普线：万象库三页签/解读阅读视图/双向链接 + 书籍解读 + A1 知识网络），届时先出二期细化计划（含 science_articles 新 DB 版本）供开发者审阅，二期收官发 2.0.0（发版流程照既有 tag/release 双体系惯例）。
- 文档现状：`idea/超级工作台2.0/` 现有总纲 design、一期实施计划、批次 A/B/C 三份 designs-specs；二期 spec 未生成。落地后全套文档按归档铁律移出 idea/（归属建议 `全局/archive/`，发版时开发者定夺）。

## 超级工作台 2.0 二期实施计划产出

> 创建于 2026-09-22 15:38

- 开发者确认一期收尾，指示进入二期。按一期惯例先出细化计划：`idea/超级工作台2.0/260922-超级工作台2.0-二期实施计划.md`（15:30 产出，15:31–15:37 自审修正四处）。
- 计划范围：批次 D 科普线（collect_science + DB v51 science_articles/science_highlights + interpretations 重建扩 kind='light' + 万象库科普页签与解读阅读视图 + 词条↔文章双向链接 + 发现箱科普分区）、批次 E 书籍解读（fflate epub 全文抽取 booktext.ts + book_digest + 图书馆书卡入口 + 追问扩 book owner）、批次 F A1 知识网络（embed_science/embed_book/learn_node 索引 + KnowledgeLinksDialog 手动建链）、批次 G 收官发 2.0.0。
- 关键取舍（计划 §0 五个待开发者确认项）：万象库四页签（问答 260912 已加入，总纲三页签未计入）、'light' 台账 kind 靠重建表、概念自动链接 origin=similarity/人工建词条=manual、书籍解读入口默认全书放开（总纲原文英文书）、A1 首发按全书粒度（章节锚点留 2.1）。
- 锚点已核实：DB v50、搜集器路由点 bootstrap.ts:11、acceptDiscover papers.ts:79 不分流、ask.ts LIT 标记前缀匹配可扩展、MdDialog readOnly/selectionActions/footerBar 可复用、epub 解压用既有 fflate unzipSync、learn_nodes level=2 知识点。
- 状态：**待开发者审阅计划（重点 §0 确认项）→ 通过后逐批生成 designs-specs 并实施**。
