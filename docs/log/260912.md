# 260912 开发日志

## 优化建议区第39轮：格言库菜单跟随点击处（渲染层小改）

> 创建于 2026-09-12 01:27

**需求**：`docs/project/优化建议区.md` 待完成区唯一条目——文笔坊-格言库单击格言条召唤功能菜单，菜单固定出现在行最右端，点行中间召唤后还要把鼠标移到右侧才能操作，不便；希望菜单出现在**鼠标点击处的正下方**。

**实施**（需求明确的小改，未开 brainstorming，两文件零后端改动）：

- **ActionMenu 通用件扩展**（`src/components/ActionMenu.tsx`）：新增可选 `anchorPoint: {x, y}` 坐标锚定（与 `anchorEl` 二选一）。点锚定 = 菜单**左缘对齐点击 x、顶部在点击 y 下方 6px**（原生右键菜单惯例）；防溢出在既有规则上扩展——点锚定**右溢向左回移**（`left + w > innerWidth - 8` 时回移至右缘内 8px）、底溢翻到点击 y 上方（翻转参考点由锚上缘改点击 y）、左缘下限 8px 复用；`anchorEl` 元素锚定分支（右缘对齐 + 底溢上翻）行为逐字不变，藏书架（字体/阅读背景菜单）与记账本两处既有使用方零影响。effect 依赖 `[anchorEl, anchorPoint]`。
- **格言库召唤链路**（`src/modules/mottos/MottosModule.tsx`）：`menuFor` 状态由 `{id, anchor: HTMLElement}`（行/⋯ 按钮元素）改存 `{id, point: {x, y}}`（点击坐标）；行单击在 260ms 防抖闭包内取 `e.clientX/clientY`（点击时刻捕获，防抖窗口内鼠标微移不影响落位）、⋯ 按钮点击同款取坐标立即开菜单；渲染处 `anchorEl` 改传 `anchorPoint`。单击防抖给双击让路、双击开笔记、互斥单开、外点/滚轮/Esc/resize 关闭等交互零改动。

**落位退化分析**：点行中间 → 菜单就在鼠标正下方弹出；点 ⋯（近右缘）→ 右溢回移后落位与原行为基本重合，自然退化无突变。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过（10.1s）。运行时未实测，**待冒烟**：行中间/左端/右端点击看菜单落位、⋯ 按钮落位与原观感对照、窗口底部行点击上翻、双击开笔记不误弹菜单、藏书架与记账本菜单回归。

## 优化建议区第39轮反馈修订：格言库菜单消费式关闭

> 创建于 2026-09-12 01:36

**需求**：第39轮冒烟前反馈——菜单已召唤后再点格言条（含误触场景），当前是「关旧菜单 + 立刻开新菜单」，缺缓冲；希望菜单开着时点击菜单外任意地方只承担关闭，仅无菜单时点击行才召唤。

**实施**（仅 `MottosModule.tsx`，ActionMenu 通用件零改动）：

- **判定陷阱**：ActionMenu 外点关闭挂在 document mousedown **捕获段**，click 触发时菜单 state 已被清空，click 里查 state 判断不出「本次点击时菜单本来开着」。
- **对策**：MottosModule 挂一个同位（document、capture）mousedown 监听，把「按下瞬间菜单是否开着」记入 `menuOpenAtDownRef`；本面板挂载必然早于 ActionMenu（菜单只能由点击召唤），同 target 同 phase 监听按注册序执行，保证**先记后关**。flag 每次 mousedown 重记，click 评估的永远是本次按下的快照，无陈旧值问题。
- **配套**：`menuForRef` 镜像菜单开态（捕获段被清的是 state，ref 需与开/关同步）；开/关收敛 `openMenu`/`closeMenu` 两助手（改 ref + state），全文件 `setMenuFor` 直调清零；行单击（260ms 防抖内）与 ⋯ 按钮两处先查 `menuOpenAtDownRef` 再召唤，命中即 return（外点 mousedown 已完成关闭）。
- **语义变化**：原「同 id 再点切换关闭」的 toggle 语义移除——菜单开着时点同行/他行/⋯ 一律纯关闭。

**边界确认**：Esc/滚轮/resize 关闭后点行正常召唤（下次按下重记 flag）；双击开笔记防抖互斥不变；点菜单项自身不受影响（mousedown 在菜单内，外点不关）。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过。**待冒烟**：菜单开着点行/⋯/另一行均只关不开、关闭后再点行正常召唤、Esc 关闭后点行正常召唤、双击开笔记不误弹菜单。

## 优化建议区第39轮反馈修订二：标签编辑条三路收起

> 创建于 2026-09-12 01:48

**需求**：行内标签编辑条（菜单「标签」展开的行下方编辑行）加完标签后退出不便——唯一退出路径是再开菜单点「标签」图标切换。

**实施**（`MottosModule.tsx` + `App.css` 一条，三路收起）：

- **①外点收起**：扩展上一节建立的 document mousedown 捕获监听——编辑条开着时按下点在条外（任意处）即 `exitTagEdit()`（清 tagEditId + tagInput）；并记 `tagEditAtDownRef` flag，`onRowClick` 据此让**本次行点击只承担收起、不召唤菜单**（消费式口径与菜单一致；⋯ 按钮不拦——显式菜单意图，点 ⋯ 收条+开菜单一步到位）。条内按下（胶囊 ×/虚线胶囊/输入框/完成键）`inside=true` 不打扰，连加多标签不受影响。`tagEditRowRef` 经 ref 回调指向当前展开的编辑条 div（卸载自动置 null）。
- **②Esc**：挂编辑条**容器级** onKeyDown（冒泡接住——焦点在输入框或胶囊按钮上均生效；初版挂输入框被否，胶囊点击后焦点在按钮上 Esc 会失灵）。
- **③「完成」按钮**：编辑条尾部 check 图标 icon-btn（title「完成标签编辑」）；App.css 补 `.tag-edit-row .tag-done { flex-shrink: 0 }` 防多胶囊换行时被挤掉。
- **顺带**：输入框 `autoFocus`（菜单点「标签」后直接打字）；placeholder 补「Esc 收起」提示；收起统一收敛到 `exitTagEdit`（两 state 一起清，防残留串到下次编辑）。

**结构核实**：`tag-edit-row` 是行容器的**兄弟节点**（Fragment 内并列渲染），条内点击本就不冒泡到行单击处理器——不存在「点输入框误弹菜单」旧患，退出逻辑无需冒泡拦截。双击开笔记不受影响（首击收条被消费、次击照常触发 dblclick）。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过。**待冒烟**：菜单开编辑条后直接打字、条外点击只收不起菜单、点 ⋯ 收条+开菜单、胶囊连加多个不收条、Esc（输入框/胶囊焦点两态）收起、完成按钮收起、收起后再点行正常召唤菜单。

## 新功能开发区：内置终端（CLI 终端面板）

> 创建于 2026-09-12 02:53

**需求**：开发者提议「给项目添加 CLI 终端干酷炫的事 / 提效」——brainstorm 当场商定：内嵌系统终端（xterm.js + node-pty，VS Code 同款技术栈）、全局底部面板（横跨中栏+右栏、左栏完整可见）、`Ctrl+J` 呼出/收起、`Ctrl+Shift+J` 新建标签、选中输出发右栏 AI、多标签（上限 6）、目录记忆 + 双主题适配；Claude Code 纯手动敲命令即用（不做专门 UI）。设计记录 `docs/project/idea/内置终端/2026-09-12-内置终端-design.md`，实施计划同目录 plan。

**实施**（9 任务全链路）：

- **主进程**：`electron/services/terminal.ts` 新增 TerminalService——node-pty 按 id 管理真实 shell（Windows ConPTY），data 合帧（10ms 窗口批量）防刷屏洪峰，exit 推事件，`killAllTerminals` 挂 `before-quit` 兜底不留僵尸 shell；cwd 失效回落主目录；shell 三选 powershell/pwsh/cmd（pwsh 启动失败渲染层自动回落 powershell）。IPC 四通道 `terminal:create/write/resize/kill` + 两事件 `terminal:data/exit`。
- **构建**：`electron.vite.config.ts` 用 `externalizeDepsPlugin({ include: ['node-pty'] })` 外置原生模块。**踩坑记录**：初版用 `build.rollupOptions.external: ['node-pty']`，把 electron-vite 对 electron 模块的默认外置覆盖掉，electron 包 index.js 被内联进 bundle，运行时在 `out/main/dist/` 找 exe 报「Electron failed to install correctly」——改官方 externalizeDepsPlugin 后恢复正常。node-pty@1.1.0 自带 win32-x64 N-API 预编译（无 ABI 钉死），Electron 44 免 rebuild 直接可用。
- **渲染层**：`src/components/terminal/` 三文件——`terminalTheme.ts` 双主题 ANSI 16 色板（浅粉底深字/深蓝底浅字）；`TerminalTab.tsx` 单标签 xterm↔pty 接线（WebGL 渲染 context loss 自动降级、ResizeObserver 自适应 fit、右键菜单复制/粘贴/全选/清屏/问 AI、Ctrl+Shift+C/V、退出条一键重开 spawnEpoch 重建）；`TerminalPanel.tsx` 面板（标签栏、上限 6、底边拖拽高度 200px~60% 记忆、问 AI 按钮、运行中关标签 ConfirmDialog 二次确认、收起仅 display:none 进程不死）。
- **App 集成**：`App.tsx` 布局加 `.app-body`/`.app-body-row` 纵向包装（main-area + right-col 移入，终端面板居下，左栏不参与保持完整可见）；`Ctrl+J`/`Ctrl+Shift+J` 渲染层 keydown（不抢系统全局）；右栏底部新增终端图标（自上而下 终端→白噪音→主题，显隐随三面板收起态）；问 AI 走 `openAiWith(prefill, { channel: 'assistant' })` 钉助手频道，预填引用块超 8000 字符截断、不自动发送。
- **个人档**：新增「终端」zone——默认 shell 下拉 + 默认工作目录（复用 `storage:pickDir` 系统对话框）；settings JSON 键 `terminal`（零 DB 迁移），`parseTerminalSettings` 坏数据逐项回落默认。
- **类型镜像**：`src/renderer/api.d.ts`（手写镜像文件，非从 preload 导入）补 terminal 组声明。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过；CDP 运行时冒烟已过——面板呼出、建标签「PowerShell 1」、真实键入 `echo bugzi_smoke_2026` 回显正常（pty 双向链路通）、默认 cwd 即项目目录、双主题终端配色即时切换正常、右栏三按钮顺序正确。**待开发者冒烟**：敲 `claude` 起 TUI 会话、`npm run typecheck` 刷屏流畅度、选中输出→问 AI 预填、运行中关标签二次确认、退出后无残留 powershell 进程、个人档改 shell/cwd 后新标签生效、AI 展开态右栏图标隐藏。另：`npm run typecheck` 若报幽灵错误先删根目录 `*.tsbuildinfo` 再跑（composite 增量缓存有陈旧问题，本次实测踩到）。

## 内置终端冒烟 bug 修复：收起再重开后输入回显向下偏移

> 创建于 2026-09-12 03:04

**症状**（开发者冒烟反馈）：开终端打字 → `Ctrl+J` 收起 → 再 `Ctrl+J` 重开 → 继续打字，输入回显位置向下偏移、显示错乱。

**根因**：面板收起 = 容器 `display:none`，ResizeObserver 照常回调但 `FitAddon.fit()` 在 0 尺寸容器上**不抛错**，而是缩到 `MINIMUM_ROWS`（2 行 2 列），且旧代码把这个 2×2 同步给了 pty（`terminal:resize`）——PowerShell 的 ConPTY 控制台被压缩成 2×2 再在重开时拉回正常尺寸，两次畸形 resize 让 ConPTY 重绘与 xterm 光标状态失同步，回显行位错乱。

**修复**（仅 `TerminalTab.tsx`）：
- `tryFit` 增加零尺寸守卫：`container.clientWidth/Height === 0` 直接 return（不 fit、不动 pty）——收起态下终端与 pty 尺寸冻结，重开后 ResizeObserver 拿到真实尺寸才 fit；
- pty resize 去重：`sentSizeRef` 记录最近已发尺寸，cols/rows 未变化不重复发 `terminal:resize`，消除重开时无谓的 ConPTY 重绘（这也是偏移的共犯）。

**验证**：`npm run typecheck` 双 tsconfig 通过。自动复现验证因开发者本人 dev 实例在跑（单实例锁 + `--user-data-dir` 参数 Electron 不采纳）未能二次起隔离实例——渲染层改动 dev 热更新直接生效，**待开发者即时复现**：开终端打字 → Ctrl+J 收 → Ctrl+J 开 → 继续打字，回显应严格接在提示符后不再偏移。

## 内置终端增强：页签点击重命名

> 创建于 2026-09-12 03:09

**需求**：开发者冒烟通过后追加——默认页签名「PowerShell 1」，点击页签名即可重命名终端。

**实施**（`TerminalPanel.tsx` + `App.css` 一条，零后端改动）：

- 点击页签文字就地切换为输入框（autoFocus + 全选，长限 30 字），**Enter 确认、Esc 取消、失焦确认**；清空提交视为放弃保留原名；输入框内点击不冒泡（不触发页签切换/关闭），页签其余区域单击仍是切换激活。
- 改名仅当前会话有效（pty 不跨重启存活，标签列表本就不持久化）；「重新打开」进程后自定义名保留（tab 对象 label 不重置）。
- 样式 `.terminal-tab-rename`：主题色描边小输入框，跟随双主题变量。

**验证**：`npm run typecheck` 双 tsconfig 通过。运行时待开发者热更新后直接试：点页签名改字、Enter/Esc/点外部三路收尾、清空放弃、改名后切页签再切回名字保持。

## 手机端期0：打包 exe + 托盘常驻 + 开机自启

> 创建于 2026-09-12 12:38

**需求**：`新功能开发区.md`「封装成客户端 + 移动设备连接」——brainstorm 商定整体方案（桌面端 exe + 手机端 PWA + 扫码配对 + Tailscale），设计文档 `idea/手机端/2026-09-12-手机端-design.md` 分五期实施；本次落地**期0**（计划 `idea/手机端/2026-09-12-手机端-期0-plan.md`，当前会话 inline 执行）。

**实施**（6 任务）：

- **图标资产**：devDep `png2icons` + `scripts/gen-icon.cjs`（resources/app.png → build/icon.ico，npm run icon 可重生成；源图 541×537 近正方，拉伸 <1%）。实际产出 32KB（PNG 压缩嵌入，小于计划预估属正常）。
- **设置层**：SettingsKeys 增 `launch_on_boot`（'1'/'0')与 `close_action`（'tray'/'exit'，缺省 tray）；IPC `app:setLaunchOnBoot`（写 settings + `app.setLoginItemSettings` 即时生效 + 托盘菜单勾选态刷新）；preload/api.d.ts 增 `app.setLaunchOnBoot`。
- **托盘服务**（`electron/services/tray.ts` 新建）：左键单击/菜单「显示主窗口」唤回；菜单含「开机自启」勾选项（读 settings 实时态，托盘与个人档两入口同口径互刷）与「退出」。
- **main.ts 集成**：① **userData 钉回** `%APPDATA%\bugzi_workspace`（打包态 productName 变化会使默认 userData 分裂、已有数据「消失」——置于单实例锁与 data_home.json 指针读取之前，关键陷阱）；② 关闭拦截 `win.on('close')`：非退出态 `preventDefault + hide`（settings close_action='exit' 时放行），`quitting` 标志在 before-quit 置位放行托盘「退出」；③ 启动时 `createTray` + 按 settings 应用 openAtLogin（键缺省=关）；④ second-instance 唤回补 `w.show()`（托盘隐藏态二次启动须重新显示）；⑤ 顺带删除文件头无作用的 esm no-op 块。
- **个人档**：新增「启动与窗口」zone——开机自启 checkbox（即时生效+toast）+ 关闭按钮行为下拉（最小化到托盘「推荐，手机端可连」/直接退出）。
- **打包**：`electron-builder.yml`（NSIS x64、桌面快捷方式、可改安装目录、asarUnpack node-pty、**npmRebuild: false**）+ npm scripts `icon`/`dist`。

**出包踩坑三连**（均已解决）：① electron 必须在 devDependencies（electron-builder 硬校验，已从 dependencies 迁移并同步 lockfile）；② @electron/rebuild 对 node-pty 源码重编必挂（winpty gyp），`npmRebuild: false` 跳过——node-pty 1.1.0 自带 N-API 预编译无需重编；③ `.npmrc` 的 electron_builder_binaries_mirror 路径缺 `-binaries` 导致 nsis-3.0.4.1 工具链 404，修正为 `https://npmmirror.com/mirrors/electron-builder-binaries/`。另中途一次 ENOENT 陈旧 chunk 引用，`rm -rf out release` 干净重建即愈。

**验证**：`npm run typecheck` 双 tsconfig 通过（Task3/Task4 后各跑一次）；`release/bugzi-workspace-Setup-1.0.0.exe` 产出（290MB；win-unpacked 内 node-pty win32-x64 预编译正确落位 app.asar.unpacked）。体积偏大系 typescript/vite/@types 等构建期依赖仍在 dependencies 被一并打包，后续可瘦身（预计 ~100MB），本次按计划保守不动。

**待开发者冒烟**（计划 Task6 清单）：双击安装（可改目录）→ 桌面快捷方式；**数据连续性**（学习库/记账本数据与 dev 一致 = userData 钉回生效，若全空立即回报）；点 × 隐藏到托盘、托盘左键唤回、托盘「退出」进程真结束（任务管理器无残留）；个人档开开机自启 → 任务管理器「启动」页出现条目、托盘勾选态同步；运行中再双击快捷方式 = 单实例唤回；打包版内 Ctrl+J 终端 `echo ok` 回显（node-pty 判据）；关闭行为切换两态各验一次。

### 冒烟修复：托盘图标空白（260912 15:16 补记）

> 创建于 2026-09-12 15:16

**症状**（开发者冒烟反馈）：除托盘外全部通过——托盘图标没加载出来（空白）、点击无效。

**根因**：`main.ts` 的 `appIcon` 打包态指向 `<resources>/app.png`，但 electron-builder 配置从未把 app.png 复制进包（原注释「预留 extraResources」一直没接上）；`new Tray(不存在路径)` 得到空图像 → 图标空白、点击无从谈起。dev 正常是因路径指向项目内真实文件。

**修复**：`electron-builder.yml` 补 `extraResources: [{ from: resources/app.png, to: app.png }]`，重出包验证 `win-unpacked/resources/app.png` 落位。

**顺带定规则**：出包第二次撞上 `out/renderer/assets/*.js` ENOENT（每次缺失 chunk 哈希还不同）——实锤是 **dev 实例（electron-vite dev）与出包并发**：dev 的 watcher 持续重写同一 `out/` 且哈希与正式构建不同，electron-builder 扫描撞上中间态。规则：**`npm run dist` 前必须先退 dev**（此前一次成功纯属窗口期运气）。

## 手机端期1：Web 服务骨架 + 扫码配对 + 手机壳

> 创建于 2026-09-12 16:32

**需求**：期0 冒烟全过后开工期1（计划 `idea/手机端/2026-09-12-手机端-期1-plan.md`，inline 执行）——主进程内嵌 HTTP 服务、Bearer 令牌鉴权、个人档扫码配对、vite 多入口手机壳（本期只做壳，业务场景期2-4）。

**实施**（8 任务全链路）：

- **remoteServer**（`electron/services/remoteServer.ts` 新建）：裸 node:http 零新依赖；静态 serve `out/renderer/`（`/` 固定到 `mobile/index.html`，路径穿越 normalize+前缀守卫 403）；`GET /api/auth/verify` 校验 Bearer；令牌 `remote_token` 32hex 首启自动生成（重置=置空，下次取用自动重生成，单一代码路径）；端口 8787 被占自动 +1 重试最多 10 个并回写 settings；`startRemoteServer/stopRemoteServer/getRemoteStatus/setRemoteEnabled` 四导出。
- **remotePairing**（新建）：`candidateIps()` IPv4 三档排序——Tailscale（100.64-100.127 段）> 私网（192.168/10/172.16-31）> 其他；`buildPairingInfo()` 组装 `http://<ip>:<port>/#token=<令牌>`（令牌走 URL fragment 不进服务端/日志）并经 qrcode 库渲染 280px dataURL；devDep `@types/qrcode`，`qrcode` 进 dependencies 由主进程 bundle 内联。
- **IPC/preload/api.d.ts**：`remote:status/setEnabled/pairing/resetToken` 四通道镜像三处同步。
- **main.ts**：whenReady 内 `startRemoteServer()`（settings enabled='0' 则不开，缺省随启）；before-quit `stopRemoteServer()`。
- **个人档「手机端」zone**：Web 服务开关（checkbox + 运行态端口文案 + toast）、出示配对二维码（内嵌面板：280px 二维码 + 完整 URL + 多候选地址兜底文案）、重置令牌（ConfirmDialog danger 二次确认 → 已配对设备全失效）。
- **手机壳**（`src/mobile/` 五文件 + `src/public/` 两静态）：electron-vite renderer 改多入口（main + mobile），产物 `out/renderer/mobile/index.html` 引根级共享 assets（壳 js 仅 7.2KB，零桌面重依赖）；`apiClient.ts`（#token 首访捕获转存 localStorage 并抹地址栏、401/网络错误分型）；`App.tsx` 四态机（loading/pairing/offline/ready）+ 底部三 tab（记录|信息源|知识，tab 页为「期X实装」占位卡）+ 主题切换（localStorage 独立偏好 + prefers-color-scheme 初值，图标用 material-symbols 非 emoji）；`mobile.css` 复用 global.css 变量（无 --color-card 用 --color-surface 系；body 底色自设补 .app-bg 缺位）；manifest + mobile-icon.png 走 src/public/。
- **Tailscale 指南**：`idea/手机端/tailscale-配置指南.md`（注册/双端安装/100.x 固定地址/防火墙放行命令/常见问题表/安全边界）。

**验证**：typecheck 双配置通过、build 通过（mobile 入口产物齐全）；dev 内 curl 六连——无令牌 401、错误令牌 401、正确令牌 `{"ok":true,...}`、`/` 出壳 HTML、manifest/图标 200、`/../` 穿越被 403 拦截；壳 HTML 的 `../assets/*` 相对引用在 `/` 与 `/mobile/` 两入口下浏览器归一化均可达。

**待开发者冒烟**：① 个人档「手机端」zone——开关切换 + 运行态文案、出示二维码有图、手机（同 WiFi）扫码打开壳、手输令牌配对路径；② 壳内三 tab 切换、右上主题切换双主题正确、断服务后刷新出配对/离线页、重置令牌后旧设备回配对页；③ 加主屏幕（iOS 分享→添加 / Android 菜单）；④ Tailscale 装好后户外流量复测（指南见 `idea/手机端/tailscale-配置指南.md`）；⑤ 打包验证记得先退 dev，新 exe 内重复 ①②。

## 手机端期1 撤销：远程连接功能整体移除

> 创建于 2026-09-12 18:51

**开发者指令**：Tailscale 本机无法 login，手机连接暂不需要——期1（远程连接）整体撤销；期0（打包/托盘常驻/开机自启）为独立桌面功能，**保留不动**。上节期1 记录留作历史，以下述移除为准。

**删除的文件**：`electron/services/remoteServer.ts`、`electron/services/remotePairing.ts`、`src/mobile/`（壳五文件）、`src/public/manifest.webmanifest` 与 `src/public/mobile-icon.png`（仅手机壳引用）、计划文档 `docs/project/idea/手机端/` 整目录（design/期0/期1 plan/Tailscale 指南四份）。

**回退的接线**：`main.ts`（startRemoteServer 随启 / before-quit stopRemoteServer / import）；`ipc.ts`（remote 四通道 + 两 import）；`preload.ts` 与 `api.d.ts`（remote 组四方法）；`ProfileModule.tsx`（「手机端」zone 整块 + toggleRemote/showPairing/resetToken/四个 state + 重置令牌 ConfirmDialog；「启动与窗口」zone 文案去掉「手机端可连」字样）；`electron.vite.config.ts`（renderer 多入口恢复单入口——注意 electron-vite 要求 `build.rollupOptions.input` 显式存在，删空会报错）；`src/shared/types.ts`（SettingsKeys 删 `RemoteEnabled/RemotePort/RemoteToken` 三键）；`package.json` 卸载 `qrcode` + `@types/qrcode`（npm uninstall 同步 lock）。

**验证**：typecheck 双配置通过、build 通过（产物仅桌面单入口）；全库 grep 无 remoteServer/remotePairing/api.remote/pairing/manifest.webmanifest/mobile 残留。settings 表可能遗留 `remote_*` 三行旧数据，无代码读取，无迁移必要。

## 学习库 v2.0 优化区：未生成阴影态 + 待加载队列（3 并行线）+ 主题折叠

> 创建于 2026-09-12 11:52

**需求**：`docs/project/左侧边栏/学习库/学习库v2.0计划.md` 优化区三条——①生成树后知识点未生成内容无区分且首点要等 30-60s；②有卡在加载时不能点其他卡，要建待加载队列（2-3 条并行线）；③树的主题要可展开收起方便定位。

**实施**（`LearnModule.tsx` + `App.css` + `electron/ai/services.ts` 三文件）：

- **①阴影态**：`App.css` 新增 `.row-item.row-pending`（opacity 0.45 + 左缘 3px 内阴影条），`.row-item` 补 opacity 过渡；`content_ready=0` 的行（知识树知识点 + 今日新学，复习卡恒为已生成不受影响）挂该类，就绪后移除即恢复常态——「阴影=点开需先生成，常态=可秒开」。
- **②待加载队列**：点击未生成卡不再弹窗阻塞等待，而是**入队后台生成**——`genStates` 记行内态（排队中 `hourglass_top` 静置 / 生成中 `progress_activity` 旋转），`pumpGenQueue` 按入队顺序起 **GEN_LANES=3 条并行线**调 `learn:getCard`；就绪 `markCardReady` 行内直改 `content_ready=1`（免整树重拉），行自动去阴影，再点即秒开；失败移出队列 toast 可再点重试、LLM 未配置走全局「去配置」弹窗；已入队的行再点忽略。原 `openingId`「单卡阻塞全模块」机制删除。**后端配套**：`generateLearnCard` 拆 `doGenerateLearnCard` 加同节点在飞去重 Map（渲染层 3 线 + 预生成泵 + 现场生成可能同卡并发，共享同一 Promise 防重复烧 LLM 调用；学习卡生成现有 UI 无取消入口，附随沿用首调 signal 可接受）。
- **③主题折叠**：知识树主题 zone-header 可点击展开/收起（`chevron_right`/`expand_more` 图标指示 + title 提示），`collapsedTopics` Set 会话内记忆（跨领域切换保持）；行内操作钮 stopPropagation 不受影响，收起时隐藏 zone-body。
- **附带行为变化**：卡片弹窗打开时点树上其他未生成卡照常入队（原被 `card != null` 守卫拦死）；「随机来一条」未改动（优先秒开池口径不变）。

**验证**：`npm run typecheck` 双 tsconfig 通过。**待冒烟**（需 LLM 配置）：建树后知识点应全阴影 → 点 4+ 张卡前 3 张转生成中、其余排队 → 逐张去阴影 → 再点秒开；中途关弹窗/切模块生成不断；失败卡 toast 重试；主题折叠展开与进度计数显示；今日新学行同款阴影与队列行为。升级区（机制讲解 + 升级商讨）另行与开发者对话进行，未动代码。

## 学习库 v2.0 升级区：机制讲解 + 三件套设计商定

> 创建于 2026-09-12 12:09

**过程**（按 `学习库v2.0计划.md` 升级区要求，优化区完成后进行；brainstorming 对话三轮）：

- **机制讲解**：应开发者「今日新学那三条是怎么来的」之问，讲清 v1.0 机制——建树懒触发只存骨架；每日队列当日首触发定档（张数随机 3-5 + 领域洗牌轮转均摊 + 领域内随机，定档当天不变）；复习到期升序取前 10；泵后台补正文。结论：「学什么」= 树骨架 + 随机轮转，**系统无先修顺序概念**——即单调感来源之一。
- **痛点澄清**：四缺失维度（结构/检验/深度/动力）中开发者选中**检验感 + 深度感**；四形态中选**每日小测 + 实战任务（明确要求小而实用）+ 深挖模式**，复习改主动回忆不做。
- **方案拍板**：A「三件套长在现有骨架」（小测在今日学习页、任务在主题行、深挖在卡片弹窗）＞ B 独立练习场 ＞ C 最小版；开发者选 A，设计四节（小测/任务/深挖/数据接口增量）当场通过。

**产出**：设计记录 `docs/project/左侧边栏/学习库/2026-09-12-学习库v2.0升级-design.md`（DB v34 两表 learn_quiz/learn_tasks、IPC 9 通道、4 新 prompt、答题实时存库、任务 30 分钟硬约束、深挖零新存储自动发右栏学习·问答频道；YAGNI 清单含错题本/积分统计/深挖回写等）。**实施未开始**——待开发者审阅设计记录后出实施计划再动码。

## 学习库 v2.0 三件套实施（每日小测 + 实战任务 + 深挖）

> 创建于 2026-09-12 12:29

**过程**：开发者审阅设计记录后拍板「可以的继续」→ writing-plans 出实施计划（`2026-09-12-学习库v2.0升级-plan.md`，9 任务全码预写 + 自审修正两处内部不一致）→ executing-plans 内联顺序执行。

**实施**（9 任务，与计划一一对应）：

- **DB v35**（`electron/db/db.ts`）：`learn_quiz`（date 主键一天一卷，questions/answers 存 JSON，answering|graded）+ `learn_tasks`（topic_id 挂主题，todo|submitted|reviewed，任务/作业/点评三 md 路径）+ `idx_learn_tasks_topic` 索引；initDb 目录清单加 `md/learn/task`。**版本偏差**：设计原写 v34，被信息源收藏占用，按「撞车顺延」条款落 v35（设计文档已回写）。
- **AI 层 4 函数**（`electron/ai/services.ts`）：`generateLearnQuiz`（读今日已学卡正文每卡 1 题，choice/blank/short 混合，jsonMode + 解析失败重试一次，无效题过滤）；`gradeLearnQuiz`（简答批量判，意思对即可）；`generateLearnTask`（30 分钟硬约束三段式任务 md，已学卡正文节选入上下文）；`reviewLearnTask`（验收标准逐条核对，0-100 评分 + 亮点/不足/改进建议点评 md）。
- **IPC 9 通道**（`electron/ipc.ts`）：quiz 五通道——`quizGet`（answering 态隐去 answerIndex/acceptable/answer/analysis 防抄答案）/ `quizCreate`（一天一卷幂等，`force=true` 换一张且生成成功才 DELETE 覆盖）/ `quizAnswer`（实时存库；choice 本地判、blank 归一化宽松比对——去空格+全角转半角+小写）/ `quizSubmit`（缺答拦截、简答批量批改、整卷 graded）/ `quizRetry`；task 四通道——`taskList`/`taskGenerate`/`taskSubmit`（作业先落盘再批改，reviewed 拒绝重复批）/ `taskDelete`（级联三 md 彻底删，不入回收站）。**`learn:topicDelete` 加任务级联彻底删**。
- **渲染层**：`LearnQuizZone.tsx` 新建（zone 自包含：未出卷空态/答题态/成绩态三视图；choice 选项按钮、blank/textarea 失焦保存；graded 展对错图标+你的答案+正确答案+AI 点评+解析；重做二次确认；答错节点经 `onWrongChange` 上抛）；`LearnTaskDialog.tsx` 新建（任务下拉切换 + AI 出新任务[有未点评任务先确认] + 作业 textarea + 提交点评 + 点评展示 + 彻底删确认；生成/批改中禁误关遮罩）；`LearnModule.tsx` 接线（`wrongNodes` 行内 error 标记、`learnedToday` 可测门槛、主题行 construction 按钮、`LearnQuizZone` 挂今日学习页尾）；`MdDialog.tsx` studyBar 扩展可选 `onDig`（操作条左侧「深挖」钮，其余模块零影响）；`App.css` 小测样式组（quiz-option/quiz-wrong-mark/quiz-wrong-icon/quiz-right-icon，全主题变量）。
- **深挖**：LearnModule `onDig` 读卡文组装四角度深挖 prompt，`onOpenAi(pre, { auto: true })` 经 CHANNEL_BY_MODULE 自动发右栏「学习·问答」频道，零新存储。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过（1.32s）。设计文档两处偏差已回写（v35、按钮落位 zone 头部）。**待开发者冒烟**（需重启 dev + LLM 已配置）：①学 2+ 张卡 → 出卷答题（切模块回来自动恢复已答）→ 交卷判分与答错行标记 → 重做/换一张；②主题出任务 → 写作业提交 → 点评评分 → 下拉切换/删除；③有未点评任务时出新任务出确认；④删带任务的主题任务级联清除；⑤卡片弹窗「深挖」自动发右栏学习·问答且带卡文；⑥LLM 未配置三入口均「去配置」；⑦今日学习/知识树/高光笔记与上午优化区（阴影队列/主题折叠）回归。git 提交由开发者手动完成。

## 学习库追加优化：生成队列可视化 + 逐项取消

> 创建于 2026-09-12 14:21

**需求**（开发者冒烟前反馈，紧接上午优化区②）：加载队列黑盒——希望可视化看到队列内容；且误触入队无法反悔——排队中的要能移出队列，已在生成的也要能取消。

**实施**（仅 `LearnModule.tsx`，零后端改动——取消复用既有 `beginJob`/`ai.cancel` 任务取消机制）：

- **状态镜像**：新增 `genQueueList`（渲染用队列镜像，排队+生成中按入队序）与 `genJobsRef`（Map<节点id, jobId>，泵起任务时登记、finally 注销）；`genQueueRef` 仍为泵消费的操作队列，两者在入队/出队/取消三个变更点同步。
- **取消双路**（`cancelGen`）：按 `genJobsRef` 是否有 jobId 判相位——**排队中**直接从 ref/镜像/genStates 三处移除（行恢复阴影，可再点重新入队）；**生成中**调 `window.api.ai.cancel(jobId)` 中止 LLM 调用，Promise 拒绝后由泵 finally 统一清理（`已取消` 错误不再弹「生成失败」toast，仅取消时提示「已取消生成」）。
- **可视化**：模块头部右侧「生成队列」按钮（队列空即隐藏），badge 显示「生成中 X · 排队 Y」；点击弹出固定定位面板（ctx-menu 样式，外点/透明遮罩关闭），逐项列状态图标（沙漏=排队中/旋转=生成中）+ 标题 + 相位文案 + × 取消钮；队列清空面板自动收起。

**边界**：取消生成中的卡若正被预生成泵共享（同节点在飞去重），取消会连带中止该次共享生成，泵侧 catch 记 warn 后下次触发补齐，无副作用；面板仅在队列非空时出现，模块头部入口跨「今日学习/知识树」两 tab 常驻可见。

**验证**：`npm run typecheck` 双 tsconfig 通过。**待冒烟**：连点 5+ 张未生成卡 → 头部出现「生成队列 生成中 3 · 排队 N」→ 点开面板逐项可见 → 取消一张排队中（行回阴影、队列数减）→ 取消一张生成中（spinner 停、行回阴影）→ 全部完成/取消后头部按钮消失；误触入队后立即取消不再消耗 LLM 额度。

## AI 活动指示器升级：全局调用面板（运行中/排队中/取消）

> 创建于 2026-09-12 15:12

**需求**（开发者提出）：右下角 AI 活动指示器扩展为可视化队列——收起态保持现状（AI ×N），点击展开面板详查当前 AI 调用（哪个模块、运行还是等待），可暂停/取消任务，面板有「收起」钮。brainstorm 三问拍板：**合并学习库排队项**进全局面板；暂停语义 = **取消即可**（单任务 + 全部取消，不做暂停开关）；**后台泵也可取消**（取消 = 跳过当前张/批，泵继续）。

**实施**（主进程 5 文件 + 渲染层 4 文件，零 DB 改动、零新 IPC——取消全走既有 `ai:cancel`）：

- **主进程广播增强**（`electron/ai/llm.ts`）：在途登记加 `signal + startedAt`；`llm:activity` payload 扩展为 `{seq, scene, configName, startedAt, jobId}`——jobId 经 `jobs.ts` 新增 `findJobIdBySignal`（signal 反查，任务表遍历开销可忽略）自动关联，未传 signal 的调用 jobId=null 即面板不可取消。`jobs.ts` 另加 `runPumpJob(label, fn)` 泵任务包装助手。
- **三泵可取消**：learnStock / wikiStock（后库+每日批次共 3 处）/ reasoningStock（补汤+题池）的 LLM 调用包上 `runPumpJob`——面板可见、可取消（中止信号贯穿 chatCompletion，泵侧既有 catch 继续下一项）。
- **学习队列全局化**（`src/services/learnGenQueue.ts` 新建）：队列状态自 LearnModule 提取为渲染层单例 store（useSyncExternalStore）——队列/等待两份表 + GEN_LANES=3 泵逻辑随迁；`enqueueLearnGen/cancelLearnGen/cancelAllLearnGen` + 就绪/失败回调由 LearnModule 注册（行内回写与 toast 口径留在模块层）。LearnModule 删本地队列五件套改消费 store，行相位/队列面板数据同源。
- **LlmActivity 双态改造**（`src/components/LlmActivity.tsx`）：收起态显示条件扩为「运行中>0 或 排队>0」（纯排队时图标不转、显示「队列 N」）；点击展开右下角浮层面板（340px，锚定按钮堆上方）——**运行中**列表（场景标签=模块·动作 / 配置名 / 已运行秒数每秒刷新 / 逐项取消，无 jobId 显示「—」title 后台不可取消）+ **排队中**列表（学习库等待项+移出钮；生成中的已在运行中体现不重复展示）+ 头部「全部取消」（可取消调用逐个 ai.cancel + 队列清空）与「收起」钮（图标/遮罩点击同效）。CSS 补 `.llm-panel*` 组与 `.llm-activity.idle`。
- **顺带**：LLM_SCENE_LABELS 补上午新增四场景（learn:quiz/quizGrade/task/taskReview）；shared/types 加 `LlmActivityItem`；preload/api.d.ts 的 onActivity payload 同步扩展。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过（1.96s）。**待冒烟**（需重启 dev——主进程改动热更新不生效）：①学习库点未生成卡 → 右下角出现指示器 → 展开面板见排队项 → 取消；②生成中展开面板见「学习库·知识卡 + 配置名 + 时长」且时长跳动 → 逐项取消 / 全部取消；③右栏对话生成时面板出现 debugzi·对话；④预生成泵触发（进万象库/推理角）面板出现后台调用且可取消跳过；⑤纯排队无运行时收起图标显示「队列 N」不旋转；⑥学习库模块头部的队列面板与新全局面板并存，两边取消互相同步；⑦回归：三右栏面板收起态才见指示器的旧规则不变。

## 致知己·预言家与十二问题（模块三 tab 化）

> 创建于 2026-09-12 18:36

**需求**（`docs/project/新功能开发区.md` 待完成区第 2/3 条）：①致知己新「预言家」标签页——输入预测/推断，AI 联网搜索给充分全面的说明，多轮对话可质疑，最后用户下判断（判断后与沉淀区联动本期不做）；②「十二问题」标签页——把自己的想法写下来记入思维库（商定：费曼式自定义 ≤12 题 + 每题多条带日期想法流 + 可选问 AI）。设计/计划见 `左侧边栏/致知己/2026-09-12-预言家与十二问题-design.md` 与同日 plan（三 tab 定名：沉淀｜预言家｜十二问题；预言家对话走频道制方案 A）。

**实施**（主进程 4 文件 + 渲染层 7 文件，DB v35 → v36）：

- **频道基建**（零 DB 迁移）：`AiChannel` 加 `prophet`（types/api.d.ts 双处）+ settings 键 `AiActiveSessionProphet`；services.ts 补 ACTIVE_SESSION_KEYS 映射、「预言分析师」频道人设（基于资料充分说明欢迎质疑、绝不代下结论）与 `appendAssistantToChannelSession`（分析说明以 assistant 消息入频道会话，作多轮对话起点）；AiSidebar 频道栏加「致知己·预言家」。
- **DB v36 三表**：`prophet_records`（claim/note/status/judgment 三选一/judgment_note/judged_at/analysis_md_path，判断可改判覆盖）、`twelve_questions`（title/ord，≤12 应用层限+服务端 TWELVE_FULL 兜底）、`twelve_thoughts`（碎片无软删列——单条想法彻底删、删题随题走题目级回收）。
- **预言家分析**（`services.ts runProphetAnalysis`，骨架同辩真 `runVerification`）：`findSearchTool` → LLM 出 3 组关键词 → 逐组 MCP 检索 → LLM 综合六节「充分全面的说明」（观点重述/支持依据/反对与风险/推演逻辑链/不确定性/仅供参考的结论倾向）→ md 快照 `md/prophet/<id>.md` 落条目 + assistant 消息推「致知己·预言家」频道；进度经 `pushAiSystemMessage(msg, 'prophet')` 推频道（同辩真核查频道模式）；新 IPC prophet:list/create/analyze/judge/discard。
- **十二问题 IPC**：twelve:list（子查询聚合 thought_count/last_thought_at）/createQuestion/renameQuestion/thoughts（倒序）/addThought/deleteThought/discardQuestion。
- **回收站**：RecycleSource 加 `prophet`（MD_FIELDS=analysis_md_path，走默认删行+删 md）/`twelve_question`（hardDelete 特判删想法行+题行）；restore 两分支（清标记，prophet 触碰 updated_at 浮顶）；RecycleModule tabOf 归致知己块 + backToOf/srcTag/summaryOf 三处文案。
- **渲染层**：ZhijijiModule 三 tab 化（`recycle-tabs` 三键，默认沉淀，选择不持久化；沉淀区原内容与全部弹窗原样包进 `tab==='core'`，切走时复位 viewQ/adding；头部「新问题」钮仅沉淀 tab 显示；App.tsx 传 onOpenAi/bumpAi）。ProphetPanel（列表+状态徽标 zj-status、新预言弹窗创建即自动分析、MdDialog 详情渲染 md 快照——无快照给占位文案、判断区走 MdDialog 新增通用 `footerBar` 槽（非编辑态渲染，同 learnBar/studyBar 位置）：理由输入+合理/不合理/存疑即点即判+重新分析+回到频道；ai.onMessage→bumpAi 同 VerifyPanel）。TwelvePanel（题目卡片行+想法条数、点开想法流倒序带时间戳 MdView 渲染、底部 textarea Enter 保存、单条删除二次确认彻底删、「问 AI」带题目+最近 5 条想法 autoAsk 进追问频道、12 题满按钮置灰）。CSS 补 zj-status/zj-judge/zj-twelve-thoughts 组。

**验证**：`npm run typecheck` 双 tsconfig 通过（中途清了过期 tsbuildinfo 增量缓存——composite 工程改 .d.ts 后缓存未失效致假报错）。**待冒烟**（需重启 dev，主进程改动含 DB v36 迁移）：预言家链路（新预言→右栏自动切「致知己·预言家」频道看进度→说明入频道→频道质疑 ≥2 轮→详情三选一判断→改判→重新分析→删除进回收站致知己块「预言 ·」→恢复）；十二问题链路（建 3 题→第 13 题被拒→记想法倒序→删想法→问 AI 自动进追问频道→删题进回收站→恢复想法还原）；失败路径（未配 LLM 点问 AI/新预言、未配 MCP 分析→去配置弹窗不崩溃）；回归（沉淀 tab 行为如旧、右栏七频道会话互不串、回收站致知己块三种来源混排）。

## 万象库·知识问答标签页

> 创建于 2026-09-12 20:13

**需求**（`docs/project/新功能开发区.md` 待完成区）：万象库再加「问答」标签页——致知己十二问题放偏思考的生活问题，这里放偏科普的知识问题（如「我很好奇流量价格是怎么降下来的」），问 AI、AI 搜索回答。设计/计划见 `左侧边栏/万象库/2026-09-12-知识问答标签页-design.md` 与同日 plan（brainstorming 商定：单轮问答存档、安静模式、强制 MCP 搜索、不联动百科词条、独立新表照搬辩真模式）。

**实施**（主进程 5 文件 + 渲染层 5 文件，DB v36 → v37）：

- **DB v37**：`qa_records`（question/answer/md_path/created_at/deleted_at，无可信度字段——问答不是核查）。
- **后端**（`services.ts runQaAnswer`，骨架同辩真 `runVerification`）：`findSearchTool`（无搜索工具即抛错，不降级纯 LLM）→ LLM 出 3 组中英文关键词（scene `qa:ask`）→ 逐组 MCP 检索（单组失败跳过）→ LLM 综合回答（简体中文 Markdown、通俗讲清来龙去脉、来源链接列出、不注入画像）；md 快照 `md/qa/<id>.md`。**安静模式**：进度仅 console 记录，不接 `pushAiSystemMessage`，右栏不自动展开。
- **IPC/preload**：qa:list/get/run（beginJob 可取消）/discard（复用通用 `item:discard`，RECYCLE_MAP 加 `qa_records → 'qa'`）；api.d.ts 补 `QaRecord` 与 qa 段。
- **回收站**：RecycleSource 加 `qa`（TABLES/MD_FIELDS=md_path 走默认删行+删 md）；restore 分支仅清标记；RecycleModule tabOf 归万象库块 + backToOf「万象库·问答历史」/ srcTag「问答 · 」/ summaryOf 取 question。
- **渲染层**：新 `QaPanel.tsx`（输入区 Ctrl+Enter 提问 + 提问中/取消 + 问答历史倒序列表 + MdDialog 详情——划词仅「问 AI」进本模块默认「万象·问答」频道、允许重复提问不查重、删除二次确认入回收站）；WikiModule tab 三化 `wiki | verify | qa`（keep-alive 三键：百科｜辩真｜问答）；MdDialog `selectionActions.onHighlight` 改可选、气泡高光按钮条件渲染（问答无词条载体，百科卡片双按钮行为不变）。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过。**待冒烟**（需重启 dev——主进程改动含 DB v37 迁移）：问答链路（提问 → 安静完成 toast → 列表回看 → MdDialog 双击编辑 → 划词问 AI 落「万象·问答」频道 → 删除 → 回收站万象库块「问答 · 」前缀 → 恢复）；失败路径（LLM 未配置/MCP 未启用分别弹去配置、全部检索失败弹失败窗）；进行中取消不落半截记录；切 tab 不打断进行中任务；回归（辩真/百科两 tab 行为如旧、MdDialog 划词高光百科卡片仍双按钮）。

## 优化建议区第40轮①：学习库深挖结果确认后写入知识卡

> 创建于 2026-09-12 20:24

**需求**（`docs/project/优化建议区.md` 待完成区）：深挖不仅在右侧边栏显示，还要直接加到当前知识卡中。brainstorming 两决策定稿——**确认后写入**（生成完先展示，点「写入卡片」才落库，可重新生成/放弃，不满意不污染卡片）+ **日期小节累加**（卡片 md 末尾追加 `## 深挖（YYMMDD）` 二级标题 + AI 内容，多次累加各成一节，写入后可随意编辑删改）；「深挖」不再自动发右栏「学习·问答」频道（入卡成为主路径，追问走手动问 AI）。设计/计划见 `左侧边栏/学习库/2026-09-12-学习库深挖入卡-design.md` 与同日 plan。

**实施**（主进程 3 文件 + 渲染层 3 文件，零 DB 迁移）：

- **services.ts `digLearnCard(nodeId)`**：四角度模板（原理再进一层/误解易错点/知识串联/实际坑点，文案与原前端模板逐字一致）从 LearnModule 前端拼接迁入主进程，读 `md/learn/<id>.md` 卡文注入，scene `learn:dig`，stripMdFence 后返回。
- **IPC 两通道**：`learn:dig(jobId, nodeId)`（beginJob 可取消，返回 `{content}`）+ `learn:digApply(nodeId, content)`（**原子写入收敛主进程单点**：读全文 → 末尾追加 `\n\n## 深挖（yyMMdd）\n\n` + 内容 → 写回，返回更新后全文，防渲染层读改写竞态）；preload/api.d.ts 透传。
- **MdDialog 通用扩展**：studyBar 加可选 `digPanel?: ReactNode`，非编辑态渲染于学习操作条上方（`.dialog-footer.learn-dig-panel`），其他模块调用方不传零影响。
- **LearnModule**：`onDig` 重写（调 learn:dig，dig 状态机 loading/done + jobId 取消 + handleAiError 三分支）；结果区 = 生成中（loading+取消）/完成（MdView 预览 + 放弃/重新生成/写入卡片）；`onDigApply` 写入成功 toast「已写入卡片」+ bump `mdVersion`；**MdDialog key 扩为 `${card.id}-${mdVersion}`** 供写入后重挂重读；关闭弹窗清 dig 态。App.css 补 `.learn-dig-panel`（纵向拉伸）与 `.learn-dig-md`（max-height 300px 内部滚动 + 主题边框底色）。

**验证**：`npm run typecheck` 双 tsconfig + `npm run build` 通过。**待冒烟**：深挖 → 取消 → 重试 → 写入卡片（弹窗刷新见日期小节）→ 再次深挖累加第二节 → 卡片编辑态删改深挖内容 → 其他模块 MdDialog 零影响 → LLM 未配置提示 → 双主题目检。

## 优化建议区第40轮②：致知己沉淀区问题分级（星级+来源）与 AI 出题

> 创建于 2026-09-12 20:24

**需求**（`docs/project/优化建议区.md` 待完成区）：致知己问题来源分级——按评判标准打星级，出处分手动输入/AI 生成两类（AI 出题为新增能力）。brainstorming 四决策定稿——**范围**先沉淀区（字段设计预留十二问题平移）；**AI 出题**候选勾选采纳（5 条候选带星评语 → 弹窗勾选 → 采纳才入库标 ai，灵感泉/万象库审核范式）；**星级**AI 初评 + 用户可调（手动创建也评）；**评级体系**1–5 星 + 四维标准（开放性/自我认知价值/可演化性/具体度）+ AI 一句评语悬停可见。设计/计划见 `左侧边栏/致知己/2026-09-12-问题分级与AI出题-design.md` 与同日 plan。

**实施**（主进程 3 文件 + 渲染层 3 文件，**DB v38**——设计时写 v37 起算，v37 已被同日并行会话万象库问答占用，按撞车顺延条款用 v38）：

- **DB v38**：zhijiji_questions 加 `origin`（TEXT DEFAULT 'manual'）/`stars`（INTEGER 可空）/`star_note`（TEXT）三列，存量行默认值兜底零回填，stars 保持 NULL 由「AI 补评」按需补。
- **services.ts 两 prompt**：`suggestZhijiQuestions`（画像 digest + 已有问题清单避重 + 高星问题特征 → 5 候选出题带星一体，scene `zhijiji:suggest`，jsonMode，stars clamp 1-5 容错）+ `rateZhijiQuestions`（四维标准批量评星按 index 对齐，scene `zhijiji:rate`，单题复用传长度 1 数组）。
- **IPC 五通道**：`zhijiji:suggestQuestions(jobId)` / `adoptQuestions(candidates)`（批量入库 origin='ai' + 评星落库 + 各开空白 v1）/ `rateQuestion(jobId, id)`（**仅写 `WHERE stars IS NULL` 行**——防异步评星覆盖用户已改星，已被手改返回 null）/ `ratePending(jobId)`（全部未评星题一次调用批量评完）/ `setStars(id, stars|null)`；zhijiji:list SELECT 扩三列；preload/api.d.ts（含 api.d.ts 内独立 ZhijijiQuestion 同步扩列 + 新 `ZhijijiQuestionCandidate`，shared/types.ts 同款）。
- **渲染层 ZhijijiModule**：头部「AI 出题」（出题中禁点）+「AI 补评」（仅存在未评星题时显示）+「新问题」三按钮；列表筛选 pill 行（全部/★4+/★5/未评，recycle-tab 复用，默认全部仍按更新倒序）；问题行标题前来源小图标（person 灰 / auto_awesome 主题色）+ 行尾星级按钮（star_rate 灰显未评 / star 主题色 + 数字，悬停 title 显示评语）→ 点击弹 ActionMenu 五档直选 + 清除（复用 260911 通用菜单组件）；AI 出题候选弹窗（loading 态 + 可取消 + checkbox 勾选 + 「采纳（N）」全不勾禁用）；手动建题成功后 **fire-and-forget 异步评星**（不阻塞创建与 v1 编辑，评完静默刷新，失败静默）。App.css 补 zj-origin/zj-star/zj-star-n。

**验证**：`npm run typecheck` 双 tsconfig（修一处 node:sqlite `changes` 为 `number|bigint` 的 Number() 收敛）+ `npm run build` 通过。**待冒烟**（重启 dev 触发 v38）：存量题全 origin=manual 无星 → AI 出题 5 候选带星评语 → 勾选采纳入库标 AI → 手动建题数秒后自动出星 → AI 补评批量 → 手动改星/清除即时生效且不被异步评星覆盖 → 星级筛选四档 → 悬停评语 → 删题恢复后 origin/星级保留 → LLM 未配置去配置 → 双主题目检。

## 万象库·问答排版修复：子面板收缩居中

> 创建于 2026-09-12 20:33

**症状**（开发者冒烟反馈）：问答标签页整体排版「收缩在中间」——输入卡与历史列表挤成约半宽一列，不随模块栏撑满。

**根因**：QaPanel 根节点复用了 `.module-page`（max-width 980 + `margin: 0 auto`），但它嵌在 WikiModule 的 `.module-page`（flex 纵向容器）内部作为 flex 项——**交叉轴 auto 边距使 `align-items: stretch` 失效**，宽度退化为 fit-content，再被 auto 边距居中，即「收缩在中间」。辩真 VerifyPanel 是同一嵌套结构，同病（260908 并入时未察觉）。其余模块的 `.module-page` 都是模块根（main-area 块级上下文），居中语义正常，不受影响。

**修复**（三文件，纯渲染层）：App.css 新增 `.module-subpage`（纵向 flex + gap 14 + `width: 100%`，即 module-page 去掉 max-width/居中），QaPanel 与 VerifyPanel 根节点改用该类。排查方式：单实例锁无法起第二 app 实例（`--user-data-dir` Electron 不采纳，与内置终端轮结论一致），改用构建产物 CSS + 静态 DOM 复现页在独立浏览器截图对比修复前后。

**验证**：`npm run typecheck` 双 tsconfig 通过、`npm run build` 通过、复现页截图确认撑满。渲染层改动 dev 热更新即时生效，**待开发者即时复核**：问答/辩真两 tab 面板均与百科同宽对齐、窗口缩放跟手。
