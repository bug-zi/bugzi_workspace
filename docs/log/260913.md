# 260913 开发日志

## 个人档「版本与更新」——应用内检测与一键更新

> 创建于 2026-09-13 00:27

- **需求来源**：开发者咨询「exe 安装包想同步更新，像有些应用可以直接在应用里更新」。会话对齐三决策：路线 A（electron-updater 完整自动更新，否决轻量跳转方案）、托管 GitHub Releases（公开仓库 `bug-zi/bugzi_workspace`，匿名零 token）、自动化程度「启动静默检查 + 手动下载」。
- **设计/计划**：`docs/project/左侧边栏/个人档/2026-09-13-版本与更新-design.md` + 同目录 `-plan.md`（brainstorming → writing-plans 流程，开发者逐节批准）。
- **实施**（本会话直接执行，8 任务）：
  - 新增依赖 `electron-updater ^6.8.9`；`electron-builder.yml` 加 `publish: github`（本地 dist 不自动发布，发版走 `gh release create`；打包自动产出 `latest.yml` + `.blockmap` 支持差量更新）。
  - 新建 `electron/services/updater.ts`：状态机 idle/checking/available/downloading/downloaded/disabled（全内存零 DB 零 settings），启动后错峰 5s 静默检查（发现新版置一次性 notice + toast），`autoDownload/autoInstallOnAppQuit` 均关、一切手动；三段数字版本号自比较（不引 semver）。
  - IPC 四通道 `app:updateState/updateCheck/updateDownload/updateInstall` + 推送通道 `app:updateEvent`（ipc.ts + preload.ts + api.d.ts 三处同步）；`window.api.updater`。
  - ProfileModule「数据存储」与「LLM 配置」之间新增「版本」小节：当前版本号、五态按钮（检查更新/检查中…/下载更新/重启并安装）、下载进度条（主题色系内联样式）、发现新版本行。
- **实施期勘误两处**：① electron-updater 6.x 类型未暴露 `requestTimeout`（旧版属性），删除该行改默认超时，失败路径本有静默/回退兜底；② 托盘兼容无需新增 `isInstallingUpdate` 标志——核实 main.ts 后确认 quitAndInstall → app.quit() → before-quit（置 quitting=true）→ close 拦截天然放行，零改动兼容（已回写设计文档）。
- **验证**：typecheck 双配置 + build 通过；开发者目检冒烟通过（版本小节显示 v1.0.0、点检查更新 toast「开发模式下不可用」、双主题样式正常）。`--smoke` 脚本实际未实现（README 陈旧），目检由开发者执行。
- **待办**：Task 8 真实链路发版为开发者手动（升版本 → `npm run dist` → `gh release create` 上传 exe + blockmap + latest.yml 三件 → 端到端验收含差量更新）；首个 Release 发布前 App 内检查会失败（404），静默/toast 均属预期。
