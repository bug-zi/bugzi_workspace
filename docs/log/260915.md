# 260915 开发日志

## 学习库弹窗问 AI 拓展坞 + 格言拖拽自动滚动（优化建议区第42轮）

> 创建于 2026-09-15 22:08

- **需求来源**：优化建议区待完成区两条——①学习库划词问 AI 应在弹窗右侧直接拓展坞显示对话交互，而不是退出弹窗回右栏查看；②格言库条目已多，拖拽格言条到可视区顶部/底部时应自动缓缓上/下滚动。
- **①学习库问 AI 拓展坞**：
  - 方案即致知己追问栏（第13/14轮）同款交互，需求指明方向明确，未开 brainstorming。
  - **通用件抽取**：ZhijijiModule 内嵌 ZhijijiChat 组件（约 420 行）泛化为共享组件 `src/components/ChannelChatPanel.tsx`——channel、激活会话 settings 键、标题、头部图标、输入框占位、空态文案、协议剥离正则（致知己画像标记）全部参数化；/clear、/compact、会话切换/改名/删除（二次确认）、左缘拖宽、收起细条（ChannelChatRail）全保留。致知己改用共享组件，行为零变化；MdDialog.css `zj-chat*` 样式类更名 `dlg-chat*`（唯一使用方随之迁移）。
  - **学习库接入**：LearnModule 卡片弹窗经 MdDialog 既有 sidePanel 扩展位挂「问 AI」拓展坞（'learn' 频道＝学习·问答，与右侧边栏同频道同会话数据）；划词「问 AI」改为先 `ai.configured()` 检查（未配置弹「去配置」）→ 坞收起时自动展开 → autoAsk 直发坞内（prompt 模板不变）。宽度/收起态持久化新 SettingsKeys `learn_ask_panel_width` / `learn_ask_panel_collapsed`（settings 表任意键，零 DB 迁移）。
  - **连带清理**：LearnModule 删除 onOpenAi prop（唯一调用点被拓展坞替换），App.tsx 对应移除传参与 CHANNEL_BY_MODULE 的 learn 条目——学习库自此不再经模块映射走右栏（右栏仍可手动切到学习·问答频道看同一会话）。
- **②格言拖拽自动滚动**：
  - MottosModule 拖拽边缘自动滚动：dragstart 时从被拖行向上找最近滚动容器（通用 overflowY auto/scroll/overlay 且可滚检测，实际即主栏 `.main-area`）；`.mottos-page` 根节点 onDragOver 计算指针相对滚动区上/下缘 64px 触发带的方向系数（0~1，越贴边越快）；rAF 循环每帧 `scrollTop += 系数 × 12px`（贴边全速约 720px/s，「缓缓移动」）。
  - 细节：rAF 循环不依赖 dragover 触发频率（指针停住也持续滚）；0.5s lastOver 时间戳护栏（拖出滚动区后 dragover 停发自动停）；dragend/drop/组件卸载三路清理。仅动渲染层一个文件，既有拖拽排序/单击菜单/双击开笔记逻辑零改动。
- **验证**：typecheck 双配置 + build 通过。**待冒烟**：①划词问 AI 坞内直接出对话不出弹窗、坞收起时点击自动展开、与右栏学习·问答频道同一会话两处一致、坞拖宽/收起重启记忆、致知己追问栏全功能回归（让 AI 追问/多会话/拖宽/收起//clear //compact/删除会话确认）、LLM 未配置「去配置」、双主题目检；②三区条目多时拖到顶部/底部缓速滚动且越贴边越快、拖出主栏或松手即停、区内拖拽排序与跨区流转回归。

## 万象库测一测提交制解析 + 知识树说明文档 + 深挖后台化（优化建议区第43轮）

> 创建于 2026-09-15 22:42

- **需求来源**：优化建议区待完成区三条——①万象库测一测作答改为预选、「提交答案」才正式作答，提交后无论对错都显示题目解析并能快速查看相关知识点卡片；②每棵知识树配 md 说明文档（说明树中内容），手动新建知识树时除名词标题外还要写期望知识点说明、再由 AI 生成树；③学习库深挖改后台执行，生成完轻提示、点击立即跳转。
- **①测一测提交制 + 解析 + 关联卡片**：
  - **出题带解析**：`WikiQuizQuestion` 增 `explanation` 字段（services.ts 出题 prompt 要求每题附「讲清正确答案为什么对、干扰项错在哪」60~120 字解析；parseQuizArray 容错解析、缺省空串），preload/api.d.ts 类型同步。
  - **交互改造**（WikiModule）：`pickOption` 改纯预选（提交前可反复改选，`.quiz-opt.selected` 描边提示）；新增 `submitAnswer`——点「提交答案」才把预选计入 `quizPicks` 正式作答并锁定；提交后亮正确项/所选错项 + `.quiz-analysis` 解析框（无论对错都展示）+「查看相关知识点卡片」按钮（`wiki.entry(entryId)` 开 MdDialog 覆盖在答题页上，关闭即回）；`quizNext` 改以提交为闸。计分口径不变（quizPicks 对齐答案）。
  - 新样式：`.quiz-actions`（提交前行）、`.quiz-analysis`（主题色软底 + 左缘主题色条，双主题走变量）等。
- **②知识树说明文档**：
  - **路径约定**（零 DB 迁移）：每棵树一份 `md/learn/tree-<domainId>.md`，结构 = 标题头 +「## 我的期望」（用户期望说明）+「## 树内容总览」（知识点清单）。
  - **建领域带期望**：`learn:domainCreate` 增 `intro` 可选参数——新建领域弹窗加期望说明 textarea（可留空不阻断快建），创建即写树说明文档，期望写入「我的期望」节。
  - **建树读期望 + 写总览**：`generateLearnTree` 建树前读树说明文档、全文截断 1500 字注入 prompt（「优先遵循」）；建树后把「树内容总览」（主题 × 知识点 × 一句话简介清单，AI 无关纯拼装）追加写入文档（保留用户既有内容；文档写失败不回滚建树）。
  - **存量兜底**：新 IPC `learn:treeDoc(domainId)`——打开前确保文档存在：已有即开；缺失（本轮之前的存量领域）按 DB 现状补「树内容总览」快照（已建树）或只写头 + 引导（未建树）。入口两处：领域管理菜单「树说明文档」、未建树空态按钮；MdDialog 打开双击可编辑。
- **③深挖后台化**：
  - LearnModule 深挖状态由单对象 `dig` 改按卡片 id 分桶 `digJobs: Record<number, DigJob>`——点「深挖」请求即入后台（弹窗可关、可切模块），同卡生成中防重入、不同卡可并行。
  - **完成轻提示 + 点击跳转**：Toast 组件扩展 `toast(text, { onClick })`（`.toast-clickable` 放开容器 pointer-events + 下划线提示，点击即跳转并消失）；完成 toast「「标题」深挖完成，点击查看」，点击派发 `MODULE_NAVIGATE_EVENT { module:'learn', target:'dig-card', payload:{nodeId} }`。
  - **跨模块跳转链路**：App.tsx 新增 MODULE_NAVIGATE_EVENT 集中监听切模块（原仅总导览 onNavigate 触达；OverviewModule 的 onNavigate prop 随之移除，深链收敛为事件单路径）；LearnModule `useModuleNavigate` 增 `dig-card` 分支——`getCard` 秒开该卡弹窗，深挖 done 面板（放弃/重新生成/写入卡片）原样呈现，**「确认后写入」决策保留**（跳回后仍需点「写入卡片」落库）。
  - 弹窗内 digPanel 文案改「后台深挖中… 可先关闭弹窗，完成后轻提示点击跳回」；失败改轻提示（未配置仍「去配置」弹窗、取消静默）；关闭弹窗不再清任务。
- **验证**：typecheck 双配置 + build 通过。**待冒烟**：①测一测——预选可改选、未选提交禁用、提交后对错 + 解析（对/错都要有）+ 相关卡片弹窗开关回归、成绩与再来一轮、双主题目检；②知识树——新建领域填/不填期望说明、建树后打开树说明文档见期望 + 总览、存量已建树领域首开树说明文档自动补总览、期望修改后再建树生效（新领域）、文档双击编辑；③深挖——点深挖关弹窗/切模块生成不中断、完成轻提示点击跨模块跳回开卡、弹窗内 done 面板写入卡片、取消/放弃/重新生成、LLM 未配置、失败轻提示。

## 音乐吧——白噪音更名 + 轻音乐 tab（新功能开发区）

> 创建于 2026-09-15 23:33

- **需求来源**：`新功能开发区.md` 待完成区任务一——白噪音更名「音乐吧」，内含轻音乐/白噪音双标签页，轻音乐支持导入 mp3。brainstorm 四决策：音源互斥、最小集+歌单分组、与白噪音播放队列完全独立、快捷控件控当前活跃音源。设计/计划见 `左侧边栏/音乐吧/2026-09-15-音乐吧-轻音乐-design.md` + 同日 plan。
- **DB v39**：`music_playlists` + `music_tracks` 两表（playlist_id NULL=未分组）；mp3 复制进 `userData/music/<id>.mp3`（同书籍入库模式）；导入去重 = 同名（去扩展名不分大小写）同字节数 stat 比对；时长懒获取（首次播放 audio.duration 回写，`music:duration`）。
- **引擎与互斥**：新建 `src/services/musicEngine.ts`（HTMLAudioElement 全局单例，切模块不断声；mp3 字节经 IPC → Blob URL；循环三模式 list-loop/single-loop/random；丢文件 toast + 自动跳下一首、整轮全败即停）；新建 `src/services/audioExclusive.ts`（两引擎注册 stop、开播 stopOthers 互停、lastActive 记最近活跃音源）；noiseEngine.play() 接入 stopOthers/markAudioActive，模块尾注册 stop。
- **渲染层**：NoisePage 更名音乐吧 + 大 tab「轻音乐｜白噪音」（默认轻音乐，会话内保留），白噪音子 tab（混音器｜播放队列）与 MixerPanel/QueuePanel 零改动；新建 MusicPanel（歌单条全部+歌单+新建/改名/删除〔删歌单曲回未分组二次确认〕、曲目行点击播放/加入歌单菜单/彻底删除二次确认连文件、底部控制条上下首/播放暂停/进度拖拽/循环三态/音量）；App.tsx 左栏控件更名「音乐吧」+ 任一音源播放高亮、右栏快捷钮改活跃音源口径（音乐活跃显 music_note 图标）、MusicState 启动恢复（记曲目/模式/音量，默认暂停）；页头大播放按钮同活跃口径。
- **验证**：typecheck 双配置 + build 通过。**待冒烟**：导入/重复跳过汇总、互斥切换（播音乐白噪音停反之）、三种循环、歌单增删改归组、删曲连文件、进度拖拽与音量独立、重启恢复默认暂停、快捷控件活跃口径、切模块音乐不断、丢文件跳下一首、双主题目检。
- **冒烟 bug 修复（00:09）**：导入曲目点击播放报「无法播放（文件丢失或损坏），已跳过」/再点报「音频初始化失败」。排查路径：数据侧完好（userData 实际为 data_home.json 重定向的 D:\Paper\app_output\bugzi_workspace，music/2.mp3 13.8MB 合法 MPEG1 Layer III，DB 行正确）；独立 Chromium（Playwright）同文件 HTTP 源可播 → 锁定 App 实例内链路；经 CDP（--remote-debugging-port=9223 + Runtime.evaluate）逐段验证——preload 暴露 ✓、IPC bytes 完好（Uint8Array 13,834,244 头 ID3）✓、Blob→Audio.play() 抛 **NotSupportedError** ✗。**根因：`src/index.html` CSP 无 `media-src`，回退 `default-src 'self'` 把 `blob:` 媒体源拦了**——本项目此前零 `<audio>/<video>` 消费者（epub/pdf 走 JS 解码渲染、图片走 img-src），首次引入 media element 才暴露。**修复：CSP 追加 `media-src 'self' blob:`**。修复后同链路 CDP 实测播放成功（currentTime 1.31s / 时长 344.08s 正确解码）。附注：排查时 user 侧曾删 id=1 重导为 id=2（music_tracks AUTOINCREMENT 断档），探针首查 id=1 的 NOT_FOUND 为假阳性，已排除。

## 学习库每日要求——每日新学 8-10 + 小测 + 自动打卡（新功能开发区）

> 创建于 2026-09-15 23:33

- **需求来源**：`新功能开发区.md` 待完成区任务二——每日必须新学 8-10 个新知识点（用户自己解锁打卡，不要随机推荐）+ 完成题目练习测试。brainstorm 四决策：每日随机 8-10、取消系统自动推荐、自动打卡（无手动按钮）、小测交卷即完成不看分数。设计/计划见 `左侧边栏/学习库/2026-09-15-每日要求-design.md` + 同日 plan。
- **DB v40**：`learn_nodes` 加 `learned_at`（本地时间 ISO——`localNowIso()`，nowIso 的 UTC 口径跨午夜会错日故弃用）；`learn_daily` 重建为 (date, goal, review_ids, done)，new_ids 定档机制废除、旧队列数据作废。
- **闭环**：定档 goal = 8 + random(0..2)（启动/零点/learn:daily 幂等，纯 SQL 零 LLM）；解锁路径 = 知识树点开学会了/随机来一条/手动添加后学，任一路径 mark learn 写 learned_at 并计数；done 两处 check-and-set（`completeDailyIfReady`：新学数 ≥ goal 且今日卷 graded → done=1），mark learn 与 quizSubmit 任一后到达都补判，达成 toast「今日要求完成！」；连胜 `learnStreak()` 从 done 列推导（done 显式落库，删卡不影响历史），缺行即断无惩罚。
- **备学池泵**：learnStock 重写——由「今日队列 new_ids 预生成」改为「todo 且 content_ready=1 保底 10 张，不足领域洗牌轮转均摊补生成」；三触发不变（启动延迟/进模块 stockCheck/mark learn 消耗后），LLM 未配置静默。
- **渲染层**：今日学习 tab 新布局 = 今日要求卡（flag 图标 + 新学 x/N 进度条 learn-goal-bar + 小测状态 + 已完成·连胜 n 天）→ 随机来一条 → 今日已学动态列表（learned_at 今日，取代原「今日新学」定档列表）→ 到期复习（不变）→ 今日小测（出卷口径改今日已学卡、门槛 ≥2、create/submit/retry 后 onChanged 上抛刷新要求卡）；OverviewModule 今日学习块改状态行「新学 x/N · 小测… · 连胜 n 天」，完成后显示已完成。
- **与并行会话（第42/43轮优化）同文件合流**：LearnModule 拓展坞/深挖后台化与本轮改造互不冲突，markCardReady 的今日列表行内补 ready 由并行侧适配为 setLearnedList（今日已学卡天然已生成，实际仅树路径生效）。
- **验证**：typecheck 双配置 + build 通过。**待冒烟**：goal 当日定档不重抽、三路径解锁计数与进度条、done 两序联动（先学满后交卷/先交卷后学满）、连胜推导与断签、备学池秒开与补池、总导览状态行、删卡后历史 done 不变、复习流转回归、LLM 未配置时定档打卡正常且小测提示去配置、双主题目检。

### 第43轮反馈修订（260915 晚）：后台生成轻提示推广 + 测一测提示文案清理

> 创建于 2026-09-15 23:33

- **需求来源**：开发者反馈——万象库测一测生成也要有轻提示；所有 AI 后台生成都需要添加轻提示；测一测「（提交前可改选）」这类提示文案直接删去。
- **①测一测出题完成轻提示**：startQuiz 成功后 toast「出题完成，点击开始作答」（可点击）——点击派发 `MODULE_NAVIGATE_EVENT { module:'wiki', target:'quiz' }`，WikiModule 深链 handler 增 quiz 分支（切百科 tab + 切答题视图），跨模块生成完成后点提示即回；失败路径从 failMsg 弹窗改轻提示「出题失败：…」（截 80 字；已取消仍静默 toast、LLM 未配置仍「去配置」弹窗；失败一律回总览防答题页卡在加载态）。
- **②提示文案清理**：删「选定选项后提交（提交前可改选）」，未选时不渲染任何文案、选中仅留「已预选 X」；`.quiz-actions` 改 `justify-content: flex-end`（提交按钮恒靠右不因文案有无跳动）。
- **③后台生成轻提示推广**：学习库卡片生成队列（learnGenQueue）onReady 原本静默回写——现 toast「「标题」已生成，点击查看」，点击经深链 `open-card` 立即开卡（jumpToDig 泛化为 jumpToCard，深链 target `dig-card`→`open-card`，深挖提示与队列提示共用同一跳转）；learnGenQueue onReady 回调签名增 title 上抛。**口径界定**：主进程三泵（推理角题池/万象库后库/学习库预生成）为主动维护性后台任务，维持「静默、永不弹窗」既有设计不改；辩真验证（第37轮已有「验证完成」toast）、复盘报告（按钮态+完成反馈）等已有完成反馈的不再叠加。
- **顺带修复**：并行会话（学习库「每日要求」功能）将 dailyNew 改名 learnedList 时漏改 markCardReady 内 setDailyNew 悬空引用，顺手补齐为 setLearnedList（一行机械改名）。
- **验证**：typecheck 双配置 + build 通过。**待冒烟**：测一测出题完成 toast 点击跳回（含跨模块）、失败 toast、未选无文案/选中「已预选 X」按钮恒靠右；队列点开未生成卡完成后 toast 点击开卡、批量队列多条 toast 逐条到达示可接受；深挖提示跳转回归（target 更名）。
