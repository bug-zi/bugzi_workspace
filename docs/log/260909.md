# 260909 开发日志

## 推理角优化：练习题生成即入题库 + 精选题库改名 + 题面渲染

> 创建于 2026-09-09 01:08

- **背景**：开发者优化指令 `docs/project/左侧边栏/推理角/优化设计.md` 两项——① 练习场生成的题目直接放进「精选题库」，板块顺带改名「题库」；② 题面显示的是未渲染的文档样式（`**` 加粗等记号裸露），改为渲染后的样式。
- **实施**：
  - **主进程**（`electron/ipc.ts`）：`wall:practiceNew` 生成即 INSERT `wall_bank`（todo 态；source「AI 练习场生成（YYYY-MM-DD）」；PracticeEntry 增 `bankId`）；`wall:practiceAnswer` 判答后回写该行终态 + `writeBankMd` 详情 md（行已终态则跳过——防同题在题库侧先答/看解答后双写）；弃做的题留题库 todo 可回补作答（题库侧无提示机制，一题一命口径不变）。新增 `practiceBankTitle()` 兜底。**零 DB 迁移**（复用 wall_bank 现有列）。
  - **出题管线**（`electron/ai/services.ts`）：`WallPuzzleDraft` 增可选 `title`，出题 JSON 输出增「题目短标题（10 字以内，点出题材不剧透）」，`composeWallPuzzle` 解析截 20 字；每日一题共用该管线但忽略 title；title 缺失时入题库侧兜底取题面首行去 md 记号截 16 字。
  - **渲染层**：BankPanel 区头「精选题库」→「题库」、副标题改「策展难题 · 练习场生成题自动入库 · 一题一命」；练习场副标题与换题提示同步（「换题后本题留『题库』待作答」）；三处题面（每日一题 WallPanel / 题库 BankPanel / 练习场 PracticePanel 的 quiz-question）由 `pre-wrap` 纯文本改 `MdView` 渲染——题面本就是 md（v1.6 出题标准第 7 条），加粗与列表从此生效。
  - **注释口径同步**：preload.ts / api.d.ts / App.css 相关注释更新（精选题库→题库、练习不落库→生成即入库）。
- **文档**：designs-specs.md 增 v1.7 版本注 + §0/§2/§3/§4/§8 增量同步（AI 维护）；design.md 板块 B 最小同步——改名、题库小节、练习场小节、存储与回收站口径，**design.md 为开发者主导文档，系按优化指令同步，请开发者复核**；CLAUDE.md 推理角条目更新；`优化设计.md` 移入 `推理角/archive/2026-09-09-练习题入库与题库改名.md`。
- typecheck 双配置 PASS。待冒烟：练习场出题后题库列表即现新行（todo + 短标题 + 出处标注）、判答后可开完整档案、弃做换题行留 todo 可在题库补答、题库侧先答后练习场提交不双写；三处题面渲染生效无裸记号。git 提交由开发者执行。

## 藏书架优化第 1 轮收尾（开发者验证通过，本轮完结）

> 创建于 2026-09-09 00:41

- **收尾动作**：①designs-specs.md §8 全节同步最终实况（四处修错偏离初稿的注记：模式切换全量重挂 / W/S 按住流式滚动 / syncAnnotations 竞态修复 / 容器 X 轴钳制 + 首帧重排）+ §8.7 验收项全勾；②设计记录 `2026-09-08-书架优化-design.md` 状态行改「已实施并验证」并移入 `archive/`；③CLAUDE.md 藏书架条目补本轮能力（双模式/键盘/目录侧栏/划词笔记 DB v24/主题色）。本节纯文档改动，零代码。
- 待开发者：git 提交（含本轮全部代码与文档）。

## 信息源优化（第28轮之二）：收件箱模式 + 时间窗口加载 + 老数据瘦身

> 创建于 2026-09-09 00:42

- **背景**：优化建议区第 28 轮——信息源「已读归档机制 + 按时间选择性加载」。经 brainstorming 三项定稿：收件箱模式（主列表只显未读、读完即消失）、30 天时间窗口（窗口外「加载更早」触达）、180 天清正文保轻行（删最占空间的正文/总结，保留标题轻行）。设计文档 `docs/project/左侧边栏/信息源/2026-09-09-信息源收件箱与时间窗口-design.md`，实施计划同目录（同名无 -design 后缀）。
- **实施**（Task 1-5）：`src/shared/types.ts` + `api.d.ts` 新 `FeedView` / `FeedListView`；`feed.ts` `listArticles(feedId, view, sinceDays)` 重构（未读/已归档分支 + `COALESCE(published_at, fetched_at) >= now-sinceDays` 窗口 + remaining 窗口外 COUNT 返回）+ `cleanupOldArticleBodies()`（180 天前置空 content_feed_html/content_fetched_html/summary_text/summary_at，仅清仍有内容者）；IPC 三件套透传新参（markAllRead 不动——现 SQL 本就无时间过滤，天然清空全部未读）；`scheduler.ts` 启动 + 零点两处挂清理；`FeedModule.tsx` 头部 recycle-tab「收件箱 | 已归档」切换、switchView/switchFeed 重置窗口 30 天、列表底部「加载更早 30 天 · 还剩 N 篇」、空状态分视图文案；feed.css 增 `.feed-load-more`。零 DB 迁移。实施一笔修正：计划中 `params: unknown[]` 展开不满足 node:sqlite `SQLInputValue[]`，收紧为 `(string | number)[]`。
- **排障一笔**：typecheck 一度「假绿」——composite 增量缓存（tsbuildinfo）重放旧诊断，清 `tsconfig.web/node.tsbuildinfo` 后真错现形（即预期的 FeedModule 两处调用点），第 23 轮同款问题复发，后续实施如见「不该通过却通过」先清缓存。
- typecheck 双侧 PASS（清缓存后验证）。待冒烟：读完一篇文章回列表行消失；已归档视图翻已读；源筛选两视图生效；窗口外文章「加载更早」触达；「全部标已读」清空收件箱（含窗口外未读）；主进程控制台观察「信息源老文章正文清理 N 条」日志（有 180 天前老文章时）。git 提交由开发者执行。

## 灵感泉优化（第28轮之一）：完整点子文档

> 创建于 2026-09-09 00:38

- **背景**：优化建议区第 28 轮——第 18 轮两阶段管线 + 第 22 轮规范 v2 之后实测，经 brainstorming 澄清唯一病灶为 **summary 两句模板太薄**（「是什么+第一步」100 字装不下点子本身，卡片与 md 正文均无完整内容载体，看不出值不值得立项）；标题/腔调/风向/配额无恙不动。
- **定稿方案 A「完整点子文档」**：自评阶段升级——选中 5 条各产出 body 完整 md 文档（引子段 + `## 这是什么` / `## 核心机制` / `## 最小版本`，300~400 字，白话具体禁文艺化压缩、「第一步」并入最小版本小节），md 正文即完整文档。发散 12 条粗点子、口味注入、避免清单、风向标全部不动；降级路径（pickDiverseFive）无 body 退单行 summary；存量 AI 条不迁移。
- **实施**（Task 1-3，`electron/ai/services.ts` + `src/modules/inspirations/InspirationsModule.tsx`）：新 `InspirationIdea` 统一类型（body 可选）；`parseInspirationArray` 容错升级——title 必填、summary/body 至少其一非空；reviewPrompt 重写为 body 骨架版（评审五维/配额/淘汰规则不动，输出去 summary 改 `{"title","form","body"}`）；入库 `mdCreate(it.body ?? summary)`；卡片预览改取首段（首个空行前压平 120 字，不再混 `##` 标记符）。零 DB/IPC 改动，`GenerateInspirationsResult` 字段不变。
- 设计文档 `docs/project/左侧边栏/灵感泉/2026-09-09-灵感泉完整点子文档-design.md`，实施计划同目录（同名无 -design 后缀）。
- typecheck 双侧 PASS。待冒烟：来 5 条 → md 弹窗为完整文档（引子+三小节）；卡片预览为引子句；手动条预览正常；断网自评两趟失败走单行 summary 降级不报错。git 提交由开发者执行。

## 书架 W/S 流式滚动 + 切模式后首视图横向可拖修复（开发者反馈两项）

> 创建于 2026-09-09 00:41

- **① W/S 改按住流式滚动**：原实现每按一次 `scrollBy` 85% 视口（smooth），粒度粗。重做——`readerKeys.ts` 新增 `createHoldScroller()`：keydown（忽略系统重发）登记方向并启动 rAF 循环，逐帧小步 `scrollTop`（约 550px/s）→ 长按连续流动、松开即停、轻点小幅位移；window blur 与组件卸载兜底全停。keyup 归一用无输入保护的 `dirOfKey`（防「按住时焦点移入输入框再松开」泄漏滚动）。epub 滚真实容器 `.epub-container`、pdf 滚宿主，两阅读器同体验。
- **② 翻页→滚动切换后首视图横向可拖（跳章即消失）**：根因为首帧视图宽度未收敛（挂载竞态残留）+ epub.js scrolled 模式给 `.epub-container` 设行内 `overflow: auto` 含 X 轴。双管齐下：createRendition 首帧 display 完成后按内容盒净尺寸 `resize()` 强制重排一次（治宽度未收敛）；bookshelf.css 增 `.bk-epub-host .epub-container { overflow-x: hidden !important }`（行内样式需 important 覆盖；竖向滚动与翻页模式不受影响）。
- typecheck/build 双绿。待人工复验：长按 W/S 流式滚屏与轻点小步、A/D 换章不受影响；翻页→滚动切换后首视图无横向拖动；批注输入框内打字不触发滚动且按住切焦点松开能停。git 提交由开发者执行。

## 书架滚动模式 W/S 无效 + 滚动→翻页空白修复（开发者反馈两项）

> 创建于 2026-09-09 00:22

- **① 滚动模式 W/S 无法上下滚动**：根因——scrolled 流式下 epub.js 会给内层 `.epub-container` 设 `overflow: auto`（default manager `updateFlow` 的 scrolled 分支默认值），**真正的滚动容器是它而非宿主 `.bk-epub-host`**，此前 `host.scrollBy` 落空。修复：键盘滚动分支先 `querySelector('.epub-container')` 取真实滚动容器再 scrollBy（找不到时退回 host）。pdf 滚动模式宿主即滚动容器，本就正常。
- **② 滚动→翻页切换空白（翻页→滚动正常）**：同 Book 销毁重建 rendition 在 scroll→page 方向仍空白（epub.js 残留状态方向性 wart，逐层排查 stage/manager/archive blob URL 未定位到单点）。修复改道：**模式切换全量重挂**——BookshelfModule 中 EpubReader 的 key 由 `id` 改 `id-mode`，切换即换新实例（新 Book + rendition + 全套事件主题标注重挂）；切换前先 `books:list` 取最新进度行更新 `reading`（防节流 3 秒窗口内的进度丢失导致回跳到开书位置）；EpubReader 内部的模式切换 effect/modeRef/renditionModeRef 相应移除。pdf 维持阅读器内部重建（无此问题）。
- **顺带收缩**：移除上一轮加的 `'html, body': { overflow: hidden }` 主题注入——核实 `contents.columns()` 在分页模式本就对内容根设 `overflow-y: hidden`，该注入冗余；防拖动靠宿主层 `bk-page-mode { overflow: hidden }` 已足够。
- typecheck/build 双绿。待人工复验：滚动模式 W/↑ S/↓ 平滑滚屏、A/← D/→ 换章；双模式来回切换（含大书）不再空白且位置保持；翻页模式仍不可拖动。git 提交由开发者执行。

## 优化建议区第29轮：主题按钮右栏展开时隐藏

> 创建于 2026-09-09 21:24

- **背景**：主题按钮 260908 自左栏底部迁右栏 `.right-col` 底部（当时为白噪音腾位，白噪音后已进列表），右栏展开时按钮拉伸成与边栏同宽（280–560px）通条，开发者反馈不美观。经 brainstorming 可视化陪伴（本地 52100 端口线框对比页）呈现四方案——A 回归左栏底部（推荐）/ B 通条改固定小方块 / C 收进 debugzi 头部+细条第三格 / D FAB 悬浮圆钮（遮挡输入框，否）；开发者定向拍板**第 5 案（原四案之外）**：右下角位置保留，右栏展开时隐藏、双面板收起时才显示。
- **实施**（`src/App.tsx` 单文件单处）：主题按钮包 `{rightPanel === null && (…)}` 条件渲染——debugzi 展开/草稿本展开两态不渲染（通条根除），仅右缘细条态显示于右下角（样式 `.right-col-theme` 与收起态外观零变化）；注释由「三态常驻」同步改口径。个人档内主题设置不受影响；无 CSS/DB/IPC 改动。
- typecheck 双配置通过。待人工冒烟：右栏两展开态右下角无主题按钮、细条态右下角可见可切换、切主题图标态随主题翻转。开发者指令本轮免写设计文档。git 提交由开发者执行。
- **第29轮反馈修订（21:24 反馈，21:44 完成）**：细条态右侧边栏未贴窗口右缘。经 Chrome devtools 打开 dev 渲染层（initScript 注入 window.api 万能桩——Proxy 路径跟踪 + 数组代理、订阅式 on* 返回退订函数、settings.* 返回字符串，绕开无 preload 环境）实测定位：主题按钮自然宽 42.5px（9px padding×2 + 图标 + 1px 边框）大于细条 36px，撑宽 `.right-col`（flex 列容器宽取最宽子元素）→ 细条右侧留 6.5px 缝；此病 260908 按钮迁来即埋下，第 29 轮改按钮为收起态专属后显眼。修复：`.right-col-theme` 钉 `width: var(--ai-width-collapsed)` 与细条同宽（按钮仅收起态渲染，钉 36px 恒成立），注释说明根因。验证：收起态细条/按钮缝隙 0、左右缘严格对齐；AI/草稿本两展开态 320px 贴右缝隙 0 且按钮正确隐藏；typecheck 通过。

## 画布侧边栏落地（Excalidraw 接入，新功能开发区待完成条目）

> 创建于 2026-09-09 22:16

- 新功能开发区待完成条目「右侧边栏再接入画布功能（excalidraw.com）」brainstorming 四项澄清（多画布管理 / 宽上限+大窗 / 删除入回收站 / npm 包内嵌）→ 设计与实施计划 → 落地完成，全程记录：`右侧边栏/画布/2026-09-09-画布侧边栏-design.md` / `-plan.md`。
- 依赖：`@excalidraw/excalidraw@0.18.1`（peerDeps 官方支持 React 19；计划按文档写对象式 `serializeAsJSON({elements, appState})`，实测 0.18.1 为四参数位置式 `(elements, appState, files, type)`，已按实际签名修正）。
- 主进程：DB v25 `canvases(id,title,path,created_at,updated_at,deleted_at)` + 目录 `canvas/`；IPC `canvas:list/create/rename/save`（create 两步回填 path + 写空白标准 .excalidraw，自动命名「未命名画布 N」；save = mdWrite + bump updated_at；rename 不动 updated_at）；回收站 `canvases` 来源（软删/恢复清标记/彻底删除走默认路径删行+删文件，`MD_FIELDS.path` 驱动）。
- 渲染层：新组件 `CanvasStage`（Excalidraw 封装：按 path 加载、缺失/损坏按空白画布打开 toast 提示、onChange 停笔 800ms 防抖 serializeAsJSON 落盘、卸载落袋、imperative flush；theme 跟随双主题、langCode zh-CN、canvasActions 关 loadScene/saveToActiveFile 防旁路写）、`CanvasDialog`（大窗作画，关闭/Esc/遮罩先 flush 再关）、`CanvasSidebar`（右缘第三面板，镜像草稿本切换条/浮层列表/hover 重命名删除/左缘拖宽 280–900px 持久化；大窗打开时右栏舞台卸载显示占位——同画布双实例场景分离，互斥挂载数据同源）。
- 集成：`App.tsx` rightPanel 扩四态 `'ai'|'draft'|'canvas'|null`（启动恢复含 canvas），`AiSidebar` 细条三图标（forum/edit_note/draw）；`RecycleModule` 页签新增「画布」（八块变九块），恢复去向「画布面板」；`global.css` `--canvas-width: 360px`；`App.css` right-col flex 规则补 canvas-sidebar。
- 验证：typecheck 双配置 / electron-vite build / npm run smoke 全通过（Excalidraw 的 Assistant 字体、rough、zh-CN locale 已进 renderer 产物）。待人工冒烟：三面板互斥与重启恢复、画几笔停笔 1s 自动保存、拖宽持久化、删除→回收站画布页签→恢复、大窗互斥回显、双主题跟随、`canvas/{id}.excalidraw` 可被 excalidraw.com 官方导入。
- 已知限制：粘贴进画布的位图不落盘（v1 不存 files 资产，重开显示占位）；工具栏无图片工具入口（未启用），绝大多数场景不触发。
- git 提交由开发者执行；`右侧边栏.md`（双面板→三面板）修订稿待开发者审核后写入。

## 推理角题库预生成落地（v1.3 设计 260907 批准，今日实施）

> 创建于 2026-09-09 22:31

- **背景**：优化建议区第 21 轮待完成条目——海龟汤/每日一题/练习场三处取题靠 AI 临时生成、用户干等。设计文档 `左侧边栏/推理角/2026-09-07-reasoning-question-bank-design.md`（260907 会话内逐节批准）因后续模块挤占 DB 版本号顺延至今日实施；开发者将文档从 archive 移回模块目录并指令继续完成。
- **实施**（9 文件）：
  - **DB v26**（`db.ts`）：新表 `wall_pool`（title/puzzle_text/answer_standard/standard_reasoning/hints/puzzle_type/difficulty/created_at）——每日一题/练习场共用预生成题池；未消费储备，无 deleted_at、不进回收站。**对设计的三处时代差适配**：①版本号 v14→实际 v26；②wall_pool 增 `title` 列（v1.6 起出题管线产 title、v1.7 练习题入题库需要标题，设计写作时两者均未存在）；③练习场取池题同步入 `wall_bank` todo 态（设计写于「练习题用完即弃」时代，须并轨 260909 上午的生成即入库口径，否则判答无 bankId 可回写）。
  - **补充泵**（新文件 `electron/services/reasoningStock.ts`，仿 scheduler 静默惯例）：`ensureReasoningStock()` 永不抛错；`pumping` 单例防重入；汤侧 fresh<5 循环 `generateSoups('random')` 补到 10（零插入即 warn 跳出防死循环），题侧池<5 逐道补到 10（难度=池内最稀缺档并列随机、题型=池内+近 2 日历史合计最少见、avoid=近 20 题+池内互避）；每补完一批推 `reasoning:stockChanged`（照 recycle:changed 模式）；LLM 未配置 `isLlmConfigured()` 前置静默跳过。
  - **三触发点**：`main.ts` 启动延迟 10s（unref，紧挨 backfillTrickNotes）；消耗后 fire-and-forget（每日题转正/兜底两条路径、练习场取题/兜底、`turtle:openSoup` 开新局——续局与终局回看不触发）；新 IPC `reasoning:stockCheck`（`ReasoningModule` 模块激活时调，覆盖 LLM 事后配置场景）。
  - **取题改道**（`ipc.ts`）：`wall:ensureToday` 四级放宽选池题（①难度匹配且题型避近 2 日→②仅难度→③池内任意→④池空兜底现场生成），命中即 BEGIN…COMMIT 同事务转正（INSERT wall_puzzles + DELETE 池行，防「既入墙又留池」），零 LLM 秒开；`wall:practiceNew` 按难度/题型筛选（随机=不限）取池题，同事务 DELETE 池行 + INSERT wall_bank（todo、source「AI 练习场生成（取用当日）」——池为不可见实现细节，题库来源标签保持单一口径）；两处取题后均 `void ensureReasoningStock()`。新增 `pickPoolPuzzle()` 只选不删助手 + `bankTitle()` 泛化（原 `practiceBankTitle` 收编，池路径/现场路径共用）。
  - **渲染层**：`TurtlePanel` 监听 `reasoning:stockChanged` 渐进刷新汤库列表（无 toast）+ 空态文案改「AI 正在后台备汤…」；`WallPanel`/`PracticePanel` loading 文案改「题库见底，现场出题中…」（正常路径秒开，仅兜底出现）；preload/api.d.ts 桥接 `reasoning.stockCheck` + `reasoning.onStockChanged`。
- **排障一笔**：`SELECT * … .all() as WallPoolRow[]` 报 TS2352（node:sqlite 返回 `Record<string, SQLOutputValue>[]`），照 `settleTurtleTimers` 惯例 `as unknown as` 过——全列宽行转换都需如此，单列投影可直接 as。
- typecheck 双配置 PASS（清 tsbuildinfo 缓存后验证）。待冒烟：首启 10s 后主进程控制台见泵日志、汤库/每日一题秒开、`wall_pool` 行数维持 5–10、练习场连取 10+ 道耗尽后兜底现场生成、汤库空态新文案、判答后题库行终态正常（池路径）。git 提交由开发者执行。
- **开发者首验反馈修订（22:31 反馈「重启后仍要等生成」，22:42 完成）**：只读查库确诊——v26 迁移已生效、LLM 已配置，但 `wall_pool` 0 行、无新汤落库；根因是泵内补充顺序写成了「先汤后题」：库里 fresh 汤仅 2（<5），汤侧 3 批串行出题+审题（每批 4~8 趟 LLM 调用）把题池挡在身后十几分钟，而设计定稿的核心痛点恰是**题**的等待。两处修正（`reasoningStock.ts`）：①**补充顺序反转为题池先、汤库后**（题是最高频「等出题」路径且首填最长；汤侧开 fresh 局本就秒开、另有手动按钮兜底）；②汤/题两路**各自 try/catch 互不阻断**（原实现一路失败另一路不跑）；顺带补「补充开始/结束」console.info 过程日志（冒烟可观察，此前只 warn 无正向日志）。另向开发者说明口径：**首启当天池空是设计内兜底**——第一次取题仍现场生成一次，题池首填约 20 趟 LLM 调用需几分钟，补满后当次会话即秒开；明日起每日一题开屏秒取。typecheck 复验通过。
- **二次反馈修订（23:07，泵瞬态错误重试层）**：开发者回传泵日志——启动正常（题池优先生效），但题池首道审题遇中转 `502 Upstream service temporarily unavailable` 即整轮中断。排查 `llm.ts`：`chatCompletion` 只内重试 429、5xx 直接抛，墙题管线无任何重试，而泵「下次触发」要等用户再消耗/再进模块，首填时段太稀疏。修（`reasoningStock.ts`）：新增 `retryTransient()` 重试层——**仅瞬态错误**（`LLM 服务返回 5xx/429`、`网络请求失败` 正则判定）重试，间隔 25s 最多 3 次；质量类失败（审题不过/字段缺失）不重试直接跳出防浪费。包裹 `generateWallPuzzle`/`generateSoups` 两处批次调用；题池 +1 / 补汤一批各补 info 日志（冒烟可数进度）。另判明日志中 `turtle:generate 已取消` 为手动路径取消（不经过泵），无害；两路并发挤同一中转通道易诱发 429/502，已写入文件头注释提醒首填时段避免手动出汤。typecheck 复验通过。
- **三次反馈修订（23:26，自愈重排）**：开发者再传日志——重试层按设计工作（3 次退避重试均执行），但通道持续故障（`fetch failed`×2 + `503 Upstream is temporarily busy`，aiedulab→gpt-5.6 晚间过载），重试用尽后泵躺平。加**自愈重排**：`schedulePostponeReRun()`——整轮瞬态中断后每 5 分钟自动重跑泵，最多连续推迟 6 次（约 30 分钟窗口覆盖过载时段），任一单位补充成功即清零计数；timer 去重（汤/题两路同轮失败只排一次）、`unref` 不阻退出；用尽则静默放弃待用户侧触发。顺带重试退避由平铺 25s×3 改递增 20s/40s/80s。判定口径：泵代码已无问题，属通道侧过载——建议开发者佐证（此刻手动 AI 功能同样失败/慢），App 挂着等通道恢复即自动补满。typecheck 复验通过。
- **待开发者**：`design.md` 中「每天首次进入思维墙，若当日无题则 AI 现场出一道」一句与实现不再一致，建议改为「从预生成题池取一道当日的题（打开现取）；池空才现场出题」（design.md 为开发者主导文档，未擅改）。
