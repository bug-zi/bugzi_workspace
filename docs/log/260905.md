# 260905 开发日志

## 问题疑惑区：致知己方案收敛 + 格言排版方案（两轮讨论收束）

- **疑惑区收束**：260904/260905 两轮长文（懂我系统 + 新模块设计）收束为「已敲定共识」清单——①「懂我」= 后台系统（个人中心「我的画像」节，注入全部 AI 上下文，条目经确认入档，预留向量库升级）②新模块定名**致知己**（辩真阁下方、回收站上方），实体 = 问题 + 多版本答案，AI 只追问不代笔 ③MdDialog 弹窗 + 版本切换条（一次一个版本）+ 回收站第五块 ④问题新表 + 每版本一个 .md ⑤版本标识 `v{序号}-{YYMMDD}`。收束前原文备份于系统临时目录，文档 188 行 → 121 行。
- **AI 回应三**（写入疑惑区待确认）：确认命名与版本格式细则（同日多版本靠序号区分、覆盖当前版本时日期更新）；提出右侧 AI 边栏**频道制**方案——外壳不变、内部按场景分频道（助手/万象·问答/致知己·追问/辩真·核查），模块动作自动切频道 + 各频道独立会话历史与人设 + channel 字段向后兼容。待开发者拍板后起草致知己 design.md。
- **问题区新增 AI 回应**：格言长句截断已定位（`src/App.css:238` `.motto-content` 单行 nowrap + ellipsis），给出三方案（推荐「双行换行」：全文自动换行 + 出处/标签转灰色副行），待开发者选定后实施。

## 优化建议区：第10轮——格言笔记弹窗标题区补出处、正文去重

- **弹窗标题区补出处**：`MdDialog` 新增可选 `subtitle` 副标题（主标题下方小字行；头部改 `.dialog-title-wrap` 列容器，其它模块不传则视觉不变）；格言笔记弹窗标题=句子，副标题=「—— 出处」（与列表行同款格式，无出处显示「—— （出处待补）」）。
- **正文去重**：新建笔记模板改空白正文（`mottos:create` / `mottos:setStatus` 两处，移除原 `# 句子\n\n> 出处` 头）。
- **存量迁移（DB v8）**：db.ts 新增 `stripMottoNoteHeader`，一次性剥离全部带 note_path 格言（不过滤 deleted_at，覆盖回收站软删行）笔记中的旧模板头——精确匹配现库句子+出处优先，退而求其次匹配「标题行=句子 + 任一 `> 出处` 行」（出处后来被改过情形），均不中（用户已自行改写）不动；头部以下自写内容保留；`setStatus` 转正时对已存在笔记同款兜底剥离（覆盖回收站恢复后再转正路径）。
- 文档同步：格言库 designs-specs.md §4/§5、优化建议区归档第10轮。
- `npm run typecheck` 双配置通过；git 提交由开发者执行。

## 问题疑惑区：格言排版方案一落地 + 致知己 design.md 起草

- **格言排版方案一实施**（开发者已选定）：`MottosModule.tsx` 格言行改上下两行结构——主行 `.motto-content` 全文自动换行（`-webkit-line-clamp: 3` 极端长文上限 + title 兜底），出处/标签转 `.motto-sub` 灰色小字副行；`App.css` 删除 `.motto-line`/nowrap+ellipsis 旧样式，新增 `.motto-sub`（对齐 row-sub 模式），`.motto-source` 改副行内省略。三区 + 搜索过滤共用行组件一并生效，拖拽排序/点击开笔记不变。typecheck 双配置通过。问题疑惑区归档第 6 轮。
- **致知己 design.md 起草**：按 260904/260905 收敛共识写入 `docs/project/需求（功能模块）/致知己/design.md`——模块本体（主列表/复用 MdDialog + 版本切换条 + 保存即版本 `v{序号}-{YYMMDD}` + 覆盖勾选细则 + 让 AI 追问联动 + 回收站第五块 + 存储 + 种子问题四锚点）+ 配套改动一（个人中心「我的画像」：条目式画像、AI 提炼经确认入档、注入全部 AI 上下文、预留 sqlite-vec）+ 配套改动二（AI 边栏频道制：四频道、独立历史与人设、channel 字段向后兼容）+ v2 备选三条。补充的设计细节：新问题创建即建空白 v1 并直开编辑态、追问为多轮对话且 AI 只提问不代笔。**待开发者审核**，通过后生成 designs-specs.md；总需求文档条目 3/5/6/9/12 的同步修改建议一并提交审核（AI 不直接改总需求文档）。
- git 提交由开发者执行。

## 优化建议区：第11轮——AI 编撰格言可编辑 + AI 边栏消息即时上屏

- **AI 编撰格言可编辑**：`MottosModule.tsx` 行操作区编辑按钮原仅正式区显示，AI 编撰条生成后落草稿区够不着 → 条件改为「正式区全量 + 草稿/沉淀区中 AI 编撰条」，判定 `origin==='ai' && gen_kind!=='excerpt'` 与 AI 徽章同款。摘录条/手动录入条在草稿、沉淀区不放开；正式区维持原有全量可编辑。`mottos:update` IPC 与分区无关，无后端改动。
- **AI 边栏消息即时上屏**：`AiSidebar.tsx` `send()` 改乐观渲染——会话就绪后先把用户消息以负 id 临时 append 上屏（负 id 不渲染删除键），「思考中…」占位照旧显示在其下，滚动效果依赖 `[messages, sending]` 自动到底；`ai.chat` 完成后 `loadMessages` 用 DB 真实记录（正 id、可删除）替换临时行。请求失败保留乐观消息——主进程 `aiChat` 先落库用户消息再调 LLM，显示与 DB 一致；LLM 未配置仍弹「去配置」。
- 文档同步：优化建议区归档第 11 轮。
- `npm run typecheck` 双配置通过；git 提交由开发者执行。

## 优化建议区：第12轮——灵感泉看板排版

- **问题**：四列看板均分宽度，卡片内容区仅约 180px；标题单行 nowrap 省略放不下几个字，正文完全不显示。
- **改动**：
  - `InspirationsModule.tsx`：`load()` 列表加载后批量 `md.read` 生成正文预览（剥离 `# 标题` 行、压平空白、取前 100 字，失败静默为空）存 `previews` state；卡片结构改为「标题（换行）→ 正文预览（有才显示）→ 日期行（AI 徽标 + 日期）」。
  - `App.css`：`.kanban` 4 列 → 2×2（≤1100px 单列）；`.kanban-card .row-title` 覆盖全局单行省略为自动换行 + `-webkit-line-clamp: 3`；新增 `.row-preview`（灰色小字 clamp 3）；卡片 `align-items: flex-start` 按钮顶对齐；删除不再引用的 `.row-title-wrap` 两条规则。
- 手动灵感条正文为空（md 仅标题头），只显示换行标题 + 日期；AI 条带 ≤50 字简介可整体看全。完整内容仍点卡片开 md 弹窗。
- 文档同步：优化建议区归档第 12 轮。
- `npm run typecheck` 双配置通过；git 提交由开发者执行。

## 致知己实施：designs-specs.md 生成 + 全量落地（DB v9）

- **designs-specs.md**：design.md 经开发者审核通过（「生成 designs-specs.md 开工」），生成 `docs/project/需求（功能模块）/致知己/designs-specs.md`——命名常量/数据表 v9/模块本体/画像/频道制/回收站/验收清单八节，含两个实现期敲定的协议：画像提炼标记 `<<<PROFILE_SUGGEST:类别|内容>>>`、版本文件名 `{qid}-v{seq}.md`（日期存 DB，覆盖不重命名）。
- **数据层（DB v9）**：`zhijiji_questions` + `zhijiji_versions` + `profile_facts` 三新表；`ai_sessions` 加 `channel`（DEFAULT 'assistant' 兜底，存量零迁移）；`md/zhijiji` 目录；种子问题四锚点；`yyMMdd()` 工具。
- **主进程**：`services.ts` 会话全面频道化（list/create/active/delete 带 channel、各频道独立激活 settings key、`appendSystemToChannelSession`）、四频道人设 + `profileBlock()` 画像注入（边栏对话/格言/万象卡片/测一测/灵感生成与完善/辩真综合）+ 画像提炼指令、`editAiMessage`；`ipc.ts` 新增 `zhijiji:*`（list/createQuestion/versions/saveNewVersion/overwriteVersion/renameQuestion/discard）与 `profile:*`（list/add/update/delete），`ai:chat` 带 channel、辩真过程消息推核查频道；`recycle.ts` 致知己来源（恢复触碰 updated_at 浮顶、彻底删除先收齐版本 md 再删行）。
- **渲染层**：`ZhijijiModule.tsx` 新建（主列表/新问题创建即开 v1 编辑态/删除确认）；`MdDialog` 加 `versioned`（版本切换条+保存即版本+覆盖勾选+让 AI 追问）与 `autoEdit`（仅首次打开生效，版本切换不触发）扩展；`AiSidebar.tsx` 重构为频道制（频道切换条、refs 化 sid/channel/sending 防切频道后乐观值、画像建议卡片加入/忽略）——**第11轮乐观上屏在重构中一度被覆盖（并行会话改动冲突），已按日志描述补回**；`App.tsx` 注册第七模块（致知己，self_improvement 图标）+ 模块→频道映射 + pending{ text, channel, auto } 联动；`RecycleModule` 第五页签；`ProfileModule` 我的画像节（datalist 类别 + AI 徽标 + useModuleActivated 刷新）。
- **桥接**：preload.ts / api.d.ts 同步（zhijiji/profile 命名空间、ai.chat channel、ai.editMessage、aiSession 频道参数、回收站 source 联合类型、ZhijijiQuestion/ZhijijiVersion/ProfileFactRow/AiChannel 类型）。
- **文档**：总需求文档条目 3/5/6/9/12 同步（五板块/七模块/频道制/md 弹窗适用清单/数据清单）；问题疑惑区归档第 7 轮。
- `npm run typecheck` 双配置通过；待人工冒烟（specs §7 验收清单）；git 提交由开发者执行。

## 优化建议区：第13轮——AI 初始化 v0 + 弹窗内嵌追问栏 + 画像记忆化

- **AI 初始化答案 v0**：新问题弹窗加「AI 初始化答案」勾选——勾选后 `zhijiji:createQuestion(title, tags, aiInit)` 先由 LLM 生成初始参考答案（含画像摘要、≤600 字、剥代码围栏），以 **v0（seq=0）**入库，正文顶部注明「AI 初始化的参考答案，请在此基础上写出你自己的 v1」；弹窗渲染态打开供阅读（空白 v1 场景仍直开编辑态，按返回 mdPath 是否 `-v0.md` 区分）。生成期间按钮「AI 思考中…」；LLM 未配置弹引导；失败抛错**不创建问题**（不落半截数据）。用户在 v0 上编辑保存即 v1（`MAX(seq)+1` 自然从 1 起）。
- **弹窗内嵌追问栏**：`MdDialog` 新增 `sidePanel` prop——传入则 `.dialog-split` 双栏（正文区 + 右侧 320px `dialog-side`），弹窗加宽 `min(1080px, 95vw)`，其他模块不传布局不变。致知己弹窗右栏为 `ZhijijiChat`：**致知己·追问频道的会话视图**（与全局 AI 边栏同频道同数据）——消息列表（复用 ai-msg 样式，MdView 渲染助手消息，协议标记剥除显示）+ 输入发送 + 乐观上屏 + 思考中占位；「让 AI 追问」按钮改经 `askPrompt` 状态直发本栏，**交互不出弹窗**。ZhijijiModule 相应去掉 onOpenAi prop。
- **画像记忆化**（不全量注入，AI 按需取用）：`services.ts` 拆 `profileIndex()`（类别+24 字摘要索引，边栏 system prompt 用）+ `profileDigest()`（60 字/条压缩摘要，生成类用）+ `lookupProfileFacts(keyword)`（类别/内容包含匹配）；新增 `PROFILE_LOOKUP` 协议与 `chatWithProfileLookup()`——AI 首轮回复带 `<<<PROFILE_LOOKUP:关键词>>>` 时注入命中画像条目再答一轮（最多补一轮，最终剥除标记），仅边栏对话走此环；生成类（格言/万象/测一测/灵感生成与完善）注入 digest；**辩真验证不再注入画像**（观点核查与画像无关）。`profileBlock()` 移除。
- 文档同步：致知己 designs-specs.md §2/§3/§4/验收清单、个人中心画像两处文案改记忆化表述；优化建议区归档第 13 轮。
- `npm run typecheck` 双配置通过；git 提交由开发者执行。
- **排障记录（AI 初始化 v0 反馈无效果）**：开发者实测勾选后秒开空白 v1。逐层核查渲染层/preload/主进程磁盘代码均正确，证据定位——运行中的 Electron dev 进程启动于 10:14，而第 13 轮 electron/ 改动 10:57 才写入：**HMR 只更新渲染层（勾选框可见），主进程与 preload 仍是旧版**，旧 preload 的 createQuestion 只转发两参、aiInit 被丢弃 → 旧主进程建空白 v1 秒回。非代码缺陷，完全重启 `npm run dev` 即恢复。经验：electron/ 下改动必须重启 dev 进程才生效。

## 优化建议区：第14轮——追问栏收起/拖宽/多会话 + /clear /compact

- **背景澄清**：开发者三条诉求均针对**致知己弹窗内嵌追问栏**（全局右侧边栏的展开/收起与拖宽 v1 即有）；并确认疑问——**会话历史就是 AI 上下文**（aiChat 每次带当前会话最近 30 条消息给模型），不同问题共用一会话确会互相污染。
- **收起/展开**：追问栏头部「›」收起为 32px 竖条（PanelRail，竖排「追问」），「‹」展开；收起态持久化 `settings.zj_panel_collapsed`；「让 AI 追问」触发时若收起自动展开。
- **拖宽**：栏左缘 7px 拖条（同全局边栏 ai-resizer 模式），范围 240–560px，实时生效松手持久化 `settings.zj_panel_width`；`.dialog-side` 改 `width:auto` 由 `.zj-chat` 内联宽度接管，弹窗总宽不变、正文区（flex:1）伸缩。
- **多会话**：追问栏头部加会话切换器（当前会话标题 ▾ + popover 列表：标题+相对时间，点击切换并写 `ai_active_session_zhijiji`，与全局边栏保持同一激活）与「+」新会话；关联问题共用会话、不相关问题开新会话由用户掌握。
- **斜杠命令**（追问栏与全局边栏输入统一支持，sendText 入口拦截 `/clear`、`/compact`）：`/clear` = 新建并切到新会话（原会话保留列表）；`/compact` = 主进程新增 `compactAiSession(sessionId)`（`aiSession:compact` IPC）——历史（剥协议标记、截 8000 字）LLM 压成 ≤500 字摘要，另存新会话（标题 `{原题} · 压缩`），摘要以「【前情摘要】」为首条 user 消息落库，此后上下文 = 摘要 + 新消息，原会话原样保留。两处输入框 placeholder 提示命令。
- 文档同步：致知己 designs-specs.md §2 追问栏增强 + §4 IPC 清单；优化建议区归档第 14 轮。
- **第14轮修订（开发者反馈）**：`/clear` 语义由「开新会话」改为**真清空**——`aiSession:clear` IPC 删当前会话全部消息（`clearAiSession`），会话行与标题保留、上下文与存储一并清零，防止历史记录无限膨胀；新会话由「+」手动开启。追问栏与全局边栏两处拦截、placeholder 同步改文案。
- **第14轮补充（开发者反馈）**：追问栏会话切换器补**重命名**（hover 铅笔 → 行内编辑，Enter 确认 / Esc 取消）与**删除**（hover 垃圾桶 → 二次确认 → `aiSession.delete(id, 'zhijiji')` 连消息彻底删除；删的是激活会话时主进程同频道自动切换，追问栏 load() 跟随）——与全局边栏会话管理同款交互。
- `npm run typecheck` 双配置通过；git 提交由开发者执行（改动含 electron/，需重启 dev 生效）。
