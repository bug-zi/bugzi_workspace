# 260908 开发日志

## 书架 + 信息源实施落地（DB v19/v20 + 渲染层五组件，typecheck/build 双绿）

> 创建于 2026-09-08 20:27

- **依据与流程**：本日立项三件套（design.md + spec + designs-specs.md）经开发者确认后指令「开始实施」（跳过实施计划，写作台页面化同款先例）。新依赖六项：pdfjs-dist（v6）/fflate/fast-xml-parser/@mozilla/readability/linkedom + epubjs **0.3.93**（设计候选 `@flow/epubjs` 经查 npm 不存在，specs 预留回退生效；自带 TS 类型免 stub）。安装遇阻一笔：npmmirror 对不存在的包重定向到已死的 r.cnpmjs.org 报 ECONNRESET，换官方源确认 404 后回退原版包名用默认镜像装齐。
- **版本号实况**：并行会话海龟汤计时占了 v18——书架落 **v19**（books 单表，无 deleted_at——物理删除不入回收站）、信息源落 **v20**（feeds+articles 两表，UNIQUE(feed_id,guid) 去重 + idx_articles_feed 索引），db.ts 目录数组增 `books/`、`covers/`。两 specs 的版本号实况条款均已回填。
- **书架后端**：`electron/services/books.ts` 新建——导入链（查重=解析后标题+文件大小，force 重导；INSERT 占位→复制→解析→回填，失败回滚删行清文件）；epub 元数据 fflate 解包 + fast-xml-parser 读 container.xml→OPF（dc:title/dc:creator），封面三级定位（meta[name=cover]→properties=cover-image→href 名含 cover 的图片项）；pdf 书名取文件名、封面占位。IPC 六通道（books:list/browse/import/readFile/saveProgress/delete），**零 LLM 不接 jobId**；封面走 `bzres://root/covers/`（root 命名空间本就映射 userData 子目录，零协议改动），书籍二进制走 IPC Uint8Array 喂引擎。
- **书架前端**（`src/modules/bookshelf/` 四文件）：BookshelfModule（封面网格/进度角标「未读→%」/悬停删除键/重复导入确认→force 重导/阅读视图主栏切换）；EpubReader（scrolled 流式、主题注入取 getComputedStyle 具体色值（iframe 内用不了 CSS 变量）、CFI 恢复 display(cfi)、relocated 节流 3s 落库+卸载 flush、locations.generate(800) 异步精化百分比、上下章浮动按钮 book.spine 跳转）；PdfReader（pdfjs v6 `render({canvas,viewport})`、worker 走 vite `?url` 资产（新增 `src/assets.d.ts`）、全页占位 aspect-ratio + IntersectionObserver 懒渲染 ±150% 预读、scrollIntoView 恢复页码、offsetTop 判当前页节流落库）。
- **信息源后端**（`electron/services/feed.ts`）：fetchFeed（UA 头 + AbortSignal.timeout(15s)，fast-xml-parser 归一化 RSS2.0/Atom，时间解析失败退 fetched_at，guid 兜底 sha256 前 16 位，INSERT OR IGNORE 去重，单源失败记 fetch_error 不阻断）；fetchAll 并发 + 回读错误标记；probe/add（UNIQUE feed_url 查重「该订阅已存在」）/rename（拉取永不覆盖源名）/remove（显式两步删）；extractArticle 两级策略（剥标签 ≥500 字符不抓，否则 linkedom+Readability 提取回写缓存）；summarizeArticle（缓存秒回、不足先懒抓、剥标签截 8000 字、temperature 0.3、**不注画像**、signal 穿透 chatCompletion）；listFeeds 首次空表幂等 seed 三源（probe 真名失败退常量名）。IPC 十通道，`articles:summarize` 按 **jobId 首参 + beginJob/endJob** 接全局取消。
- **信息源前端**（`src/modules/feed/` 三文件）：FeedModule（源列表「全部文章」+各源未读徽标+失败红点悬停改删、文章列表未读加粗+预览、进模块自动拉+手动刷新+全部标已读、添加订阅弹窗「验证→订阅」两步、重命名/删源确认连文章）；ArticleView（AI 总结卡置顶——打开无缓存自动生成、jobId 取消接线（停止/已取消轻提示/LLM_NOT_CONFIGURED→GoConfigDialog 直达个人中心）、失败重试；正文 DOMPurify 消毒后渲染、双失败兜底「去原文」外链；去原文复用 `shell:openExternal`）。CSP img-src 增 `https:`（src/index.html:8）。
- **文档收尾**：两份设计记录状态行更新「已实施」并移入各自 `archive/`（README 落地归档机制）；designs-specs × 2 回填版本号实况（v19/v20）与 epubjs 落地包名、spec 引用路径改 archive/；CLAUDE.md 现状区更新（十一模块、边栏顺序去「待实施」标注、specs 14 份）；`新功能开发区.md` 待完成两条移归档区。
- **typecheck/build 双绿**（中途三处类型错全部即时修复：node:sqlite 返回值经 unknown 双断言 ×2、pdfjs v6 render 改传 canvas）。**待人工冒烟**：导入 epub/pdf→网格→阅读→翻页→退重开进度恢复；重复导入确认流；删书物理清理；首次进信息源 seed 三源自动拉；添加订阅验证流；未读加粗/点开标已读/全部已读；全文源直渲染、摘要源自动抓正文、双失败兜底；AI 总结生成/取消/重试/缓存秒开；LLM 未配置弹去配置；双主题两模块配色。git 提交由开发者执行。
- **遗留**：`idea/信息源(后面再做)/` 旧草稿仍未动（删留由开发者定）；`左侧边栏/左侧边栏.md` 边栏顺序行需开发者同步（现 App 实际顺序已含书架→信息源）。

## 优化建议区第26轮：海龟汤计时净用时 + 复盘报告懒生成（9 任务内联实施完毕）

> 创建于 2026-09-08 20:17

- **依据与流程**：优化建议区待完成两条。brainstorming 四项定稿——①计时口径=净思考用时（挂机/切模块/最小化/遮挡/关 App 不计时，失焦但可见仍计时）；②懒生成范围=破汤弃汤统一；③计时架构=主进程记账（渲染层发边界事件）；④报告语义=查看时 ensure。设计 `docs/project/左侧边栏/推理角/2026-09-08-海龟汤计时与懒生成复盘-design.md`，计划同目录同名无后缀文档，executing-plans 内联执行 9 任务全毕。
- **计时（DB v18 + 主进程记账）**：`turtle_games` 加 `active_ms`/`segment_start_at` 两列；新 IPC `turtle:timerStart/timerPause`（对非 playing 局静默 no-op，幂等），结算助手 `settleGameSegment`；`openSoup` 恢复对局清残留段（跨 run 即崩溃自愈——异常退出残留开段丢弃不计离线时间）；`main.ts` 挂 `before-quit` 调导出的 `settleTurtleTimers()` 兜底结算；App.tsx 新增 `MODULE_DEACTIVATED_EVENT`（moduleRef 捕获旧模块派发，防 strict-mode 双触发），TurtlePanel 以「模块激活 && visibilitychange 可见 && playing」合成活跃边界翻转开停，用时显示改本地段模型（payload 新增 `activeMs` 种子，每秒跳动，终局取 `durationMs`）。
- **懒生成（终局秒回 + 查看时 ensure）**：`finishTurtleGame` 拆分——删 soupReview 调用与 md 写盘，只落状态/ended_at/净用时（**弃汤/破汤即显汤底，无 LLM 等待**，`md_path` 留 NULL）；原 fmtMsg 提出为 `fmtTurtleMsg`；新 IPC `turtle:report(jobId)`——`md_path` 已存在直接返回（旧记录零变化秒开），无则现场跑点评→拼 md→写盘→回填（失败不落半截数据可重试，接 beginJob 全局取消）；渲染层 `openRecord` 改 `openReport(gameId, …, knownPath?)` 单点承接三入口（对局内按钮/记录列表行/终局汤回看），生成中按钮转圈+停止键，记录行 `md_path` 空时尾注「复盘待生成」，放弃确认弹窗文案同步；顺带修复存量瑕疵——终局后 `durationMs` 未并入 payload 致头部用时闪回 0。
- **上一节衔接**：全局取消机制给 `turtle:abandon`/`finishTurtleGame` 接的 signal 随本轮终局链拆分而移除（终局已无 LLM 段，取消语义整体移到 `turtle:report`）。
- **⚠ 版本号互备**：本日早前书架/信息源 spec 规划 DB v18/v19（未实施）——**v18 已被本轮占用**，两模块实施时按其 spec 内「版本号实况」条款顺延（书架 v19、信息源 v20），文笔坊 v17 先例同款。
- **typecheck**：本轮 8 文件改动全绿（清 tsbuildinfo 全量重查）。但清缓存后浮出 `electron/services/books.ts:157/187` 两处**存量隐性类型错**（`SQLOutputValue` 直 cast，与本轮无关、增量缓存长期掩盖，第23轮同源现象）——已向开发者汇报待处置（两行 `as unknown as` 可解）。
- **git 提交由开发者执行**；冒烟清单见计划文档 Task 9（重启 dev 触发 v18 迁移 → 计时暂停续跳/离线不计 → 弃汤秒出汤底 → 查看复盘生成/取消/重试 → 记录行标记 → 旧记录秒开）。

## 书架 + 信息源立项：design.md × 2 + designs-specs.md × 2（应开发者指令「走立项流程」）

> 创建于 2026-09-08 19:10

- **立项动作**：本日上一节 brainstorming 两份 spec 经开发者确认后立项——依 `docs/project/README.md` 机制：design.md 入 `左侧边栏/<模块>/`（应开发者指令由 AI 依会话结论起草，格式对齐文笔坊 design.md，**待开发者审核**），spec 从 `idea/` 迁入同目录（状态行更新为「已立项；待实施」，idea 空目录已清），designs-specs.md 各生成一份（开发直接依据）。本会话纯文档，零代码改动。
- **书架（`左侧边栏/书架/`）**：design.md + spec + designs-specs.md 三件套。specs 要点：ModuleId `bookshelf`（wenbi 后、recycle 前）、DB **v18** books 单表、目录数组增 `books/`+`covers/`、封面走 `bzres://root/covers/`（**零协议改动**——bzres root 命名空间本就映射 userData 子目录，本次查证 bzres.ts:26-34 确认）、书籍二进制走 `books:readFile` IPC 喂引擎、新依赖 @flow/epubjs+pdfjs-dist+fflate+fast-xml-parser、epub 元数据主进程解包提取（fflate+fast-xml-parser 读 OPF）、pdf 书名取文件名封面占位、进度 CFI/页码节流 3 秒落库、IPC 六通道（**无 LLM 通道，不接 jobId 取消机制**）、不入回收站。
- **信息源（`左侧边栏/信息源/`）**：同三件套。specs 要点：ModuleId `feed`、DB **v19** feeds+articles 两表（guid 去重、不依赖外键级联——显式两步删）、新依赖 fast-xml-parser+@mozilla/readability+linkedom、CSP `img-src` 增 `https:`（src/index.html:8）、「去原文」复用现有 `shell:openExternal`（ipc.ts:1384，本次查证确认已存在）、正文两级策略阈值 500 字符/源超时 15s、`articles:summarize` 按 **jobId 首参 + beginJob/endJob 模式**接入全局取消机制（260908 当日落地的 ai/jobs.ts，新 AI 通道从设计起即接线）、总结不注画像、首次 `feeds:list` 幂等 seed 三源。
- **版本号互备**：书架 v18 / 信息源 v19 互不依赖，实施顺序对调则版本号对调（specs 内已注记，文笔坊 v17「版本号实况」先例）。
- **CLAUDE.md 同步**：specs 计数 12→14、架构规划区补两模块（标注待实施）、边栏顺序插入「书架→信息源」（标注待实施）；「src/ 九模块」等现状描述留待实施时更新。git 提交由开发者执行。
- **遗留**：①`idea/信息源(后面再做)/`（开发者原始草稿，ursb.me 源已并入新 spec）仍未动，删留由开发者定；②`新功能开发区.md` 待完成区两条按优化建议区惯例**实施完成后**再移归档区（立项阶段不动）；③design.md 为 AI 应指令起草，开发者审核后方可作为正式设计正本；④`左侧边栏/左侧边栏.md` 的边栏顺序行开发者主导，实施时同步（AI 可协助）。

## AI 生成全局取消：Task 1-19 全量落地（主进程 signal 穿透 + 渲染层 13 组件改造）

> 创建于 2026-09-08 18:42

- **依据与验证**：`docs/project/全局/2026-09-08-AI生成全局取消.md`（方案 A：通用 jobId + 统一取消通道）逐 Task 内联实施完毕。`npm run typecheck` 双配置通过、`npm run build` 通过——上一节（18:29）遗留的 13 个中间态错误（ProfileModule/推理角三面板/辩真阁/致知己）已随本节组件改造全部收口。git 提交由开发者执行。
- **机制层（Task 1-2）**：新建 `electron/ai/jobs.ts`（jobId→AbortController 注册表：beginJob/endJob/cancelJob/ensureNotCancelled）；`llm.ts` 把已声明的 `ChatOptions.signal` 真接到 fetch（请求前检查、abort 归一抛「已取消」、429 退避与 abort race——`sleepOrAbort` 取消立即中断不等计时），`testLlmConnection` 加可选 signal；`McpSession` 构造器第三参 signal（rpc/initialized 通知 fetch 均挂），`findSearchTool` 加 signal，`mcpResearch.ts` 三处 chatCompletion 穿透 + 阶段间 ensureNotCancelled + 各降级 catch 先判取消/未配置再吞错。
- **services 层（Task 3-4）**：signal 一律末位可选参（scheduler/backfill 无 signal 调用点零改动）。对话族 aiChat/compactAiSession/chatWithProfileLookup；格言 generateMottos 首轮入库前 + 补足轮入口拦截、补足 catch 判取消上抛（已入库首轮条目按「已写库的保留」）；灵感两阶段共享 signal、解析重试 catch 均先判取消、批末入库前统一拦截（取消全批作废）；refine/copilot/万象三函数/辩真 runVerification（LLM 段全穿 + 逐组检索与写库前阶段检查，MCP fetch 经 McpSession 尽力传播）；推理角 generateSoups 出题链全链传参、重出空 catch 改判取消上抛（原会吞掉取消异常）、judge 判问判答/soupReview/思维墙两阶段/judgeWallAnswer 全穿。
- **IPC 层（Task 5-6）**：新增 `ai:cancel` 统一通道；**23 个 handler** 统一「jobId 首参 + beginJob → 调用穿 ac.signal → finally endJob」模式——设计已列 20 个 + 核对补接 3 个：`wall:answer`（每日题作答含 judgeWallAnswer）、`turtle:abandon`（终局点评段）、`aiSession:compact`（压缩含 chatCompletion）。`finishTurtleGame` 加第三参传给点评。preload.ts 与 api.d.ts 逐方法镜像（23 方法首参 jobId + `ai.cancel`），llm:models/turtle:openSoup/wall:practiceHint/wall:bankReveal 等无 LLM 通道不动。
- **终局点评取消语义（写死）**：finishTurtleGame 的 soupReview try/catch 本就降级为固定文案，signal 传入后取消点评 → catch 降级 → **终局链照常完成**（对局消息/汤状态/复盘 md 均落库）——破汤/弃汤判定在点评前已落库，终局不可因取消悬置；该窗口点「停止」表现为点评段立即结束、渲染层走成功路径。
- **渲染层（Task 7-18）**：loading 态升级持 jobId（`genJob != null` 派生布尔，JSX 零改动），catch 统一加 `msg.includes('已取消')` 轻提示分支（排 LLM_NOT_CONFIGURED 前），旁挂 `stop_circle` 取消按钮（`btn`/`btn btn-ghost`，无彩亮色）。本会话改造 11 文件：Mottos/Wiki（三态）/Inspirations（含完善预览弹窗 footer 分支切换）/Verify/Turtle（`busyJob` + 裁判思考行 `.rs-stop` 停止键，App.css 追加）/Wall/Practice/Bank/Zhijiji（主模块 createJob + 内嵌追问栏 sendJob，对话取消重载替换乐观行、/compact 共用）/AiSidebar（同款，AiSidebar.css 追加 `.ai-stop`）/Profile（三处 footer 取消）。**ArticlePage 为上一节已被动适配**（copilotJob + stop_circle 取消键），本节验证签名吻合即计——故计划中 MdDialog/WritingPanel 的 copilot 扩展位改造确认失效未做（扩展位已在页面化时删除），实际组件落点 12 文件。
- **待人工冒烟**（设计 §验证）：每模块任选一处生成中取消 → 立即停止、toast「已取消」、按钮恢复、可立即重试；格言/灵感取消后库内无残留半批；海龟汤取消后汤库仅完整碗；对话取消后无 assistant 占位；copilot 取消后建议卡不出现；429 退避等待中取消立即生效；取消已结束任务无害静默。
- **归档提示**：design 与 plan 两文档待开发者验收后移入 `docs/project/全局/archive/`（按 docs/project/README.md 机制）。

## 新模块 brainstorming：书架 + 信息源（idea 阶段设计记录两份）

> 创建于 2026-09-08 18:39

- **依据**：`docs/project/新功能开发区.md` 待完成区两条任务。本会话纯设计（brainstorming 流程，逐题澄清→双方案对比→分节呈现确认），零代码改动；typecheck 状态与上节相同（并行会话中间态，非本会话引入）。
- **书架（`idea/书架/2026-09-08-书架-design.md`）**：纯本地电子书阅读——epub.js（@flow/epubjs fork）+ pdfjs-dist 双引擎、主栏内切换（书架↔阅读）、复制入库（`<userData>/books/`+`covers/`，bzres:// 需补两目录映射）、进度记忆（epub 存 CFI+百分比、pdf 存页码，节流 3 秒落库）；范围刻意收窄为纯阅读（书签/高亮/笔记进 v2 备选）；删除二次确认直接彻底删，不入回收站。
- **信息源（`idea/信息源/2026-09-08-信息源-design.md`）**：精简版 Folo——主进程全本地链路（fetch + fast-xml-parser + readability/linkedom 抓正文兜底）、三段式（源列表+文章列表+阅读视图）、AI 总结**打开时按需生成+缓存**（token 友好，复用现有 LLM 服务）、正文一套数据喂总结与阅读两用、基础已读/未读；进模块自动拉+手动刷新；首次进入预置 tw93 weekly/aiznb weekly/ursb.blog 三源。
- **跨模块共识**：两模块均不入回收站（保持八块）；边栏顺序在文笔坊后追加「书架→信息源」；均不新增 AI 边栏频道。
- **遗留待开发者处置**：`idea/信息源(后面再做)/`（开发者原始草稿，其中 ursb.me 源已并入新 spec）未动——是否删除或留档由开发者定；两 spec 待开发者审阅，立项后迁入 `左侧边栏/` 并生成 designs-specs.md。

## 优化建议区第25轮反馈修订：编辑⇄预览切换保持滚动位置 + ArticlePage 接入全局取消

> 创建于 2026-09-08 18:29

- **滚动位置保持（开发者冒烟反馈）**：症状——文章页切预览/编辑会跳回开头。根因——两态滚动容器不同（编辑态 textarea 内部滚动、预览态 `.article-body` 滚动），切换即换容器，scrollTop 自然丢。修法（`ArticlePage.tsx`）——切换瞬间按比例捕获（`scrollTop / (scrollHeight - clientHeight)`，`captureScroll`），新态渲染完成后等比恢复（mode effect 内 rAF，等预览 innerHTML 注入后再量取新容器高度）；两态内容高度不等（原文 vs 渲染后 md），像素不可直映，比例近似长文下误差可接受。捕获时机：toPreview 在 `await flush` 之后、`setMode` 之前（flush 重渲染不动 textarea scrollTop，越晚取越准）；toEdit 同步直取。
- **ArticlePage 接入全局取消（被动适配）**：本会话上一节落地写作台页面化后，并行会话的「AI 生成全局取消」也已落盘——全部 AI IPC 加 `jobId` 首参 + `ai.cancel` 统一取消通道（`electron/ai/jobs.ts`），ArticlePage 的 `wenbi.copilot` 调用按新签名适配：`crypto.randomUUID()` 生成 jobId（WikiModule 同款）、生成中工具栏挂 `stop_circle` 取消键、catch 增「已取消」轻提示分支（排在 LLM_NOT_CONFIGURED 前）。上一节预警的「计划中 MdDialog copilot 扩展位加 jobId」一项已随扩展位删除失效，实际落点即本处。
- **typecheck 注意**：本会话文件（ArticlePage/WritingPanel/MdDialog/WikiModule）零报错；当前全量 typecheck 余 13 个错误在 ProfileModule/推理角三面板/辩真阁/致知己——系并行会话全局取消改造进行中的中间态，非本会话改动，留该会话收口。
- 待冒烟：长文下编辑⇄预览往返位置基本保持（比例近似，前后文结构差异大处会有小幅偏移属预期）；协笔生成中取消键生效。

## 优化建议区第25轮：万象知识卡板块标签 + 写作台页面化（双项落地）

> 创建于 2026-09-08 18:17

- **依据与流程**：两份设计文档当日定稿后开发者指示直接实施（跳过实施计划编写）——`左侧边栏/万象库/2026-09-08-知识卡板块标签-design.md`、`左侧边栏/文笔坊/2026-09-08-写作台页面化-design.md`，实施后均已更新状态行并归档至所属模块 `archive/`（万象库 archive 为本次新建）。
- **板块标签（万象库）**：MdDialog 新增 `titleTag?` 可选 prop——标题行改横排 `.dialog-title-line`（标题省略/悬停全文、pill `flex-shrink:0` 不被挤掉，可编辑标题模块 input 由 width:100% 改 flex 填充不冲突），右侧主题色小胶囊 `.dialog-title-tag`（primary-soft 底+主题色描边+primary-deep 文字、0.75em、999px）；WikiModule 传 `sections.find(...)?.name`，板块页/总览/笔记本跳转/生成审核四入口自动生效；板块查不到不渲染不报错；其余使用方不传不变，零后端改动。实施一笔：文字色用 `--color-primary-deep` 而非字面 primary——浅色主题 primary(#e8a0bf) 在 primary-soft(#f8bbd0) 底上不可读，deep 即主题色的文字安全档（md-view 链接同款先例）。
- **写作台页面化（文笔坊）**：新建 `ArticlePage.tsx`+`ArticlePage.css`，WritingPanel 的 openDoc 语义弹窗→页面（非空渲染 `<ArticlePage key={id}>`、为空渲染看板；新建文章创建后直接进页面）。工具栏：←看板｜标题行内编辑（回车/失焦 rename、Esc 还原）｜区标签 pill｜编辑⇄预览｜Copilot 四键（仅编辑态）｜去空白字数实时。排版：限宽 46em 居中；编辑态 textarea `font: inherit` 跟随个人中心字体、行高 2.0、透明底主题色光标、占满高度内部滚动；预览态复用 renderMd，行高 1.9+h1/h2/h3 层级+段距 1em（scoped `.article-md`）。自动保存：停止输入 800ms 落盘+touch（草稿本先例），flush 兜底四时机（切预览/返回/模块失活/卸载），无「完成」按钮。
- **布局实现要点**：含文章页时 `.module-page:has(.article-page)` 定高 `calc(100vh - 36px)`（main-area 上下 padding）+ `.writing-panel` flex:1，令工具栏常驻、正文区内部滚动、建议卡沉底；纯看板时 `:has` 不匹配布局不变。
- **Copilot 移植与 MdDialog 减法**：选区跟踪/四动作/建议卡/采纳逻辑整套从 MdDialog 搬至 ArticlePage（`wenbi.copilot` IPC 不动），新增一处增强——请求前先 flush，主进程 copilotWriting 从落盘正文取上下文，防 800ms 防抖窗口内的输入 AI 看不到；LLM 未配置去配置分流留页面（GoConfigDialog 仍由 WritingPanel 持有）。MdDialog 侧纯减法：copilot prop、taRef/selRef/copilotBusy/suggestion、runCopilot/adoptSuggestion/trackSel、编辑态工具行、建议卡 UI、App.css `.md-copilot-*` 样式块全删；typecheck 兜底确认其余 7 个使用方无感。
- **交叉影响一笔**：顶部「AI 生成全局取消」实施计划（Task 1-19，待审未实施）中「MdDialog copilot.request 扩展位加 jobId 第三参，由 WritingPanel 包装」一项已失效——copilot 扩展位本轮删除，实施该计划时需改在 ArticlePage 的 `runCopilot` 落点接取消。
- 浮生记/看板/后端/DB 零改动。`npm run typecheck` 双配置通过；**待人工冒烟**（板块标签四入口+标题超长 pill 不挤；新建→默认编辑态聚焦→800ms 落盘重进验证→两态切换内容一致→返回看板刷新→keep-alive 切模块往返原样→Copilot 四动作建议卡采纳→浮生记弹窗不受影响）；git 提交由开发者执行。

## AI 生成全局取消：实施计划编写（待开发者审核）

> 创建于 2026-09-08 13:47

- **依据**：`docs/project/全局/2026-09-08-AI生成全局取消-design.md`（方案 A，设计已逐节确认）。本会话通读机制层与渲染层全部相关代码（electron/{ai/{llm,services,mcp,mcpResearch},ipc,preload,scheduler} + 13 个组件文件），产出实施计划 `docs/project/全局/2026-09-08-AI生成全局取消.md`（Task 1-19）。
- **逐 handler 核对偏差（按设计预留口子补接 3 处，设计写 18 实施为 23）**：`wall:answer`（每日题判答 judgeWallAnswer 含 LLM，设计取消语义表已覆盖「作答」）；`turtle:abandon`（finishTurtleGame→soupReview 终局点评）；`aiSession:compact`（/compact 含 chatCompletion，AiSidebar 与致知己追问栏均有等待态）。核对确认不接：llm:models（快速 GET 非生成）、backfillTrickNotes（启动后台）、定时格言任务——services signal 形参一律可选以兼容无 signal 调用点。
- **两处语义在计划中写死待审**：①格言补足轮取消——首轮已入库条目按「已写库的保留」留库（「无残留半批」验证项指主路径）；②海龟汤终局点评取消=点评降级固定文案、终局链照常完成（判定消息在点评前已落库，不可悬置）。
- **组件实数 13 文件**（设计写 10：思维墙拆 WallPanel/PracticePanel/BankPanel 三文件、追问栏 ZhijijiChat 内嵌于 ZhijijiModule）。渲染层方案：loading 态升级持 jobId + 派生布尔保持 JSX 零改动；取消按钮统一 `stop_circle` + btn 类；catch 新增「已取消」轻提示分支。MdDialog copilot.request 扩展位按设计加 `jobId` 第三参，由 WritingPanel 包装。
- 开发者选择**先审计划再实施**，实施未开始；git 提交由开发者执行。

## 优化建议区第24轮：格言删除记忆墓碑（防「来10条格言」复现已删格言）

- **背景（brainstorming 三问定稿）**：待完成区「已删格言在生成中复现」。摸底发现生成查重双保险（prompt 避免清单 500 条含回收站软删行 + 代码侧 `isDupMotto` 判重）本就覆盖回收站，真正空洞在三条**物理删除**路径（回收站彻底删除 / 3 天自动清理 / 模块内直接删除）——行被 DELETE 后查重依据归零。三项决策：墓碑只挂物理删除钩子；补足统一一轮（墓碑/库内/句式三种剔除一律按 5/5 配比补缺口，最多两轮，失败非致命）；墓碑永久留底 + prompt 注入限量 300。
- **改动（7 文件）**：①`db.ts`——DB v16 新表 `motto_tombstones`（content / content_norm 预计算 / created_at，只增不删、无索引）+ `recordMottoTombstone`（按 norm 去重防重复行）；②`recycle.ts` `hardDelete` mottos 分支删前写墓碑（手动彻底删除 + 自动清理两路径共用一处）；③`ipc.ts` `mottos:deleteForever` 删前写墓碑；④`ai/services.ts` `generateMottos` 重构——墓碑两端接入（prompt 追加「已删除格言清单」段 ≤300 条 / 代码侧全量 norms 并入判重，墓碑先判归因 `tombstoneRejected`、库内后判维持静默）+ `buildPrompt`/`callAndParse`/`insertBatch` 三内部件参数化 + 补足轮（缺口按入库计数配比，本批已入库进避免清单，重跑同一套过滤，try/catch 非致命）；`GenerateMottosResult` 增 `tombstoneRejected`/`supplemented`；⑤`preload.ts` + `api.d.ts` 返回类型透传；⑥`MottosModule` 生成 toast 增「其中 M 条为补足生成」「K 条与已删除格言重复已剔除」两个条件从句。手动新增不判墓碑（手动加回同一句是用户自由）。
- **实施偏差一笔**：计划稿的 headSort 写法（`if (x == null) x = 1` 后在闭包内使用）被 `insertBatch` 闭包捕获后失去 null 收窄（TS18047），改三元初始化归一，语义不变，计划文档已同步修正。
- 设计记录 `docs/project/左侧边栏/格言库/2026-09-07-格言删除记忆墓碑-design.md`，实施计划同目录同名（无 -design 后缀）；`npm run typecheck` 双配置通过；**待人工冒烟**（三删除路径墓碑写入 / 同句重复删不产生重复行 / 彻底删除后生成不复现 / 补足与剔除 toast / 断 LLM 失败路径）；改动含 electron/db，需重启 dev 进程触发 v16 迁移；git 提交由开发者执行。

## 文笔坊模块实施（DB v17 + 双板块浮生记/写作台 + Copilot 协笔 + 回收站第八块）

> 创建于 2026-09-08 00:26

- **依据与执行方式**：`左侧边栏/文笔坊/designs-specs.md`（260907 brainstorming → 立项 → 开发者审核通过）+ 实施计划同目录 `2026-09-07-文笔坊.md`（Task 1-10）。子代理驱动起步——组A（DB+类型层）经实施+规格+质量两阶段审查闭环；开发者要求提速后转本会话内联执行：组B被中止现场盘点（已完成七成：ipc 12 通道/recycle 类型层均落）补齐缺件，组C-G 内联完成。
- **版本与命名实况**：DB v16 已被并行会话格言墓碑（第24轮）占用 → 文笔坊落 **v17**（计划预案生效）；质量审查采纳**表名复数化** `wenbi_journals`/`wenbi_articles`（全库复数惯例，趁 IPC 未动工在组A闭环前改名），索引 `idx_wenbi_articles_zone`；RecycleSource slug（wenbi_journal/wenbi_article）与 md 目录（md/wenbi/{journal,article}/，对齐 md/turtle 先例）保持单数。
- **主进程**：v17 两表 + ensureDirs 双目录；IPC 12 通道（journal 4 + article 7 含 `wenbi:articleExport` showSaveDialog 导出 + `wenbi:copilot`）；services.ts `copilotWriting` 四分支（起稿/续写 temp 0.7、润色/改写 0.4，注入 profileDigest，只读 wenbi_articles——**浮生记路径零 AI 边界硬性**：md/wenbi/journal/ 永不进任何 prompt）；回收站 RecycleSource 增两来源（恢复分线：浮生记清标记、文章回构思区区末 sort=MAX+1；hardDelete 走 MD_FIELDS 通用路径连 md 删；顺手补 shared/types RecycleItem source 联合欠账）。
- **渲染层**：`WenbiModule` 双板块壳（照 ReasoningModule：.recycle-tabs 切换 + module-live/module-hidden 常驻挂载，默认浮生记不持久化）；`JournalPanel` 浮生记（按月/按周分节〔周=周一始〕、大事件置顶小节、新建 autoEdit 直进编辑态、行=创建日期+首行摘要、零 AI）；`WritingPanel` 四区看板（灵感泉同构拖拽/区内排序归一化 + more_horiz 菜单移动/复制 Markdown/导出 + MdDialog onTitleChange 改名不动 updated_at、articleTouch 编辑保存浮顶）；MdDialog 两个新扩展位——`eventToggle`（编辑态头部大事件开关，切换即时落库）+ `copilot`（编辑态工具行四按钮〔起稿/续写常亮，润色/改写需选区〕+ 建议预览卡〔原文/AI 版对照 + 采纳/放弃/重新生成〕，采纳只改 textarea 草稿不落盘、随「完成」一并保存；spec 原案「选区浮动条」简化为工具行禁用态——textarea 无原生选区坐标 API，镜像 div 计算脆弱，specs §4.1 已同步措辞）；RecycleModule 第八块页签（两来源混排 + srcTag 来源小字）；App.css 增 `.md-copilot-*` 与 `.journal-*` 样式（主题变量）。
- **specs 同步 5 处**：§1 DB v17+复数表名实况注、§4.1 工具行措辞、§6 补 `wenbi:articleReorder` 行、§0 AiChannel 映射句（默认 assistant 无需改代码）、§8 验收 v17；回收站 designs-specs 同步八块/九值（含草稿本第21轮欠账一并补上）。spec 状态行改「已实施」并随 plan 按机制归档 `archive/`。
- `npm run typecheck` 双配置通过、`npm run build` 通过；**待人工冒烟**（双板块切换；浮生记新建 autoEdit/按周按月分节/大事件标记与置顶/删除恢复；写作台四区拖拽/标题编辑/copilot 四操作建议卡采纳放弃重生成/复制导出/删除恢复回构思区；回收站第八块混放与分线恢复；LLM 未配置时 copilot 点击弹去配置）；改动含 electron/db，需重启 dev 进程触发 v17 迁移；git 提交由开发者执行。
