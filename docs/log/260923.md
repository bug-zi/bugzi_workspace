# 260923 开发日志

## 超级工作台 2.0 二期·批次 D（科普线）落地

> 创建于 2026-09-23 00:02

依《260922-超级工作台2.0-二期实施计划.md》执行批次 D，§0 五确认项按计划默认推荐执行。开发前先生成 `docs/project/idea/超级工作台2.0/260922-超级工作台2.0-designs-specs-批次D-科普线.md`（含两点工程化细化：中文轻加工改逐块 JSON 续跑防长文输出截断；`embed_science` 留批次 F 注册后再入队，避免批间「任务类型未注册」失败），随后实现，`npm run typecheck` + `npm run build` 全绿。git 未动（纪律）。

### D1 DB v51 + 类型

- `electron/db/db.ts`：新增 `science_articles`（en/zh CHECK、concepts/glossary JSON 列、status ready/meta_only）、`science_highlights` 两表；`interpretations` 同事务（显式 BEGIN/COMMIT/ROLLBACK）重建，kind CHECK 扩 `'light'`，`idx_interpret_owner` 随 DROP 重建；`initDb` 目录清单补 `science`。迁移前核实 db.ts 实际最新版 v50，v51 未被挤占。
- `src/shared/types.ts` + `src/renderer/api.d.ts`：新增 `ScienceArticleRow`（hydrate 后形态，联 domain_name）、`ScienceConcept`、`ScienceHighlightRow`；`InterpretationRow.kind` 扩 `'light'`。

### D2 科普海选

- 新建 `electron/services/agent/collectScience.ts`：`collect_science` 任务（priority 10 / singleton / maxRetries 2），仅 MCP 渠道（渠道注册表 `CHANNELS` 留扩展点），科普向海选 prompt（排除学术论文/新闻稿/营销文，无结果合法，language 由 LLM 判定）；url 防编造白名单改为从 MCP 原文正则抽取（深读线的白名单依赖 arXiv 结构化候选，科普线无结构化候选）；去重复用 collectDeep 导出件（url_hash + `isNearDuplicate` >0.92 + `embedDiscoverVector`，近重复判定跨线共享 discover 向量池）。
- `collectDeep.ts` 改 export：`extractJson` / `matchesWhitelist` / `candidateMetaMap` / `isNearDuplicate` / `embedDiscoverVector` / `Selection`。
- `bootstrap.ts`：搜集器按 `domain.track` 路由（science→`collect_science`），side-effect import 补 `./collectScience`、`./interpretScience`；错峰巡检/预算/负载暂停两线同口径自动生效。
- `ipc.ts agent:runCollect`：查域后按 track 入队（渲染层签名不变）。

### D3 转正分流 + 全文抓取

- 新建 `electron/services/agent/science.ts`：`scienceAcceptRow`（discover→science_articles、science_fetch 入队）、`science_fetch` 任务（politeFetch+Readability 抽正文存 `science/{id}.txt`，失败 meta_only 不炸，完成后按语言自动入队 en→science_translate / zh→science_light）、`retryScienceFetch`、`deleteScienceArticle`（先库后文件连产物/高光/链接/向量）、高光登记/解除、`linkConcepts`/`mergeScienceConcepts`/`scienceLinkManual`、`listLinks`（双向合并联标题）/`entitySearch`/`addLink`/`deleteLink`（批次 F 复用）。
- `papers.ts acceptDiscover`：`source_type==='article'` 分流 `scienceAcceptRow`，返回类型放宽 `{paperId} | {scienceId}`；渲染层调用零改动。

### D4 科普解读 + 追问

- 新建 `electron/services/agent/interpretScience.ts`：`science_translate`（术语表先行存 `science_articles.glossary` → 逐章翻译 `## 第 N 章` 续跑，产物 `sa-{id}-translate.md`）、`science_light`（逐块 jsonMode `{md, concepts}`——完整加注版全文、`### ` 结构导航、术语括注、`[术语](wiki://术语)` 内链、硬约束不删减信息点；`## 第 N 部分` 续跑，concepts 跨块聚合去重封顶 30）、`science_lecture`（按需）；均 wrapMaterial 包裹 + token 记账（scene `agent:s-*`）+ meta_only 退化摘要版卡头注明；完成尾 mergeScienceConcepts + linkConcepts。`runScienceInterpret` 照 `runInterpret` 口径（INTERPRET_EXISTS）。
- `ask.ts` 重构为 owner 模式 `{ type: 'paper' | 'science', id }`：science 注入解读产物（translation||light 优先）|| 摘要+全文节选 40k，标记 `<<<LIT:SA:{id}>>>`（aiChat 的 `LIKE '%<<<LIT:%'` 零改动）；`agent:askLiterature(type, id)`，LiteraturePanel 调用点迁移。

### D5 双向链接 + MdDialog

- 自动建链：concepts 逐 term 匹配 wiki_entries（精确=1 → 双向模糊=0.8，排除 pool/已删），similarity 链接 + entry_id 回填；「建词条」成功走 `agent:scienceLinkManual`（concepts 回填 + manual 链接，语义归位照确认项 #3）。
- 新 IPC：`agent:linksList / entitySearch / linkAdd / linkDel / scienceLinkManual / scienceHighlightAdd / scienceHighlightRemove / scienceList / scienceDetail / scienceDelete / scienceRetryFetch / scienceInterpret`；preload 增 `science` / `links` 命名空间，api.d.ts 同步。
- `MdDialog.tsx`：可选 prop `onWikiLink`——渲染拦截 `wiki://` 前缀链接回调术语（decodeURIComponent 容错），prop 缺省行为零变化。

### D6 万象库科普页签 + 阅读视图

- 新建 `src/modules/wiki/SciencePanel.tsx` + `science.css`：科普海选卡（science 领域下拉）、文章列表（领域/语言/日期/关联词条数/状态徽章）、解读阅读视图（MdDialog readOnly + 划词高光/取消高光〔md `==标记==` 写回产物 + science_highlights〕+ 问AI + footerBar：解读版/精讲/追问 chips、原文出处/重试抓取、关联词条 chips〔命中开词条、未命中「＋建」走建词条流〕）；实时刷新订阅 `onAgentStatus`（collect_science / science_fetch / science_* 三类事件）。
- `WikiModule.tsx`：四页签「百科 | 科普 | 辩真 | 问答」（确认项 #1）；SciencePanel keep-alive 挂载，回调注入（onOpenEntry / onCreateEntry 包装既有 runGenerate〔改返回 entryId〕，万象生成链路保持单点）；词条弹窗 footerBar 增「出自科普文章」回链（linksList 过滤 science_article，点击切科普页签直达该文）。

### D7 文献页签发现箱分组

- `LiteraturePanel.tsx`：发现箱按 source_type 分「论文 / 科普」两组（行同构）；AI 海选卡扩科普领域第二选择器（`runSciCollect`）；接受 toast 按类型分流；`collect_science` 事件并入实时刷新；正式文献「相关内容」词条跳转占位保持（批次 F 统一）。

### 待办

- 批次 D 交开发者按 spec §10 验收清单（6 条）在 App 内验证；通过后进批次 E（书籍解读）。

## 学习/生活双模式 + 工作台入口迁移 + 总导览随模式换块（优化建议区第50轮）

> 创建于 2026-09-23 00:16

依《docs/project/全局/archive/2026-09-22-学习生活双模式-design.md》（brainstorming 六决策拍板）与同目录 plan 执行，8 任务全部完成。改动四个文件：`src/shared/types.ts`（新 `ModuleMode` 类型 + settings 三键 `app_mode`/`mode_last_learn`/`mode_last_life`，零 DB 迁移）、`src/App.tsx`（MODULES 加 `mode` 字段；左栏按当前模式过滤、主栏 keep-alive 仍全量挂载；工作台呼吸灯自底部迁至总导览后第 2 位随学习库行插位；`appMode` 状态 + 启动恢复 + `modeOfView`/`switchMode`（带 `landTo` 显式落点参数，防异步 last 抢占深链跳转）+ last 专属模块记忆（常驻不记）；深链与任务中心直达的模式跟随；右下角列尾新增 `.right-col-mode` 单按钮往返，自下而上=模式→主题→音源→终端→AI 活动）、`src/App.css`（`.right-col-mode` 样式同款钉宽/边框/hover）、`src/modules/zonglan/OverviewModule.tsx`（接 `mode` prop 随模式换块：公共=问候/我是谁/挑战/热力图；学习=今日学习/待学习区/超级工作台/数字条·信息源未读/格言轮播；生活=每日一题/续读/数字条·本月支出+十二问题；块内逻辑与加载照旧）。实施中与并行会话批次 D 在 `types.ts` 发生同文件竞态（两处编辑一度失配），核验后双方改动均完整落盘无覆盖。typecheck 双配置 + build 通过。**待冒烟**：双模式左栏两套 9 项序列、工作台第 2 位三态、切换按钮位置/图标显目标/四面板展开隐藏、切换落点 last 记忆（常驻不记）、重启恢复模式、深链跟随（debugzi toast/回收站恢复/任务中心直达）、总导览两模式块组合与数字条拆源、keep-alive 任务不中断、双主题——九项清单见 plan Task 7。CLAUDE.md 与优化建议区归档已同步；设计/计划文档待冒烟通过后移入 `docs/project/全局/archive/`。git 未动（纪律）。

## 超级工作台 2.0 二期·批次 E（书籍解读）落地

> 创建于 2026-09-23 00:20

依《260922-超级工作台2.0-二期实施计划.md》§4 执行批次 E。开发前先生成 `docs/project/idea/超级工作台2.0/260922-超级工作台2.0-designs-specs-批次E-书籍解读.md`，随后实现，typecheck + build 全绿（与并行会话「学习/生活双模式」改动的合并态复验通过）。git 未动（纪律）。零 DB 迁移（book_digest 在 v48 CHECK 已合法，状态以 interpretations 库行为准）。

### E1 书籍全文抽取

- 新建 `electron/services/agent/booktext.ts`：`ensureBookFulltext`（缓存 `books/{id}.txt` ≥200 字符直接复用）——epub 走 `unzipSync`（books.ts 同款 fflate/XMLParser 容错骨架）container.xml→OPF→**spine 顺序**逐项 xhtml 剥标签（去 style/script/head 块 + 实体解码；href 相对 OPF 目录 + decodeURIComponent 容错），spine 缺失兜底 manifest 全量 xhtml；pdf 复用 `papers.extractPdfText`（本批改 export）；<200 字符抛明确错误（扫描版/异构包）不写缓存不炸其他功能。附 `extractOutline`（章节标题行扫描，≤80 行，供超长书导读结构感知）。

### E2 book_digest 任务 + 追问扩展

- 新建 `electron/services/agent/bookDigest.ts`：`book_digest` 任务（priority 15 / singleton / maxRetries 1，ipc.ts import 即注册）。状态机以库行为准：runner 开始自愈残留 running 行→failed 再置 running（规避 queue.runningRuns 不带 refId 的盲区，重启中断下次查询显 failed 可重试）；异常先置 failed 再抛。生成：全文 ≤60k 直用，超长注入「章节清单 + 开头 50k + 结尾 10k」→ 单调用中文导读五节 H2 骨架（这本书讲什么/全书结构/各部分要点/适合谁/怎么读，与论文线同风格），wrapMaterial 包裹，scene `agent:book-digest`，token 记账；产物 `md/interpretations/book-{id}-digest.md` + `upsertInterpretation('book', …, 'book_digest')`。`bookDigestStatus` / `runBookDigest`（INTERPRET_EXISTS 口径）。
- `ask.ts` owner 联合扩 `{ type: 'book', id }`：注入导读 md || 全文节选 40k，标记 `<<<LIT:B:{id}>>>`（aiChat `LIKE '%<<<LIT:%'` 零改动）；`agent:askLiterature` type 枚举扩 `'book'`。
- `books.ts deleteBook` 级联补：interpretations（book）行删 + 产物 md 删（mdDelete）+ `books/{id}.txt` 缓存删。
- 新 IPC：`agent:bookDigestGet / bookDigestRun`；preload 增 `bookDigest` 命名空间，api.d.ts 同步。

### E3 图书馆入口 UI

- 新建 `src/modules/bookshelf/BookDigestDialog.tsx`：书卡「AI 解读」入口（书卡第三颗悬浮钮 `psychology`，`.bk-menu-digest` right:56px）；状态机弹窗（none=说明+生成 / running=旋转态可关窗 / failed=失败说明+重试 / done=自动打开导读 MdDialog）+ 导读 MdDialog（readOnly，headerAction 重新生成→ConfirmDialog，footerBar 追问 `askLiterature('book', id)`）；`onAgentStatus` book_digest 事件驱动刷新，done 自动开卷（手关不抢开，重生成复位）。入口范围照确认项 #4 全书放开。
- 实现与 spec 的一处对齐修正：导读骨架分节由「一级标题」改为 H2（与论文线导读卡 `# 导读` + 六节 H2 同风格），spec 文档已同步改。

### 待办

- 批次 E 交开发者按 spec §6 验收清单（5 条）在 App 内验证；通过后进批次 F（A1 知识网络：embed 扩实体 + KnowledgeLinksDialog + 相关内容统一）。

## 开发者反馈修订：科普候选迁出信息源（批次 D）

> 创建于 2026-09-23 00:41

开发者反馈「信息源中不需要有科普文章；应放在万象库-科普标签页下」。修订（批次 D spec §8 已加修订注记）：
- `LiteraturePanel.tsx`：发现箱恢复论文单组（discovered 过滤 `source_type !== 'article'`）、AI 科普海选卡与 `runSciCollect` 撤出、`collect_science` 事件监听移除、accept 恢复单参论文文案；literature.css 撤分组标签样式。
- `SciencePanel.tsx`：页签内新增「发现箱」候选区（`discoverList` 过滤 article+discovered，行含来源徽章/语言/日期/推荐理由，接受→discoverAccept 转正入正式列表并自动解读、拒绝、删除二次确认）；`collect_science` 事件同时刷新候选与正式列表。
- `papers.ts pendingDiscoverCount`：只计 `source_type='paper'`（任务中心/总导览「发现箱待终选」计数口径同步，科普候选不再计入）。
- 主进程 discoverAccept 分流、collect_science 海选入发现箱表的机制不变（仅 UI 归属调整）。typecheck + build 全绿。
- **追加（同日反馈二）**：SciencePanel 再照文献页签口径改「发现箱 | 正式文章」双子页签（recycle-tabs 带计数，发现箱默认；两页签各自空态文案），原「候选区叠正式列表」单滚动态作废；science.css 撤分组标签样式、加 `.sci-toolbar`。复验全绿。
- **追加（同日反馈三）**：解读阅读视图页脚折叠——关联词条/相关内容 chips 过多时挤占 80vh 弹窗正文约一半，改为默认收起（动作行内新增「关联与相关（N）/收起关联」开关，expand_more/less 图标），换文章自动复位收起；动作行（解读版/精讲/追问/原文出处/重试抓取/关联知识/删除）保持单行常驻。复验全绿。

## 超级工作台 2.0 二期·批次 F（A1 知识网络）落地

> 创建于 2026-09-23 00:41

依《260922-超级工作台2.0-二期实施计划.md》§5 执行批次 F。开发前先生成 `docs/project/idea/超级工作台2.0/260922-超级工作台2.0-designs-specs-批次F-知识网络.md`（已注明科普候选区迁址修订与本批落点），随后实现，typecheck + build 全绿。零 DB 迁移、零新依赖。git 未动（纪律）。

### F1 索引与相关推荐扩展

- `embedIndex.ts` 重构扩容：新任务 `embed_science`（标题+轻加工/解读产物节选 8k）、`embed_book`（书名+作者+导读节选 8k），均 priority 20/singleton/maxRetries 1，`storeEmbedding('science_article'|'book')`；相关推荐 `searchSimilar` 白名单扩 `['paper','wiki','science_article','book']`，top-3 ≥0.6 写 similarity 链接（`linkRelated` 通用化）；新增 `backfillLearnNodes`（learn_nodes level=2 未删，`title：summary` 输入批量 32），与词条补索引在三个 embed 任务开头各推进一批；EmbedUnavailableError 静默返回不炸。
- 挂载兑现（批次 D 留位）：`interpretScience.scienceRunnerOf` 完成尾入队 `embed_science`；`bookDigestRunner` 完成尾入队 `embed_book`。
- `science.ts`：`resolveEntityTitle`/`entitySearch` 补 `book`（books.title）与 `learn_node`（level=2 未删）两类型。

### F2 手动建链共享弹窗

- 新建 `src/components/KnowledgeLinksDialog.tsx` + `.css`：双向链接列表（类型图标 + `手动`/`相似 N%` 徽章 + 移除二次确认 `links.del`）+「添加关联」（五类型下拉 + 标题即输即搜 `entitySearch` 前 10 条 + 点击 `links.add` manual 直写）；open=false 渲染 null，四处复用。

### F3 相关内容统一 + 跨模块跳转

- 统一口径：`agent:paperDetail.related` 与 `agent:scienceDetail.related` 主进程侧均改 `listLinks` 全量输出（papers.ts `paperRelated` 删除；preload/api.d.ts related 行结构同步 `{link_id, peer_type, peer_id, title, origin, score}`）。
- 新建 `src/services/relatedNav.ts`：`openRelated`（五类实体统一映射深链：paper→feed/literature-paper、science_article→wiki/science-article、wiki→wiki/wiki-entry、book→zangyue/open-digest、learn_node→learn/open-card〔既有靶点〕）+ `relatedIcon`。
- 新增深链靶点：FeedModule `literature-paper`（切文献页签 + 新 prop `openPaperId/onOpenPaperConsumed` 自动开详情）；WikiModule `science-article`（切科普页签开文）与 `wiki-entry`（开词条卡）；BookshelfModule `open-digest`（找书开 BookDigestDialog）。
- 四处详情接入：文献详情（相关内容行新口径可点 + footer「关联知识」）；科普详情（footer「相关内容」chips 区 + 「关联知识」）；书解读弹窗（footer 相关内容 chips + 「关联知识」）；万象词条弹窗（D7 科普过滤升级为全量相关内容行 + 「关联知识」）。知识点行无菜单不加入口（计划兜底口径），仅作链接目标与展示。

### 待办

- 批次 F 交开发者按 spec §4 验收清单（5 条）在 App 内验证；通过后进批次 G 收官发版（版本号 2.0.0 + 文档归档归属定夺 + 日志收官）。

## 我是谁·候选画像条目就地编辑（优化建议区第51轮）

> 创建于 2026-09-23 00:41

依《docs/project/左侧边栏/个人档/archive/2026-09-23-我是谁候选条目就地编辑-{design,plan}.md》实施（brainstorming 一问定稿：行内就地编辑，否掉「加入时弹窗改」）。改动：①`src/shared/types.ts` 导出 `PROFILE_CATEGORIES` 十类清单（原主进程 whoami.ts 与 ProfileModule 各一份拷贝，收敛为单一来源，两处改 import 删本地）；②`electron/services/whoami.ts` 新 `whoamiSuggestionUpdate(id,index,category,content)`——行存在/下标越界/类别须在清单（BAD_CATEGORY）/内容 trim 非空 slice(0,120)（CONTENT_EMPTY）四道校验后原位替换 suggestions JSON 写回；`whoamiResolve` 零改动（加入读的就是存后候选）；③IPC 三件套新通道 `whoami:suggestionUpdate`（ipc.ts handle + preload `whoami.suggestionUpdate` + api.d.ts）；④`WhoamiZone.tsx` 候选行 action 区加铅笔（edit 图标），点击行内切换编辑态：类别 `<select>` 十类 + 内容 input（Enter 保存/Esc 取消/空值禁保存）+ 保存/取消，保存成功经 `patch` 行级回写退出编辑态、失败 toast 保持编辑态不丢字；today/pending 两列表共用 WhoamiItem 自动同享；`WhoamiZone.css` 补 `.whoami-suggestion-editing`（实线+实底区分默认虚线）与 `.whoami-select`。边界：答案「提交即定稿」不变、已入画像仍去画像区改、提炼中/失败态不显铅笔、纯本地操作不涉 LLM。**验证情况**：本轮改动的七个文件 typecheck 零错误、`npm run build` 打包通过；全仓 typecheck 此刻被并行会话批次 D/E 在改的两个文件（`LiteraturePanel.tsx`/`SciencePanel.tsx`，RelatedLink 形态调整中间态）挡住未全绿——该两文件非本轮改动，待其会话收尾后复跑全仓 typecheck 确认。**待冒烟**：改类别/改内容保存后行即时更新、重启后候选保持改后版本、改后加入画像落库为改后内容、取消不落库、today/pending 两处、空内容保存禁用、双主题。git 未动（纪律）。

## 我是谁·「来一问」（260923 开发者指令）

> 创建于 2026-09-23 01:01

会话内指令（非优化建议区条目）：我是谁组件加「来一问」按键，点击让 AI 按当前情况**实时**生成一问加入；明确不走万象库式缓存/预生成。设计/计划见 `docs/project/左侧边栏/个人档/archive/2026-09-23-我是谁来一问-{design,plan}.md`。实施——①`electron/services/whoami.ts` 新 `whoamiAskOne()`：独立单飞标志 `askingOne`（连点抛 BUSY 静默）+ LLM 未配置预检 → 复用 `buildDailyPrompt(1)`（prompt 本就参数化「恰好 N 个」）+ `parseQuestions(…,1)` 取首条（空抛 PARSE_EMPTY）→ INSERT 今日一行返回 view；**点击时刻现读广谱聚合上下文（含 recentAsked(40) 防重复），零缓存零预生成零定时器**；不动每日批次幂等语义与 `WhoamiDailyDate` 标记（边界语义：当日批次未跑时先点来一问=当日视为已供，想要更多继续点，不做来源列区分免 DB 迁移）；②场景标签补挂 `whoami:daily`/`whoami:extract`/`whoami:askOne` 三行（AI 使用表可读性）；③IPC `whoami:askOne` 三件套；④`WhoamiZone.tsx` 正文顶部右对齐 `.whoami-toolbar` 常驻按钮（生成中「思考中…」禁用），成功 `setData` **追加**今日行（新行不走 patch）、失败 toast（未配置专属文案、BUSY 静默）；⑤`.whoami-toolbar` 样式。typecheck 双配置 + build 全绿（含并行会话批次 F 收尾后的合并态）。**待冒烟**：点击出一问入列且可作答入档、连点不并发、每日批次先在时为纯追加、LLM 未配置提示、AI 使用表出现「我是谁·来一问」行、双主题。git 未动（纪律）。

## 超级工作台 2.0 二期·批次 G（收官发版 2.0.0）

> 创建于 2026-09-23 01:27

开发者指令「标记 2.0.0 + 发布安装包上传 GitHub Release + 本地安装包换新」。执行：
- **G1 版本号**：`package.json` 1.1.0 → **2.0.0**；CLAUDE.md 项目性质补记 2.0 收官（超级工作台 2.0 引擎/科普线/书籍解读/知识网络、DB v51）。
- **G2 打包**：`npm run dist` 全绿——`release/` 产出三件 `bugzi-workspace-Setup-2.0.0.exe`（291MB）/ `.exe.blockmap` / `latest.yml`（version: 2.0.0，sha512 就绪）；predist 清目录即完成「本地安装包历史版本换新」（1.1.0 三件已被 2.0.0 三件替换）。
- **发布上传（已完成）**：开发者提交全部改动（收官提交 13c09f1）并推送 tag `v2.0.0`（核对指向 = 远端 main HEAD = 构建源码，260916 先例口径满足）；AI 执行 `gh release create v2.0.0 <三件> --title "v2.0.0（应用版本 2.0.0）" --notes <2.0 收官要点>` 成功——Release 公开发布（非 draft，`releases/latest` 可见，electron-updater 按 latest.yml version 2.0.0 > 1.1.0 判可更新），三件资产字节数与本地逐一核对一致（exe 304,899,741 / blockmap 313,746 / yml 359）。
- **发版验收防自举提醒**：装 1.1.0 的机器走应用内「检查更新」验 2.0.0 升级链路；全新安装直接用 Setup-2.0.0.exe。
- **G3 文档归档**：`idea/超级工作台2.0/` 全套（总纲/一二期计划/批次 A-F specs/萌芽记录）待开发者定夺归属后 AI 执行移出 idea/（建议 `docs/project/全局/archive/`，同步更新文内互引）。
- git 未动（纪律）；「学习/生活双模式」「我是谁来一问」两并行会话改动同在待提交集合内。

## 个人档提纲导航 + 图书馆目录定位当前章（优化建议区第52轮）

> 创建于 2026-09-23 18:39

待完成区两条建议一轮清零，均需求明确未开 brainstorming。①**个人档提纲导航**——`.profile-page` 改左右双栏：新增 `nav.profile-nav` 吸顶提纲列（sticky/124px/surface 底+描边，≤1024px 窄窗隐藏），原内容整体包入 `.profile-main` 列宽不变（总宽 720→872 补偿导航占位）；`PROFILE_NAV` 13 项与 zone 渲染顺序一一对应（个人信息/我的画像/我是谁/App 设置/终端/启动与窗口/数据存储/版本/LLM 配置/Embedding/AI 使用/MCP 配置/超级工作台），各 zone 挂 `profile-zone-*` id（WhoamiZone 与 AgentSettingsSection 两处以裸 div 包裹挂 id）；点击平滑滚动至目标区顶缘（向上找最近滚动容器，偏移 12px），默认折叠的三区（我的画像/AI 使用/我是谁）跳转时先展开再定位（双 rAF 等提交后量位）；滚动高亮——监听主栏 scroll（passive），以「最后一个顶缘越过视口顶 88px 的区」为当前项，`.profile-nav-item.active` 主题色软底（与 `.bk-toc-item.active` 同款语言）。改动 `ProfileModule.tsx` + `App.css` 两文件，纯渲染层零 IPC 零 DB。②**图书馆目录定位**——排查定位：高亮机制本就存在（`.bk-toc-item.active`），真实缺口有二——epub 只在目录顶层匹配 href（当前章为嵌套子项〔epub 目录常态〕时匹配不到、无高亮）+ 打开目录不定位（列表从头渲染，长书要手动翻找）。`ReaderSidebar.tsx` activeLabel 判定改扁平全树三级匹配（锚点级精确〔locate.href 系 260910 锚点解析产物、与目录项 href 同源〕→ 剥锚点文件级 → spineIndex 区间判定 epubChapterAt 兜底），pdf 页码区间判定不变；`bk-sidebar-body` 挂 ref，tab/toc/locate 变化后 rAF 查 `.bk-toc-item.active`，高亮项变化即居中滚动（getBoundingClientRect 差值只滚侧栏内部、不惊动外层），同一条目不重复滚动、不与用户手动翻找目录对抗。typecheck 双配置 + build 通过。**待冒烟**：提纲 13 项齐全、点击跳转与折叠区自动展开、滚动高亮跟随、窄窗隐藏、双主题；epub 嵌套章节高亮、打开目录即居中当前章、翻章时目录跟随、手动翻目录不被抢滚动、pdf 目录回归。CLAUDE.md 与优化建议区归档已同步。git 未动（纪律）。

## 信息源少数派文章图片不显示（问题疑惑区第12轮）

> 创建于 2026-09-23 18:42

开发者报「订阅的少数派文章只有文字、图片无法加载」。取证链：①查库（bugzi.db 只读）——少数派订阅实为 RSSHub 镜像源 `hub.slarker.me/sspai/index`，feed 全文自带 11 个 `<img>`（`content_feed_html` 5~22KB，超 `FULL_TEXT_MIN=500` 故 readability 网页抓取不触发、`content_fetched_html` 全 NULL，阅读视图渲染的就是 feed 全文 HTML）；图片标签全部带 `referrerpolicy="no-referrer"`（RSSHub 输出惯例）。②DOMPurify 3.4.14 实测保留 `referrerpolicy` 与 `src`，属性原样进 DOM。③curl 多轮实测 `cdnfile.sspai.com` 图片：**无 Referer 一律 403 Forbidden，任何非空 Referer（连 localhost）都 200**——「空 Referer 防盗链」。④Chromium 按 no-referrer 策略对图片请求不发 Referer → 403；打包态页面源 file:// 本就不发 Referer，双重必挂。修复（`electron/main.ts` 最小增量）：`app.whenReady()` 内 defaultSession 挂 `webRequest.onBeforeSendHeaders`（url 过滤 `cdnfile.sspai.com`/`cdn.sspai.com`），无 Referer 请求统一补 `Referer: https://sspai.com/`——网络层注入，dev/prod 双端生效（仅删属性救不了打包态故不采用）；该 Referer 为 curl 实测 200 放行值。typecheck 双配置通过。注：live Electron 四象限验证 harness 因本会话无法启动 GUI 进程未跑通（electron.exe 秒退不执行脚本），根因各环已独立实证、修复为该类 CDN 标准做法，**待开发者重启 dev 目验**：打开任一少数派文章正文配图应正常显示（已读文章同样生效——图片为请求时实时加载，无需重新拉取）。临时验证文件已清理。问题疑惑区已归档第12轮。git 未动（纪律）。

## 左侧边栏文档目录按常驻/学习/生活三类重组（开发者指令）

> 创建于 2026-09-23 19:47

开发者指令「按常驻模块、学习模块、生活模块三类重新组合 `docs/project/左侧边栏/`」。两项开放决策经开发者选择：遗留文件夹**合并进承接模块**（非原样保留）；工作台**建档并本次一并迁出 idea/**。执行——①`docs/project/左侧边栏/` 下 16 个现行模块文件夹按 App.tsx `MODULES[].mode` 归入三类子目录：`常驻模块/`（总导览、音乐吧、回收站、个人档）、`学习模块/`（工作台〔新建〕、学习库、万象库、文笔坊、信息源）、`生活模块/`（致知己、灵感泉、图书馆、推理角、记账本〔原「账本」夹改名〕）；`左侧边栏.md` 留目录根。②遗留 8 夹合并：个人中心→个人档（design/specs/AI调用记录文档入根，archive 并入）、白噪音→音乐吧（design/specs 与 260908/260911 文档入根）、辩真阁→万象库/archive/辩真阁、格言库→文笔坊/archive/格言库、藏阅阁 archive 两件→图书馆/archive/ 根、收藏夹/书架→图书馆/archive/{收藏夹,书架}（整夹保分组防同名冲突）；书架夹内 `design-收编稿-待审.md` 随夹入档（260910 旧物，冒烟如仍需审可移回）。③工作台：新建 `学习模块/工作台/`，`idea/超级工作台2.0/` 全套 10 件（总纲 design、批次 A-F specs、一二期实施计划、萌芽想法记录）移入其 `archive/`（2.0.0 已发版收官=落地，归档铁律「落地必须移出 idea/」，原「待开发者定夺归属」就此定夺），idea/ 空夹已删。④路径引用全库更新：69 个活动文档（优化建议区/新功能开发区、docs/study/README、模块 specs 与随迁 plan/design 内互引）旧路径 `左侧边栏/<模块>/` 批量替换为三类新路径，docs/log/ 历史日志按机制不改，替换后残留检查为零。⑤机制正本 `docs/project/README.md` 存放位置表改三类结构并附模块归属清单；CLAUDE.md 权责表两行改 `<分类>` 层级、spec/plan 存放条同步、项目性质行 2.0 specs 指向改新址。⑥同日开发者指令同步更新仓库根 `README.md`：状态行 v1.1.0→v2.0.0 收官、功能模块表补超级工作台行与万象库科普页签 / 图书馆书籍解读 / 个人档我是谁·提纲导航、左栏双模式过滤表述、AI 助手「频道制」改「统一会话流」、SQLite 46→51 版、文档体系表路径改三类结构并附分组与归档说明、项目结构 electron/src 补 2.0 引擎与任务中心、使用说明补双模式切换。git 未动（纪律）。
