# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目性质

本项目是「bugzi's workspace」——「生活化个人专属工作台」桌面端 App 的 AI coding 项目。技术栈 Electron 44 + React 19 + TypeScript，electron-vite 构建，本地单机运行。**v1.0 代码已全部生成**（260902，基于 7 份 designs-specs.md；后续新增致知己、推理角、文笔坊三模块与右侧边栏 debugzi、草稿本；260908 新增书架、信息源、账本三模块与白噪音全局背景音（非模块），同日辩真阁并入万象库、三模块改名（藏书架/记账本/个人档）、左栏顺序重排，specs 现共 16 份）：主进程在 `electron/`（DB/IPC/LLM/MCP/定时器/回收站/书架/信息源/账本服务），渲染层在 `src/`（主题/三栏布局/全局组件/十一模块 + 白噪音引擎与场景注册表 `src/services/`）。常用命令：`npm install`（`.npmrc` 已配 legacy-peer-deps + 国内镜像，直接裸装即可）、`npm run dev`（开发启动）、`npm run typecheck`（双 tsconfig 类型检查）、`npm run build`。数据存储：结构化数据存 SQLite（node:sqlite，用户数据目录 bugzi.db），md 文档（格言笔记/灵感文档/万象卡片/辩真记录/致知己答案版本/推理角对局记录与每日题详情/文笔坊浮生记与写作台文章）存真实 .md 文件，均在用户数据目录下；头像/背景图经 bzres:// 协议加载。

## 文档体系与维护权责（最重要）

`docs/` 目录是开发者与 AI 共同维护的项目文档库，权责划分如下：


| 内容                                                    | 维护方            | AI 的角色                                                                            |
| --------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------- |
| `docs/project/{左侧边栏,右侧边栏}/<模块>/design.md` | 开发者主导        | 可提修改建议，或者在开发者要求下帮忙完善（必须由开发者审核通过才能修改）不得擅自改写；右侧边栏/debugzi、草稿本两份为 260907 应开发者指令由 AI 依现状整理创建 |
| `docs/project/{左侧边栏,右侧边栏}/<模块>/designs-specs.md` | **AI 生成并维护** | 基于 design.md 严格生成的 AI coding prompt 文档，是开发的直接依据；开发者仅简单审核  |
| `docs/project/样式/design.md`                           | 开发者            | 同上，仅可建议                                                                       |
| `docs/log/<YYMMDD>.md`（如 260902.md）                  | AI                | 每天一个开发日志，按日期命名，由 AI 记录当天开发情况；**每个小节标题下必须附创建时间行**（格式 `> 创建于 YYYY-MM-DD HH:MM`，精确到分钟，写入时取实际时间——260907 开发者指令，便于核验） |
| `docs/project/idea/`                                    | **开发者**        | AI coding 时**不需要关注、不得修改**此目录（例外：AI 产出的 spec/plan 按上表规则存放于此，AI 只动自己产出的文件） |
| `docs/project/project.md`                              | 开发者主导        | 项目总介绍与文档协作权责总则；AI 可提建议，或在开发者要求下协助完善（需审核通过） |
| `docs/project/左侧边栏/左侧边栏.md`                     | 开发者主导        | 左栏跨模块全局硬性规则（原总需求文档对应内容）；AI 可提建议，或在开发者要求下协助完善（需审核通过） |
| `docs/project/右侧边栏/右侧边栏.md`                     | 开发者主导        | 右侧边栏总说明（260907 应开发者指令由 AI 依现状整理创建）；AI 可提建议，协助完善需审核通过 |
| `docs/project/临时草稿（待写入).md`                     | **开发者**        | 开发者写需求与对齐用的草稿本；**AI 不需要读取**                                     |
| `docs/project/` 内各模块目录的 spec/plan 文档与 `docs/project/右侧边栏/` | **AI**            | brainstorming / 写计划会话产出的设计记录与实施计划，按模块就地存放：idea 阶段 → `idea/<模块>/`、已立项 → `左侧边栏/<模块>/`、右侧边栏相关 → `右侧边栏/`；落地或废弃后移入所属目录 `archive/` 子目录，机制正本见 `docs/project/README.md` |
| `docs/project/优化建议区.md`                          | **开发者**        | 开发者记录使用中遇到的优化建议；AI 据此实施优化（实施后移入「归档区」）           |
| `docs/project/问题疑惑区.md`                          | **开发者**        | 开发者记录问题疑惑处；**AI 不要主动查阅**，开发者指明时才阅读并答疑               |

工作流程：开发者写 design.md → AI 据此生成 designs-specs.md → AI 按 designs-specs.md 开发。designs-specs.md 现共 16 份：260902 生成 7 份（六模块 + 样式），260905（致知己）、260906（推理角）、260907（文笔坊）、260908（书架、信息源、账本、白噪音）随模块新增，另有右侧边栏 debugzi、草稿本两份；v1.0 代码已于 260902 全部生成（typecheck/build 通过），后续模块代码随各自 specs 生成。

## Git 操作纪律（开发者指令）

- **禁止 AI 自主执行任何 git 操作**（add / commit / push / pull / checkout / reset 等全部命令）。
- 所有 git 操作由开发者自主完成；AI 最多在开发者明确要求时给出命令建议，不得代为执行。

## 全局硬性规则（来自 左侧边栏/左侧边栏.md 与 样式/design.md）

- 所有「放入回收站/删除」操作必须有二次确认弹窗，确认后才生效。
- 双主题：浅色 = 樱花粉 + 黑字；深色 = 宝蓝 + 白字。背景使用开发者上传的 bg-light / bg-dark 图片。
- 禁止使用 emoji 作图标；需要图标时从 https://fonts.google.com/icons 获取。
- 组件框、按键、弹窗等禁止彩亮颜色，须使用与主题色相近的色系，保持整体风格一致。
- 三栏式布局：左侧边栏（图标+模块名，260908 重排后顺序：格言库→万象库→藏书架→信息源→文笔坊→致知己→灵感泉→推理角→记账本→白噪音→回收站→个人档；白噪音为非模块的全局功能入口）→ 中间主栏（当前模块内容）→ 右侧边栏（AI 助手，可收起/展开，按场景分频道：助手/万象·问答/致知己·追问/辩真·核查，每频道独立会话历史持久保存）。
- md 弹窗为全局统一组件（格言笔记/万象卡片/灵感文档/辩真记录详情/致知己答案版本/推理角对局记录与每日题详情/文笔坊浮生记与写作台文章共用）：默认渲染态，双击切换编辑态。
- LLM/MCP 未配置时 AI 功能按钮不置灰，点击弹窗提示并「去配置」直达个人中心；定时任务在 App 未运行时错过即跳过；首次启动有欢迎引导（用户名+主题+LLM 提示）。

## 应用架构（规划）

无需账号登录注册。十一个功能模块 + 白噪音全局功能（260908 辩真阁并入万象库为「辩真」板块）：

- **格言库**：名言格言三级管理（草稿区/沉淀区/正式区）+ 回收站流转。AI 依据正式区格言风格在草稿区生成格言（每次 10 条、每条标明出处），定时（暂定每晚 10 点）或手动「来10条格言」触发。正式区每条格言有独立 md 笔记文档，弹窗打开、实时编辑实时渲染。
- **万象库**：非计算机领域知识词条（初始板块：经济学/法学/心理学/博弈论/历史神话，可自定义）。词条点击弹窗显示详情，支持划词高光（存入笔记本板块）和划词问 AI（在右侧 AI 助手交互）。支持随机或手动输入词条由 AI 生成知识卡片。**双板块**：顶部「百科 | 辩真」tab——辩真板块（260908 原辩真阁并入）：输入观点，AI 联网搜索（需接 MCP 配置）验证真实性并保存验证记录，过程推「辩真·核查」频道。
- **灵感泉**：项目灵感四层管理（草稿区/立项区/开发区/归档区），列表行组件 + 专属 md 文档（弹窗打开，双击编辑）。各单位之间可自由拖拽跨区移动。
- **致知己**：自我认知沉淀——「一个问题 + 我的多个答案版本」（保存即版本，AI 只追问检验不代笔）；配套个人档「我的画像」（注入全部 AI 功能上下文）与 AI 边栏频道制。
- **推理角**：推理健身房，双板块——海龟汤（AI 出题/裁判/局终点评，问答留档复盘）+ 思维墙（每日一题打卡连胜、题库、练习场）；260909 优化——「精选题库」改名「题库」并收入练习场生成题（生成即入 wall_bank todo 态、判答回写终态与详情 md、弃做留待回补），三处题面（每日一题/题库/练习场）由纯文本改 MdView 渲染；260909 题库预生成落地（v1.3 设计顺延实施）——新表 wall_pool（DB v26）+ 补充泵 reasoningStock（启动/消耗后/进模块三触发，汤库 fresh 与题池低于 5 后台补到 10、静默失败），每日一题/练习场取题池优先秒开（同事务转正/入题库）、池空才兜底现场生成，汤库列表随 `reasoning:stockChanged` 渐进刷新。
- **文笔坊**：写作生活双板块——浮生记（周记/月记/随心记自由记录，零 AI 纯私密）+ 写作台（博客文章四区流转：构思/写作/完稿/已发布，Copilot 式 AI 协笔——起稿/续写/润色/改写以建议卡呈现、逐段采纳）。
- **藏书架**（260908 由「书架」更名）：本地电子书阅读——epub/pdf 导入复制入库、内置阅读器（epub.js + pdfjs 双引擎、主栏内切换）、进度记忆（epub CFI / pdf 页码，节流 3 秒落库）；260909 优化第 1 轮——双阅读模式（滚动/翻页全局记忆，切换全量重挂）、WASD/方向键导航（滚动模式 W/S 按住流式滚动）、目录侧栏（epub navigation / pdf outline，当前章高亮）、epub 划词笔记（高光/批注，CFI 定位存 book_notes 表 DB v24，主题色标注渲染、笔记页签跳回、删除二次确认）、划词/高光主题色；纯本地零 AI；删书二次确认直接彻底删（连笔记），不入回收站。
- **信息源**：精简版 Folo——RSS 订阅聚合（初始预置三源、可随时添加，进模块自动拉 + 手动刷新）、AI 总结打开时按需生成并缓存（LLM 未配置按全局规则）、内置阅读视图（RSS 全文优先 + readability 抓正文兜底，正文喂总结与阅读两用）、基础已读/未读；删除不入回收站。
- **记账本**（260908 由「账本」更名）：轻量记账——逐笔记流水（金额+分类+账户+备注），多账户（预设现金/微信/支付宝/银行卡）余额实时滚存（期初+收支，金额存分）；单主视图：总资产/当月收支数字卡 + 月份切换 + 支出分类占比条（点击下钻筛选）+ 按日分组流水列表；账户/分类管理弹窗（可设期初余额）；纯本地零 AI；删除入回收站。
- **回收站**：按来源分八块（格言库/万象库〔含辩真〕/灵感泉/致知己/推理角/草稿本/文笔坊/记账本，260908 辩真并入万象库后九块变八块），条目有「恢复」（回来源模块最初级区）和「删除」（彻底删除）两个操作，3 天后自动彻底删除（启动+每日零点检查）。
- **个人档**（260908 由「个人中心」更名）：个人信息（用户名/头像/个性签名）+ App 设置（字体大小/样式/粗细、LLM 配置、MCP 配置）。
- **白噪音**（非模块的全局功能）：全局背景音——Web Audio 纯合成多场景混音（首版仅「雨」8 层），切模块声音不断；入口为左栏底部常驻控件，**整块单击进主栏混音器页**（不进模块列表，播放态图标主题色高亮提示，快捷播放/暂停在页内——260908 实施后修订，原双区设计作废）；出厂预设 + 自定义混音保存；重启记参数默认暂停；纯渲染层零主进程改动（settings 存 JSON）；「主题」按钮居右栏底部右下角（自左栏底部迁来）。
